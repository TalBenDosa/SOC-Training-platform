# נוהל תגובה לאירוע אבטחה ודיווח הפרה — HACK THE SOC

מסמך זה סוגר את ממצא #5 בדוח ה-AppSec: היעדר מנגנון מוגדר לזיהוי "אירוע אבטחה
חמור" ולתמיכה בחובת הדיווח. הוא מותאם לפלטפורמה קטנה (Next.js + Supabase על Vercel)
ולדרישות תקנות הגנת הפרטיות (אבטחת מידע) התשע"ז-2017 ותיקון 13 לחוק הגנת הפרטיות
(בתוקף אוגוסט 2025).

> ⚠️ אינו ייעוץ משפטי. פרשנות "אירוע חמור" וחובת הדיווח בפועל — לאמת מול הנוסח
> הרשמי ומול יועץ משפטי.

---

## 1. מה נחשב "אירוע אבטחה" בפלטפורמה

כל אחד מאלה, לכל הפחות:

- גישה בלתי-מורשית לחשבון משתמש או לטבלת `profiles` / `user_progress`.
- שינוי הרשאה לא-מורשה (למשל חשבון שהפך ל-`role='admin'` שלא דרך הענקה מכוונת).
- דליפה או חשיפה של כתובות אימייל / handles של משתמשים.
- שימוש לרעה במפתחות: חשיפת `SUPABASE_SERVICE_ROLE_KEY`, `ANTHROPIC_API_KEY`,
  `OPENAI_API_KEY`, או כל סוד אחר.
- ניצול פגיעות בתלות (למשל Next.js) שאיפשר RCE/SSRF/דליפה.
- שיבוש/מחיקה של רשומות ה-`audit_log`.

**"אירוע חמור"** (המדרג את חובת הדיווח) — אירוע שבו מידע אישי של נושאי מידע
נחשף/הועתק/שונה בפועל, או שקיים חשש ממשי לכך.

---

## 2. אותות זיהוי (Detection) — היכן מסתכלים

| מקור | מה מחפשים |
|------|-----------|
| **`public.audit_log`** (נכתב ע"י `src/lib/audit/logAudit.ts`) | פעולות admin חריגות; רצף `*.denied` (משתמש מאומת שמנסה להגיע ל-endpoint של admin — אות לחיפוש הסלמה) |
| **Supabase → Auth logs** | ריבוי כשלי התחברות, התחברויות ממיקומים חריגים, איפוסי סיסמה חריגים |
| **Supabase → Postgres logs / Database → Roles** | חשבונות עם `role='admin'` שלא אמורים להיות; שינויי סכימה לא מוכרים |
| **Vercel → Logs / Firewall** | תעבורה חריגה ל-`/api/*`, ריבוי 429 (ניסיון מיצוי תקציב), 5xx חריגים |
| **`npm audit` / Dependabot** | פגיעויות חדשות בתלויות (ראה ממצא #1) |

**בדיקה תקופתית מומלצת (חודשית):** הרצת שאילתה על `audit_log` לאיתור `*.denied`
ופעולות admin לא-צפויות; סקירת Supabase Auth logs; `npm audit`.

```sql
-- ניסיונות גישה שנדחו + פעולות admin ב-30 הימים האחרונים
select action, actor_id, created_at, metadata
from public.audit_log
where created_at > now() - interval '30 days'
  and (action like '%.denied' or action like 'admin.%')
order by created_at desc;
```

---

## 3. תגובה (מיד עם זיהוי חשד)

1. **הכלה (Contain).** אם דלף מפתח — בטל אותו מיד (rotate) בקונסולת הספק
   (Supabase / Anthropic / OpenAI / Vercel env). אם חשבון נפרץ — השבת/אפס סיסמה.
2. **תיעוד (Preserve).** שמור עותק של `audit_log`, Supabase Auth logs, ו-Vercel
   logs הרלוונטיים **לפני** שהם מתגלגלים (retention מוגבל). רשום ציר זמן.
3. **הערכה (Assess).** מה נחשף? כמה נושאי מידע? האם מידע אישי בפועל? → קובע אם
   "חמור" ואם קמה חובת דיווח.
4. **תיקון (Eradicate/Recover).** סגור את שורש הבעיה (עדכון תלות, סגירת RLS,
   ביטול הרשאה), ודא שאין persistence.

---

## 4. חובת דיווח (אם האירוע "חמור")

תחת תיקון 13, אירוע אבטחה חמור מקים חובת דיווח **לרשות להגנת הפרטיות**, ובמקרים
מסוימים חובת יידוע לנושאי המידע עצמם.

- **בצע:** דווח לרשות להגנת הפרטיות בהתאם לנוהל ולמועדים הקבועים בדין (יש לאמת את
  לוח הזמנים המדויק מול הנוסח/היועץ המשפטי — אל תסתמך על זיכרון).
- **תעד:** מה קרה, מתי התגלה, מה היקף המידע, אילו צעדי הכלה ננקטו, וכיצד ימנע בעתיד.
- **שקול יידוע נושאי מידע:** כשקיים סיכון לפגיעה בהם.

> אי-דיווח כשהיה נדרש הוא כשלעצמו חשיפה לעיצום כספי תחת תיקון 13.

---

## 5. אחריות

- **בעל הפלטפורמה (Tal)** — מנהל האירוע כברירת מחדל; מבצע rotation, הערכה, ודיווח.
- אין כרגע צוות תורנות; לפלטפורמה בהיקף הנוכחי זה סביר. עם צמיחה (רמת אבטחה
  בינונית/גבוהה) — יש למנות ממונה אבטחת מידע ולהגדיר SLA לתגובה.

---

## 6. מוכנות מוקדמת (Checklist)

- [x] `audit_log` נעול ונכתב (migration 0007 + `logAudit.ts`).
- [ ] הגדרת התראה/סקירה חודשית של `audit_log` ו-Auth logs.
- [ ] אימות מדיניות שמירת לוגים (retention) ב-Supabase ו-Vercel.
- [ ] אימות מול יועץ משפטי של לוח הזמנים המדויק לדיווח תחת תיקון 13.
- [ ] מסמך פרטים ליצירת קשר עם הרשות להגנת הפרטיות, מוכן מראש.
