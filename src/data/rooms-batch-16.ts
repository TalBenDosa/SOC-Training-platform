/**
 * Rooms Batch 16 — Cloud parity
 * - azure-security — Azure IaaS security for SOC analysts (parity with the
 *   existing aws-security and gcp-security rooms).
 */
import r1 from "@/data/rooms-batch-16-r1";

export default [...r1];
