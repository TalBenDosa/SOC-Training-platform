/**
 * Rooms Batch 15 — Alignment Audit Gap-Fill
 *
 * 1 room added to close a confirmed practiced-without-theory gap: the
 * platform's dashboard/scenario library includes a full Kubernetes Pod
 * Escape → Cloud Metadata Theft attack scenario (APT40, T1610/T1611) and
 * k8s_pod_create/k8s_exec/k8s_rbac telemetry events, but no room ever taught
 * container/Kubernetes security fundamentals before this gap-fill.
 * - kubernetes-container-security — privileged pods, hostPID/hostNetwork/hostPath,
 *   RBAC over-permissioning, and the container-to-node-to-cloud-metadata escalation chain
 */

import r1 from "@/data/rooms-batch-15-r1";

export default [...r1];
