// Learning Path lessons — Foundations module M
// Cryptography fundamentals. Added after an explanation-quality audit found that
// hashes appear in 25 of 34 lessons, and TLS certificates, DKIM signatures and
// Kerberos RC4-vs-AES all lean on concepts no lesson ever taught.
const lessons = [
  {
    "id": "local-lesson-36",
    "slug": "hashing-encryption-and-certificates-for-analysts",
    "title": "Hashing, Encryption, and Certificates: The Maths You Actually Meet in the SOC",
    "topic": "Cryptography Fundamentals",
    "difficulty": "beginner",
    "kind": "lesson",
    "intro": "You are going to meet cryptography on your very first shift, and you are going to meet it constantly. An EDR alert hands you a SHA-256 hash and asks whether the file is known-bad. A phishing investigation turns on whether a DKIM signature validated. A Kerberos event shows an encryption type of 0x17 and a hunting rule says that matters. A user asks whether a site is safe \"because it has the padlock.\" Every one of those is a cryptography question wearing a SOC uniform.\n\nThis lesson is not a maths course. You will not calculate anything, and you will never be asked to implement an algorithm. What you need is a working mental model — precise enough that you draw the right conclusion from a log field, and honest enough that you know when a field proves less than it appears to.\n\nThree ideas carry almost all of the weight. **Hashing** produces a fingerprint that identifies data without containing it. **Encryption** scrambles data so that only a key-holder can read it back. **Certificates** bind a public key to an identity so that strangers can trust each other. Get these three straight and a surprising amount of the job stops being mysterious.\n\nTwo of the most common beginner mistakes in security are treating hashing and encryption as the same thing, and treating a padlock icon as proof of honesty. Both mistakes lead to confidently wrong analysis. By the end of this lesson you will not make either.",
    "sections": [
      {
        "heading": "A Hash Is a Fingerprint, Not a Container",
        "content": "### From Zero: Four Words You Must Not Confuse\n\nBefore going deeper, lock these four ideas into separate boxes using one everyday scene: a sealed letter. **Hashing** is like taking a photograph of the letter's wax seal — a unique fingerprint of exactly what is inside, with no key involved and no way to reconstruct the letter from the photo. **Encoding** is like writing the letter in Morse code — anyone who knows the (public, non-secret) Morse alphabet can translate it straight back; nothing is hidden, only reformatted. **Encryption** is like locking the letter in a box: `symmetric` encryption uses one shared key that both locks and unlocks it, while `asymmetric` encryption uses a matched pair of keys — a public one anyone can use to lock the box, and a private one only the intended recipient holds to unlock it. A **certificate**, finally, is like a notary's stamp on the box: it hides nothing, it is a signed statement from a trusted third party (a Certificate Authority) that says \"this public key genuinely belongs to this named sender.\" Hold these four apart — fingerprint, translation, locked box, notary stamp — and every field in this lesson slots neatly into one of them. We start with the fingerprint.\n\nA hash function takes an input of any size — a one-line text file, a 4 GB disk image, an entire database — and produces a short, fixed-length string called a **digest**. SHA-256 always produces exactly 64 hexadecimal characters, whether you fed it one byte or one terabyte.\n\nThat fixed size tells you something important about what a hash is *not*. Your 4 GB file cannot possibly be stored inside 64 characters. The hash does not contain the file. It only identifies it, the way a fingerprint identifies a person without being a person.\n\nThree properties make this useful, and each one shows up in your daily work:\n\n- **Deterministic** — the same input always produces the same digest, on any machine, in any country, forever.\n- **One-way** — given a digest, there is no practical way to work backwards and recover the original input.\n- **Avalanche** — change a single bit of the input and the digest changes completely, not slightly.\n\nThe avalanche property is the one beginners underestimate. Two files differing by one character do not produce two similar hashes. They produce two hashes with no visible relationship at all. This means you can never eyeball two digests and judge that the files are \"close\" — they either match exactly or they tell you nothing.\n\n### Why this is so useful to an analyst\n\nBecause hashing is deterministic and one-way, a digest becomes a safe, portable name for a file. You can send a hash to a colleague, paste it into a threat-intelligence lookup, or check it against a blocklist without ever moving the file itself. That matters when the file is malware you would rather not email, or a document containing customer data you are not permitted to share.\n\nIt also gives you integrity checking. Hash a file today, hash it again next month, and if the digests differ then something changed the file — even if the size and timestamp look untouched.\n\n### The algorithms you will see, and which ones are broken\n\nYou will meet three names constantly, and you can tell them apart by digest length alone:\n\n- **MD5** — 32 hex characters. Cryptographically broken; attackers can deliberately create two different files with the same MD5.\n- **SHA-1** — 40 hex characters. Also broken, demonstrated publicly in 2017 with two different PDFs sharing one digest.\n- **SHA-256** — 64 hex characters. The current standard, and what you should prefer whenever you have a choice.\n\n\"Broken\" here has a specific meaning worth getting right. It does not mean MD5 is useless or that you should refuse to work with it — your logs will be full of MD5 hashes for years to come, and using one to look up a known sample is perfectly reasonable. It means MD5 cannot be trusted as *proof* of identity when an adversary controls the file, because a motivated attacker can engineer a collision. Use it as a lookup key; do not use it as evidence that two files are the same.",
        "image": {
          "src": "/lesson-images/crypto/hashing-vs-encryption.svg",
          "alt": "Side-by-side comparison of hashing and encryption. On the left, a file passes through SHA-256 to produce a fixed 64-character digest, a one-byte change produces a completely different digest, and a broken arrow shows the process cannot be reversed. On the right, plaintext passes through AES with a key to produce ciphertext, and the same key reverses it back to the original text.",
          "caption": "Hashing identifies and cannot be undone; encryption conceals and is designed to be undone by a key-holder.",
          "credit": "Figure authored for this course."
        },
        "codeExample": "// The avalanche property, shown with one changed character.\n// Input A:\n\"Transfer approved for account 4471\"\nSHA-256 -> 8f14e45fceea167a5a36dedd4bea2543b9d1b0f2c9e7a3d5c1f0e8b7a6d4c2e1\n\n// Input B: the SAME string, with the final \"1\" changed to a \"2\"\n\"Transfer approved for account 4472\"\nSHA-256 -> c9f0f895fb98ab9159f51fd0297e236d4a7b2c8e1f6d3a0b5c9e2d7f4a1b8c6d\n\n// Note what did NOT happen: the digests are not \"similar\".\n// One character in changed essentially every character out.\n// This is why you can only ever test hashes for EXACT equality."
      },
      {
        "heading": "Why a Hash Is the Weakest Thing You Can Hunt On",
        "content": "Everything above makes hashes sound powerful, so here is the limitation that matters most operationally. A file hash identifies one exact build of one exact file — and the attacker controls the file.\n\nRecompile the malware with a comment added. Pad it with a few junk bytes. Change one string in the configuration. The behaviour is identical, the campaign is identical, the operator is identical, and the hash is completely different. Your carefully maintained blocklist does not fire.\n\nThis is the idea behind the **Pyramid of Pain**, a model that ranks indicators by how much it costs the attacker when you block them. Hashes sit at the very bottom, because changing one costs the attacker seconds of effort. As you climb — IP addresses, domains, network artefacts, tools — each rung costs the adversary more. At the top sit **TTPs**, the tactics, techniques and procedures that describe how they actually operate, which cannot be changed without changing how they work.\n\nThe practical lesson is not that hashes are worthless. They are excellent for what they are: fast, unambiguous, zero-false-positive identification of a *known* sample. A hash match is one of the few signals in security that is essentially never a false positive.\n\nThe lesson is about expectations. A hash match confirms; it does not hunt. If your entire detection strategy is a list of known-bad hashes, you will catch yesterday's malware and miss today's. This is precisely why EDR products moved toward behavioural detection, and why the threat-hunting discipline exists at all."
      },
      {
        "heading": "Encryption Is the Opposite: Built to Be Reversed",
        "content": "Where hashing is deliberately one-way, encryption is deliberately two-way. The entire purpose is that the right person can get the original data back. Encryption transforms readable **plaintext** into unreadable **ciphertext**, and a **key** transforms it back.\n\nOne principle underpins all of it: the secrecy lives in the key, never in the algorithm. AES is published, studied, and attacked worldwide by everyone who cares to. That openness is a feature — an algorithm nobody is allowed to examine is an algorithm nobody has verified. If a vendor ever tells you their encryption is safe because the method is proprietary and secret, that is a warning sign, not a selling point.\n\n### Symmetric and asymmetric\n\nThere are two families, and they solve different problems:\n\n- **Symmetric** — one shared key both encrypts and decrypts. AES is the standard here. It is fast, so it does the heavy lifting on bulk data. The hard part is getting that shared key to the other party without anyone intercepting it.\n- **Asymmetric** — a matched pair of keys, one public and one private. What the public key locks, only the private key opens. RSA and elliptic-curve algorithms live here. It is slow, so it is used for small things: agreeing on a session key, and signing.\n\nThink of the public key as a post box slot on the street. Anyone walking past can drop a letter in, and you publish the address deliberately. Only you hold the key that opens the box and retrieves the mail. Publishing the slot costs you nothing, because dropping mail in and taking mail out are genuinely different capabilities.\n\nReal systems use both together. When your browser opens an HTTPS connection, it uses asymmetric cryptography briefly to agree on a shared secret with the server, then switches to fast symmetric encryption for the actual traffic. You get the key-exchange benefit of one and the speed of the other.\n\n### Signing: the same maths, run backwards\n\nReverse the direction and asymmetric cryptography does something else entirely. If I encrypt something with my *private* key, anyone with my public key can decrypt it — which is useless for secrecy but perfect for proof. Only the holder of the private key could have produced it. That is a **digital signature**, and it is how DKIM proves an email really came from a domain, and how certificates prove a server is who it claims.\n\n### The trap: encoding is not encryption\n\nThis one catches nearly every beginner, so learn it now. **Base64** is encoding, not encryption. It rearranges data into a safe character set for transport. There is no key. Anyone can decode it instantly.\n\nWhen you find a long base64 blob in a PowerShell command line, you have not found something protected. You have found something *obscured*, and your correct next move is to decode it and read what it says. Attackers rely on analysts assuming that unreadable means inaccessible.\n\n### What encryption does not hide\n\nEncryption protects content, not the fact that a conversation happened. Even on a fully encrypted connection, your network logs still record who talked to whom, when, for how long, and how many bytes moved. That metadata is often enough to detect beaconing, exfiltration, or a connection to a known-bad destination — which is why network detection still works in an HTTPS world.",
        "codeExample": "// ENCODING vs ENCRYPTION — a distinction that changes your next action.\n\n// Base64 ENCODED PowerShell, as it appears in a real command line:\npowershell.exe -nop -w hidden -enc <long base64-encoded blob>...\n\n// There is NO key involved. Decode it during triage:\n//   base64 -d   (Linux)   |   [Convert]::FromBase64String()  (PowerShell)\n// -> the decoded text reveals a download-and-execute stager: it fetches a\n//    remote payload over HTTP and runs it in memory. (Shown defanged here.)\n// You just read the attacker's intent. Obscured, not protected.\n\n// ENCRYPTED content, by contrast, looks similar but is NOT recoverable\n// without the key — no amount of decoding will produce readable text.\n\n// The analyst habit: when you see an unreadable blob, ask \"encoded or\n// encrypted?\" If encoded, decode it now. If encrypted, pivot to metadata\n// instead — who, when, how much, to where."
      },
      {
        "heading": "Certificates: How Strangers Decide to Trust Each Other",
        "content": "Public-key cryptography has one unsolved problem. If a server hands you a public key and claims to be your bank, how do you know the key belongs to the bank and not to someone sitting between you? Certificates exist to answer exactly that.\n\nA **certificate** is a small file that binds together three things: an identity (a domain name), a public key, and a signature from a **Certificate Authority** that vouches for the binding. Your operating system and browser ship with a built-in list of root CAs they trust, and everything else chains back to one of them.\n\nWhen your browser opens an HTTPS connection, it runs a short series of checks before it shows the padlock:\n\n- Does the name on the certificate match the site actually being visited?\n- Is the certificate within its validity dates, and not expired?\n- Does the signature chain back to a root CA in the local trust store?\n- Has the certificate been revoked since it was issued?\n\nIf all four pass, you get the padlock. Notice what is on that list — and, more importantly, what is not.\n\n### What the padlock actually proves\n\nThe padlock proves the connection is encrypted and the certificate matches the domain you are visiting. It proves *nothing* about whether the people running that domain are honest.\n\nBasic domain-validated certificates are free and issued automatically within seconds. Anyone who can prove they control a domain — including an attacker who registered it twenty minutes ago — gets one. The result is that essentially every phishing site now has a valid certificate and a padlock.\n\nThis is worth saying plainly to users who ask, because the padlock was over-sold to the public for years. \"It is secure\" and \"it is trustworthy\" are different claims, and the certificate only supports the first one.\n\n### The certificate fields you will meet in logs\n\nProxy, firewall, and network sensor logs routinely record certificate details, and a few fields do real investigative work:\n\n- **Issuer** — who signed it. A self-signed certificate (issuer equals subject) means nobody independent vouched for it.\n- **Subject / Common Name and SAN** — which names the certificate claims to cover.\n- **Validity dates** — a certificate created hours before you saw it is worth a second look.\n- **Fingerprint** — a hash of the certificate itself, useful for tracking one exact certificate across sightings.\n- **JA3 / JA3S** — a fingerprint of *how* the client and server negotiated the handshake, not of the content. Because different software negotiates differently, this can identify the client program even when everything it sends is encrypted.\n\n### The false positives you must expect\n\nSelf-signed certificates get flagged as suspicious constantly, and they genuinely are common in attacker infrastructure. They are also completely normal on internal appliances, printers, management interfaces, lab servers, and development environments — none of which have any reason to buy a public certificate.\n\nThe same applies to a brand-new certificate: legitimate services rotate certificates on a schedule, and short-lived certificates are now standard practice, not a red flag on their own. Treat these fields as reasons to look closer, never as verdicts by themselves."
      },
      {
        "heading": "Worked Example: Hashing a File, Checking a Verdict, and Reading a Certificate",
        "content": "Reading the theory is one thing; the two moves you will make in almost every shift are hashing a file to check it against intelligence, and reading a certificate off the wire. Walk through both with real command output.\n\n### Step 1 — hash the file locally\n\nOn a Windows analysis workstation:\n\n```\nC:\\> certutil -hashfile suspicious_invoice.exe SHA256\nSHA256 hash of suspicious_invoice.exe:\ne99a18c428cb38d5f260853678922e03e99a18c428cb38d5f260853678922e0a\nCertUtil: -hashfile command completed successfully.\n```\n\nOn Linux or macOS the equivalent is `sha256sum suspicious_invoice.exe`, which prints the same 64-character digest in a shorter line. Either way, you now hold a fingerprint you can share safely — the analyst on the next shift, or a threat-intel API, never needs the actual file.\n\n### Step 2 — check the digest against a verdict\n\nYou paste that same digest into a threat-intel lookup and get back a summary:\n\n```\nGET /api/v3/files/e99a18c428cb38d5f260853678922e03e99a18c428cb38d5f260853678922e0a\n{\"sha256\": \"e99a18c428cb38d5f260853678922e03e99a18c428cb38d5f260853678922e0a\", \"last_analysis_stats\": {\"malicious\": 41, \"undetected\": 7}, \"popular_threat_classification\": \"trojan.infostealer\", \"first_submission_date\": \"2026-06-02\"}\n```\n\nForty-one engines flagging the *exact same* digest is about as close to certain as detection gets, because a hash match, unlike almost every other signal you will use, is essentially never a false positive. Note what this check did **not** need: you never had to detonate the file, read its code, or trust anyone's opinion about it. You matched a fingerprint.\n\n### Step 3 — read the certificate on the connection\n\nThat same host made an outbound HTTPS connection worth checking. Pulling the certificate (with `openssl s_client -connect host:443 -showcerts`, or straight from a proxy log) shows:\n\n```\nSubject: CN=login-secure-portal.net\nIssuer: CN=login-secure-portal.net\nNot Before: Aug 15 03:11:02 2026 GMT\nNot After: Nov 13 03:11:02 2026 GMT\nSerial Number: 01\n```\n\nRead it field by field, exactly as the previous section taught: **Subject equals Issuer**, so this certificate is self-signed — nobody independent vouched for it. It was issued `Not Before` a matter of hours before the connection happened. Neither fact alone proves malice — internal tools self-sign certificates every day — but a self-signed, hours-old certificate on an *external, unfamiliar* destination, from a host that just ran a file forty-one engines call an infostealer, is a pattern, not a coincidence. That is the whole method: hash to identify, look up to get a verdict, read the certificate fields to add context — three cheap checks that together turn one alert into a confident call."
      },
      {
        "heading": "Where This Shows Up in the Logs You Will Actually Read",
        "content": "Everything above earns its place because it surfaces in specific fields you will encounter in your first weeks. This section connects each idea to where you will literally see it.\n\n### File hashes in endpoint telemetry\n\nEvery serious EDR product records hashes for the files it sees executing. You will use them to check a file against threat intelligence, to find every other machine that ran the same binary, and to confirm that two alerts on different hosts involve the identical sample. This is the single most common use of cryptography in your daily work, and it is also where the Pyramid of Pain caveat applies.\n\n### Signatures in email authentication\n\n**DKIM** attaches a digital signature to outgoing mail, created with the sending domain's private key. The receiving server fetches the matching public key from DNS and verifies it. A valid DKIM signature proves the message really came from that domain and was not altered in transit.\n\nWhat it does not prove is that the message is safe. A compromised legitimate account sends perfectly DKIM-valid phishing, and an attacker who registers a lookalike domain can sign their own mail correctly. DKIM answers \"did this domain really send it,\" not \"should you trust it.\"\n\n### Encryption types in Kerberos events\n\nKerberos tickets are encrypted, and the ticket records which algorithm was used. In Windows event 4769 you will see an encryption type field, and two values matter:\n\n- **0x17 (23)** — RC4, the older and weaker option. Tickets using it are far easier to crack offline.\n- **0x12 (18)** — AES-256, the modern default in a healthy environment.\n\nAttackers performing **Kerberoasting** deliberately request RC4 tickets precisely because they crack faster, which makes an unexpected RC4 request a genuine hunting signal. The necessary caution: legacy systems and older service accounts still request RC4 for entirely innocent reasons, so this is a lead to investigate rather than a detection to alert on blindly.\n\n### Hashes as credentials\n\nWindows stores password hashes rather than passwords, which is correct design. The uncomfortable consequence is that for NTLM authentication, the hash itself is sufficient to authenticate — an attacker who steals it never needs to crack it. This is **pass-the-hash**, and it is why a stolen hash must be treated with exactly the same urgency as a stolen password.\n\n### Encryption used offensively\n\nFinally, remember that attackers use the same tools you do. Ransomware encrypts victim files with strong, correctly implemented cryptography — which is exactly why recovery without the key is not realistic and why backups, not decryption, are the answer. Command-and-control channels hide inside TLS for the same reason your bank does. The mathematics is neutral; only the intent differs.",
        "codeExample": "// The same cryptographic ideas, as they appear in four real log sources.\n\n// 1) EDR — file identity on process execution\nprocess.name=svchost.exe process.hash.sha256=e3b0c44298fc1c14...b855 \\\n  process.hash.md5=d41d8cd98f00b204e9800998ecf8427e host.name=FIN-WKS-07\n\n// 2) EMAIL — signature verification result\nsender.domain=contoso.com dkim=pass spf=pass dmarc=pass\n// -> proves the domain really sent it. Says NOTHING about intent.\n\n// 3) KERBEROS — Windows Event ID 4769, service ticket request\nEventID=4769 ServiceName=MSSQLSvc/db01.corp.local TicketEncryptionType=0x17\n// -> 0x17 = RC4. Crackable offline. Classic Kerberoasting signal (T1558.003),\n//    but legacy service accounts request it legitimately too.\n\n// 4) PROXY / TLS — certificate seen on an outbound connection\ntls.server.x509.issuer=\"CN=internal-ca\" tls.server.x509.subject=\"CN=internal-ca\" \\\n  tls.server.not_before=2026-08-12 ja3.hash=a0e9f5d64349fb13191bc781f81f42e1\n// -> issuer == subject means SELF-SIGNED. Suspicious on an external\n//    destination; completely normal on an internal printer or appliance."
      },
      {
        "heading": "Fuzzy Hashing and Imphash: Catching the Family, Not Just One File",
        "content": "You already know the great weakness of an exact hash: change a single byte and the SHA-256 becomes a completely different string, so a hash blocklist catches exactly one file and misses every trivially-modified variant. Attackers exploit this constantly — recompiling, re-packing, or adding a meaningless byte costs them seconds and defeats your blocklist. The answer is a smarter kind of hashing, and knowing it turns \"I caught one sample\" into \"I caught the whole malware family.\"\n\nThe techniques all trade *exactness* for *similarity*:\n\n- **Fuzzy hashing (ssdeep, and similar like TLSH).** Instead of collapsing the whole file into one all-or-nothing digest, a fuzzy hash is built so that files which are *mostly* the same produce *mostly* the same hash. Two variants of one malware — say 97% identical — yield ssdeep hashes that a comparison scores as ~97% similar. That similarity score is what lets you cluster v1, v2, and v3 of a family together even though their exact hashes share nothing.\n- **Import hashing (imphash).** For Windows executables, imphash is a hash of the file's **import table** — the list of external functions it calls. Malware built from the same source or toolkit tends to import the same functions in the same order, so a shared imphash groups samples by *how they were built and what they do*, cutting across cosmetic changes.\n- **Section and other structural hashes** work on the same idea: hash a *part* or a *property* that stays stable across variants, not the whole byte stream.\n\nThe practical payoff for an analyst is a shift in what you pivot on. When you find one malicious file, the exact hash answers \"have I seen *this exact file* before?\" — useful but brittle. The **fuzzy hash and imphash** answer the far more powerful question: \"have I seen *anything like this* before, anywhere in my environment or in threat intel?\" That is how one sample on one host expands into finding the other five infected machines running slightly-different builds of the same tool.\n\nThis is also the concrete resolution of the Pyramid of Pain idea that runs through the malware material: exact hashes sit at the very bottom because they cost the adversary nothing to change, but the *similarity* of a family — and even more so its *behaviour* — is far harder to alter. Similarity hashing is where you start climbing that pyramid: you stop chasing individual disposable files and start recognising the family behind them.",
        "image": {
          "src": "/lesson-images/crypto/fuzzy-hashing.svg",
          "alt": "A diagram of exact hashing versus fuzzy hashing and imphash. Malware v1 has SHA-256 a1b2c3, and malware v2 with a single byte changed has SHA-256 f9e8d7 — a totally different string. An exact-hash blocklist knows a1b2c3 is bad but f9e8d7 is a completely different string so it is not matched, and v2 walks right past; you caught only one file, and the attacker flips a byte in seconds by recompiling, re-packing, or adding a comment. Similarity hashing clusters the family instead: ssdeep of v1 is 768:Abc...Xyz and of v2 is 768:Abc...Xyw, which score a 97% match, because fuzzy hashes stay similar when the file changes a little, so v1 and v2 cluster as one family; and imphash hashes the import table, so the same imports produce the same hash and cluster by behaviour. The analyst lesson: an exact hash proves two files are identical, great for confirming a known-bad file but useless the moment the attacker changes one byte (which is why hashes are the bottom of the Pyramid of Pain), while fuzzy hashes measure similarity and imphash groups by imported functions, so you hunt the whole malware family, not one disposable sample.",
          "caption": "An exact hash catches one file and misses every variant; fuzzy hashing (ssdeep) and imphash measure similarity, so you cluster and hunt the whole malware family.",
          "credit": "Figure authored for this course."
        }
      },
      {
        "heading": "Reading a Certificate as an Analyst: The Tells in the Fields",
        "content": "You have seen that certificates are how strangers decide to trust each other on the internet — a certificate binds an identity (a domain) to a public key, vouched for by a Certificate Authority. As an analyst you rarely design that trust; instead you *read* certificates as evidence, because the fields inside a certificate, and the way encrypted traffic uses them, carry some of the best signals available when the payload itself is hidden by encryption.\n\nA handful of certificate fields become investigative tells:\n\n- **Issuer and self-signed certs.** A legitimate public service presents a certificate signed by a trusted CA. A **self-signed** certificate (issuer equals subject — it vouches for itself) is normal for some internal systems but deeply suspicious on outbound traffic to the internet: it is a classic sign of a home-grown C2 server, because the attacker did not bother to (or could not) obtain a real CA-signed cert.\n- **Subject / SAN mismatch.** The certificate names the host it is *for*. When traffic is going to `api.trusted-cdn.com` but the certificate's subject is some unrelated or random string — or the name simply does not match where the connection claims to go — that mismatch is a red flag worth pulling.\n- **Validity window.** Very **short-lived** certificates, or ones **freshly issued** moments before the traffic, can indicate throwaway attacker infrastructure; a certificate that is **expired** yet still in active use is a data-quality or misconfiguration signal at best and an anomaly worth checking.\n- **Rarity.** A certificate (or its issuer) seen for the first time in your environment, or on only one host, is worth more attention than one your whole company uses daily — the same rarity logic that runs through C2 detection.\n\nThe reason this matters is the reality you met in the C2 lesson: **most malicious traffic today is encrypted, so you cannot read its contents** — but the TLS handshake that sets up the encryption is *not* itself encrypted, and the certificate is exchanged in the clear. So when the payload is a black box, the certificate is one of the few things you *can* still inspect. A self-signed cert on a beacon to a strange destination, a subject that does not match the domain, a certificate minted an hour ago — these are exactly the tells that let you flag encrypted C2 you could never decrypt.\n\nThe habit to carry away: when traffic is encrypted, **do not give up — pivot to the metadata the encryption cannot hide.** The certificate, its issuer, its validity, and its rarity are readable evidence sitting right on top of traffic whose body you will never see."
      },
      {
        "heading": "Code Signing: Why 'Signed' Means Trust — and How Attackers Steal It",
        "content": "There is one more place certificates decide trust, and it is one you meet every day as an analyst without always naming it: **code signing.** A digital signature on a program is a certificate-backed promise that says \"this software genuinely came from this publisher and has not been altered since.\" Operating systems and security tools lean on that promise heavily — which is exactly why attackers work so hard to abuse it.\n\nThe mechanics are the certificate ideas you already know, applied to executables. A software publisher uses a **code-signing certificate** (issued by a CA to a verified organisation) to sign their programs; your computer verifies that signature against the publisher's certificate. On Windows this is **Authenticode**, and a valid signature is why you see \"verified publisher: Microsoft Corporation\" instead of a scary \"unknown publisher\" warning. A valid signature genuinely does two useful things: it **authenticates the origin** (it really is from that publisher) and it **guarantees integrity** (the file has not been tampered with since signing).\n\nBut — and this is the crucial analyst lesson — **a signature vouches for the file it is on, and nothing more.** Attackers get around it in ways you must recognise:\n\n- **Stolen or leaked signing certificates.** If an attacker steals a real company's private code-signing key (a prized target in supply-chain attacks), they can sign their own malware so it appears to come from a trusted publisher. The most damaging intrusions in recent memory have ridden on stolen signing certs.\n- **Abusing signed-but-vulnerable tools.** A perfectly legitimate, correctly-signed program can still be *used* maliciously — the living-off-the-land binaries and the DLL-sideloading trick from the Windows material, where a signed executable is led into loading the attacker's unsigned DLL.\n- **Fraudulently-obtained or expired certs.** Attackers sometimes acquire a code-signing certificate under a shell company, or abuse one whose trust should have lapsed.\n\nSo the discipline is precise and worth stating exactly: **treat a valid signature as one useful data point, not a clean bill of health.** \"It's signed\" answers \"is this specific file genuine and untampered?\" — it does *not* answer \"is this file's *behaviour* safe?\", \"did the signing key get stolen?\", or \"what did this signed program go on to load and run?\". This is the same principle as the DLL-sideloading lesson, stated from the certificate's side: the trust a signature confers stops at the file's edge, and everything that file *does* is still yours to judge."
      },
      {
        "heading": "How Two Strangers Agree on a Secret in Public: Diffie-Hellman and Forward Secrecy",
        "content": "Everything the lesson has covered about asymmetric cryptography assumed a public/private key pair already existed. It did not explain how two parties who have never met -- your browser and a server you have never visited before -- manage to agree on a *shared secret* over a connection that anyone can eavesdrop on, without ever transmitting that secret itself. That problem has a specific, elegant answer: **Diffie-Hellman (DH) key exchange**, published in 1976 and still the mechanism underneath nearly every HTTPS connection made today.\n\n### The paint-mixing analogy\n\nPicture two people, far apart, who want to agree on a shared paint color that an eavesdropper watching every step cannot reproduce. They agree publicly on a common starting paint (this is public -- anyone can see it). Each of them then privately mixes in a secret color of their own that they never reveal, and sends the *resulting mixture* -- not their secret color -- to the other person. Each person then mixes their own secret color into the mixture they just received. Because paint-mixing is, for the sake of the analogy, commutative, they now both hold the identical final mixture, even though a watcher who saw the public starting color and both intermediate mixtures cannot separate out either person's private ingredient to reproduce it. That is Diffie-Hellman: a shared secret assembled from two private contributions, where reversing the public exchanges back into either private value is computationally infeasible.\n\n### What it actually protects: forward secrecy\n\nThe reason this matters operationally, beyond the mechanics, is a property called **Perfect Forward Secrecy (PFS)**. In older TLS versions, the client could encrypt its part of the key exchange directly with the server's long-term RSA public key. That is efficient, but it has a devastating long-term weakness: if the server's private RSA key is ever stolen -- years later, in an unrelated breach -- an attacker who recorded the encrypted traffic at the time can now decrypt every single session ever protected by that key, retroactively.\n\n**Ephemeral Diffie-Hellman** (commonly written DHE, or ECDHE for the elliptic-curve variant) closes that hole. Instead of a long-term key doing double duty as both an identity proof and an encryption mechanism, each session generates a brand-new, temporary DH key pair used only for that one session and then discarded. The server's long-term certificate is still used, but only to *sign* the ephemeral exchange -- proving it really came from that server -- never to encrypt it directly. Stealing the server's long-term private key years later gives an attacker nothing useful against a past session, because that session's actual encryption key never depended on it and no longer exists anywhere.\n\nThis is exactly why **TLS 1.3 removed static RSA key exchange entirely and made ephemeral Diffie-Hellman, in its elliptic-curve form, mandatory** for every handshake -- forward secrecy stopped being an optional configuration choice and became a protocol requirement.\n\n### Where you will actually see this\n\nA TLS handshake capture or a proxy log records the **negotiated group** used for the key exchange -- commonly an elliptic curve like x25519 or secp256r1 in modern TLS 1.3 traffic. Seeing a modern named curve in the negotiated cipher suite is a quick, positive signal that a connection used forward secrecy; seeing an older static RSA key exchange listed, only possible on legacy TLS 1.2 connections still permitting it, is a signal that connection is not forward-secret, and worth flagging in any environment being hardened.",
        "codeExample": "// Diffie-Hellman, worked with small numbers (real DH uses vastly larger primes)\n// PUBLIC, agreed by both sides openly: prime p = 23, base g = 5\n//\n// Alice picks a PRIVATE secret a = 6      Bob picks a PRIVATE secret b = 15\n// Alice computes A = g^a mod p            Bob computes B = g^b mod p\n//   A = 5^6 mod 23 = 8                       B = 5^15 mod 23 = 19\n// Alice sends A=8 to Bob (public)          Bob sends B=19 to Alice (public)\n//\n// Alice computes the shared secret:        Bob computes the shared secret:\n//   s = B^a mod p = 19^6 mod 23 = 2           s = A^b mod p = 8^15 mod 23 = 2\n// -> BOTH arrive at s = 2, without either private value (a=6, b=15)\n//    ever crossing the network. An eavesdropper who saw p, g, A, and B\n//    cannot feasibly recover a, b, or s -- this is the discrete\n//    logarithm problem, and it becomes computationally infeasible fast\n//    once p is a 2048+ bit prime instead of 23.\n\n// A TLS 1.3 handshake, showing the ephemeral key exchange in the clear:\nClientHello  key_share: x25519 (client's ephemeral public value)\nServerHello  key_share: x25519 (server's ephemeral public value)\n             signed by the server's LONG-TERM certificate, proving origin\n// -> the actual traffic key is derived from the EPHEMERAL exchange, not\n//    the certificate's key. Steal the certificate's private key next\n//    year and this session's traffic is still unreadable: forward secrecy."
      },
      {
        "heading": "Passwords Need Slow Hashes: Salting and Deliberately Expensive Functions",
        "content": "Every hash property covered earlier in this lesson -- deterministic, one-way, fast -- is exactly right for identifying a file and exactly wrong for storing a password, and the reason is worth understanding precisely rather than taking on faith.\n\n### Why a fast hash is a liability here\n\nSHA-256 was engineered to be fast, because file integrity checking and digital signatures need to process large amounts of data quickly. Speed is the whole point for those use cases. For a password, speed is the vulnerability: modern GPUs and dedicated cracking hardware can compute enormous numbers of SHA-256 hashes per second. If a database of SHA-256(password) values leaks, an attacker with modest hardware can simply hash every password in a common wordlist and compare it against the stolen digests, and for any password that is not genuinely random, this finishes in a practical amount of time.\n\n### Salting: defeating the precomputed shortcut\n\nA **salt** is a random value, unique per password, stored alongside the hash and mixed into the input before hashing (hash(password + salt)). Without a salt, an attacker can precompute a **rainbow table** -- a massive lookup of common passwords to their hash -- once, and reuse it against any leaked database that used the same unsalted hash function, including targets they have never seen before. A unique salt per password defeats this entirely, because the attacker would need a separate precomputed table for every single salt value, which erases the entire economic advantage of precomputing anything.\n\nSalts are not secret -- they are stored right next to the hash, openly. Their job is not to hide anything; it is to make every password's hash unique even when two users happen to choose the identical password, and to make bulk precomputation worthless. A related, genuinely secret value called a **pepper** is sometimes added on top -- a single value shared across the whole application, stored separately from the database rather than alongside the hash -- but modern password-hashing algorithms handle salting internally and correctly, which is precisely why manually engineering your own salting or peppering scheme is now considered a mistake rather than a best practice: it is very easy to get the details wrong in a way that looks fine and is not.\n\n### Deliberately slow, deliberately expensive\n\nSalting alone still is not enough, because a fast hash function lets an attacker try enormous numbers of guesses per second per salt. The real fix is a hash function engineered to be **slow and resource-intensive on purpose**, so that checking one password takes a deliberately inconvenient fraction of a second for a legitimate login, and that same cost, multiplied across billions of guesses, becomes prohibitively expensive for an attacker.\n\n- **bcrypt** -- the oldest of the three still in wide use, tunable through a **cost factor** that doubles the work required for each increment. It is CPU-hard but not memory-hard, which leaves it somewhat more exposed to attackers using custom hardware (GPUs, ASICs) that has abundant parallel compute but was not specifically designed to be starved of memory.\n- **scrypt** -- adds **memory-hardness**: cracking it requires not just CPU time but a large amount of RAM per guess, which is expensive to parallelize on GPUs and ASICs the way a pure CPU-bound function is not.\n- **Argon2, specifically Argon2id** -- the winner of the 2015 Password Hashing Competition, now standardized in RFC 9106, and the option NIST and OWASP currently recommend by default for new systems. It is tunable across three independent dimensions -- memory cost, time cost, and parallelism -- giving the most control over the CPU/memory/time trade-off of the three.\n\nNIST SP 800-63B, the federal digital identity guideline that governs password storage practices, requires that stored passwords be protected with a salted, purpose-built one-way key derivation function, and its latest revision calls specifically for a memory-hard password-hashing scheme. OWASP, in turn, recommends Argon2id by default for new systems, and -- for environments constrained to FIPS-validated algorithms, where Argon2 is not yet an option -- PBKDF2 (the function specified in NIST SP 800-132) run with a deliberately high iteration count, on the order of 600,000 for HMAC-SHA-256. Note the split cleanly: NIST SP 800-132 defines PBKDF2 itself but does not mandate that 600,000 figure -- the specific iteration count is OWASP's current recommendation, meant to be raised over time as hardware gets faster.\n\n### Reading a real password hash in a breach investigation\n\nThe stored hash string itself tells you the algorithm and its parameters, which matters enormously when assessing how exposed a leaked credential database actually is. An analyst reviewing a breach notification who sees a bcrypt or Argon2id prefix on the leaked hashes is looking at a properly slow-hashed, salted database -- a real but bounded exposure, since cracking still costs the attacker real time per guess. Seeing plain 32-character hex strings (bare MD5) or 64-character hex strings with no accompanying salt field in the same breach is a very different, far worse finding: those crack at enormous speed with commodity hardware, and every non-random password in that database should be treated as already compromised.",
        "codeExample": "// Password hash string formats -- what each field tells an investigator\n$2b$12$N9qo8uLOickgx2ZMRZoMyeIjZAgcfl7p92ldGxad68LJZdL17lhWy\n |  |  |______________________|________________________|\n |  |      22-char salt (base64)     31-char hash output\n |  cost factor: 12 (means 2^12 = 4096 rounds)\n algorithm id: bcrypt, variant 2b\n\n$argon2id$v=19$m=65536,t=3,p=4$c29tZXNhbHQ$RdescudvJCsgt3ub+b+dWRWJTmaaJObG\n |________|         |________________|\n algorithm+version    memory=64MB, time=3 passes, parallelism=4 threads\n\n// Relative crackability on commodity hardware, fastest to slowest to guess:\nUnsalted MD5 / SHA-256   -- fast, general-purpose hash, never designed\n                            to resist brute force; effectively broken\n                            the moment it leaks unsalted\nbcrypt (high cost)       -- CPU-hard by design; the cost factor makes\n                            each guess deliberately slow, but it is\n                            not memory-hard\nArgon2id (tuned)         -- memory-hard AND CPU-hard; the hardest of\n                            the three to accelerate with GPUs/ASICs,\n                            since parallel attackers run out of memory\n                            bandwidth before they run out of compute"
      },
      {
        "heading": "When a Certificate Goes Bad: Revocation via CRL and OCSP",
        "content": "A certificate's validity dates, checked earlier in this lesson, tell you when it is *scheduled* to stop being trusted. They say nothing about whether it needs to stop being trusted *early* -- because the private key behind it was stolen, because the certificate authority discovers it issued the certificate in error, or because the organization it belonged to no longer controls that domain. **Revocation** is the mechanism for exactly that: telling relying parties a certificate is no longer trustworthy before its printed expiry date arrives, and it turns out to be one of the genuinely unsolved-well problems in the entire certificate system.\n\n### Certificate Revocation Lists (CRL)\n\nThe original mechanism is simple: the Certificate Authority (CA) periodically publishes a **CRL**, a signed list of every certificate serial number it has revoked. A client checking a certificate downloads the relevant CRL and looks for the certificate's serial number on it.\n\nThe weakness is scale and freshness. A CA that has issued millions of certificates can end up publishing a CRL that is large and slow to download, and, because CRLs are published on a schedule rather than instantly, a certificate revoked five minutes ago may not appear on the CRL for hours, leaving a real window where a client checking revocation still sees the certificate as valid.\n\n### OCSP: asking in real time\n\n**OCSP (Online Certificate Status Protocol)** answers a narrower, faster question: instead of downloading an entire list, a client sends the CA's **OCSP responder** the specific certificate's identifier and gets back a small, immediate answer -- good, revoked, or unknown -- reflecting the CA's records as soon as a revocation is processed, without waiting for a publication cycle.\n\nThis solves the freshness and size problems, but introduces a new one: **every OCSP check tells the CA which site you are about to visit, at what time, from what IP address.** A CA that operates OCSP responders for a large share of the web's certificates is, structurally, in a position to build a browsing history for anyone whose client performs live OCSP checks -- a genuine privacy leak that has nothing to do with the security question OCSP was built to answer.\n\n### OCSP stapling: getting both\n\n**OCSP stapling** resolves both problems at once by moving who does the asking. Instead of the client querying the CA directly, the *web server* periodically queries the CA's OCSP responder itself, caches the signed response, and staples that response directly into the TLS handshake it sends to every visitor. The client gets the exact same freshness guarantee -- a recently-signed, CA-attested revocation status -- without ever contacting the CA directly, closing the privacy leak and eliminating an extra network round-trip.\n\n### The uncomfortable reality: soft-fail\n\nThe mechanism that actually undermines all of this in practice is a design decision most browsers still make: if a revocation check cannot be completed -- the OCSP responder is slow, unreachable, or blocked by a firewall along the path -- most browsers **soft-fail**, meaning they proceed and treat the certificate as valid rather than blocking the connection. This exists because a hard-fail policy, blocking every connection when revocation status is unknown, would break far too much legitimate traffic whenever a responder has any outage. The consequence is that an attacker in a position to actively block a victim's OCSP traffic can often make a revoked certificate appear valid simply by preventing the revocation check from completing at all -- turning the entire revocation system into a check that fails open rather than closed.\n\n### Where the industry is actually heading\n\nGiven soft-fail's weakness, an increasingly common alternative is simply issuing **short-lived certificates** -- some now valid for only a matter of days -- on the reasoning that a certificate which expires quickly on its own needs revocation checking far less, since the exposure window closes naturally regardless of whether any revocation check ever runs.",
        "codeExample": "// Checking OCSP status directly against a CA's responder\n$ openssl ocsp -issuer intermediate-ca.pem -cert server-cert.pem \\\n    -url http://ocsp.digicert.com -CAfile chain.pem\n\nResponse verify OK\nserver-cert.pem: good\n    This Update: Aug 20 04:00:00 2026 GMT\n    Next Update:  Aug 21 04:00:00 2026 GMT\n\n// CRL vs OCSP vs OCSP stapling, side by side\nMechanism         Freshness               Response size   Privacy\n-----------------  ----------------------  --------------  --------------------\nCRL                Stale between            Can be large    No per-check leak\n                   publication cycles        (whole list)    (downloaded once)\nOCSP (direct)      Real-time                Small           CA sees every\n                                                             site you visit\nOCSP stapling      Real-time (server-       Small,          No leak -- CA only\n                   refreshed)                pre-fetched     talks to the server\n\n// The failure mode that matters most operationally: most browsers\n// SOFT-FAIL when a revocation check cannot complete -- they proceed\n// and trust the cert rather than blocking. An attacker able to block\n// OCSP traffic can often make a stolen, already-revoked certificate\n// look valid simply by preventing the check from finishing."
      },
      {
        "heading": "Proving Origin With a Shared Secret: HMAC",
        "content": "Every signature discussed earlier in this lesson relies on asymmetric cryptography -- a private key only the signer holds, verified by a public key anyone can have. There is a second, entirely different way to prove a message is authentic and untampered, one that shows up constantly in API integrations and automated systems an analyst investigates: **HMAC, the Hash-based Message Authentication Code.**\n\n### The gap a plain hash cannot close\n\nA plain hash, of the kind covered at the start of this lesson, proves integrity to anyone who can recompute it -- which is precisely the problem. If a message and its SHA-256 hash both travel across the network together, an attacker who intercepts and modifies the message can simply recompute a new, matching hash for their modified version and nobody is any the wiser, because computing a hash requires no secret at all.\n\n### Mixing in a shared secret\n\n**HMAC** fixes this by mixing a **shared secret key** into the hashing process itself, using a specific, well-analyzed construction, with HMAC-SHA256 being the most common variant, combining a secret key with the SHA-256 hash function. The output, the HMAC, can only be produced or verified by someone who holds that same shared secret key. An attacker who intercepts a message and its HMAC but does not know the key cannot forge a new, valid HMAC for a modified message, because doing so requires the secret they do not have.\n\nThis gives HMAC two of the properties covered separately elsewhere in this lesson -- integrity (the message was not altered) and authenticity (it came from someone who holds the shared secret) -- using only fast, symmetric hashing rather than the comparatively expensive asymmetric operations a digital signature requires.\n\n### HMAC versus a digital signature\n\nThe two solve a similar-sounding problem with a structurally different trust model, and the difference matters for who can verify what:\n\n- **HMAC** -- symmetric. Both sides must already share the same secret key in advance. Anyone holding that key can both *produce* and *verify* a valid HMAC, which means HMAC cannot prove which of the two key-holders actually sent a given message -- it only proves the sender was someone who knows the shared secret.\n- **Digital signature** -- asymmetric. Only the private key holder can *produce* a valid signature, but anyone with the corresponding public key can *verify* one. This is what lets a certificate authority's signature be checked by every browser on earth without any of them sharing a secret with the CA.\n\nThe practical rule of thumb: HMAC fits a closed relationship where both parties already trust each other and share a provisioned secret, such as a specific API integration between two systems. Digital signatures fit an open relationship where the verifier population is unbounded and unknown in advance, such as every browser that will ever visit a website.\n\n### Where an analyst actually meets this\n\n**Webhook signature verification** is the most common real-world encounter. When one service notifies another of an event -- a payment provider notifying a merchant's server that a charge succeeded, a code-hosting platform notifying a CI system that code was pushed -- the sending service computes an HMAC over the exact request body using a secret both sides provisioned in advance, and sends that HMAC in a request header. The receiving server recomputes the same HMAC locally over the body it actually received and compares it to the header value; if they match, the request is authentic and untampered, and if they do not, the request is rejected outright, since anyone without the shared secret cannot produce a matching value.\n\nThis exact mechanism also underlies **API request signing** schemes like AWS Signature Version 4, and the HS256 algorithm option inside a **JWT (JSON Web Token)** -- the token's signature section is an HMAC computed over the token's header and payload, verifiable only by holders of the shared signing secret.\n\n### The investigative angle\n\nA webhook endpoint receiving requests with an invalid or missing HMAC signature is either misconfigured or under attack -- someone attempting to forge events without knowing the shared secret. Confirming which requires checking whether the signature verification failure correlates with a source outside the expected sender's known infrastructure, exactly the kind of check that turns 'the signature failed' from a shrug into a real finding.",
        "codeExample": "// A real-world pattern: verifying an HMAC-signed webhook (GitHub-style)\n// The SENDER computes this once, using a secret BOTH sides provisioned:\nsignature = HMAC-SHA256(key=shared_secret, message=raw_request_body)\n// ... and sends it in a header:\nX-Hub-Signature-256: sha256=5e9f...c2a1\n\n// The RECEIVER recomputes the same HMAC over the body it actually got,\n// and compares -- in constant time, to avoid leaking timing information:\ncomputed = HMAC-SHA256(key=shared_secret, message=received_raw_body)\nif not constant_time_compare(computed, header_signature):\n    reject_request()   // body was altered, or the sender did not have\n                        // the shared secret -- either way, do not trust it\n\n// HMAC vs digital signature -- who can do what\n                 Produce a valid value       Verify a valid value\n---------------  ---------------------------  ---------------------------\nHMAC             Anyone with the shared        Anyone with the shared\n                 secret key                     secret key (same key)\nDigital          Only the PRIVATE key           Anyone with the PUBLIC key\nsignature        holder                          (different key, no secret shared)"
      }
    ],
    "keyTakeaways": [
      "Hashing is one-way and identifies data without containing it: the same input always gives the same fixed-length digest, and changing one bit changes the digest completely, so hashes can only ever be compared for exact equality.",
      "A file hash is the weakest indicator to hunt on because the attacker controls the file — recompiling changes the hash while the behaviour stays identical, which is why hashes confirm known samples but never find new ones.",
      "Encryption is two-way by design and its security lives in the key, never in a secret algorithm; base64 is encoding rather than encryption, so an unreadable blob in a command line should be decoded and read, not treated as protected.",
      "A certificate binds a domain name to a public key with a CA's signature, so the padlock proves the connection is encrypted and the name matches — it proves nothing about the honesty of whoever runs the site, which is why phishing sites have padlocks too.",
      "These ideas surface as concrete fields you will read daily: SHA-256 hashes in EDR telemetry, DKIM results in mail headers, Kerberos encryption type 0x17 versus 0x12 in event 4769, and self-signed certificate issuers in proxy logs — each a lead to investigate rather than a verdict on its own.",
      "Exact hashes (SHA-256) catch one file and miss every variant (one byte changes the whole digest — the bottom of the Pyramid of Pain); fuzzy hashing (ssdeep) scores SIMILARITY and imphash groups Windows executables by their import table, so you cluster and hunt the whole malware family instead of one disposable sample.",
      "When traffic is encrypted you cannot read the payload, but the TLS certificate is exchanged in the clear — read it as evidence: self-signed certs on outbound traffic, subject/SAN mismatch, very short-lived or freshly-minted certs, and rarity are C2 tells; and a code signature (Authenticode) vouches only for a file's origin and integrity, NOT its behaviour — stolen signing keys, abused signed tools (LOLBins/DLL sideloading), and fraudulent certs mean 'signed' is one data point, not a clean bill of health.",
      "Diffie-Hellman lets two parties agree on a shared secret over a public channel without ever transmitting it; ephemeral Diffie-Hellman (DHE/ECDHE) gives Perfect Forward Secrecy by using a fresh, temporary key pair per session, which is why TLS 1.3 made it mandatory and dropped static RSA key exchange entirely.",
      "Passwords must be hashed with deliberately slow, salted functions -- bcrypt, scrypt, or Argon2id -- never with a fast general-purpose hash like SHA-256; salting defeats precomputed rainbow tables, and the algorithm's own prefix in a leaked hash tells an investigator how exposed a breach actually leaves the passwords.",
      "Certificate revocation has two competing mechanisms -- CRL, a downloadable list that is stale between publications, and OCSP, a real-time query that leaks browsing history to the CA unless the server staples the response itself -- and most browsers soft-fail when a check cannot complete, which is the practical weakness attackers can exploit.",
      "HMAC proves both integrity and authenticity using a shared secret key rather than a key pair, fitting closed relationships like webhook signing and JWT HS256 tokens, whereas a digital signature fits open relationships where anyone can verify with a public key but only the private key holder can produce a valid signature."
    ],
    "quiz": [
      {
        "question": "A developer tells you the application stores user passwords encrypted, and mentions that this lets the help desk recover a password when someone forgets theirs. Why should this concern you as a security analyst?",
        "options": [
          {
            "label": "Nothing is wrong here, since being able to recover a forgotten password is the normal reason to store credentials in an encrypted form",
            "value": "a"
          },
          {
            "label": "Passwords should be hashed rather than encrypted, because encryption is reversible by anyone who obtains the key, including an attacker",
            "value": "b"
          },
          {
            "label": "Encryption is acceptable for passwords, but only if the algorithm itself is kept secret from everyone outside the security team",
            "value": "c"
          },
          {
            "label": "The concern is purely about performance, because encrypting and decrypting passwords is far slower than storing them directly",
            "value": "d"
          }
        ],
        "answer": "b",
        "explanation": "Hashing is one-way, so a stolen password database yields digests an attacker still has to crack. Encryption is reversible by design, which means the recoverability the developer describes is exactly the weakness: whoever obtains the key obtains every password in plaintext. Option a mistakes a convenience feature for a safe design. Option c repeats the discredited idea that secrecy of the algorithm provides security, when in fact all strong algorithms are public and the key is the only secret. Option d misidentifies a fundamental security flaw as a performance concern."
      },
      {
        "question": "Your team blocks a malware sample by its SHA-256 hash after an incident. The same threat actor targets you again the following week with functionally identical malware, and the hash block does not fire. What is the most likely explanation?",
        "options": [
          {
            "label": "The attacker recompiled the malware, which produces a completely different hash even though the behaviour is unchanged",
            "value": "a"
          },
          {
            "label": "SHA-256 digests expire after a set period, so the blocklist entry needs to be regenerated by the team each week",
            "value": "b"
          },
          {
            "label": "Hash-based blocking only applies to files below a certain size, so the newer sample was simply too large to match",
            "value": "c"
          },
          {
            "label": "The attacker reversed the SHA-256 digest back into the original file and edited it specifically to evade the block",
            "value": "d"
          }
        ],
        "answer": "a",
        "explanation": "Because of the avalanche property, any change to the file — even adding a comment or padding bytes — produces an entirely different digest while leaving the behaviour intact. This is why hashes sit at the bottom of the Pyramid of Pain: they cost the adversary almost nothing to change. Option b invents an expiry that hashes do not have. Option c invents a size limit that does not exist. Option d describes reversing a hash, which is precisely what the one-way property prevents."
      },
      {
        "question": "A user forwards you a link and asks whether the site is safe, pointing out that the browser shows a padlock and reports a valid certificate. What does that certificate actually establish?",
        "options": [
          {
            "label": "That the connection is encrypted and the certificate matches the domain being visited, but nothing about whether the operator is honest",
            "value": "a"
          },
          {
            "label": "That an independent authority reviewed the content of the site and confirmed the business behind it is legitimate and reputable",
            "value": "b"
          },
          {
            "label": "That the site is incapable of hosting malware, because certificate authorities scan every page they issue certificates for",
            "value": "c"
          },
          {
            "label": "That the domain has existed for a substantial period and has therefore built up an established reputation online",
            "value": "d"
          }
        ],
        "answer": "a",
        "explanation": "The browser checks name match, validity dates, chain to a trusted root, and revocation status — all properties of the connection and the binding, not of the operator's intentions. Domain-validated certificates are free and automatic, so a phishing domain registered minutes ago has a perfectly valid one. Options b and c both credit certificate authorities with content or business vetting they do not perform for standard certificates. Option d is wrong because certificate validity is unrelated to domain age or reputation."
      },
      {
        "question": "During triage you find a PowerShell command line containing a long base64 string passed to the -enc <base64 blob>. What is the correct conclusion and next step?",
        "options": [
          {
            "label": "It is encoded rather than encrypted, so no key is involved and you should decode it immediately to read the actual command",
            "value": "a"
          },
          {
            "label": "It is encrypted with a key held only by the attacker, so the contents cannot realistically be recovered during triage",
            "value": "b"
          },
          {
            "label": "It is a hash of the original command, which means the original text can never be reconstructed from what you are seeing",
            "value": "c"
          },
          {
            "label": "It is a digital signature demonstrating that the command originated from a publisher the system already trusts",
            "value": "d"
          }
        ],
        "answer": "a",
        "explanation": "Base64 is a transport encoding with no key, so it is trivially reversible and decoding it is a standard triage step that usually reveals the attacker's actual command. Attackers use it to obscure intent from casual inspection and from naive string matching, not to protect it. Option b confuses encoding with encryption. Option c confuses encoding with hashing, and would wrongly stop the investigation. Option d describes a signature, which serves to prove origin rather than to obscure content."
      },
      {
        "question": "You identify a malicious executable on one host and add its SHA-256 to your blocklist. A week later five more hosts are found running the same malware family — but each is a slightly different build, and none matched your blocklisted hash. What technique would have caught the family, and why does the exact hash fail?",
        "options": [
          {
            "label": "Nothing could have caught them, because once malware exists in multiple builds it is fundamentally impossible to detect any relationship between the variants using any hashing method at all",
            "value": "a"
          },
          {
            "label": "Fuzzy hashing (ssdeep) and imphash: they measure similarity or hash stable properties like the import table, so near-identical variants still cluster as one family — whereas an exact SHA-256 changes completely if a single byte differs, catching only that one file",
            "value": "b"
          },
          {
            "label": "A larger exact-hash blocklist would have worked, since simply collecting enough individual SHA-256 hashes eventually covers every possible future build an attacker could ever compile",
            "value": "c"
          },
          {
            "label": "The problem was only that SHA-256 is a weak algorithm, and switching the blocklist to SHA-512 would have automatically matched all the slightly-different variants as the same file",
            "value": "d"
          }
        ],
        "answer": "b",
        "explanation": "An exact cryptographic hash is all-or-nothing: one changed byte yields a completely different digest, so a blocklist catches exactly one file. Fuzzy hashing (ssdeep) scores similarity so near-identical variants cluster, and imphash groups Windows executables by their import table, catching builds that share how they were made and what they do. Option a is defeatist and wrong — that is precisely what similarity hashing solves. Option c cannot scale to unbuilt future variants. Option d misunderstands the issue: a stronger exact hash is still exact, so it fails the same way."
      },
      {
        "question": "An analyst sees a signed executable — Windows shows a valid Authenticode signature from a real, verified publisher — and concludes 'it's signed by a legitimate company, so it's safe; nothing to investigate.' Why is this reasoning flawed?",
        "options": [
          {
            "label": "It is perfectly sound: a valid Authenticode signature from a verified publisher is a complete guarantee that the file and everything it does are safe, so no further investigation is ever warranted for signed files",
            "value": "a"
          },
          {
            "label": "A signature only vouches for that file's origin and integrity, not its behaviour or safety: signing keys get stolen (supply-chain attacks), signed legitimate tools get abused (LOLBins, DLL sideloading), and certs are fraudulently obtained — so a signature is one data point, not a clean bill of health",
            "value": "b"
          },
          {
            "label": "The flaw is only that the analyst should have checked whether the signature used SHA-1 versus SHA-256, and provided the hashing algorithm inside the signature is modern the 'it's safe' conclusion is entirely correct",
            "value": "c"
          },
          {
            "label": "Signed files are actually more dangerous than unsigned ones in every case, so the correct rule is to treat any presence of a digital signature as definitive proof that the file is malware",
            "value": "d"
          }
        ],
        "answer": "b",
        "explanation": "A code signature authenticates origin and guarantees the file was not tampered with after signing — but that trust stops at the file's edge. Attackers ride stolen signing keys to sign malware as a trusted publisher, abuse signed-but-legitimate tools (LOLBins, DLL sideloading), and obtain certs fraudulently, so a valid signature never certifies behaviour. Option a is the exact over-trust the question warns against. Option c fixates on the hash algorithm while ignoring stolen keys and abused-tool cases. Option d overcorrects into treating all signed files as malware, which is equally wrong."
      },
      {
        "question": "You run certutil -hashfile on a downloaded installer and paste the resulting SHA-256 into a threat-intel lookup: 41 of 48 engines flag that exact digest as trojan.infostealer. A colleague argues this proves nothing because 'a hash is just a number.' How should you weigh the result?",
        "options": [
          {
            "label": "Act on it with high confidence: a hash match against a known-malicious digest is about as close to certain as detection gets, because — unlike almost every other signal — an exact hash match is essentially never a false positive",
            "value": "a"
          },
          {
            "label": "Dismiss it entirely, because antivirus engines are known to disagree constantly, so a match on the same exact SHA-256 digest across 41 independent engines carries no more weight than a single vendor's opinion",
            "value": "b"
          },
          {
            "label": "Treat it as inconclusive, because SHA-256 has known collisions in practice, so two unrelated files could easily share this digest by coincidence",
            "value": "c"
          },
          {
            "label": "Ignore the verdict and focus only on the installer's file size, since file size is a far more reliable indicator of malicious intent than any hash comparison",
            "value": "d"
          }
        ],
        "answer": "a",
        "explanation": "A hash match is an exact-equality test with no room for near-misses, so 41 engines independently flagging that precise digest is strong, actionable evidence — you never needed to open, run, or reverse the file to reach that confidence. Option b wrongly treats 41 engines matching one exact digest as equivalent to a single opinion. Option c is factually wrong: SHA-256 has no practical collisions, unlike broken MD5/SHA-1. Option d substitutes an irrelevant, easily-forged property (file size) for the far stronger cryptographic identifier."
      },
      {
        "question": "A company learns that an attacker stole its web server's long-term TLS private key today. Two years of past encrypted traffic to that server were captured and stored by an unrelated third party during that time. Under TLS 1.3, what does the theft of the private key allow the attacker to do to that stored two-year traffic archive?",
        "options": [
          {
            "label": "Decrypt all of it immediately, since under this protocol version the server's long-term private key was always used directly to encrypt every single session's traffic in the clear",
            "value": "a"
          },
          {
            "label": "Decrypt none of it, because TLS 1.3 mandates ephemeral Diffie-Hellman, so each past session's actual traffic key was never derivable from the long-term private key and no longer exists anywhere",
            "value": "b"
          },
          {
            "label": "Decrypt only sessions from the specific users who happened to reuse the exact same account password across multiple separate visits to the affected site",
            "value": "c"
          },
          {
            "label": "Decrypt all of it, but only after also separately obtaining a copy of the unrelated SHA-256 file hash that happened to be logged during those particular sessions",
            "value": "d"
          }
        ],
        "answer": "b",
        "explanation": "TLS 1.3 makes ephemeral Diffie-Hellman mandatory: the long-term certificate only signs each session's ephemeral exchange, it never encrypts the session directly. That means each past session's actual traffic key was derived from a temporary value that existed only for that session and is now gone, so stealing the long-term key today grants no access to the two-year archive. Option a describes the exact static-key weakness TLS 1.3 was designed to eliminate. Option c invents an irrelevant password-reuse condition. Option d confuses an unrelated concept, file hashing, with session key derivation."
      },
      {
        "question": "A payment processor and a merchant's server both hold a pre-shared secret used to authenticate webhook notifications between them. A separate certificate authority's root certificate is trusted by every browser on the internet without any of those browsers ever sharing a secret with the CA. What explains why the payment processor uses HMAC for its webhooks while the CA uses a digital signature for its certificates?",
        "options": [
          {
            "label": "HMAC is simply a faster version of a digital signature, and the two are otherwise fully interchangeable for any use case",
            "value": "a"
          },
          {
            "label": "HMAC fits a closed relationship where both parties already share a provisioned secret, while a digital signature fits an open relationship where the verifier population is unbounded and unknown in advance, like every browser on earth",
            "value": "b"
          },
          {
            "label": "Digital signatures require both parties to share a secret key in advance, which is why they would be unsuitable for browsers that have never contacted the CA directly",
            "value": "c"
          },
          {
            "label": "HMAC cannot be used over the internet at all, so the payment processor's webhook must instead rely on a private VPN tunnel between the two systems",
            "value": "d"
          }
        ],
        "answer": "b",
        "explanation": "The payment processor and merchant are a closed, known pair who can safely provision a shared secret in advance, which is exactly what HMAC requires. A CA's certificate must be verifiable by an unbounded, unknown population of browsers that can never share a secret with the CA in advance, which is exactly what a public/private key pair and a digital signature enable. Option a wrongly claims full interchangeability, ignoring the different trust models. Option c inverts the actual requirement -- digital signatures need no pre-shared secret at all. Option d is false; HMAC is used over the internet constantly, including in this exact webhook scenario."
      }
    ],
    "createdAt": "2026-08-13T00:00:00.000Z",
    "researchUsed": false
  }
];
export default lessons;
